# @git-stacks/web

The browser-only git-stacks client. It renders the path- and secret-minimized web projection and connects to service-owned terminals. Built assets and their manifest are consumed by `@git-stacks/service`.
