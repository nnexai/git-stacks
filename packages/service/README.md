# @git-stacks/service

The Node.js interactive authority for git-stacks. It owns revisioned projections, typed operations, events, signals, filesystem reconciliation, loopback authentication, browser routing, and service-lifetime PTYs. Trusted local clients use the constrained `@git-stacks/service/client` export for discovery, authenticated requests, events, and operations without importing server construction APIs.
