# @git-stacks/cli

The Node.js 24 command-line application. Ordinary commands execute locally through `@git-stacks/core`; only explicit service exposure, pairing/target administration, signal publication, web, and dashboard commands cross the encrypted interactive-service boundary.
