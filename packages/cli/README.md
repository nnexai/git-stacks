# @git-stacks/cli

The Node.js 24 command-line application. Ordinary commands execute locally through `@git-stacks/core`; only explicit service, web, and dashboard launch commands cross the interactive-service boundary.
