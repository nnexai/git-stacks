import { describe, test } from "bun:test"

describe("doctor --fix", () => {
  test.todo("lists all issues then asks confirmation for fixable ones", () => {})
  test.todo("--fix --force skips confirmation prompt", () => {})
  test.todo("continues past individual fix failures", () => {})
  test.todo("reports N fixed, M failed summary at end", () => {})
  test.todo("issues without fix show (no auto-fix) annotation", () => {})
  test.todo("--json + --fix emits JSON with fix results", () => {})
})
