import { describe, test, expect } from "bun:test"

describe("status --json", () => {
  test.todo("emits JSON array of workspace objects")
  test.todo("each workspace includes repos array with per-repo detail")
  test.todo("per-repo objects include task_path field")
  test.todo("no human-readable text mixed with JSON output")
})
