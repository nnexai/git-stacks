# Note: 2026-03-18

Add an `install --skills` command (or similar) to git-stacks that sets up an agentskills.io-based skill for using the tool. This would allow users to quickly bootstrap Claude Code skills for git-stacks workflows directly from the CLI.
