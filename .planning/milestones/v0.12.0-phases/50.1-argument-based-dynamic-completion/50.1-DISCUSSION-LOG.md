# Phase 50.1: Argument-Based Dynamic Completion - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-01
**Phase:** 50.1-argument-based-dynamic-completion
**Areas discussed:** Argument-name inference, New completion types, Multi-position completion, Migration strategy

---

## Argument-name inference

| Option | Description | Selected |
|--------|-------------|----------|
| Convention-based | Arguments named `<workspace>`, `[template]`, `<repo>` auto-map to completion types. DYNAMIC_COMPLETIONS becomes override for edge cases. | ✓ |
| Explicit annotation | Add `.completionType()` method or metadata to Commander arguments. More explicit but requires touching every command. | |
| Keep manual map | Just add missing Phase 50 entries. No inference — keep explicit approach. | |

**User's choice:** Convention-based (Recommended)
**Notes:** User selected the preview showing `buildNode()` inference from `registeredArguments` with DYNAMIC_COMPLETIONS as override.

---

## New completion types

| Option | Description | Selected |
|--------|-------------|----------|
| Integration IDs | Complete integration names from registered list | ✓ |
| Shells only | Keep existing 4 types, no new types | |
| Enum from Commander .choices() | Extract `.choices()` / `.argChoices()` as completion values | ✓ |

**User's choice:** Integration IDs + Enum from Commander .choices() (multi-select)
**Notes:** Both selected — integration IDs as a new DynamicCompletion type, and Commander .choices() as a general mechanism.

---

## Multi-position completion

| Option | Description | Selected |
|--------|-------------|----------|
| Per-argument array | Change `dynamic` from single value to array of per-argument completions. buildNode() reads ALL registeredArguments. | ✓ |
| First-arg only | Keep completing only first positional argument. | |
| You decide | Claude picks best approach. | |

**User's choice:** Per-argument array (Recommended)
**Notes:** User selected preview showing `argCompletions: { name, type, required }[]` replacing `dynamic: DynamicCompletion | null`.

---

## Migration strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Override layer | Convention inference runs first, DYNAMIC_COMPLETIONS overrides for edge cases. Most entries deleted. | ✓ |
| Full replacement | Delete DYNAMIC_COMPLETIONS entirely. Edge cases handled by renaming arguments. | |
| Parallel period | Keep both systems active. Inference fills gaps, map stays complete. | |

**User's choice:** Override layer (Recommended)
**Notes:** User selected preview showing priority order: explicit override → convention → .choices() → null. Map shrinks from ~40 to ~2-3 entries.

---

## Claude's Discretion

- Whether to rename `DynamicCompletion` type or keep existing name
- Exact convention matching logic
- Integration ID completion generation in shell scripts
- Test approach for completion validation

## Deferred Ideas

None — discussion stayed within phase scope.
