---
phase: 50.1-argument-based-dynamic-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/workspace.ts
  - src/commands/repo.ts
  - src/commands/template.ts
autonomous: true
requirements:
  - D-01
must_haves:
  truths:
    - "All workspace commands use <workspace> or [workspace] as argument name (not <name>)"
    - "All repo subcommands use <repo> as argument name (not <name>)"
    - "All template subcommands use <template> as argument name (not <name>)"
    - "Rename source args use <workspace>/<repo>/<template> for the entity being renamed, <new-name> for the target"
    - "clone command uses [workspace] instead of [source]"
    - "All CLI behavior is identical -- only help text changes"
  artifacts:
    - path: "src/commands/workspace.ts"
      provides: "Workspace commands with convention-matching arg names"
      contains: "<workspace>"
    - path: "src/commands/repo.ts"
      provides: "Repo commands with convention-matching arg names"
      contains: "<repo>"
    - path: "src/commands/template.ts"
      provides: "Template commands with convention-matching arg names"
      contains: "<template>"
  key_links: []
---

<objective>
Rename Commander.js argument names in all command definition files so that positional arguments match the naming convention used by the convention-based completion inference (D-01).

Purpose: Convention inference maps argument names to completion types via exact match (workspace -> workspace, repo -> repo, template -> template). Current commands use generic `<name>` which would produce no match. This plan performs the prerequisite renames.

Output: Three updated command files with convention-matching argument names. No behavior changes -- Commander.js parses positional args by position, not by name. Only help text changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50.1-argument-based-dynamic-completion/50.1-CONTEXT.md
@.planning/phases/50.1-argument-based-dynamic-completion/50.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename workspace command arguments</name>
  <files>src/commands/workspace.ts</files>
  <read_first>src/commands/workspace.ts</read_first>
  <action>
Rename Commander.js `.command()` argument names in workspace.ts. These are purely help-text changes -- Commander parses by position, not name. The action handler parameter names may also change for clarity but functionality is identical.

Exact renames (line references from current source):

| Line | Current | After |
|------|---------|-------|
| 116 | `.command("clone [source]")` | `.command("clone [workspace]")` |
| 123 | `.command("edit <name>")` | `.command("edit <workspace>")` |
| 140 | `.command("open <name>")` | `.command("open <workspace>")` |
| 243 | `.command("close <name>")` | `.command("close <workspace>")` |
| 307 | `.command("status [name]")` | `.command("status [workspace]")` |
| 354 | `.command("clean [name]")` | `.command("clean [workspace]")` |
| 489 | `.command("remove <name>")` | `.command("remove <workspace>")` |
| 519 | `.command("cd <name> [repo]")` | `.command("cd <workspace> [repo]")` |
| 543 | `.command("merge <name>")` | `.command("merge <workspace>")` |
| 573 | `.command("run <name> [repo]")` | `.command("run <workspace> [repo]")` |
| 724 | `.command("rename <old> <new>")` | `.command("rename <workspace> <new-name>")` |
| 753 | `.command("sync [name]")` | `.command("sync [workspace]")` |
| 919 | `.command("pull [name]")` | `.command("pull [workspace]")` |

Do NOT rename:
- `.command("new [name]")` -- `new` creates a new workspace, the name is freeform input, not an existing workspace. `name` correctly produces no convention match.
- `.command("paths [workspace]")` -- already uses `[workspace]`, no change needed.
- `.command("list")` -- no positional args.

For the `rename` command, the action handler currently receives `(old, newName, ...)`. After the rename to `<workspace> <new-name>`, update the handler parameter names to `(workspace, newName, ...)` for clarity. The positional parsing is unchanged.

For `clone`, the action handler currently receives `(source?, ...)`. Update to `(workspace?, ...)`.

For all other commands, the handler parameter name `name` can optionally be updated to `workspace` for readability, but this is cosmetic.

Per D-01: Convention inference uses exact match on `arg.name()`. After these renames, `workspace` maps to completion type `"workspace"`, `repo` maps to `"repo"`.
  </action>
  <verify>
    <automated>cd /home/nnex/dev/prj/git-stacks && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"clone \[workspace\]"' src/commands/workspace.ts` returns 1
    - `grep -c '"open <workspace>"' src/commands/workspace.ts` returns 1
    - `grep -c '"close <workspace>"' src/commands/workspace.ts` returns 1
    - `grep -c '"edit <workspace>"' src/commands/workspace.ts` returns 1
    - `grep -c '"status \[workspace\]"' src/commands/workspace.ts` returns 1
    - `grep -c '"clean \[workspace\]"' src/commands/workspace.ts` returns 1
    - `grep -c '"remove <workspace>"' src/commands/workspace.ts` returns 1
    - `grep -c '"cd <workspace> \[repo\]"' src/commands/workspace.ts` returns 1
    - `grep -c '"merge <workspace>"' src/commands/workspace.ts` returns 1
    - `grep -c '"run <workspace> \[repo\]"' src/commands/workspace.ts` returns 1
    - `grep -c '"rename <workspace> <new-name>"' src/commands/workspace.ts` returns 1
    - `grep -c '"sync \[workspace\]"' src/commands/workspace.ts` returns 1
    - `grep -c '"pull \[workspace\]"' src/commands/workspace.ts` returns 1
    - `grep -c '"new \[name\]"' src/commands/workspace.ts` returns 1 (NOT renamed)
    - `grep -c '"paths \[workspace\]"' src/commands/workspace.ts` returns 1 (unchanged)
    - `bun run typecheck` exits 0
  </acceptance_criteria>
  <done>All workspace commands use <workspace>/[workspace] except `new [name]` and `paths [workspace]` (already correct). TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Rename repo and template command arguments</name>
  <files>src/commands/repo.ts, src/commands/template.ts</files>
  <read_first>src/commands/repo.ts, src/commands/template.ts</read_first>
  <action>
Rename Commander.js `.command()` argument names in repo.ts and template.ts.

**repo.ts renames:**

| Line | Current | After |
|------|---------|-------|
| 123 | `.command("show <name>")` | `.command("show <repo>")` |
| 140 | `.command("remove <name>")` | `.command("remove <repo>")` |
| 168 | `.command("rename <old> <new>")` | `.command("rename <repo> <new-name>")` |

Do NOT rename:
- `.command("add <path>")` -- `path` is a filesystem path, not a repo name. No convention match needed.
- `.command("scan <dir>")` -- `dir` is a directory path. No convention match needed.
- `.command("list")` -- no positional args.

For the `rename` command handler, update parameter name from `old` to `repo` for clarity.

**template.ts renames:**

| Line | Current | After |
|------|---------|-------|
| 38 | `.command("show <name>")` | `.command("show <template>")` |
| 75 | `.command("edit <name>")` | `.command("edit <template>")` |
| 92 | `.command("clone <name> <new-name>")` | `.command("clone <template> <new-name>")` |
| 110 | `.command("rename <old> <new>")` | `.command("rename <template> <new-name>")` |
| 127 | `.command("remove <name>")` | `.command("remove <template>")` |

Do NOT rename:
- `.command("new [name]")` -- `name` is freeform input for a new template name. No convention match needed.
- `.command("list")` -- no positional args.

For the `rename` command handler, update parameter name from `old` to `template` for clarity.
For the `clone` command handler, update parameter name from `name` to `template` for clarity.

Per D-01: After renames, `repo` maps to completion type `"repo"`, `template` maps to `"template"`.
  </action>
  <verify>
    <automated>cd /home/nnex/dev/prj/git-stacks && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"show <repo>"' src/commands/repo.ts` returns 1
    - `grep -c '"remove <repo>"' src/commands/repo.ts` returns 1
    - `grep -c '"rename <repo> <new-name>"' src/commands/repo.ts` returns 1
    - `grep -c '"show <template>"' src/commands/template.ts` returns 1
    - `grep -c '"edit <template>"' src/commands/template.ts` returns 1
    - `grep -c '"clone <template> <new-name>"' src/commands/template.ts` returns 1
    - `grep -c '"rename <template> <new-name>"' src/commands/template.ts` returns 1
    - `grep -c '"remove <template>"' src/commands/template.ts` returns 1
    - `grep -c '"add <path>"' src/commands/repo.ts` returns 1 (NOT renamed)
    - `grep -c '"new \[name\]"' src/commands/template.ts` returns 1 (NOT renamed)
    - `bun run typecheck` exits 0
  </acceptance_criteria>
  <done>All repo subcommands use <repo>, all template subcommands use <template> except new/list/add/scan. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `bun run typecheck` passes (no type errors from parameter renames)
- `bun run src/index.ts --help` shows updated argument names in help text
- `bun run src/index.ts open --help` shows `Usage: git-stacks open <workspace>` (not `<name>`)
- `bun run src/index.ts repo show --help` shows `Usage: git-stacks repo show <repo>` (not `<name>`)
- `bun run src/index.ts template edit --help` shows `Usage: git-stacks template edit <template>` (not `<name>`)
</verification>

<success_criteria>
All command argument names match the convention map keys (workspace, repo, template) where completion is desired. Commands with freeform input (new, add, scan) retain generic names. TypeScript compiles. No functional behavior changes.
</success_criteria>

<output>
After completion, create `.planning/phases/50.1-argument-based-dynamic-completion/50.1-01-SUMMARY.md`
</output>
