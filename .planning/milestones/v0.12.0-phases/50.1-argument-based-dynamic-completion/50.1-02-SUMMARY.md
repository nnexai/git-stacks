---
phase: 50.1-argument-based-dynamic-completion
plan: 02
subsystem: cli
tags: [completion, commander, convention-inference, shell-completion]

# Dependency graph
requires:
  - "50.1-01: Convention-matching arg names in workspace/repo/template commands"
provides:
  - "Convention inference replaces DYNAMIC_COMPLETIONS for all 46+ commands except 4 issue.link overrides"
  - "Integration ID completion in bash/zsh/fish"
  - "Multi-arg position dispatch for run/cd commands"
  - "argChoices extraction for .choices() Commander arguments"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "ArgCompletion interface: name + type + required + choices per argument position"
    - "Priority chain: DYNAMIC_COMPLETIONS override → convention NAME_TO_COMPLETION_TYPE → argChoices → skip"
    - "Convention map: workspace/template/repo/shell/integration keys map to DynamicCompletion types"
    - "Multi-arg dispatch: argCompletions.length > 1 emits position-aware (COMP_CWORD/count(commandline -opc)/CURRENT) branches"

key-files:
  created: []
  modified:
    - src/lib/completion-generator.ts
    - tests/lib/completion-generator.test.ts

key-decisions:
  - "ArgCompletion array replaces .dynamic + .firstArgRequired fields — atomic interface migration, no shim"
  - "NAME_TO_COMPLETION_TYPE convention map: workspace/template/repo/shell/integration keys only"
  - "DYNAMIC_COMPLETIONS shrunk from 50 entries to 4 (issue.link overrides with workspace-or-issue arg name)"
  - "Integration helper emitted as static hardcoded list (not filesystem scan) — IDs stable at generation time"
  - "Multi-arg fish uses commandline -opc count for position dispatch (not seen_subcommand_from per position)"
  - "Multi-arg commands excluded from simple workspace for loop in fish — get individual position-aware directives"
  - "choices type generates compgen -W (bash), inline values (zsh), -a flag (fish)"

# Metrics
duration: 8min
completed: 2026-04-01
---

# Phase 50.1 Plan 02: Refactor Completion Generator Summary

**Convention inference replaces 46-entry DYNAMIC_COMPLETIONS with a 5-key NAME_TO_COMPLETION_TYPE map, adds integration/choices types, and supports multi-arg position dispatch across bash/zsh/fish**

## Performance

- **Duration:** 8 min
- **Started:** 2026-04-01T20:52:12Z
- **Completed:** 2026-04-01T21:00:10Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Replaced `CommandNode.dynamic` + `CommandNode.firstArgRequired` with `argCompletions: ArgCompletion[]`
- Rewrote `buildNode()` to iterate all `cmd.registeredArguments` with 4-priority chain
- Added `ArgCompletion` interface: `{ name, type, required, choices? }`
- Added `integration` and `choices` `DynamicCompletion` types
- Shrunk `DYNAMIC_COMPLETIONS` from 50 entries to 4 (issue.link overrides only)
- Added `NAME_TO_COMPLETION_TYPE` convention map with 5 keys
- Updated all 3 shell generators (bash/zsh/fish) to use `argCompletions` array
- Added `_${id}_integrations()` helper to zsh (hardcoded IDs from `integrations` array)
- Added `__${id}_integrations` helper to fish (space-split string)
- Added multi-arg position dispatch: bash uses `COMP_CWORD -eq N`, zsh uses `_arguments` with multiple specs, fish uses `count (commandline -opc) -eq N`
- Updated test program: renamed all entity arg names to convention-matching names (workspace/repo/template)
- Added `run <workspace> [repo]`, `cd <workspace> [repo]`, `format` (.choices) commands to test program
- Added 30+ new test cases covering D-01 through D-06 requirements
- All 107 completion tests pass; full suite (unit + 37 integration tests) passes

## Task Commits

Each task was committed atomically:

1. **Task 1: Atomic interface migration — types, buildNode, and all three shell generators** - `2ac80ca` (feat)
2. **Task 2: Add convention inference, integration, multi-arg, choices, and override tests** - `a6322ac` (test)

## Files Created/Modified

- `src/lib/completion-generator.ts` - Full rewrite: ArgCompletion interface, NAME_TO_COMPLETION_TYPE convention map, 4-entry DYNAMIC_COMPLETIONS, updated buildNode() + all shell generators + integration helpers
- `tests/lib/completion-generator.test.ts` - Updated test program with convention-matching arg names; added multi-arg/format commands; added D-01 through D-06 test blocks

## Decisions Made

- Commander.js `registeredArguments` iterated in full (not just [0]) to support multi-arg completions
- `integration` type hardcodes IDs at completion-script generation time (not runtime filesystem scan)
- `choices` type from `.argChoices` handled inline in all shells — no separate helper function needed
- Fish multi-arg uses `count (commandline -opc)` position detection, not seen_subcommand_from chains
- Test for "no completion for `<name>` arg" checks absence of `create)` case branch rather than absence of "workspaces" string (FLAG_COMPLETIONS preamble always emits --workspace handler)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed test assertion for `<name>` arg producing no completion**
- **Found during:** Task 2
- **Issue:** Test checking `not.toContain("workspaces")` failed because bash generator always emits `--workspace` FLAG_COMPLETIONS handler in preamble (regardless of command args)
- **Fix:** Changed assertion to check `not.toContain("    create)")` — verifies no case branch is emitted (correct behavior)
- **Files modified:** tests/lib/completion-generator.test.ts
- **Commit:** a6322ac

**2. [Rule 1 - Bug] Fixed D-03 integration test to use a program with `<integration>` arg**
- **Found during:** Task 2
- **Issue:** Test checked for "vscode" in the test program's bash output, but the test program has no command with `<integration>` arg — integration IDs only appear when such a command exists
- **Fix:** Changed test to use a fresh minimal `Command` with `enable <integration>` instead of `buildTestProgram()`
- **Files modified:** tests/lib/completion-generator.test.ts
- **Commit:** a6322ac

## Known Stubs

None — all generated output is wired to real data (integrations list, convention map, Commander registeredArguments).

## Self-Check: PASSED

- src/lib/completion-generator.ts: FOUND
- tests/lib/completion-generator.test.ts: FOUND
- Commit 2ac80ca: FOUND
- Commit a6322ac: FOUND
