---
phase: 107.3
status: complete
---

# Phase 107.3 Plan

1. Compose secure loopback pairing and browser-safe workspace projections over the existing local service.
2. Build a bounded service-owned PTY manager with multiple tabs, resize, replay, visibility streaming, and deterministic cleanup.
3. Implement the workspace-first browser interface with creation, commands, Git state, labels, priority, context actions, signals, and terminal focus behavior.
4. Harden reconnect, stale-lock recovery, signal lifecycle, resource usage, and package delivery.
5. Verify focused service behavior, browser behavior, full tests, typechecks, dependency boundaries, build output, and release gates.
