---
phase: 89
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/config.ts
  - src/lib/composition.ts
  - tests/lib/config.test.ts
  - tests/lib/composition.test.ts
autonomous: true
requirements:
  - FSYNC-01
must_haves:
  truths:
    - "D-01: files.sync uses object-only entries with source, target, and optional git_exclude fields."
    - "D-02: Phase 89 does not add name, direction, delete, replace, merge, skip, or conflict-policy fields."
    - "D-03: Template composition appends sync entries in include order."
    - "D-04: files.sync is allowed anywhere current file ops are allowed: Template.files, Workspace.files, and per-repo files."
    - "D-05: Duplicate target policy is not a schema concern."
  artifacts:
    - path: "src/lib/config.ts"
      provides: "FileSyncEntrySchema and shared FilesSchema sync support"
      contains: "sync"
    - path: "src/lib/composition.ts"
      provides: "include-order sync merge behavior"
      contains: "mergeFiles"
    - path: "tests/lib/config.test.ts"
      provides: "schema parse/reject coverage for files.sync"
      contains: "files.sync"
    - path: "tests/lib/composition.test.ts"
      provides: "composition coverage for sync arrays"
      contains: "sync"
---

<objective>
Extend the declarative config model so users can define `files.sync` entries in templates, workspaces, and per-repo workspace config without changing existing `files.copy` or `files.symlink` behavior.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add schema coverage for sync entries before implementation</name>
  <read_first>
    - tests/lib/config.test.ts
    - src/lib/config.ts
    - .planning/phases/89-files-sync-schema-and-materialization/89-CONTEXT.md
  </read_first>
  <files>tests/lib/config.test.ts</files>
  <action>Add tests proving `TemplateSchema`, `WorkspaceSchema`, and `WorkspaceRepoSchema` parse `files: { sync: [{ source: ".planning", target: ".planning", git_exclude: true }] }`. Add rejection tests proving `files.sync: [".planning"]`, missing `source`, missing `target`, and non-boolean `git_exclude` throw Zod validation errors. Keep existing copy/symlink tests unchanged.</action>
  <acceptance_criteria>
    - `tests/lib/config.test.ts` contains a test where `TemplateSchema.parse(...).files?.sync?.[0].source` equals `.planning`.
    - `tests/lib/config.test.ts` contains a test where `WorkspaceSchema.parse(...).files?.sync?.[0].target` equals `.codex`.
    - `tests/lib/config.test.ts` contains a test where `WorkspaceRepoSchema.parse(...).files?.sync?.[0].git_exclude` equals `true`.
    - `tests/lib/config.test.ts` contains rejection assertions for string sync entries, missing `source`, missing `target`, and non-boolean `git_exclude`.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/lib/config.test.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add composition coverage for include-order sync concatenation</name>
  <read_first>
    - tests/lib/composition.test.ts
    - src/lib/composition.ts
    - src/lib/config.ts
    - .planning/phases/89-files-sync-schema-and-materialization/89-CONTEXT.md
  </read_first>
  <files>tests/lib/composition.test.ts</files>
  <action>Add a test in the existing `files merging` describe block that writes two templates with `files.sync` arrays and asserts `composeTemplates(["files-a", "files-b"]).files?.sync` is exactly `[{ source: "a-src", target: "a-target" }, { source: "b-src", target: "b-target", git_exclude: true }]`. Also assert existing `copy` and `symlink` arrays still concatenate in the same result.</action>
  <acceptance_criteria>
    - `tests/lib/composition.test.ts` contains an assertion that sync entries from `files-a` precede sync entries from `files-b`.
    - The same test asserts `result.files?.copy` and `result.files?.symlink` retain their existing include-order behavior.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/lib/composition.test.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Implement FileSyncEntrySchema and sync composition</name>
  <read_first>
    - src/lib/config.ts
    - src/lib/composition.ts
    - tests/lib/config.test.ts
    - tests/lib/composition.test.ts
  </read_first>
  <files>src/lib/config.ts, src/lib/composition.ts</files>
  <action>In `src/lib/config.ts`, export `FileSyncEntrySchema` with `source: z.string()`, `target: z.string()`, and `git_exclude: z.boolean().optional()`, then add `sync: z.array(FileSyncEntrySchema).optional()` to `FilesSchema`. In `src/lib/composition.ts`, update `mergeFiles()` to collect `tpl.files?.sync` entries into a `sync` array, set `hasAny` when sync entries are present, and include `sync` in the returned files object only when non-empty. Do not add any fields beyond `source`, `target`, and `git_exclude`.</action>
  <acceptance_criteria>
    - `src/lib/config.ts` exports `FileSyncEntrySchema`.
    - `FilesSchema` contains `sync: z.array(FileSyncEntrySchema).optional()`.
    - `src/lib/composition.ts` `mergeFiles()` returns a `sync` key only when at least one template contributes sync entries.
    - Production imports in `src/` remain relative.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/lib/config.test.ts tests/lib/composition.test.ts && bun run typecheck</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML config -> typed runtime config | User-controlled template/workspace YAML enters Zod schemas. |
| Template includes -> composed workspace template | Multiple template files combine into one effective file operation list. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01 | Tampering | `FilesSchema` | mitigate | Reject malformed `files.sync` entries through object-only Zod schema before materialization code can run. |
| T-89-02 | Repudiation | Template composition | mitigate | Preserve deterministic include-order concatenation so later execution can trace sync entries back to template order. |
</threat_model>

<verification>
Required checks:
- `bun test tests/lib/config.test.ts tests/lib/composition.test.ts`
- `bun run typecheck`
</verification>

<success_criteria>
- FSYNC-01 is represented in the shared config schema.
- `files.copy` and `files.symlink` remain backward-compatible.
- `files.sync` entries compose additively in include order.
- No Phase 90 policy fields are added.
</success_criteria>

