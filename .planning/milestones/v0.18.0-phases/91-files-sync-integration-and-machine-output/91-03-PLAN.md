---
phase: 91
plan: 03
type: execute
wave: 2
depends_on:
  - 90-03
  - 91-02
files_modified:
  - src/commands/files.ts
  - src/lib/completion-generator.ts
  - tests/commands/files.test.ts
  - tests/lib/completion-generator.test.ts
  - README.md
autonomous: true
requirements:
  - FSYNC-09
  - DOCS-01
requirements_addressed:
  - FSYNC-09
  - DOCS-01
must_haves:
  truths:
    - "D-09: Do not build dashboard UI in Phase 91."
    - "D-11: Add shell completion and help coverage for git-stacks files status|pull|push and common flags."
    - "D-13: README primary use case is private files that need to appear as real workspace files: dotfiles, specs, and agent configuration such as skills and hooks."
    - "D-14: Concrete examples may include .planning and .codex."
    - "D-15: Explain symlink versus sync with security/practicality framing."
    - "D-16: README warns that default pull/push behavior is conservative and --force mirrors and can delete destination-only files."
    - "D-17: Do not include JSON examples in README now."
  artifacts:
    - path: "src/lib/completion-generator.ts"
      provides: "files command completion coverage"
      contains: "files"
    - path: "README.md"
      provides: "real-file sync examples and safety guidance"
      contains: "files.sync"
---

<objective>
Expose `files status|pull|push` help/completion coverage and document the real-file sync workflow without adding dashboard UI or full README JSON examples.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add help and completion tests</name>
  <read_first>
    - tests/lib/completion-generator.test.ts
    - tests/commands/files.test.ts
    - src/lib/completion-generator.ts
    - src/commands/files.ts
    - src/index.ts
    - .planning/phases/91-files-sync-integration-and-machine-output/91-CONTEXT.md
  </read_first>
  <files>tests/lib/completion-generator.test.ts, tests/commands/files.test.ts</files>
  <action>Add completion tests proving generated completion output includes the `files` command group and subcommands `status`, `pull`, and `push`. Assert common flags appear in the appropriate command contexts: `--json` for all three subcommands, `--verbose` for status, and `--dry-run` plus `--force` for pull and push. Add command help tests or extend existing command tests to run `files --help`, `files status --help`, `files pull --help`, and `files push --help`, asserting the same subcommands and flags are discoverable.</action>
  <acceptance_criteria>
    - `tests/lib/completion-generator.test.ts` asserts completion output includes `files`, `status`, `pull`, and `push`.
    - Completion tests assert `--json`, `--verbose`, `--dry-run`, and `--force` are present where applicable.
    - `tests/commands/files.test.ts` or an equivalent command test asserts `files --help` includes `status`, `pull`, and `push`.
    - Help tests assert `files status --help` includes `--json` and `--verbose`.
    - Help tests assert `files pull --help` and `files push --help` include `--json`, `--dry-run`, and `--force`.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/lib/completion-generator.test.ts tests/commands/files.test.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Implement completion and help surface</name>
  <read_first>
    - src/lib/completion-generator.ts
    - src/commands/files.ts
    - src/index.ts
    - tests/lib/completion-generator.test.ts
    - tests/commands/files.test.ts
    - .planning/phases/91-files-sync-integration-and-machine-output/91-PATTERNS.md
  </read_first>
  <files>src/lib/completion-generator.ts, src/commands/files.ts</files>
  <action>Update the completion generator or command metadata so shell completion discovers `git-stacks files status|pull|push` and their flags. Ensure `--json` is offered for status, pull, and push; `--verbose` only for status; and `--dry-run` plus `--force` for pull and push. Keep command help concise and consistent with the current Commander patterns. Do not add dashboard action menu entries, TUI tabs, or TUI status widgets.</action>
  <acceptance_criteria>
    - Generated completion contains `files status`, `files pull`, and `files push` entries or equivalent nested command completion.
    - `--json` completion is available for all files subcommands.
    - `--verbose` completion is scoped to `files status`.
    - `--dry-run` and `--force` completion is scoped to `files pull` and `files push`.
    - No file under `src/tui/` is modified by this plan.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/lib/completion-generator.test.ts tests/commands/files.test.ts && bun run typecheck</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Add README real-file sync examples and safety guidance</name>
  <read_first>
    - README.md
    - .planning/phases/91-files-sync-integration-and-machine-output/91-CONTEXT.md
    - .planning/phases/90-files-command-surface-and-conflict-policy/90-CONTEXT.md
  </read_first>
  <files>README.md</files>
  <action>Add a README section or extend the existing files/workspace section to document `files.sync` for private files that must appear as real workspace files. Include examples for dotfiles, specs, and agent configuration such as `.planning` and `.codex` skills/hooks. Explain that sync is useful when tools or agents need real files and may refuse external symlinks, while symlink remains useful when a live external reference is acceptable. Document lifecycle behavior exactly: create materializes sync targets once, normal open does not auto-pull, missing-worktree recreation only materializes missing targets, and explicit `git-stacks files status|pull|push` remains the manual workflow. Warn that default pull/push behavior is conservative and that `--force` mirrors the source direction and can delete destination-only files. Do not include a full JSON response example in README.</action>
  <acceptance_criteria>
    - README contains `files.sync`.
    - README contains `git-stacks files status`, `git-stacks files pull`, and `git-stacks files push`.
    - README mentions dotfiles, specs, and agent configuration.
    - README contains `.planning` and `.codex` examples or equivalent concrete agent/GSD config examples.
    - README states normal open does not auto-pull existing sync targets.
    - README warns that `--force` can delete destination-only files.
    - README does not contain a fenced JSON example for `git-stacks files`.
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Command metadata -> shell completion | Users rely on completion/help to discover destructive flags and safe defaults. |
| README examples -> user filesystem behavior | Misleading docs can cause accidental overwrite/delete expectations. |
| Phase boundary -> future TUI | Machine output should be prepared without adding dashboard UI now. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-07 | Repudiation | Completion/help | mitigate | Test common flags are discoverable in help and completion. |
| T-91-08 | Tampering | README force guidance | mitigate | Document conservative defaults and explicit force mirror/delete behavior. |
| T-91-09 | Scope creep | TUI/dashboard | mitigate | Acceptance criteria assert no `src/tui/` modifications. |
</threat_model>

<verification>
Required checks:
- `bun test tests/lib/completion-generator.test.ts tests/commands/files.test.ts`
- `bun run typecheck`
</verification>

<success_criteria>
- Shell completion and help cover `files status|pull|push` and common flags.
- README documents real-file sync for dotfiles, specs, and agent configuration.
- README explains sync versus symlink and create/open behavior.
- README warns about conservative defaults and destructive `--force`.
- No dashboard UI is added.
</success_criteria>

