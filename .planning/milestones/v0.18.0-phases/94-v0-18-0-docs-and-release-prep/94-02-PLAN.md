---
phase: 94
plan: 02
title: User-Facing Release Notes And RC Packaging Metadata
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - package.json
requirements:
  - REL-01
autonomous: true
must_haves:
  truths:
    - "The changelog entry leads with file sync and forge-source workspace creation, not internal phase mechanics, per D-01 through D-03."
    - "The release notes explain conservative overwrite/delete behavior, explicit push-back, and forge validation limits in plain user terms per D-03 and D-08."
    - "Package metadata is prepared as `0.18.0-rc.1`, not final `0.18.0`, per D-12 through D-15."
    - "The release candidate can be tagged as `v0.18.0-rc.1` without mismatching the package version or changelog."
  artifacts:
    - "`CHANGELOG.md` entry for `0.18.0-rc.1`"
    - "`package.json` version set to `0.18.0-rc.1`"
  key_links:
    - "CHANGELOG wording mirrors the README safety/validation boundaries instead of overstating forge coverage"
    - "package.json version and rc tag name use the same `0.18.0-rc.1` identifier"
---

# Phase 94 Plan 02 - User-Facing Release Notes And RC Packaging Metadata

<objective>
Prepare the release-candidate metadata for v0.18.0 by writing user-facing release notes and aligning package versioning with the rc tag name.

Purpose: Fulfill REL-01 and the RC packaging decisions without mixing release notes back into implementation detail.
Output: A `0.18.0-rc.1` changelog entry plus matching package metadata ready for RC verification and tagging.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/94-v0-18-0-docs-and-release-prep/94-CONTEXT.md
@.planning/phases/91-files-sync-integration-and-machine-output/91-SUMMARY.md
@.planning/phases/92-forge-source-research-and-resolver-design/92-04-SUMMARY.md
@CHANGELOG.md
@package.json
</context>

<tasks>
  <task type="auto">
    <name>Task 1: Write the `0.18.0-rc.1` changelog entry in user language</name>
    <read_first>
      - CHANGELOG.md
      - .planning/PROJECT.md
      - .planning/phases/94-v0-18-0-docs-and-release-prep/94-CONTEXT.md
      - .planning/phases/91-files-sync-integration-and-machine-output/91-SUMMARY.md
      - .planning/phases/92-forge-source-research-and-resolver-design/92-04-SUMMARY.md
    </read_first>
    <files>CHANGELOG.md</files>
    <action>Add a new topmost `0.18.0-rc.1` entry that follows D-01 through D-03: lead with the two user workflows, syncing private/project files into workspaces and creating workspaces from forge changes. Describe the shipped behavior in user terms only, call out manual sync-back and conservative overwrite/delete defaults, and explain forge early-support limits without harsh warning language per D-03, D-07, and D-08. Do not narrate phase numbers, internal refactoring, or local-only test mechanics unless they matter directly to user trust in the release.</action>
    <verify>
      <automated>rg -n "^## \\[0\\.18\\.0-rc\\.1\\]" CHANGELOG.md</automated>
    </verify>
    <done>`CHANGELOG.md` has a user-facing `0.18.0-rc.1` entry that accurately describes file sync and forge-source behavior plus their safety boundaries.</done>
  </task>

  <task type="auto">
    <name>Task 2: Align package metadata with the release-candidate identifier</name>
    <read_first>
      - package.json
      - CHANGELOG.md
      - .planning/phases/94-v0-18-0-docs-and-release-prep/94-CONTEXT.md
    </read_first>
    <files>package.json</files>
    <action>Set the package version to `0.18.0-rc.1` per D-12 and D-13, leaving the publish surface otherwise unchanged. Preserve D-14 and D-15 by treating `v0.18.0-rc.1` as the matching release-candidate tag name and by leaving final `0.18.0` tagging out of scope. If a later verification step finds an existing local `v0.18.0-rc.1` tag pointing elsewhere, fail clearly instead of silently moving it.</action>
    <verify>
      <automated>node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).version)" | rg '^0\\.18\\.0-rc\\.1$'</automated>
    </verify>
    <done>`package.json` advertises `0.18.0-rc.1`, establishing the exact version string the RC tag and publish step must use.</done>
  </task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| changelog -> users | Users rely on release notes to decide whether the RC behavior matches their needs and risk tolerance. |
| package metadata -> publish/tag flow | The package version and git tag must describe the same release-candidate state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-03 | Tampering | CHANGELOG.md | mitigate | Write the RC notes around user-visible workflows and safety limits; verify the new entry exists and is phrased as release-facing output rather than internal mechanics. |
| T-94-04 | Repudiation | package.json + rc tag name | mitigate | Pin package version to `0.18.0-rc.1` and require the later tag step to use `v0.18.0-rc.1` or fail on mismatch. |
| T-94-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs occur in this plan. |
</threat_model>

<verification>
- `rg -n "^## \\[0\\.18\\.0-rc\\.1\\]" CHANGELOG.md`
- `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).version)"`
</verification>

<success_criteria>
- The top changelog entry is `0.18.0-rc.1` and leads with the two user workflows.
- The entry names file-sync safety boundaries and forge validation limits accurately.
- `package.json` version equals `0.18.0-rc.1`.
</success_criteria>

<output>
Create `.planning/phases/94-v0-18-0-docs-and-release-prep/94-02-SUMMARY.md` when done.
</output>
