---
phase: 94
plan: 01
title: README And Help Alignment For Files Sync And Forge Sources
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - src/commands/workspace.ts
  - src/commands/files.ts
requirements:
  - DOCS-01
  - DOCS-02
autonomous: true
must_haves:
  truths:
    - "README explains `files.sync` as real-file materialization for dotfiles, specs, `.planning`, and `.codex` paths per D-09 and D-10."
    - "README states that normal open does not sync back local edits and that `git-stacks files push` remains explicit per D-11."
    - "README documents `git-stacks new --source` near the workspace creation flow with a short full-URL example, `--template` requirement, `--repo <name>` ambiguity override, and early-support caveats per D-04 through D-08."
    - "CLI help text and README use the same safety language for `files` commands and forge-source preview behavior per D-18."
  artifacts:
    - "README sections covering file sync behavior, local exclude behavior, and forge-source workspace creation"
    - "Commander help text for `new --source` and `files status|pull|push`"
  key_links:
    - "README examples match the option names exposed by `src/commands/workspace.ts` and `src/commands/files.ts`"
    - "`files` help, README safety wording, and Phase 90/91 conflict policy stay aligned"
---

# Phase 94 Plan 01 - README And Help Alignment For Files Sync And Forge Sources

<objective>
Make the user-facing workspace docs accurate enough for a release candidate by aligning README guidance with the shipped `files` command surface and the forge-source workspace flow.

Purpose: Fulfill DOCS-01 and DOCS-02 without inventing behavior the CLI does not actually expose.
Output: Updated README sections plus any help-text wording fixes needed to keep command help and docs in sync.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/94-v0-18-0-docs-and-release-prep/94-CONTEXT.md
@.planning/phases/89-files-sync-schema-and-materialization/89-CONTEXT.md
@.planning/phases/90-files-command-surface-and-conflict-policy/90-CONTEXT.md
@.planning/phases/91-files-sync-integration-and-machine-output/91-SUMMARY.md
@.planning/phases/92-forge-source-research-and-resolver-design/92-RESEARCH.md
@README.md
@src/commands/workspace.ts
@src/commands/files.ts

<interfaces>
From `src/commands/workspace.ts`:

```typescript
.option("--source <forge-url>", "Create workspace from forge change source URL (requires --template)")
.option("--repo <name>", "Template repo name to use for --source when multiple repos match")
.option("--dry-run", "Preview source resolution and planned creation without writing workspace/worktrees")
```

From `src/commands/files.ts`:

```typescript
filesCommand.command("status [workspace]").option("--verbose").option("--json")
filesCommand.command("pull [workspace]").option("--force").option("--dry-run").option("--json")
filesCommand.command("push [workspace]").option("--force").option("--dry-run").option("--json")
```
</interfaces>
</context>

<tasks>
  <task type="auto">
    <name>Task 1: Align help text with the release-facing safety contract</name>
    <read_first>
      - src/commands/workspace.ts
      - src/commands/files.ts
      - .planning/phases/90-files-command-surface-and-conflict-policy/90-CONTEXT.md
      - .planning/phases/91-files-sync-integration-and-machine-output/91-SUMMARY.md
      - .planning/phases/94-v0-18-0-docs-and-release-prep/94-CONTEXT.md
    </read_first>
    <files>src/commands/workspace.ts, src/commands/files.ts</files>
    <action>Review the Commander descriptions and option help for `new --source`, `--repo`, `--dry-run`, and `files status|pull|push`. Tighten wording only where needed so the CLI itself expresses the same safety boundaries the docs must promise: `--template` is required for `--source` per D-04 and D-06, `--repo <name>` is an ambiguity override per D-06, `files push` is explicit per D-11, and `--force` remains the destructive mirror path from Phase 90. Do not change runtime behavior in this plan; limit edits to release-facing help text and command descriptions.</action>
    <verify>
      <automated>bun test tests/commands/files.test.ts tests/commands/workspace-source.test.ts</automated>
    </verify>
    <done>`git-stacks new --help` and `git-stacks files --help` expose wording that matches the intended README safety and workflow language without changing command behavior.</done>
  </task>

  <task type="auto">
    <name>Task 2: Update README for file sync and forge-source workspace creation</name>
    <read_first>
      - README.md
      - .planning/phases/89-files-sync-schema-and-materialization/89-CONTEXT.md
      - .planning/phases/90-files-command-surface-and-conflict-policy/90-CONTEXT.md
      - .planning/phases/91-files-sync-integration-and-machine-output/91-SUMMARY.md
      - .planning/phases/92-forge-source-research-and-resolver-design/92-RESEARCH.md
      - .planning/phases/94-v0-18-0-docs-and-release-prep/94-CONTEXT.md
    </read_first>
    <files>README.md</files>
    <action>Extend the Workspaces and Real File Sync documentation to satisfy D-04 through D-11. Keep the forge-source docs next to `git-stacks new`, not under the general forge integration section, and use one short practical full-URL example per D-04 and D-05. Document that `--template` is mandatory, `--repo <name>` resolves ambiguous matches, and current support accepts full forge URLs only per D-06. Use the softer early-support language from D-07 and D-08: mention that provider auth, self-hosted instances, and fork refs may still need manual verification, and do not claim live GitLab/GitHub coverage beyond local or injected validation. In the file sync section, keep the real-file materialization framing from D-09 and D-10, add repo-level `git_exclude` / `.git/info/exclude` behavior, and state plainly that `files push` is the explicit sync-back path while normal open remains conservative per D-11. Preserve the existing no-JSON-example boundary from Phase 91.</action>
    <verify>
      <automated>bun test tests/commands/files.test.ts tests/commands/workspace-source.test.ts</automated>
    </verify>
    <done>README covers file sync examples, local exclude behavior, manual push-back, and the forge-source create flow with accurate caveats and command names.</done>
  </task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> CLI users | Users trust README/help text to describe destructive and validation-sensitive behavior accurately. |
| forge docs -> release claims | Release docs must not imply live provider validation that the project has not actually run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-01 | Tampering | README.md + command help | mitigate | Keep README examples and safety notes aligned with the exact Commander option names and existing Phase 90/91 behavior; verify via command-help tests. |
| T-94-02 | Spoofing | forge-source documentation | mitigate | Require early-support caveats covering auth, self-hosted base URLs, and fork refs; avoid wording that implies live GitLab/GitHub validation. |
| T-94-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs occur in this plan. |
</threat_model>

<verification>
- `bun test tests/commands/files.test.ts tests/commands/workspace-source.test.ts`
- `bun run typecheck`
</verification>

<success_criteria>
- README documents `files.sync`, local exclude behavior, conservative defaults, and explicit `files push`.
- README documents `git-stacks new --source` near the workspace creation flow with a short example and accurate early-support caveats.
- Help text and README wording describe the same command contract.
</success_criteria>

<output>
Create `.planning/phases/94-v0-18-0-docs-and-release-prep/94-01-SUMMARY.md` when done.
</output>
