# Phase 93.1: Parallel Integration Test Runner and Coherent Coverage Merging - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-16T11:40:26+02:00
**Phase:** 93.1-parallel-integration-test-runner-and-coherent-coverage-mergi
**Areas discussed:** todo folding, parallelism defaults, output semantics, fixture isolation, coverage merge behavior

---

## Todo Folding

| Option | Description | Selected |
|--------|-------------|----------|
| Fold nothing | Treat all todo matches as broad keyword noise. | ✓ |
| Fold TUI/dashboard todo | Fold a weak UI match. | |
| Fold manual commands todo | Fold a weak tooling match. | |

**User's choice:** Fold nothing.
**Notes:** No pending todo matched Phase 93.1 strongly.

---

## Parallelism Defaults

| Option | Description | Selected |
|--------|-------------|----------|
| Conservative fixed default | Fixed 2 workers. | |
| Conservative fixed default 4 | Fixed 4 workers, with serial fallback through `--workers 1`. | ✓ |
| CPU-based default | Use CPU count. | |
| Serial default | Parallel only when explicitly requested. | |

**User's choice:** Fixed default 4 workers.
**Notes:** User initially considered 2 but preferred maybe 4. User selected CLI flag only for override, `--workers 1` as serial compatibility, and same worker count for integration tests and coverage integration runs.

---

## Output Semantics

| Option | Description | Selected |
|--------|-------------|----------|
| Buffer per file | Print each file's output after it completes. | ✓ |
| Stream with prefixes | Interleave prefixed live output. | |
| Suppress unless failure | Hide passing file output. | |

**User's choice:** Buffer per file.
**Notes:** User selected completion-order printing, file-attributed failure summaries with exit code and excerpt, and preserving file-level `N/M integration files passed` semantics.

---

## Fixture Isolation

| Option | Description | Selected |
|--------|-------------|----------|
| Per-file process with existing fixtures | Keep current process isolation and per-file temp/config setup. | ✓ |
| Runner-created temp roots | Wrap every file with externally assigned roots. | |
| Just parallelize | No new guardrails. | |

**User's choice:** Per-file process with existing fixtures.
**Notes:** User preferred making every current integration file parallel-safe if possible, rather than defaulting to a serial bucket. User selected targeted checks for temp/config/git/fixed-path collisions and no fail-fast default.

---

## Coverage Merge Behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Per-file coverage dirs merged after workers finish | Isolated Istanbul outputs merged into canonical artifacts. | ✓ |
| Shared coverage dir | Workers write to same coverage dir. | |
| Keep coverage serial | Do not parallelize coverage yet. | |

**User's choice:** Per-file/process coverage dirs.
**Notes:** User selected merging successful artifacts even when a worker fails, preserving canonical output paths, and cleaning temp dirs after successful merge while preserving them on failure/debug.

---

## the agent's Discretion

- Planner may choose exact worker-pool implementation and output excerpt length.
- Planner may define a debug flag name for preserving coverage temp directories.
- Planner may add a documented serial bucket only after investigating a concrete impractical parallel-safety case.

## Deferred Ideas

- CI workflow work.
- Broad test-suite rearchitecture beyond this runner and fixture-safety scope.
