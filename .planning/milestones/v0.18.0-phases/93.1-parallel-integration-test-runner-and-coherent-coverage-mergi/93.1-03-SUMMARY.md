---
phase: 93.1-parallel-integration-test-runner-and-coherent-coverage-mergi
plan: 03
subsystem: testing
tags: [bun, coverage, istanbul, parallel-runner]

requires:
  - phase: 93.1-01
    provides: shared worker parsing and bounded isolated pool
  - phase: 93.1-02
    provides: parallel-safe fixture paths for current integration files
provides:
  - Coverage integration scheduling through the shared bounded-worker pool
  - Per-file coverage shard directories under `.coverage/shards`
  - Canonical Istanbul merge continuity for `.coverage/coverage-final.json`, `.coverage/coverage-summary.json`, `.coverage/lcov.info`, and `.coverage/index.html`
  - Debug preserve behavior through `--keep-workdir` and automatic failure preservation
affects:
  - 94

tech-stack:
  added: []
  patterns:
    - "Coverage integration uses the same `--workers` parser as the test runner."
    - "Worker coverage shards are written to unique per-file directories and only successful worker shard dirs are merged."

key-files:
  created:
    - tests/lib/coverage-runner.test.ts
    - .planning/phases/93.1-parallel-integration-test-runner-and-coherent-coverage-mergi/93.1-03-SUMMARY.md
  modified:
    - scripts/coverage-runner.ts
    - tests/lib/coverage-surface.test.ts

key-decisions:
  - "Unit coverage remains serial; integration coverage uses the bounded worker count."
  - "Successful shard directories are still merged after a worker failure, then the overall command exits nonzero."
  - "Canonical report filenames remain unchanged for local release gates."

patterns-established:
  - "Use `mergeableShardDirs()` to separate successful worker coverage from failed-worker debug artifacts."
  - "Use `cleanupCoveragePaths()`/`cleanupCoverageWorkdirs()` so success cleanup and failure preservation are testable."

requirements-completed: [TEST-01, COV-01]

duration: 18min
completed: 2026-05-16
---

# Phase 93.1 Plan 03 Summary

**Parallel coverage integration with per-worker shards and unchanged canonical Istanbul reports**

## Performance

- **Duration:** ~18 min
- **Started:** 2026-05-16T10:37:00Z
- **Completed:** 2026-05-16T10:55:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Reworked `scripts/coverage-runner.ts` so coverage integration reuses `parseRunnerArgs()` and `runIsolatedPool()` with `--workers`.
- Added per-file `GS_COVERAGE_SHARD_DIR` values so parallel workers write separate shard directories before the final merge.
- Added `--keep-workdir`, success cleanup, and automatic failure preservation semantics with focused tests.
- Preserved canonical report outputs: `.coverage/coverage-final.json`, `.coverage/coverage-summary.json`, `.coverage/lcov.info`, and `.coverage/index.html`.

## Task Commits

1. **Tasks 93.1-03-01 and 93.1-03-02:** `59c1ed7` (feat)

**Plan metadata:** this summary commit follows.

## Files Created/Modified

- `scripts/coverage-runner.ts` - Bounded coverage worker scheduling, per-file shard dirs, mergeable shard selection, and cleanup policy.
- `tests/lib/coverage-runner.test.ts` - Deterministic coverage runner tests for worker args, partial-failure shard mergeability, and cleanup preservation.
- `tests/lib/coverage-surface.test.ts` - Surface assertions for `--workers`, `--keep-workdir`, worker shard env, and canonical report names.

## Decisions Made

- Coverage unit mode remains one worker to avoid changing unit-mode selection semantics.
- Failed worker shard directories are preserved for debugging, while successful worker shard dirs are used for canonical merge continuity.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Verification

- `bun test tests/lib/coverage-runner.test.ts tests/lib/coverage-surface.test.ts tests/lib/verify.test.ts tests/commands/istanbul-smoke.test.ts`
- `bun run typecheck`
- `bun run coverage:integ -- --workers 4 tests/commands/istanbul-smoke.test.ts`

## Next Phase Readiness

Phase 94 release prep can rely on the unchanged canonical coverage artifact paths while using bounded parallel integration coverage for faster local gates.

---
*Phase: 93.1-parallel-integration-test-runner-and-coherent-coverage-mergi*
*Completed: 2026-05-16*
