---
phase: 93.1-parallel-integration-test-runner-and-coherent-coverage-mergi
plan: 01
subsystem: testing
tags: [bun, integration-tests, parallel-runner, buffered-output]

requires:
  - phase: 34.1
    provides: isolated integration test runner split from shared unit execution
provides:
  - Shared bounded-worker parser and isolated pool helpers for test and coverage runners
  - Buffered completion-order integration output with failed-file summaries
  - Deterministic runner tests for worker parsing, full-suite drain, summaries, and serial metadata
affects:
  - 93.1-02
  - 93.1-03

tech-stack:
  added: []
  patterns:
    - "Integration subprocess output is captured per file, then flushed after that file completes."
    - "Worker parsing lives in scripts/test-runner-core.ts so coverage can reuse the same contract."

key-files:
  created:
    - scripts/test-runner-core.ts
    - tests/lib/test-runner.test.ts
    - .planning/phases/93.1-parallel-integration-test-runner-and-coherent-coverage-mergi/93.1-01-SUMMARY.md
  modified:
    - scripts/test-runner.ts

key-decisions:
  - "Default integration worker count is fixed at 4, with `--workers 1` as serial compatibility mode."
  - "The unit runner remains a single shared Bun process; only isolated integration files use bounded workers."
  - "Serial exceptions are represented by an explicit empty `serialOnlyFiles` map until a concrete file proves otherwise."

patterns-established:
  - "Use `parseRunnerArgs()` for test and coverage worker flag semantics."
  - "Use `runIsolatedPool()` for bounded isolated-file execution and completion-order reporting."

requirements-completed: [TEST-01, TEST-02]

duration: 15min
completed: 2026-05-16
---

# Phase 93.1 Plan 01 Summary

**Bounded parallel integration runner with shared worker parsing and attributed buffered output**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-05-16T10:10:00Z
- **Completed:** 2026-05-16T10:25:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Added `scripts/test-runner-core.ts` with `parseRunnerArgs()`, `runIsolatedPool()`, `summarizeFailures()`, `formatIntegrationSummary()`, and empty `serialOnlyFiles` metadata.
- Updated `scripts/test-runner.ts` so integration files run through bounded workers, buffer stdout/stderr per file, flush after completion, and still drain the full scheduled suite.
- Added focused tests for default workers, `--workers 1`, invalid worker inputs, completion-order flushing, all-file execution after failure, final counts, and failure summaries.

## Task Commits

1. **Tasks 93.1-01-01 and 93.1-01-02:** `31e75a0` (feat)

**Plan metadata:** this summary commit follows.

## Files Created/Modified

- `scripts/test-runner-core.ts` - Shared worker parsing, bounded isolated-file execution, failure summaries, and serial exception metadata.
- `scripts/test-runner.ts` - Wires integration mode to the shared pool while preserving shared-process unit mode.
- `tests/lib/test-runner.test.ts` - Deterministic tests for the new runner contract.

## Decisions Made

- Completion-order output is implemented by pushing each finished worker result into a completed list and printing the buffered output at that point.
- Unknown runner arguments now fail clearly through `parseRunnerArgs()` instead of being ignored.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The initial excerpt test used too little synthetic output to prove truncation; it was corrected to request a shorter tail explicitly.

## User Setup Required

None - no external service configuration required.

## Verification

- `bun test tests/lib/test-runner.test.ts`
- `bun run typecheck`

## Next Phase Readiness

Plan 02 can import `serialOnlyFiles` for fixture-safety assertions. Plan 03 can reuse `parseRunnerArgs()` and `runIsolatedPool()` for coverage integration scheduling.

---
*Phase: 93.1-parallel-integration-test-runner-and-coherent-coverage-mergi*
*Completed: 2026-05-16*
