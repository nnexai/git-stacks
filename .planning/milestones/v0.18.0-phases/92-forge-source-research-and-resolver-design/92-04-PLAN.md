---
phase: 92
plan: 04
type: execute
wave: 3
depends_on:
  - 92-01
  - 92-02
  - 92-03
files_modified:
  - docs/forge-source-resolver.md
  - src/lib/integrations/forge-source.ts
  - tests/lib/integrations/forge-source.test.ts
  - tests/lib/config.test.ts
autonomous: true
requirements:
  - FSRC-02
  - FSRC-03
  - FSRC-08
must_haves:
  truths:
    - "D-18: Phase 92 designs fetch/checkout strategy; Phase 93 implements it."
    - "D-19: Preferred internal strategy is plain Git fetch/checkout through existing git-stacks workspace logic."
    - "D-20: Provider checkout commands are documented research references only."
    - "D-21: Workspace branch name should use the forge source branch name."
    - "D-22: Existing local branch handling aligns with v0.17.2 behavior: reuse when it matches the same source/ref; fail clearly on mismatch."
    - "D-23: Resolver must identify fork/source project metadata where available."
    - "D-01: GitLab research and local validation constraints remain visible in final docs."
    - "D-02: Gitea local validation constraints remain visible in final docs."
    - "D-03: GitHub research constraints remain visible in final docs."
  key_links:
    - from: "src/lib/git.ts"
      pattern: "createWorktree"
    - from: "src/lib/git.ts"
      pattern: "ensureUpstreamTracking"
    - from: "src/lib/workspace-lifecycle.ts"
      pattern: "createWorkspace"
    - from: ".planning/phases/92-forge-source-research-and-resolver-design/92-RESEARCH.md"
      pattern: "branch_conflict"
  artifacts:
    - path: "docs/forge-source-resolver.md"
      provides: "Phase 93 handoff checklist and fetch strategy"
      contains: "branch_conflict"
    - path: "tests/lib/integrations/forge-source.test.ts"
      provides: "Focused parser and contract gate"
      contains: "metadataForWorkspace"
---

<objective>
Complete the Phase 92 handoff by verifying that the docs, typed contract, config schemas, parser tests, and Phase 93 fetch/checkout constraints agree with each other and do not require live provider CLIs.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 93 handoff checklist and branch conflict policy</name>
  <read_first>
    - docs/forge-source-resolver.md
    - src/lib/git.ts
    - src/lib/workspace-lifecycle.ts
    - .planning/phases/92-forge-source-research-and-resolver-design/92-CONTEXT.md
  </read_first>
  <files>docs/forge-source-resolver.md</files>
  <action>Add a `Phase 93 handoff` section to `docs/forge-source-resolver.md`. Specify that Phase 93 should fetch exact source refs with plain Git before `createWorkspace()`, use the forge source branch name as the workspace branch, reuse an existing local branch only when it matches the same source/ref metadata, and fail with `branch_conflict` when the local branch name points at different source metadata. Specify that fork/source project metadata from `headRepository`, GitLab source project fields, or Gitea `head` fields should drive the fetch remote/ref. State again that trunk and dir repos fail with `not_worktree_mode`.</action>
  <acceptance_criteria>
    - `docs/forge-source-resolver.md` contains a `Phase 93 handoff` heading.
    - The handoff contains `branch_conflict`.
    - The handoff contains `not_worktree_mode`.
    - The handoff says `createWorkspace()` is called after the source ref is fetched.
    - The handoff says the workspace branch name uses the forge source branch name.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Phase 93 handoff|branch_conflict|not_worktree_mode|createWorkspace\\(\\)|forge source branch name" docs/forge-source-resolver.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Run focused contract and compatibility gates</name>
  <read_first>
    - tests/lib/integrations/forge-source.test.ts
    - tests/lib/config.test.ts
    - src/lib/integrations/forge-source.ts
    - src/lib/config.ts
    - docs/forge-source-resolver.md
  </read_first>
  <files>tests/lib/integrations/forge-source.test.ts, tests/lib/config.test.ts, src/lib/integrations/forge-source.ts, src/lib/config.ts, docs/forge-source-resolver.md</files>
  <action>Run the final focused gate `bun test tests/lib/integrations/forge-source.test.ts tests/lib/config.test.ts && bun run typecheck`. If it fails, fix only Phase 92 contract/config/parser/doc issues needed for the gate. Do not add `git-stacks new --source`, do not call provider checkout commands, and do not require live `glab`, `gh`, or authenticated `tea`.</action>
  <acceptance_criteria>
    - `bun test tests/lib/integrations/forge-source.test.ts tests/lib/config.test.ts` exits 0.
    - `bun run typecheck` exits 0.
    - `rg -n "git-stacks new --source" src/commands src/lib` returns no Phase 92 implementation addition unless it already existed before this phase.
    - `rg -n "mr checkout|pr checkout|pulls checkout" src/lib src/commands` shows no new internal checkout use added by Phase 92.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/lib/integrations/forge-source.test.ts tests/lib/config.test.ts && bun run typecheck</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Normalized source metadata -> Phase 93 Git operations | The next phase will fetch refs based on this handoff. |
| Existing local branch -> source workspace branch | Branch reuse can accidentally attach to the wrong source if metadata is ignored. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-07 | Tampering | Local branch reuse | mitigate | Require `branch_conflict` when a same-name local branch does not match source/ref metadata. |
| T-92-08 | Elevation of privilege | Provider CLI checkout | mitigate | Final source assertions prevent adding provider checkout commands as an internal path. |
</threat_model>

<verification>
Required checks:
- `bun test tests/lib/integrations/forge-source.test.ts tests/lib/config.test.ts`
- `bun run typecheck`
- `rg -n "Phase 93 handoff|branch_conflict|Provider checkout commands are not internal checkout implementation" docs/forge-source-resolver.md`
</verification>

<success_criteria>
- Phase 93 has a concrete fetch/checkout handoff.
- Focused contract and config tests are planned as the release gate for the design slice.
- Live provider CLIs are not required for automated validation.
</success_criteria>
