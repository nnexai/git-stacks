---
phase: 92
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/config.ts
  - tests/lib/config.test.ts
autonomous: true
requirements:
  - FSRC-02
  - FSRC-03
  - FSRC-08
must_haves:
  truths:
    - "D-04: GitLab and Gitea self-hosted support requires base URL fields, not hardcoded SaaS hosts."
    - "D-05: Phase 92 designs top-level integration defaults and repo-level override metadata."
    - "D-06: Existing `{ enabled: true }` integration config remains valid."
    - "D-13: Strongest match is explicit repo-level forge metadata: forge id, base URL/instance, and repo path/slug."
    - "D-14: Repo-level forge metadata overrides top-level integration defaults."
  key_links:
    - from: "src/lib/config.ts"
      pattern: "GlobalConfigSchema"
    - from: "src/lib/config.ts"
      pattern: "RepoRegistryEntrySchema"
    - from: ".planning/phases/92-forge-source-research-and-resolver-design/92-CONTEXT.md"
      pattern: "integrations.gitlab.base_url"
    - from: ".planning/phases/92-forge-source-research-and-resolver-design/92-CONTEXT.md"
      pattern: "repo-level forge metadata"
  artifacts:
    - path: "src/lib/config.ts"
      provides: "Backwards-compatible typed forge config and repo metadata schema"
      contains: "ForgeRepoMetadataSchema"
    - path: "tests/lib/config.test.ts"
      provides: "Regression coverage for existing and new forge config shapes"
      contains: "base_url"
---

<objective>
Add the backwards-compatible config schema surface needed to represent self-hosted forge instance defaults and repo-level forge metadata before the resolver implementation uses it.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add config tests for base URL and repo metadata shapes</name>
  <read_first>
    - tests/lib/config.test.ts
    - src/lib/config.ts
    - .planning/phases/92-forge-source-research-and-resolver-design/92-CONTEXT.md
  </read_first>
  <files>tests/lib/config.test.ts</files>
  <action>Add tests proving `GlobalConfigSchema.parse({ integrations: { gitlab: { enabled: true, base_url: "https://gitlab.example.com" }, gitea: { enabled: true, base_url: "https://git.example.test" }, github: { enabled: true } } })` preserves each existing integration object. Add tests proving `RepoRegistryEntrySchema.parse(...)` accepts `forge: "gitlab"` plus `forge_metadata: { forge: "gitlab", base_url: "https://gitlab.example.com", repo_path: "group/subgroup/api" }`. Add a compatibility test proving the existing minimal registry entry with only `forge: "gitea"` still parses.</action>
  <acceptance_criteria>
    - `tests/lib/config.test.ts` contains a `GlobalConfigSchema.parse` assertion for `integrations.gitlab.base_url`.
    - `tests/lib/config.test.ts` contains a `RepoRegistryEntrySchema.parse` assertion for `forge_metadata.repo_path`.
    - `tests/lib/config.test.ts` contains a compatibility assertion where `forge: "gitea"` parses without `forge_metadata`.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/lib/config.test.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add typed forge config schemas without breaking existing config</name>
  <read_first>
    - src/lib/config.ts
    - tests/lib/config.test.ts
  </read_first>
  <files>src/lib/config.ts</files>
  <action>In `src/lib/config.ts`, export `ForgeIntegrationConfigSchema` with `enabled: z.boolean().optional()` and `base_url: z.string().url().optional()`. Export `ForgeRepoMetadataSchema` with `forge: z.enum(["github", "gitlab", "gitea"])`, `base_url: z.string().url().optional()`, and `repo_path: z.string().min(1).optional()`. Add optional `forge_metadata: ForgeRepoMetadataSchema.optional()` to `RepoRegistryEntrySchema`. Keep `GlobalConfigSchema.integrations` as `z.record(z.string(), z.unknown()).default({})` so existing arbitrary integration configs remain valid. Do not rename existing `forge`.</action>
  <acceptance_criteria>
    - `src/lib/config.ts` exports `ForgeIntegrationConfigSchema`.
    - `src/lib/config.ts` exports `ForgeRepoMetadataSchema`.
    - `RepoRegistryEntrySchema` contains `forge_metadata: ForgeRepoMetadataSchema.optional()`.
    - `GlobalConfigSchema.integrations` remains a `z.record(z.string(), z.unknown()).default({})` style schema.
    - Existing `forge` field remains present and optional.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/lib/config.test.ts && bun run typecheck</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User YAML -> typed config | User-controlled config provides forge base URLs and repo metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-03 | Spoofing | `forge_metadata.base_url` | mitigate | Parse base URLs with Zod URL validation before resolver matching uses them. |
| T-92-04 | Tampering | Backwards compatibility | mitigate | Keep existing `integrations` record permissive and existing `forge` enum valid. |
</threat_model>

<verification>
Required checks:
- `bun test tests/lib/config.test.ts`
- `bun run typecheck`
</verification>

<success_criteria>
- Config can represent self-hosted GitLab/Gitea/GitHub defaults.
- Registry entries can represent explicit repo-level forge metadata.
- Existing `{ enabled: true }` integration config and simple `forge` entries remain valid.
</success_criteria>
