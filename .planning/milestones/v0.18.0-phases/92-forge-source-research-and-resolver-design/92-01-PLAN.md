---
phase: 92
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/forge-source-resolver.md
autonomous: true
requirements:
  - FSRC-02
  - FSRC-03
  - FSRC-08
must_haves:
  truths:
    - "D-01: GitLab is first implementation target and `glab` research is official-docs only because `glab` is not installed locally."
    - "D-02: Gitea is shape-aware and locally validated only through `tea 0.14.0` help output because authenticated Tea validation is blocked."
    - "D-03: GitHub is shape-aware from official `gh` documentation because `gh` is not installed locally."
    - "D-04: GitLab and Gitea are self-hosted aware; URL parsing must preserve instance/base URL."
    - "D-05: Design includes integration defaults such as `integrations.gitlab.base_url` / `integrations.gitea.base_url` and repo-level overrides."
    - "D-06: Existing `{ enabled: true }` integration config remains valid."
    - "D-18: Phase 92 designs fetch/checkout; Phase 93 implements workspace creation."
    - "D-19: Preferred internal strategy is plain Git fetch/checkout through existing git-stacks logic."
    - "D-20: `glab mr checkout`, `gh pr checkout`, and `tea pulls checkout` are research references, not internal checkout paths."
  key_links:
    - from: ".planning/phases/92-forge-source-research-and-resolver-design/92-RESEARCH.md"
      pattern: "glab mr view"
    - from: ".planning/phases/92-forge-source-research-and-resolver-design/92-RESEARCH.md"
      pattern: "provider checkout commands"
    - from: "src/lib/integrations/gitlab.ts"
      pattern: "mr\", \"view\", \"--output\", \"json"
    - from: "src/lib/integrations/gitea.ts"
      pattern: "pulls\", \"ls\", \"--output\", \"json"
  artifacts:
    - path: "docs/forge-source-resolver.md"
      provides: "Provider research and resolver design reference for Phase 93"
      contains: "plain Git fetch"
    - path: "docs/forge-source-resolver.md"
      provides: "GitLab-first research boundary"
      contains: "glab mr view"
    - path: "docs/forge-source-resolver.md"
      provides: "Gitea and GitHub shape-aware follow-up notes"
      contains: "tea pulls"
---

<objective>
Create the durable forge source resolver design document that captures official GitLab/GitHub research, local Tea validation limits, self-hosted instance config design, and the plain Git checkout boundary for Phase 93.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add provider research and validation boundaries</name>
  <read_first>
    - .planning/phases/92-forge-source-research-and-resolver-design/92-CONTEXT.md
    - .planning/phases/92-forge-source-research-and-resolver-design/92-RESEARCH.md
    - src/lib/integrations/gitlab.ts
    - src/lib/integrations/github.ts
    - src/lib/integrations/gitea.ts
  </read_first>
  <files>docs/forge-source-resolver.md</files>
  <action>Create `docs/forge-source-resolver.md` with sections named `Provider research`, `Validation limits`, `Self-hosted instance config`, `Resolver contract`, `Repo matching`, and `Fetch and checkout strategy`. Include these concrete command references: `glab mr view <iid> --output json`, `glab mr checkout <id|branch|url>`, `gh pr view <number|url|branch> --json number,url,baseRefName,headRefName,headRepository,headRepositoryOwner,isCrossRepository`, `gh pr checkout <number|url|branch>`, `tea pulls --fields index,url,base,head --output json`, and `tea pulls checkout <pull index>`. State that checkout commands are research references only and internal checkout uses plain Git fetch plus existing git-stacks worktree creation.</action>
  <acceptance_criteria>
    - `docs/forge-source-resolver.md` contains `glab mr view <iid> --output json`.
    - `docs/forge-source-resolver.md` contains `gh pr view <number|url|branch> --json number,url,baseRefName,headRefName,headRepository,headRepositoryOwner,isCrossRepository`.
    - `docs/forge-source-resolver.md` contains `tea pulls --fields index,url,base,head --output json`.
    - `docs/forge-source-resolver.md` contains the exact phrase `Provider checkout commands are not internal checkout implementation`.
    - `docs/forge-source-resolver.md` says `glab` and `gh` were not installed locally and authenticated Tea validation was blocked by token refresh.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "glab mr view|gh pr view|tea pulls|Provider checkout commands are not internal checkout implementation|token refresh" docs/forge-source-resolver.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Specify config and repo matching design</name>
  <read_first>
    - docs/forge-source-resolver.md
    - src/lib/config.ts
    - src/lib/integrations/forge-utils.ts
    - .planning/phases/92-forge-source-research-and-resolver-design/92-CONTEXT.md
  </read_first>
  <files>docs/forge-source-resolver.md</files>
  <action>Extend `docs/forge-source-resolver.md` so `Self-hosted instance config` names optional config keys `integrations.gitlab.base_url`, `integrations.gitea.base_url`, and `integrations.github.base_url`, and names repo-level override metadata fields `forge_metadata.forge`, `forge_metadata.base_url`, and `forge_metadata.repo_path`. In `Repo matching`, specify this exact precedence order: repo-level forge metadata, integration-level base URL config, then URL/remote inference only when it produces one enabled matching repo. Specify typed failures `repo_not_matched`, `ambiguous_repo`, `template_repo_missing`, and `not_worktree_mode`.</action>
  <acceptance_criteria>
    - `docs/forge-source-resolver.md` contains `integrations.gitlab.base_url`.
    - `docs/forge-source-resolver.md` contains `forge_metadata.repo_path`.
    - `docs/forge-source-resolver.md` lists repo matching precedence with repo-level metadata before integration-level config.
    - `docs/forge-source-resolver.md` contains `ambiguous_repo`, `template_repo_missing`, and `not_worktree_mode`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "integrations.gitlab.base_url|forge_metadata.repo_path|repo-level forge metadata|ambiguous_repo|template_repo_missing|not_worktree_mode" docs/forge-source-resolver.md</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External forge docs -> implementation design | Provider CLI docs and local help output inform future command execution. |
| User source URL -> repo selection design | User-controlled URLs will later choose which repo/ref to fetch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01 | Spoofing | Forge instance identity | mitigate | Require base URL/instance identity in the design and make repo-level metadata the strongest match. |
| T-92-02 | Tampering | Checkout strategy | mitigate | Document provider checkout commands as references only; Phase 93 must use explicit Git fetch/checkout logic. |
</threat_model>

<verification>
Required checks:
- `rg -n "glab mr view|gh pr view|tea pulls|Provider checkout commands are not internal checkout implementation" docs/forge-source-resolver.md`
</verification>

<success_criteria>
- FSRC-02 has GitLab-first official-doc research and local validation limits.
- FSRC-03 has a concrete repo matching precedence and failure model.
- FSRC-08 has shape-aware GitHub/Gitea notes that preserve future implementation options.
</success_criteria>
