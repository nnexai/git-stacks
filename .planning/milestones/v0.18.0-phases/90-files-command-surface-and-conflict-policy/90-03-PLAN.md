---
phase: 90
plan: 03
type: execute
wave: 3
depends_on:
  - 90-02
files_modified:
  - src/commands/files.ts
  - src/index.ts
  - tests/commands/files.test.ts
  - tests/lib/files.test.ts
autonomous: true
requirements:
  - FSYNC-04
  - FSYNC-05
  - FSYNC-06
  - FSYNC-07
  - FSYNC-08
requirements_addressed:
  - FSYNC-04
  - FSYNC-05
  - FSYNC-06
  - FSYNC-07
  - FSYNC-08
must_haves:
  truths:
    - "D-01: Keep the command names git-stacks files status|pull|push."
    - "D-02: Workspace selection matches existing workspace commands with optional [workspace] and CWD auto-detection."
    - "D-09: Normal text status shows per-entry rows with workspace/repo scope, target, state, counts, and short hints."
    - "D-10: Counts are shown by default; file path listings require --verbose and should be capped."
    - "D-16: Phase 90 includes --dry-run for pull and push."
    - "D-19: Do not add safer middle flags such as --merge or --add-only in Phase 90."
    - "Deferred: Lifecycle integration and stable machine-readable output stay in Phase 91."
  artifacts:
    - path: "src/commands/files.ts"
      provides: "git-stacks files status|pull|push command group"
      contains: "files"
    - path: "src/index.ts"
      provides: "files command registration before completion generation"
      contains: "filesCommand"
    - path: "tests/commands/files.test.ts"
      provides: "CLI command, CWD detection, output, and policy coverage"
      contains: "files status"
---

<objective>
Expose the Phase 90 file sync behavior through `git-stacks files status|pull|push [workspace]` with compact text output, optional CWD workspace detection, `--dry-run`, and `--force`.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add command-level tests for status, pull, push, and CWD detection</name>
  <read_first>
    - tests/commands/workspace-execution-context.test.ts
    - tests/helpers.ts
    - src/commands/workspace.ts
    - src/index.ts
    - .planning/phases/90-files-command-surface-and-conflict-policy/90-CONTEXT.md
  </read_first>
  <files>tests/commands/files.test.ts</files>
  <action>Create `tests/commands/files.test.ts` using `runCli()`, `makeTmpDir()`, `cleanup()`, and workspace/config fixture helpers. Add tests that `git-stacks files status <workspace>` prints the workspace name and sync count labels `source-only`, `target-only`, and `differing`; `git-stacks files status <workspace> --verbose` prints representative paths and an omitted-count marker when the cap is exceeded; `git-stacks files pull <workspace> --dry-run` reports planned writes without changing target files; `git-stacks files push <workspace> --dry-run` reports planned writes without changing source files; omitted `[workspace]` works when `cwd` is inside a workspace repo task path; and unsafe default push exits nonzero on differing/source-only conflict with a formatted refusal.</action>
  <acceptance_criteria>
    - `tests/commands/files.test.ts` includes a status test asserting stdout contains `source-only`, `target-only`, and `differing`.
    - `tests/commands/files.test.ts` includes a `--verbose` status test with capped path output.
    - `tests/commands/files.test.ts` includes pull and push `--dry-run` tests that assert filesystem contents are unchanged.
    - `tests/commands/files.test.ts` includes an omitted-workspace CWD detection test.
    - `tests/commands/files.test.ts` includes a default push refusal test with nonzero exit code.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/commands/files.test.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Implement the files command group and registration</name>
  <read_first>
    - src/commands/workspace.ts
    - src/index.ts
    - src/lib/files.ts
    - src/lib/config.ts
    - src/lib/workspace-status.ts
    - src/lib/errors.ts
    - tests/commands/files.test.ts
    - .planning/phases/90-files-command-surface-and-conflict-policy/90-PATTERNS.md
  </read_first>
  <files>src/commands/files.ts, src/index.ts</files>
  <action>Add `src/commands/files.ts` exporting `filesCommand = new Command("files")` with subcommands `status [workspace]`, `pull [workspace]`, and `push [workspace]`. Resolve `[workspace]` by explicit name or `detectWorkspaceFromCwd()`; on failure print `formatError("Missing workspace name", "usage: git-stacks files <verb> <workspace>")` or an equivalent existing-style message and exit 1. Wire `status` to the Plan 90-01 status helper and format compact rows with columns for scope, target, state, counts, and hint. Add `--verbose` only to status. Wire `pull` and `push` to the Plan 90-02 helpers with `--force` and `--dry-run` options, printing planned/applied writes, deletes, and refusals. Exit nonzero when operation result `ok` is false. In `src/index.ts`, import `filesCommand` and add it before completion command registration. Do not add `--json`, `--merge`, `--add-only`, lifecycle integration, or TUI code.</action>
  <acceptance_criteria>
    - `src/commands/files.ts` exports `filesCommand`.
    - `src/index.ts` imports and registers `filesCommand` before `createCompletionCommand(program)`.
    - `git-stacks files status [workspace]` supports `--verbose`.
    - `git-stacks files pull [workspace]` supports `--force` and `--dry-run`.
    - `git-stacks files push [workspace]` supports `--force` and `--dry-run`.
    - No `--json`, `--merge`, or `--add-only` option is added to the files command.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/commands/files.test.ts && bun run typecheck</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 90 gates and record evidence</name>
  <read_first>
    - package.json
    - .planning/phases/90-files-command-surface-and-conflict-policy/90-VALIDATION.md
    - .planning/phases/90-files-command-surface-and-conflict-policy/90-CONTEXT.md
  </read_first>
  <files>.planning/phases/90-files-command-surface-and-conflict-policy/90-SUMMARY.md</files>
  <action>Run the final focused gate `bun test tests/lib/files.test.ts tests/commands/files.test.ts`, then `bun run typecheck`, then `bun run verify:gates`. During execute-phase, record the exact commands and pass/fail results in the Phase 90 summary. If `verify:gates` fails for unrelated pre-existing inventory debt, keep the exact failure output in the summary and do not mark it green.</action>
  <acceptance_criteria>
    - Focused Phase 90 tests pass.
    - `bun run typecheck` passes.
    - `bun run verify:gates` is run and its exact result is recorded.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/lib/files.test.ts tests/commands/files.test.ts && bun run typecheck && bun run verify:gates</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> workspace resolution | User-provided or CWD-detected workspace names select config and filesystem roots. |
| Library operation results -> human output | Destructive plans must be understandable before the user chooses force mode. |
| Files command group -> future automation | Phase 90 text output must not pretend to be a stable machine contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-08 | Spoofing | CWD workspace detection | mitigate | Reuse existing `detectWorkspaceFromCwd()` behavior and explicit workspace validation. |
| T-90-09 | Tampering | CLI destructive flags | mitigate | Expose destructive behavior only behind `--force`; default refusals exit nonzero. |
| T-90-10 | Repudiation | Dry-run and refusal output | mitigate | Print planned writes/deletes/refusals for pull and push, and test output contracts. |
| T-90-11 | Information Disclosure | Verbose status | mitigate | Keep default output to counts; require `--verbose` and cap path listings. |
</threat_model>

<verification>
Required checks:
- `bun test tests/commands/files.test.ts`
- `bun test tests/lib/files.test.ts tests/commands/files.test.ts`
- `bun run typecheck`
- `bun run verify:gates`
</verification>

<success_criteria>
- `git-stacks files status [workspace]` shows copy/symlink/sync state with useful sync drift counts.
- `git-stacks files pull [workspace]` refreshes sync targets through the safe policy layer.
- `git-stacks files push [workspace]` explicitly syncs target changes back to sources through the safe policy layer.
- Default push refuses conflicts and unsafe deletes/overwrites.
- No mandatory full per-file hash manifest, stable JSON output, lifecycle integration, or TUI behavior is added in Phase 90.
</success_criteria>

