---
phase: 15.1-action-menu-cursor-unification-and-centered-dialog
plan: 03
type: execute
wave: 3
depends_on: [15.1-01, 15.1-02]
files_modified:
  - src/tui/dashboard/HelpOverlay.tsx
  - src/tui/dashboard/MessageOverlay.tsx
  - src/tui/dashboard/WizardView.tsx
  - src/tui/dashboard/ProgressView.tsx
  - src/tui/dashboard/SyncProgressView.tsx
  - src/tui/dashboard/CreateProgressView.tsx
  - src/tui/dashboard/App.tsx
autonomous: false
requirements: [D-13, D-14, D-15, D-17, D-18, D-19, D-20]

must_haves:
  truths:
    - "HelpOverlay renders as a large centered dialog with 'Keybindings' title and dim background"
    - "MessageOverlay renders as a large centered dialog with workspace name as title"
    - "WizardView renders as a medium centered dialog"
    - "Progress views (ProgressView, SyncProgressView, CreateProgressView) render as medium centered dialogs"
    - "All promoted views render at App root level, overlaying the split pane"
    - "List views, detail panes, filter input, and BatchBar are unchanged per D-17/D-18/D-19/D-20"
  artifacts:
    - path: "src/tui/dashboard/HelpOverlay.tsx"
      provides: "Large centered help overlay"
      contains: "CenteredDialog"
    - path: "src/tui/dashboard/MessageOverlay.tsx"
      provides: "Large centered message overlay"
      contains: "CenteredDialog"
    - path: "src/tui/dashboard/WizardView.tsx"
      provides: "Medium centered wizard"
      contains: "CenteredDialog"
    - path: "src/tui/dashboard/ProgressView.tsx"
      provides: "Medium centered progress view"
      contains: "CenteredDialog"
    - path: "src/tui/dashboard/SyncProgressView.tsx"
      provides: "Medium centered sync progress"
      contains: "CenteredDialog"
    - path: "src/tui/dashboard/CreateProgressView.tsx"
      provides: "Medium centered create progress"
      contains: "CenteredDialog"
  key_links:
    - from: "src/tui/dashboard/App.tsx"
      to: "src/tui/dashboard/WizardView.tsx"
      via: "Root-level Show block for wizard-create views"
      pattern: "Show when=.*wizard-create"
---

<objective>
Wrap large/medium dialog components (HelpOverlay, MessageOverlay, WizardView, ProgressView, SyncProgressView, CreateProgressView) with CenteredDialog and promote remaining views from the detail pane to App root level.

Purpose: Completes the centered dialog conversion for all overlay/wizard/progress views per D-13/D-14/D-15. After this plan, ALL dialogs render as full-screen centered overlays; the detail pane only shows detail views for the list state.

Output: Updated HelpOverlay, MessageOverlay, WizardView, ProgressView, SyncProgressView, CreateProgressView with CenteredDialog wrapping. App.tsx fully restructured with all dialog views at root level.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.1-action-menu-cursor-unification-and-centered-dialog/15.1-CONTEXT.md
@.planning/phases/15.1-action-menu-cursor-unification-and-centered-dialog/15.1-RESEARCH.md
@.planning/phases/15.1-action-menu-cursor-unification-and-centered-dialog/15.1-01-SUMMARY.md
@.planning/phases/15.1-action-menu-cursor-unification-and-centered-dialog/15.1-02-SUMMARY.md

<interfaces>
From src/tui/dashboard/CenteredDialog.tsx (created in Plan 01):
```typescript
type Size = "small" | "medium" | "large"
type Props = { title: string; size?: Size; children: JSX.Element }
export function CenteredDialog(props: Props)
// small: 50% width, medium: 70%, large: 90%
```

From src/tui/dashboard/HelpOverlay.tsx (current):
```typescript
type Props = { tab: Tab; onClose: () => void }
// Currently renders own border + title="Keybindings", height="100%", width="100%"
```

From src/tui/dashboard/MessageOverlay.tsx (current):
```typescript
type Props = {
  workspaceName: string
  messages: MessageRecord[]
  tick: number
  onClose: () => void
  onClearSender: (sender: string | undefined) => Promise<void>
}
// Currently renders own border + title with workspaceName, height="100%", width="100%"
```

From src/tui/dashboard/WizardView.tsx (current):
```typescript
type Props<T> = { steps: WizardStep<T>[]; onComplete: (data: T) => void; onCancel: () => void }
// Currently renders as <box flexDirection="column" paddingTop={1} paddingLeft={2}>
```

From src/tui/dashboard/ProgressView.tsx (current):
```typescript
type Props = { title: string; lines: string[]; done: boolean }
```

From src/tui/dashboard/SyncProgressView.tsx (current):
```typescript
type Props = { rows: SyncRow[]; done: boolean; summary: { text: string; color: ... } }
```

From src/tui/dashboard/CreateProgressView.tsx (current):
```typescript
type Props = { rows: CreateRow[]; done: boolean; summary: { text: string; color: ... } }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap large/medium components with CenteredDialog</name>
  <files>
    src/tui/dashboard/HelpOverlay.tsx
    src/tui/dashboard/MessageOverlay.tsx
    src/tui/dashboard/WizardView.tsx
    src/tui/dashboard/ProgressView.tsx
    src/tui/dashboard/SyncProgressView.tsx
    src/tui/dashboard/CreateProgressView.tsx
  </files>
  <read_first>
    - src/tui/dashboard/HelpOverlay.tsx (has its own border+title that must be REMOVED)
    - src/tui/dashboard/MessageOverlay.tsx (has its own border+title that must be REMOVED)
    - src/tui/dashboard/WizardView.tsx (simple box wrapper)
    - src/tui/dashboard/ProgressView.tsx (simple box wrapper)
    - src/tui/dashboard/SyncProgressView.tsx (simple box wrapper)
    - src/tui/dashboard/CreateProgressView.tsx (simple box wrapper)
    - src/tui/dashboard/CenteredDialog.tsx (wrapper component)
  </read_first>
  <action>
**HelpOverlay.tsx** — Convert to large CenteredDialog per D-13/Pitfall 5/Pitfall 6:

1. Add import: `import { CenteredDialog } from "./CenteredDialog"`
2. REMOVE the outer `<box border title="Keybindings" flexDirection="column" height="100%" width="100%">` — CenteredDialog owns the border and title per Pitfall 6.
3. Replace with `<CenteredDialog title="Keybindings" size="large">` per D-07/D-10.
4. Keep all inner `<text>` content. Wrap in `<box flexDirection="column">` for layout.

Result:
```tsx
return (
  <CenteredDialog title="Keybindings" size="large">
    <box flexDirection="column">
      <text fg="white">{"\n"}  Global:</text>
      {/* ...rest of keybinding text lines unchanged... */}
      <text fg="gray">{"\n"}  Press Esc or ? to close</text>
    </box>
  </CenteredDialog>
)
```

**MessageOverlay.tsx** — Convert to large CenteredDialog per D-13/Pitfall 5/Pitfall 6:

1. Add import: `import { CenteredDialog } from "./CenteredDialog"`
2. The current outer box is: `<box border title={\` \${props.workspaceName} — Messages (\${props.messages.length}) \`} flexDirection="column" height="100%" width="100%">`
3. REMOVE border, title, height, width from this box. Replace with `<CenteredDialog title={\`\${props.workspaceName} — Messages (\${props.messages.length})\`} size="large">` per D-07.
4. Keep the inner content (Show/For groups) inside a `<box flexDirection="column" flexGrow={1}>`.
5. The `contentHeight` memo currently uses `dims().height - 4`. Update to `dims().height - 6` to account for the CenteredDialog border+title chrome. (Or simply keep the current value — the 90% sizing of "large" still provides ample space.)

Result:
```tsx
return (
  <CenteredDialog title={`${props.workspaceName} \u2014 Messages (${props.messages.length})`} size="large">
    <box flexDirection="column" flexGrow={1}>
      <Show when={groups().length === 0}>
        <text fg="gray">  No messages</text>
      </Show>
      <Show when={groups().length > 0}>
        {/* ...For each visibleLines unchanged... */}
      </Show>
    </box>
  </CenteredDialog>
)
```

**WizardView.tsx** — Wrap with medium CenteredDialog per D-14:

1. Add import: `import { CenteredDialog } from "./CenteredDialog"`
2. Add `title` prop to Props: `title?: string` (defaults to "Wizard")
3. Replace outer `<box flexDirection="column" paddingTop={1} paddingLeft={2}>` with `<CenteredDialog title={props.title ?? "Wizard"} size="medium">`
4. Wrap inner content in `<box flexDirection="column" paddingTop={1} paddingLeft={1}>` for padding.

**ProgressView.tsx** — Wrap with medium CenteredDialog per D-15:

1. Add import: `import { CenteredDialog } from "./CenteredDialog"`
2. Replace outer `<box flexDirection="column">` with `<CenteredDialog title={props.title} size="medium">`
3. Keep inner content in a `<box flexDirection="column">`.

**SyncProgressView.tsx** — Wrap with medium CenteredDialog per D-15:

1. Add import: `import { CenteredDialog } from "./CenteredDialog"`
2. Add `title` prop to Props: `title?: string` (defaults to "Sync Progress")
3. Replace outer `<box flexDirection="column">` with `<CenteredDialog title={props.title ?? "Sync Progress"} size="medium">`
4. Keep inner content in a `<box flexDirection="column">`.

**CreateProgressView.tsx** — Wrap with medium CenteredDialog per D-15:

1. Add import: `import { CenteredDialog } from "./CenteredDialog"`
2. Add `title` prop to Props: `title?: string` (defaults to "Creating...")
3. Replace outer `<box flexDirection="column">` with `<CenteredDialog title={props.title ?? "Creating..."} size="medium">`
4. Keep inner content in a `<box flexDirection="column">`.
  </action>
  <verify>
    <automated>bun run typecheck && bun test tests/tui/dashboard/SyncProgressView.test.tsx tests/tui/dashboard/CreateProgressView.test.tsx tests/tui/dashboard/WizardView.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - HelpOverlay.tsx contains `import { CenteredDialog }` and `<CenteredDialog title="Keybindings" size="large">`
    - HelpOverlay.tsx does NOT contain `border title="Keybindings"` on a `<box>` element (CenteredDialog owns border)
    - HelpOverlay.tsx does NOT contain `height="100%" width="100%"` on any box
    - MessageOverlay.tsx contains `<CenteredDialog title=` and `size="large">`
    - MessageOverlay.tsx does NOT contain `border title=` on any `<box>` element
    - WizardView.tsx contains `<CenteredDialog` and `size="medium">`
    - WizardView.tsx Props type contains `title?: string`
    - ProgressView.tsx contains `<CenteredDialog title={props.title} size="medium">`
    - SyncProgressView.tsx contains `<CenteredDialog` and `size="medium">`
    - CreateProgressView.tsx contains `<CenteredDialog` and `size="medium">`
    - `bun run typecheck` passes
    - Existing tests for SyncProgressView, CreateProgressView, WizardView still pass
  </acceptance_criteria>
  <done>All six large/medium components wrapped in CenteredDialog with appropriate size and title props; existing border/height/width attributes removed from HelpOverlay and MessageOverlay</done>
</task>

<task type="auto">
  <name>Task 2: Promote remaining views to App root level and clean up detail pane</name>
  <files>src/tui/dashboard/App.tsx</files>
  <read_first>
    - src/tui/dashboard/App.tsx (current state — after Plan 02 promotions)
    - src/tui/dashboard/types.ts (UIView union)
  </read_first>
  <action>
Promote the remaining dialog/wizard/progress views from the detail pane to the App root level. After Plan 02, the small dialogs (action-menu, confirm, inline-input, repo-action-menu, repo-remove-blocked) are already at root level. This task moves the remaining views.

**Views to promote from the detail pane to root level:**

1. **Progress view** (`view().view === "progress"`): Move the Show block to root. Pass `title` prop from `(view() as any).message`.

2. **Sync progress view** (`view().view === "sync-progress"`): Move the Show block to root. Pass `title` prop from `(view() as any).message`.

3. **Wizard: template-based create** (`view().view === "wizard-create"`): Move the Show block to root. Pass `title="Create Workspace"` to WizardView.

4. **Wizard: ad-hoc create** (`view().view === "wizard-create-adhoc"`): Move the Show block to root. Pass `title="Create Workspace"` to WizardView.

5. **Wizard: template create** (`view().view === "wizard-create-template"`): Move the Show block to root. Pass `title="Create Template"` to WizardView.

6. **Create progress view** (`view().view === "create-progress"`): Move the Show block to root. Pass `title` prop from `"Creating ${(v as any).workspaceName}..."`.

**After promotion, the bottom detail box should ONLY contain:**
- `<Show when={view().view === "list"}>` with the Switch for WorkspaceDetail/TemplateDetail/RepoDetail

**Update detailBoxTitle memo:** Simplify to only handle the "list" case since all other views are promoted. The detail box title for list view should be the selected entity name (current behavior for the "list" view case — the fallback at the end of the memo).

**Update the message overlay help bar:** The MessageOverlay help bar text (`<box height={1}>` with navigation instructions after the MessageOverlay Show block) currently renders inside the `<Show when={!helpOpen() && messagesOpen()}>` block. Keep this in place — MessageOverlay still renders at the same root level it was at before (it was already outside the split pane).

**Root-level Show blocks should be ordered by z-priority** (later = on top):
1. Split-pane (always visible base layer) — `<Show when={!helpOpen() && !messagesOpen()}>`
2. Small dialogs — action menus, confirm, inline-input, repo-action-menu, repo-remove-blocked
3. Medium dialogs — wizards, progress views
4. Large overlays — HelpOverlay, MessageOverlay (already at root, keep their position)

The exact DOM order within the root `<box>`:
```
<box flexDirection="column" height="100%">
  {/* Base layer: split pane + help bar (always rendered, dialogs cover it) */}
  <Show when={!helpOpen() && !messagesOpen()}>
    <box border> (top: lists) </box>
    <box border> (bottom: detail panes for list view ONLY) </box>
    <box height={1}> (help bar) </box>
  </Show>

  {/* Small dialog overlays */}
  <Show when={!helpOpen() && !messagesOpen() && view().view === "action-menu"}>...</Show>
  <Show when={!helpOpen() && !messagesOpen() && view().view === "confirm"}>...</Show>
  <Show when={!helpOpen() && !messagesOpen() && view().view === "inline-input"}>...</Show>
  <Show when={!helpOpen() && !messagesOpen() && view().view === "repo-action-menu"}>...</Show>
  <Show when={!helpOpen() && !messagesOpen() && view().view === "repo-remove-blocked"}>...</Show>

  {/* Medium dialog overlays */}
  <Show when={!helpOpen() && !messagesOpen() && view().view === "progress"}>...</Show>
  <Show when={!helpOpen() && !messagesOpen() && view().view === "sync-progress"}>...</Show>
  <Show when={!helpOpen() && !messagesOpen() && view().view === "wizard-create"}>...</Show>
  <Show when={!helpOpen() && !messagesOpen() && view().view === "wizard-create-adhoc"}>...</Show>
  <Show when={!helpOpen() && !messagesOpen() && view().view === "wizard-create-template"}>...</Show>
  <Show when={!helpOpen() && !messagesOpen() && view().view === "create-progress"}>...</Show>

  {/* Large overlays (already at root) */}
  <Show when={helpOpen()}>
    <HelpOverlay ... />
  </Show>
  <Show when={!helpOpen() && messagesOpen()}>
    <MessageOverlay ... />
    <box height={1}>...</box>
  </Show>
</box>
```

**Pass title props to newly promoted components:**
- WizardView (wizard-create): `title="Create Workspace"`
- WizardView (wizard-create-adhoc): `title="Create Workspace"`
- WizardView (wizard-create-template): `title="Create Template"`
- SyncProgressView: `title={(view() as any).message}` (e.g., "Syncing myworkspace...")
- CreateProgressView: `title={"Creating " + (view() as any).workspaceName + "..."}`
- ProgressView: already has title prop

**Verify all keyboard guards remain** in the main useKeyboard handler. No guard changes needed — the view-state checks work regardless of where the component renders in the DOM.
  </action>
  <verify>
    <automated>bun run typecheck && bun test tests/tui/dashboard/</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx bottom detail box ONLY contains Show block for `view().view === "list"`
    - App.tsx has root-level Show blocks for progress, sync-progress, wizard-create, wizard-create-adhoc, wizard-create-template, create-progress
    - WizardView receives `title` prop ("Create Workspace" or "Create Template")
    - SyncProgressView receives `title` prop
    - CreateProgressView receives `title` prop
    - HelpOverlay and MessageOverlay remain at root level (unchanged position)
    - All keyboard guards in useKeyboard handler remain present and unchanged
    - `bun run typecheck` passes
    - `bun test tests/tui/dashboard/` passes (all dashboard tests)
    - `bun test tests/` passes (full suite)
  </acceptance_criteria>
  <done>All dialog/wizard/progress views promoted to App root level. Detail pane contains only list-view detail components. All 11 dialog components render as full-screen centered overlays with dim backgrounds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of centered dialogs</name>
  <files>src/tui/dashboard/App.tsx</files>
  <action>
Human visually verifies all 11 centered dialog components render correctly in the TUI.

Components to verify:
- Action menus (workspace, template, repo) — small, with cursor navigation
- ConfirmDialog — small, with entity name as title
- RemoveBlockedView — small
- InlineInput (rename/clone) — small
- WizardView (create workspace/template) — medium
- ProgressView — medium
- SyncProgressView — medium
- CreateProgressView — medium
- HelpOverlay — large
- MessageOverlay — large

Verification steps:
1. Run `bun run dev` then `git-stacks manage` to launch dashboard
2. In Workspaces tab: press Enter on a workspace — verify centered dialog with workspace name title, dim background, cyan cursor on first item, arrow keys move cursor
3. Press Esc, switch to Templates tab (2), press Enter — verify centered dialog with template name title and cursor
4. Switch to Repos tab (3), press Enter — verify centered dialog with repo name title and cursor
5. Press ? — verify large centered "Keybindings" dialog with dim background
6. Press Esc, navigate to workspace with messages, press m — verify large centered messages dialog
7. In workspace action menu press r — verify centered confirm dialog with workspace name title
8. In templates tab, press Enter then w — verify medium centered "Create Workspace" wizard dialog
  </action>
  <verify>
    <automated>bun test tests/tui/dashboard/</automated>
  </verify>
  <done>All 11 dialog types render as centered overlays with dim backgrounds, correct titles, and proper sizing (small/medium/large). Cursor navigation works in all three action menus.</done>
</task>

</tasks>

<verification>
- `bun test tests/` passes (full test suite)
- `bun run typecheck` passes
- All 11 dialog components import and use CenteredDialog
- App.tsx detail pane only contains list-view details
- Visual confirmation that dialogs render centered with dim backgrounds
</verification>

<success_criteria>
- HelpOverlay and MessageOverlay use CenteredDialog size="large"
- WizardView, ProgressView, SyncProgressView, CreateProgressView use CenteredDialog size="medium"
- All views promoted from detail pane to App root level
- Detail pane only shows detail views for list state
- Visual verification confirms centered rendering with dim backgrounds
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-action-menu-cursor-unification-and-centered-dialog/15.1-03-SUMMARY.md`
</output>
