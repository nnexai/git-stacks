---
phase: 15.2-integration-overrides-per-template-and-workspace
plan: "01"
subsystem: integrations
tags: [clack-prompts, integrations, template-wizard, wizard-helpers]

# Dependency graph
requires:
  - phase: 15.1-action-menu-cursor-unification-and-centered-dialog
    provides: stable wizard patterns and dialog infrastructure
provides:
  - promptIntegrationOverrides() shared helper in src/lib/integrations/wizard-helpers.ts
  - Template new wizard prompts for integration overrides after hooks step
  - Template edit wizard has Integrations menu option with full override flow
affects:
  - 15.2-02-workspace-wizard-overrides
  - any future phase using integration override UX

# Tech tracking
tech-stack:
  added: []
  patterns:
    - confirm-guard pattern for optional wizard sections (same as hooks)
    - shared helper extracted from config.ts multiselect+configure loop
    - conditional YAML key storage (D-04: no integrations key when user declines)

key-files:
  created:
    - src/lib/integrations/wizard-helpers.ts
    - tests/lib/integrations/wizard-helpers.test.ts
  modified:
    - src/tui/template-wizard.ts

key-decisions:
  - "promptIntegrationOverrides uses same confirm-guard pattern as hooks (ask-first, undefined = no key stored)"
  - "Helper accepts initialEnabledIds and currentConfigs to support both global-config and template-override pre-population"
  - "runTemplateEdit integrations handler returns early after processing (consistent with repos and description handlers)"

patterns-established:
  - "Integration override prompt: confirm guard -> multiselect -> configurePrompt per enabled -> { ...current, enabled: false } for disabled"
  - "Template YAML integrations key only stored when user opts in (D-04)"

requirements-completed: [D-01, D-02, D-03, D-04]

# Metrics
duration: 7min
completed: 2026-03-21
---

# Phase 15.2 Plan 01: Integration Override Helper and Template Wizard Summary

**Shared `promptIntegrationOverrides()` helper extracted from config.ts and wired into both `template new` and `template edit` wizards with confirm-guard and conditional YAML storage (D-01 through D-04)**

## Performance

- **Duration:** ~7 min
- **Started:** 2026-03-21T18:20:47Z
- **Completed:** 2026-03-21T18:28:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Created `src/lib/integrations/wizard-helpers.ts` with `promptIntegrationOverrides()` following the confirm-guard pattern from the existing hooks prompt
- 5 unit tests covering all paths (decline, confirm-cancel, multiselect-cancel, opt-in with configure, disabled integration config preservation)
- Wired into `runTemplateNew` after the hooks step — integrations key only stored when user opts in
- Wired into `runTemplateEdit` as new "Integrations" menu option with current-override-aware pre-selection

## Task Commits

Each task was committed atomically:

1. **Task 1 RED: Add failing tests** - `18d085b` (test)
2. **Task 1 GREEN: Create promptIntegrationOverrides helper** - `812930b` (feat)
3. **Task 2: Wire into template wizard** - `3853d83` (feat)

_Note: TDD tasks have two commits (test RED → feat GREEN)_

## Files Created/Modified

- `src/lib/integrations/wizard-helpers.ts` - Shared helper: confirm guard + multiselect + configurePrompt loop
- `tests/lib/integrations/wizard-helpers.test.ts` - 5 unit tests for all 3 paths (decline, cancel, opt-in)
- `src/tui/template-wizard.ts` - Integration override step added to runTemplateNew and runTemplateEdit

## Decisions Made

- Helper accepts `initialEnabledIds` and `currentConfigs` parameters to allow callers (template wizard, workspace wizard in plan 02) to control pre-selection independently — global-config-based for new templates, template-override-based for edit
- `runTemplateEdit` integrations handler calls `return` early after processing, consistent with repos and description handlers in the same function
- No change to `cancel()` from utils.ts — the existing process.exit(0) pattern is correct for wizard cancellation

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None - typecheck passed clean on first attempt.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `promptIntegrationOverrides()` is ready for reuse in Plan 02 (workspace-wizard and workspace-clone)
- Template YAML `integrations` field correctly populated and ready for cascade display in Plan 03 (detail pane display)
- `readGlobalConfig`, `integrations`, `resolveEnabledGlobally` imports now in template-wizard.ts — Plan 02 will add same to workspace-wizard.ts

## Self-Check: PASSED

- FOUND: src/lib/integrations/wizard-helpers.ts
- FOUND: tests/lib/integrations/wizard-helpers.test.ts
- FOUND: .planning/phases/15.2-integration-overrides-per-template-and-workspace/15.2-01-SUMMARY.md
- FOUND commit: 18d085b (test RED)
- FOUND commit: 812930b (feat GREEN)
- FOUND commit: 3853d83 (feat task 2)

---
*Phase: 15.2-integration-overrides-per-template-and-workspace*
*Completed: 2026-03-21*
