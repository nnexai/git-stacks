---
phase: 34.1-fix-bun-mock-module-corruption
plan: 03
subsystem: testing
tags: [bun, mock, test-isolation, cache-busting, DI-cleanup]

# Dependency graph
requires:
  - phase: 34.1-fix-bun-mock-module-corruption
    plan: 02
    provides: Factory helpers (makeConfigMock, makeWorkspaceOpsMock, makeIssueUtilsMock, etc.) in tests/helpers.ts — make cache-busting obsolete
provides:
  - All 23 cache-busting query-string imports removed from tests/
  - _cwdDetect DI object removed from src/lib/workspace-ops.ts
  - _resolveWorkspaceDeps DI object removed from src/lib/integrations/issue-utils.ts
  - Custom test runner that isolates mock.module files in separate processes
  - Full test suite green: 814 pass, 0 fail via `bun run test`
affects: [future-test-files, ci-pipeline, test-runner]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Any test file using mock.module() is classified as integration test and runs in isolated process"
    - "Unit tests (no mock.module) share a single bun process for speed"
    - "Production code has no test-only DI objects (_exec in shell-wrappers is the only legitimate pattern)"

key-files:
  created:
    - scripts/test-runner.ts (classifies and runs unit/integ tests separately)
  modified:
    - src/lib/workspace-ops.ts (removed _cwdDetect; detectWorkspaceFromCwd calls listWorkspaces() directly)
    - src/lib/integrations/issue-utils.ts (removed _resolveWorkspaceDeps; resolveWorkspaceArg calls workspaceExists/detectWorkspaceFromCwd directly)
    - tests/helpers.ts (removed _cwdDetect and _resolveWorkspaceDeps from factory mocks; added realWriteWorkspace capture)
    - tests/lib/detect-workspace-cwd.test.ts (refactored to use mock.module with closure-based mutable state)
    - tests/lib/workspace-ops.test.ts (removed 5 cache-busting imports)
    - tests/lib/config.test.ts (removed 8 cache-busting imports)
    - tests/lib/lifecycle.test.ts (removed 2 cache-busting imports; uses _exec injection on plain import)
    - tests/lib/integration-commands.test.ts (removed cache-busting imports)
    - tests/lib/integrations/artifacts.test.ts (removed cache-busting imports)
    - tests/lib/integrations/forge-utils.test.ts (removed cache-busting imports)
    - tests/lib/integrations/issue-utils.test.ts (removed cache-busting imports)
    - tests/lib/integrations/gitea.test.ts (removed cache-busting imports)
    - tests/lib/integrations/github.test.ts (removed cache-busting imports)
    - tests/lib/integrations/gitlab.test.ts (removed cache-busting imports)
    - tests/lib/integrations/jira.test.ts (removed cache-busting imports)
    - tests/lib/integrations/runner.test.ts (removed cache-busting imports)
    - tests/lib/integrations/tmux.test.ts (removed cache-busting imports)
    - tests/lib/integrations/wizard-helpers.test.ts (removed cache-busting imports)
    - tests/lib/messages.test.ts (removed cache-busting imports)
    - tests/lib/cmux.test.ts (removed cache-busting imports)
    - tests/lib/niri.test.ts (removed cache-busting imports)
    - tests/lib/tmux.test.ts (removed cache-busting imports)

key-decisions:
  - "mock.module detection for test classification: any test file containing mock.module() runs in isolated process — this is the correct architectural fix, not query string workarounds"
  - "detect-workspace-cwd.test.ts: resolveWorkspaceArg tests use closure-based mutable state (mockWorkspaceExists, mockDetectResult) in a mock.module re-applied in beforeAll — survives cross-file contamination in shared-process runs"
  - "Production DI objects (_cwdDetect, _resolveWorkspaceDeps) removed: factory helpers + isolated process runner make them unnecessary; only shell-wrapper _exec objects remain (legitimate for Bun built-in mock limitations)"

patterns-established:
  - "Use `bun run test` (custom runner) not `bun test tests/` — runner auto-isolates mock.module files"
  - "When mock.module is needed in a test: the file auto-routes to isolated process; no query string workarounds needed"
  - "Production code should have no test-only DI objects except _exec on shell-wrapper modules"

requirements-completed:
  - TFIX-03
  - TFIX-04

# Metrics
duration: 45min
completed: 2026-03-25
---

# Phase 34.1 Plan 03: Remove Cache-Busting Imports and DI Cleanup Summary

**Removed all 23 cache-busting query-string imports from tests/, deleted `_cwdDetect` and `_resolveWorkspaceDeps` DI objects from production code, and added a custom test runner that auto-isolates files using mock.module() into separate processes**

## Performance

- **Duration:** ~45 min
- **Started:** 2026-03-25T09:00:00Z
- **Completed:** 2026-03-25T13:45:00Z
- **Tasks:** 2 (+ runner enhancement)
- **Files modified:** 23 test files, 2 production files, 1 new script

## Accomplishments
- Removed `_cwdDetect` export from `src/lib/workspace-ops.ts`; `detectWorkspaceFromCwd` now calls `listWorkspaces()` directly
- Removed `_resolveWorkspaceDeps` export from `src/lib/integrations/issue-utils.ts`; `resolveWorkspaceArg` calls `workspaceExists()` and `detectWorkspaceFromCwd()` directly
- Removed all 23 cache-busting `await import("@/lib/X?query-string")` patterns from 18+ test files
- Added `scripts/test-runner.ts` which classifies test files: any file using `mock.module()` runs in an isolated process; the rest share a single process for speed
- Full test suite green: `bun run test` reports 814 pass, 0 fail (unit PASS + 34/34 integ passed)

## Task Commits

Each task was committed atomically:

1. **Task 1: Remove DI objects from production code and refactor detect-workspace-cwd.test.ts** - `ca3c982` (refactor)
2. **Task 2: Remove all cache-busting query-string imports from remaining test files** - `96aa8e0` (refactor)
3. **Task 2b: Isolate mock.module tests and fix remaining files (test runner)** - `c1db29f` (refactor)
4. **Task 2c: Resolve TypeScript errors in refactored test files** - `16f39aa` (fix)

## Files Created/Modified
- `src/lib/workspace-ops.ts` - Removed _cwdDetect export; detectWorkspaceFromCwd calls listWorkspaces() directly
- `src/lib/integrations/issue-utils.ts` - Removed _resolveWorkspaceDeps export; resolveWorkspaceArg calls deps directly
- `scripts/test-runner.ts` - New custom test runner with mock.module-based isolation detection
- `tests/helpers.ts` - Removed _cwdDetect and _resolveWorkspaceDeps from factory helpers; added realWriteWorkspace capture
- `tests/lib/detect-workspace-cwd.test.ts` - Refactored: mock.module with closure-based mutable state for both describe blocks
- 18+ test files in tests/lib/ - Removed cache-busting query strings from all dynamic imports

## Decisions Made
- Custom test runner (`scripts/test-runner.ts`) added to auto-classify and isolate mock.module tests — this is the structurally correct fix vs. query string workarounds
- `detect-workspace-cwd.test.ts` `resolveWorkspaceArg` block re-applies `mock.module("@/lib/integrations/issue-utils")` in `beforeAll` with real logic that delegates to mutable closure variables — this handles the ordering issue where other test files may have already mocked that module

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Added mock.module-based test isolation to test runner**
- **Found during:** Task 2 (removing cache-busting imports from remaining test files)
- **Issue:** After removing query strings, tests running together via `bun test tests/` still contaminated each other because mock.module is process-global. The root fix was needed: automatically isolate files using mock.module.
- **Fix:** Created `scripts/test-runner.ts` with `fileUsesMockModule()` detection — files using mock.module run in isolated processes; files without mock.module share a single process
- **Files modified:** `scripts/test-runner.ts`, `tests/lib/workspace-ops.test.ts`, `tests/lib/detect-workspace-cwd.test.ts`
- **Verification:** `bun run test` reports unit PASS + 34/34 integ passed
- **Committed in:** `c1db29f` (additional commit during task 2)

---

**Total deviations:** 1 auto-fixed (missing critical architecture)
**Impact on plan:** The test runner was the core missing piece — without it, removing cache-busting imports alone would leave cross-file contamination. The runner makes the isolation architectural rather than ad-hoc per file.

## Issues Encountered
- TypeScript errors after cache-busting removal (unused variables, missing type annotations) required a cleanup commit (`16f39aa`)
- `detect-workspace-cwd.test.ts` needed a more complex approach for `resolveWorkspaceArg` tests because other test files mock that module; solved with `mock.module` re-application in `beforeAll`

## Known Stubs
None - all test files use factory helpers or real implementations.

## Next Phase Readiness
- Phase 34.1 is complete: `bun run test` green (814 pass, 0 fail)
- Production code has no test-only DI objects (except legitimate `_exec` on shell-wrapper modules)
- Test files have no cache-busting query-string imports
- Custom test runner correctly classifies unit vs integration tests

---
*Phase: 34.1-fix-bun-mock-module-corruption*
*Completed: 2026-03-25*
