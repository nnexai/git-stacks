---
phase: 34.1-fix-bun-mock-module-corruption
plan: 03
type: execute
wave: 3
depends_on: ["34.1-02"]
files_modified:
  - src/lib/workspace-ops.ts
  - src/lib/integrations/issue-utils.ts
  - tests/lib/detect-workspace-cwd.test.ts
  - tests/lib/lifecycle.test.ts
  - tests/lib/config.test.ts
  - tests/lib/workspace-ops.test.ts
  - tests/lib/integration-commands.test.ts
  - tests/lib/integrations/artifacts.test.ts
  - tests/lib/integrations/forge-utils.test.ts
  - tests/lib/integrations/issue-utils.test.ts
  - tests/lib/integrations/gitea.test.ts
  - tests/lib/integrations/github.test.ts
  - tests/lib/integrations/gitlab.test.ts
  - tests/lib/integrations/jira.test.ts
  - tests/lib/integrations/runner.test.ts
  - tests/lib/integrations/niri.test.ts
  - tests/lib/integrations/tmux.test.ts
  - tests/lib/integrations/wizard-helpers.test.ts
  - tests/lib/messages.test.ts
  - tests/lib/cmux.test.ts
  - tests/lib/niri.test.ts
  - tests/lib/tmux.test.ts
autonomous: true
requirements:
  - TFIX-03
  - TFIX-04

must_haves:
  truths:
    - "No cache-busting query-string imports exist in any test file"
    - "No `_cwdDetect` export exists in src/lib/workspace-ops.ts"
    - "No `_resolveWorkspaceDeps` export exists in src/lib/integrations/issue-utils.ts"
    - "All tests still pass after cleanup"
  artifacts:
    - path: "src/lib/workspace-ops.ts"
      provides: "detectWorkspaceFromCwd calls listWorkspaces() directly (no DI indirection)"
    - path: "src/lib/integrations/issue-utils.ts"
      provides: "resolveWorkspaceArg calls workspaceExists/detectWorkspaceFromCwd directly (no DI indirection)"
    - path: "tests/lib/detect-workspace-cwd.test.ts"
      provides: "Refactored to use mock.module instead of removed DI objects"
  key_links:
    - from: "src/lib/workspace-ops.ts"
      to: "src/lib/config.ts"
      via: "direct listWorkspaces() import (no _cwdDetect indirection)"
      pattern: "listWorkspaces\\(\\)"
    - from: "src/lib/integrations/issue-utils.ts"
      to: "src/lib/workspace-ops.ts"
      via: "direct detectWorkspaceFromCwd() import (no _resolveWorkspaceDeps indirection)"
      pattern: "detectWorkspaceFromCwd\\(\\)"
---

<objective>
Clean up all test isolation hacks from the codebase: remove all 23 cache-busting query-string imports from test files, remove `_cwdDetect` and `_resolveWorkspaceDeps` DI objects from production code, and refactor the affected test files to use mock.module with factory helpers instead.

Purpose: Eliminate the accumulated technical debt from previous failed isolation attempts. Production code should have no test-only DI objects (per D-06). Test files should have no query-string cache-busting imports (per D-07/D-08). The complete mock factories from Plan 01 make all of this safe.

Output: Cleaner production code, cleaner test code, same green test suite.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.1-fix-bun-mock-module-corruption-tests-are-passing-in-isolation-but-not-when-run-together-this-has-been-a-constant-problem-in-the-project-and-requires-a-real-fix/34.1-CONTEXT.md
@.planning/phases/34.1-fix-bun-mock-module-corruption-tests-are-passing-in-isolation-but-not-when-run-together-this-has-been-a-constant-problem-in-the-project-and-requires-a-real-fix/34.1-RESEARCH.md
@.planning/phases/34.1-fix-bun-mock-module-corruption-tests-are-passing-in-isolation-but-not-when-run-together-this-has-been-a-constant-problem-in-the-project-and-requires-a-real-fix/34.1-01-SUMMARY.md
@.planning/phases/34.1-fix-bun-mock-module-corruption-tests-are-passing-in-isolation-but-not-when-run-together-this-has-been-a-constant-problem-in-the-project-and-requires-a-real-fix/34.1-02-SUMMARY.md

<interfaces>
<!-- DI objects to remove -->
From src/lib/workspace-ops.ts (lines 1204-1207):
```typescript
/** Injectable deps for test isolation (same pattern as _exec in niri/tmux/cmux) */
export const _cwdDetect = {
  listWorkspaces: () => listWorkspaces(),
}
```
After removal, detectWorkspaceFromCwd (line 1219) changes from:
  `const workspaces = _cwdDetect.listWorkspaces()`
to:
  `const workspaces = listWorkspaces()`

From src/lib/integrations/issue-utils.ts (lines 92-95):
```typescript
export const _resolveWorkspaceDeps = {
  workspaceExists: (name: string) => workspaceExists(name),
  detectWorkspaceFromCwd: () => detectWorkspaceFromCwd(),
}
```
After removal, resolveWorkspaceArg changes from:
  `if (!_resolveWorkspaceDeps.workspaceExists(workspaceName))` → `if (!workspaceExists(workspaceName))`
  `const detection = _resolveWorkspaceDeps.detectWorkspaceFromCwd()` → `const detection = detectWorkspaceFromCwd()`

<!-- Factory helpers from tests/helpers.ts -->
```typescript
export function makeConfigMock(overrides?: Record<string, unknown>)
export function makeWorkspaceOpsMock(overrides?: Record<string, unknown>)
export function makeIssueUtilsMock(overrides?: Record<string, unknown>)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove DI objects from production code and refactor detect-workspace-cwd.test.ts</name>
  <files>
    src/lib/workspace-ops.ts,
    src/lib/integrations/issue-utils.ts,
    tests/lib/detect-workspace-cwd.test.ts
  </files>
  <read_first>
    - src/lib/workspace-ops.ts (lines 1195-1243 — _cwdDetect and detectWorkspaceFromCwd)
    - src/lib/integrations/issue-utils.ts (lines 88-127 — _resolveWorkspaceDeps and resolveWorkspaceArg)
    - tests/lib/detect-workspace-cwd.test.ts (full file — most complex refactor, uses both DI objects)
    - tests/helpers.ts (factory helpers for mock replacement)
  </read_first>
  <action>
### Production code changes (per D-06):

**1. `src/lib/workspace-ops.ts`** — Remove `_cwdDetect` export and inline:

Delete lines 1204-1207 (the `_cwdDetect` constant):
```typescript
// DELETE THIS:
export const _cwdDetect = {
  listWorkspaces: () => listWorkspaces(),
}
```

Change line 1219 in `detectWorkspaceFromCwd` from:
```typescript
const workspaces = _cwdDetect.listWorkspaces()
```
to:
```typescript
const workspaces = listWorkspaces()
```

Verify `listWorkspaces` is already imported at the top of `workspace-ops.ts` (it is — from `./config`). The `_cwdDetect` export type can also be removed if referenced in any type export.

Also remove `_cwdDetect` from the `makeWorkspaceOpsMock` factory in `tests/helpers.ts` — it will no longer be an export. (Check if Plan 01 included it; if so, remove it now.)

**2. `src/lib/integrations/issue-utils.ts`** — Remove `_resolveWorkspaceDeps` export and inline:

Delete lines 92-95 (the `_resolveWorkspaceDeps` constant):
```typescript
// DELETE THIS:
export const _resolveWorkspaceDeps = {
  workspaceExists: (name: string) => workspaceExists(name),
  detectWorkspaceFromCwd: () => detectWorkspaceFromCwd(),
}
```

Change line 111 from:
```typescript
if (!_resolveWorkspaceDeps.workspaceExists(workspaceName)) {
```
to:
```typescript
if (!workspaceExists(workspaceName)) {
```

Change line 117 from:
```typescript
const detection = _resolveWorkspaceDeps.detectWorkspaceFromCwd()
```
to:
```typescript
const detection = detectWorkspaceFromCwd()
```

Verify `workspaceExists` is imported from `../config` and `detectWorkspaceFromCwd` is imported from `../workspace-ops` at the top of the file. If not, add the imports.

Also remove `_resolveWorkspaceDeps` from the `makeIssueUtilsMock` factory in `tests/helpers.ts`.

### Test refactor:

**3. `tests/lib/detect-workspace-cwd.test.ts`** — This test currently uses BOTH removed DI objects:
- `_cwdDetect.listWorkspaces = () => [...]` to inject workspace data for `detectWorkspaceFromCwd` tests
- `_resolveWorkspaceDeps.workspaceExists = ...` and `_resolveWorkspaceDeps.detectWorkspaceFromCwd = ...` for `resolveWorkspaceArg` tests

After DI removal, this test must use `mock.module` instead:

**For `detectWorkspaceFromCwd` tests:**
The function now calls `listWorkspaces()` from `@/lib/config` directly. Use:
```typescript
import { makeConfigMock } from "../helpers"
mock.module("@/lib/config", () => makeConfigMock({
  listWorkspaces: mock(() => [/* workspace objects as currently defined in test */]),
}))
```
Then import `detectWorkspaceFromCwd` with a plain `await import("@/lib/workspace-ops")` (no cache-busting).

**For `resolveWorkspaceArg` tests:**
The function now calls `workspaceExists()` from `@/lib/config` and `detectWorkspaceFromCwd()` from `@/lib/workspace-ops` directly. Use:
```typescript
mock.module("@/lib/config", () => makeConfigMock({
  workspaceExists: mock((name: string) => name === "existing-ws"),
}))
mock.module("@/lib/workspace-ops", () => makeWorkspaceOpsMock({
  detectWorkspaceFromCwd: mock(() => ({ ok: true, workspace: { name: "detected-ws" } })),
}))
```
Then import `resolveWorkspaceArg` with a plain `await import("@/lib/integrations/issue-utils")` (no cache-busting).

**IMPORTANT:** Remove ALL cache-busting query strings from this file's imports:
- `@/lib/config?cwd-detect` → `@/lib/config`
- `@/lib/workspace-ops?cwd-detect` → `@/lib/workspace-ops`
- `@/lib/integrations/issue-utils?cwd-detect` → `@/lib/integrations/issue-utils`

The test file structure (describe blocks, test cases, assertions) should be preserved. Only the module import mechanism and DI injection mechanism change.
  </action>
  <verify>
    <automated>bun test tests/lib/detect-workspace-cwd.test.ts 2>&1 | tail -5 && grep -c "_cwdDetect\|_resolveWorkspaceDeps" src/lib/workspace-ops.ts src/lib/integrations/issue-utils.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "_cwdDetect" src/lib/workspace-ops.ts` produces no output
    - `grep "_resolveWorkspaceDeps" src/lib/integrations/issue-utils.ts` produces no output
    - `src/lib/workspace-ops.ts` line in detectWorkspaceFromCwd reads `const workspaces = listWorkspaces()` (no _cwdDetect)
    - `src/lib/integrations/issue-utils.ts` line in resolveWorkspaceArg reads `if (!workspaceExists(workspaceName))` (no _resolveWorkspaceDeps)
    - `tests/lib/detect-workspace-cwd.test.ts` does not contain `_cwdDetect` or `_resolveWorkspaceDeps`
    - `tests/lib/detect-workspace-cwd.test.ts` does not contain any `?` query-string cache-busting imports
    - `bun test tests/lib/detect-workspace-cwd.test.ts` passes
    - `bun run typecheck` passes (no type errors from removed exports)
  </acceptance_criteria>
  <done>Both DI objects removed from production code. detectWorkspaceFromCwd calls listWorkspaces() directly. resolveWorkspaceArg calls workspaceExists() and detectWorkspaceFromCwd() directly. detect-workspace-cwd.test.ts refactored to use mock.module with factory helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Remove all cache-busting query-string imports from remaining test files</name>
  <files>
    tests/lib/lifecycle.test.ts,
    tests/lib/config.test.ts,
    tests/lib/workspace-ops.test.ts,
    tests/lib/integration-commands.test.ts,
    tests/lib/integrations/artifacts.test.ts,
    tests/lib/integrations/forge-utils.test.ts,
    tests/lib/integrations/issue-utils.test.ts,
    tests/lib/integrations/gitea.test.ts,
    tests/lib/integrations/github.test.ts,
    tests/lib/integrations/gitlab.test.ts,
    tests/lib/integrations/jira.test.ts,
    tests/lib/integrations/runner.test.ts,
    tests/lib/integrations/niri.test.ts,
    tests/lib/integrations/tmux.test.ts,
    tests/lib/integrations/wizard-helpers.test.ts,
    tests/lib/messages.test.ts,
    tests/lib/cmux.test.ts,
    tests/lib/niri.test.ts,
    tests/lib/tmux.test.ts
  </files>
  <read_first>
    - Run: `grep -rn "await import.*@/.*?" tests/` to get the exact list of all 23 cache-busting imports and their locations
    - tests/lib/lifecycle.test.ts (has 2 cache-busting imports — special case per research Open Question 1)
    - tests/lib/config.test.ts (has 8 cache-busting imports — most in any single file)
    - tests/lib/workspace-ops.test.ts (has 5 cache-busting imports)
    - Each remaining file with cache-busting imports
  </read_first>
  <action>
Remove ALL remaining cache-busting query-string imports per D-07 and D-08. After Plan 02 fixed the partial mocks with factory helpers, cache-busting is no longer needed — the mock.module calls are now complete, so module re-imports get the same (correct) mock shape.

### Approach per file:

For each `await import("@/lib/X?query-string")`, determine the replacement:

**If the file uses `mock.module("@/lib/X", ...)` for module X:**
Replace `await import("@/lib/X?query-string")` with plain `await import("@/lib/X")`. The mock.module will intercept the import and provide the mocked version.

**If the file does NOT mock module X (imports it for real usage):**
Replace `await import("@/lib/X?query-string")` with either:
- `import { fn } from "@/lib/X"` (static import at top of file) if the import is in the top-level scope
- `await import("@/lib/X")` (plain dynamic import without query string) if the import is inside a describe/beforeEach

### Special cases:

**`tests/lib/lifecycle.test.ts`** (2 cache-busting imports):
- `@/lib/lifecycle?lifecycle-real` — This import gets the "real" lifecycle module for testing actual shell execution. Since `_exec` is a legitimate DI object (D-05 keeps it), the test can use `_exec` injection on a plain import. Replace with `await import("@/lib/lifecycle")`.
- `@/lib/lifecycle?lifecycle-exec-test` — Same module, used in a different describe block for `_exec` injection tests. Replace with plain `await import("@/lib/lifecycle")`. If both describe blocks need different behavior, they can both import the same module and set `_exec.spawn = mock(...)` independently in each describe's beforeEach.

**`tests/lib/config.test.ts`** (8 cache-busting imports):
This file uses `mock.module("@/lib/paths", ...)` at various levels and re-imports config with different query strings per describe block. After `makePathsMock` exists:
- Replace each `await import("@/lib/config?X")` with `await import("@/lib/config")`
- The paths mock (via `useIsolatedConfig()` or direct `mock.module("@/lib/paths", ...)`) will be active when config is imported
- Since mock.module is process-global and doesn't reset, the first paths mock will affect all subsequent config imports. This is fine — all describe blocks in this file set up paths before importing config.

**`tests/lib/workspace-ops.test.ts`** (5 cache-busting imports):
- `@/lib/lifecycle?ws-ops-lifecycle-real` — Replace with `await import("@/lib/lifecycle")`. The mock factory from Plan 02 ensures no partial lifecycle mock contaminates this.
- `@/lib/config?ws-ops-test`, `@/lib/git?ws-ops-test`, `@/lib/workspace-ops?ws-ops-test`, `@/lib/workspace-ops?ws-ops-edit-yaml-test` — Replace with plain `await import(...)` without query strings.
- Also look for `mock.module("@/lib/lifecycle", () => realLifecycle)` reset hack (line ~20) — this can be removed since integration-commands.test.ts now uses `makeLifecycleMock()` with complete exports.

**All other files:** Simple query-string removal. Replace `await import("@/lib/X?suffix")` with `await import("@/lib/X")`.

### Verification approach:
After all removals, run `grep -rn "await import.*@/.*?" tests/` — must return 0 results.
Then run `bun test tests/` — must still pass with 0 failures.
  </action>
  <verify>
    <automated>grep -rn 'await import.*@/.*?' tests/ | wc -l && bun test tests/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'await import.*@/.*?' tests/` returns 0 results (zero cache-busting imports remain)
    - `bun test tests/` shows 0 fail, 0 errors
    - `bun run test` via the custom runner shows all tests passing
    - `bun run typecheck` passes
    - `grep -rn "_cwdDetect\|_resolveWorkspaceDeps" src/` returns 0 results
    - `grep -rn "_cwdDetect\|_resolveWorkspaceDeps" tests/` returns 0 results
  </acceptance_criteria>
  <done>All 23 cache-busting query-string imports removed. All DI hack objects removed. Production code is clean. Full test suite green.</done>
</task>

</tasks>

<verification>
- `grep -rn 'await import.*@/.*?' tests/ | wc -l` returns 0
- `grep -rn "_cwdDetect\|_resolveWorkspaceDeps" src/ tests/` returns 0
- `bun test tests/ 2>&1 | tail -5` shows 0 fail, 0 errors
- `bun run test 2>&1 | tail -10` shows all tests passing via custom runner
- `bun run typecheck` passes
</verification>

<success_criteria>
1. Zero cache-busting query-string imports in the entire tests/ directory
2. `_cwdDetect` and `_resolveWorkspaceDeps` do not exist in src/ or tests/
3. `detectWorkspaceFromCwd` calls `listWorkspaces()` directly
4. `resolveWorkspaceArg` calls `workspaceExists()` and `detectWorkspaceFromCwd()` directly
5. Full test suite passes: `bun test tests/` and `bun run test` both green
</success_criteria>

<output>
After completion, create `.planning/phases/34.1-fix-bun-mock-module-corruption-tests-are-passing-in-isolation-but-not-when-run-together-this-has-been-a-constant-problem-in-the-project-and-requires-a-real-fix/34.1-03-SUMMARY.md`
</output>
