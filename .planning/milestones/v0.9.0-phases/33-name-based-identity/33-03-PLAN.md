---
phase: 33-name-based-identity
plan: 03
type: execute
wave: 1
depends_on: ["33-01", "33-02"]
files_modified: ["src/lib/workspace-ops.ts"]
autonomous: true
gap_closure: true
requirements: ["IDEN-01", "IDEN-02", "IDEN-03"]

must_haves:
  truths:
    - "removeWorkspace with malformed YAML and --force succeeds (D-12 test passes)"
    - "removeWorkspace with malformed YAML without --force returns 'Cannot parse' error (D-12 test passes)"
    - "All 60 workspace-ops tests pass (0 failures)"
  artifacts:
    - path: "src/lib/workspace-ops.ts"
      provides: "Fallback existsSync check in removeWorkspace when workspaceExists returns false"
      contains: "existsSync(workspacePath(name))"
  key_links:
    - from: "src/lib/workspace-ops.ts:removeWorkspace"
      to: "src/lib/config.ts:workspacePath"
      via: "existsSync fallback check"
      pattern: "existsSync\\(workspacePath\\(name\\)\\)"
---

<objective>
Fix the backward compatibility regression in removeWorkspace where scan-based workspaceExists returns false for corrupt YAML files, making the D-12 malformed-YAML handling path unreachable.

Purpose: Restore the D-12 corrupt-file recovery path that was broken when workspaceExists switched from filename-based existsSync to scan-based YAML parsing.
Output: Patched removeWorkspace function; all 60 workspace-ops tests pass.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-name-based-identity/33-VERIFICATION.md
@.planning/phases/33-name-based-identity/33-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add existsSync fallback in removeWorkspace for corrupt YAML files</name>
  <files>src/lib/workspace-ops.ts</files>
  <read_first>
    - src/lib/workspace-ops.ts (lines 419-453 — the removeWorkspace function and D-12 handling)
    - src/lib/config.ts (lines 244-256 — workspacePath and workspaceExists definitions)
    - tests/lib/workspace-ops.test.ts (search for "D-12" to find the two failing tests)
  </read_first>
  <action>
In `src/lib/workspace-ops.ts`, modify the `removeWorkspace` function at line 424.

Current code (lines 424-426):
```typescript
if (!workspaceExists(name)) {
  return { ok: false, error: `Workspace '${name}' not found.` }
}
```

Replace with:
```typescript
if (!workspaceExists(name)) {
  // Scan-based lookup returns false for corrupt YAML — fall back to filesystem check
  // so the D-12 malformed-YAML handling path below remains reachable
  if (!existsSync(workspacePath(name))) {
    return { ok: false, error: `Workspace '${name}' not found.` }
  }
}
```

No new imports needed — `existsSync` (line 1) and `workspacePath` (line 8) are already imported.

This allows removeWorkspace to fall through to the try/catch at line 428 when the file exists on disk but has corrupt YAML, restoring the D-12 behavior where:
- Without --force: returns `{ ok: false, error: "Cannot parse workspace YAML..." }`
- With --force: deletes the directory and config file, returns `{ ok: true }`
  </action>
  <verify>
    <automated>cd /home/nnex/dev/prj/git-stacks && bun test tests/lib/workspace-ops.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "existsSync(workspacePath(name))" src/lib/workspace-ops.ts returns a match inside the removeWorkspace function (around line 426)
    - bun test tests/lib/workspace-ops.test.ts exits 0 with 60 pass, 0 fail
    - bun run typecheck exits 0
  </acceptance_criteria>
  <done>All 60 workspace-ops tests pass including both D-12 malformed YAML scenarios. The removeWorkspace function correctly handles corrupt YAML files by falling back to filesystem existence check.</done>
</task>

</tasks>

<verification>
1. `bun test tests/lib/workspace-ops.test.ts` — all 60 tests pass, 0 fail
2. `bun test tests/lib/config.test.ts` — all 41 config tests still pass (no regression)
3. `bun run typecheck` — exits 0
</verification>

<success_criteria>
- Both D-12 tests pass: "removeWorkspace --force with malformed YAML succeeds" and "removeWorkspace without --force and malformed YAML returns error"
- No other test regressions
- Type-check passes
</success_criteria>

<output>
After completion, create `.planning/phases/33-name-based-identity/33-03-SUMMARY.md`
</output>
