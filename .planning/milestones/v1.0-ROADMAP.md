---
milestone: v1.0
published_as: v0.2.0
archived: 2026-03-18
status: complete
phases: 7
plans: 21
requirements: 61
commits: 169
files_changed: 494
lines_added: 116293
lines_removed: 1166
timeline_start: 2026-03-17
timeline_end: 2026-03-18
---

# Milestone Archive: v1.0 → v0.2.0

**Project:** git-stacks
**GSD Milestone:** v1.0
**Published as:** v0.2.0 (npm / semver)
**Archived:** 2026-03-18
**Status:** Complete — 61/61 requirements satisfied, 7/7 phases passed

---

## Milestone Goal

Harden the PoC into a tool you can trust: stable config schemas that survive upgrades, a test safety net for git operations, destructive commands that cannot corrupt state, a full design decision replacing the Stack model with Repo Registry + Templates, and UX polish that makes the tool feel complete.

---

## Key Accomplishments

1. **Foundation hardened** — Test infrastructure with real git repos (`makeGitRepo`), Zod schema resilience with human-readable errors, prerequisite checks (git version, integration binaries), and 5 live bug fixes (atomic merge/remove/rename, mergeNoFF detached HEAD)
2. **File ops engine** — Copy/symlink operations with glob expansion at workspace-instance and per-repo levels; idempotent re-application on `new` and `open`; loud-fail semantics
3. **Dynamic version** — `git-stacks -V` shows `package.json` version + git commit hash + `-dirty` flag when running from source
4. **Destructive op safety** — `--dry-run` and `--force` flags on `remove`, `clean`, `merge`, `rename`; consistent prompt gating; external file warnings before teardown
5. **Stack model replaced** — Repo Registry + Template + Workspace primitives fully implemented; Stack schemas, commands, and TUI wizards entirely removed; workspace YAML self-contained at creation time
6. **UX & execution** — `formatError` with actionable hints, `--json` on `status`/`doctor`/`sync`, `doctor --fix`, richer `list` columns, `run --parallel`
7. **Tech debt closed** — Full `openWorkspace()` lifecycle on "open now?", typed `Workspace` object in new flow, `mergeWorkspace` external file warnings, all dead code removed

---

## Phase Summary

| Phase | Goal | Plans | Requirements | Completed |
|-------|------|-------|--------------|-----------|
| 01-foundation | Stable schemas, test infra, live bug fixes | 5/5 | TEST-01-03, CONF-01-04, PREREQ-01-03, BUG-01-04 | 2026-03-17 |
| 01.1-file-ops | Copy/symlink engine with glob, idempotent re-application | 2/2 | FILES-01-15, SCHEMA-01-04, IDEMPOTENT-01-02 | 2026-03-17 |
| 01.2-version-command | Dynamic version from package.json + git hash | 1/1 | VER-01 | 2026-03-17 |
| 02-safety | Dry-run, --force, consistent destructive op safety | 2/2 | SAFE-01-03, FILES-16-17 | 2026-03-17 |
| 03-design | Stack replaced by Registry+Template model | 5/5 | DESIGN-01-02, REPO-01-04, TMPL-01-05 | 2026-03-18 |
| 04-ux-and-execution | Errors, --json, doctor --fix, richer list, parallel run | 4/4 | UX-01-04, RUN-01 | 2026-03-18 |
| 05-tech-debt-cleanup | Lifecycle fix, type fix, merge warnings, dead code | 2/2 | DEBT-01-04 | 2026-03-18 |

---

## Full Roadmap (at time of archive)

### Overview

The PoC is working. This roadmap hardens it into a tool you can trust: stable config schemas that survive upgrades, a test safety net for git operations, destructive commands that cannot corrupt state, a design decision on whether Stacks become Templates backed by a Repo Registry, and the UX polish that makes the tool feel complete. Four phases, each delivering a coherent capability before the next begins.

---

### Phase 1: Foundation
**Goal**: The existing PoC does not break mysteriously — corrupt configs are handled gracefully, schema upgrades never break existing user files, git operations have test coverage, and known live bugs are fixed
**Requirements**: PREREQ-01, PREREQ-02, PREREQ-03, TEST-01, TEST-02, TEST-03, CONF-01, CONF-02, CONF-03, CONF-04, BUG-01, BUG-02, BUG-03, BUG-04
**Success Criteria**:
1. A corrupt or invalid YAML file in workspaces/ or stacks/ causes a stderr warning and is skipped — no crash, all valid entries still load
2. Adding a new field to a Stack or Workspace Zod schema without a .default() is caught by a CI test before it ships
3. Core git operations (createWorktree, removeWorktree, mergeNoFF, rebaseBranch) are covered by integration tests using real local git repos
4. git-stacks fails fast with a clear install instruction when git is absent or below version 2.24
5. mergeWorkspace, removeWorkspace, and renameWorkspace cannot leave workspace YAML deleted or git worktrees unregistered after a mid-operation failure

Plans:
- [x] 01-01-PLAN.md — Test infrastructure: makeGitRepo helper + git.ts integration tests (TEST-01, TEST-02)
- [x] 01-02-PLAN.md — Config resilience: formatZodError, safeParse, schema_version, shape guard (CONF-01-04)
- [x] 01-03-PLAN.md — Prerequisite checks: git version check, integration binary guards, doctor reporting (PREREQ-01-03)
- [x] 01-04-PLAN.md — Bug fixes: mergeNoFF detached HEAD, atomic merge/remove/clean/rename (BUG-01-04)
- [x] 01-05-PLAN.md — Workspace lifecycle integration tests (TEST-03)

---

### Phase 01.2: Version command enhancement (INSERTED)
**Goal:** The -V/--version flag shows the real package.json version and git commit hash (with dirty flag) when running from source
**Requirements**: VER-01

Plans:
- [x] 01.2-01-PLAN.md — Dynamic version string from package.json + git hash + dirty flag (VER-01)

---

### Phase 01.1: File and folder copy/symlink support (INSERTED)
**Goal:** Stack and workspace configs support a `files:` block with `copy` and `symlink` arrays at workspace-instance and per-repo levels, with loud-fail error semantics and idempotent re-application
**Requirements**: FILES-01-15, SCHEMA-01-04, IDEMPOTENT-01-02

Plans:
- [x] 01.1-01-PLAN.md — Core file ops engine: schema extensions, files.ts rewrite, full test coverage
- [x] 01.1-02-PLAN.md — Wire file ops into workspace-wizard.ts (new) and workspace-ops.ts (open)

---

### Phase 2: Safety
**Goal**: Users can safely run remove, clean, and merge knowing exactly what will happen before it does, with consistent --force semantics
**Requirements**: SAFE-01, SAFE-02, SAFE-03, FILES-16, FILES-17

Plans:
- [x] 02-01-PLAN.md — Ops layer: warnExternalFiles + dryRun on all four ops functions (SAFE-01, FILES-16, FILES-17)
- [x] 02-02-PLAN.md — Command layer: --dry-run flags, --force prompt gating, rename gains both flags (SAFE-02, SAFE-03)

---

### Phase 3: Design and Conditional Implementation
**Goal**: Stacks are replaced by a three-primitive model (Repo Registry, Template, Workspace) — design decision documented, all REPO-* and TMPL-* requirements implemented, Stack code fully removed
**Requirements**: DESIGN-01, DESIGN-02, REPO-01-04, TMPL-01-05

Plans:
- [x] 03-01-PLAN.md — Schema foundation: design doc, new schemas (Registry, Template), Stack removal
- [x] 03-02-PLAN.md — Repo registry commands and TUI wizard
- [x] 03-03-PLAN.md — Template commands and TUI wizard
- [x] 03-04-PLAN.md — Workspace ops migration from Stack to Registry model
- [x] 03-05-PLAN.md — Workspace commands, wizard rewrite, --from, --recreate, test updates

---

### Phase 4: UX and Execution
**Goal**: The tool communicates clearly, works with scripts and agents, and surfaces workspace health at a glance
**Requirements**: UX-01-04, RUN-01

Plans:
- [x] 04-01-PLAN.md — formatError helper + error message audit across commands (UX-01)
- [x] 04-02-PLAN.md — List richer default columns, last_opened tracking, status --json (UX-04, UX-02)
- [x] 04-03-PLAN.md — Doctor --json, --fix, --force flags (UX-02, UX-03)
- [x] 04-04-PLAN.md — Run --parallel concurrent execution, sync --json (RUN-01, UX-02)

---

### Phase 5: Tech Debt Cleanup
**Goal:** Fix open-now lifecycle bypass, workspace type contract in new flow, mergeWorkspace external file warnings, and dead code removal
**Requirements**: DEBT-01-04

Plans:
- [x] 05-01-PLAN.md — Fix lifecycle bypass in workspace-wizard.ts and workspace-clone.ts + type contract fix
- [x] 05-02-PLAN.md — Add warnExternalFiles to mergeWorkspace + dead code removal

---

## Tech Debt at Archive

| Item | Severity | Description |
|------|----------|-------------|
| Nyquist VALIDATION.md drafts | info | 6 phases have draft VALIDATION.md files (nyquist_compliant: false); Phase 01.2 has none |
| `listRegistryEntries` orphan | info | Exported from config.ts but never imported by any consumer — dead API surface |
| Missing test: mergeWorkspace + warnExternalFiles | info | Code correct since Phase 05; coverage gap only |
| REQUIREMENTS.md traceability stale labels | info | DEBT-01-04 show "Planned" in table; body [x] and VERIFICATION correct |

---

_Archived: 2026-03-18_
_Audit: v1.0-MILESTONE-AUDIT.md (status: tech_debt, all requirements satisfied)_
