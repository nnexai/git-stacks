---
phase: 46-release-prep
plan: 01
type: standard
wave: 1
depends_on: []
files_modified:
  - package.json
  - CHANGELOG.md
  - README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "`package.json` `version` field reads `\"0.11.0\"`"
    - "CHANGELOG.md has a `## [0.11.0]` section at the top (above `## [0.10.1]`) dated with the release date"
    - "CHANGELOG.md v0.11.0 entry covers four features: AeroSpace shell wrappers, core integration plugin, layout control & normalization, app launching with snapshot-delta detection"
    - "CHANGELOG.md follows the existing Keep a Changelog format: `### Added` subsection with bold title + 2-3 sentence descriptions"
    - "README.md documents AeroSpace integration in the Integrations section with a YAML config example showing `workspace`, `layout`, `normalization`, `flatten_before_open`, `focus`, and `commands` fields"
    - "README.md AeroSpace section follows the niri documentation pattern: brief description, bullet points for key behaviors, YAML example"
    - "README.md Integrations table includes AeroSpace as a tier-3 integration"
  artifacts:
    - path: "package.json"
      provides: "Version 0.11.0"
      contains: "\"version\": \"0.11.0\""
    - path: "CHANGELOG.md"
      provides: "v0.11.0 release notes"
      contains: "## [0.11.0]"
    - path: "README.md"
      provides: "AeroSpace integration documentation"
      contains: "AeroSpace"
  key_links:
    - from: "CHANGELOG.md"
      to: "package.json"
      via: "Version number must match"
      pattern: "0.11.0"
---

<objective>
Release preparation for v0.11.0: verify version in package.json, add CHANGELOG entry covering all four AeroSpace features (shell wrappers, core plugin, layout control, app launching), and update README with AeroSpace integration documentation and config YAML example.

Purpose: Ship v0.11.0 as a documented release with the AeroSpace integration discoverable by users reading the CHANGELOG and README.

Output: Verified package.json (version), updated CHANGELOG.md (new release section), and updated README.md (AeroSpace integration docs).
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-release-prep/46-CONTEXT.md
@.planning/phases/43-aerospace-shell-wrappers-doctor/43-CONTEXT.md
@.planning/phases/44-core-integration-plugin/44-CONTEXT.md
@.planning/phases/45-layout-control-app-launching/45-CONTEXT.md
@.planning/milestones/v0.10.0-phases/41-release-prep/41-01-PLAN.md

@package.json
@CHANGELOG.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify version in package.json is 0.11.0</name>
  <files>package.json</files>
  <read_first>
    - package.json (current version field — should already read "0.11.0")
  </read_first>
  <action>
Read `package.json` and verify the `version` field reads `"0.11.0"`.

If the version already reads `"0.11.0"` — no edit needed, move to Task 2.

If the version reads something else (e.g., `"0.10.1"`), edit it:
- Old: `"version": "0.10.1"`
- New: `"version": "0.11.0"`

No other fields in package.json should be changed.

Commit (only if changed): `chore(46-01): bump version to 0.11.0`
  </action>
  <acceptance_criteria>
    - `package.json` contains `"version": "0.11.0"`
    - `grep '"version": "0.11.0"' package.json` returns a match
    - No other fields in package.json were modified
  </acceptance_criteria>
  <verify>
    <automated>grep '"version": "0.11.0"' package.json</automated>
  </verify>
  <done>package.json version field reads "0.11.0".</done>
</task>

<task type="auto">
  <name>Task 2: Add v0.11.0 section to CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (lines 1-40 — existing format: `## [version] — date` headers, `### Added` / `### Security` / `### Fixed` subsections, bold title + 2-3 sentence description per feature)
    - .planning/phases/43-aerospace-shell-wrappers-doctor/43-CONTEXT.md (Phase 43 feature summary: typed async CLI wrappers with injectable `_exec`, platform-gated detection, doctor binary check)
    - .planning/phases/44-core-integration-plugin/44-CONTEXT.md (Phase 44 feature summary: snapshot-delta window detection, workspace validation, window movement, tier-3 plugin registration)
    - .planning/phases/45-layout-control-app-launching/45-CONTEXT.md (Phase 45 feature summary: normalization-aware layout, flatten-before-open, workspace focus, commands array with delta detection)
    - .planning/phases/46-release-prep/46-CONTEXT.md (D-04 and D-05 — format and feature list decisions)
  </read_first>
  <action>
Insert a new `## [0.11.0]` section at the top of CHANGELOG.md, between the first `---` separator (line 5) and the existing `## [0.10.1]` section (line 7).

The new section to insert (use today's date in YYYY-MM-DD format):

```markdown
## [0.11.0] — 2026-03-28

### Added

**AeroSpace shell wrappers** — typed async CLI wrappers for `aerospace` commands (`list-windows`, `list-workspaces`, `move-node-to-workspace`, `focus`, `layout`, `flatten-workspace-tree`) with injectable `_exec` for test isolation. Platform-gated: silently skips on non-macOS. `git-stacks doctor` reports `aerospace` binary availability as a warn-level check on macOS.

**AeroSpace integration plugin** — tier-3 integration (order 31, disabled by default) that detects newly opened windows via snapshot-delta and moves them to a configured AeroSpace workspace. Configure `settings.integrations.aerospace.workspace` in workspace or template YAML to set the target workspace. Validates the target workspace exists before attempting moves.

**AeroSpace layout control** — normalization-aware layout management for the target AeroSpace workspace. Set `layout` to `h_tiles`, `v_tiles`, `h_accordion`, or `v_accordion`. Enable `flatten_before_open` to reset nested containers before window placement. `normalization: true` (default) uses `flatten-workspace-tree` + `layout` commands; `normalization: false` uses `split`-based alternatives. `focus: true` switches to the target workspace after setup.

**AeroSpace app launching** — a `commands` array in AeroSpace integration config launches arbitrary apps whose windows are automatically detected via snapshot-delta and moved to the target workspace. Supports `app` (direct spawn), `command` (shell string), `source` (reuse prior integration windows), `repo` (resolves to task path for cwd), and environment variable expansion (`$GS_WORKSPACE_NAME`, `$GS_WORKSPACE_PATH`, etc.).

---
```

The inserted text goes between line 5 (`---`) and line 7 (`## [0.10.1]`). The result should be:
- Line 5: `---` (existing)
- Line 6: empty line
- Lines 7-22: new v0.11.0 section
- Line 23: `---` (separator before 0.10.1)
- Line 24: empty line
- Line 25: `## [0.10.1] — 2026-03-28` (existing, shifted down)

Commit: `docs(46-01): add v0.11.0 CHANGELOG entry`
  </action>
  <acceptance_criteria>
    - CHANGELOG.md contains `## [0.11.0]`
    - The `## [0.11.0]` section appears BEFORE `## [0.10.1]`
    - CHANGELOG.md contains `**AeroSpace shell wrappers**`
    - CHANGELOG.md contains `**AeroSpace integration plugin**`
    - CHANGELOG.md contains `**AeroSpace layout control**`
    - CHANGELOG.md contains `**AeroSpace app launching**`
    - CHANGELOG.md contains `### Added` under the `## [0.11.0]` header
    - `grep -c "## \[0.11.0\]" CHANGELOG.md` returns `1`
    - The section follows Keep a Changelog format matching v0.10.0 style (bold title + description)
  </acceptance_criteria>
  <verify>
    <automated>grep "## \[0.11.0\]" CHANGELOG.md && grep "AeroSpace shell wrappers" CHANGELOG.md && grep "AeroSpace integration plugin" CHANGELOG.md && grep "AeroSpace layout control" CHANGELOG.md && grep "AeroSpace app launching" CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG.md has a complete v0.11.0 section with all four AeroSpace features documented.</done>
</task>

<task type="auto">
  <name>Task 3: Add AeroSpace integration to README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (lines 234-290 — existing Integrations section: integration table, niri documentation pattern with bullet points and YAML example)
    - .planning/phases/46-release-prep/46-CONTEXT.md (D-01 through D-03 — README documentation decisions)
    - .planning/phases/45-layout-control-app-launching/45-CONTEXT.md (D-01, D-02 — config schema with YAML example)
  </read_first>
  <action>
Make two additions to README.md in the Integrations section:

**Addition 1: Add AeroSpace row to the integration table (line 244)**

After the niri row:
```
| 3 | niri | Arranges windows on a named niri workspace | — |
```

Insert a new row:
```
| 3 | AeroSpace | Moves windows to a named AeroSpace workspace (macOS) | — |
```

This goes BEFORE the tier-5 forge integrations (GitHub row).

**Addition 2: Add AeroSpace integration section**

After the niri YAML example block (after line 285 — the closing triple-backtick of the niri YAML example), insert a new section:

```markdown

**AeroSpace integration** (for [AeroSpace](https://nikitabobko.github.io/AeroSpace/) tiling window manager on macOS):
- Moves newly opened windows (VSCode, IntelliJ, etc.) to a dedicated AeroSpace workspace per git-stacks workspace
- Normalization-aware layout: `flatten-workspace-tree` + `layout` when normalization is on (default), `split`-based alternatives when off
- `flatten_before_open` resets nested containers for clean repeated opens
- `commands` array launches arbitrary apps with automatic snapshot-delta window detection and movement
- Requires `aerospace` binary on macOS — silently skips on Linux/Windows
- `git-stacks doctor` reports binary availability as a warn-level check on macOS

```yaml
# Example AeroSpace config in workspace YAML
settings:
  integrations:
    aerospace:
      workspace: "2"
      layout: h_tiles
      normalization: true
      flatten_before_open: true
      focus: true
      commands:
        - source: vscode
        - app: kitty
          cwd: "$GS_WORKSPACE_PATH"
        - command: "open -a Firefox"
```
```

The two additions preserve all existing README content. The AeroSpace section follows the niri documentation pattern: brief description, bullet points for key behaviors, YAML config example.

Commit: `docs(46-01): add AeroSpace integration docs to README`
  </action>
  <acceptance_criteria>
    - README.md Integrations table contains `AeroSpace` row with `Moves windows to a named AeroSpace workspace (macOS)`
    - README.md contains `**AeroSpace integration**` as a section heading
    - README.md contains `aerospace:` in a YAML example block
    - README.md contains `workspace: "2"` in the YAML example
    - README.md contains `layout: h_tiles` in the YAML example
    - README.md contains `normalization: true` in the YAML example
    - README.md contains `flatten_before_open: true` in the YAML example
    - README.md contains `focus: true` in the YAML example
    - README.md contains `commands:` in the YAML example
    - README.md mentions `macOS` in the AeroSpace context
    - README.md mentions `git-stacks doctor` in AeroSpace context
    - `grep "AeroSpace" README.md | wc -l` returns at least 3 matches (table + section heading + description)
    - The niri section is unchanged (content above the insertion point preserved)
    - The forge integrations section is unchanged (content below insertion point preserved)
  </acceptance_criteria>
  <verify>
    <automated>grep "AeroSpace" README.md && grep "aerospace:" README.md && grep "workspace: \"2\"" README.md && grep "layout: h_tiles" README.md && grep "normalization: true" README.md && grep "flatten_before_open: true" README.md && grep "focus: true" README.md && grep "commands:" README.md</automated>
  </verify>
  <done>README.md updated with AeroSpace integration documentation including table row, description, bullet points, and YAML config example.</done>
</task>

</tasks>

<verification>
```bash
# Version
grep '"version": "0.11.0"' package.json

# CHANGELOG has v0.11.0 entry
grep "## \[0.11.0\]" CHANGELOG.md

# CHANGELOG covers all four features
grep "AeroSpace shell wrappers" CHANGELOG.md
grep "AeroSpace integration plugin" CHANGELOG.md
grep "AeroSpace layout control" CHANGELOG.md
grep "AeroSpace app launching" CHANGELOG.md

# README has AeroSpace in integration table
grep "AeroSpace.*macOS" README.md

# README has AeroSpace section
grep "AeroSpace integration" README.md

# README has YAML config example with all fields
grep "aerospace:" README.md
grep "workspace:" README.md
grep "layout: h_tiles" README.md
grep "normalization: true" README.md
grep "flatten_before_open: true" README.md
grep "focus: true" README.md
grep "commands:" README.md

# Typecheck still passes (no code changes, but verify)
bun run typecheck
```
</verification>

<success_criteria>
1. `package.json` version field reads `0.11.0`
2. CHANGELOG.md has a `## [0.11.0]` section covering all four features: shell wrappers, core plugin, layout control, app launching
3. README.md documents AeroSpace integration with a config YAML example showing `workspace`, `layout`, `normalization`, `flatten_before_open`, `focus`, and `commands` fields
4. README.md Integrations table includes AeroSpace as tier-3 integration
5. All existing content in CHANGELOG and README is preserved (no regressions)
6. Typecheck still passes
</success_criteria>

<output>
After completion, create `.planning/phases/46-release-prep/46-01-SUMMARY.md`
</output>
