---
phase: 68-release-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - CHANGELOG.md
  - README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "`package.json` `version` field reads `\"0.15.0\"`"
    - "`git-stacks --version` outputs `0.15.0`"
    - "CHANGELOG.md has a `## [0.15.0]` section at the top (above `## [0.14.0]`) with today's date"
    - "CHANGELOG.md v0.15.0 entry covers dir mode as a cohesive feature area spanning Phases 64-67"
    - "CHANGELOG.md follows Keep a Changelog format with bold title + description matching existing style"
    - "No 'unreleased' marker appears in CHANGELOG.md"
    - "README.md has a standalone `## Dir Repos` section explaining the concept and expected behavior"
    - "README.md Concepts section mentions dir mode as a third mode alongside worktree and trunk"
  artifacts:
    - path: "package.json"
      provides: "Version 0.15.0"
      contains: "\"version\": \"0.15.0\""
    - path: "CHANGELOG.md"
      provides: "v0.15.0 release notes"
      contains: "## [0.15.0]"
    - path: "README.md"
      provides: "Dir repos documentation"
      contains: "## Dir Repos"
  key_links:
    - from: "CHANGELOG.md"
      to: "package.json"
      via: "Version number must match"
      pattern: "0.15.0"
    - from: "README.md"
      to: "CHANGELOG.md"
      via: "Dir mode feature coverage consistency"
      pattern: "dir"
---

<objective>
Release preparation for v0.15.0: bump version in package.json, add CHANGELOG section covering the dir mode feature from Phases 64-67 (schema/registry, lifecycle, git guards, display/health), and update README with dir repos documentation.

Purpose: Ship v0.15.0 as a documented release with the dir mode feature discoverable by users reading the CHANGELOG and README.

Output: Updated package.json (version), CHANGELOG.md (new release section), and README.md (Concepts update + standalone Dir Repos section).
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-release-prep/68-CONTEXT.md
@.planning/phases/64-schema-registry/64-CONTEXT.md
@.planning/phases/65-workspace-lifecycle/65-CONTEXT.md
@.planning/phases/66-git-operation-guards/66-CONTEXT.md
@.planning/phases/67-status-display-health/67-CONTEXT.md

@package.json
@CHANGELOG.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version in package.json to 0.15.0</name>
  <files>package.json</files>
  <read_first>
    - package.json (current version field — reads "0.14.0-rc.0" on line 3)
    - src/lib/version.ts (confirm it reads version dynamically from package.json — no other files need updating)
  </read_first>
  <action>
Read `package.json` and change the `version` field from `"0.14.0-rc.0"` to `"0.15.0"`.

Edit:
- Old: `"version": "0.14.0-rc.0"`
- New: `"version": "0.15.0"`

No other fields in package.json should be changed. Per D-01, `src/lib/version.ts` reads version dynamically at runtime via `Bun.file()` so no other files need version updates.

Verify with: `bun run src/index.ts --version` — should output `0.15.0`.

Commit: `chore(68-01): bump version to 0.15.0`
  </action>
  <acceptance_criteria>
    - `package.json` contains `"version": "0.15.0"`
    - `grep '"version": "0.15.0"' package.json` returns a match
    - `bun run src/index.ts --version` outputs `0.15.0`
    - No other fields in package.json were modified
  </acceptance_criteria>
  <verify>
    <automated>grep '"version": "0.15.0"' package.json && bun run src/index.ts --version 2>&1 | grep "0.15.0"</automated>
  </verify>
  <done>package.json version field reads "0.15.0" and CLI outputs correct version.</done>
</task>

<task type="auto">
  <name>Task 2: Add v0.15.0 section to CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (lines 1-40 — existing format: `## [version] — date` headers, `### Added` / `### Fixed` / `### Changed` subsections, bold title + description per feature)
    - .planning/phases/64-schema-registry/64-CONTEXT.md (Phase 64: type "dir" in Zod schemas, mode "dir" in workspace YAML, repo add/scan accept plain directories)
    - .planning/phases/65-workspace-lifecycle/65-CONTEXT.md (Phase 65: open/close/clean/remove handle dir repos — reference main_path, no worktrees, hooks+env include dir paths)
    - .planning/phases/66-git-operation-guards/66-CONTEXT.md (Phase 66: push/pull/sync/merge skip dir repos, ahead/behind+dirty skip dir repos)
    - .planning/phases/67-status-display-health/67-CONTEXT.md (Phase 67: status shows [dir] label with no git metrics, list works with dir-only workspaces, TUI shows dir indicator, doctor validates directory existence)
    - .planning/phases/68-release-prep/68-CONTEXT.md (D-02 and D-03 — CHANGELOG format decisions)
  </read_first>
  <action>
Insert a new `## [0.15.0]` section at the top of CHANGELOG.md, between the first `---` separator (line 5) and the existing `## [0.14.0]` section (line 7). Per D-02, use a single entry with `### Added` subsection documenting dir mode as a cohesive feature area. Per D-03, use bold feature title + description paragraph matching the v0.14.0 style.

The new section to insert (use today's date — the date the release is prepared):

```markdown
## [0.15.0] — 2026-04-05

### Added

**Dir repos** — non-git directories can now be registered and included in workspaces alongside regular git repos. `git-stacks repo add /some/plain/dir` registers a plain directory as a "dir" type repo, and `git-stacks repo scan` offers non-git directories for registration alongside detected git repos. Templates reference dir repos like any other repo; workspaces store them with `mode: "dir"` and `main_path` only — no worktree is created, no branch is set, and no git operations are attempted.

Dir repos are included in workspace lifecycle: `open` injects their paths into hook and env context, while `close`, `clean`, and `remove` complete without errors (nothing to delete). All git-aware commands — `push`, `pull`, `sync`, `merge`, ahead/behind tracking, and dirty-file detection — silently skip dir repos. `git-stacks status` shows dir repos with a `[dir]` label and no git metrics, `git-stacks list` handles workspaces containing only dir repos without aggregation errors, and the TUI dashboard renders a `[dir]` indicator with no git badges. `git-stacks doctor` skips git health checks for dir repos and instead validates that the referenced directory exists and is accessible.

---
```

The inserted text goes between line 5 (`---`) and line 7 (`## [0.14.0]`). The result should be:
- Line 5: `---` (existing)
- Line 6: empty line
- Lines 7+: new v0.15.0 section (including trailing `---` separator)
- Then: empty line
- Then: `## [0.14.0] — 2026-04-03` (existing, shifted down)

Commit: `docs(68-01): add v0.15.0 CHANGELOG entry`
  </action>
  <acceptance_criteria>
    - CHANGELOG.md contains `## [0.15.0]`
    - The `## [0.15.0]` section appears BEFORE `## [0.14.0]`
    - CHANGELOG.md contains `**Dir repos**`
    - CHANGELOG.md contains `### Added` under the `## [0.15.0]` header
    - CHANGELOG.md contains `mode: "dir"` in the dir repos description
    - CHANGELOG.md contains `repo add` in the dir repos description
    - CHANGELOG.md contains `repo scan` in the dir repos description
    - CHANGELOG.md contains `git-stacks doctor` in the dir repos description
    - CHANGELOG.md contains `git-stacks status` in the dir repos description
    - `grep -c "## \[0.15.0\]" CHANGELOG.md` returns `1`
    - No "unreleased" text appears in CHANGELOG.md
    - The section follows Keep a Changelog format matching v0.14.0 style (bold title + description)
  </acceptance_criteria>
  <verify>
    <automated>grep "## \[0.15.0\]" CHANGELOG.md && grep "Dir repos" CHANGELOG.md && grep "mode: .dir." CHANGELOG.md && grep "repo add" CHANGELOG.md && grep "repo scan" CHANGELOG.md && grep "git-stacks doctor" CHANGELOG.md && grep "git-stacks status" CHANGELOG.md && grep -ci "unreleased" CHANGELOG.md | grep "^0$"</automated>
  </verify>
  <done>CHANGELOG.md has a complete v0.15.0 section documenting all dir mode behaviors from Phases 64-67.</done>
</task>

<task type="auto">
  <name>Task 3: Update README.md with dir repos documentation</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — identify Concepts section at line 19, Repo Registry section at line 46, existing feature sections as style template)
    - .planning/phases/64-schema-registry/64-CONTEXT.md (schema decisions for dir repos)
    - .planning/phases/65-workspace-lifecycle/65-CONTEXT.md (lifecycle behavior for dir repos)
    - .planning/phases/66-git-operation-guards/66-CONTEXT.md (git ops skip dir repos)
    - .planning/phases/67-status-display-health/67-CONTEXT.md (display/doctor behavior)
    - .planning/phases/68-release-prep/68-CONTEXT.md (D-04 and D-05 — README decisions)
  </read_first>
  <action>
Make two changes to README.md per D-04 and D-05:

**Change 1: Update Concepts section to mention dir mode (per D-05)**

Find the Workspace concept paragraph (line 25):
```
**Workspace** — a task/ticket-scoped instance created from a template. Each workspace has a branch; repos can be in `worktree` mode (isolated git worktree at `{workspace_root}/tasks/{workspace_name}/{repo_name}`) or `trunk` mode (references the main clone directly). Stored at `~/.config/git-stacks/workspaces/{name}.yml`. Looked up by the name field inside the YAML, not by filename.
```

Replace with:
```
**Workspace** — a task/ticket-scoped instance created from a template. Each workspace has a branch; repos can be in `worktree` mode (isolated git worktree at `{workspace_root}/tasks/{workspace_name}/{repo_name}`), `trunk` mode (references the main clone directly), or `dir` mode (references a non-git directory — no branches, no worktrees, no git operations). Stored at `~/.config/git-stacks/workspaces/{name}.yml`. Looked up by the name field inside the YAML, not by filename.
```

Also update the Repo Registry concept paragraph (line 21):
```
**Repo Registry** — a flat list of local git repos with names, paths, types, and default branches. Stored at `~/.config/git-stacks/registry.yml`. Managed via `git-stacks repo add|scan|list|show|remove|rename`.
```

Replace with:
```
**Repo Registry** — a flat list of local git repos and directories with names, paths, types, and default branches. Stored at `~/.config/git-stacks/registry.yml`. Managed via `git-stacks repo add|scan|list|show|remove|rename`.
```

**Change 2: Add standalone Dir Repos section (per D-04)**

Insert a new `## Dir Repos` section AFTER the `## Repo Registry` section (after line 56, before `## Templates` at line 58). This follows the existing structure where registry-related features sit near the registry section.

Content to insert:

```markdown
## Dir Repos

Non-git directories can be registered and included in workspaces alongside regular git repos. Use this for shared config directories, documentation folders, or any path you want available in your workspace without git operations.

```bash
# Register a plain directory
git-stacks repo add ~/shared/config

# Scan also discovers non-git directories
git-stacks repo scan ~/dev/myproject
```

In templates, dir repos behave like any other repo entry. In workspaces, they use `mode: "dir"` with `main_path` only — no worktree is created and no branch is set. All git-aware commands (`push`, `pull`, `sync`, `merge`, status tracking) silently skip dir repos. `git-stacks open` still injects dir repo paths into hook and env context, so hooks can reference them. `git-stacks doctor` validates that dir repo directories exist and are accessible instead of running git health checks.

```bash
# Status shows dir repos with a [dir] label and no git metrics
git-stacks status my-workspace
#   shared-config  [dir]  ~/shared/config

# Doctor validates directory existence
git-stacks doctor
#   ✓ shared-config: directory exists
```
```

These changes preserve all existing README content. The new section follows the established pattern of heading, description, code examples, explanation.

Commit: `docs(68-01): add dir repos section and update concepts in README`
  </action>
  <acceptance_criteria>
    - README.md Concepts Workspace paragraph contains `dir` mode mentioned alongside worktree and trunk
    - README.md Concepts Workspace paragraph contains the text `or \`dir\` mode`
    - README.md Concepts Repo Registry paragraph contains `and directories`
    - README.md contains `## Dir Repos` as a section heading
    - README.md Dir Repos section contains `git-stacks repo add ~/shared/config`
    - README.md Dir Repos section contains `git-stacks repo scan`
    - README.md Dir Repos section contains `mode: "dir"`
    - README.md Dir Repos section contains `main_path`
    - README.md Dir Repos section contains `git-stacks doctor`
    - README.md Dir Repos section contains `git-stacks status`
    - README.md Dir Repos section contains `[dir]`
    - `## Dir Repos` section appears AFTER `## Repo Registry` and BEFORE `## Templates`
    - All existing README content outside the modified sections is preserved
    - `grep "## Dir Repos" README.md` returns a match
    - `grep "dir. mode" README.md` returns a match (Concepts section update)
  </acceptance_criteria>
  <verify>
    <automated>grep "## Dir Repos" README.md && grep "dir. mode" README.md && grep "and directories" README.md && grep "repo add ~/shared/config" README.md && grep "mode: .dir." README.md && grep "main_path" README.md && grep "\[dir\]" README.md && grep "git-stacks doctor" README.md</automated>
  </verify>
  <done>README.md updated with dir mode mentioned in Concepts section and a standalone Dir Repos section with usage examples and behavior explanation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — this plan modifies only documentation files (package.json version field, CHANGELOG.md, README.md). No code changes, no user input handling, no external service calls.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | T (Tampering) | package.json | accept | Version field is a metadata string with no security impact; verified by automated grep + CLI output check |
</threat_model>

<verification>
```bash
# Version
grep '"version": "0.15.0"' package.json

# CLI version output
bun run src/index.ts --version 2>&1 | grep "0.15.0"

# CHANGELOG has v0.15.0 entry
grep "## \[0.15.0\]" CHANGELOG.md

# CHANGELOG covers dir mode
grep "Dir repos" CHANGELOG.md
grep "mode: .dir." CHANGELOG.md

# No unreleased marker
grep -ci "unreleased" CHANGELOG.md | grep "^0$"

# README has Dir Repos section
grep "## Dir Repos" README.md
grep "repo add ~/shared/config" README.md

# README Concepts updated
grep "dir. mode" README.md
grep "and directories" README.md

# Typecheck still passes (no code changes, but verify)
bun run typecheck
```
</verification>

<success_criteria>
1. `package.json` version field reads `0.15.0` and `git-stacks --version` outputs `0.15.0`
2. CHANGELOG.md has a `## [0.15.0]` section with no "unreleased" marker, covering dir repos as a cohesive feature with bold title + description paragraph
3. README.md Concepts section mentions `dir` mode as a third mode alongside worktree and trunk
4. README.md has a standalone `## Dir Repos` section with usage examples, behavior explanation, and code samples
5. All existing content in CHANGELOG and README is preserved (no regressions)
6. Typecheck still passes
</success_criteria>

<output>
After completion, create `.planning/phases/68-release-prep/68-01-SUMMARY.md`
</output>
