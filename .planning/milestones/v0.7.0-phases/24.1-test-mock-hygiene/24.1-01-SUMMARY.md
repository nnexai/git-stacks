---
phase: 24.1-test-mock-hygiene
plan: 01
subsystem: testing
tags: [bun, mock.module, tui/utils, clack/prompts, test-hygiene]

# Dependency graph
requires:
  - phase: 24-mock-architecture-refactor
    provides: prompts wrapper in @/tui/utils — production code now imports from there, not @clack/prompts directly
provides:
  - Dead @clack/prompts mock.module calls eliminated from all 7 test files
  - Explicit @/tui/utils mocks with prompts object in niri.test.ts, tmux.test.ts, artifacts.test.ts
  - No mock-cache ordering dependency in any test file
affects: [any future test files that mock prompt interactions]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Always mock @/tui/utils (not @clack/prompts) in tests — production code routes all prompts through the wrapper"
    - "Include full prompts object in @/tui/utils mock: spinner, log, intro, outro, text, select, multiselect, confirm, isCancel, cancel, group, note, groupMultiselect"
    - "Shared mock variable declarations live above the mock.module call and are referenced inside it — variables serve the @/tui/utils mock, not @clack/prompts"

key-files:
  created: []
  modified:
    - tests/lib/integrations/niri.test.ts
    - tests/lib/integrations/tmux.test.ts
    - tests/lib/integrations/artifacts.test.ts
    - tests/tui/workspace-wizard.test.ts
    - tests/commands/workspace-edit.test.ts
    - tests/lib/integrations/wizard-helpers.test.ts
    - tests/lib/integration-commands.test.ts

key-decisions:
  - "integration-commands.test.ts needs no @/tui/utils mock — tested code (integrationCommand) only exercises command structure, not prompt paths"
  - "artifacts.test.ts kept existing safeText in @/tui/utils mock and added prompts object alongside it — mockSpinner shared instance preserved for spinner assertion tests"

patterns-established:
  - "Mock comment pattern: shared variable declarations get comment 'Shared mock instances used by @/tui/utils mock below' not '@clack/prompts first'"
  - "Fragile files (niri, tmux, artifacts) now have explicit @/tui/utils mock — no implicit module cache ordering dependency"

requirements-completed: [MOCK-05]

# Metrics
duration: 2min
completed: 2026-03-22
---

# Phase 24.1 Plan 01: Test Mock Hygiene Summary

**Eliminated all 7 dead `mock.module("@clack/prompts")` calls from the test suite; 3 fragile files now have explicit `@/tui/utils` mocks with full prompts object, making the mock chain explicit and order-independent**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-22T13:53:39Z
- **Completed:** 2026-03-22T13:56:00Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments
- Removed all 7 `mock.module("@clack/prompts")` blocks from test files — zero occurrences remain
- Added explicit `@/tui/utils` mocks with full `prompts` object to niri.test.ts, tmux.test.ts, and artifacts.test.ts (the 3 files that had fragile mock-cache ordering dependency)
- Preserved all existing `@/tui/utils` mocks in workspace-wizard.test.ts, workspace-edit.test.ts, wizard-helpers.test.ts intact
- 574 tests pass, 0 failures — no regressions

## Task Commits

Each task was committed atomically:

1. **Task 1: Add explicit @/tui/utils mocks to 3 fragile test files** - `004057a` (fix)
2. **Task 2: Remove dead @clack/prompts mocks from 4 files** - `cd5c6be` (fix)

## Files Created/Modified
- `tests/lib/integrations/niri.test.ts` - Replaced dead @clack/prompts mock with explicit @/tui/utils mock containing full prompts object
- `tests/lib/integrations/tmux.test.ts` - Replaced dead @clack/prompts mock with explicit @/tui/utils mock containing full prompts object
- `tests/lib/integrations/artifacts.test.ts` - Removed dead @clack/prompts mock, enhanced existing @/tui/utils mock with prompts object and cancel function
- `tests/tui/workspace-wizard.test.ts` - Removed dead @clack/prompts mock, updated comment on shared variable declarations
- `tests/commands/workspace-edit.test.ts` - Removed dead @clack/prompts mock, updated comment on shared variable declarations
- `tests/lib/integrations/wizard-helpers.test.ts` - Removed dead @clack/prompts mock, updated comment on shared variable declarations
- `tests/lib/integration-commands.test.ts` - Removed dead @clack/prompts mock, added explanatory comment (no replacement needed)

## Decisions Made
- `integration-commands.test.ts` needs no `@/tui/utils` mock — tested code (`integrationCommand`) only exercises command structure (name, subcommands), not prompt paths; confirmed by test content
- `artifacts.test.ts` kept existing `safeText` mock alongside new `prompts` object and `cancel` function — `mockSpinner` shared instance preserved for spinner assertion tests in tmux/cmux artifact tests

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 24.1 complete — all dead `@clack/prompts` mocks eliminated from test suite
- Mock chain is now explicit: production code imports `{ prompts as p }` from `@/tui/utils`; tests mock `@/tui/utils` with `prompts` object directly
- No ordering dependency remains — tests are safe to run in any order
- 574 tests pass — full suite green

---
*Phase: 24.1-test-mock-hygiene*
*Completed: 2026-03-22*
