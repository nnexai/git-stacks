---
phase: 28-issue-task-tracking-integration
plan: 04
type: execute
wave: 3
depends_on: ["28-02", "28-03"]
files_modified:
  - CHANGELOG.md
  - README.md
autonomous: true
requirements:
  - ISSUE-04
  - ISSUE-05
  - ISSUE-06
  - ISSUE-07

must_haves:
  truths:
    - "CHANGELOG.md documents issue tracking integration under [Unreleased] Added section"
    - "CHANGELOG.md mentions issue link/unlink/open commands for all four trackers"
    - "CHANGELOG.md mentions Jira configurable template"
    - "README.md Integrations section documents issue tracking with usage examples"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Issue tracking changelog entries"
      contains: "issue link"
    - path: "README.md"
      provides: "Issue tracking documentation"
      contains: "issue link"
  key_links: []
---

<objective>
Update CHANGELOG.md and README.md to document the issue tracking integration features added in Phase 28.

Purpose: Users need to discover the new issue link/unlink/open commands across all four trackers (GitHub, GitLab, Gitea, Jira) and understand the Jira configurable template system. This plan runs after all implementation is complete.

Output: Updated CHANGELOG.md and README.md with comprehensive issue tracking documentation.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-issue-task-tracking-integration/28-RESEARCH.md
@.planning/phases/28-issue-task-tracking-integration/28-01-SUMMARY.md
@.planning/phases/28-issue-task-tracking-integration/28-02-SUMMARY.md
@.planning/phases/28-issue-task-tracking-integration/28-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CHANGELOG.md with issue tracking entries</name>
  <files>CHANGELOG.md</files>
  <read_first>CHANGELOG.md, .planning/phases/28-issue-task-tracking-integration/28-01-SUMMARY.md, .planning/phases/28-issue-task-tracking-integration/28-02-SUMMARY.md, .planning/phases/28-issue-task-tracking-integration/28-03-SUMMARY.md</read_first>
  <action>
Add the following entries to the `[Unreleased]` section of `CHANGELOG.md`, under the `### Added` heading (after the forge integration entries added by Phase 27 Plan 04):

```markdown
**Issue & task tracking integration (GitHub, GitLab, Gitea, Jira)** — link workspaces to issues and open them from CLI:
- `git-stacks integration <tracker> issue link <workspace> <issue-id>` — associate an issue with a workspace
- `git-stacks integration <tracker> issue unlink <workspace>` — remove the association
- `git-stacks integration <tracker> issue open <workspace>` — print issue URL to stdout; `--web` opens in browser (forge trackers)
- Supported trackers: `github` (via `gh issue view`), `gitlab` (via `glab issue view`), `gitea` (via `tea issues ls` JSON extraction), `jira` (via configurable command template)
- Issue references stored in workspace YAML under `settings.integrations.<tracker>.issue` — no schema migration needed
- Jira integration uses configurable `open_cmd` template (default: `jira open $ISSUE_ID`) — tool-agnostic via `$ISSUE_ID` env var substitution

**Jira integration plugin** — standalone integration for Jira issue tracking:
- `git-stacks integration jira issue link <workspace> <issue-key>` — link Jira issues like `PROJ-123`
- `git-stacks integration jira issue open <workspace>` — opens issue via configurable command
- Configure via `git-stacks config` — set custom `open_cmd` template (e.g. `xdg-open https://company.atlassian.net/browse/$ISSUE_ID`)
- `git-stacks doctor` checks `jira` CLI availability
```

Keep all existing changelog entries intact. Insert the issue tracking entries as additional bullet groups after the forge integration entries.
  </action>
  <verify>
    <automated>grep -c "issue link" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md contains "Issue & task tracking integration" under [Unreleased] Added
    - CHANGELOG.md mentions all four trackers: github, gitlab, gitea, jira
    - CHANGELOG.md mentions `issue link`, `issue unlink`, `issue open` commands
    - CHANGELOG.md mentions Jira configurable `open_cmd` template
    - CHANGELOG.md mentions `$ISSUE_ID` env var substitution
    - CHANGELOG.md mentions doctor checks for jira
    - Existing changelog entries are preserved
  </acceptance_criteria>
  <done>CHANGELOG.md has issue tracking entries documenting all four trackers, link/unlink/open commands, Jira configurable template, and doctor check.</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md with issue tracking documentation</name>
  <files>README.md</files>
  <read_first>README.md</read_first>
  <action>
In `README.md`, in the `## Integrations` section, update the forge integration rows in the table to reflect both PR and issue capabilities, and add a Jira row:

**Step 1: Update table rows.** Find the forge integration rows (added by Phase 27 Plan 04). Update them to mention issues:

```
| 5 | GitHub | GitHub PRs and issues via `gh` CLI | — |
| 5 | GitLab | GitLab MRs and issues via `glab` CLI | — |
| 5 | Gitea | Gitea PRs and issues via `tea` CLI | — |
| 5 | Jira | Jira issue tracking via configurable command | — |
```

**Step 2: Add issue tracking documentation.** After the forge integration section (added by Phase 27 Plan 04), add an issue tracking section:

```markdown
**Issue tracking** (GitHub, GitLab, Gitea, Jira):

Link workspaces to issues from any supported tracker. Issue references are stored in workspace YAML — no external state.

```bash
# Link an issue to a workspace
git-stacks integration github issue link my-feature 42
git-stacks integration jira issue link my-feature PROJ-123

# View linked issue URL
git-stacks integration github issue open my-feature

# Open in browser
git-stacks integration github issue open my-feature --web

# Remove link
git-stacks integration github issue unlink my-feature
```

Jira uses a configurable command template (default: `jira open $ISSUE_ID`). Configure via `git-stacks config` or edit the config YAML directly:

```yaml
# ~/.config/git-stacks/config.yml
integrations:
  jira:
    enabled: true
    open_cmd: "xdg-open https://company.atlassian.net/browse/$ISSUE_ID"
```

Issue data is stored per-workspace:

```yaml
# ~/.config/git-stacks/workspaces/my-feature.yml
settings:
  integrations:
    github:
      issue: "42"
    jira:
      issue: "PROJ-123"
```
```

Keep all existing README content intact. The issue tracking section is purely additive.
  </action>
  <verify>
    <automated>grep -c "issue link" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md integrations table has updated rows for GitHub, GitLab, Gitea mentioning "issues"
    - README.md integrations table has Jira row
    - README.md has "Issue tracking" documentation section
    - README.md shows usage examples for issue link, issue open, issue open --web, issue unlink
    - README.md shows Jira configurable template with `$ISSUE_ID`
    - README.md shows example config.yml with jira open_cmd
    - README.md shows example workspace YAML with issue references
    - Existing README content is preserved
  </acceptance_criteria>
  <done>README.md documents issue tracking integration with table entries, usage examples for all four trackers, Jira configurable template, config YAML example, and workspace YAML storage example.</done>
</task>

</tasks>

<verification>
- CHANGELOG.md contains issue tracking entries
- README.md contains issue tracking documentation
- No existing content removed
</verification>

<success_criteria>
- CHANGELOG.md documents all four trackers with link/unlink/open commands
- README.md documents issue tracking with table, examples, and Jira template config
- All existing documentation preserved
</success_criteria>

<output>
After completion, create `.planning/phases/28-issue-task-tracking-integration/28-04-SUMMARY.md`
</output>
