---
phase: 22-niri-display-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tui/dashboard/configUtils.ts
  - src/tui/dashboard/WorkspaceDetail.tsx
  - src/tui/dashboard/TemplateDetail.tsx
  - tests/tui/dashboard/configUtils.test.ts
  - tests/tui/dashboard/WorkspaceDetail.test.tsx
  - tests/tui/dashboard/TemplateDetail.test.tsx
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "Niri columns configuration displays as readable text (e.g., '3 columns') in TUI detail pane instead of '[object Object]'"
    - "Primitive config values (strings, numbers, booleans) still render as before (e.g., 'cmd: code')"
    - "All other TUI detail pane fields are unaffected by the fix"
  artifacts:
    - path: "src/tui/dashboard/configUtils.ts"
      provides: "formatConfigValue helper for serializing non-primitive config values"
      exports: ["formatConfigValue"]
    - path: "tests/tui/dashboard/configUtils.test.ts"
      provides: "Unit tests for formatConfigValue"
      min_lines: 15
  key_links:
    - from: "src/tui/dashboard/WorkspaceDetail.tsx"
      to: "src/tui/dashboard/configUtils.ts"
      via: "import { formatConfigValue }"
      pattern: "formatConfigValue"
    - from: "src/tui/dashboard/TemplateDetail.tsx"
      to: "src/tui/dashboard/configUtils.ts"
      via: "import { formatConfigValue }"
      pattern: "formatConfigValue"
---

<objective>
Fix the TUI workspace and template detail panes to render niri `columns` configuration as human-readable text instead of the raw `[object Object]` JavaScript serialization artifact.

Purpose: When a niri integration has `columns` config (an array of objects), the detail pane currently shows `columns: [object Object]` because the template literal `${v}` calls `.toString()` on the array. This fix serializes non-primitive values to readable summaries.

Output: A shared `formatConfigValue` helper used by both `WorkspaceDetail.tsx` and `TemplateDetail.tsx`, with unit tests confirming correct rendering of strings, numbers, booleans, arrays, and nested objects.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/tui/dashboard/WorkspaceDetail.tsx
@src/tui/dashboard/TemplateDetail.tsx
@src/lib/integrations/niri.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/tui/dashboard/WorkspaceDetail.tsx (line 132-136) — the bug:
```typescript
const extras = Object.entries(rawConfig)
  .filter(([k]) => k !== "enabled")
  .map(([k, v]) => `${k}: ${v}`)      // <-- BUG: v.toString() on objects = "[object Object]"
  .join(", ")
```

Same pattern in src/tui/dashboard/TemplateDetail.tsx (line 86-89).

From src/lib/integrations/niri.ts — the niri config shape:
```typescript
const niriColumnSchema = z.object({
  width: z.string().optional(),
  windows: z.array(niriWindowConfigSchema).min(1),
})

const niriConfigSchema = z.object({
  enabled: z.boolean().optional(),
  focus: z.boolean().optional(),
  columns: z.array(niriColumnSchema).optional(),
})
```

A typical niri config object (after filtering out "enabled"):
```typescript
{ focus: true, columns: [{ width: "50%", windows: [{ app: "foot" }] }, { windows: [{ source: "vscode" }] }] }
// Should render as: (focus: true, columns: 2 cols)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create formatConfigValue helper with unit tests</name>
  <files>src/tui/dashboard/configUtils.ts, tests/tui/dashboard/configUtils.test.ts</files>
  <read_first>
    src/tui/dashboard/WorkspaceDetail.tsx
    src/tui/dashboard/TemplateDetail.tsx
    src/tui/dashboard/messageUtils.ts
    src/lib/integrations/niri.ts
  </read_first>
  <behavior>
    - formatConfigValue("code") returns "code" (string pass-through)
    - formatConfigValue(true) returns "true" (boolean)
    - formatConfigValue(42) returns "42" (number)
    - formatConfigValue([{width: "50%", windows: [{app: "foot"}]}, {windows: [{source: "vscode"}]}]) returns "2 cols" (array of objects with windows key = niri columns)
    - formatConfigValue(["a", "b", "c"]) returns "a, b, c" (array of primitives)
    - formatConfigValue({nested: "obj"}) returns JSON compact string (generic object fallback)
    - formatConfigValue(null) returns "" (null/undefined safety)
    - formatConfigValue(undefined) returns "" (null/undefined safety)
  </behavior>
  <action>
    1. Create `src/tui/dashboard/configUtils.ts` with a single exported function `formatConfigValue(value: unknown): string`.

    Logic:
    - If `value` is `null` or `undefined`, return `""`.
    - If `value` is a string, number, or boolean, return `String(value)`.
    - If `value` is an array:
      - If every element is a primitive (string/number/boolean), return elements joined with `", "`.
      - If the array looks like niri columns (objects with a `windows` property), return `"{length} cols"` (e.g., `"2 cols"` or `"1 col"`).
      - Otherwise, return `JSON.stringify(value)` as a compact fallback.
    - If `value` is a non-null object (but not array), return `JSON.stringify(value)`.

    2. Create `tests/tui/dashboard/configUtils.test.ts` with the test cases from the behavior block above. These are pure functions — no mocking needed, no TUI rendering.

    3. Run tests RED first (write tests, verify they fail because configUtils.ts does not exist yet or is empty), then implement the function and verify GREEN.
  </action>
  <verify>
    <automated>bun test tests/tui/dashboard/configUtils.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/tui/dashboard/configUtils.ts` exists and exports `formatConfigValue`
    - `grep -c "export function formatConfigValue" src/tui/dashboard/configUtils.ts` returns 1
    - `bun test tests/tui/dashboard/configUtils.test.ts` shows all tests passing
    - formatConfigValue handles: string, boolean, number, niri columns array, primitive array, generic object, null, undefined
  </acceptance_criteria>
  <done>formatConfigValue correctly serializes all value types; all unit tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Apply formatConfigValue to both detail panes and add integration tests</name>
  <files>src/tui/dashboard/WorkspaceDetail.tsx, src/tui/dashboard/TemplateDetail.tsx, tests/tui/dashboard/WorkspaceDetail.test.tsx, tests/tui/dashboard/TemplateDetail.test.tsx</files>
  <read_first>
    src/tui/dashboard/configUtils.ts
    src/tui/dashboard/WorkspaceDetail.tsx
    src/tui/dashboard/TemplateDetail.tsx
    tests/tui/dashboard/WorkspaceDetail.test.tsx
    tests/tui/dashboard/TemplateDetail.test.tsx
  </read_first>
  <action>
    1. In `src/tui/dashboard/WorkspaceDetail.tsx`:
       - Add import: `import { formatConfigValue } from "./configUtils"` (place after existing imports).
       - Change line 134 from `.map(([k, v]) => \`${k}: ${v}\`)` to `.map(([k, v]) => \`${k}: ${formatConfigValue(v)}\`)`.
       - No other changes.

    2. In `src/tui/dashboard/TemplateDetail.tsx`:
       - Add import: `import { formatConfigValue } from "./configUtils"` (place after existing imports).
       - Change line 88 from `.map(([k, v]) => \`${k}: ${v}\`)` to `.map(([k, v]) => \`${k}: ${formatConfigValue(v)}\`)`.
       - No other changes.

    3. In `tests/tui/dashboard/WorkspaceDetail.test.tsx`:
       - Add a new test (Test 7) that passes a niri-like config with `columns` array and verifies the rendered frame contains `"columns: 2 cols"` (not `"[object Object]"`).
       - The test should add a mock `niri` integration to the integrations array in the mock.module call (with `id: "niri"`, `enabledByDefault: true`, no `applies`).
       - Set workspace settings to: `{ integrations: { niri: { enabled: true, focus: true, columns: [{ width: "50%", windows: [{ app: "foot" }] }, { windows: [{ source: "vscode" }] }] } } }`.
       - Verify frame contains `"2 cols"` and does NOT contain `"[object Object]"`.

    4. In `tests/tui/dashboard/TemplateDetail.test.tsx`:
       - Add a new test (Test 5) that passes a template with niri integration config containing `columns` array.
       - Set template integrations to: `{ niri: { enabled: true, columns: [{ windows: [{ app: "foot" }] }] } }`.
       - Add `niri` to the mock integrations array (with `id: "niri"`, `enabledByDefault: true`).
       - Verify frame contains `"1 col"` and does NOT contain `"[object Object]"`.

    5. Run ALL detail pane tests to confirm both old and new tests pass:
       `bun test tests/tui/dashboard/WorkspaceDetail.test.tsx tests/tui/dashboard/TemplateDetail.test.tsx`
  </action>
  <verify>
    <automated>bun test tests/tui/dashboard/WorkspaceDetail.test.tsx tests/tui/dashboard/TemplateDetail.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "formatConfigValue" src/tui/dashboard/WorkspaceDetail.tsx` matches the import and the usage on the .map line
    - `grep "formatConfigValue" src/tui/dashboard/TemplateDetail.tsx` matches the import and the usage on the .map line
    - `grep "object Object" src/tui/dashboard/WorkspaceDetail.tsx` returns NO matches (the raw ${v} pattern is gone)
    - `grep "object Object" src/tui/dashboard/TemplateDetail.tsx` returns NO matches
    - WorkspaceDetail test "Test 7" asserts frame contains "2 cols" and does not contain "[object Object]"
    - TemplateDetail test "Test 5" asserts frame contains "1 col" and does not contain "[object Object]"
    - All 12+ tests pass across both test files (10 existing + 2 new)
    - Existing tests (Test 5: "cmd: code" rendering) still pass unchanged
  </acceptance_criteria>
  <done>Both detail panes use formatConfigValue for config display; niri columns render as "N cols" instead of "[object Object]"; all tests pass</done>
</task>

</tasks>

<verification>
1. `bun test tests/tui/dashboard/configUtils.test.ts` — all formatConfigValue unit tests pass
2. `bun test tests/tui/dashboard/WorkspaceDetail.test.tsx tests/tui/dashboard/TemplateDetail.test.tsx` — all detail pane tests pass (including new object value tests)
3. `bun run typecheck` — no type errors introduced
4. `grep -r "object Object" src/tui/dashboard/WorkspaceDetail.tsx src/tui/dashboard/TemplateDetail.tsx` — returns no matches (bug pattern eliminated)
</verification>

<success_criteria>
- Niri columns config renders as "N cols" (e.g., "2 cols") in both workspace and template detail panes
- Primitive config values still render as before (e.g., "cmd: code", "focus: true")
- No "[object Object]" appears anywhere in the config summary rendering path
- All existing tests continue to pass
- New tests verify the fix for both detail panes
- Type check passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/22-niri-display-fix/22-01-SUMMARY.md`
</output>
