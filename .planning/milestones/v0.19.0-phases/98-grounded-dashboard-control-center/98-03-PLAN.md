---
phase: 98-grounded-dashboard-control-center
plan: 03
title: Dashboard Footer And Snapshot Acceptance Coverage
type: execute
wave: 3
depends_on:
  - 98-01
  - 98-02
files_modified:
  - src/tui/dashboard/App.tsx
  - tests/tui/dashboard/snapshots/WorkspaceDetail.snap.test.tsx
  - tests/tui/dashboard/snapshots/WorkspaceList.snap.test.tsx
  - tests/tui/dashboard/snapshots/WorkspaceRow.snap.test.tsx
  - tests/tui/dashboard/WorkspaceDetail.test.tsx
autonomous: true
requirements:
  - NOTE-03
  - TUI-01
  - TUI-02
  - TUI-03
  - TUI-07
user_setup: []
must_haves:
  truths:
    - "D-13: focused terminal snapshots cover narrow/medium/wide rows, grouped headers, detail ordering, file status display, note summary/detail, and contextual footers."
    - "D-14: at least one long-detail scrolling case proves overflow detail content remains navigable."
    - "D-11: contextual footer/help copy must not advertise repo edit implementation before Phase 99."
    - "D-12: contextual footer/help copy must not advertise manual workspace command menus before Phase 99."
    - "The final dashboard help/footer text reflects active grouping, filtering, and detail-scroll state without introducing Phase 99 actions."
  artifacts:
    - path: "src/tui/dashboard/App.tsx"
      provides: "Contextual footer/help copy for grouping, filtering, and detail scroll"
    - path: "tests/tui/dashboard/snapshots/WorkspaceDetail.snap.test.tsx"
      provides: "Detail ordering, notes, file-status, and scroll snapshots"
    - path: "tests/tui/dashboard/snapshots/WorkspaceList.snap.test.tsx"
      provides: "Grouped header snapshots"
    - path: "tests/tui/dashboard/snapshots/WorkspaceRow.snap.test.tsx"
      provides: "Width-tier row snapshots"
  key_links:
    - from: "src/tui/dashboard/App.tsx"
      to: "tests/tui/dashboard/snapshots/WorkspaceDetail.snap.test.tsx"
      via: "footer/detail-scroll observable output"
      pattern: "helpBarText"
    - from: "tests/tui/dashboard/WorkspaceDetail.test.tsx"
      to: "src/tui/dashboard/WorkspaceDetail.tsx"
      via: "terminal snapshot of ordered detail sections"
      pattern: "WorkspaceDetail"
---

# Phase 98 Plan 03 - Dashboard Footer And Snapshot Acceptance Coverage

<objective>
Finish Phase 98 by locking the user-visible terminal acceptance surface: contextual footers, width-tier rows, grouped headers, ordered details, notes/file-status rendering, and long-detail scrolling.

Purpose: satisfy TUI-07 completely and catch regressions in the grounded dashboard reset before Phase 99 adds new actions.
Output: contextual footer copy plus focused component/snapshot coverage tied to the roadmap acceptance set.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/98-grounded-dashboard-control-center/98-CONTEXT.md
@.planning/phases/98-grounded-dashboard-control-center/98-UI-SPEC.md
@.planning/phases/98-grounded-dashboard-control-center/98-VALIDATION.md
@src/tui/dashboard/App.tsx
@src/tui/dashboard/WorkspaceDetail.tsx
@src/tui/dashboard/WorkspaceList.tsx
@src/tui/dashboard/WorkspaceRow.tsx
@tests/tui/dashboard/snapshots/WorkspaceRow.snap.test.tsx
</context>

<tasks>
  <task type="auto" tdd="true">
    <name>Task 1: Make footer/help copy reflect active dashboard control state</name>
    <files>src/tui/dashboard/App.tsx</files>
    <read_first>src/tui/dashboard/App.tsx, .planning/phases/98-grounded-dashboard-control-center/98-UI-SPEC.md, .planning/phases/98-grounded-dashboard-control-center/98-VALIDATION.md</read_first>
    <behavior>
      - Test 1: footer text exposes grouping mode and grouping shortcut only on the workspaces tab.
      - Test 2: footer text exposes filter state and detail-scroll hints only when relevant.
      - Test 3: footer text does not advertise repo edit, issue opening, or manual command menus.
    </behavior>
    <action>Update the existing width-tiered `helpBarText` in `App.tsx` so it remains compact but contextual for D-13: active workspace grouping mode, filter state, and detail-scroll availability. Keep copy within existing terminal width tiers and do not add visible feature descriptions or Phase 99 action labels. Preserve the existing action/menu shortcuts that already work.</action>
    <verify>
      <automated>bun run test tests/tui/dashboard/integ-tab-switching.test.tsx tests/tui/dashboard/snapshots/WorkspaceList.snap.test.tsx</automated>
    </verify>
    <done>The footer gives operators relevant grouping/filter/detail-scroll hints without overflow or premature Phase 99 action promises.</done>
  </task>

  <task type="auto" tdd="true">
    <name>Task 2: Add final terminal snapshots for the Phase 98 acceptance matrix</name>
    <files>tests/tui/dashboard/snapshots/WorkspaceDetail.snap.test.tsx, tests/tui/dashboard/snapshots/WorkspaceList.snap.test.tsx, tests/tui/dashboard/snapshots/WorkspaceRow.snap.test.tsx, tests/tui/dashboard/WorkspaceDetail.test.tsx</files>
    <read_first>tests/tui/dashboard/snapshots/WorkspaceRow.snap.test.tsx, tests/tui/dashboard/WorkspaceDetail.test.tsx, .planning/phases/98-grounded-dashboard-control-center/98-VALIDATION.md</read_first>
    <behavior>
      - Test 1: D-13 acceptance matrix has explicit tests for narrow/medium/wide rows, grouped headers, detail order, file status display, note summary/detail, and contextual footers.
      - Test 2: D-14 long-detail scrolling test proves content below the first visible detail page is reachable.
      - Test 3: tests fail if notes appear as list badges or if file-status rendering references CLI subprocess execution.
    </behavior>
    <action>Add or extend focused OpenTUI snapshots and component tests to cover every required sample from `98-VALIDATION.md`. Use deterministic fixtures for messages, notes, file-status loaded/error states, repos, integrations, and config. Assert source strings such as `Messages`, `Repos`, `Files`, `Source/Issues`, `Integrations`, `Notes`, and `Config` in order. Include negative assertions for no notes row badge and no `git-stacks files status` subprocess path in production dashboard code.</action>
    <verify>
      <automated>bun run test tests/tui/dashboard/WorkspaceDetail.test.tsx tests/tui/dashboard/snapshots/WorkspaceRow.snap.test.tsx tests/tui/dashboard/snapshots/WorkspaceList.snap.test.tsx tests/tui/dashboard/snapshots/WorkspaceDetail.snap.test.tsx</automated>
    </verify>
    <done>The Phase 98 acceptance matrix is fully represented in automated dashboard tests and terminal snapshots.</done>
  </task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard state -> footer copy | The footer summarizes current operator controls and must not promise unavailable actions. |
| snapshots -> regression gate | Snapshot fixtures become the main guard for compact TUI behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-06 | Repudiation | footer/help copy | mitigate | Snapshot contextual footer states so advertised controls match implemented controls. |
| T-98-07 | Tampering | snapshot acceptance matrix | mitigate | Cover both positive and negative assertions so future changes cannot silently move notes into rows or add CLI subprocess file-status rendering. |
| T-98-SC | Tampering | dependencies | mitigate | No dependency changes; use existing Bun/OpenTUI snapshot infrastructure. |
</threat_model>

<verification>
- `bun run test tests/tui/dashboard/WorkspaceDetail.test.tsx tests/tui/dashboard/snapshots/WorkspaceRow.snap.test.tsx tests/tui/dashboard/snapshots/WorkspaceList.snap.test.tsx tests/tui/dashboard/snapshots/WorkspaceDetail.snap.test.tsx`
- `bun run typecheck`
</verification>

<success_criteria>
- Contextual footers remain compact and accurate for grouping, filtering, and detail-scroll state.
- The full D-13 and D-14 acceptance set is covered by automated component tests or terminal snapshots.
- No Phase 99 action implementation or stale cleanup recommendation is introduced.
</success_criteria>

<output>
Create `.planning/phases/98-grounded-dashboard-control-center/98-03-SUMMARY.md` when done.
</output>
