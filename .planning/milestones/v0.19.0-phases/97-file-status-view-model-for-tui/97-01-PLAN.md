---
phase: 97-file-status-view-model-for-tui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/workspace-file-status.ts
  - tests/lib/workspace-file-status.test.ts
  - tests/commands/files.test.ts
autonomous: true
requirements:
  - TUI-04
user_setup: []
must_haves:
  truths:
    - "A shared source helper returns grouped workspace and repo file-status sections without shelling out to `git-stacks files status`."
    - "Copy, symlink, and sync entries preserve the existing v0.18.0 states while adding a TUI-facing severity/attention signal."
    - "Section and top-level summaries expose enough counts and warnings for the dashboard to render compact status without recomputing drift policy."
  artifacts:
    - path: "src/lib/workspace-file-status.ts"
      provides: "Grouped workspace/repo file-status view model and summary helpers"
      exports: ["getWorkspaceFileStatusView"]
    - path: "tests/lib/workspace-file-status.test.ts"
      provides: "Focused grouped-model coverage for copy/symlink/sync states and summaries"
    - path: "tests/commands/files.test.ts"
      provides: "Parity coverage between the shared grouped model and CLI-visible file status"
  key_links:
    - from: "src/lib/workspace-file-status.ts"
      to: "src/lib/files.ts"
      via: "getFileEntryStatuses()"
      pattern: "getFileEntryStatuses"
    - from: "tests/commands/files.test.ts"
      to: "src/lib/workspace-file-status.ts"
      via: "CLI parity assertions against the grouped model"
      pattern: "getWorkspaceFileStatusView"
---

<objective>
Create the reusable grouped file-status model that Phase 98 can render directly, while keeping `src/lib/files.ts` as the single sync-policy source of truth.

Purpose: satisfy D-01 through D-04 and D-07 through D-08 by wrapping the shipped v0.18.0 file-status behavior in a TUI-friendly shape instead of duplicating sync logic.
Output: one shared lib module plus focused lib/command tests that lock state/severity/summary parity.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/97-file-status-view-model-for-tui/97-CONTEXT.md
@.planning/milestones/v0.18.0-phases/89-files-sync-schema-and-materialization/89-03-SUMMARY.md
@.planning/milestones/v0.18.0-phases/90-files-command-surface-and-conflict-policy/90-01-SUMMARY.md
@.planning/milestones/v0.18.0-phases/90-files-command-surface-and-conflict-policy/90-02-SUMMARY.md
@.planning/milestones/v0.18.0-phases/90-files-command-surface-and-conflict-policy/90-03-SUMMARY.md
@.planning/milestones/v0.18.0-phases/91-files-sync-integration-and-machine-output/91-SUMMARY.md
@src/lib/files.ts
@src/commands/files.ts
@tests/lib/files.test.ts
@tests/commands/files.test.ts

<interfaces>
From `src/lib/files.ts`:

```typescript
export type SimpleFileEntryState = "materialized" | "missing" | "error";
export type SyncEntryState = "ok" | "pullable" | "pushable" | "diverged" | "error";
export type FileEntryStatus =
  | { kind: "copy" | "symlink"; scope: "workspace" | "repo"; name: string; target: string; state: SimpleFileEntryState; hint: string; error?: string }
  | { kind: "sync"; scope: "workspace" | "repo"; name: string; target: string; state: SyncEntryState; counts: SyncComparisonCounts; hint: string; verbosePaths?: SyncVerbosePaths; error?: string };

export function getFileEntryStatuses(
  workspace: Workspace,
  wsInstanceRoot: string,
  options?: FileEntryStatusOptions
): FileEntryStatus[];
```

Existing CLI parity constraints from `src/commands/files.ts`:

```typescript
function statusSummary(rows: FileEntryStatus[]): Record<string, number>;
function statusJson(workspace: Workspace, rows: FileEntryStatus[], verbose: boolean): unknown;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the grouped workspace/repo file-status view model</name>
  <files>src/lib/workspace-file-status.ts, tests/lib/workspace-file-status.test.ts</files>
  <behavior>
    - Test 1: per D-01 and D-02, a workspace with workspace-level and repo-level file entries yields one workspace section plus ordered repo sections, each with entries and a compact summary.
    - Test 2: per D-03 and D-04, sync rows keep `ok|pullable|pushable|diverged|error` and simple rows keep `materialized|missing|error`, while every entry also exposes a derived severity/attention signal.
    - Test 3: per D-07 and D-08, missing roots, missing targets, missing sources, drift, and invalid targets surface as summary warnings/errors plus entry-level detail buckets instead of throwing.
    - Test 4: repo sections carry enough metadata to distinguish dir, trunk, and worktree repos without recomputing that information in the dashboard.
  </behavior>
  <action>Create `src/lib/workspace-file-status.ts` as the shared TUI-facing wrapper around `getFileEntryStatuses()` and existing workspace repo metadata. Do not copy comparison logic out of `src/lib/files.ts`; derive grouped sections, summaries, compact warning/error totals, and per-entry detail buckets from the existing rows and workspace config per D-01, D-02, D-03, D-04, D-07, and D-08. Keep the module source-level only, with explicit exported return types that Phase 98 can consume directly.</action>
  <verify>
    <automated>bun run test tests/lib/workspace-file-status.test.ts</automated>
  </verify>
  <done>The repo has one shared grouped file-status helper whose tests prove copy/symlink/sync grouping, severity mapping, summary aggregation, and missing-path handling without changing sync policy behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock CLI-visible parity against the shared grouped model</name>
  <files>tests/commands/files.test.ts</files>
  <behavior>
    - Test 1: the grouped helper and `git-stacks files status --json` expose the same workspace/repo state names and target paths for representative copy, symlink, and sync entries.
    - Test 2: grouped summary counts match the command's summary and still reflect diverged/pullable/pushable cases from v0.18.0 behavior.
    - Test 3: no CLI subprocess is required inside the helper; parity is proven by comparing helper output to existing command behavior, not by spawning the command from production code.
  </behavior>
  <action>Extend `tests/commands/files.test.ts` so Phase 97 locks parity between the new grouped helper and the existing `git-stacks files status` contract called out in the roadmap. Keep the CLI behavior unchanged unless a narrow adapter change is required to expose already-shipped state names cleanly; do not move sync decision logic into `src/commands/files.ts`, and do not add new CLI states or new `files` subcommands. Reference D-03 explicitly in the assertions so future Phase 98 rendering can rely on the same vocabulary.</action>
  <verify>
    <automated>bun run test tests/lib/workspace-file-status.test.ts tests/commands/files.test.ts</automated>
  </verify>
  <done>There is automated proof that the grouped helper stays in lock-step with the shipped `git-stacks files status` states, targets, and summaries required by TUI-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace config -> grouped helper | User-authored `files.copy`, `files.symlink`, and `files.sync` paths flow into the TUI-facing status model. |
| filesystem -> status summaries | Current disk state is sampled to derive drift/missing/error summaries that later dashboard code will trust. |
| tests -> CLI parity gate | Parity assertions are the main guard against silently drifting away from shipped `files status` behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01 | Tampering | `src/lib/workspace-file-status.ts` | mitigate | Build the grouped model strictly from `getFileEntryStatuses()` and workspace repo metadata so Phase 97 adds presentation structure, not a second sync-policy engine. |
| T-97-02 | Information Disclosure | grouped warning/detail buckets | mitigate | Surface only the already-configured targets, relative drift buckets, and explicit error strings needed for TUI rendering; do not add new filesystem scans beyond the existing file-status helper path. |
| T-97-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager changes are allowed in this plan; keep the dependency set unchanged and gate the work with focused existing test commands only. |
</threat_model>

<verification>
- `bun run test tests/lib/workspace-file-status.test.ts tests/commands/files.test.ts`
- `bun run typecheck`
</verification>

<success_criteria>
- `src/lib/workspace-file-status.ts` exports a grouped workspace/repo view model with summary and severity fields that satisfies D-01 through D-04 and D-07 through D-08.
- Focused tests cover copy, symlink, sync, missing target/source, drift, summary aggregation, and CLI parity for the grouped model.
- No production code shells out to `git-stacks files status`, and no sync comparison logic is copied out of `src/lib/files.ts`.
</success_criteria>

<output>
Create `.planning/phases/97-file-status-view-model-for-tui/97-01-SUMMARY.md` when done
</output>
