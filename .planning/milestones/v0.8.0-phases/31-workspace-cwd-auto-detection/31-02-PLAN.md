---
phase: 31-workspace-cwd-auto-detection
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - src/lib/integrations/jira.ts
  - src/lib/integrations/github.ts
  - src/lib/integrations/gitlab.ts
  - src/lib/integrations/gitea.ts
  - tests/lib/integrations/jira.test.ts
  - tests/lib/integrations/github.test.ts
  - tests/lib/integrations/gitlab.test.ts
  - tests/lib/integrations/gitea.test.ts
autonomous: true
requirements: [WUX-02, WUX-03]

must_haves:
  truths:
    - "Running 'git-stacks integration jira issue link PROJ-123' from inside a worktree links the issue without explicit workspace"
    - "Running 'git-stacks integration jira issue link my-ws PROJ-123' still works (backward compatible)"
    - "Running 'git-stacks integration jira issue unlink' from inside a worktree unlinks without explicit workspace"
    - "Running 'git-stacks integration jira issue open' from inside a worktree opens the linked issue"
    - "GitHub, GitLab, and Gitea issue link/unlink/open all support CWD auto-detection"
    - "Running issue commands from outside any workspace prints a clear error with usage hint"
    - "link command with one arg that matches a workspace name but no issue-id prints 'Missing issue ID' error"
  artifacts:
    - path: "src/lib/integrations/jira.ts"
      provides: "Jira issue commands with optional workspace and CWD fallback"
      contains: "resolveWorkspaceArg"
    - path: "src/lib/integrations/github.ts"
      provides: "GitHub issue commands with optional workspace and CWD fallback"
      contains: "resolveWorkspaceArg"
    - path: "src/lib/integrations/gitlab.ts"
      provides: "GitLab issue commands with optional workspace and CWD fallback"
      contains: "resolveWorkspaceArg"
    - path: "src/lib/integrations/gitea.ts"
      provides: "Gitea issue commands with optional workspace and CWD fallback"
      contains: "resolveWorkspaceArg"
    - path: "tests/lib/integrations/jira.test.ts"
      provides: "Tests for CWD fallback and backward compat on Jira issue commands"
    - path: "tests/lib/integrations/github.test.ts"
      provides: "Tests for CWD fallback on GitHub issue commands"
    - path: "tests/lib/integrations/gitlab.test.ts"
      provides: "Tests for CWD fallback on GitLab issue commands"
    - path: "tests/lib/integrations/gitea.test.ts"
      provides: "Tests for CWD fallback on Gitea issue commands"
  key_links:
    - from: "src/lib/integrations/jira.ts"
      to: "src/lib/integrations/issue-utils.ts"
      via: "resolveWorkspaceArg import"
      pattern: "import.*resolveWorkspaceArg.*issue-utils"
    - from: "src/lib/integrations/github.ts"
      to: "src/lib/integrations/issue-utils.ts"
      via: "resolveWorkspaceArg import"
      pattern: "import.*resolveWorkspaceArg.*issue-utils"
    - from: "src/lib/integrations/gitlab.ts"
      to: "src/lib/integrations/issue-utils.ts"
      via: "resolveWorkspaceArg import"
      pattern: "import.*resolveWorkspaceArg.*issue-utils"
    - from: "src/lib/integrations/gitea.ts"
      to: "src/lib/integrations/issue-utils.ts"
      via: "resolveWorkspaceArg import"
      pattern: "import.*resolveWorkspaceArg.*issue-utils"
---

<objective>
Wire CWD auto-detection into all 4 tracker integrations (Jira, GitHub, GitLab, Gitea) by changing Commander.js command signatures from required to optional positionals and adding the resolveWorkspaceArg() fallback logic.

Purpose: This is the user-facing change that makes `<workspace>` optional on all issue commands. After this plan, running `git-stacks integration jira issue link PROJ-123` from inside a worktree just works.

Output: All 4 integration files updated with optional workspace arguments, disambiguation logic for `link` commands, and comprehensive tests for both CWD fallback and backward compatibility.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-workspace-cwd-auto-detection/31-CONTEXT.md
@.planning/phases/31-workspace-cwd-auto-detection/31-RESEARCH.md
@.planning/phases/31-workspace-cwd-auto-detection/31-01-SUMMARY.md

@src/lib/integrations/jira.ts
@src/lib/integrations/github.ts
@src/lib/integrations/gitlab.ts
@src/lib/integrations/gitea.ts
@src/lib/integrations/issue-utils.ts
@tests/lib/integrations/jira.test.ts
@tests/lib/integrations/github.test.ts
@tests/lib/integrations/gitlab.test.ts
@tests/lib/integrations/gitea.test.ts
@tests/lib/integration-commands.test.ts

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From src/lib/integrations/issue-utils.ts (added in Plan 01):
```typescript
export function resolveWorkspaceArg(
  workspaceName: string | undefined,
  tracker: string,
  action: string
): string
// Returns workspace name. Calls process.exit(1) on failure.
// If workspaceName provided: validates via workspaceExists(), returns it.
// If undefined: calls detectWorkspaceFromCwd(), returns workspace.name or exits with error.
```

From src/lib/workspace-ops.ts (added in Plan 01):
```typescript
export type CwdDetectionResult =
  | { ok: true; workspace: Workspace }
  | { ok: false; error: "no_match" }

export function detectWorkspaceFromCwd(cwd?: string): CwdDetectionResult
```

Existing exports still used:
```typescript
// issue-utils.ts
export function linkIssue(workspaceName: string, trackerId: string, issueId: string): void
export function unlinkIssue(workspaceName: string, trackerId: string): void
export function resolveIssueRef(workspaceName: string, trackerId: string): IssueRefResolution
export function formatIssueError(err: IssueRefResolutionError): string

// config.ts
export function workspaceExists(name: string): boolean
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Jira issue commands with CWD fallback and add tests</name>
  <files>src/lib/integrations/jira.ts, tests/lib/integrations/jira.test.ts</files>
  <behavior>
    link command:
    - "link my-ws PROJ-123" (two args) -> calls linkIssue("my-ws", "jira", "PROJ-123") — backward compatible
    - "link PROJ-123" (one arg, not a workspace name) -> detects workspace from CWD, calls linkIssue(detectedName, "jira", "PROJ-123")
    - "link PROJ-123" from outside any workspace -> process.exit(1) with "Could not detect workspace" error
    - "link my-ws" (one arg that IS a workspace name) -> process.exit(1) with "Missing issue ID" error
    - "link" (no args) -> process.exit(1) with "Missing issue ID" error

    unlink command:
    - "unlink my-ws" -> calls unlinkIssue("my-ws", "jira") — backward compatible
    - "unlink" (no args) -> detects workspace from CWD, calls unlinkIssue(detectedName, "jira")

    open command:
    - "open my-ws" -> resolves issue and opens — backward compatible
    - "open" (no args) -> detects workspace from CWD, resolves issue
  </behavior>
  <action>
    **Update src/lib/integrations/jira.ts:**

    1. Add import: `import { resolveWorkspaceArg } from "./issue-utils"` (add to existing import from "./issue-utils")

    2. Change `link` command signature from `link <workspace> <issue-id>` to `link [workspace-or-issue] [issue-id]`:
       ```
       issue.command("link [workspace-or-issue] [issue-id]")
         .description("Link a Jira issue to a workspace (e.g. PROJ-123)")
         .action(async (firstArg: string | undefined, secondArg: string | undefined) => {
       ```
       Disambiguation logic in action handler:
       - If secondArg defined: firstArg is workspace, secondArg is issueId. Validate workspace via workspaceExists(firstArg). If not found, exit(1).
       - If only firstArg defined: check workspaceExists(firstArg). If it IS a workspace name, error "Missing issue ID." If NOT a workspace name, treat firstArg as issueId and call resolveWorkspaceArg(undefined, "jira", "link") for CWD detection.
       - If no args: error "Missing issue ID."
       - Then call linkIssue(workspaceName, "jira", issueId) and print confirmation.

    3. Change `unlink` command from `unlink <workspace>` to `unlink [workspace]`:
       ```
       issue.command("unlink [workspace]")
       ```
       In action: call `resolveWorkspaceArg(workspaceName, "jira", "unlink")` to resolve. Then unlinkIssue as before.

    4. Change `open` command from `open <workspace>` to `open [workspace]`:
       ```
       issue.command("open [workspace]")
       ```
       In action: call `resolveWorkspaceArg(workspaceName, "jira", "open")` to resolve. Then resolveIssueRef as before.

    **Update tests/lib/integrations/jira.test.ts:**

    5. Add mock for resolveWorkspaceArg: in the existing `mock.module("@/lib/integrations/issue-utils", ...)` block, add `resolveWorkspaceArg: resolveWorkspaceArgMock` where `resolveWorkspaceArgMock = mock((name: string | undefined) => name ?? "detected-ws")`.

    6. Add mock for workspaceExists that returns true for "my-ws" specifically: update the existing `mock.module("@/lib/config", ...)` to use a function that checks the name: `workspaceExists: mock((name: string) => name === "my-ws")`.

    7. Add new test cases:
       - "link with two args (backward compat)" — parseAsync with ["issue", "link", "my-ws", "PROJ-123"], expect linkIssue called with ("my-ws", "jira", "PROJ-123")
       - "link with one arg (issue-id, CWD fallback)" — parseAsync with ["issue", "link", "PROJ-123"], expect resolveWorkspaceArg called with (undefined, "jira", "link"), expect linkIssue called with detected workspace name
       - "link with one arg that is a workspace name" — make workspaceExists return true for the arg, parseAsync with ["issue", "link", "my-ws"], expect process.exit(1) (missing issue ID)
       - "link with no args" — parseAsync with ["issue", "link"], expect process.exit(1)
       - "unlink with no args (CWD fallback)" — parseAsync with ["issue", "unlink"], expect resolveWorkspaceArg called with (undefined, "jira", "unlink")
       - "open with no args (CWD fallback)" — parseAsync with ["issue", "open"], expect resolveWorkspaceArg called with (undefined, "jira", "open")

    8. Update existing test "calls linkIssue with jira tracker" to use the two-arg form and verify backward compat.

    IMPORTANT: The existing tests already use `parent.parseAsync(["node", "x", "issue", ...])` pattern. Keep using that. Commander needs `parent.exitOverride()` to prevent exit on parse errors — add it like github.test.ts does in buildParent().

    IMPORTANT Commander.js behavior: When both positionals are optional and zero args are given, Commander does NOT error — it calls the action with both args as undefined. The "link with no args" test should expect the action handler to detect both undefined and exit(1).
  </action>
  <verify>
    <automated>cd /home/nnex/dev/prj/git-stacks && bun test tests/lib/integrations/jira.test.ts</automated>
  </verify>
  <done>
    - Jira link/unlink/open commands accept optional workspace with CWD fallback
    - Two-arg backward compat preserved for link command
    - Disambiguation handles workspace-name-only case with clear error
    - All existing + new tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Update GitHub, GitLab, and Gitea issue commands with CWD fallback and tests</name>
  <files>src/lib/integrations/github.ts, src/lib/integrations/gitlab.ts, src/lib/integrations/gitea.ts, tests/lib/integrations/github.test.ts, tests/lib/integrations/gitlab.test.ts, tests/lib/integrations/gitea.test.ts</files>
  <action>
    Apply the same pattern from Task 1 to the remaining 3 tracker integrations. All 3 follow identical command structures.

    **For each of github.ts, gitlab.ts, gitea.ts:**

    1. Add `resolveWorkspaceArg` to the import from `"./issue-utils"`.

    2. Change `link <workspace> <issue-id>` to `link [workspace-or-issue] [issue-id]` with the SAME disambiguation logic as Jira:
       - Two args: first is workspace, second is issue-id (backward compat)
       - One arg: check workspaceExists(). If workspace -> "Missing issue ID" error. If not workspace -> treat as issue-id, CWD detect workspace.
       - No args: "Missing issue ID" error.
       - Use `resolveWorkspaceArg(undefined, "{tracker}", "link")` for CWD detection.
       - Replace `workspaceExists` inline check with the disambiguation block.

    3. Change `unlink <workspace>` to `unlink [workspace]`:
       - In action: `const resolved = resolveWorkspaceArg(workspaceName, "{tracker}", "unlink")`
       - Then call unlinkIssue(resolved, "{tracker}")

    4. Change `open <workspace> [repo]` to `open [workspace] [repo]` for GitHub, GitLab, Gitea:
       CRITICAL Commander.js note: When the signature is `open [workspace] [repo]` and only one arg is given, Commander assigns it to position 0 (workspace). This is correct for backward compat — a single arg IS the workspace name. When zero args are given, both are undefined and we CWD-detect.

       BUT there is a subtlety for `open`: When both positionals are optional and one arg is given, it COULD be a repo name if the user is inside a worktree and wants to specify which repo. However, the existing pattern is that `open` always requires workspace as the first positional and repo is truly optional. Keep this: one arg = workspace name. Zero args = CWD detect. This is backward compatible.

       In action: call `resolveWorkspaceArg(workspaceName, "{tracker}", "open")` to resolve. The rest of the logic (resolveIssueRef, resolveForgeRepo, etc.) stays unchanged — just use the resolved workspace name.

    **For each test file (github.test.ts, gitlab.test.ts, gitea.test.ts):**

    5. Add `resolveWorkspaceArg` mock to the existing `mock.module("@/lib/integrations/issue-utils", ...)` block.

    6. Update `mock.module("@/lib/config", ...)` to have workspaceExists return true for "my-ws" specifically (function-based mock).

    7. Add CWD fallback tests for each command:
       - "issue link with one arg (CWD fallback)" — single issue-id arg, expect resolveWorkspaceArg called
       - "issue unlink with no args (CWD fallback)" — no args, expect resolveWorkspaceArg called with undefined
       - "issue open with no args (CWD fallback)" — no args, expect resolveWorkspaceArg called with undefined

    8. Verify existing two-arg tests still pass (backward compat).

    IMPORTANT: github.test.ts, gitlab.test.ts, and gitea.test.ts all use `buildParent()` with `parent.exitOverride()`. Keep this pattern. For gitlab.test.ts and gitea.test.ts, check if they have `buildParent()` — if not, add it following the github.test.ts pattern.

    TRACKER IDS: Use "github" for GitHub, "gitlab" for GitLab, "gitea" for Gitea in resolveWorkspaceArg calls.

    After all 3 are updated, run the full test suite to verify nothing is broken.
  </action>
  <verify>
    <automated>cd /home/nnex/dev/prj/git-stacks && bun test tests/lib/integrations/github.test.ts tests/lib/integrations/gitlab.test.ts tests/lib/integrations/gitea.test.ts</automated>
  </verify>
  <done>
    - GitHub, GitLab, and Gitea issue link/unlink/open all accept optional workspace with CWD fallback
    - Backward compat preserved: two-arg link and explicit workspace still work
    - CWD fallback tests pass for all 3 trackers
    - Full test suite: `bun test tests/` passes with no regressions
  </done>
</task>

</tasks>

<verification>
bun test tests/lib/integrations/jira.test.ts tests/lib/integrations/github.test.ts tests/lib/integrations/gitlab.test.ts tests/lib/integrations/gitea.test.ts tests/lib/detect-workspace-cwd.test.ts
bun test tests/lib/integration-commands.test.ts
bun run typecheck
bun test tests/
</verification>

<success_criteria>
- All 4 tracker integrations (jira, github, gitlab, gitea) have optional [workspace] on link/unlink/open commands
- link command disambiguates single arg: workspace name -> "Missing issue ID" error; issue-id -> CWD detection
- unlink and open with no workspace arg trigger CWD detection via resolveWorkspaceArg
- Backward compatibility: all existing two-arg forms still work identically
- Full test suite green with no regressions
- TypeScript typecheck passes
</success_criteria>

<output>
After completion, create `.planning/phases/31-workspace-cwd-auto-detection/31-02-SUMMARY.md`
</output>
