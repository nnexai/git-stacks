---
phase: quick
plan: 260403-j1s
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tui/dashboard/run.tsx
autonomous: true
must_haves:
  truths:
    - "bun run typecheck passes with zero errors"
    - "bun run test passes with no regressions"
  artifacts:
    - path: "src/tui/dashboard/run.tsx"
      provides: "Dashboard render config with updated OpenTUI API"
      contains: "screenMode"
  key_links: []
---

<objective>
Fix TypeScript error caused by OpenTUI upgrade from 0.1.87 to 0.1.96.

Purpose: The `useAlternateScreen` property was removed from `CliRendererConfig` and replaced by `screenMode: ScreenMode`. The typecheck currently fails.
Output: Clean typecheck, no regressions.
</objective>

<execution_context>
@.planning/quick/260403-j1s-fix-usealternatescreen-ts-error-after-op/260403-j1s-RESEARCH.md
</execution_context>

<context>
@src/tui/dashboard/run.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace useAlternateScreen with screenMode in run.tsx</name>
  <files>src/tui/dashboard/run.tsx</files>
  <action>
    In `src/tui/dashboard/run.tsx` line 71, replace:
    ```
    useAlternateScreen: true,
    ```
    with:
    ```
    screenMode: "alternate-screen",
    ```
    No other changes needed -- `targetFps`, `exitOnCtrlC`, and `onDestroy` are all still valid in `CliRendererConfig`.

    Also update CLAUDE.md to reflect the new installed versions of OpenTUI packages:
    - `@opentui/core 0.1.87` -> `@opentui/core 0.1.96`
    - `@opentui/solid 0.1.87` -> `@opentui/solid 0.1.96`
  </action>
  <verify>
    <automated>cd /home/nnex/dev/prj/git-stacks && bun run typecheck && bun run test</automated>
  </verify>
  <done>
    - `bun run typecheck` exits 0 with no errors
    - `bun run test` passes with no regressions
    - Line 71 of run.tsx reads `screenMode: "alternate-screen",`
  </done>
</task>

</tasks>

<verification>
bun run typecheck exits 0
bun run test exits 0
</verification>

<success_criteria>
TypeScript error TS2353 on run.tsx line 71 is resolved. All existing tests pass.
</success_criteria>

<output>
Commit with message: fix: replace useAlternateScreen with screenMode for OpenTUI 0.1.96 API
</output>
