---
phase: quick
plan: 260402-6c0
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "CHANGELOG.md v0.12.0 section documents all three new phases (50, 50.1, 51)"
    - "README.md documents port allocation feature with YAML examples and CLI usage"
    - "README.md integration helper commands section includes new subcommands (list, config, aerospace focus, vscode open)"
    - "No new version header created — entries extend existing v0.12.0 section"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Phase 50, 50.1, 51 entries under v0.12.0"
      contains: "Integration helper commands"
    - path: "README.md"
      provides: "Port Allocation section, updated Integration helper commands"
      contains: "Port Allocation"
  key_links: []
---

<objective>
Update CHANGELOG.md and README.md to document features from phases 50, 50.1, and 51 under the existing v0.12.0 release.

Purpose: Complete release documentation for v0.12.0 covering integration-specific tools, argument-based dynamic completion, and workspace port allocation.
Output: Updated CHANGELOG.md and README.md ready for publish.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CHANGELOG.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 50, 50.1, 51 entries to CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <action>
Add new entries UNDER the existing `## [0.12.0] — 2026-03-29` section, AFTER the existing Added subsection content (after the cross-entry snapshot isolation paragraph, before the `---` separator that precedes v0.11.1). Do NOT create a new version header. Extend the existing `### Added` block.

Add these entries to the `### Added` section:

**Phase 50 — Integration specific tools:**

**Integration config introspection** — `git-stacks integration list` shows a table of all integrations with their enabled state and order (`--json` for scripting). `git-stacks integration <id> config example` prints a YAML config snippet for the integration (available for aerospace, vscode, niri, tmux; others show a fallback message). `git-stacks integration <id> config show [workspace]` displays the resolved config for an integration with global, template, and workspace cascade (`--json` for scripting).

**Integration action commands** — `git-stacks integration aerospace focus <workspace>` focuses the AeroSpace workspace configured for a workspace (resolves `focus: true` entry or falls back to `workspaces[0]`). `git-stacks integration vscode open <workspace>` generates and opens the `.code-workspace` file for a workspace. Integrations can now declare a `commands()` method to register per-integration CLI subcommands.

**Phase 50.1 — Argument-based dynamic completion (internal heading style, shorter):**

**Convention-based completion inference** — shell completion generation now infers dynamic completion types from Commander.js argument names (`<workspace>`, `<template>`, `<repo>`, `<integration>`) instead of maintaining a manual 50-entry lookup table (reduced to 4 override entries). Also adds multi-position argument dispatch for commands with 2+ args (e.g., `run <workspace> [repo]`), `argChoices` extraction for `.choices()` arguments, and integration ID completion across bash/zsh/fish.

**Phase 51 — Workspace port allocation:**

**Workspace port allocation** — templates and workspaces can declare named port slots (`ports: { PORT: ~, DEBUG_PORT: ~ }`) that are automatically allocated from a contiguous range when `git-stacks open` runs. Allocated port numbers are injected as environment variables into hooks and integration contexts via `mergeEnv`. Ports are freed implicitly when a workspace is removed.

Key details to mention:
- Global config `ports.range_start` (default 10000) and `ports.range_end` (default 65000) control the allocation range
- `git-stacks open --reallocate` forces reallocation of conflicting ports
- Race-safe allocation via O_EXCL filesystem lockfile
- Atomic YAML writes with fsync-before-rename
- Port name prompt in workspace creation wizard (after description, before integration overrides)
- Template composition merges ports with last-wins precedence
- Conflict detection against env vars and env_file entries

Also add a `### Changed` subsection (after Added) if not already present under v0.12.0:

**Atomic config writes** — `writeYaml` now uses fsync-before-rename for crash-safe persistence (previously used direct `writeFileSync`).

Also add a `### Internal` subsection:

**Completion generator refactor** — `DYNAMIC_COMPLETIONS` lookup table replaced by convention-based inference from Commander.js argument names. `ArgCompletion` interface replaces `CommandNode.dynamic` + `firstArgRequired` fields.
  </action>
  <verify>
    <automated>grep -c "port allocation\|Integration config introspection\|Convention-based completion\|Integration action commands" CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG.md v0.12.0 section contains entries for all three phases: integration config introspection + action commands (Phase 50), convention-based completion inference (Phase 50.1), workspace port allocation (Phase 51). No new version header created.</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md with port allocation and integration commands</name>
  <files>README.md</files>
  <action>
Two changes to README.md:

**1. Add Port Allocation section** — Insert a new `## Port Allocation` section AFTER the `## Hooks & Env Injection` section and BEFORE the `## Shell Completions` section. Content:

Explain that templates and workspaces can declare named port slots that are automatically allocated on `git-stacks open`. Show YAML example of declaring ports in a template:

```yaml
# In a template or workspace YAML
ports:
  API_PORT: ~
  DEBUG_PORT: ~
```

Explain that `~` means "allocate on open" and allocated values are written back:

```yaml
# After git-stacks open (allocated values written back)
ports:
  API_PORT: 10000
  DEBUG_PORT: 10001
```

Explain that allocated port numbers are injected as environment variables into hooks, integration contexts, and env_files. Show the global config range control:

```yaml
# ~/.config/git-stacks/config.yml
ports:
  range_start: 10000
  range_end: 65000
```

Mention `git-stacks open --reallocate` to force reallocation of conflicting ports. Note that ports are freed automatically when a workspace is removed. Mention the creation wizard prompts for port names. Note race-safe allocation via lockfile for concurrent workspace opens.

**2. Update Integration helper commands** — The current section under `## Integrations` shows only tmux attach and niri focus-workspace. Replace the "Integration helper commands:" code block with the full set:

```bash
git-stacks integration list                              # List all integrations with status
git-stacks integration <id> config example               # Show YAML config snippet
git-stacks integration <id> config show [workspace]      # Show resolved config (--json)
git-stacks integration tmux attach <workspace>           # Attach to workspace tmux session
git-stacks integration niri focus-workspace <workspace>  # Focus workspace niri workspace
git-stacks integration aerospace focus <workspace>       # Focus workspace AeroSpace workspace
git-stacks integration vscode open <workspace>           # Open workspace in VSCode
```

Keep the surrounding prose paragraph ("Integration helper commands:") as-is, just update the code block.
  </action>
  <verify>
    <automated>grep -c "Port Allocation\|integration list\|integration.*config example\|aerospace focus\|vscode open" README.md</automated>
  </verify>
  <done>README.md contains a Port Allocation section with YAML examples and CLI usage. Integration helper commands section lists all 7 subcommands including list, config example, config show, aerospace focus, and vscode open.</done>
</task>

</tasks>

<verification>
- `grep "## \[0.12.0\]" CHANGELOG.md` returns exactly one match (no duplicate version header)
- `grep -c "port allocation\|Integration config\|Convention-based completion" CHANGELOG.md` returns 3+ (all phases documented)
- `grep "## Port Allocation" README.md` returns a match
- `grep "integration list" README.md` returns a match
- `grep "aerospace focus" README.md` returns a match
</verification>

<success_criteria>
- CHANGELOG.md v0.12.0 section covers Phase 50 (integration list/config/focus/open), Phase 50.1 (convention-based completion), and Phase 51 (port allocation with all key details)
- README.md has a Port Allocation section with YAML examples for declaration, allocation result, and global config
- README.md integration helper commands block lists all 7 subcommands
- No new version bumps or version headers
- package.json untouched
</success_criteria>

<output>
After completion, create `.planning/quick/260402-6c0-update-changelog-and-readme-to-prepare-f/260402-6c0-SUMMARY.md`
</output>
