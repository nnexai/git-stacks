---
phase: quick
plan: 260323-cd3
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/doctor.ts
  - src/commands/install.ts
  - src/commands/repo.ts
  - src/commands/template.ts
  - src/commands/workspace.ts
  - src/lib/integrations/cmux.ts
  - src/lib/integrations/tmux.ts
  - src/lib/integrations/niri.ts
  - package.json
  - CHANGELOG.md
  - CLAUDE.md
autonomous: true
requirements: [FIX-PATH-ALIAS, VERSION-BUMP, CHANGELOG-UPDATE]

must_haves:
  truths:
    - "Published npm package resolves all imports at runtime (no @/ path alias failures)"
    - "package.json version is 0.7.1"
    - "CHANGELOG.md has a v0.7.1 entry describing the fix"
    - "CLAUDE.md documents @/* as test-only to prevent recurrence"
    - "All existing tests still pass"
  artifacts:
    - path: "src/commands/doctor.ts"
      provides: "Relative import replacing @/tui/utils"
      contains: "../tui/utils"
    - path: "src/lib/integrations/cmux.ts"
      provides: "Relative import replacing @/tui/utils"
      contains: "../../tui/utils"
    - path: "package.json"
      provides: "Version bump"
      contains: '"0.7.1"'
    - path: "CHANGELOG.md"
      provides: "v0.7.1 release entry"
      contains: "[0.7.1]"
  key_links:
    - from: "all 8 src/ files"
      to: "src/tui/utils.ts"
      via: "relative imports (not @/ alias)"
      pattern: '"\.\./tui/utils"|"\.\./\.\./tui/utils"'
---

<objective>
Fix the "Cannot find module @/tui/utils" error that occurs when git-stacks is installed via npm. Replace all 8 `@/tui/utils` imports in `src/` with relative paths, bump version to v0.7.1, update CHANGELOG, and update CLAUDE.md to mark `@/*` as test-only.

Purpose: The published npm package does not ship tsconfig.json, and Bun does not resolve tsconfig path aliases from within node_modules. This makes the CLI crash on any installed user's machine when commands import from `@/tui/utils`.

Output: Working npm-publishable package with all imports resolved via relative paths.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260323-cd3-fix-cannot-find-module-tui-utils-path-al/260323-cd3-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace @/tui/utils imports with relative paths in all 8 src/ files</name>
  <files>src/commands/doctor.ts, src/commands/install.ts, src/commands/repo.ts, src/commands/template.ts, src/commands/workspace.ts, src/lib/integrations/cmux.ts, src/lib/integrations/tmux.ts, src/lib/integrations/niri.ts</files>
  <action>
In each of the 8 files listed below, find the import that references `@/tui/utils` and replace it with the correct relative path. Do NOT touch any other imports in these files. Do NOT modify test files.

For files in `src/commands/` (5 files):
- `src/commands/doctor.ts`: replace `@/tui/utils` with `../tui/utils`
- `src/commands/install.ts`: replace `@/tui/utils` with `../tui/utils`
- `src/commands/repo.ts`: replace `@/tui/utils` with `../tui/utils`
- `src/commands/template.ts`: replace `@/tui/utils` with `../tui/utils`
- `src/commands/workspace.ts`: replace `@/tui/utils` with `../tui/utils`

For files in `src/lib/integrations/` (3 files):
- `src/lib/integrations/cmux.ts`: replace `@/tui/utils` with `../../tui/utils`
- `src/lib/integrations/tmux.ts`: replace `@/tui/utils` with `../../tui/utils`
- `src/lib/integrations/niri.ts`: replace `@/tui/utils` with `../../tui/utils`

After editing, verify no `@/` imports remain in `src/` (excluding comments):
```bash
grep -rn "from ['\"]@/" src/ --include="*.ts" --include="*.tsx" | grep -v "^.*//.*@/" | grep -v "\.test\."
```
That command should return zero results.
  </action>
  <verify>
    <automated>cd /home/nnex/dev/prj/git-stacks && bun run typecheck && bun test tests/</automated>
  </verify>
  <done>All 8 files use relative imports. No active @/ imports remain in src/ (only in test files and comments). Typecheck passes, all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Bump version to v0.7.1, update CHANGELOG, update CLAUDE.md convention</name>
  <files>package.json, CHANGELOG.md, CLAUDE.md</files>
  <action>
1. **package.json**: Change `"version": "0.7.0"` to `"version": "0.7.1"`.

2. **CHANGELOG.md**: Add a new section at the top (after the `---` separator, before the `## [0.7.0]` entry):

```
## [0.7.1] — 2026-03-23

### Fixed

- **Published package fails with "Cannot find module @/tui/utils"** — replaced all `@/*` path alias imports in `src/` with relative paths. Bun does not resolve tsconfig path aliases from within `node_modules`, so the `@/*` shorthand only works in local development/test contexts. The 8 affected files in `src/commands/` and `src/lib/integrations/` now use `../tui/utils` or `../../tui/utils` respectively.
```

3. **CLAUDE.md**: Update two locations to mark `@/*` as test-only:

   a. Line 14 (top-level description): Change:
      `No build step — Bun executes TypeScript source directly. The @/* path alias resolves to ./src/*.`
      To:
      `No build step — Bun executes TypeScript source directly. The @/* path alias resolves to ./src/* but is **test-only** — production code in src/ must use relative imports (the alias is not available in the published npm package).`

   b. Lines 195-196 (Import Organization section): Change:
      ```
      - `@/*` resolves to `./src/*` (configured in `tsconfig.json`)
      - Used in tests and deep-nested imports: `import { cancel } from "@/tui/utils"`
      ```
      To:
      ```
      - `@/*` resolves to `./src/*` (configured in `tsconfig.json`) — **test-only**; do NOT use in `src/` production code (the alias is not available in the published npm package since Bun does not resolve tsconfig paths from within `node_modules`)
      - Used in tests only: `import type { X } from "@/tui/utils"` (type imports are erased at compile time)
      ```
  </action>
  <verify>
    <automated>cd /home/nnex/dev/prj/git-stacks && grep '"version": "0.7.1"' package.json && grep '\[0.7.1\]' CHANGELOG.md && grep 'test-only' CLAUDE.md</automated>
  </verify>
  <done>package.json version is 0.7.1, CHANGELOG has v0.7.1 fix entry, CLAUDE.md documents @/* as test-only in both locations.</done>
</task>

<task type="auto">
  <name>Task 3: Verify published package works by unlinking and re-linking</name>
  <files></files>
  <action>
Run the following verification sequence to confirm the fix works in a simulated "installed" context:

1. Unlink the current global install:
   ```bash
   npm unlink -g git-stacks 2>/dev/null || true
   ```

2. Re-link from the project:
   ```bash
   cd /home/nnex/dev/prj/git-stacks && npm link
   ```

3. Test that the CLI runs without module resolution errors:
   ```bash
   git-stacks --help
   git-stacks --version
   ```

4. Confirm version output shows `0.7.1`.

If any command fails with "Cannot find module", the fix is incomplete.
  </action>
  <verify>
    <automated>cd /home/nnex/dev/prj/git-stacks && npm unlink -g git-stacks 2>/dev/null; npm link && git-stacks --version 2>&1 | grep -q "0.7.1"</automated>
  </verify>
  <done>git-stacks --help and --version run without errors. Version output is 0.7.1. No "Cannot find module" errors.</done>
</task>

</tasks>

<verification>
1. `bun run typecheck` passes with zero errors
2. `bun test tests/` passes all tests
3. No active `@/` imports in `src/` (only in tests and comments)
4. `git-stacks --version` outputs 0.7.1 after npm link
5. `git-stacks --help` runs without module resolution errors
</verification>

<success_criteria>
- All 8 `@/tui/utils` imports in src/ replaced with relative paths
- package.json version is 0.7.1
- CHANGELOG.md has v0.7.1 fix entry
- CLAUDE.md updated to document @/* as test-only (2 locations)
- All tests pass, typecheck passes
- CLI works correctly after npm link (simulating installed package)
</success_criteria>

<output>
After completion, create `.planning/quick/260323-cd3-fix-cannot-find-module-tui-utils-path-al/260323-cd3-SUMMARY.md`
</output>
