---
phase: quick
plan: 260403-x04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/secrets.ts
  - tests/lib/secrets.test.ts
autonomous: true
must_haves:
  truths:
    - "New key=value,key=value syntax resolves keychain secrets on Linux with arbitrary attributes"
    - "Legacy service/account syntax still works unchanged"
    - "macOS rejects more than 2 attributes with a clear error"
    - "macOS maps first pair to -s, second pair to -a"
  artifacts:
    - path: "src/lib/secrets.ts"
      provides: "parseKeychainPath returning KeychainAttr[] and keychainResolver consuming it"
      contains: "KeychainAttr"
    - path: "tests/lib/secrets.test.ts"
      provides: "Tests for new syntax, legacy compat, macOS limit"
      contains: "key=value"
  key_links:
    - from: "src/lib/secrets.ts:parseKeychainPath"
      to: "src/lib/secrets.ts:keychainResolver"
      via: "parseKeychainPath returns KeychainAttr[], resolver builds command from it"
      pattern: "parseKeychainPath\\(path\\)"
---

<objective>
Make the keychain resolver's attribute names configurable instead of hardcoded `service` and `account`.

Purpose: Allow users to specify arbitrary key-value attributes for `secret-tool lookup` on Linux, while maintaining backward compatibility with the existing `service/account` path format and respecting macOS's 2-attribute limit.

Output: Updated `src/lib/secrets.ts` with new parsing and command building, plus comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/secrets.ts
@tests/lib/secrets.test.ts
</context>

<interfaces>
<!-- Current parseKeychainPath signature being replaced -->
From src/lib/secrets.ts:
```typescript
function parseKeychainPath(path: string): { service: string; account: string }
```
<!-- keychainResolver calls parseKeychainPath and builds platform-specific command -->
```typescript
export const keychainResolver: SecretResolver = {
  id: "keychain",
  async resolve(path: string): Promise<string> {
    const { service, account } = parseKeychainPath(path)
    // ... builds command from service/account
  }
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor parseKeychainPath to support arbitrary key-value attributes</name>
  <files>src/lib/secrets.ts, tests/lib/secrets.test.ts</files>
  <behavior>
    - Test: New syntax `service=myapp,account=db` parses to `[{key: "service", value: "myapp"}, {key: "account", value: "db"}]`
    - Test: Arbitrary N pairs `app=myapp,env=prod,key=db` parses to 3 attributes
    - Test: Legacy syntax `myapp/db` parses to `[{key: "service", value: "myapp"}, {key: "account", value: "db"}]` (backward compat)
    - Test: Detection heuristic — presence of `=` in path triggers new syntax, absence triggers legacy
    - Test: Empty key or empty value in new syntax throws descriptive error
    - Test: Legacy path without slash still throws (existing behavior preserved)
  </behavior>
  <action>
    1. Define `export interface KeychainAttr { key: string; value: string }` in secrets.ts.

    2. Refactor `parseKeychainPath(path: string): KeychainAttr[]`:
       - If `path` contains `=`: new syntax mode
         - Split on `,` to get pairs
         - Split each pair on first `=` to get key and value
         - Validate: key and value must be non-empty, throw `Invalid keychain attribute '...' — expected 'key=value'` on failure
         - Return array of `{ key, value }`
       - Else: legacy syntax mode (no `=` anywhere)
         - Split on `/` (existing behavior)
         - If no slash, throw existing error: `Invalid keychain path '...' — expected 'service/account'`
         - Return `[{ key: "service", value: first }, { key: "account", value: rest }]`

    3. Export `parseKeychainPath` so tests can import it directly (currently unexported).

    4. Write tests FIRST (RED), then implement (GREEN). Update the existing `keychainResolver` test that checks "expected 'service/account'" — it should still pass for paths without `=`.
  </action>
  <verify>
    <automated>bun run test -- tests/lib/secrets.test.ts</automated>
  </verify>
  <done>parseKeychainPath handles both `key=val,key=val` and legacy `service/account` formats, all new tests pass, existing tests still pass</done>
</task>

<task type="auto">
  <name>Task 2: Update keychainResolver to use KeychainAttr[] and enforce macOS limit</name>
  <files>src/lib/secrets.ts, tests/lib/secrets.test.ts</files>
  <behavior>
    - Test: Linux command for `service=myapp,account=db` produces `["secret-tool", "lookup", "service", "myapp", "account", "db"]`
    - Test: Linux command for `app=x,env=y,key=z` produces `["secret-tool", "lookup", "app", "x", "env", "y", "key", "z"]` (3 pairs)
    - Test: macOS command for `service=myapp,account=db` produces `["security", "find-generic-password", "-s", "myapp", "-a", "db", "-w"]`
    - Test: macOS with >2 attributes throws error containing "macOS supports at most 2 keychain attributes"
    - Test: macOS maps first attribute to `-s` and second to `-a` regardless of key names
    - Test: Legacy format `myapp/db` still builds correct commands on both platforms
  </behavior>
  <action>
    1. Update `keychainResolver.resolve()` to consume the `KeychainAttr[]` return from `parseKeychainPath`.

    2. Linux command building:
       - Start with `["secret-tool", "lookup"]`
       - For each attr, append `attr.key`, `attr.value` (flatMap)
       - No attribute count limit

    3. macOS command building:
       - If `attrs.length > 2`, throw `Error("macOS supports at most 2 keychain attributes (-s service, -a account). Got ${attrs.length}.")`
       - Build: `["security", "find-generic-password"]`
       - First attr maps to `-s ${attrs[0].value}`
       - Second attr (if present) maps to `-a ${attrs[1].value}`
       - Append `-w`

    4. For testing, since `keychainResolver` spawns actual processes, test the macOS >2 limit by temporarily mocking `process.platform` to `"darwin"` (or structurally: extract the command-building into a testable `buildKeychainCommand(attrs: KeychainAttr[], platform: string): string[]` helper, export it, and test that directly without spawning). Prefer the extracted helper approach since it avoids platform mocking.

    5. Extract `buildKeychainCommand(attrs: KeychainAttr[], platform: NodeJS.Platform): string[]` as an exported function. Have `keychainResolver.resolve()` call it with `process.platform`. Test `buildKeychainCommand` directly for all platform/attr-count scenarios.
  </action>
  <verify>
    <automated>bun run test -- tests/lib/secrets.test.ts</automated>
  </verify>
  <done>keychainResolver uses arbitrary attributes on Linux, enforces 2-attribute max on macOS with clear error, legacy paths work on both platforms, all tests pass including typecheck</done>
</task>

</tasks>

<verification>
```bash
bun run test -- tests/lib/secrets.test.ts && bun run typecheck
```
</verification>

<success_criteria>
- `${{ keychain:service=myapp,account=db }}` resolves identically to legacy `${{ keychain:myapp/db }}`
- `${{ keychain:app=x,env=y,key=z }}` works on Linux (3 attributes to secret-tool)
- `${{ keychain:app=x,env=y,key=z }}` fails fast on macOS with clear error
- Legacy `${{ keychain:myapp/db }}` still works unchanged on both platforms
- All existing tests pass, no regressions
- `bun run typecheck` passes
</success_criteria>

<output>
After completion, create `.planning/quick/260403-x04-make-keychain-resolver-attribute-names-c/260403-x04-SUMMARY.md`
</output>
