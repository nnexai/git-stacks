---
phase: quick
plan: 260329-9ll
type: execute
wave: 1
depends_on: []
files_modified: [package.json, bun.lock]
autonomous: true
requirements: []

must_haves:
  truths:
    - "All dependencies and devDependencies are at their latest published versions"
    - "TypeScript type-checking passes with zero errors"
    - "All unit and integration tests pass"
    - "No breaking changes remain unresolved"
  artifacts:
    - path: "package.json"
      provides: "Updated dependency version ranges"
    - path: "bun.lock"
      provides: "Resolved lockfile with latest versions"
  key_links:
    - from: "package.json"
      to: "bun.lock"
      via: "bun update resolves and locks versions"
      pattern: "version ranges in package.json match resolved versions in bun.lock"
---

<objective>
Upgrade all dependencies (both dependencies and devDependencies) to their latest versions, verify the project still builds and passes all tests, and fix any breaking changes.

Purpose: Keep dependencies current for security patches, bug fixes, and new features.
Output: Updated package.json and bun.lock with all packages at latest versions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@package.json
@bun.lock

Current installed versions (baseline):
- @clack/prompts: 0.9.1
- @opentui/core: 0.1.87
- @opentui/solid: 0.1.87
- @types/bun: 1.3.10
- commander: 12.1.0
- opentui-spinner: 0.0.6
- solid-js: 1.9.11
- typescript: 5.9.3
- yaml: 2.8.2
- zod: 3.25.76
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade all dependencies to latest versions</name>
  <files>package.json, bun.lock</files>
  <action>
    Run `bun update` to update all packages to the latest versions allowed by semver ranges.

    Then check if any packages have newer major versions available beyond the current ranges:
    ```bash
    bun outdated
    ```

    If `bun outdated` shows packages with newer versions outside current ranges, manually update the version ranges in package.json to allow the latest versions, then run `bun install` again.

    After updating, run `bun pm ls` to confirm all packages resolved to their latest versions.

    Note: `@types/bun` is pinned to `latest` — just run `bun update` and it will pull the newest.
  </action>
  <verify>
    <automated>cd /home/nnex/dev/prj/git-stacks && bun pm ls 2>/dev/null | head -20</automated>
  </verify>
  <done>All dependencies and devDependencies are at their latest published versions. package.json ranges updated if needed. bun.lock reflects resolved latest versions.</done>
</task>

<task type="auto">
  <name>Task 2: Verify typecheck and tests pass, fix breaking changes</name>
  <files>package.json, bun.lock</files>
  <action>
    Run the TypeScript type checker:
    ```bash
    bun run typecheck
    ```

    If typecheck fails, investigate the errors. Common breaking change patterns to watch for:
    - Zod API changes (schema methods renamed or removed)
    - Commander.js option/argument API changes
    - SolidJS reactivity or JSX type changes
    - @opentui component prop changes
    - TypeScript stricter checks in newer versions

    Fix any type errors in the affected source files.

    Then run the full test suite using the project's isolated test runner (NEVER `bun test tests/` directly):
    ```bash
    bun run test
    ```

    If tests fail, investigate and fix. Distinguish between:
    1. Genuine breaking changes from upgraded deps (fix the code)
    2. Test infrastructure issues (fix the test setup)
    3. Pre-existing flaky tests (note but don't block on these)

    Important: Use `bun run test` (the project's isolated test runner), not `bun test tests/` directly, to avoid mock pollution between test files.
  </action>
  <verify>
    <automated>cd /home/nnex/dev/prj/git-stacks && bun run typecheck && bun run test</automated>
  </verify>
  <done>TypeScript type-checking passes with zero errors. All tests pass. Any breaking changes from dependency upgrades are resolved.</done>
</task>

</tasks>

<verification>
1. `bun pm ls` shows all packages at latest versions
2. `bun run typecheck` exits 0
3. `bun run test` exits 0 with all tests passing
</verification>

<success_criteria>
- All dependencies upgraded to latest versions
- Zero typecheck errors
- All tests green
- package.json and bun.lock committed with updates
</success_criteria>

<output>
After completion, create `.planning/quick/260329-9ll-upgrade-dependencies/260329-9ll-SUMMARY.md`
</output>
