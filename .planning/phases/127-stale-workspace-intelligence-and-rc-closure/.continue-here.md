---
context: phase
phase: 127-stale-workspace-intelligence-and-rc-closure
plan: 12
task: 1
total_tasks: 2
status: in_progress
last_updated: 2026-07-17T18:38:20.323Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure or is an explicit authority boundary.
> Acknowledge each one explicitly before proceeding.

- [ ] **CONTROLLED AGENT COUNT:** The user's “up to 5 agents” allowance was violated when recursive fan-out created hundreds of agents. Resume **solo**. Do not launch an agent or workflow unless the user explicitly authorizes it again. Any later numeric cap includes all nested/recursive work, not just top-level calls.
- [ ] **NEW EXACT SHA REQUIRED:** `f7bdca75f2545664251e88b233693b67fd37ee5c` predates three runtime fixes and is superseded as the intended RC candidate. Do not reuse its deterministic or human approvals. Run a fresh complete regression, freeze a new clean SHA, and reset every hosted/authenticated/manual receipt to PENDING for that SHA.
- [ ] **RELEASE AUTHORITY ABSENT:** Do not tag, push, publish packages, create a GitHub Release, or dispatch release-only workflows. Candidate or feature approval never implies release authorization.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Recursive fan-out hidden behind a top-level cap | A five-agent allowance was interpreted as five top-level agents whose work recursively spawned far more; the user stopped the run. | blocking | Resume solo. If agents are explicitly reauthorized, use flat calls and make “no nested agents” part of every dispatched scope; count the full recursive total against the cap. |
| Reusing a pre-fix candidate | Runtime smoke testing found a >16-catalog failure and fatal TUI terminal leak after the old SHA had been frozen and receipted. | blocking | Full regression and manual smoke precede the replacement freeze; all exact-SHA ledgers and approvals are regenerated for the new SHA. |
| Crossing the release boundary | The milestone prepares an RC but no outward release action was authorized. | blocking | Treat tag, push, publication, GitHub Release, and release-workflow dispatch as separate hard-confirmation operations. |
| Treating Node 24 as a ceiling | The initial web failure was incorrectly attributed to Node 26; the user confirmed Node 26 works and Node 24 is only the supported floor/default target. | advisory | Diagnose the actual failing path and stack trace. Validate Node 24 as the floor without claiming newer Node versions are unsupported. |
| Assuming launcher recovery is proven | The first `web` launch returned `LocalEnvironmentPreparationError`; manually starting the service allowed progress, but the one-step launch path was not reproduced after the fixes. | advisory | Rebuild, stop any service, retry the normal one-command launch, and investigate only if the failure reproduces. |

<current_state>

Phase 127 is paused between completed Plan 127-12 and the two Plan 127-13 human checkpoints. Corrective work effectively returns to Plan 127-12 Task 1 because the old exact candidate is no longer the intended RC.

Before this handoff commit:

- Branch: `planning/phase-127-revision-1`
- Clean HEAD: `9d319c3243b594d3a3d71c15a51234099419a8e7`
- Package version: `0.22.0-rc.1`
- Old, superseded candidate: `f7bdca75f2545664251e88b233693b67fd37ee5c`
- Plan summaries exist through `127-12-SUMMARY.md`; `127-13-SUMMARY.md` and `127-14-SUMMARY.md` do not exist.
- `.planning/STATE.md`, `127-RECEIPTS.md`, `127-VALIDATION.md`, and `127-12-SUMMARY.md` still refer to the old candidate and are not current evidence for the repaired source.
- The user selected pending hosted/authenticated evidence. Do not start Plan 127-14 or milestone closure.
- No outstanding async-job manifests and no persistent service/web/TUI process were observed.
- A placeholder scan found only summary prose stating that no placeholders were introduced; no Phase 127 summary contains an actual “To be filled” or `TBD` completion hole.

The current source contains three post-freeze repairs:

1. `42d92518` — authoritative catalogs and full-set projections can contain more than 16 workspaces; creation admission remains limited to 16.
2. `e5a59502` — correct OpenTUI Solid preload, explicit renderer ownership, fatal-mount cleanup, and prompt `q`/Ctrl+C/SIGINT/SIGTERM shutdown.
3. `9d319c32` — an in-flight authentication superseded by `closeServiceClient()` cannot repopulate cached access.

Focused validation for these repairs is green, including the larger-catalog suites, secure frame bounds, real PTY launcher tests, service-auth shutdown test, `npm run test:tui`, the full Vitest suite, package builds/typechecks, and architecture checks. A final all-up regression after all three commits was interrupted before completion.

</current_state>

<completed_work>

Completed work:

- Plans `127-01` through `127-12`: stale-workspace protocol, acquisition, policy, evaluator/cache, service route, shared client semantics, web UI, TUI UI, conformance/security coverage, RC metadata, documentation, and the original candidate ledger.
- Catalog scalability repair: active, archived, stale, pin, and priority read/projection sets no longer fail or truncate at row 17; the service still rejects workspace creation 17 before mutation.
- TUI runtime repair: `@opentui/solid/preload` is guaranteed before dashboard import/render; direct un-preloaded `dist/run.js` fails clearly before renderer creation.
- TUI lifecycle repair: the application owns `CliRenderer` and destroys it in `finally`, restoring TTY/raw/alternate-screen state after fatal mount failures and normal signal/key exits.
- Authentication repair: generation-safe shutdown closes superseded late authentication results.
- Focused regression coverage for every repair is committed and green.
- Stale recursive review tasks from the stopped agent fan-out were removed from the session task tracker. Only Phase 127 execution/regression and milestone lifecycle tasks remain active/pending.

</completed_work>

<remaining_work>

1. On resume, explicitly acknowledge the three blocking constraints above and proceed solo.
2. Consume/remove `.planning/HANDOFF.json` according to the resume workflow; preserve this checkpoint until its context is restored. Ensure the tree is clean before freezing a candidate.
3. Rebuild packages and reproduce the normal one-command web launch with no managed service already running:
   - `npm run build:packages`
   - `node packages/cli/dist/index.js web`
   - If `LocalEnvironmentPreparationError` still occurs, diagnose the managed-service/environment-refresh sequence from evidence. Do not attribute it to Node 26 by assumption.
4. Run the complete post-fix regression on the final clean source:
   - `npm test`
   - `npm run typecheck`
   - `npm run coverage`
   - `npm run release:check` **without** `--tag`
   - Preserve tag refs and planning directories and confirm the tree remains clean.
5. If any command fails, fix and rerun the complete affected gate. Do not freeze until all local deterministic gates are green.
6. Freeze a replacement clean candidate SHA. Reconcile every old-SHA reference, at minimum:
   - `.planning/STATE.md`
   - `.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md`
   - `.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md`
   - `.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-12-SUMMARY.md`
   Mark `f7bdca75f2545664251e88b233693b67fd37ee5c` superseded and keep all HST/AUTH/FORGE/LIVE/PHYS/VIS/INT/HUMAN rows PENDING for the replacement SHA.
7. Do not run Plan 127-13 until exact-replacement-SHA evidence is actually available. The user previously chose to keep both checkpoint classes pending.
8. Run Plan 127-14 only after both Plan 127-13 checkpoints are complete and safe. Then perform milestone audit/completion/cleanup.

</remaining_work>

<decisions_made>

- The 16-workspace value is a **creation/admission policy**, not an authoritative catalog read limit. Existing larger catalogs must remain fully readable and projectable without silent truncation.
- Correct the TUI at the runtime/preload boundary rather than adding `fallback={null}` to every false `<Show>`; under the wrong Solid server runtime a false `<Show>` yields raw `""`, which OpenTUI rejects under `<box>`.
- Create and own the OpenTUI renderer explicitly and always destroy it in `finally`; internal renderer ownership cannot guarantee cleanup when mount rejects.
- `closeServiceClient()` increments an access generation; a late authentication from an older generation closes itself and cannot restore cache state.
- Node 24 is the minimum/default validation target, not an exclusive maximum. Node 26 is valid and must not be used as an unsupported-version explanation without proof.
- Hosted/authenticated/manual evidence remains pending by user choice. Local deterministic tests cannot substitute for it.
- Release actions remain separately prohibited without explicit authorization.

</decisions_made>

<blockers>

- **Superseded candidate:** the old receipt and validation SHA predates runtime repairs. Replacement regression and freeze are mandatory before Plan 127-13.
- **Hosted/authenticated evidence:** HST-RUNTIME, HST-SHELL, HST-SSH, AUTH-GH-STATUS, AUTH-GL-STATUS, all four same/fork forge rows, and FORGE-RECOVERY remain PENDING.
- **Physical/manual evidence:** archived/removal/stale/attention/Phase126 lifecycle rows, physical browser/xterm, responsive screenshots, interactive TUI, and human parity remain PENDING.
- **Web auto-launch observation:** one initial `LocalEnvironmentPreparationError` remains unreproduced after the repairs; manually starting the service was the workaround.
- **Release authority:** `NOT_AUTHORIZED`; no outward release command may run.

</blockers>

## Human Actions Pending

1. Supply safe exact-SHA hosted runtime, configured-shell/SSH-agent, authenticated GitHub/GitLab status, and complete GitHub/GitLab same/fork forge create/recovery receipts for the replacement candidate.
2. Perform exact-SHA live lifecycle, physical browser/xterm, responsive screenshot, interactive OpenTUI, and side-by-side web/TUI parity verification.
3. If an outward release is desired later, provide separate explicit authorization. Phase validation alone is not authorization.

## Required Reading (in order)

1. `CLAUDE.md` — architecture, runtime, test, and release constraints.
2. `.planning/HANDOFF.json` — machine-readable task, blocker, decision, and next-action state.
3. This file — mandatory agent-count, exact-SHA, and release constraints.
4. `.planning/STATE.md` — roadmap position; note that its candidate fields are stale.
5. `127-12-SUMMARY.md`, `127-RECEIPTS.md`, and `127-VALIDATION.md` — old candidate evidence that must be superseded, not reused.
6. `127-13-PLAN.md` — two blocking human checkpoints; no generic or wrong-SHA approval is accepted.
7. `127-14-PLAN.md` — reconciliation is forbidden before complete Plan 127-13 evidence.
8. `git show 42d92518`, `git show e5a59502`, and `git show 9d319c32` — post-freeze runtime repairs and their tests.
9. `packages/protocol/src/service.ts`, `packages/protocol/src/web.ts`, `packages/tui/src/index.ts`, `packages/tui/src/run.tsx`, and `packages/service/src/policy/client.ts` — primary repaired runtime surfaces.

## Critical Anti-Patterns (do NOT repeat these)

- **Recursive orchestration after a flat cap:** never count only top-level agents; resume with zero agents unless explicitly reauthorized.
- **Freezing before final runtime smoke/regression:** do not create another exact candidate until the web/TUI repaired source passes the complete all-up gate.
- **Promoting old or local-only evidence:** every human/external row is bound to the replacement exact SHA and remains PENDING until actually supplied.
- **Inferring unsupported Node versions:** Node 24 is the floor; inspect the real failure.
- **Papering over OpenTUI runtime mismatch in JSX:** preserve the preload/runtime guard and explicit renderer cleanup.
- **Crossing release authority:** never infer permission from RC readiness or human feature approval.

## Infrastructure State

- Branch before handoff commit: `planning/phase-127-revision-1`
- HEAD before handoff commit: `9d319c3243b594d3a3d71c15a51234099419a8e7`
- Working tree before handoff creation: clean
- Managed service/web/TUI processes: none observed
- `.planning/async-jobs/*.json`: none
- User-observed runtime: Node `v26.5.0`; this is not inherently unsupported
- Project validation floor/default: Node 24 and npm; Bun only for optional OpenTUI build/tests
- Existing TUI test warnings: `TerminalConsoleCache` listener warnings may appear but did not fail the focused/full TUI suites

<context>

The implementation is largely complete. The immediate job is evidence hygiene and final verification, not new feature design. Treat the three post-freeze fixes as invalidating the old candidate identity. Use the existing focused green results as confidence, but do not claim completion until the complete regression has run on a clean final tree and a replacement exact SHA is recorded. Keep external/manual rows honest and pending. Do not use agents during the resume/refreeze work.

</context>

<next_action>

Start with: acknowledge the three blocking constraints, consume the one-shot handoff, proceed solo, reproduce the web auto-launch path from a fresh build, then run the complete post-fix regression before freezing any replacement candidate SHA.

</next_action>
