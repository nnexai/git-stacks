---
phase: 127-stale-workspace-intelligence-and-rc-closure
plan: "13"
type: execute
wave: 10
depends_on:
  - 127-12
files_modified: []
autonomous: false
requirements:
  - STALE-01
  - STALE-02
  - STALE-03
  - STALE-04
  - STALE-05
  - REL-01
  - REL-02
must_haves:
  truths:
    - "Every reviewed receipt is bound to the immutable candidate SHA from `127-RECEIPTS.md`; evidence for another SHA cannot be substituted."
    - "Hosted runtime, configured-shell/SSH-agent, authenticated GitHub/GitLab status, and complete same-repository/fork Resolve → Review → Create evidence is accepted only from separately authorized, already-performed exact-SHA checks; this plan does not push or dispatch anything."
    - "Archived-workspace flows, stale flows, attention/fuzzy navigation, broader Phase 126 actions/notes/file-status, full lifecycle/reconnect, physical browser/xterm, responsive screenshots, interactive OpenTUI, and human cross-client parity are explicit human verification rather than deterministic-test claims."
    - "Authenticated receipts contain provider/host/state/result/timestamp and safe reference metadata without credentials, tokens, raw output, machine paths, or private change content; self-hosted hosts remain NOT_CLAIMED without an explicit list."
    - "Browser checks cover physical shortcuts/focus/input isolation and desktop/375px/320px light/dark stale plus Phase 126 states; TUI checks cover every width tier and the archived/action/operation/notes/file/forge/stale interaction matrix."
    - "Human parity compares the same authoritative fixtures across web and TUI for action labels, disabled reasons, confirmations, operation stages/cancellation, notes, file status, forge transitions, stale order/reasons/timestamps/unknowns/cautions, counts, terminal state, selection, signals, failures, and reconciliation."
    - "Missing or mismatched evidence remains PENDING and blocks approval; it is never waived, inferred, or converted from a local pass."
    - "Release authorization remains absent: no tag, push, publication, GitHub Release, or release-only workflow dispatch is performed or approved by these checkpoints."
  artifacts:
    - path: .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md
      provides: "Pending exact-SHA rows that the human checks validate or leave pending for Plan 127-14 reconciliation."
    - path: .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-13-SUMMARY.md
      provides: "Structured human checkpoint responses and safe receipt metadata for final automated reconciliation."
  key_links:
    - from: .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-13-SUMMARY.md
      to: .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md
      via: "Exact candidate SHA and evidence-row identifiers"
      pattern: "candidate_sha|hosted|authenticated|physical|screenshot|interactive|human"
    - from: human verification
      to: packages/web/src/stale-workspaces.ts
      via: "Physical focus, xterm/input, pointer, responsive, and retained-state behavior"
      pattern: "Refresh|Open|Archive|Remove|Force"
    - from: human verification
      to: packages/tui/src/StaleWorkspacesView.tsx
      via: "Real terminal dimensions, key ownership, detail scroll, actions, and origin restore"
      pattern: "80|56|40|Escape"
  assumptions:
    - statement: "A-EDGE-REL-01 remains unresolved and flagged until these checkpoints return exact-SHA evidence and Plan 127-14 reconciles the ledger without pending required rows."
      status: unresolved
      flagged: true
  prohibitions:
    - statement: "Deterministic or local evidence must not be represented as hosted, authenticated, physical, screenshot, interactive, human approval, or release authorization."
      status: unverified
      flagged: true
    - statement: "Release-candidate preparation must not imply authorization to tag, push, publish, create a GitHub Release, or dispatch release-only workflows."
      status: unverified
      flagged: true
---

<objective>
Obtain explicit human review of every exact-candidate-SHA hosted/authenticated, configured-shell/SSH-agent, archived/stale/navigation/Phase126 lifecycle, physical/visual/interactive, and cross-client evidence class while preserving the no-release-action boundary.

Purpose: Close evidence that deterministic agents cannot honestly produce and block rather than fabricate any unavailable class.
Output: Structured checkpoint responses in `127-13-SUMMARY.md` for Plan 127-14 to sanitize and reconcile into the ledger.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.claude/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-CONTEXT.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-COVERAGE.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-12-SUMMARY.md
@.planning/phases/126-web-workflow-and-forge-source-parity/126-PHASE127-HANDOFF.md
@docs/stale-workspaces.md
@docs/releasing.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify exact-SHA hosted runtimes, configured shells/SSH agent, and authenticated provider/forge evidence</name>
  <files>None — human checkpoint only</files>
  <read_first>
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-COVERAGE.md
    - .planning/phases/126-web-workflow-and-forge-source-parity/126-PHASE127-HANDOFF.md
    - docs/releasing.md
  </read_first>
  <what-built>The immutable candidate, deterministic local release checks, configured-shell/environment authority, GitHub/GitLab stale-status integration, the complete Phase 126 Resolve → Review → Create implementation, and a ledger whose hosted/authenticated rows remain PENDING.</what-built>
  <action>Review only evidence already produced through separately authorized channels for the exact candidate SHA. Do not push the branch, dispatch a workflow, install or authenticate a provider CLI, disclose credentials, or perform any release action as part of this checkpoint. Supply safe metadata for HST-RUNTIME; HST-SHELL; HST-SSH; AUTH-GH-STATUS; AUTH-GL-STATUS; FORGE-GH-SAME; FORGE-GH-FORK; FORGE-GL-SAME; FORGE-GL-FORK; and FORGE-RECOVERY. Runtime receipts must cover the required Node 24 Linux/macOS x64/ARM, package/policy, optional Bun TUI, and supported shell jobs. Shell receipts must cover Bash/zsh/fish configured commands, hooks, and PTYs; profile-only runtime/function/alias behavior; overlay precedence; broken startup/missing executable/unsupported shell/timeout diagnostics; and dynamic PATH plus SSH_AUTH_SOCK refresh with `ssh-add`/socket rotation. Provider status receipts must cover authenticated GitHub.com and GitLab.com merged, closed, open, auth, rate-limit, unavailable, malformed, and recovery behavior. Forge creation receipts must exercise full URL Resolve → editable Review → explicit one-shot Create for GitHub same-repository and fork branches and GitLab same-project and fork branches, preserving immutable host/repository/change/head/base/source anchors, using the reviewed real head SHA and ordinary Git/worktree machinery, and never issuing provider checkout. FORGE-RECOVERY must cover every Phase 126 handoff failure/recovery class: missing CLI/auth, unsupported/unconfigured host, malformed/inaccessible/closed/missing change, rate limit/provider outage, no/ambiguous repository match, template mismatch/non-worktree mode, unreachable fork/source, source movement, stale revision, expired token, branch conflict, cancellation, timeout, creation failure, and rollback with no unintended YAML/worktree/private ref/browser draft/replayable intent. Each receipt requires exact SHA, row ID, provider/host or workflow/job, conclusion, UTC time, and safe URL/reference/checksum. Structure the checkpoint response row-by-row so each approved row echoes `candidate_sha`, `approved: true`, and an `approved_subcases` array exactly equal to that row's immutable `required_subcases` from the canonical JSON ledger; do not rewrite, collapse, or omit required subcases. Self-hosted hosts remain NOT_CLAIMED unless an explicit list is supplied. If any required row is unavailable, mismatched, or belongs to another SHA, respond pending for that row and do not approve the checkpoint.</action>
  <how-to-verify>
    1. Copy `candidate_sha` from `127-RECEIPTS.md`; compare every HST/AUTH/FORGE receipt to that exact full SHA.
    2. Confirm HST-RUNTIME covers every required Node 24 OS/architecture and package/policy/TUI job with safe artifact references/checksums; unavailable cells remain PENDING.
    3. Confirm HST-SHELL and HST-SSH cover configured Bash/zsh/fish commands/hooks/PTYs, profile-only runtime/function/alias behavior, overlay precedence, safe diagnostics, PATH refresh, SSH agent discovery, `ssh-add`, and socket rotation without raw environment disclosure.
    4. Confirm AUTH-GH-STATUS and AUTH-GL-STATUS cover merged/closed/open plus sanitized operational failures and recovery in an authorized environment.
    5. Confirm all four FORGE provider/topology rows complete Resolve → Review → explicit Create with editable fields, immutable source anchors, reviewed real head SHA, same-repository/project and fork behavior, and no provider checkout.
    6. Confirm FORGE-RECOVERY covers every listed rejection/recovery and leaves no unintended workspace YAML, worktree, private ref, browser-local draft, or replayable create intent.
    7. Confirm no Gitea status claim, provider mutation/search/inference, tag, push, publication, GitHub Release, or release-only workflow action was performed.
  </how-to-verify>
  <acceptance_criteria>
    - Every HST/AUTH/FORGE row is accepted only with the exact candidate SHA and all required safe fields.
    - The complete configured-shell and SSH-agent matrix and complete GitHub/GitLab same/fork Resolve → Review → Create/recovery matrix are represented; no class is collapsed into a generic provider pass.
    - Missing GitLab tooling, unpushed commits, unavailable runners, absent host lists, provider accounts, shell/SSH rows, or forge cases remain explicit PENDING/NOT_CLAIMED.
    - No credential, raw output/environment, path, or private-data disclosure is supplied.
    - The checkpoint is not approved while any required HST/AUTH/FORGE row remains PENDING.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('node:fs'); const s=fs.readFileSync('.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md','utf8'); for (const x of ['candidate_sha','HST-RUNTIME','HST-SHELL','HST-SSH','AUTH-GH-STATUS','AUTH-GL-STATUS','FORGE-GH-SAME','FORGE-GH-FORK','FORGE-GL-SAME','FORGE-GL-FORK','FORGE-RECOVERY','PENDING']) if (!s.includes(x)) throw new Error(x);"</automated>
    <human-check>Verify all seven steps above against separately authorized evidence for the exact candidate SHA.</human-check>
  </verify>
  <done>The checkpoint response contains safe exact-SHA hosted runtime, shell/SSH-agent, authenticated status, and complete forge same/fork/recovery metadata for every required row, or explicitly remains blocked/PENDING without fabrication or outward action.</done>
  <resume-signal>Reply `approved-hosted` followed by row-keyed safe metadata containing candidate_sha, approved=true, and exact approved_subcases for every HST/AUTH/FORGE row, or reply `pending-hosted` with the missing row/subcase IDs and no approval.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete live lifecycle, archived/stale/navigation/parity, responsive browser, and interactive TUI evidence</name>
  <files>None — human checkpoint only</files>
  <read_first>
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-UI-SPEC.md
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md
    - .planning/phases/126-web-workflow-and-forge-source-parity/126-PHASE127-HANDOFF.md
    - docs/stale-workspaces.md
  </read_first>
  <what-built>Complete Phase 123 archived/removal authority, Phase 125 attention/fuzzy navigation, Phase 126 actions/notes/file-status/forge/lifecycle parity, and Phase 127 stale surfaces with deterministic coverage; all live, physical, visual, interactive, and human rows remain PENDING.</what-built>
  <action>Exercise the exact candidate build on live web, physical browser/xterm, and interactive terminal surfaces, and review responsive/parity evidence for every ledger row: LIVE-ARCHIVE; LIVE-REMOVE-FORCE; LIVE-STALE; LIVE-ATTENTION-FUZZY; LIVE-P126-ACTIONS; LIVE-P126-NOTES; LIVE-P126-FILE-STATUS; LIVE-LIFECYCLE; PHYS-BROWSER-XTERM; VIS-RESPONSIVE; INT-TUI; and HUMAN-PARITY. This checkpoint may run local applications and interact with disposable fixtures but must not tag, push, publish, create a release, or dispatch a release-only workflow. Archive evidence must cover terminal stop, ordering, Undo, unarchive without terminal recreation, archived-list semantics, all-active-empty and archive-empty transitions. Remove evidence must cover clean removal, dirty blockers, exact-name Force, stale confirmation, terminal failure, inventory/progress, and unrelated-state preservation. Stale evidence must cover all approved states, every reason/timestamp/unknown/caution, Refresh/Open, canonical Archive/Remove/Force, revision recovery, no automatic mutation, and reconciliation. Attention/fuzzy evidence must cover top partial-match Enter, archived exclusion, singleton/focus restoration, xterm-safe keys, deterministic next-attention wrapping/skips, and no-attention result. Broader Phase 126 evidence must cover archive/unarchive/remove/rename/open/close/pin/unpin/sync/pull/push/merge, context/visible/shortcut entry points, notes zero/one/many/add/clear/failure retention, file-status groups/counts/severity/reasons/loading/error/retry, and operation progress/cancellation/failure/result parity. Full lifecycle evidence must cover disconnect/reconnect by durable operation ID, cancel once and too late, refresh-failed lock/retry, simultaneous clients, stale-confirmation replay prevention, terminal state, authoritative selection/count/signal convergence, and no mutation replay. Physical browser evidence must cover pointer, focus, xterm, held/repeated keys, AltGraph/IME/dead/non-US input, singleton replacement, safe default focus, and exact nested focus return. Responsive evidence must include light/dark desktop, 375px, and 320px screenshots for stale states plus Phase 126 action/confirmation/operation/notes/file/forge states from the handoff. Interactive TUI evidence must cover wide, below-80, below-56, short-height, and too-small layouts plus archived, actions, operations, notes, file status, forge review/create, stale view, owned keys, no leakage, and origin restoration. HUMAN-PARITY must compare the same authoritative fixtures across web/TUI for all these labels, disabled reasons, confirmations, stages, cancellations, notes, file status, forge transitions, stale evidence, terminal results, selection, counts, signals, and reconciliation. Record only safe exact-SHA metadata/artifact references. Structure the checkpoint response row-by-row so each approved row echoes `candidate_sha`, `approved: true`, and an `approved_subcases` array exactly equal to the row's immutable `required_subcases` in the canonical JSON ledger; do not rewrite or narrow the required list. If any row or subcase is missing, respond pending and do not approve.</action>
  <how-to-verify>
    1. LIVE-ARCHIVE and LIVE-REMOVE-FORCE: execute the complete archived-workspace, active/archive-empty, clean remove, dirty blocker, exact-name Force, stale-confirmation, terminal-stop/failure, progress, and unrelated-state flows in both clients.
    2. LIVE-STALE and LIVE-ATTENTION-FUZZY: exercise every stale state/action/revision path plus fuzzy top-partial Enter, archived exclusion, singleton/focus behavior, next-attention skip/wrap, and no-attention results.
    3. LIVE-P126-ACTIONS, LIVE-P126-NOTES, and LIVE-P126-FILE-STATUS: exercise all supported actions and entry surfaces, notes states/mutations, file groups/states/counts/reasons/retry, and authoritative results in web/TUI.
    4. LIVE-LIFECYCLE: disconnect/reconnect by the same operation ID, cancel once and too late, interrupt terminal refresh, retry, and run simultaneous web/TUI actions; verify no replay and authoritative convergence.
    5. PHYS-BROWSER-XTERM: test pointer, physical shortcuts with terminal and normal focus, Tab/Shift+Tab, Escape, repeats/holds, AltGraph/IME/dead/non-US input, menu/form focus defaults, and exact focus restoration.
    6. VIS-RESPONSIVE: review light/dark desktop, 375px, and 320px screenshots for all stale states and the handoff's action, confirmation, operation, notes, file-status, and forge Resolve/Review/recovery states; verify no overflow, clipping, hover-only action, contrast, or disclosure failure.
    7. INT-TUI: review all width/height tiers and the complete archived/action/operation/notes/file/forge/stale interaction matrix, including too-small safety and no key leakage.
    8. HUMAN-PARITY: compare the same fixtures side by side for every action label/disabled reason/confirmation, operation stage/cancel/failure/result, notes/file status, GitHub/GitLab Resolve → Review → Create terminology, stale evidence/order, selection/count/signal/terminal reconciliation, and safe defaults.
    9. Confirm no evidence or UI claims deletion safety, hides unknowns, leaks machine/provider secrets, or authorizes a release.
  </how-to-verify>
  <acceptance_criteria>
    - Every LIVE/PHYS/VIS/INT/HUMAN row and every enumerated subcase has an exact-candidate safe receipt; no Phase 123/125/126 handoff class is replaced by stale-only evidence.
    - Archived flows, attention/fuzzy navigation, broader actions/notes/file-status, complete lifecycle, physical input/focus, all responsive screenshots, interactive TUI, and cross-client parity are all explicit.
    - Reconnect/concurrency produces no mutation replay and both clients converge on authoritative revisions, selections, counts, signals, and terminal state.
    - Human parity confirms identical policy/presentation/action authority without approving release actions.
    - The checkpoint is not approved while any required LIVE/PHYS/VIS/INT/HUMAN row remains PENDING.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('node:fs'); const s=fs.readFileSync('.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md','utf8'); for (const x of ['LIVE-ARCHIVE','LIVE-REMOVE-FORCE','LIVE-STALE','LIVE-ATTENTION-FUZZY','LIVE-P126-ACTIONS','LIVE-P126-NOTES','LIVE-P126-FILE-STATUS','LIVE-LIFECYCLE','PHYS-BROWSER-XTERM','VIS-RESPONSIVE','INT-TUI','HUMAN-PARITY','PENDING']) if (!s.includes(x)) throw new Error(x);"</automated>
    <human-check>Perform all nine steps on the exact candidate and compare safe receipts to every pending ledger row.</human-check>
  </verify>
  <done>The checkpoint response contains complete safe exact-SHA archived/stale/navigation/Phase126/lifecycle/physical/visual/interactive/parity evidence, or remains explicitly blocked/PENDING without a false approval.</done>
  <resume-signal>Reply `approved-manual` followed by row-keyed safe metadata containing candidate_sha, approved=true, and exact approved_subcases for every LIVE/PHYS/VIS/INT/HUMAN row, or reply `pending-manual` with the missing row/subcase IDs and no approval. This signal does not authorize any release action.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| External workflow/provider receipt → human approval | Evidence may be stale, wrong-SHA, incomplete, forged, or credential-bearing. |
| Physical UI observation → phase claim | Manual checks must cover the exact candidate and prescribed states, not memory or another build. |
| Human checkpoint → release authority | Feature/evidence approval must not be interpreted as tag/push/publish/release authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-127-13-01 | Spoofing | Hosted/authenticated receipts | high | mitigate | Exact full-SHA comparison, required provider/job fields, safe URLs, and reject mismatched/missing evidence. |
| T-127-13-02 | Information Disclosure | Human-supplied receipt data | high | mitigate | Request sanitized metadata only; reject tokens, credentials, raw output/environment, paths, and private payloads. |
| T-127-13-03 | Repudiation | Physical/manual evidence | high | mitigate | Enumerated state/device/size/action steps and structured receipt IDs tied to candidate SHA. |
| T-127-13-04 | Tampering | Cross-client parity approval | medium | mitigate | Same fixture/state, explicit field/action comparison, and no approval with missing rows. |
| T-127-13-05 | Elevation of Privilege | Release authorization confusion | high | mitigate | Both checkpoint actions and resume signals explicitly exclude release authorization/outward actions. |
| T-127-13-06 | Denial of Service | Unavailable external environment | low | accept | Honest pending status blocks completion rather than forcing unsafe setup or fabricated proof. |

**ASVS L1 gate:** wrong-SHA evidence, disclosure, false manual claims, and release-authorization confusion are high severity; missing evidence blocks rather than being accepted.
</threat_model>

<verification>
1. Automated prechecks confirm the exact-SHA ledger contains separate pending hosted/authenticated and physical/visual/interactive/human rows.
2. Human checkpoint 1 approves only complete safe exact-SHA runtime, configured-shell/SSH-agent, authenticated status, and GitHub/GitLab same/fork forge-creation/recovery receipts.
3. Human checkpoint 2 approves only complete exact-SHA archived/stale/attention/fuzzy/Phase126 action-notes-file/full-lifecycle, physical, responsive, interactive TUI, and parity receipts.
4. Neither checkpoint authorizes or performs tag, push, publication, GitHub Release, or release-only workflow dispatch.
</verification>

<success_criteria>
- Every non-deterministic ROADMAP success-criterion-4 and Phase 126 handoff evidence class is explicitly reviewed against the exact candidate SHA.
- Missing evidence remains pending and blocks approval without scope reduction or fabrication.
- Supplied evidence is safe to record and contains no secret/raw/private data.
- The checkpoint outcome is structured for Plan 127-14 reconciliation and carries no release authorization.
</success_criteria>

## Artifacts this phase produces

- Structured exact-SHA human checkpoint responses in `127-13-SUMMARY.md`.
- No product, release metadata, or receipt-ledger edits; reconciliation is isolated to Plan 127-14.

<output>
Create `.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-13-SUMMARY.md` when both checkpoints are approved; otherwise preserve the blocking pending state without claiming completion.
</output>
