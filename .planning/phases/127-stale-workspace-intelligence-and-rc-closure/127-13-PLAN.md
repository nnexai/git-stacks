---
phase: 127-stale-workspace-intelligence-and-rc-closure
plan: "13"
type: execute
wave: 10
depends_on:
  - 127-12
files_modified: []
autonomous: false
requirements:
  - STALE-01
  - STALE-02
  - STALE-03
  - STALE-04
  - STALE-05
  - REL-01
  - REL-02
must_haves:
  truths:
    - "Every reviewed receipt is bound to the immutable candidate SHA from `127-RECEIPTS.md`; evidence for another SHA cannot be substituted."
    - "Hosted runtime and authenticated GitHub/GitLab evidence is accepted only from separately authorized, already-performed exact-SHA checks; this plan does not push or dispatch anything."
    - "Physical browser/xterm, responsive screenshot, interactive OpenTUI, reconnect/two-client, and human cross-client parity are explicit human verification rather than deterministic-test claims."
    - "Authenticated receipts contain provider/host/state/result/timestamp and safe reference metadata without credentials, tokens, raw output, machine paths, or private change content."
    - "Browser checks cover physical shortcuts/focus/input isolation and desktop/375px/320px light/dark states; TUI checks cover every width tier, owned key, refresh, Open, actions, and Escape restoration."
    - "Human parity compares the same fixture/service response across web and TUI for order, reasons, timestamps, unknowns, cautions, counts, actions, failures, and reconciliation."
    - "Missing or mismatched evidence remains PENDING and blocks approval; it is never waived, inferred, or converted from a local pass."
    - "Release authorization remains absent: no tag, push, publication, GitHub Release, or release-only workflow dispatch is performed or approved by these checkpoints."
  artifacts:
    - path: .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md
      provides: "Pending exact-SHA rows that the human checks validate or leave pending for Plan 127-14 reconciliation."
    - path: .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-13-SUMMARY.md
      provides: "Structured human checkpoint responses and safe receipt metadata for final automated reconciliation."
  key_links:
    - from: .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-13-SUMMARY.md
      to: .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md
      via: "Exact candidate SHA and evidence-row identifiers"
      pattern: "candidate_sha|hosted|authenticated|physical|screenshot|interactive|human"
    - from: human verification
      to: packages/web/src/stale-workspaces.ts
      via: "Physical focus, xterm/input, pointer, responsive, and retained-state behavior"
      pattern: "Refresh|Open|Archive|Remove|Force"
    - from: human verification
      to: packages/tui/src/StaleWorkspacesView.tsx
      via: "Real terminal dimensions, key ownership, detail scroll, actions, and origin restore"
      pattern: "80|56|40|Escape"
  assumptions:
    - statement: "A-EDGE-REL-01 remains unresolved and flagged until these checkpoints return exact-SHA evidence and Plan 127-14 reconciles the ledger without pending required rows."
      status: unresolved
      flagged: true
  prohibitions:
    - statement: "Deterministic or local evidence must not be represented as hosted, authenticated, physical, screenshot, interactive, human approval, or release authorization."
      status: unverified
      flagged: true
    - statement: "Release-candidate preparation must not imply authorization to tag, push, publish, create a GitHub Release, or dispatch release-only workflows."
      status: unverified
      flagged: true
---

<objective>
Obtain explicit human review of the exact-candidate-SHA hosted/authenticated and physical/visual/interactive/cross-client evidence classes while preserving the no-release-action boundary.

Purpose: Close evidence that deterministic agents cannot honestly produce and block rather than fabricate any unavailable class.
Output: Structured checkpoint responses in `127-13-SUMMARY.md` for Plan 127-14 to sanitize and reconcile into the ledger.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.claude/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-CONTEXT.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-COVERAGE.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-12-SUMMARY.md
@.planning/phases/126-web-workflow-and-forge-source-parity/126-PHASE127-HANDOFF.md
@docs/stale-workspaces.md
@docs/releasing.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify exact-SHA hosted runtime and authenticated provider evidence</name>
  <files>None — human checkpoint only</files>
  <read_first>
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-COVERAGE.md
    - docs/releasing.md
  </read_first>
  <what-built>The immutable candidate, deterministic local release checks, GitHub/GitLab read-only status integration, and an evidence ledger whose hosted/authenticated rows are still pending.</what-built>
  <action>Review only evidence already produced through separately authorized channels for the exact candidate SHA. Do not push the branch, dispatch a workflow, install/authenticate a provider CLI, disclose credentials, or perform any release action as part of this checkpoint. Supply safe metadata for: Node 24 Linux/macOS x64/ARM runtime-matrix jobs and artifacts/checksums; GitHub.com PR merged/closed/open plus sanitized auth/rate-limit/recovery behavior; GitLab.com MR merged/closed/open plus sanitized auth/rate-limit/recovery behavior; and any claimed self-hosted host list. Each receipt must include exact SHA, provider/host or workflow/job, conclusion, UTC time, and safe URL/reference. If any required evidence is unavailable, mismatched, or belongs to another SHA, respond `pending` for that row and do not approve the checkpoint.</action>
  <how-to-verify>
    1. Copy the `candidate_sha` from `127-RECEIPTS.md`; compare every supplied workflow/job/provider receipt to that exact full SHA.
    2. Confirm required runtime matrix conclusions are successful for the declared Node 24 OS/architecture set and artifact references/checksums are safe to record.
    3. Confirm authenticated GitHub.com and GitLab.com receipts cover merged, closed, open, and sanitized operational failure/recovery without exposing token, credential, raw environment, private payload, path, stdout, or stderr.
    4. Confirm no Gitea status claim exists and no provider mutation/search/inference action was performed.
    5. Confirm no tag, push, publication, GitHub Release, or release-only workflow action was performed by this review.
  </how-to-verify>
  <acceptance_criteria>
    - Every accepted hosted/authenticated receipt matches the exact candidate SHA and required safe fields.
    - Missing GitLab tooling, unpushed commits, unavailable runners, absent host lists, or missing provider accounts remain explicit PENDING/NOT_CLAIMED.
    - No credential/raw-output/path/private-data disclosure is supplied.
    - The checkpoint is not approved while a required hosted/authenticated row remains pending.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('node:fs'); const s=fs.readFileSync('.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md','utf8'); if (!/candidate[_ ]sha/i.test(s) || !/hosted/i.test(s) || !/authenticated/i.test(s) || !/PENDING/i.test(s)) process.exit(1);"</automated>
    <human-check>Verify the five steps above against evidence already available for the exact candidate SHA.</human-check>
  </verify>
  <done>The checkpoint response contains safe exact-SHA hosted/authenticated receipt metadata for every required row, or explicitly remains blocked/pending without fabrication or outward action.</done>
  <resume-signal>Reply `approved-hosted` followed by the safe exact-SHA receipt metadata for every required row, or reply `pending-hosted` with the missing row IDs and no approval.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify physical browser, responsive, interactive TUI, reconnect, and human parity evidence</name>
  <files>None — human checkpoint only</files>
  <read_first>
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-UI-SPEC.md
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md
    - docs/stale-workspaces.md
  </read_first>
  <what-built>Complete web and TUI stale-workspace surfaces with deterministic state, responsive, key, lifecycle, generation, and conformance coverage; physical/visual/interactive/human rows remain pending.</what-built>
  <action>Exercise the exact candidate build on physical browser/xterm and interactive terminal surfaces, and review screenshots/parity. This checkpoint may run local applications and interact with them but must not tag, push, publish, create a release, or dispatch a release-only workflow. Record only safe metadata and artifact references. Use the same sanitized candidate/incomplete fixture or authoritative workspace set in both clients so parity is meaningful. If the exact candidate cannot be exercised or any required state/device/size is missing, respond `pending` and do not approve.</action>
  <how-to-verify>
    1. Browser/xterm: with terminal focus and normal focus, test the platform stale-entry shortcut, active-overlay `R`, Tab/Shift+Tab containment, Escape/backdrop, pointer actions, held/repeated keys, AltGraph/IME/dead-key/non-US input where available, and exact focus restoration.
    2. Web states: capture light/dark desktop, 375px, and 320px loading, populated, confirmed-plus-unknown/caution, incomplete-only, all-clear, retained-data failure, first-load failure, Open failure, and Remove-return states; verify no horizontal overflow and full accessible long text.
    3. Interactive TUI: exercise 80+ two-pane, 56-79 stacked, under-56 single-column, short-height scrolling, under-40x12 Escape-only fallback, all navigation keys, repeated refresh suppression, Open success/failure, candidate-only actions, dirty Force sequence, and exact origin restoration.
    4. Live operation behavior: disconnect/reconnect by durable operation ID, cancel once and too late, force-refresh failure/recovery, and simultaneous web/TUI action; verify no replay and authoritative convergence.
    5. Side-by-side parity: compare service order, every reason/timestamp, unknown/caution split, counts, disabled reasons, Open, confirmations, terminal result, and reconciliation for the same data.
    6. Confirm no evidence or UI claims deletion safety, hides unknowns, leaks machine/provider secrets, or authorizes a release.
  </how-to-verify>
  <acceptance_criteria>
    - Every required browser width/theme/state and every TUI size/interaction has an exact-candidate safe receipt.
    - Physical input and focus behavior matches canonical shortcut ownership.
    - Reconnect/concurrency produces no mutation replay and both clients converge.
    - Human parity confirms identical policy/presentation/action authority without approving release actions.
    - The checkpoint is not approved while any required physical/visual/interactive/human row remains pending.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('node:fs'); const s=fs.readFileSync('.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md','utf8'); for (const x of ['physical','screenshot','interactive','human','candidate']) if (!s.toLowerCase().includes(x)) throw new Error(x);"</automated>
    <human-check>Perform all six steps on the exact candidate and compare safe receipts to the pending ledger rows.</human-check>
  </verify>
  <done>The checkpoint response contains complete safe exact-SHA physical/visual/interactive/reconnect/parity evidence, or remains explicitly blocked/pending without a false approval.</done>
  <resume-signal>Reply `approved-manual` followed by safe receipt metadata for every required row, or reply `pending-manual` with the missing row IDs and no approval. This signal does not authorize any release action.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| External workflow/provider receipt → human approval | Evidence may be stale, wrong-SHA, incomplete, forged, or credential-bearing. |
| Physical UI observation → phase claim | Manual checks must cover the exact candidate and prescribed states, not memory or another build. |
| Human checkpoint → release authority | Feature/evidence approval must not be interpreted as tag/push/publish/release authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-127-13-01 | Spoofing | Hosted/authenticated receipts | high | mitigate | Exact full-SHA comparison, required provider/job fields, safe URLs, and reject mismatched/missing evidence. |
| T-127-13-02 | Information Disclosure | Human-supplied receipt data | high | mitigate | Request sanitized metadata only; reject tokens, credentials, raw output/environment, paths, and private payloads. |
| T-127-13-03 | Repudiation | Physical/manual evidence | high | mitigate | Enumerated state/device/size/action steps and structured receipt IDs tied to candidate SHA. |
| T-127-13-04 | Tampering | Cross-client parity approval | medium | mitigate | Same fixture/state, explicit field/action comparison, and no approval with missing rows. |
| T-127-13-05 | Elevation of Privilege | Release authorization confusion | high | mitigate | Both checkpoint actions and resume signals explicitly exclude release authorization/outward actions. |
| T-127-13-06 | Denial of Service | Unavailable external environment | low | accept | Honest pending status blocks completion rather than forcing unsafe setup or fabricated proof. |

**ASVS L1 gate:** wrong-SHA evidence, disclosure, false manual claims, and release-authorization confusion are high severity; missing evidence blocks rather than being accepted.
</threat_model>

<verification>
1. Automated prechecks confirm the exact-SHA ledger contains separate pending hosted/authenticated and physical/visual/interactive/human rows.
2. Human checkpoint 1 approves only complete safe exact-SHA runtime/provider receipts.
3. Human checkpoint 2 approves only complete exact-SHA physical, responsive, interactive, reconnect, and parity receipts.
4. Neither checkpoint authorizes or performs tag, push, publication, GitHub Release, or release-only workflow dispatch.
</verification>

<success_criteria>
- Every non-deterministic evidence class is explicitly reviewed against the exact candidate SHA.
- Missing evidence remains pending and blocks approval without scope reduction or fabrication.
- Supplied evidence is safe to record and contains no secret/raw/private data.
- The checkpoint outcome is structured for Plan 127-14 reconciliation and carries no release authorization.
</success_criteria>

## Artifacts this phase produces

- Structured exact-SHA human checkpoint responses in `127-13-SUMMARY.md`.
- No product, release metadata, or receipt-ledger edits; reconciliation is isolated to Plan 127-14.

<output>
Create `.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-13-SUMMARY.md` when both checkpoints are approved; otherwise preserve the blocking pending state without claiming completion.
</output>
