---
phase: 127-stale-workspace-intelligence-and-rc-closure
plan: "11"
type: execute
wave: 8
depends_on:
  - 127-10
files_modified:
  - tests/commands/release-rc.test.ts
  - tests/architecture/release-publish.test.mjs
  - CHANGELOG.md
  - README.md
  - docs/stale-workspaces.md
  - docs/releasing.md
autonomous: true
requirements:
  - STALE-01
  - STALE-02
  - STALE-03
  - STALE-04
  - STALE-05
  - REL-01
  - REL-02
must_haves:
  truths:
    - "CHANGELOG contains exactly `## [0.22.0-rc.1] - 2026-07-17` and the wording `0.22.0-rc.1 / v0.22.0-rc.1; npm prerelease publication uses next`."
    - "Documentation explains the fixed strict 30-day advisory policy, every confirmed reason, incomplete unknown evidence, cautions, transparent ordering, and absence of safety/confidence scores."
    - "Migration notes state that Phase 127 changes no YAML schema, identity model, threshold setting, database, ORM, browser storage, or persisted evidence cache."
    - "Web and TUI entry/refresh/navigation/action shortcuts are documented from canonical metadata, including configured-shell/xterm/input ownership and Open before cleanup."
    - "Supported-provider scope names read-only GitHub.com PR and GitLab.com MR status, sanitized unknown failures, explicit Gitea opt-out, no inference/search/mutation, and no unclaimed self-hosted support."
    - "Archive/Remove/Force documentation points to canonical confirmations and dirty/terminal/fresh-inventory gates rather than granting authority from stale evidence."
    - "Release docs require local `npm run release:check` without `--tag`, exact-candidate-SHA evidence, preserved planning directories, and separate explicit authorization for every outward release action."
    - "The local RC gate runs successfully without changing tag refs, pushing, publishing, creating a GitHub Release, dispatching release-only workflows, or claiming pending human/hosted evidence."
  artifacts:
    - path: tests/commands/release-rc.test.ts
      provides: "Immediate RED changelog/docs assertions layered after Plan 127-10 metadata without weakening side-effect tests."
    - path: tests/architecture/release-publish.test.mjs
      provides: "Documentation and release-stop architecture assertions run by native Node."
    - path: docs/stale-workspaces.md
      provides: "Authoritative advisory policy, UI/shortcut, provider, migration, shell, action, and privacy documentation."
    - path: README.md
      provides: "Discoverable stale-workspace and RC documentation entry points."
    - path: CHANGELOG.md
      provides: "Exact dated `0.22.0-rc.1` release-candidate heading and scope."
    - path: docs/releasing.md
      provides: "Validation-only candidate procedure, tag/channel metadata, exact-SHA evidence classes, and release stop."
  key_links:
    - from: docs/stale-workspaces.md
      to: packages/client/src/shortcuts.ts
      via: "Canonical key labels and scope"
      pattern: "Ctrl.*S|workspace.stale"
    - from: docs/stale-workspaces.md
      to: packages/service/src/policy/stale-workspaces.ts
      via: "Exact qualification/ranking/unknown/caution semantics"
      pattern: "30|merged|closed|remote branch|worktree|incomplete"
    - from: docs/releasing.md
      to: scripts/release-rc-check.mjs
      via: "Validation-only command with tag flag omitted"
      pattern: "npm run release:check"
    - from: CHANGELOG.md
      to: package.json
      via: "Exact RC version/date/tag/channel wording"
      pattern: "0.22.0-rc.1"
  prohibitions:
    - statement: "Stale guidance must not state or imply that a workspace is safe to delete, assign a confidence or safety score, or pressure cleanup."
      status: unverified
      flagged: true
    - statement: "Unknown, unavailable, or failed evidence must not be hidden or converted into confirmed absence, presence, or proof of staleness."
      status: unverified
      flagged: true
    - statement: "Repository-scoped branch or worktree evidence must not be generalized into a workspace-wide safety claim."
      status: unverified
      flagged: true
    - statement: "Evaluation or refresh must not silently acquire lifecycle authority or perform automatic archive, remove, terminal, worktree, or YAML mutation."
      status: unverified
      flagged: true
    - statement: "Provider status must not be inferred from branch names or remotes or presented as authenticated fact without validated persisted provenance."
      status: unverified
      flagged: true
    - statement: "Deterministic or local evidence must not be represented as hosted, authenticated, physical, screenshot, interactive, human approval, or release authorization."
      status: unverified
      flagged: true
    - statement: "Release-candidate preparation must not imply authorization to tag, push, publish, create a GitHub Release, or dispatch release-only workflows."
      status: unverified
      flagged: true
---

<objective>
Document the complete stale policy/product boundary and exact `0.22.0-rc.1` release-candidate metadata, then run the local validation-only release gate without outward action.

Purpose: Make advisory semantics, migration/shortcut/shell/provider scope, package/tag identity, and the release stop explicit for users and operators.
Output: Immediate documentation RED assertions, new stale guide, README entry, changelog release-candidate section, releasing guidance, and the first complete post-manifest/post-doc `npm test` plus validation-only `release:check` receipt.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.claude/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-CONTEXT.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-UI-SPEC.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-COVERAGE.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-10-SUMMARY.md
@README.md
@CHANGELOG.md
@docs/releasing.md
@docs/forge-source-resolver.md
@package.json
@scripts/release-rc-check.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce changelog and documentation RED assertions immediately before writing docs</name>
  <read_first>
    - tests/commands/release-rc.test.ts
    - tests/architecture/release-publish.test.mjs
    - CHANGELOG.md
    - README.md
    - docs/releasing.md
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-CONTEXT.md
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-COVERAGE.md
  </read_first>
  <files>tests/commands/release-rc.test.ts, tests/architecture/release-publish.test.mjs</files>
  <action>After Plan 127-10 has made the manifest/lock assertions green, add the remaining RC documentation assertions immediately before implementation. Require the exact changelog heading `## [0.22.0-rc.1] - 2026-07-17`, exact sentence `0.22.0-rc.1 / v0.22.0-rc.1; npm prerelease publication uses next`, a README link to `docs/stale-workspaces.md`, stale policy/migration/shortcut/configured-shell/provider coverage, releasing guidance that runs `npm run release:check` without a tag flag, exact-SHA evidence separation, and preservation of `.planning/phases`. Preserve all Plan 127-02 side-effect spies and Plan 127-10 manifest/lock assertions. The native architecture file must run with `node --test`; the TypeScript command file must run with Vitest. The focused pair must be RED only on missing changelog/docs outputs.</action>
  <acceptance_criteria>
    - Manifest/lock assertions remain green while new changelog/docs assertions are RED for their intended missing outputs.
    - Side-effect, default-package, and planning-tree assertions remain present and green.
    - No test requires hosted/authenticated/physical/visual/interactive/human evidence to be marked passed.
    - Each test file is executed by its correct runner.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/architecture/release-publish.test.mjs</automated>
    <automated>npx vitest run tests/commands/release-rc.test.ts</automated>
  </verify>
  <done>The documentation RED gate is introduced at the immediate implementation boundary without breaking the already-green manifest or no-side-effect contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the stale-workspace, migration, shortcut, shell, provider, changelog, and release guides</name>
  <read_first>
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-CONTEXT.md
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-UI-SPEC.md
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-COVERAGE.md
    - README.md
    - CHANGELOG.md
    - docs/releasing.md
    - docs/forge-source-resolver.md
    - tests/commands/release-rc.test.ts
    - tests/architecture/release-publish.test.mjs
  </read_first>
  <files>docs/stale-workspaces.md, README.md, CHANGELOG.md, docs/releasing.md</files>
  <action>Create `docs/stale-workspaces.md` as the user/operator contract and add a discoverable README link/feature summary. State that the view is advisory and never automatic; define the strict fixed 30-day cutoff and all five confirmed reasons; explain provider/observation/activity timestamps, candidate versus incomplete, cautions, deduplication, service ranking, Refresh/cache behavior, and absence of score/safety claims. Document web location, exact macOS/Linux entry keys, active-overlay `R`, TUI keys and width fallback, canonical Open first, and unchanged Archive/Remove/dirty Force sequence. Add migration notes covering no YAML/schema/identity/config/database/ORM/browser-storage change, local evidence for workspaces without source provenance, and volatile-cache reset. Document configured-shell compatibility, xterm/input ownership, and GitHub/GitLab read-only status with explicit Gitea/search/mutation and unclaimed self-hosted dispositions. Add the exact changelog heading and metadata sentence, summarizing stale policy, prior parity closure, lockstep packages, configured-shell/migration notes, and validation-only boundary without claiming external receipts. Update releasing docs so preparation runs `npm run release:check` with no tag argument, freezes one exact candidate SHA, records deterministic and every hosted/manual class separately, preserves `.planning/phases`, and stops before tag, push, npm publication, GitHub Release creation, or release-only workflow dispatch absent separate explicit authorization.</action>
  <acceptance_criteria>
    - Guide matches D-01 through D-20 without adding a CLI, configurable threshold, Gitea status, inference, polling, or persistence.
    - Every shortcut and responsive/TUI tier is accurate and linked from README; migration and configured-shell sections are explicit.
    - Changelog heading/date and exact package/tag/channel sentence match D-21.
    - Releasing guide distinguishes preparation, evidence, authorization, and outward release actions and preserves planning artifacts.
    - Focused native/Vitest release tests are green without asserting any hosted/manual pass.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('node:fs'); const s=fs.readFileSync('docs/stale-workspaces.md','utf8'); const required=['30','GitHub','GitLab','Gitea','Ctrl','Command','Alt','Shift','Open','Archive','Remove','Force','unknown','incomplete','configured shell','no YAML']; for (const x of required) if (!s.toLowerCase().includes(x.toLowerCase())) throw new Error(x); if (!fs.readFileSync('README.md','utf8').includes('docs/stale-workspaces.md')) throw new Error('README link');"</automated>
    <automated>node --test tests/architecture/release-publish.test.mjs</automated>
    <automated>npx vitest run tests/commands/release-rc.test.ts</automated>
  </verify>
  <done>Users and operators have one complete discoverable stale/RC documentation set and every immediate docs assertion is green without overstating external evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Run the complete post-documentation local test and release-candidate gate</name>
  <read_first>
    - docs/releasing.md
    - scripts/release-rc-check.mjs
    - tests/architecture/release-publish.test.mjs
    - tests/commands/release-rc.test.ts
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md
  </read_first>
  <files>None — verification only</files>
  <action>Only after Plan 127-10 manifests/lock and this plan's changelog/docs are present, run the complete aggregate `npm test`, then run `npm run release:check` exactly as documented with no tag flag. Capture tag refs before and after and require equality. Let the release check build, typecheck, run Vitest/native Node/architecture/isolated TUI/coverage/security/license/package dry-run gates as defined by repository tooling. Do not amend tests to bypass a failure, do not tag/push/publish/release/dispatch, and do not delete or move any `.planning` artifact. Record commands/results in `127-11-SUMMARY.md` as local deterministic evidence only; leave every hosted/authenticated/live-service/physical/screenshot/interactive/human class pending for Plans 127-12/13.</action>
  <acceptance_criteria>
    - `npm test` exits zero after both manifests and docs have landed.
    - `npm run release:check` exits zero without a tag argument.
    - Tag ref snapshot is byte-identical before and after.
    - Release side-effect tests, package checks, and planning-tree preservation pass.
    - Summary makes no external/manual evidence claim and reports any unavailable optional environment honestly.
  </acceptance_criteria>
  <verify>
    <automated>npm test &amp;&amp; before="$(git for-each-ref --format='%(refname):%(objectname)' refs/tags)" &amp;&amp; npm run release:check &amp;&amp; after="$(git for-each-ref --format='%(refname):%(objectname)' refs/tags)" &amp;&amp; test "$before" = "$after"</automated>
  </verify>
  <done>The exact RC passes the first complete post-manifest/post-doc local suite with unchanged tag refs, preserved planning artifacts, no outward action, and no overstated evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Internal policy/metadata → user/operator documentation | Inaccurate wording could create unsafe cleanup or release authority. |
| Documentation command → local release tooling | Copy/paste guidance must not trigger an outward action by default. |
| Local validation result → external evidence claim | A green local suite cannot impersonate hosted/manual proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-127-11-01 | Spoofing | Stale documentation | high | mitigate | Exact locked reasons/unknowns/cautions/timestamps, neutral advisory language, and no score/safety claim. |
| T-127-11-02 | Elevation of Privilege | Lifecycle documentation | high | mitigate | Canonical Open/Archive/Remove/Force gates are documented; stale evidence grants no authority. |
| T-127-11-03 | Tampering | RC version/tag/channel text | high | mitigate | Exact literal heading/sentence plus release tests across manifests/changelog/docs. |
| T-127-11-04 | Elevation of Privilege | Release command guidance | high | mitigate | Validation command omits tag flag and docs require separate explicit authorization for all outward actions. |
| T-127-11-05 | Repudiation | Evidence claims | medium | mitigate | Local result labeled deterministic; external classes explicitly pending and moved to exact-SHA ledger. |
| T-127-11-06 | Tampering | Planning artifact retention | high | mitigate | Documentation and architecture tests explicitly preserve all phase directories. |

**ASVS L1 gate:** misleading cleanup/lifecycle/release authority and metadata/planning tampering are high severity and block candidate freeze if documentation or release tests fail.
</threat_model>

<verification>
1. Immediate documentation RED assertions are added, then documentation content checks and correctly routed release command/architecture tests pass.
2. The complete `npm test` and `npm run release:check` gates run only after manifests and docs land; both pass without `--tag` and tag refs remain unchanged.
3. `git diff --check` passes for changelog/README/docs.
4. No external/manual evidence is marked complete and no planning directory is deleted.
</verification>

<success_criteria>
- Users can understand exactly why rows appear, what remains unknown, and why the view never proves deletion safety.
- Migration, shortcuts, configured-shell behavior, supported providers, and canonical actions are documented.
- Exact RC package/tag/channel/date wording is consistent.
- Local release validation is green and stops before every outward action.
</success_criteria>

## Artifacts this phase produces

- Immediate changelog/docs RED assertions in the native release architecture test and Vitest release command test.
- `docs/stale-workspaces.md` complete advisory feature guide.
- README discovery link and summary.
- Exact dated RC changelog section.
- Validation-only release and exact-SHA evidence guidance.
- Green complete post-manifest/post-doc `npm test` and local `release:check` receipts in `127-11-SUMMARY.md`.

<output>
Create `.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-11-SUMMARY.md` when done.
</output>
