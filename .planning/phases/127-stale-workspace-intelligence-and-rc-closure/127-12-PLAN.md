---
phase: 127-stale-workspace-intelligence-and-rc-closure
plan: "12"
type: execute
wave: 9
depends_on:
  - 127-11
files_modified:
  - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md
  - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md
autonomous: true
requirements:
  - STALE-01
  - STALE-02
  - STALE-03
  - STALE-04
  - STALE-05
  - REL-01
  - REL-02
must_haves:
  truths:
    - "The exact candidate SHA is the clean committed HEAD after Plan 127-11 and before the receipt ledger; it is never amended or silently replaced."
    - "The ledger records version `0.22.0-rc.1`, intended tag `v0.22.0-rc.1`, npm channel `next`, branch, candidate SHA, command, timestamp, result, and safe artifact references."
    - "A second local `npm run release:check` without the tag flag passes for the exact candidate SHA with unchanged tag refs, proving repeated validation remains side-effect free."
    - "Deterministic/local, hosted runtime, authenticated GitHub, authenticated GitLab, reconnect/two-client, physical browser/xterm, responsive screenshots, interactive TUI, human parity, and release authorization are separate evidence rows."
    - "Per D-22, only performed evidence can be PASS; missing hosted/authenticated/physical/visual/interactive/human evidence remains PENDING and does not inherit local test status."
    - "Provider capability rows retain GitHub/GitLab read-only INTEGRATE, Gitea status OPT-OUT with reason, and provider mutation/search/inference OPT-OUT with reason."
    - "Self-hosted GitHub/GitLab support is `NOT_CLAIMED` unless the operator supplies an explicit supported-host list and exact-SHA receipts."
    - "Per D-23, no tag, push, publication, GitHub Release, release-only workflow dispatch, or implied release authorization occurs."
    - "A-EDGE-REL-01 remains unresolved and flagged until operator review accepts the exact-SHA ledger and all required external/manual evidence is supplied."
    - "Validation status becomes awaiting-human-evidence with Wave 0 and deterministic rows green, while nyquist_compliant stays false and Approval stays pending."
    - "Every prior phase-planning directory remains present and unchanged by release preparation."
  artifacts:
    - path: .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md
      provides: "Exact-candidate-SHA evidence index with safe PASS/PENDING/NOT_CLAIMED/OPT_OUT distinctions."
    - path: .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md
      provides: "Reconciled deterministic-green and external-pending validation ledger without false Nyquist approval."
  key_links:
    - from: .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md
      to: .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md
      via: "Every validation row references its exact evidence class/status for one candidate SHA"
      pattern: "candidate SHA|PASS|PENDING|NOT_CLAIMED|OPT_OUT"
    - from: .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md
      to: .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-COVERAGE.md
      via: "Provider capability dispositions and unresolved assumptions"
      pattern: "GitHub|GitLab|Gitea|A-EDGE-REL-01"
    - from: .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md
      to: scripts/release-rc-check.mjs
      via: "Exact command receipt with tag flag absent"
      pattern: "npm run release:check"
  assumptions:
    - statement: "A-EDGE-REL-01: an exact-predecessor-SHA receipt ledger with explicit pending evidence is the intended RC closure structure, but it remains unresolved and flagged until operator review accepts or supplies every required receipt."
      status: unresolved
      flagged: true
  prohibitions:
    - statement: "Deterministic or local evidence must not be represented as hosted, authenticated, physical, screenshot, interactive, human approval, or release authorization."
      status: unverified
      flagged: true
    - statement: "Release-candidate preparation must not imply authorization to tag, push, publish, create a GitHub Release, or dispatch release-only workflows."
      status: unverified
      flagged: true
---

<objective>
Freeze the exact post-documentation candidate SHA, rerun local RC validation without side effects, and create the canonical honest evidence ledger/validation state for D-21 through D-23.

Purpose: Make every performed and missing evidence class auditable without changing the candidate or converting local proof into external/human proof.
Output: `127-RECEIPTS.md` and a reconciled still-pending `127-VALIDATION.md`.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.claude/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-CONTEXT.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-COVERAGE.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-09-SUMMARY.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-10-SUMMARY.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-11-SUMMARY.md
@docs/releasing.md
@package.json
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze the exact clean candidate and build the safe evidence index</name>
  <read_first>
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-COVERAGE.md
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-09-SUMMARY.md
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-10-SUMMARY.md
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-11-SUMMARY.md
    - docs/releasing.md
  </read_first>
  <files>.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md</files>
  <action>Require a clean worktree/index at Plan start, then capture `git rev-parse HEAD` as immutable `candidate_sha`; do not amend or modify candidate product/docs afterward. Capture branch, `0.22.0-rc.1`, intended `v0.22.0-rc.1`, npm `next`, UTC timestamp, and tag-ref snapshot. Run `npm run release:check` again with no tag flag, require success and identical tag refs, and record a concise sanitized local receipt referencing command and result without embedding machine paths, credentials, environment, raw provider output, or bearer material. Create `127-RECEIPTS.md` with a status legend and separate rows for deterministic suites, Node/package matrices, hosted exact-SHA runtime workflows, authenticated GitHub.com/GitLab.com merged/closed/open/failure checks, reconnect/cancellation/two-client convergence, physical browser/xterm keys, desktop/375/320 light/dark screenshots, interactive TUI tiers/actions, human cross-client parity, and release authorization. Mark only performed exact-SHA evidence PASS. Mark absent evidence PENDING, self-hosted hosts NOT_CLAIMED absent an explicit list, and Gitea/mutation/search/inference OPT_OUT with the reasons from `127-COVERAGE.md`. Do not dispatch a workflow, authenticate, push, tag, publish, release, or invent URLs/checksums/screenshots/approval.</action>
  <acceptance_criteria>
    - Candidate SHA was captured from a clean committed tree before the ledger file existed and remains unchanged.
    - Repeated release check passes and tag refs are identical before/after.
    - Every manual/hosted row from `127-VALIDATION.md` exists with exact-SHA and status fields.
    - No PENDING/NOT_CLAIMED/OPT_OUT row is described as passed or covered by deterministic tests.
    - Provider dispositions exactly match `127-COVERAGE.md` and every opt-out has a reason.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('node:fs'),cp=require('node:child_process'); const s=fs.readFileSync('.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md','utf8'); const sha=cp.execFileSync('git',['rev-parse','HEAD'],{encoding:'utf8'}).trim(); for (const x of [sha,'0.22.0-rc.1','v0.22.0-rc.1','next','GitHub','GitLab','Gitea','PASS','PENDING','NOT_CLAIMED','OPT_OUT']) if (!s.includes(x)) throw new Error(x);"</automated>
  </verify>
  <done>An immutable exact-candidate-SHA ledger records the repeated local pass and every external/manual class as performed or honestly pending/not-claimed/opted-out, with no release side effect.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile validation state without false Nyquist or approval claims</name>
  <read_first>
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-CONTEXT.md
  </read_first>
  <files>.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md</files>
  <action>Update the validation ledger from actual summaries/receipts: set Wave 0 complete only if every focused test/fixture exists; mark deterministic rows green only when their commands passed on the recorded candidate; set phase validation status to awaiting human evidence; keep `nyquist_compliant: false`; keep Approval pending; and link each manual/hosted row to the corresponding PENDING receipt. Record A-EDGE-STALE-03's actual deterministic result and retain A-EDGE-REL-01 as unresolved/flagged for operator review. Do not turn unavailable `glab`, unpushed hosted workflows, absent screenshots, or missing human approval into waivers or passes. Do not edit product, package, docs, changelog, candidate commit, or prior phase artifacts.</action>
  <acceptance_criteria>
    - Wave 0 and deterministic status accurately match file existence and green commands.
    - `nyquist_compliant` remains false and Approval remains pending while any required external/manual row is pending.
    - Each pending row points to the exact candidate SHA receipt, not a later ledger commit.
    - Only the two declared planning evidence files change after candidate freeze.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('node:fs'); const v=fs.readFileSync('.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md','utf8'); if (!/nyquist_compliant:\s*false/.test(v) || !/wave_0_complete:\s*true/.test(v) || !/Approval:\s*pending/i.test(v) || !/PENDING/i.test(v)) process.exit(1);"</automated>
    <automated>node -e "const cp=require('node:child_process'); const ok=new Set(['.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md','.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md']); const files=cp.execFileSync('git',['diff','--name-only'],{encoding:'utf8'}).trim().split(/\n/).filter(Boolean); for (const f of files) if (!ok.has(f)) throw new Error(f);"</automated>
  </verify>
  <done>The validation ledger truthfully records deterministic completion and exact-SHA pending evidence without declaring Nyquist compliance, approval, or release authorization.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Git HEAD/worktree → candidate identity | Uncommitted or later changes could make receipts refer to the wrong source. |
| Local command results → receipt status | Deterministic success could be mislabeled as hosted/manual success. |
| Human/provider evidence → planning ledger | URLs, output, credentials, screenshots, and approvals require sanitization and exact-SHA binding. |
| Evidence preparation → release authority | Recording metadata must not trigger or imply a release. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-127-12-01 | Spoofing | Candidate SHA | high | mitigate | Clean-tree precondition, capture HEAD before ledger, immutable SHA field, and no candidate product/docs edits afterward. |
| T-127-12-02 | Repudiation | Evidence status/classes | high | mitigate | Explicit status legend, exact command/SHA/time, separate evidence rows, and no inherited pass status. |
| T-127-12-03 | Information Disclosure | Receipt content | high | mitigate | Sanitized summaries only; exclude paths, credentials, environment, bearer material, raw output, and fabricated artifacts. |
| T-127-12-04 | Elevation of Privilege | Release actions | high | mitigate | Local release check without tag flag; no push/dispatch/auth/publish/release commands; tag refs compared. |
| T-127-12-05 | Tampering | Validation/Nyquist state | high | mitigate | Automated assertions keep Nyquist false and approval pending while external rows are pending. |
| T-127-12-06 | Tampering | Post-freeze file scope | medium | mitigate | Allowlist check permits only receipt and validation files after candidate freeze. |

**ASVS L1 gate:** candidate spoofing, evidence misrepresentation/disclosure, release authority, and false validation approval are high severity and block handoff if any assertion fails.
</threat_model>

<verification>
1. Worktree/index is clean before candidate SHA capture.
2. Repeated `npm run release:check` succeeds without tag flag and tag refs remain unchanged.
3. Receipt validator confirms exact SHA/version/tag/channel/provider/status vocabulary.
4. Validation stays Nyquist-false/approval-pending with all unperformed evidence explicitly pending.
5. Only receipt and validation files change after candidate freeze; every phase directory remains present.
</verification>

<success_criteria>
- One immutable candidate SHA anchors all local and pending evidence.
- Local validation is repeated and side-effect free.
- Every required external/manual class has an honest status, never a fabricated pass.
- A-EDGE-REL-01 remains visibly flagged for the operator boundary.
- No outward release action or candidate mutation occurs.
</success_criteria>

## Artifacts this phase produces

- `127-RECEIPTS.md` exact-SHA evidence index and provider capability status ledger.
- Updated `127-VALIDATION.md` with deterministic green rows and honest pending human/hosted rows.

<output>
Create `.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-12-SUMMARY.md` when done.
</output>
