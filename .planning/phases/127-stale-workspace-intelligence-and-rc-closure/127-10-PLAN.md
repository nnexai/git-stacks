---
phase: 127-stale-workspace-intelligence-and-rc-closure
plan: "10"
type: execute
wave: 7
depends_on:
  - 127-09
files_modified:
  - tests/commands/release-rc.test.ts
  - tests/architecture/release-publish.test.mjs
  - package.json
  - packages/protocol/package.json
  - packages/client/package.json
  - packages/core/package.json
  - packages/cli/package.json
  - packages/service/package.json
  - packages/web/package.json
  - packages/tui/package.json
  - package-lock.json
autonomous: true
requirements:
  - REL-01
  - REL-02
must_haves:
  truths:
    - "Per D-21, root facade and all seven workspace package versions equal `0.22.0-rc.1` in one lockstep candidate."
    - "Every internal `@git-stacks/*` dependency range is the exact value `0.22.0-rc.1`; no caret, tilde, workspace placeholder, or prior RC remains."
    - "`package-lock.json` is regenerated by npm from the final manifests and records the same root/workspace versions and exact internal graph."
    - "The default root facade continues to expose CLI without pulling TUI, Bun, or OpenTUI into the default production graph."
    - "Repeated local release validation has no outward release side effects: no tag, push, package publication, GitHub Release, or release-workflow dispatch occurs; ordinary local build/package outputs are permitted."
    - "Parallel implementation or verification work is restricted to local/read-only gates and cannot perform release side effects."
    - "Per D-23, this plan prepares metadata only; intended tag `v0.22.0-rc.1` and npm channel `next` are documentation metadata, not actions."
    - "Release preparation preserves every `.planning/phases` directory and prior phase artifact."
  artifacts:
    - path: tests/commands/release-rc.test.ts
      provides: "Immediate RED manifest/internal-range/lockfile assertions plus preserved validation-only side-effect tests."
    - path: tests/architecture/release-publish.test.mjs
      provides: "Immediate RED default-package and planning-preservation assertions for the RC manifest graph."
    - path: package.json
      provides: "Root facade version `0.22.0-rc.1` and exact CLI dependency."
    - path: packages/protocol/package.json
      provides: "Protocol RC manifest."
    - path: packages/client/package.json
      provides: "Client RC manifest and exact protocol dependency."
    - path: packages/core/package.json
      provides: "Core RC manifest and exact internal dependencies."
    - path: packages/cli/package.json
      provides: "CLI RC manifest and exact core dependency."
    - path: packages/service/package.json
      provides: "Service RC manifest and exact internal dependencies."
    - path: packages/web/package.json
      provides: "Web RC manifest and browser-safe exact dependencies."
    - path: packages/tui/package.json
      provides: "Optional TUI RC manifest without entering the root default graph."
    - path: package-lock.json
      provides: "npm-generated lockstep RC dependency graph."
  key_links:
    - from: package.json
      to: packages/cli/package.json
      via: "Exact root-facade dependency"
      pattern: "@git-stacks/cli.*0.22.0-rc.1"
    - from: packages/*/package.json
      to: package-lock.json
      via: "`npm install --package-lock-only` regeneration after all manifest edits"
      pattern: "0.22.0-rc.1"
    - from: tests/commands/release-rc.test.ts
      to: package.json
      via: "Wave 0 lockstep and no-default-release assertions"
      pattern: "0.22.0-rc.1|v0.22.0-rc.1"
  prohibitions:
    - statement: "Deterministic or local evidence must not be represented as hosted, authenticated, physical, screenshot, interactive, human approval, or release authorization."
      status: unverified
      flagged: true
    - statement: "Release-candidate preparation must not imply authorization to tag, push, publish, create a GitHub Release, or dispatch release-only workflows."
      status: unverified
      flagged: true
---

<objective>
Move the root facade, all seven workspace manifests, every exact internal range, and npm lockfile into one `0.22.0-rc.1` release-candidate graph per D-21 without performing a release.

Purpose: Establish the exact package identity required before changelog/documentation and exact-SHA evidence closure.
Output: Immediate manifest/lock RED assertions, eight lockstep manifests, and a regenerated package lock that pass focused package/release architecture tests without depending on Plan 127-11 documentation.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.claude/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-CONTEXT.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-COVERAGE.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-02-SUMMARY.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-09-SUMMARY.md
@package.json
@packages/protocol/package.json
@packages/client/package.json
@packages/core/package.json
@packages/cli/package.json
@packages/service/package.json
@packages/web/package.json
@packages/tui/package.json
@package-lock.json
@tests/commands/release-rc.test.ts
@tests/architecture/release-publish.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce manifest and lockfile RED assertions immediately before metadata changes</name>
  <read_first>
    - tests/commands/release-rc.test.ts
    - tests/architecture/release-publish.test.mjs
    - package.json
    - package-lock.json
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-02-SUMMARY.md
  </read_first>
  <files>tests/commands/release-rc.test.ts, tests/architecture/release-publish.test.mjs</files>
  <action>Migrate the existing top-level v0.21.0 release-candidate smoke assertions in `tests/commands/release-rc.test.ts`—including the hard-coded `0.21.0-rc.6` suite/version/changelog expectations—to the Phase 127 `0.22.0-rc.1` contract while retaining still-relevant historical architecture/workflow coverage as explicitly historical assertions. Extend the green Wave 0 release-authority fence with assertions scoped only to Plan 127-10 outputs: root plus seven workspace versions must equal `0.22.0-rc.1`, every internal `@git-stacks/*` range must be exact, the npm lockfile root/workspace records must match, and the default production graph must still exclude TUI, Bun, and OpenTUI. Preserve the hermetic repeated no-tag harness and planning-tree sentinel from Plan 127-02. Do not add changelog, README, stale-guide, or releasing-guide expectations here; those are introduced in Plan 127-11 immediately before documentation implementation. Run the native architecture file with `node --test` and the TypeScript command file with Vitest. The focused pair must be RED only on the pre-RC manifest/lock state, never on stale v0.21 expectations or missing Plan 127-11 files.</action>
  <acceptance_criteria>
    - Focused release tests fail only because manifest/internal-range/lockfile values are not yet `0.22.0-rc.1`; no active release-current assertion still expects `0.21.0-rc.6`.
    - Historical v0.21 coverage that remains relevant is clearly separated from the current RC identity rather than silently deleted.
    - No assertion reads or requires Plan 127-11 changelog/docs outputs.
    - Existing no-side-effect and planning-preservation assertions remain green within the focused run.
    - The native Node and Vitest files are executed by their correct runners.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/architecture/release-publish.test.mjs</automated>
    <automated>npx vitest run tests/commands/release-rc.test.ts</automated>
  </verify>
  <done>The immediate RC metadata RED gate exists and is isolated from future documentation, while release side-effect and planning-preservation tests stay intact.</done>
</task>

<task type="auto">
  <name>Task 2: Advance root, protocol, client, core, and CLI manifests in lockstep</name>
  <read_first>
    - tests/commands/release-rc.test.ts
    - package.json
    - packages/protocol/package.json
    - packages/client/package.json
    - packages/core/package.json
    - packages/cli/package.json
  </read_first>
  <files>package.json, packages/protocol/package.json, packages/client/package.json, packages/core/package.json, packages/cli/package.json</files>
  <action>Set each of these five manifest `version` fields to exactly `0.22.0-rc.1`. Update every internal `@git-stacks/*` dependency, devDependency, optionalDependency, and peerDependency in these files to the exact same value, including the root facade's `@git-stacks/cli` edge. Preserve existing scripts, exports, files lists, engines, dependency categories, and optional-package boundaries. Do not add, remove, rename, or install any package. Do not edit changelog/docs/lockfile yet, alter package publication settings, tag, push, publish, create a release, dispatch a workflow, or delete/move any planning artifact.</action>
  <acceptance_criteria>
    - All five versions equal `0.22.0-rc.1`.
    - Every internal dependency in these five manifests is exact and matches the version.
    - Root production graph still reaches CLI only and contains no TUI/Bun/OpenTUI edge.
    - No non-version manifest semantics or planning files change.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('node:fs'); const files=['package.json','packages/protocol/package.json','packages/client/package.json','packages/core/package.json','packages/cli/package.json']; for (const f of files) { const p=JSON.parse(fs.readFileSync(f,'utf8')); if (p.version!=='0.22.0-rc.1') throw new Error(f); for (const k of ['dependencies','devDependencies','optionalDependencies','peerDependencies']) for (const [n,v] of Object.entries(p[k]||{})) if (n.startsWith('@git-stacks/') &amp;&amp; v!=='0.22.0-rc.1') throw new Error(f+':'+n+':'+v); }"</automated>
  </verify>
  <done>The facade and protocol/client/core/CLI half of the graph carry the exact RC version/ranges with no package-boundary or release side effect.</done>
</task>

<task type="auto">
  <name>Task 3: Advance service, web, and optional TUI manifests and regenerate the lockfile</name>
  <read_first>
    - packages/service/package.json
    - packages/web/package.json
    - packages/tui/package.json
    - package-lock.json
    - tests/architecture/release-publish.test.mjs
    - tests/commands/release-rc.test.ts
  </read_first>
  <files>packages/service/package.json, packages/web/package.json, packages/tui/package.json, package-lock.json</files>
  <action>Set service, web, and TUI package versions to exactly `0.22.0-rc.1` and update every internal dependency range to the exact same value while preserving dependency category and optional TUI isolation. After all eight manifests are final, regenerate `package-lock.json` with `npm install --package-lock-only --ignore-scripts --no-audit --no-fund`; do not add a dependency or run lifecycle scripts. Inspect the lockfile through automated tests rather than hand-editing it. Run only the focused native release architecture test, Vitest release command test, and package dry-runs; do not run aggregate `npm test` or `release:check` until Plan 127-11 has added the changelog and documentation assertions and implemented their outputs. If npm proposes unrelated dependency changes, stop and resolve the manifest/lock mismatch rather than accepting drift. This task remains metadata/validation only and must not perform any outward release action or remove prior phase planning directories.</action>
  <acceptance_criteria>
    - All eight manifest versions and every internal exact range equal `0.22.0-rc.1`.
    - Lockfile root and workspace package records match the manifests without unrelated package churn.
    - Root/default package dry-run does not include TUI, Bun, or OpenTUI.
    - Focused release tests prove ordinary/repeated validation has zero tag/push/publish/release/dispatch calls and require no Plan 127-11 output.
  </acceptance_criteria>
  <verify>
    <automated>npm install --package-lock-only --ignore-scripts --no-audit --no-fund</automated>
    <automated>node --test tests/architecture/release-publish.test.mjs</automated>
    <automated>npx vitest run tests/commands/release-rc.test.ts</automated>
    <automated>npm run check:packages</automated>
  </verify>
  <done>The complete manifest/lock graph is a coherent `0.22.0-rc.1` candidate, focused package/release gates pass independently of documentation, and no outward release action occurs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Workspace manifests → npm lock/package graph | Version/range drift could publish incompatible internal packages or pull optional runtimes by default. |
| Local npm/release validation → external registries/Git hosts | Metadata preparation must not become publication or tag/release activity. |
| Release scripts → repository planning tree | Preparation must preserve all prior phase evidence and plans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-127-10-01 | Tampering | Manifest/internal range graph | high | mitigate | Automated scan requires all eight versions and all internal ranges to equal the exact RC. |
| T-127-10-02 | Elevation of Privilege | Default package graph | high | mitigate | Release architecture and package dry-runs reject TUI/Bun/OpenTUI in default production install. |
| T-127-10-03 | Tampering | npm lock regeneration | medium | mitigate | Package-lock-only, no scripts, final-manifest ordering, and unrelated-drift stop condition. |
| T-127-10-04 | Elevation of Privilege | Release tooling side effects | high | mitigate | Wave 0 command spies plus explicit omission of tag/push/publish/release/dispatch commands. |
| T-127-10-05 | Tampering | Planning artifact preservation | high | mitigate | Release architecture test retains a planning-tree sentinel; task forbids deletion/move. |
| T-127-10-06 | Information Disclosure | npm command | low | accept | Lockfile-only metadata resolution uses normal npm configuration and prints no secret in committed artifacts; no new package is introduced. |

**ASVS L1 gate:** lockstep integrity, default-graph isolation, release side effects, and planning preservation are high severity and must all pass before documentation/evidence plans.
</threat_model>

<verification>
1. Automated manifest/internal-range scan passes for root and all seven workspaces.
2. `package-lock.json` is regenerated from final manifests with no lifecycle scripts or unrelated dependency drift.
3. Release architecture/command tests and `npm run check:packages` pass.
4. `git status --short` shows only the nine declared metadata files plus pre-existing Phase 127 planning artifacts; no phase directory is deleted.
</verification>

<success_criteria>
- Root and all seven workspace packages form one exact `0.22.0-rc.1` graph.
- Internal package ranges and lockfile are synchronized.
- Default installation remains Node/CLI-only with optional TUI isolation.
- No tag, push, publish, GitHub Release, workflow dispatch, or planning-artifact deletion occurs.
</success_criteria>

## Artifacts this phase produces

- Immediate focused manifest/lock RED assertions in `tests/commands/release-rc.test.ts` and `tests/architecture/release-publish.test.mjs`, with no Plan 127-11 dependency.
- Eight lockstep `0.22.0-rc.1` package manifests.
- Regenerated `package-lock.json` with exact internal RC dependencies.
- Green release metadata, architecture, and package dry-run receipts in `127-10-SUMMARY.md`.

<output>
Create `.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-10-SUMMARY.md` when done.
</output>
