---
phase: 127-stale-workspace-intelligence-and-rc-closure
plan: "14"
type: execute
wave: 11
depends_on:
  - 127-13
files_modified:
  - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md
  - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md
autonomous: true
requirements:
  - STALE-01
  - STALE-02
  - STALE-03
  - STALE-04
  - STALE-05
  - REL-01
  - REL-02
must_haves:
  truths:
    - "Only safe exact-candidate-SHA evidence explicitly approved in `127-13-SUMMARY.md` can change a required PENDING receipt to PASS; canonical JSON row IDs, candidate_sha, and required_subcases remain byte-for-byte equivalent to the committed Plan 127-12 ledger."
    - "Wrong-SHA, incomplete, unsanitized, or still-pending human evidence stops reconciliation and leaves validation unapproved."
    - "The immutable candidate commit exists, is an ancestor of the ledger commit, and contains package version `0.22.0-rc.1`; receipt edits never move the candidate."
    - "Every required HST/AUTH/FORGE/LIVE/PHYS/VIS/INT/HUMAN row—including archived flows, stale flows, attention/fuzzy navigation, configured-shell/SSH-agent behavior, the complete GitHub/GitLab same/fork Resolve → Review → Create and recovery matrix, broader Phase 126 actions/notes/file status, full lifecycle, responsive screenshots, interactive TUI, and cross-client human parity—is complete before `nyquist_compliant` or Approval can become true/approved."
    - "Intentional Gitea, provider mutation, provider search/inference, and unclaimed self-hosted dispositions remain OPT_OUT/NOT_CLAIMED with reasons rather than false failures or passes."
    - "A-EDGE-STALE-03 records its actual deterministic no-mutation outcome; A-EDGE-REL-01 is resolved only by the accepted exact-SHA ledger."
    - "Release authorization remains `NOT_AUTHORIZED` and successful REL-02 evidence is the preserved stop: no tag, push, publication, GitHub Release, or release-only workflow dispatch."
    - "All seven recalled prohibitions remain visible with their actual reviewed outcomes and are never silently removed or auto-dismissed."
    - "All prior planning directories remain intact and the final ledger contains no credential, token, raw environment, raw provider output, machine path, or private payload."
  artifacts:
    - path: .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md
      provides: "Final sanitized exact-SHA evidence ledger whose strict canonical JSON preserves the original row/subcase matrix and explicit release stop."
    - path: .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md
      provides: "Final evidence-backed validation state, or an honest still-pending state if any required receipt is absent."
  key_links:
    - from: .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-13-SUMMARY.md
      to: .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md
      via: "Sanitized exact-SHA receipt IDs and human approval outcomes"
      pattern: "approved-hosted|approved-manual|candidate_sha"
    - from: .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md
      to: .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md
      via: "Required evidence rows gate Nyquist and Approval fields"
      pattern: "PASS|OPT_OUT|NOT_CLAIMED|NOT_AUTHORIZED|PENDING"
    - from: .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md
      to: package.json
      via: "Candidate commit object contains exact RC version"
      pattern: "0.22.0-rc.1"
  assumptions:
    - statement: "A-EDGE-REL-01 remains unresolved and flagged unless exact-SHA receipt reconciliation passes every required row and operator review from Plan 127-13 is present; report either resolved or blocking-pending explicitly."
      status: unresolved
      flagged: true
  prohibitions:
    - statement: "Stale guidance must not state or imply that a workspace is safe to delete, assign a confidence or safety score, or pressure cleanup."
      status: unverified
      flagged: true
    - statement: "Unknown, unavailable, or failed evidence must not be hidden or converted into confirmed absence, presence, or proof of staleness."
      status: unverified
      flagged: true
    - statement: "Repository-scoped branch or worktree evidence must not be generalized into a workspace-wide safety claim."
      status: unverified
      flagged: true
    - statement: "Evaluation or refresh must not silently acquire lifecycle authority or perform automatic archive, remove, terminal, worktree, or YAML mutation."
      status: unverified
      flagged: true
    - statement: "Provider status must not be inferred from branch names or remotes or presented as authenticated fact without validated persisted provenance."
      status: unverified
      flagged: true
    - statement: "Deterministic or local evidence must not be represented as hosted, authenticated, physical, screenshot, interactive, human approval, or release authorization."
      status: unverified
      flagged: true
    - statement: "Release-candidate preparation must not imply authorization to tag, push, publish, create a GitHub Release, or dispatch release-only workflows."
      status: unverified
      flagged: true
---

<objective>
Reconcile the approved Plan 127-13 human evidence into the exact-SHA ledger, enforce final evidence/prohibition/security gates, and close validation without changing or releasing the candidate.

Purpose: Separate human verification from file mutation while ensuring final status is mechanically derived from safe exact-SHA evidence rather than narrative approval.
Output: Final receipts and validation state, or an explicit blocking pending state.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.claude/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-CONTEXT.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-COVERAGE.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-12-SUMMARY.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-13-SUMMARY.md
@package.json
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sanitize and reconcile approved exact-SHA human receipts</name>
  <read_first>
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-13-SUMMARY.md
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-COVERAGE.md
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md
  </read_first>
  <files>.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md</files>
  <action>Parse the marker-delimited canonical JSON from the committed Plan 127-12 ledger and the structured row-keyed checkpoint responses from `127-13-SUMMARY.md`. Before mutation, load the committed ledger with `git show HEAD:.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md`; treat its schema_version, candidate_sha, exact row ID set, and each row's required_subcases as immutable baseline data. Require both approved signals and a full SHA equal to the immutable ledger candidate for every required row: HST-RUNTIME; HST-SHELL; HST-SSH; AUTH-GH-STATUS; AUTH-GL-STATUS; FORGE-GH-SAME; FORGE-GH-FORK; FORGE-GL-SAME; FORGE-GL-FORK; FORGE-RECOVERY; LIVE-ARCHIVE; LIVE-REMOVE-FORCE; LIVE-STALE; LIVE-ATTENTION-FUZZY; LIVE-P126-ACTIONS; LIVE-P126-NOTES; LIVE-P126-FILE-STATUS; LIVE-LIFECYCLE; PHYS-BROWSER-XTERM; VIS-RESPONSIVE; INT-TUI; and HUMAN-PARITY. For each row, require `approved: true` and an approved_subcases set exactly equal to the immutable required_subcases set; never accept a generic hosted/manual approval or edit the required list to fit supplied evidence. Reject and stop on any pending signal, wrong SHA, duplicate/missing/extra row, missing/extra subcase, missing field, secret/token pattern, raw environment/output, absolute machine path, private payload, or ambiguous narrative. For valid rows, copy only safe provider/host/workflow/job/state/conclusion/UTC/reference/checksum metadata into that row's JSON evidence and change only its status from PENDING to PASS; regenerate any human-readable table from the JSON rather than maintaining a second authority. Preserve deterministic receipts, provider capability OPT_OUT/NOT_CLAIMED reasons, self-hosted NOT_CLAIMED absent an explicit list, and release authorization `NOT_AUTHORIZED`. Record the reviewed outcome of each recalled prohibition without deleting its statement, flagged provenance, or distinction between deterministic and human evidence. Do not run provider/release commands, modify the candidate, or invent evidence absent from the summary.</action>
  <acceptance_criteria>
    - Canonical JSON parses once; schema_version, candidate_sha, exact row IDs, and every required_subcases array equal the committed pre-reconciliation ledger.
    - Every named HST/AUTH/FORGE/LIVE/PHYS/VIS/INT/HUMAN row and its required subcases are PASS only with matching full SHA, approved=true, exact approved-subcase set, and safe required metadata.
    - Any pending, duplicate, extra, omitted, collapsed, wrong-SHA, or unsafe row/subcase stops the task and remains PENDING.
    - Capability opt-outs/not-claimed and release not-authorized status remain explicit with reasons.
    - Candidate SHA and deterministic receipt content are unchanged.
    - No secret, private content, raw output/environment, or absolute machine path enters the ledger.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('node:fs'),cp=require('node:child_process'),p='.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md',a='&lt;!-- phase127-receipts-json:start --&gt;',b='&lt;!-- phase127-receipts-json:end --&gt;'; const parse=s=&gt;{const i=s.indexOf(a),j=s.indexOf(b); if(i&lt;0||j&lt;=i||s.indexOf(a,i+1)!==-1||s.indexOf(b,j+1)!==-1) throw new Error('receipt JSON markers'); return JSON.parse(s.slice(i+a.length,j).trim())}; const base=parse(cp.execFileSync('git',['show','HEAD:'+p],{encoding:'utf8'})),cur=parse(fs.readFileSync(p,'utf8')),ids=['HST-RUNTIME','HST-SHELL','HST-SSH','AUTH-GH-STATUS','AUTH-GL-STATUS','FORGE-GH-SAME','FORGE-GH-FORK','FORGE-GL-SAME','FORGE-GL-FORK','FORGE-RECOVERY','LIVE-ARCHIVE','LIVE-REMOVE-FORCE','LIVE-STALE','LIVE-ATTENTION-FUZZY','LIVE-P126-ACTIONS','LIVE-P126-NOTES','LIVE-P126-FILE-STATUS','LIVE-LIFECYCLE','PHYS-BROWSER-XTERM','VIS-RESPONSIVE','INT-TUI','HUMAN-PARITY','RELEASE-AUTHORIZATION']; if(cur.schema_version!==base.schema_version||cur.candidate_sha!==base.candidate_sha||JSON.stringify(Object.keys(cur.rows).sort())!==JSON.stringify([...ids].sort())) throw new Error('immutable ledger identity'); for(const id of ids.slice(0,-1)){const before=base.rows[id],row=cur.rows[id]; if(JSON.stringify(row.required_subcases)!==JSON.stringify(before.required_subcases)||row.candidate_sha!==cur.candidate_sha||row.status!=='PASS'||row.evidence?.approved!==true||row.evidence?.candidate_sha!==cur.candidate_sha) throw new Error(id); const x=[...row.required_subcases].sort(),y=[...(row.evidence.approved_subcases||[])].sort(); if(JSON.stringify(x)!==JSON.stringify(y)) throw new Error(id+':subcases'); const bad=new Set(['token','credential','credentials','authorization','bearer','stdout','stderr','environment','raw_environment','raw_output','absolute_path','private_key']); const walk=v=&gt;{if(Array.isArray(v)){for(const z of v) walk(z); return} if(v&amp;&amp;typeof v==='object'){for(const [k,z] of Object.entries(v)){if(bad.has(k.toLowerCase())) throw new Error(id+':unsafe-key:'+k); walk(z)} return} if(typeof v==='string'&amp;&amp;/(?:^|[\s\"'])\/(?:home|Users|tmp)\//.test(v)) throw new Error(id+':unsafe-path')}; walk(row.evidence); } if(cur.rows['RELEASE-AUTHORIZATION'].status!=='NOT_AUTHORIZED'||cur.capabilities?.self_hosted!=='NOT_CLAIMED') throw new Error('scope disposition'); cp.execFileSync('git',['cat-file','-e',cur.candidate_sha+'^{commit}']); const pkg=JSON.parse(cp.execFileSync('git',['show',cur.candidate_sha+':package.json'],{encoding:'utf8'})); if(pkg.version!=='0.22.0-rc.1') throw new Error(pkg.version);"</automated>
  </verify>
  <done>All and only approved safe exact-SHA human receipts are reconciled; otherwise the ledger remains honestly pending and this plan stops.</done>
</task>

<task type="auto">
  <name>Task 2: Derive final validation, assumption, prohibition, and release-stop status</name>
  <read_first>
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-COVERAGE.md
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-CONTEXT.md
  </read_first>
  <files>.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md, .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md</files>
  <action>Parse the single canonical JSON receipt object rather than grepping narrative text. Require the candidate commit to exist, differ from the evidence-ledger commit, and be an ancestor of current HEAD with `git merge-base --is-ancestor`; require every required row exactly once, PASS, bound to candidate_sha, and carrying an approved_subcases set exactly equal to immutable required_subcases. Audit every required validation/evidence row against that reconciled ledger and all seven recalled prohibitions against their actual deterministic/human proof. Explicitly gate ROADMAP success criterion 4 and the entire Phase 126 handoff: archived-workspace lifecycle; clean/dirty/stale-confirmation removal; stale intelligence; attention and fuzzy navigation; configured Bash/zsh/fish plus PATH/SSH_AUTH_SOCK/ssh-add rotation; authenticated GitHub/GitLab status; GitHub/GitLab same-repository/project and fork Resolve → Review → Create plus every recovery/rollback case; broader actions, notes, file-status and operation parity; reconnect/cancellation/refresh-failed lock/simultaneous-client lifecycle; physical browser/xterm input; all responsive light/dark screenshot states; interactive TUI; and human cross-client parity. If any required HST/AUTH/FORGE/LIVE/PHYS/VIS/INT/HUMAN row or subcase is PENDING, or any high ASVS mitigation/prohibition review is unresolved, leave `nyquist_compliant: false`, Approval pending, A-EDGE-REL-01 unresolved/flagged, and stop with a blocking gap. If all required rows are PASS and intentional scope dispositions have valid reasons, set validation complete, `nyquist_compliant: true`, Wave 0 complete, and Approval to the human-reviewed candidate SHA/date; record A-EDGE-REL-01 resolved by accepted exact-SHA ledger and A-EDGE-STALE-03 by green no-mutation proof. Keep self-hosted hosts NOT_CLAIMED unless an explicit list was reviewed, release authorization `NOT_AUTHORIZED`, and REL-02 satisfied by unchanged tag refs plus absence of tag/push/publish/release/dispatch. Confirm all D-01..D-23, STALE-01..05, REL-01..02, UI/edge rows, provider matrix, and planning-directory preservation are represented. Do not alter product/docs/package metadata or perform outward actions.</action>
  <acceptance_criteria>
    - Candidate commit identity/version and candidate-to-ledger/current-HEAD ancestry are mechanically proven before approval.
    - Final validation status is mechanically consistent with every named row/subcase; no pending required row from ROADMAP success criterion 4 or the Phase 126 handoff coexists with Nyquist true or approval.
    - Both edge assumptions have explicit actual outcomes, never silent dismissal.
    - All seven recalled prohibitions remain listed with reviewed outcomes.
    - Release remains not authorized and no tag/push/publish/release/dispatch occurred.
    - Candidate commit, every prior phase directory, and all source coverage remain intact.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('node:fs'),cp=require('node:child_process'),p='.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-RECEIPTS.md',s=fs.readFileSync(p,'utf8'),v=fs.readFileSync('.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md','utf8'),a='&lt;!-- phase127-receipts-json:start --&gt;',b='&lt;!-- phase127-receipts-json:end --&gt;',i=s.indexOf(a),j=s.indexOf(b); if(i&lt;0||j&lt;=i) throw new Error('receipt JSON'); const d=JSON.parse(s.slice(i+a.length,j).trim()),ids=['HST-RUNTIME','HST-SHELL','HST-SSH','AUTH-GH-STATUS','AUTH-GL-STATUS','FORGE-GH-SAME','FORGE-GH-FORK','FORGE-GL-SAME','FORGE-GL-FORK','FORGE-RECOVERY','LIVE-ARCHIVE','LIVE-REMOVE-FORCE','LIVE-STALE','LIVE-ATTENTION-FUZZY','LIVE-P126-ACTIONS','LIVE-P126-NOTES','LIVE-P126-FILE-STATUS','LIVE-LIFECYCLE','PHYS-BROWSER-XTERM','VIS-RESPONSIVE','INT-TUI','HUMAN-PARITY']; const head=cp.execFileSync('git',['rev-parse','HEAD'],{encoding:'utf8'}).trim(); if(head===d.candidate_sha) throw new Error('ledger commit missing'); cp.execFileSync('git',['merge-base','--is-ancestor',d.candidate_sha,head]); for(const id of ids){const r=d.rows[id],x=[...r.required_subcases].sort(),y=[...(r.evidence?.approved_subcases||[])].sort(); if(r.status!=='PASS'||r.candidate_sha!==d.candidate_sha||r.evidence?.candidate_sha!==d.candidate_sha||r.evidence?.approved!==true||JSON.stringify(x)!==JSON.stringify(y)) throw new Error(id);} if(d.rows['RELEASE-AUTHORIZATION'].status!=='NOT_AUTHORIZED'||!/nyquist_compliant:\s*true/.test(v)||!/Approval:\s*approved/i.test(v)||!v.includes(d.candidate_sha)) throw new Error('final approval state'); for(const x of ['A-EDGE-STALE-03','A-EDGE-REL-01','NOT_AUTHORIZED']) if(!(s+v).includes(x)) throw new Error(x);"</automated>
    <automated>git diff --check &amp;&amp; node --test tests/architecture/release-publish.test.mjs &amp;&amp; npx vitest run tests/commands/release-rc.test.ts</automated>
  </verify>
  <done>Validation is either fully evidence-backed for the immutable candidate or explicitly blocked/pending; release remains unauthorized and no source or planning artifact is lost.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Human checkpoint summary → canonical receipt ledger | Unstructured or unsafe evidence must not be trusted or copied blindly. |
| Receipt statuses → validation approval | A status derivation bug could falsely close missing evidence. |
| Ledger commit → immutable candidate | Evidence bookkeeping must not silently redefine the release candidate. |
| Feature approval → release authorization | Completing validation must preserve the explicit release stop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-127-14-01 | Spoofing | Human receipt ingestion | high | mitigate | Full-SHA equality, approved signals, required-field validation, and stop on ambiguity/pending. |
| T-127-14-02 | Information Disclosure | Ledger sanitization | high | mitigate | Reject tokens, raw environment/output, absolute paths, private payloads; copy safe metadata only. |
| T-127-14-03 | Tampering | Candidate identity | high | mitigate | Candidate commit object must exist, contain exact RC version, and remain separate from ledger commits. |
| T-127-14-04 | Elevation of Privilege | Nyquist/Approval derivation | high | mitigate | Automated pending-versus-Nyquist consistency and high-threat/prohibition gates. |
| T-127-14-05 | Repudiation | Assumption/prohibition outcomes | medium | mitigate | Explicit A-EDGE outcomes and retained statements/review status in final ledger/validation. |
| T-127-14-06 | Elevation of Privilege | Release stop | high | mitigate | NOT_AUTHORIZED remains explicit; release tests prove no default side effect; no outward command in plan. |

**ASVS L1 gate:** receipt spoofing/disclosure, candidate tampering, false approval, and release-authority confusion are high severity; any failure leaves validation pending and blocks completion.
</threat_model>

<verification>
1. Canonical JSON row IDs and required_subcases are unchanged from the committed pre-reconciliation ledger; every ingested human receipt has an approved signal, exact candidate SHA, exact subcase set, safe required fields, and no prohibited data.
2. Candidate commit exists, contains exact `0.22.0-rc.1` package metadata, differs from the ledger commit, and is its ancestor.
3. Validation cannot become Nyquist-compliant or approved unless every required row is exact-SHA PASS.
4. Edge assumptions and all seven recalled prohibitions have explicit outcomes.
5. Release architecture tests remain green; release authorization remains absent; planning directories remain intact.
</verification>

<success_criteria>
- Human and deterministic evidence is reconciled mechanically for one immutable candidate.
- Missing evidence blocks rather than being waived or fabricated.
- Final validation status, assumptions, prohibitions, and provider dispositions are honest and complete.
- No tag, push, publish, GitHub Release, release-only workflow dispatch, candidate mutation, or planning-artifact deletion occurs.
</success_criteria>

## Artifacts this phase produces

- Final sanitized `127-RECEIPTS.md` for the immutable RC candidate.
- Final evidence-derived `127-VALIDATION.md`, either approved/complete or explicitly blocking/pending.

<output>
Create `.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-14-SUMMARY.md` when done.
</output>
