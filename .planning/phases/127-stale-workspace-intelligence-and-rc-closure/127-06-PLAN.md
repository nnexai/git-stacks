---
phase: 127-stale-workspace-intelligence-and-rc-closure
plan: "06"
type: execute
wave: 4
depends_on:
  - 127-02
  - 127-05
files_modified:
  - packages/client/src/stale-workspaces.ts
  - packages/client/src/shortcuts.ts
  - packages/client/src/index.ts
  - packages/tui/src/official-service.ts
  - packages/tui/src/workspace-action-inventory.ts
autonomous: true
requirements:
  - STALE-02
  - STALE-04
  - STALE-05
must_haves:
  truths:
    - "Both renderers consume one shared fixed label/order/timestamp presentation vocabulary and preserve service candidate order without local classification or scoring."
    - "Client request generations and response revisions reject older results; a conflict reloads authoritative state and retries exactly once per D-10."
    - "Per D-18, Open is coordinated as one canonical descriptor submission and navigation occurs only after authoritative success."
    - "Per D-19, Archive/Remove retain existing canonical descriptors and Force Remove cannot appear until typed dirty failure, stopped terminals, same-revision fresh inventory, force_allowed, and exact current-name confirmation."
    - "Per D-20, stale entry and view refresh use canonical shared metadata: macOS Ctrl+Command+S, Linux Ctrl+Alt+Shift+S, TUI `s`, and active-view-only `r/R` with no browser-global refresh default."
    - "Composing, Process, Dead, AltGraph, terminal/input focus, repeated keydown, and pending actions cannot leak or duplicate shortcut/action execution."
    - "The TUI calls stale service policy only through `@git-stacks/service/client` inside the official adapter; shared client code remains browser-safe."
  artifacts:
    - path: packages/client/src/stale-workspaces.ts
      provides: "Safe fixed presentation maps, exact/relative time model, request-generation gate, and one-conflict retry coordinator."
    - path: packages/client/src/shortcuts.ts
      provides: "Canonical stale entry and scoped refresh metadata across web/TUI labels."
    - path: packages/client/src/index.ts
      provides: "Browser-safe stale coordination exports."
    - path: packages/tui/src/official-service.ts
      provides: "Trusted typed stale fetch seam."
    - path: packages/tui/src/workspace-action-inventory.ts
      provides: "TUI generation/revision gate adapter consistent with existing inventory gates."
  key_links:
    - from: packages/client/src/stale-workspaces.ts
      to: packages/protocol/src/web.ts
      via: "Exhaustive finite-code presentation and strict request/response types"
      pattern: "WebStaleWorkspace"
    - from: packages/client/src/stale-workspaces.ts
      to: packages/client/src/workspace-actions.ts
      via: "Canonical one-shot action coordinator; no duplicated lifecycle policy"
      pattern: "workspace action|operation"
    - from: packages/client/src/shortcuts.ts
      to: packages/protocol/src/web.ts
      via: "Exhaustive metadata for both new action IDs"
      pattern: "workspace.stale|workspace.stale.refresh"
    - from: packages/tui/src/official-service.ts
      to: packages/service/src/policy/client.ts
      via: "Only trusted import of `fetchStaleWorkspaceEvaluation`"
      pattern: "@git-stacks/service/client"
  prohibitions:
    - statement: "Stale guidance must not state or imply that a workspace is safe to delete, assign a confidence or safety score, or pressure cleanup."
      status: unverified
      flagged: true
    - statement: "Unknown, unavailable, or failed evidence must not be hidden or converted into confirmed absence, presence, or proof of staleness."
      status: unverified
      flagged: true
    - statement: "Evaluation or refresh must not silently acquire lifecycle authority or perform automatic archive, remove, terminal, worktree, or YAML mutation."
      status: unverified
      flagged: true
---

<objective>
Implement the shared D-18 through D-20 presentation, generation, one-conflict retry, shortcut, and trusted TUI adapter seams that web and TUI must reuse before their parallel renderer plans.

Purpose: Prevent renderer-owned labels, sorting, retries, shortcut bindings, or lifecycle authority from diverging.
Output: Browser-safe shared stale helpers plus canonical shortcut metadata and official TUI service adapters.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.claude/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-CONTEXT.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-UI-SPEC.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-PATTERNS.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-02-SUMMARY.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-05-SUMMARY.md
@packages/client/src/presentation.ts
@packages/client/src/shortcuts.ts
@packages/client/src/workspace-actions.ts
@packages/client/src/index.ts
@packages/tui/src/official-service.ts
@packages/tui/src/workspace-action-inventory.ts
@tests/service/phase127-cross-client-conformance.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared stale presentation, generations, and one-conflict coordination</name>
  <read_first>
    - packages/protocol/src/web.ts
    - packages/client/src/presentation.ts
    - packages/client/src/workspace-actions.ts
    - tests/service/phase127-cross-client-conformance.test.ts
  </read_first>
  <files>packages/client/src/stale-workspaces.ts, packages/client/src/index.ts</files>
  <behavior>
    - "Every finite confirmed/unknown/caution code maps exhaustively to fixed neutral copy with no safety claim or score."
    - "Presentation exposes exact UTC plus existing relative-time output and canonical terminal-before-inactivity reason order while preserving service row order."
    - "A monotonic request token accepts only the newest response whose revision matches the expected authoritative revision."
    - "Conflict triggers authoritative reload and one retry with the new revision; a second conflict or any other error returns without a loop."
    - "Open/lifecycle coordination delegates to existing one-shot canonical action APIs rather than building descriptors locally."
  </behavior>
  <action>Create a browser-safe module using only protocol and client imports. Export exhaustive fixed label maps for confirmed reasons, unknown evidence, and cautions; a formatter that pairs validated exact UTC with the existing relative-time helper; canonical reason display ordering for a row; and count/disabled explanation helpers shared by both renderers. Add a generation gate whose `begin(expectedRevision)` returns a monotonic token and whose acceptance requires latest token plus response revision equality. Add a generic load coordinator accepting injected fetch and authoritative-reload callbacks: on the existing typed conflict outcome, reload once and retry exactly once, with each attempt carrying its own token; never loop or reuse a superseded result. Re-export through client index. Reuse existing workspace-action coordination for Open/Archive/Remove/Force; do not manufacture action descriptors, sort rows, infer evidence, or import Node/service/core APIs.</action>
  <acceptance_criteria>
    - Exhaustiveness fails compilation when a protocol evidence code lacks presentation copy.
    - Late response, wrong revision, first conflict, second conflict, abort, and normal error cases are deterministic.
    - Shared helpers preserve candidate/incomplete ordering from the response.
    - Generated labels contain no delete-safety, confidence, score, path, credential, or raw-provider wording.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/service/phase127-cross-client-conformance.test.ts -t "shared|label|timestamp|generation|conflict"</automated>
    <automated>npm run typecheck --workspace @git-stacks/client</automated>
  </verify>
  <done>One browser-safe shared module owns stale copy, timestamp presentation, reason display order, generation acceptance, and exactly-one conflict recovery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register canonical stale entry and scoped refresh shortcuts</name>
  <read_first>
    - packages/client/src/shortcuts.ts
    - packages/protocol/src/web.ts
    - tests/service/phase127-cross-client-conformance.test.ts
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-UI-SPEC.md
  </read_first>
  <files>packages/client/src/shortcuts.ts</files>
  <behavior>
    - "`workspace.stale` uses physical KeyS with Ctrl+Meta on macOS and Ctrl+Alt+Shift on Linux, plus TUI label `s`."
    - "`workspace.stale.refresh` uses view-scoped KeyR/r metadata and has no browser-global default registration."
    - "Composing, Process, Dead, or AltGraph input never matches either action."
    - "Existing shortcuts retain their defaults and exhaustive action metadata remains complete."
  </behavior>
  <action>Extend canonical shortcut metadata rather than creating renderer-local constants. Define action labels, accessibility descriptions, physical-code/modifier defaults, TUI key labels, and scope. Use the exact D-20 web entry bindings; mark refresh as `stale-view` scoped with no browser-global default while retaining `R` for the web view and `r` for TUI display/matching. Preserve existing input preprocessing for composition, dead/process keys, AltGraph, repeats, and physical codes. If the metadata type currently assumes every action is global, make scope/default explicit and update existing entries without changing their behavior. Keep matching pure and browser-safe.</action>
  <acceptance_criteria>
    - Platform entry bindings and scoped refresh behavior are asserted from shared metadata.
    - Refresh cannot appear in the browser-global shortcut dispatcher.
    - Existing shortcut tests and registry exhaustiveness remain green.
    - Neither renderer needs a copied stale shortcut label or physical-code constant.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/service/phase127-cross-client-conformance.test.ts -t "shortcut|entry|refresh"</automated>
    <automated>npm run typecheck --workspace @git-stacks/client</automated>
  </verify>
  <done>Canonical metadata supplies exact platform/TUI entry labels and a scoped refresh action without creating a browser-global `R` binding.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose the trusted TUI stale fetch and response gate</name>
  <read_first>
    - packages/tui/src/official-service.ts
    - packages/tui/src/workspace-action-inventory.ts
    - packages/service/src/policy/client.ts
    - packages/client/src/stale-workspaces.ts
    - tests/tui/dashboard/StaleWorkspaces.test.tsx
  </read_first>
  <files>packages/tui/src/official-service.ts, packages/tui/src/workspace-action-inventory.ts</files>
  <behavior>
    - "Official TUI adapter forwards strict request and AbortSignal to the service client and returns the parsed stale response."
    - "TUI gate reuses shared generation/revision semantics and rejects late or mismatched responses."
    - "Existing action-inventory generation gates and lifecycle reauthorization behavior are unchanged."
    - "No TUI file outside the official adapter imports service implementation or core/provider/Git modules."
  </behavior>
  <action>Add a typed official-service wrapper around `fetchStaleWorkspaceEvaluation` from `@git-stacks/service/client`. Add/export the TUI-facing stale request gate beside existing inventory/notes gates by composing the shared client generation/retry primitives rather than reimplementing them. Preserve all current inventory gate APIs. The adapter must pass abort and force flags, surface typed conflict to the shared retry coordinator, and return only protocol response types. Do not read files, Git, provider CLIs, or machine state from TUI.</action>
  <acceptance_criteria>
    - TUI tests can inject the official stale fetcher and deterministic gate without importing service internals.
    - Late-response/revision cases match shared-client behavior exactly.
    - Existing workspace-action-inventory tests remain green.
    - Package architecture permits service client only at the trusted official adapter seam.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/service/phase127-cross-client-conformance.test.ts -t "trusted|TUI|generation"</automated>
    <automated>npm run typecheck --workspace @git-stacks/tui</automated>
  </verify>
  <done>The TUI has one official typed stale fetch seam and one shared-semantics response gate, ready for the dedicated view without authority duplication.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Protocol response → shared presentation | Finite authority becomes user-facing copy and ordering. |
| Concurrent RPC/reload → client state | Late or conflicted responses could replace newer authoritative state. |
| Keyboard events → action IDs | Input composition/focus/repeats could trigger unintended commands. |
| TUI package → trusted service client | Service access must remain isolated to the approved adapter seam. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-127-06-01 | Spoofing | Shared labels/presentation | medium | mitigate | Exhaustive fixed maps, exact UTC, neutral wording, and no local classification/score. |
| T-127-06-02 | Tampering | Generation/conflict coordinator | high | mitigate | Monotonic tokens, expected revision equality, one reload/retry, and late-response tests. |
| T-127-06-03 | Elevation of Privilege | Shared action coordination | high | mitigate | Delegate only to existing canonical one-shot descriptors; no local inventory construction. |
| T-127-06-04 | Spoofing / Repudiation | Shortcut matching | medium | mitigate | Physical codes, exact modifiers, composition/AltGraph guards, scope metadata, and repeat suppression. |
| T-127-06-05 | Elevation of Privilege | TUI official-service boundary | high | mitigate | Static package checks and one isolated `@git-stacks/service/client` import seam. |

**ASVS L1 gate:** generation, lifecycle authority, and trusted-client boundary threats are high severity and must be green before either renderer plan runs.
</threat_model>

<verification>
1. Shared portions of the Phase 127 conformance suite pass for labels, timestamps, generation, retry, shortcuts, and trusted adapter behavior.
2. Client and TUI package typechecks pass.
3. Existing workspace-action authority tests remain green.
4. `npm run test:architecture` confirms client is browser-safe and service access is confined to the trusted TUI adapter.
</verification>

<success_criteria>
- Web and TUI have no reason to duplicate stale wording, ordering, generations, conflict retry, or shortcut metadata.
- The exact platform and TUI bindings are canonical, with refresh scoped to the active view.
- Open and lifecycle execution continues through existing one-shot canonical authority.
- Trusted service access remains isolated from browser-safe client and renderer packages.
</success_criteria>

## Artifacts this phase produces

- Shared stale presentation and request-generation/retry module.
- Canonical stale entry and scoped refresh shortcut metadata.
- Official trusted TUI stale service adapter and gate.

<output>
Create `.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-06-SUMMARY.md` when done.
</output>
