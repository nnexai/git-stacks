---
phase: 127-stale-workspace-intelligence-and-rc-closure
plan: "09"
type: execute
wave: 6
depends_on:
  - 127-07
  - 127-08
files_modified: []
autonomous: true
requirements:
  - STALE-01
  - STALE-02
  - STALE-03
  - STALE-04
  - STALE-05
  - REL-01
must_haves:
  truths:
    - "The same strict fixture produces identical candidate/incomplete order, fixed labels, reason order, exact/relative timestamps, unknown/caution split, counts, and disabled explanations in web and TUI."
    - "One confirmed reason qualifies, confirmed-plus-unknown remains a candidate, unknown-only is incomplete, and no renderer can change that outcome."
    - "All five confirmed reasons and all provider/branch/worktree/activity failure modes remain distinguishable and sanitized through real adapters."
    - "Open and lifecycle intent submit once; pending repeats do not resubmit; concurrent actions remain revision/lease bound; reconciliation does not replay mutation."
    - "Revision mismatch causes zero probes, one authoritative reload, and at most one retry; service/cache/client generations reject late overwrites."
    - "Architecture proves web/TUI cannot import or perform provider, Git, filesystem, classification, ranking, lifecycle authorization, or release operations."
    - "Browser/TUI output excludes machine paths, credentials, raw environment, bearer material, argv, stdout, stderr, and unapproved launch context."
    - "A-EDGE-STALE-03 is not treated as closed unless hostile no-mutation capability tests are green."
    - "Per D-22, deterministic closure is recorded only as deterministic evidence and does not claim hosted, authenticated, physical, screenshot, interactive, or human approval."
    - "No validation command tags, pushes, publishes, creates a release, dispatches release-only workflows, or deletes any phase-planning directory."
  artifacts:
    - path: tests/service/phase127-cross-client-conformance.test.ts
      provides: "Green real-adapter parity and lifecycle/reconciliation matrix."
    - path: tests/architecture/phase127-stale-authority.test.mjs
      provides: "Green authority, disclosure, import, CLI, and no-mutation boundaries."
    - path: .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-09-SUMMARY.md
      provides: "Deterministic command receipt and explicit separation from external evidence."
  key_links:
    - from: tests/service/phase127-cross-client-conformance.test.ts
      to: packages/client/src/stale-workspaces.ts
      via: "One shared response and real renderer adapters"
      pattern: "candidate|incomplete|generation|operation"
    - from: tests/architecture/phase127-stale-authority.test.mjs
      to: packages/web/src
      via: "Browser-safe dependency and disclosure scan"
      pattern: "@git-stacks/client|@git-stacks/protocol"
    - from: tests/architecture/phase127-stale-authority.test.mjs
      to: packages/service/src/policy/stale-workspace-evaluator.ts
      via: "Read-only capability graph and mutation sentinels"
      pattern: "evaluate|refresh"
  assumptions:
    - statement: "A-EDGE-STALE-03: the unclassified suggestion-only edge is considered deterministically closed only if no-mutation runtime and architecture proofs both pass; keep flagged in the plan and report the actual result in the summary."
      status: unresolved
      flagged: true
  prohibitions:
    - statement: "Stale guidance must not state or imply that a workspace is safe to delete, assign a confidence or safety score, or pressure cleanup."
      status: unverified
      flagged: true
    - statement: "Unknown, unavailable, or failed evidence must not be hidden or converted into confirmed absence, presence, or proof of staleness."
      status: unverified
      flagged: true
    - statement: "Repository-scoped branch or worktree evidence must not be generalized into a workspace-wide safety claim."
      status: unverified
      flagged: true
    - statement: "Evaluation or refresh must not silently acquire lifecycle authority or perform automatic archive, remove, terminal, worktree, or YAML mutation."
      status: unverified
      flagged: true
    - statement: "Provider status must not be inferred from branch names or remotes or presented as authenticated fact without validated persisted provenance."
      status: unverified
      flagged: true
    - statement: "Deterministic or local evidence must not be represented as hosted, authenticated, physical, screenshot, interactive, human approval, or release authorization."
      status: unverified
      flagged: true
---

<objective>
Close the deterministic stale-workspace product boundary across protocol, core, service, shared client, web, TUI, architecture, security, and package builds before release metadata changes.

Purpose: Prove the complete advisory feature and ASVS L1 mitigations as one system while keeping deterministic results separate from external/human evidence.
Output: Green focused, conformance, architecture, build, type, full-test, and coverage gates with an honest summary receipt.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.claude/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-CONTEXT.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-COVERAGE.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-03-SUMMARY.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-04-SUMMARY.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-05-SUMMARY.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-06-SUMMARY.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-07-SUMMARY.md
@.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-08-SUMMARY.md
@tests/lib/core/forge-change-status.test.ts
@tests/lib/service/stale-workspaces.test.ts
@tests/service/web-stale-workspaces.test.ts
@tests/service/phase127-cross-client-conformance.test.ts
@tests/architecture/phase127-stale-authority.test.mjs
@tests/tui/dashboard/StaleWorkspaces.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the focused evidence, renderer, and cross-client matrix</name>
  <read_first>
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md
    - tests/lib/core/forge-change-status.test.ts
    - tests/lib/service/stale-workspaces.test.ts
    - tests/service/web-stale-workspaces.test.ts
    - tests/service/phase127-cross-client-conformance.test.ts
    - tests/tui/dashboard/StaleWorkspaces.test.tsx
  </read_first>
  <files>None — verification only</files>
  <action>Run every Phase 127 focused Node/Vitest suite together under the file keystore, then run the isolated OpenTUI launcher. Confirm all 12 covered edge predicates, all eight UI considerations, GitHub/GitLab/Gitea dispositions, cache/revision races, disclosure canaries, one-shot actions, responsive web behavior, exact TUI tiers/keys, and real-adapter parity. Use non-watch `npx vitest run`; use only `npm run test:tui` for OpenTUI. If a test fails, stop this plan and report the exact gap for implementation repair rather than weakening tests, changing locked decisions, or marking the assumption/evidence passed.</action>
  <acceptance_criteria>
    - Core, service, web, and conformance focused suites all pass in one run.
    - Isolated TUI suite passes without direct combined Bun execution.
    - No skipped/only/todo marker hides a required Phase 127 matrix row.
    - A-EDGE-STALE-03 result is reported honestly from the no-mutation assertions.
  </acceptance_criteria>
  <verify>
    <automated>GIT_STACKS_KEY_STORE=file npx vitest run tests/lib/core/forge-change-status.test.ts tests/lib/service/stale-workspaces.test.ts tests/service/web-stale-workspaces.test.ts tests/service/phase127-cross-client-conformance.test.ts</automated>
    <automated>npm run test:tui</automated>
  </verify>
  <done>All focused policy/provider/web/TUI/conformance behavior is green with no required case skipped or reclassified.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce architecture, ASVS, package, build, and type boundaries</name>
  <read_first>
    - tests/architecture/phase127-stale-authority.test.mjs
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-COVERAGE.md
    - CLAUDE.md
  </read_first>
  <files>None — verification only</files>
  <action>Run the hostile Phase 127 authority test, standard architecture/dependency gates, all package typechecks/builds, web build, and optional TUI build. Confirm protocol/browser packages are platform-safe; web has no core/service/Node imports; TUI service access stays at the official adapter; the evaluator capability graph is read-only; the CLI remains daemonless and has no stale command; default installation excludes TUI/Bun/OpenTUI; and no browser DTO exposes secret/path/raw process data. Treat every high-severity ASVS threat as blocking and every medium threat on a primary boundary as requiring mitigation. Do not waive or transfer a failure during execution.</action>
  <acceptance_criteria>
    - Phase-specific and repository architecture/dependency tests pass.
    - All workspace typechecks and package builds pass.
    - Web and TUI builds pass in their supported toolchains.
    - No high-severity threat remains open or accepted.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/architecture/phase127-stale-authority.test.mjs &amp;&amp; npm run build:packages &amp;&amp; npm run typecheck &amp;&amp; npm run test:architecture &amp;&amp; npm run test:deps &amp;&amp; npm run web:build &amp;&amp; npm run tui:build</automated>
  </verify>
  <done>All package, type, build, architecture, dependency, disclosure, and ASVS L1 gates are green with no authority leak.</done>
</task>

<task type="auto">
  <name>Task 3: Run the complete local test and coverage regression boundary</name>
  <read_first>
    - CLAUDE.md
    - package.json
    - .planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-VALIDATION.md
  </read_first>
  <files>None — verification only</files>
  <action>Run the complete repository test command and V8 coverage after focused/architecture closure. Keep Bun absent from Node production behavior paths; allow the repository's optional TUI launcher to invoke Bun only for isolated OpenTUI tests. Confirm no test touches real IDEs, provider accounts, user workspace YAML, credentials, or release hosts. Record exact commands/results in the plan summary as deterministic local evidence only. Do not run `release:check` until the RC metadata/docs plans, and do not perform any tag, push, publish, release, workflow dispatch, or planning-directory cleanup.</action>
  <acceptance_criteria>
    - `npm test` and `npm run coverage` pass.
    - Node production/conformance tests do not require Bun in PATH.
    - Test execution has no external provider/release side effect.
    - Summary labels these receipts deterministic/local and lists external evidence as not yet performed.
  </acceptance_criteria>
  <verify>
    <automated>npm test &amp;&amp; npm run coverage</automated>
  </verify>
  <done>The complete local regression and coverage boundary is green and recorded without overstating external evidence or altering release state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test fixtures → product assurance | Deterministic fixtures can prove code behavior but not live environment behavior. |
| Package graph → platform runtimes | Browser/Node/Bun boundaries must remain enforceable after integration. |
| Validation commands → developer machine/release state | Tests must not access real user/provider data or create release side effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-127-09-01 | Spoofing | Cross-client conformance | high | mitigate | One fixture through real adapters, shared labels, exact order/times, and no duplicated expected policy. |
| T-127-09-02 | Elevation of Privilege | Package/import architecture | high | mitigate | Hostile static tests reject client probes, policy, mutation authority, and browser service/core imports. |
| T-127-09-03 | Information Disclosure | Built browser/TUI artifacts | high | mitigate | Schema/canary tests plus build/package scans for path/secret/raw process fields. |
| T-127-09-04 | Tampering | Skipped or weakened tests | high | mitigate | Explicit complete command set; failures stop execution and cannot be waived in this plan. |
| T-127-09-05 | Repudiation | Deterministic versus external evidence | medium | mitigate | Summary records command class and explicitly leaves hosted/manual classes unclaimed. |
| T-127-09-06 | Elevation of Privilege | Validation side effects | high | mitigate | Local test/build commands only; release validation and outward operations are excluded. |

**ASVS L1 gate:** every high-severity register row is blocking; this plan cannot complete while any focused, architecture, disclosure, package, or side-effect assertion fails.
</threat_model>

<verification>
1. Focused Vitest and isolated TUI suites pass.
2. Phase-specific plus repository architecture/dependency, build, and type gates pass.
3. Full repository tests and V8 coverage pass.
4. The summary separates deterministic receipts from every pending hosted/authenticated/physical/visual/interactive/human class.
</verification>

<success_criteria>
- The advisory stale feature is deterministically green across every package and both clients.
- A-EDGE-STALE-03 is resolved only by actual green no-mutation evidence, otherwise reported as a blocking gap.
- No high ASVS threat, package-boundary violation, disclosure, or regression remains.
- No release side effect or external/manual evidence claim occurs.
</success_criteria>

## Artifacts this phase produces

- Green Phase 127 focused, cross-client, architecture, package, build, type, full-test, and coverage receipts in `127-09-SUMMARY.md`.
- No production file changes; failures route back to the owning implementation plan.

<output>
Create `.planning/phases/127-stale-workspace-intelligence-and-rc-closure/127-09-SUMMARY.md` when done.
</output>
