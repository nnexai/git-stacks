---
phase: 101-completion-completeness-repair
plan: 02
type: execute
wave: 2
depends_on: ["101-01"]
files_modified:
  - src/lib/completion-audit.ts
  - scripts/verify-gates.ts
  - tests/lib/completion-generator.test.ts
  - tests/commands/support-readonly.test.ts
  - tests/lib/verify-gates.test.ts
  - tests/lib/verify.test.ts
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
requirements_addressed:
  - COMP-01
  - COMP-02
  - COMP-03
must_haves:
  truths:
    - "D-08/D-09: Bash, zsh, and fish completion coverage is compared against a machine-readable live command inventory so new commands cannot silently ship uncovered."
    - "D-10: Focused shell-specific assertions still protect prior completion fixes for no repeated satisfied positionals, no parent-flag leakage, scoped enum choices, and optional positional handling."
    - "D-03: The regression surface explicitly includes `files`, `command`, `notes`, forge/source-adjacent workspace paths, support commands, and existing workspace/template/repo/integration surfaces."
    - "D-04: The gate only enforces completion coverage and release-path integrity; it does not add new CLI behavior or CI."
  artifacts:
    - path: "src/lib/completion-audit.ts"
      provides: "Shell-specific live-inventory audit helper for bash, zsh, and fish"
      contains: "auditCompletionCoverage"
    - path: "scripts/verify-gates.ts"
      provides: "Release gate that fails when completion output drifts from the live command tree"
      contains: "completionCoverage"
    - path: "tests/commands/support-readonly.test.ts"
      provides: "Public CLI smoke checks for current completion markers in all three shells"
      contains: "completion bash"
    - path: "tests/lib/verify-gates.test.ts"
      provides: "Synthetic verify:gates coverage for completion drift aggregation"
      contains: "completion drift"
  key_links:
    - from: "src/lib/completion-audit.ts"
      to: "src/lib/cli-program.ts"
      via: "Audit helper checks generated bash/zsh/fish output against the shared live command-path inventory"
      pattern: "collectCommandPaths"
    - from: "scripts/verify-gates.ts"
      to: "src/lib/completion-audit.ts"
      via: "verify:gates consumes the shell audit report and fails on missing paths"
      pattern: "auditCompletionCoverage"
    - from: "tests/lib/verify.test.ts"
      to: "scripts/verify.ts"
      via: "Verify workflow order keeps `bun run verify:gates` in the existing release path before `bun run test`"
      pattern: "getVerifyCommands"
---

<objective>
Add shell-specific regression coverage and wire completion drift into the existing local release gate.

Purpose: This plan turns the repaired completion surface into a durable contract. Bash, zsh, and fish each get explicit coverage, and `bun run verify:gates` becomes the enforcement point already reached by `bun run verify`.

Output: shell audit helper, expanded completion tests and CLI smoke checks, and verify-gates coverage that fails on completion drift.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/101-completion-completeness-repair/101-CONTEXT.md
@.planning/phases/101-completion-completeness-repair/101-RESEARCH.md
@.planning/phases/101-completion-completeness-repair/101-PATTERNS.md
@.planning/phases/101-completion-completeness-repair/101-VALIDATION.md
@.planning/milestones/v0.9.0-phases/34-completion-audit-forge-issue-coverage/34-02-SUMMARY.md
@.planning/milestones/v0.9.0-phases/35-dynamic-name-completion/35-01-SUMMARY.md

@src/lib/cli-program.ts
@src/lib/completion-generator.ts
@scripts/verify-gates.ts
@scripts/verify.ts
@tests/lib/completion-generator.test.ts
@tests/commands/support-readonly.test.ts
@tests/lib/verify-gates.test.ts
@tests/lib/verify.test.ts

<interfaces>
From scripts/verify-gates.ts:
`export function collectVerifyGateReport(options?: CollectOptions): VerifyGateReport`
`export function formatVerifyGateReport(report: VerifyGateReport): string`

From scripts/verify.ts:
`export function getVerifyCommands(): string[]`
`export async function runVerifyWorkflow(options?: VerifyWorkflowOptions): Promise<number>`

From src/lib/completion-generator.ts:
`export function generateBash(program: Command): string`
`export function generateZsh(program: Command): string`
`export function generateFish(program: Command): string`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shell-specific completion audit helpers and expand regression tests</name>
  <files>src/lib/completion-audit.ts, tests/lib/completion-generator.test.ts, tests/commands/support-readonly.test.ts</files>
  <read_first>
    - src/lib/cli-program.ts
    - src/lib/completion-generator.ts
    - tests/lib/completion-generator.test.ts
    - tests/commands/support-readonly.test.ts
    - src/commands/workspace.ts
    - src/commands/files.ts
    - src/commands/command.ts
    - src/commands/notes.ts
    - src/commands/integration.ts
  </read_first>
  <action>
    Create `src/lib/completion-audit.ts` exporting `auditCompletionCoverage(program: Command)` plus shell-specific helpers that compare live paths from `collectCommandPaths()` against `generateBash()`, `generateZsh()`, and `generateFish()` output. Encode the checks around the current emitter structures instead of full-shell snapshots: bash top-level `compgen -W` and nested `case` bodies, zsh `_top_commands` plus recursive helper functions, and fish `complete -c` directives plus `__fish_seen_subcommand_from` chains. Extend `tests/lib/completion-generator.test.ts` to use these helpers for current top-level and nested paths, and keep explicit D-05/D-06 assertions for repeated-satisfied positional suppression, parent-flag leakage, enum scoping, optional positional handling, and `command run [workspaceOrCommand] [commandMaybe]` boundary behavior. Update `tests/commands/support-readonly.test.ts` so public `git-stacks completion bash|zsh|fish` output must expose current markers `command`, `notes`, `files`, `env`, `paths`, `integration`, and `message`. Do not introduce golden snapshot files or new CLI semantics.
  </action>
  <acceptance_criteria>
    - `src/lib/completion-audit.ts` can report missing live command paths separately for bash, zsh, and fish.
    - Real-program completion tests fail if any live top-level command or expected nested leaf from the shared inventory is absent from one of the three shell outputs.
    - Public CLI completion smoke checks include `command`, `notes`, `files`, `env`, `paths`, `integration`, and `message` markers for bash, zsh, and fish.
    - No full bash/zsh/fish snapshots are added; the tests stay invariant-based and shell-specific.
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- tests/lib/completion-generator.test.ts tests/commands/support-readonly.test.ts</automated>
  </verify>
  <done>
    Bash, zsh, and fish each have explicit automated coverage against the live command surface plus focused dynamic regression assertions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fail verify:gates on completion drift and preserve release-path ordering</name>
  <files>scripts/verify-gates.ts, tests/lib/verify-gates.test.ts, tests/lib/verify.test.ts</files>
  <read_first>
    - scripts/verify-gates.ts
    - scripts/verify.ts
    - tests/lib/verify-gates.test.ts
    - tests/lib/verify.test.ts
    - src/lib/completion-audit.ts
    - src/lib/cli-program.ts
    - .planning/phases/101-completion-completeness-repair/101-VALIDATION.md
  </read_first>
  <action>
    Extend `scripts/verify-gates.ts` with a completion-specific report section sourced from `auditCompletionCoverage(buildCliProgram())`. The gate must fail when bash, zsh, or fish omits live command paths from the shared inventory, and the formatted error output must list the missing paths per shell in the same run as any existing inventory, coverage, or functional-readiness failures. Update `tests/lib/verify-gates.test.ts` with synthetic completion-drift cases that prove completion failures aggregate with existing gate findings. Update `tests/lib/verify.test.ts` only as needed to keep the release path explicit: `bun run verify` still invokes `bun run verify:gates` before `bun run test`, and package script names remain `bun run test`, `bun run verify:gates`, and `bun run verify`. Do not add CI wiring or new top-level package scripts.
  </action>
  <acceptance_criteria>
    - `collectVerifyGateReport()` exposes a completion-drift result that can fail the gate independently of coverage or inventory drift.
    - `formatVerifyGateReport()` prints missing bash, zsh, and fish paths in a diagnosable form.
    - `tests/lib/verify-gates.test.ts` covers both passing and failing completion-drift scenarios without requiring shell binaries or user config files.
    - `tests/lib/verify.test.ts` still asserts the verify order `verify:prereqs -> coverage -> verify:gates -> test -> test:deps -> typecheck`.
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- tests/lib/verify-gates.test.ts tests/lib/verify.test.ts</automated>
  </verify>
  <done>
    Completion drift is enforced through the existing local verify path, and the release gate order remains explicit and unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated shell output -> audit/parser logic | Shell-specific audit code must detect real omissions without relying on brittle snapshots or false-positive string matches. |
| Completion audit -> release gate | `verify:gates` decides whether missing completion coverage can ship. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-04 | Tampering | `src/lib/completion-audit.ts` | mitigate | Task 1 compares each shell output against the shared live inventory with shell-specific helpers, reducing false-positive path detection that could hide missing coverage. |
| T-101-05 | Information Disclosure | completion smoke and audit tests | mitigate | Tests stay synthetic or isolated and only read generated output plus expected config-root lookup patterns; they must not require wider filesystem scans or external shell installs. |
| T-101-06 | Elevation of Privilege | `scripts/verify-gates.ts` | mitigate | Task 2 makes completion drift a blocking verify:gates condition so incomplete shell coverage cannot bypass the local release path. |
| T-101-SC | Tampering | Bun/Commander toolchain | mitigate | No new packages are introduced; verification stays on existing `bun run test`, `bun run verify:gates`, and `bun run verify` conventions. |
</threat_model>

<verification>
- `bun run test -- tests/lib/completion-generator.test.ts tests/commands/support-readonly.test.ts tests/lib/verify-gates.test.ts tests/lib/verify.test.ts`
- `bun run verify:gates`
- `bun run verify`
</verification>

<success_criteria>
- Bash, zsh, and fish completion coverage is audited against the shared live command inventory.
- Public `git-stacks completion <shell>` smoke tests cover current v0.18/v0.19 command-family markers.
- `verify:gates` fails when one shell omits live command paths, and `verify` continues to route through that gate before the main test suite.
- Focused regression assertions still cover repeated positionals, parent flag leakage, enum scoping, and optional positional handling.
</success_criteria>

<output>
Create `.planning/phases/101-completion-completeness-repair/101-02-SUMMARY.md` when done
</output>
