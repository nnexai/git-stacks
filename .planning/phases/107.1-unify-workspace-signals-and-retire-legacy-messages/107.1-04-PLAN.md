---
phase: 107.1-unify-workspace-signals-and-retire-legacy-messages
plan: "04"
type: execute
wave: 4
depends_on: ["107.1-02", "107.1-03"]
files_modified:
  - src/lib/agent-hooks/integration-manager.ts
  - src/lib/service/snapshot.ts
  - native/linux/ghostty_surface.zig
  - native/linux/app.zig
  - tests/lib/agent-hooks.test.ts
  - tests/lib/agent-hooks/signal-integration.test.ts
  - native/tests/attention_test.zig
  - native/tests/service_client_test.zig
autonomous: true
requirements: [SIG-01, SIG-04, SIG-05]
must_haves:
  truths:
    - "Provider hooks, secure OSC, app-owned ACP, and automation normalize into the same signal identity and reducer semantics."
    - "Dual hook/OSC delivery is idempotent and ACP labels appear only for client-owned ACP sessions."
  artifacts:
    - "Provider integration fixtures prove stable session and exact-surface identity."
    - "Ghostty OSC emits strict signal input rather than a parallel attention model."
  key_links:
    - "Provisioned workspace/repository/surface identity combines with provider session identity at the signal ingress edge."
---

<objective>
Finish provider and terminal ingress after the atomic publication cutover, without changing installer ownership policy or creating a second state model.

Purpose: Prove every AI-tool ingress reaches the same strict signal reducer with exact terminal identity.
Output: Provider/ACP normalization, secure OSC signal ingress, and end-to-end identity/idempotency tests.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/107.1-unify-workspace-signals-and-retire-legacy-messages/107.1-CONTEXT.md
@.planning/phases/107.1-unify-workspace-signals-and-retire-legacy-messages/107.1-02-SUMMARY.md
@.planning/phases/107.1-unify-workspace-signals-and-retire-legacy-messages/107.1-03-SUMMARY.md
@src/lib/agent-hooks/integration-manager.ts
@native/linux/ghostty_surface.zig
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize provider, owned ACP, and automation ingress</name>
  <files>src/lib/agent-hooks/integration-manager.ts, src/lib/service/snapshot.ts, tests/lib/agent-hooks.test.ts, tests/lib/agent-hooks/signal-integration.test.ts</files>
  <behavior>
    - Codex, Claude, Copilot, and OpenCode fixtures produce source/session/surface-scoped activity signals.
    - App-owned ACP updates normalize as ACP-owned; ordinary PTY commands never do.
    - Automation notifications remain discrete and completed activity remains non-unread.
  </behavior>
  <action>Complete the generic lifecycle normalization boundary after Plan 02's atomic command cutover. Preserve owned-entry merge and atomic installer behavior, the hook deadline, and unowned provider configuration byte-for-byte. Route the existing client-owned ACP seam and automation CLI through the same strict constructor; do not add session ownership or reinterpret ordinary typed commands as ACP.</action>
  <verify><automated>bun test tests/lib/agent-hooks.test.ts tests/lib/agent-hooks/signal-integration.test.ts</automated></verify>
  <done>Every supported provider, owned ACP fixture, and automation fixture constructs the same strict signal envelope with accurate ownership/source labels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize authenticated terminal OSC without duplicates</name>
  <files>native/linux/ghostty_surface.zig, native/linux/app.zig, native/tests/attention_test.zig, native/tests/service_client_test.zig</files>
  <behavior>
    - Tokenized OSC and journal delivery for one lifecycle reduce to one activity row.
    - Invalid token, lifecycle, session, provider, or exact-surface identity is safely rejected.
    - Completed OSC activity updates history without creating unread attention.
  </behavior>
  <action>Adapt the Phase 107 OSC parser/callback to construct the same source/session/surface activity identity as service events and dispatch the common signal reducer action. Preserve per-surface authentication and Ghostty ownership; parse no terminal text. Make dual ingress idempotent by deterministic identity and sequence semantics rather than presentation-layer filtering.</action>
  <verify><automated>bun run native:test:attention && bun run native:test:service-client</automated></verify>
  <done>Ordinary terminal sessions reach the unified state through authenticated OSC with exact-surface routing and no duplicate or false unread row.</done>
</task>

</tasks>

<verification>Run provider signal fixtures and native OSC/service-client verification.</verification>
<success_criteria>Provider hooks, OSC, owned ACP, and automation converge on one strict signal state model.</success_criteria>
<output>Create `.planning/phases/107.1-unify-workspace-signals-and-retire-legacy-messages/107.1-04-SUMMARY.md` when done.</output>
