# Phase 107.1 Implementation Follow-up

Implementation is already active outside this planning pass. Verify these findings against the executed code rather than blocking the active run:

- Service, journal, SSE, CLI/provider producers, discovery, native decoder/model/ABI, and projection must cross the signal boundary atomically; no committed build may leave installed hooks targeting a removed endpoint or native decoding an obsolete discovery contract.
- Use actual repository scripts: `bun run native:verify`, `bun run native:test:attention`, `bun run native:test:service-client`, `bun run native:test:service-sync`, and `bun run native:test:surface-abi` where applicable.
- Notification dismissal is authenticated, service-authoritative, replayable, and converges across native and TUI clients without native-only dismissed-ID persistence.
- Completed/idle/working activity remains history but is not unread attention unless accompanied by a discrete notification.
- TUI must consume the same service-backed signal projection, outside-update replay, and dismissal state before legacy message UI/store/socket/commands are deleted.
- README, help snapshots, completions, fixtures, and acceptance docs must remove legacy message and duplicate attention publication terminology.
- Forbidden-symbol and full production UAT gates remain release-blocking.
- External workspace deletion must remain quiet after `95ca6735`; a vanished Git worktree is expected, while a still-present corrupt repository remains an error.
