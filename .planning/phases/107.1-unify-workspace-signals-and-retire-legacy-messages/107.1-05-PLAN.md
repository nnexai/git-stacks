---
phase: 107.1-unify-workspace-signals-and-retire-legacy-messages
plan: "05"
type: execute
wave: 4
depends_on: ["107.1-03"]
files_modified:
  - src/commands/message.ts
  - src/lib/messages.ts
  - src/lib/paths.ts
  - src/tui/dashboard/hooks/useMessages.ts
  - src/tui/dashboard/hooks/useSignals.ts
  - src/tui/dashboard/App.tsx
  - src/tui/dashboard/WorkspaceList.tsx
  - src/tui/dashboard/WorkspaceRow.tsx
  - src/tui/dashboard/WorkspaceDetail.tsx
  - src/tui/dashboard/MessageOverlay.tsx
  - src/tui/dashboard/SignalOverlay.tsx
  - src/tui/dashboard/run.tsx
  - src/tui/dashboard/HelpOverlay.tsx
  - tests/tui/dashboard/signal-sync.test.tsx
autonomous: true
requirements: [SIG-02, SIG-03, SIG-04, SIG-05]
must_haves:
  truths:
    - "The legacy file/socket message subsystem is deleted, while the TUI retains a useful service-derived signal projection."
    - "TUI and native clients converge on service-authoritative dismissal and outside updates through journal replay/SSE."
    - "The TUI owns no parallel persistence, polling message store, or dismissal authority."
  artifacts:
    - "useSignals consumes the authenticated service snapshot/event/replay boundary."
    - "SignalOverlay and workspace rows project unread state using the same completed/non-unread policy."
  key_links:
    - "External signal publication and dismissal refresh both TUI and native from the same ordered service stream."
---

<objective>
Retire the unused message implementation while preserving its generalized UI intent as a synchronized TUI signal client.

Purpose: Slim storage/IPC without deleting useful dashboard functionality or creating client-specific state.
Output: Service-backed TUI signal projection, cross-client dismissal convergence, and deletion of legacy message files/socket/command.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/107.1-unify-workspace-signals-and-retire-legacy-messages/107.1-CONTEXT.md
@.planning/phases/107.1-unify-workspace-signals-and-retire-legacy-messages/107.1-03-SUMMARY.md
@src/lib/messages.ts
@src/tui/dashboard/App.tsx
@src/lib/service/workspace-change-monitor.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace TUI message polling with service signal replay</name>
  <files>src/tui/dashboard/hooks/useMessages.ts, src/tui/dashboard/hooks/useSignals.ts, src/tui/dashboard/App.tsx, src/tui/dashboard/WorkspaceList.tsx, src/tui/dashboard/WorkspaceRow.tsx, src/tui/dashboard/WorkspaceDetail.tsx, src/tui/dashboard/MessageOverlay.tsx, src/tui/dashboard/SignalOverlay.tsx, src/tui/dashboard/run.tsx, src/tui/dashboard/HelpOverlay.tsx, tests/tui/dashboard/signal-sync.test.tsx</files>
  <behavior>
    - Signals published outside the TUI appear through the existing service event/replay path without restart.
    - TUI dismissal calls the authenticated service and converges in native after replay; native dismissal converges back to TUI.
    - Completed/idle/working activity stays visible as non-unread history and notifications remain independent.
    - Disconnect, sequence gap, and reconnect rebuild from authoritative service state without duplicate rows.
  </behavior>
  <action>Replace useMessages/file polling/socket handling with useSignals built on the same authenticated service discovery, snapshot, SSE sequence, and gap-rebuild behavior used by other clients. Retain a focused overlay, workspace badges, keyboard help, and dismissal action, but derive them from signal state. Reuse the service workspace-update event/replay mechanism so outside workspace and signal changes invalidate dashboard data together. Do not add a TUI journal, dismissed-id file, or local authority.</action>
  <verify><automated>bun test tests/tui/dashboard/signal-sync.test.tsx tests/tui/dashboard/WorkspaceDetail.test.tsx tests/tui/dashboard/snapshots/WorkspaceRow.snap.test.tsx tests/tui/dashboard/snapshots/HelpOverlay.snap.test.tsx && bun run typecheck</automated></verify>
  <done>The live TUI projects and dismisses signals through the service, including outside updates, replay gaps, and cross-client convergence.</done>
</task>

<task type="auto">
  <name>Task 2: Delete legacy message command, store, paths, and socket</name>
  <files>src/commands/message.ts, src/lib/messages.ts, src/lib/paths.ts</files>
  <action>After the TUI is fully service-backed, delete the message command/store files, JSONL directory, publication singleton, Unix socket client, message runtime paths, and setup hooks. Remove their imports and obsolete tests/fixtures without aliases or migration code. Preserve only the generalized user capability now implemented by signals.</action>
  <verify><automated>bun test tests/lib/paths.test.ts tests/lib/config.test.ts tests/commands/e2e-harness.test.ts && bun run typecheck</automated></verify>
  <done>No runtime path, CLI command, file store, socket, or TUI component depends on the legacy message subsystem.</done>
</task>

</tasks>

<verification>Run TUI focused tests plus cross-client event/replay tests and typecheck.</verification>
<success_criteria>The TUI remains feature-complete while all state and dismissal authority lives in the unified service signal system.</success_criteria>
<output>Create `107.1-05-SUMMARY.md` when done.</output>
