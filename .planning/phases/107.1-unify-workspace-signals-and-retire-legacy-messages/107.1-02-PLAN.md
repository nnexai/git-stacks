---
phase: 107.1-unify-workspace-signals-and-retire-legacy-messages
plan: "02"
type: execute
wave: 2
depends_on: ["107.1-01"]
files_modified:
  - tests/lib/service/event-journal.test.ts
  - tests/lib/service/event-broker.test.ts
  - tests/service/events.test.ts
  - tests/lib/agent-hooks.test.ts
  - native/tests/attention_test.zig
  - native/tests/service_client_test.zig
  - native/tests/abi_harness.c
  - scripts/verify-native.ts
autonomous: true
requirements: [SIG-01, SIG-02, SIG-04, SIG-05]
must_haves:
  truths:
    - "Failing tests define the entire service-to-native cutover before production protocols change."
    - "The test harness can prove both signal kinds, service-authoritative dismissal, replay, exact-surface focus, and completed non-unread semantics."
  artifacts:
    - "TypeScript and Zig tests fail against the old attention transport for the expected assertions."
    - "Existing focused native package scripts run the relevant Zig suites without unsupported filters."
  key_links:
    - "Plan 03 consumes these red tests as the pre-cutover safety net for its atomic migration."
---

<objective>
Establish the red pre-cutover verification boundary for the intentionally large atomic protocol migration.

Purpose: Prevent the service and native halves from being migrated in separately committed, incompatible states.
Output: Signal journal/SSE, provider command, native reducer/decoder, ABI, dismissal, and reconnect tests that are red only because production still uses the old protocol.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/107.1-unify-workspace-signals-and-retire-legacy-messages/107.1-CONTEXT.md
@.planning/phases/107.1-unify-workspace-signals-and-retire-legacy-messages/107.1-01-SUMMARY.md
@src/lib/service/event-journal.ts
@native/linux/service_client.zig
@scripts/verify-native.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add red service, producer, replay, and dismissal contract tests</name>
  <files>tests/lib/service/event-journal.test.ts, tests/lib/service/event-broker.test.ts, tests/service/events.test.ts, tests/lib/agent-hooks.test.ts</files>
  <behavior>
    - Activity and notification publish through one authenticated route and one journal/SSE event.
    - Dismissal is journaled, replayed, and converges after reconnect and gap rebuild.
    - Owned provider commands target only signal publication with stable session/surface identity.
    - Completed activity is retained but never unread and cannot dismiss a notification.
  </behavior>
  <action>Add migration-facing tests without changing production. Record the expected old-protocol failures in the summary so Plan 03 can distinguish intended red assertions from unrelated regressions. Cover authentication, ordering, idempotency, backpressure, discovery capacities, producer command generation, and two-client dismissal replay.</action>
  <verify><automated>bun test tests/lib/service/event-journal.test.ts tests/lib/service/event-broker.test.ts tests/service/events.test.ts tests/lib/agent-hooks.test.ts</automated></verify>
  <done>The focused TypeScript suite contains explicit red assertions for every service/producer side of the atomic cutover.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add red native decoder, model, ABI, and projection tests</name>
  <files>native/tests/attention_test.zig, native/tests/service_client_test.zig, native/tests/abi_harness.c, scripts/verify-native.ts</files>
  <behavior>
    - Native accepts strict signal and dismissal events and rejects legacy transport variants.
    - Activity coalesces by source/session/surface and notifications remain independent.
    - Reconnect rebuild converges dismissal; exact-surface focus and overflow remain safe.
  </behavior>
  <action>Add signal fixtures and harness cases while leaving production native code unchanged. Wire them into the existing `native:test:attention`, `native:test:service-client`, and `native:test:service-sync` script paths. Do not invent a generic native verifier or pass unsupported filter arguments.</action>
  <verify><automated>bun run native:test:attention && bun run native:test:service-client && bun run native:test:service-sync</automated></verify>
  <done>The focused native commands exercise the complete consumer side and fail only at old-protocol expectations before Plan 03.</done>
</task>

</tasks>

<verification>Capture intended red output and confirm unrelated baseline tests remain green.</verification>
<success_criteria>Plan 03 begins with executable tests spanning both sides of the indivisible cutover.</success_criteria>
<output>Create `107.1-02-SUMMARY.md` with the exact expected-red inventory.</output>
