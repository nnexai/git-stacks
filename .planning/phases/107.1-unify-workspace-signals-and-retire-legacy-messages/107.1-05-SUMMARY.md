---
phase: 107.1-unify-workspace-signals-and-retire-legacy-messages
plan: "05"
status: complete
---

# Plan 05 Summary

Replaced TUI message storage and socket polling with the authenticated service signal projection and dismissal mutation. Workspace rows/details and the signal overlay now share activity/notification semantics. Deleted the message command, filesystem store, paths constant, Unix socket server/state, polling hook, overlay, utilities, tests, and CLI registration with no compatibility path.

Verification: dashboard tests and snapshots, service transport tests, completion coverage, typecheck, and the forbidden-symbol audit pass.
