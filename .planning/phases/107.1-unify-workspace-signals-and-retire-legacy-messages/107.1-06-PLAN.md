---
phase: 107.1-unify-workspace-signals-and-retire-legacy-messages
plan: "06"
type: execute
wave: 5
depends_on: ["107.1-03", "107.1-04", "107.1-05"]
files_modified:
  - README.md
  - docs/native-workspace-acceptance.md
  - src/lib/completion-generator.ts
  - tests/lib/completion-generator.test.ts
  - tests/e2e-inventory.ts
  - tests/helpers.ts
  - tests/lib/coverage-surface.test.ts
  - tests/lib/coverage-runner.test.ts
  - tests/lib/verify-gates.test.ts
  - scripts/verify-gates.ts
  - scripts/verify-native.ts
autonomous: true
requirements: [SIG-03, SIG-05]
must_haves:
  truths:
    - "README, docs, help, completion, fixtures, and inventories describe only the signal system."
    - "Release gates forbid legacy message and transport-attention symbols in live source while allowing presentation-only attention wording."
  artifacts:
    - "User documentation explains signal publish/dismiss semantics and non-unread completed activity."
    - "Release verification contains path-aware forbidden-symbol and full behavior gates."
  key_links:
    - "Generated completion/help inventory is derived from the live Commander tree after obsolete commands are deleted."
---

<objective>
Finish user-facing cleanup and make protocol removal mechanically release-blocking.

Purpose: Prevent stale docs/completions and prevent either deleted transport from returning.
Output: Updated docs/help/completion fixtures, cleaned inventories, and precise hardening gates.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/107.1-unify-workspace-signals-and-retire-legacy-messages/107.1-CONTEXT.md
@.planning/phases/107.1-unify-workspace-signals-and-retire-legacy-messages/107.1-04-SUMMARY.md
@.planning/phases/107.1-unify-workspace-signals-and-retire-legacy-messages/107.1-05-SUMMARY.md
@README.md
@scripts/verify-gates.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean README, documentation, help, completion, and inventories</name>
  <files>README.md, docs/native-workspace-acceptance.md, src/lib/completion-generator.ts, tests/lib/completion-generator.test.ts, tests/e2e-inventory.ts, tests/helpers.ts, tests/lib/coverage-surface.test.ts, tests/lib/coverage-runner.test.ts</files>
  <action>Replace the README message section and any native acceptance references with concise signal publication, activity/notification, service-authoritative dismissal, completed-history, provider-ingress, and scope semantics. Remove message commands/shortcuts/paths from help, generated shell completion expectations, e2e inventories, helper mocks, and coverage argument fixtures. Document the live `service signal publish` and notification dismissal commands exactly; retain no compatibility examples.</action>
  <verify><automated>bun test tests/lib/completion-generator.test.ts tests/lib/coverage-surface.test.ts tests/lib/coverage-runner.test.ts tests/e2e-inventory.ts && bun run typecheck</automated></verify>
  <done>All user-facing and generated command surfaces describe only signals and contain no obsolete message or attention publication instructions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add forbidden-symbol and complete migration gates</name>
  <files>scripts/verify-gates.ts, scripts/verify-native.ts, tests/lib/verify-gates.test.ts</files>
  <behavior>
    - Live production source cannot contain the removed message command/store/path/socket or attention endpoint/schema/journal/action names.
    - Presentation-only attention names in native/UI paths remain allowed by a narrow path/identifier allowlist.
    - Signal schema, dismissal replay, hooks, OSC, ACP, automation, reconnect, ordering, capacity, and ABI goldens are release-blocking.
  </behavior>
  <action>Add path-aware removal audits that exclude historical planning artifacts and avoid matching generic English words. Explicitly enumerate removed domain symbols and route/command literals, with narrow presentation-only attention allowances. Wire all focused TypeScript/Zig migration tests and ABI fixtures into native/release gates and delete stale allowlists or inventories rather than masking failures.</action>
  <verify><automated>bun test tests/lib/verify-gates.test.ts && bun run native:verify && bun run verify:gates</automated></verify>
  <done>Release gates fail on reintroduced legacy transport/storage symbols and require every unified-signal behavior.</done>
</task>

</tasks>

<verification>Run focused docs/completion/inventory tests, typecheck, native verification, and release gates.</verification>
<success_criteria>Documentation and generated CLI surfaces are current, and obsolete protocols cannot return unnoticed.</success_criteria>
<output>Create `.planning/phases/107.1-unify-workspace-signals-and-retire-legacy-messages/107.1-06-SUMMARY.md` when done.</output>
