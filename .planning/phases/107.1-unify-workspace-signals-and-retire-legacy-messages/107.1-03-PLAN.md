---
phase: 107.1-unify-workspace-signals-and-retire-legacy-messages
plan: "03"
type: execute
wave: 3
depends_on: ["107.1-01", "107.1-02"]
files_modified:
  - src/lib/service/event-journal.ts
  - src/service/server.ts
  - src/service/main.ts
  - src/commands/service.ts
  - src/lib/cli-program.ts
  - src/lib/agent-hooks/types.ts
  - src/lib/agent-hooks/native-session.ts
  - src/lib/agent-hooks/codex.ts
  - src/lib/agent-hooks/claude-code.ts
  - src/lib/agent-hooks/copilot.ts
  - native/core/model.zig
  - native/core/reducer.zig
  - native/core/abi.zig
  - native/linux/service_client.zig
  - native/linux/attention_view.zig
  - native/linux/app_graph.zig
  - native/linux/app.zig
  - native/tests/fixtures/abi-v1-signal-activity.json
  - native/tests/fixtures/abi-v1-signal-notification.json
autonomous: true
requirements: [SIG-01, SIG-02, SIG-03, SIG-04, SIG-05]
must_haves:
  truths:
    - "One commit atomically switches service, journal, SSE, CLI, installed producers, native discovery, decoder, model, reducer, ABI, and projection to signals."
    - "No committed state contains dual protocols or a signal producer paired with an attention-only consumer."
    - "Dismissal is service-authoritative and completed activity is non-unread in every client model."
  artifacts:
    - "The sole publication/event/action contract is signal plus journaled notification dismissal."
    - "Native signal ABI fixtures cover both kinds and the legacy attention fixture is absent."
  key_links:
    - "The discovery capability and byte capacities emitted by the service exactly match native parsing/storage."
---

<objective>
Perform the service-through-native protocol migration as one explicitly atomic task and commit boundary.

Purpose: A normal file-count split would create a broken intermediate release, so compatibility safety overrides the usual plan-size heuristic here.
Output: One signal protocol across every producer, transport, consumer, reducer, ABI, and Linux projection, with no dual or incompatible committed state.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/107.1-unify-workspace-signals-and-retire-legacy-messages/107.1-CONTEXT.md
@.planning/phases/107.1-unify-workspace-signals-and-retire-legacy-messages/107.1-01-SUMMARY.md
@.planning/phases/107.1-unify-workspace-signals-and-retire-legacy-messages/107.1-02-SUMMARY.md
@src/lib/service/event-journal.ts
@src/service/server.ts
@native/core/abi.zig
@native/linux/service_client.zig
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atomically cut every producer, transport, and consumer to signals</name>
  <files>src/lib/service/event-journal.ts, src/service/server.ts, src/service/main.ts, src/commands/service.ts, src/lib/cli-program.ts, src/lib/agent-hooks/types.ts, src/lib/agent-hooks/native-session.ts, src/lib/agent-hooks/codex.ts, src/lib/agent-hooks/claude-code.ts, src/lib/agent-hooks/copilot.ts, native/core/model.zig, native/core/reducer.zig, native/core/abi.zig, native/linux/service_client.zig, native/linux/attention_view.zig, native/linux/app_graph.zig, native/linux/app.zig, native/tests/fixtures/abi-v1-signal-activity.json, native/tests/fixtures/abi-v1-signal-notification.json</files>
  <behavior>
    - One authenticated signal publication route journals and streams strict activity/notification records.
    - One authenticated dismissal mutation journals and replays cross-client state in sequence.
    - CLI automation and every owned provider installation target the new live command at the same boundary.
    - Native discovery, SSE decoder, action ABI, state model, reducer, and GTK projection consume the exact advertised contract.
    - Activity coalesces by source/session/surface; completed/idle/working are non-unread; notifications remain independently dismissible.
    - Legacy attention publication/event/action/fixture variants are absent when the task commits.
  </behavior>
  <action>First rerun Plan 02's expected-red commands and verify every failure matches its summary. Then migrate service publication, journal/replay, broker/SSE, capabilities/capacities, CLI, all owned provider command builders, native discovery, fixed-capacity storage, decoder, reducer action, ABI, app graph, and Linux attention projection together. Use one task commit only after all focused TypeScript and native suites are green. Do not create checkpoint commits during this task, do not retain compatibility unions, and do not expose both routes. This deliberately exceeds the normal file-scope heuristic because any smaller committed split would install producers that consumers cannot decode or remove a route still used by installed hooks.</action>
  <verify><automated>bun test tests/lib/service/contract.test.ts tests/lib/service/event-journal.test.ts tests/lib/service/event-broker.test.ts tests/service/events.test.ts tests/lib/agent-hooks.test.ts && bun run native:test:attention && bun run native:test:service-client && bun run native:test:service-sync && bun run native:test:surface-abi && bun run typecheck</automated></verify>
  <done>A single reviewed commit moves the complete live graph to signals; all pre-cutover red tests are green and repository HEAD has neither dual protocol nor incompatible producer/consumer state.</done>
</task>

</tasks>

<verification>Run focused pre/post suites, inspect the single task commit diff, and search live source for old publication/event/action symbols.</verification>
<success_criteria>The committed product has exactly one compatible end-to-end signal protocol.</success_criteria>
<output>Create `107.1-03-SUMMARY.md` documenting the atomicity exception, pre/post test output, and one commit hash.</output>
