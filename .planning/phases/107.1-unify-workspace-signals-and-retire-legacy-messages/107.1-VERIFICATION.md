---
phase: 107.1-unify-workspace-signals-and-retire-legacy-messages
status: complete
automated_verified: 2026-07-13
---

# Phase 107.1 Verification

## Automated result

All release-blocking automated commands pass on the final implementation:

- `bun run test`: unit PASS; integration 83/83 files PASS.
- `bun run typecheck`: PASS.
- `bun run test:deps`: 133 files processed; no circular dependency.
- `bun run native:verify`: production graph, ABI, Zig model, Ghostty seams, stress, accessibility templates, GTK/terminal/multisurface smoke, and real workspace lifecycle smoke PASS.
- `bun run verify:gates`: inventory, mapped tests, coverage artifacts, functional readiness, completion coverage, and forbidden legacy symbols PASS.
- `git diff --check`: PASS.

The production client was launched successfully against the real managed service. Its pre-cutover journal contained retired attention records; startup now removes only those records, preserves valid events, and keeps the sequence cursor monotonic. The live app is open for the blocking observations below.

Coverage artifacts were regenerated with 134 instrumented source files. The coverage run produced valid reports used by `verify:gates`; two unrelated instrumented integration cases exceeded their per-test timing budget, while the canonical non-instrumented suite passed 83/83 integration files.

## Requirement evidence

| Requirement | Source and automated evidence | Status |
| --- | --- | --- |
| SIG-01 | Strict `SignalSchema`; CLI/provider hooks; stable managed session identity; authenticated OSC; ACP/automation source labels; provider and loopback tests | Automated PASS |
| SIG-02 | `SignalState` coalesces source/session/exact-surface activity; notifications remain discrete; journal projection uses that state; dismissal is journaled and replayed | Automated PASS |
| SIG-03 | Message CLI/store/socket/TUI files deleted; old attention transport rejected; release gate finds no forbidden symbol | Automated PASS |
| SIG-04 | One journal/SSE `signal` event, discovery capability, native decoder/model/ABI actions, and TUI projection; attention remains only a UI projection | Automated PASS |
| SIG-05 | Full TS/native/release suites cover replay, gap recovery, reconnect, capacity, dismissal, provider ingress, exact identity, and native lifecycle | PASS |

## Blocking production Linux UAT

The following observations must be performed in the real production client and recorded before this phase is complete:

- [x] Two sessions from one provider remain separate and focus their exact originating terminal tabs.
- [x] Completed activity remains visible but non-unread and does not clear an unrelated notification.
- [x] Service-acknowledged notification dismissal survives reconnect/client restart and is reflected by a second consumer.
- [x] Ordinary managed PTY and owned ACP sources show accurate source and ownership labels.
- [x] Inbox keyboard/mouse navigation, waiting/failure presentation, and capacity diagnostics are accessible.
- [x] CLI help exposes signal commands and no legacy message commands.

Production Linux UAT approved by the user on 2026-07-13 after the Codex inbox UTF-8 and embedded-NUL fixes. Phase 107.1 no longer blocks downstream work.
