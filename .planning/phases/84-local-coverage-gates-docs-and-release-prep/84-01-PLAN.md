---
phase: 84-local-coverage-gates-docs-and-release-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - GATE-01
  - GATE-02
must_haves:
  truths:
    - "Phase 84 work stops before any verify/docs/release code is added when the canonical Phase 80 inventory surface is missing."
    - "Phase 84 work stops before any verify/docs/release code is added when the Phase 83 coverage command surface or stable .coverage outputs are missing."
    - "The prerequisite report tells maintainers exactly which upstream inventory or coverage surfaces are absent instead of silently absorbing Phase 80 or Phase 83 work."
  artifacts:
    - path: "tests/e2e-inventory.ts"
      provides: "Canonical machine-parseable E2E inventory required by D-09"
    - path: "tests/lib/e2e-inventory.test.ts"
      provides: "Executable validation for the canonical inventory module"
    - path: "package.json"
      provides: "Phase 83 coverage command surface"
      contains:
        - "\"coverage\""
        - "\"coverage:unit\""
        - "\"coverage:integ\""
    - path: ".coverage/coverage-final.json"
      provides: "Stable machine-readable coverage artifact required by D-10"
  key_links:
    - from: "tests/lib/e2e-inventory.test.ts"
      to: "tests/e2e-inventory.ts"
      via: "inventory validation test execution"
      pattern: "e2e-inventory"
    - from: "package.json"
      to: ".coverage/coverage-final.json"
      via: "bun run coverage"
      pattern: "\"coverage\""
---

<objective>
Establish the explicit prerequisite guard required for Phase 84 so local gate/docs/release work only starts when the canonical Phase 80 inventory surface and the Phase 83 coverage surface already exist on the branch.

Purpose: enforce the locked boundary that Phase 84 consumes upstream artifacts instead of backfilling them.
Output: a pass/fail branch-surface audit and a blocking prerequisite report with no Phase 84 implementation edits when upstream inputs are missing.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/84-local-coverage-gates-docs-and-release-prep/84-CONTEXT.md
@.planning/phases/84-local-coverage-gates-docs-and-release-prep/84-RESEARCH.md
@.planning/phases/84-local-coverage-gates-docs-and-release-prep/84-VALIDATION.md
@.planning/phases/80-e2e-cli-harness-and-living-inventory/80-CONTEXT.md
@.planning/phases/83-istanbul-based-subprocess-coverage-reporting/83-CONTEXT.md
@.planning/phases/83-istanbul-based-subprocess-coverage-reporting/83-VALIDATION.md
@package.json
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit the canonical Phase 80 inventory surface before any Phase 84 implementation work</name>
  <files>NONE — prerequisite verification only</files>
  <action>Before creating or editing any Phase 84 implementation file, verify the canonical Phase 80 inventory surface required by D-09 is already present on the branch. Check that `tests/e2e-inventory.ts` exists, `tests/lib/e2e-inventory.test.ts` exists, and the inventory test suite passes as-is. If either file is missing or the test fails, stop immediately, print a consolidated missing-surface report, and do not touch `package.json`, `scripts/`, `README.md`, or `CHANGELOG.md`. This task is a hard blocker, not a fallback implementation path.</action>
  <verify>
    <automated>test -f tests/e2e-inventory.ts &amp;&amp; test -f tests/lib/e2e-inventory.test.ts &amp;&amp; bun test tests/lib/e2e-inventory.test.ts -x</automated>
  </verify>
  <done>The executor either proves the Phase 80 inventory surface is present and green, or exits non-zero before any Phase 84 implementation files are created or edited.</done>
</task>

<task type="auto">
  <name>Task 2: Audit the Phase 83 coverage command surface and local .coverage artifact contract</name>
  <files>NONE — prerequisite verification only</files>
  <action>After Task 1 passes, verify the Phase 83 coverage surface required by D-10 is already available. Confirm `package.json` exposes `coverage`, `coverage:unit`, and `coverage:integ`; confirm `.gitignore` ignores `.coverage/`; then run the existing coverage workflow to prove it emits `.coverage/coverage-final.json`, `.coverage/coverage-summary.json`, `.coverage/lcov.info`, and `.coverage/index.html`. If the coverage scripts are absent, `.coverage/` is not the output directory, or the artifacts cannot be produced, stop and report every missing coverage prerequisite in one report. Do not add replacement coverage tooling in this phase.</action>
  <verify>
    <automated>rg -q '"coverage"' package.json &amp;&amp; rg -q '"coverage:unit"' package.json &amp;&amp; rg -q '"coverage:integ"' package.json &amp;&amp; rg -q '^\\.coverage/$' .gitignore &amp;&amp; bun run coverage &amp;&amp; test -f .coverage/coverage-final.json &amp;&amp; test -f .coverage/coverage-summary.json &amp;&amp; test -f .coverage/lcov.info &amp;&amp; test -f .coverage/index.html</automated>
  </verify>
  <done>The executor either proves the Phase 83 coverage surface and stable `.coverage/` outputs exist locally, or exits non-zero before any Phase 84 implementation files are changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prior-phase artifacts → Phase 84 executor | Phase 84 depends on inventory and coverage surfaces created elsewhere and must not assume they exist. |
| local command surface → blocking report | Failing prerequisite checks must surface complete branch-state drift without silently continuing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-01 | T | prerequisite audit | mitigate | Run explicit file-exists plus executable test/coverage commands before any Phase 84 implementation step so missing Phase 80/83 work cannot be absorbed accidentally. |
| T-84-02 | D | branch-surface guard | mitigate | Aggregate every missing inventory/coverage prerequisite into one blocking report so maintainers can fix branch drift in one pass instead of repeated reruns. |
</threat_model>

<verification>
- `test -f tests/e2e-inventory.ts && test -f tests/lib/e2e-inventory.test.ts && bun test tests/lib/e2e-inventory.test.ts -x`
- `rg -q '"coverage"' package.json && rg -q '^\.coverage/$' .gitignore`
- `bun run coverage && test -f .coverage/coverage-final.json && test -f .coverage/coverage-summary.json && test -f .coverage/lcov.info && test -f .coverage/index.html`
</verification>

<success_criteria>
- Phase 84 execution blocks immediately when canonical Phase 80 inventory inputs are missing.
- Phase 84 execution blocks immediately when Phase 83 coverage commands or `.coverage/` artifacts are missing.
- No Phase 84 implementation file is edited while prerequisite drift remains unresolved.
</success_criteria>

<output>
After completion, create `.planning/phases/84-local-coverage-gates-docs-and-release-prep/84-01-SUMMARY.md`
</output>
