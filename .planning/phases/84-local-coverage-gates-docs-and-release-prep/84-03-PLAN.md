---
phase: 84-local-coverage-gates-docs-and-release-prep
plan: 03
type: execute
wave: 3
depends_on:
  - 84-02
files_modified:
  - README.md
  - CHANGELOG.md
  - package.json
autonomous: true
requirements:
  - GATE-03
must_haves:
  truths:
    - "Maintainers can find `bun run verify`, the underlying component commands, and the local-only coverage/gate workflow in the README without a broad docs rewrite."
    - "README debug guidance shows the shipped stderr format and documents `GS_DEBUG` as primary with `GIT_STACKS_DEBUG=1` kept as a legacy alias."
    - "Release metadata is narrowly updated for the new verify/coverage/gate surface and the v0.17.1 release."
  artifacts:
    - path: "README.md"
      provides: "Focused maintainer verification and debug-format documentation"
    - path: "CHANGELOG.md"
      provides: "Focused v0.17.1 release note for verify/gates/coverage docs"
    - path: "package.json"
      provides: "v0.17.1 release version metadata"
      contains:
        - "\"version\": \"0.17.1\""
  key_links:
    - from: "README.md"
      to: "package.json"
      via: "documented verify/coverage/gate command names"
      pattern: "bun run verify"
    - from: "README.md"
      to: "src/index.ts"
      via: "GS_DEBUG primary, GIT_STACKS_DEBUG=1 legacy alias"
      pattern: "GS_DEBUG"
    - from: "README.md"
      to: "src/lib/observability.ts"
      via: "shipped key/value stderr examples"
      pattern: "op=.*module=.*msg="
---

<objective>
Apply the narrow documentation and release-prep updates for Phase 84: document the local verification path, refresh the debug-output examples to match shipped behavior, and prepare focused v0.17.1 release metadata.

Purpose: make the new local verification workflow maintainable and releasable without expanding into a broad README or testing-architecture rewrite.
Output: README verify/debug updates, a focused `CHANGELOG.md` release entry, and `package.json` version metadata for v0.17.1.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-local-coverage-gates-docs-and-release-prep/84-CONTEXT.md
@.planning/phases/84-local-coverage-gates-docs-and-release-prep/84-RESEARCH.md
@.planning/phases/84-local-coverage-gates-docs-and-release-prep/84-VALIDATION.md
@.planning/phases/79-release-prep/79-01-SUMMARY.md
@.planning/phases/75-di-seams-structured-logging/75-02-SUMMARY.md
@package.json
@README.md
@CHANGELOG.md
@src/index.ts
@src/lib/observability.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh README verify and debug guidance in place</name>
  <files>README.md</files>
  <action>Edit `README.md` surgically per D-02/D-05/D-06/D-07/D-08. Add one narrow maintainer-facing verification section that introduces `bun run verify` as the primary local workflow and lists the underlying component commands directly beneath it (`bun run verify:prereqs`, `bun run verify:gates`, `bun run coverage`, `bun run test`, `bun run test:deps`, `bun run typecheck`) so maintainers can debug individual steps. Update the existing Debug Output section in place to make `GS_DEBUG` the primary interface, keep `GIT_STACKS_DEBUG=1` as a legacy compatibility alias, and replace the old bracket/timing-only examples with examples matching the shipped stderr line shape from `src/lib/observability.ts`, including key/value fields such as `op=... module=... msg=... ms=...`. Do not rewrite unrelated README sections or broaden testing architecture prose.</action>
  <verify>
    <automated>rg -n "bun run verify|bun run verify:prereqs|bun run verify:gates|bun run coverage|GS_DEBUG|GIT_STACKS_DEBUG|op=.*module=.*msg=" README.md</automated>
  </verify>
  <done>`README.md` documents the new local verification path and the shipped debug-output format without expanding into unrelated documentation work.</done>
</task>

<task type="auto">
  <name>Task 2: Apply focused v0.17.1 release metadata updates</name>
  <files>package.json, CHANGELOG.md</files>
  <action>Update `package.json` version metadata from `0.17.0` to `0.17.1`, then add a focused `CHANGELOG.md` entry for `0.17.1` using the repo's user-facing release-prep style (Added / Changed / Internal). Limit changelog content to the new `bun run verify` workflow, the underlying verify/coverage gate commands, aggregated inventory/mapping gate behavior, local-only verification positioning, and the debug-output documentation refresh. Do not add unrelated milestone recap, CI language, or numeric coverage-threshold policy text per D-05/D-06/D-11.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const pkg=JSON.parse(fs.readFileSync('package.json','utf8')); if (pkg.version !== '0.17.1') process.exit(1)" &amp;&amp; rg -n '^## \\[0\\.17\\.1\\]' CHANGELOG.md &amp;&amp; rg -n "bun run verify|verify:gates|coverage|GS_DEBUG" CHANGELOG.md</automated>
  </verify>
  <done>The repo is release-prepped for v0.17.1 with focused version/changelog metadata that matches the new verify/coverage/docs surface and nothing broader.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped command/debug behavior → docs | README and changelog must reflect the actual local workflow and stderr format, not stale examples. |
| release metadata → published package | Version and release notes must stay tightly scoped to the shipped Phase 84 surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-06 | I | README verify/debug docs | mitigate | Derive examples and wording directly from `package.json`, `src/index.ts`, and `src/lib/observability.ts`, replacing the stale bracket/timing examples in place. |
| T-84-07 | T | release metadata | mitigate | Limit `package.json` and `CHANGELOG.md` updates to the verify/coverage/gate/debug release surface so unrelated milestone content is not accidentally introduced. |
</threat_model>

<verification>
- `rg -n "bun run verify|bun run coverage|GS_DEBUG|GIT_STACKS_DEBUG|op=.*module=.*msg=" README.md`
- `node -e "const fs=require('fs'); const pkg=JSON.parse(fs.readFileSync('package.json','utf8')); if (pkg.version !== '0.17.1') process.exit(1)"`
- `rg -n '^## \[0\.17\.1\]' CHANGELOG.md`
- `bun run verify`
</verification>

<success_criteria>
- README documents `bun run verify` first and shows the underlying component commands beneath it.
- README debug examples match the shipped key/value stderr format and promote `GS_DEBUG` over the legacy alias.
- CHANGELOG and package version updates stay tightly focused on the new local verification and release metadata surface.
</success_criteria>

<output>
After completion, create `.planning/phases/84-local-coverage-gates-docs-and-release-prep/84-03-SUMMARY.md`
</output>
