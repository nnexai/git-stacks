# Phase 82.1: Support Commands and Error-Path E2E Coverage - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-10
**Phase:** 82.1-support-commands-and-error-path-e2e-coverage
**Areas discussed:** Support command boundary, edit --yaml contract depth, completion contract strictness, representative failure matrix, Istanbul subprocess proof

---

## Support command boundary

| Option | Description | Selected |
|--------|-------------|----------|
| Keep 82.1 non-interactive only | Cover support commands through automation-safe entrypoints and leave prompt-driven `config` / interactive `install` flows out of scope | ✓ |
| Include prompt-driven install/config flows | Drive the remaining interactive branches in this phase | |
| Split it | Keep `config` wizard out of scope, but include interactive install selection | |

**User's choice:** Keep 82.1 non-interactive only.
**Notes:** The phase should target deterministic support flows such as `config show`, `completion`, `version`, `env`, `paths`, `doctor`, `integration list`, `integration <id> config show/example`, flag-driven `install --hooks ...`, and YAML edit commands.

---

## edit --yaml contract depth

| Option | Description | Selected |
|--------|-------------|----------|
| Fake editor round-trip | Use a fake `$EDITOR` script that records the target file and makes a small valid edit so the suite proves launch + target + persistence | ✓ |
| Launch-only contract | Only assert that the CLI launches `$EDITOR` with the correct file path | |
| Success + failure in one suite | Cover both successful edits and validation/editor failure variants together | |

**User's choice:** Use a fake `$EDITOR` script with a real valid edit round-trip.
**Notes:** Validation-failure and editor-error variants should move into the representative failure slice rather than bloating the success-path edit contract.

---

## Completion contract strictness

| Option | Description | Selected |
|--------|-------------|----------|
| Invariant checks | Assert key commands/options/current tree shape without full golden snapshots | ✓ |
| Full snapshots | Use exact bash/zsh/fish completion snapshots | |
| Hybrid | Lock exact headers/usage and use fragment assertions for the rest | |

**User's choice:** Use shell-specific invariant checks.
**Notes:** Completion should cover bash, zsh, and fish equally rather than treating zsh/fish as second-class smoke outputs.

---

## Completion shell breadth

| Option | Description | Selected |
|--------|-------------|----------|
| All shipped shells | Run the same invariant contract across bash, zsh, and fish | ✓ |
| Deep bash, smoke others | Use bash as the main contract and keep zsh/fish light | |
| Bash + zsh only | Leave fish out of scope | |

**User's choice:** Run the same invariant contract across bash, zsh, and fish.
**Notes:** All shipped shells should stay first-class in support-command coverage.

---

## Representative failure matrix

| Option | Description | Selected |
|--------|-------------|----------|
| Representative per family | One meaningful failure path per in-scope command family, spanning the roadmap's failure classes without a cross-product matrix | ✓ |
| Go deeper on fewer commands | Concentrate failures on a smaller high-risk subset and leave some families success-only | |
| Broad matrix | Cover multiple failure variants for most command families | |

**User's choice:** Keep failure coverage representative per command family.
**Notes:** The combined phase should still span malformed input, missing entity, missing path, validation failure, dirty repo, and permission/error classes.

---

## Istanbul subprocess proof

| Option | Description | Selected |
|--------|-------------|----------|
| Dedicated tiny proof | Instrument one isolated subprocess E2E invocation and write a mergeable artifact, separate from the main support suites | ✓ |
| Embed in a support suite | Hide the proof inside one real support-command E2E suite | |
| Start full tooling now | Begin general coverage tooling in 82.1 instead of keeping it as a proof | |

**User's choice:** Make it a tiny dedicated proof.
**Notes:** This is meant as a clean handoff into Phase 83, not an early expansion of full coverage-report tooling.

---

## Istanbul proof location

| Option | Description | Selected |
|--------|-------------|----------|
| Committed automated artifact | Keep the proof as a committed automated smoke test or verification artifact in the repo | ✓ |
| One-off script/experiment | Leave it as a documented manual experiment | |
| Let the planner decide | Defer the decision to planning | |

**User's choice:** Keep the proof as a committed automated artifact in the repo.
**Notes:** Phase 83 should be able to build directly on the proof without having to reconstruct a transient experiment.

---

## the agent's Discretion

- Exact support-suite file boundaries
- Exact command-family-to-failure mapping
- Exact harness naming for the fake editor and Istanbul smoke proof

## Deferred Ideas

- Prompt-driven `config` wizard E2E coverage
- Interactive `install` selection coverage
- Full completion snapshots
- Exhaustive negative-path matrices
- Full subprocess coverage tooling before Phase 83
