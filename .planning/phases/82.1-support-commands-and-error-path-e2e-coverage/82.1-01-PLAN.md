---
phase: "82.1-support-commands-and-error-path-e2e-coverage"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/helpers.ts
  - tests/support/fake-editor.ts
  - tests/commands/support-readonly.test.ts
  - tests/commands/support-workspace-surfaces.test.ts
autonomous: true
requirements:
  - E2E-12
  - E2E-13
must_haves:
  truths:
    - "Phase 82.1 work does not start unless Phase 80, 81.1, 81.1.1, and 82 prerequisite surfaces are visibly present on the branch."
    - "Support readonly commands prove stable subprocess contracts for config show, --version, and completion bash/zsh/fish per D-02, D-05, and D-06."
    - "Workspace-adjacent support commands prove env, paths, and edit --yaml behavior through real CLI subprocesses, including a fake-editor persisted round trip per D-03 and D-04."
  artifacts:
    - path: "tests/commands/support-readonly.test.ts"
      provides: "Readonly support-command subprocess coverage"
    - path: "tests/commands/support-workspace-surfaces.test.ts"
      provides: "env, paths, and YAML-edit subprocess coverage"
    - path: "tests/support/fake-editor.ts"
      provides: "Deterministic executable fake editor"
  key_links:
    - from: "tests/commands/support-readonly.test.ts"
      to: "src/index.ts"
      via: "Bun.spawnSync(['bun','run','src/index.ts', ...])"
      pattern: "Bun\\.spawnSync"
    - from: "tests/commands/support-workspace-surfaces.test.ts"
      to: "src/lib/workspace-yaml.ts"
      via: "workspace|template|config|repo --yaml invoking openYamlInEditor"
      pattern: "openYamlInEditor|spawnEditor"
---

<objective>
Block Phase 82.1 execution on missing prerequisite drift, then add the first two support-command E2E slices.

Purpose: protect the phase boundary so 82.1 never backfills Phases 80/81.1/81.1.1/82, while still landing the readonly and workspace-adjacent support contracts once prerequisites exist.
Output: one prerequisite guard task, one readonly support suite, one workspace-support suite, and one committed fake-editor executable.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/82.1-support-commands-and-error-path-e2e-coverage/82.1-CONTEXT.md
@.planning/phases/82.1-support-commands-and-error-path-e2e-coverage/82.1-RESEARCH.md
@.planning/phases/82.1-support-commands-and-error-path-e2e-coverage/82.1-VALIDATION.md
@tests/helpers.ts
@tests/commands/env.test.ts
@tests/commands/doctor-json.test.ts
@src/index.ts
@src/commands/config.ts
@src/commands/completion.ts
@src/commands/workspace.ts
@src/lib/workspace-yaml.ts
@tests/lib/completion-generator.test.ts

<interfaces>
From `src/commands/workspace.ts`:
```ts
export type PathsResult =
  | { ok: true; paths: string[]; skipped: string[] }
  | { ok: false; error: string }

export function getWorkspacePaths(
  workspaceName: string,
  opts: { prefix?: string; filter?: "worktree" | "trunk" }
): PathsResult

export function registerWorkspaceCommands(program: Command)
```

From `src/lib/workspace-yaml.ts`:
```ts
export const _exec = {
  spawnEditor: (editor: string, path: string): { exited: Promise<number> } => Bun.spawn([editor, path], ...)
}

export async function openYamlInEditor(
  path: string,
  validate: () => { ok: boolean; error?: string }
): Promise<void>
```

From `tests/helpers.ts`:
```ts
export function makeTmpDir(prefix?: string): string
export function cleanup(dir: string): void
export function makeGitRepo(base: string, name?: string): string
export function applyTestGitEnv(baseDir: string): () => void
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify prerequisite surfaces and stop on drift</name>
  <files>NONE — prerequisite verification only</files>
  <read_first>
    @.planning/phases/82.1-support-commands-and-error-path-e2e-coverage/82.1-CONTEXT.md
    @.planning/phases/82.1-support-commands-and-error-path-e2e-coverage/82.1-RESEARCH.md
    @.planning/phases/82.1-support-commands-and-error-path-e2e-coverage/82.1-VALIDATION.md
    @src/commands/repo.ts
    @src/commands/workspace.ts
    @src/lib/messages.ts
    @tests/helpers.ts
  </read_first>
  <action>Before creating any 82.1 artifact, run a hard prerequisite gate for Phases 80, 81.1, 81.1.1, and 82. The guard must check these exact surfaces: (1) `tests/e2e-inventory.ts` exists for Phase 80. (2) `src/commands/repo.ts` or `src/lib/integrations/forge-utils.ts` contains enabled-aware forge gating for Phase 81.1 via `resolveEnabledGlobally`, `allowedForges`, or an equivalent enabled-only detector path. (3) `src/commands/workspace.ts` contains `--non-interactive` support for `new` and `clone`, and at least one command test already exercises `new --non-interactive` or `clone --non-interactive`, for Phase 81.1.1. (4) Phase 82 success-path surfaces are present: at least one `tests/commands/` file covering repo registry commands, one covering message CLI commands, and one explicit message socket opt-out seam in `src/lib/messages.ts` or `src/commands/message.ts`. If any surface is absent, fail immediately, print the missing item list, and stop instead of creating `support-readonly.test.ts`, `support-workspace-surfaces.test.ts`, `tests/support/fake-editor.ts`, or any inventory/coverage update. This is the explicit stop gate required by the planning constraint and preserves D-01/D-07 scope fidelity.</action>
  <acceptance_criteria>
    - `test -f tests/e2e-inventory.ts` succeeds.
    - `rg -q "resolveEnabledGlobally|allowedForges|enabledForge" src/commands/repo.ts src/lib/integrations/forge-utils.ts` succeeds.
    - `rg -q -- "--non-interactive" src/commands/workspace.ts` succeeds.
    - `rg -l "new --non-interactive|clone --non-interactive" tests/commands | grep -q .` succeeds.
    - `rg -l "repo add|repo list|repo show|repo rename|repo remove" tests/commands | grep -q .` succeeds.
    - `rg -l "message send|message list|message clear" tests/commands | grep -q .` succeeds.
    - `rg -q "GS_.*SOCKET|MESSAGE_SOCKET|disable.*socket" src/lib/messages.ts src/commands/message.ts` succeeds.
    - When any check fails, execution stops before 82.1 files are created or edited.
  </acceptance_criteria>
  <verify>
    <automated>test -f tests/e2e-inventory.ts &amp;&amp; rg -q "resolveEnabledGlobally|allowedForges|enabledForge" src/commands/repo.ts src/lib/integrations/forge-utils.ts &amp;&amp; rg -q -- "--non-interactive" src/commands/workspace.ts &amp;&amp; rg -l "new --non-interactive|clone --non-interactive" tests/commands | grep -q . &amp;&amp; rg -l "repo add|repo list|repo show|repo rename|repo remove" tests/commands | grep -q . &amp;&amp; rg -l "message send|message list|message clear" tests/commands | grep -q . &amp;&amp; rg -q "GS_.*SOCKET|MESSAGE_SOCKET|disable.*socket" src/lib/messages.ts src/commands/message.ts</automated>
  </verify>
  <done>Either all prerequisite surfaces are confirmed and later tasks may start, or execution exits non-zero without backfilling earlier phases.</done>
</task>

<task type="auto">
  <name>Task 2: Add readonly support-command subprocess contracts</name>
  <files>tests/commands/support-readonly.test.ts</files>
  <read_first>
    @tests/commands/support-readonly.test.ts
    @tests/lib/completion-generator.test.ts
    @src/index.ts
    @src/commands/config.ts
    @src/commands/completion.ts
    @package.json
  </read_first>
  <action>Create `tests/commands/support-readonly.test.ts` after Task 1 passes. Use the established `Bun.spawnSync(["bun", "run", "src/index.ts", ...])` subprocess pattern from existing command tests. Cover D-02, D-05, and D-06 with these exact assertions: `config show` exits 0 and prints `workspace_root:`, `Integrations:`, `Secrets:`, and `Config:` using an isolated config dir; top-level `--version` exits 0, writes only the version string, and leaves stderr empty; `completion bash`, `completion zsh`, and `completion fish` each exit 0 and assert shell-specific invariants instead of snapshots — bash wrapper + `complete -F`, zsh `#compdef git-stacks` + `_git_stacks_top_commands`, fish `complete -c git-stacks` + function wrapper — plus current command-tree markers for `env`, `paths`, `integration`, and `message`. Do not add golden snapshot files. Do not cover prompt-driven config or install flows here (D-01 deferred boundary).</action>
  <acceptance_criteria>
    - `tests/commands/support-readonly.test.ts` exists and uses `Bun.spawnSync`.
    - The file contains one success-path test for `config show`.
    - The file contains one top-level `--version` contract test.
    - The file contains shell-specific completion assertions for bash, zsh, and fish.
    - No `toMatchSnapshot` assertion is added.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/commands/support-readonly.test.ts -x</automated>
  </verify>
  <done>`config show`, `--version`, and completion bash/zsh/fish are covered by deterministic subprocess tests with invariant assertions only.</done>
</task>

<task type="auto">
  <name>Task 3: Add env, paths, and YAML-edit support coverage with a fake editor</name>
  <files>tests/helpers.ts, tests/support/fake-editor.ts, tests/commands/support-workspace-surfaces.test.ts</files>
  <read_first>
    @tests/helpers.ts
    @tests/commands/support-workspace-surfaces.test.ts
    @tests/commands/env.test.ts
    @tests/commands/workspace-edit.test.ts
    @src/commands/workspace.ts
    @src/commands/config.ts
    @src/commands/repo.ts
    @src/lib/workspace-yaml.ts
  </read_first>
  <action>Create a committed executable fake editor at `tests/support/fake-editor.ts` with a shebang and chmod step so Bun can launch it directly per D-03. Support three env-driven modes: `mutate-valid`, `mutate-invalid`, and `fail`; always record the target file path to a caller-provided capture file. Extend `tests/helpers.ts` only as needed to resolve the executable path and pass capture/env settings into subprocess tests. Then create `tests/commands/support-workspace-surfaces.test.ts` covering D-02/D-03/D-04 with real subprocess checks for: `env <workspace>` and cwd-detected `env` (including `--repo`), `paths <workspace>` and cwd-detected `paths` (including `--prefix` and stderr warnings for skipped repos), `workspace edit <name> --yaml`, `template edit <name> --yaml`, `config --yaml`, and `repo --yaml` using fake-editor `mutate-valid` mode. Assert the recorded target path, persisted YAML mutation, exit 0, and no validation warning on the success path. Keep invalid-YAML and editor-fail variants out of this file; those belong in Plan 03 per D-04.</action>
  <acceptance_criteria>
    - `tests/support/fake-editor.ts` is executable and contains `mutate-valid`, `mutate-invalid`, and `fail` mode handling.
    - `tests/commands/support-workspace-surfaces.test.ts` contains subprocess coverage for `env`, `paths`, and all four `--yaml` edit entrypoints.
    - Success-path edit tests assert the editor captured the exact YAML file path it opened.
    - Success-path edit tests assert the YAML mutation persisted to disk.
    - The file does not add invalid-YAML or editor-fail assertions.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/commands/support-workspace-surfaces.test.ts -x</automated>
  </verify>
  <done>`env`, `paths`, and every in-scope YAML edit surface are covered by deterministic subprocess tests, and the fake-editor round trip proves the correct file was launched and saved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture files → CLI parser | Untrusted YAML and argv enter the real CLI subprocess. |
| fake editor executable → YAML validation | An external process mutates config files before validation re-runs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82.1-01 | T | `tests/support/fake-editor.ts` | mitigate | Use a committed executable with only three explicit modes, record the target path, and assert exact persisted content after each subprocess run. |
| T-82.1-02 | D | prerequisite guard in Task 1 | mitigate | Fail fast before any 82.1 files are written when Phase 80/81.1/81.1.1/82 prerequisite surfaces are missing. |
</threat_model>

<verification>
Run the prerequisite guard first. If it passes, run:
- `bun test tests/commands/support-readonly.test.ts -x`
- `bun test tests/commands/support-workspace-surfaces.test.ts -x`
</verification>

<success_criteria>
- Phase 82.1 execution blocks immediately on missing prerequisite drift.
- Readonly support commands have deterministic subprocess coverage without snapshots.
- `env`, `paths`, and all in-scope `--yaml` edit commands have success-path subprocess coverage with a real fake-editor executable.
</success_criteria>

<output>
After completion, create `.planning/phases/82.1-support-commands-and-error-path-e2e-coverage/82.1-01-SUMMARY.md`
</output>
