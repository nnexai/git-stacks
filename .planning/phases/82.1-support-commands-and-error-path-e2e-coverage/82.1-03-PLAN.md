---
phase: "82.1-support-commands-and-error-path-e2e-coverage"
plan: "03"
type: execute
wave: 3
depends_on:
  - "82.1-01"
  - "82.1-02"
files_modified:
  - tests/commands/support-failures.test.ts
  - tests/commands/istanbul-smoke.test.ts
  - tests/support/istanbul-smoke.ts
  - tests/e2e-inventory.ts
  - package.json
  - bun.lock
autonomous: true
requirements:
  - E2E-12
  - E2E-13
must_haves:
  truths:
    - "Representative failure coverage spans malformed input, missing entity, missing path, validation failure, dirty repo, environment-sensitive behavior, permission/editor-error cases, and one bounded install-family failure without becoming a matrix."
    - "The canonical Phase 80 inventory maps every new Phase 82.1 surface and records deferred exclusions inline."
    - "A dedicated committed smoke proves Istanbul-style source instrumentation can produce mergeable coverage artifacts from an isolated subprocess."
  artifacts:
    - path: "tests/commands/support-failures.test.ts"
      provides: "Representative negative-path subprocess coverage"
    - path: "tests/e2e-inventory.ts"
      provides: "Updated canonical support-command mapping"
    - path: "tests/commands/istanbul-smoke.test.ts"
      provides: "Dedicated subprocess coverage handoff proof"
  key_links:
    - from: "tests/commands/support-failures.test.ts"
      to: "src/commands/workspace.ts"
      via: "dirty-repo guard carried forward only as a representative failure"
      pattern: "remove|clean"
    - from: "tests/commands/support-failures.test.ts"
      to: "src/commands/install.ts"
      via: "bounded install failure outside any detected workspace"
      pattern: "No workspaces found"
    - from: "tests/commands/istanbul-smoke.test.ts"
      to: "tests/support/istanbul-smoke.ts"
      via: "instrument-and-run helper"
      pattern: "__coverage__|istanbul"
---

<objective>
Close the representative error-path requirement and leave Phase 83 a working subprocess-coverage proof instead of a research spike.

Purpose: finish E2E-13 without scope creep, update the canonical inventory, and commit a tiny Istanbul proof per D-09/D-10.
Output: one representative failure suite, one inventory update, and one dedicated subprocess-coverage smoke.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/82.1-support-commands-and-error-path-e2e-coverage/82.1-CONTEXT.md
@.planning/phases/82.1-support-commands-and-error-path-e2e-coverage/82.1-RESEARCH.md
@.planning/phases/82.1-support-commands-and-error-path-e2e-coverage/82.1-VALIDATION.md
@tests/support/fake-editor.ts
@src/commands/workspace.ts
@src/commands/completion.ts
@src/lib/messages.ts
@package.json

<interfaces>
From `src/lib/messages.ts`:
```ts
export interface MessageRecord {
  workspace: string
  text: string
  from?: string
  timestamp: string
}

export async function pushToSocket(record: MessageRecord): Promise<void>
```

From `src/commands/completion.ts`:
```ts
export function createCompletionCommand(program: Command): Command
```

From `src/commands/workspace.ts`:
```ts
program.command("paths [workspace]")
program.command("env [workspace]")
program.command("edit <workspace>").option("--yaml", ...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add representative failure coverage without exploding the matrix</name>
  <files>tests/commands/support-failures.test.ts</files>
  <read_first>
    @tests/commands/support-failures.test.ts
    @tests/support/fake-editor.ts
    @src/commands/completion.ts
    @src/commands/workspace.ts
    @src/commands/integration.ts
    @src/commands/config.ts
    @tests/helpers.ts
  </read_first>
  <action>Create `tests/commands/support-failures.test.ts` as one focused representative-failure suite for D-07 and D-08. Cover these exact classes with one case each and stop there: malformed input via `completion powershell`; missing entity via `integration vscode config show missing-ws`; missing path via `paths broken-ws` where all repo paths are absent; validation failure via `workspace edit broken-ws --yaml` using fake-editor `mutate-invalid`; dirty repo via one carried-forward workspace command guard (`clean` or `remove` without `--force`) against a deliberately dirty worktree fixture; environment-sensitive behavior via `env env-ws --format json` against a workspace fixture whose env contains `${{ env:MISSING_SECRET }}` with `secrets.resolvers: [env]`, asserting `Secret resolution failed:`; permission/editor error via fake-editor `fail` on `config --yaml` or `repo --yaml`; and one bounded install-family failure via `install --hooks --claude` from a cwd outside any workspace with an empty config, asserting the exact `No workspaces found. Create one first with: git-stacks new` error. Use the carried-forward dirty-repo guard only because no remaining support-only command has a genuine dirty-worktree failure mode; keep the install failure to a single deterministic case rather than opening a second matrix for doctor/install. Keep the file small and command-family-scoped.</action>
  <acceptance_criteria>
    - `tests/commands/support-failures.test.ts` exists.
    - The file contains exactly one representative case for each required failure class: malformed input, missing entity, missing path, validation failure, dirty repo, environment-sensitive behavior, permission/editor error.
    - `grep -q 'install --hooks --claude' tests/commands/support-failures.test.ts`
    - `grep -q 'No workspaces found. Create one first with: git-stacks new' tests/commands/support-failures.test.ts`
    - `grep -q 'MISSING_SECRET' tests/commands/support-failures.test.ts`
    - `grep -q 'Secret resolution failed:' tests/commands/support-failures.test.ts`
    - The file does not add a cross-product matrix of variants.
    - Dirty-repo coverage is explicitly limited to one carried-forward guard case.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/commands/support-failures.test.ts -x</automated>
  </verify>
  <done>E2E-13 is satisfied with one meaningful representative case per required failure class and no exhaustive matrix growth.</done>
</task>

<task type="auto">
  <name>Task 2: Update the canonical E2E inventory for every new 82.1 surface</name>
  <files>tests/e2e-inventory.ts</files>
  <read_first>
    @tests/e2e-inventory.ts
    @.planning/phases/80-e2e-cli-harness-and-living-inventory/80-CONTEXT.md
    @.planning/phases/82.1-support-commands-and-error-path-e2e-coverage/82.1-CONTEXT.md
    @tests/commands/support-readonly.test.ts
    @tests/commands/support-workspace-surfaces.test.ts
    @tests/commands/support-doctor-install.test.ts
    @tests/commands/support-integration-config.test.ts
    @tests/commands/support-failures.test.ts
    @tests/commands/istanbul-smoke.test.ts
  </read_first>
  <action>Update `tests/e2e-inventory.ts` only after prerequisite verification and the support suites exist. Preserve the Phase 80 typed-inventory model per D-04/D-05 from Phase 80 and map every new 82.1 surface to concrete test files: `config show`, `completion`, `version`, `env`, `paths`, `doctor`, `install --hooks`, `edit --yaml`, `integration list`, `integration <id> config show/example`, the representative failure suite, and the Istanbul smoke proof. Record deferred exclusions inline with rationale for prompt-driven `config`, interactive `install`, completion snapshots, exhaustive failure matrices, and full subprocess coverage tooling before Phase 83. Do not create a prose sidecar.</action>
  <acceptance_criteria>
    - `tests/e2e-inventory.ts` maps every D-02 support surface to one or more `tests/commands/*.test.ts` files.
    - Deferred exclusions remain inline in the inventory source, not a new markdown file.
    - No in-scope 82.1 support item is left unmapped after this update.
  </acceptance_criteria>
  <verify>
    <automated>rg -q "config show|completion|version|env|paths|doctor|install --hooks|edit --yaml|integration list|config show/example|support-failures|istanbul-smoke" tests/e2e-inventory.ts</automated>
  </verify>
  <done>The canonical inventory reflects every shipped 82.1 surface and its test mapping, with deferred items documented inline.</done>
</task>

<task type="auto">
  <name>Task 3: Commit a dedicated Istanbul subprocess smoke for Phase 83 handoff</name>
  <files>package.json, bun.lock, tests/support/istanbul-smoke.ts, tests/commands/istanbul-smoke.test.ts</files>
  <read_first>
    @package.json
    @tests/commands/istanbul-smoke.test.ts
    @src/index.ts
    @src/commands/completion.ts
    @.planning/phases/82.1-support-commands-and-error-path-e2e-coverage/82.1-RESEARCH.md
  </read_first>
  <action>Add the minimal dependencies needed for the proof if they are absent (`istanbul-lib-instrument` and `istanbul-lib-coverage`), then create `tests/support/istanbul-smoke.ts` plus `tests/commands/istanbul-smoke.test.ts` for D-09/D-10. The smoke must stay separate from the support suites and exercise the real CLI subprocess path `bun run src/index.ts completion bash` using temporary Istanbul-instrumented source, not Bun's built-in coverage. Have the helper instrument the target TypeScript source with `parserPlugins: ["typescript"]`, run the instrumented subprocess, collect `globalThis.__coverage__` into a JSON artifact in a test-owned temp dir, and assert that `istanbul-lib-coverage` can load/merge the artifact and that at least one statement/function count is non-zero. Do not add full report generation, coverage directories, or merged-suite tooling here; that remains Phase 83.</action>
  <acceptance_criteria>
    - `package.json` lists any new Istanbul proof dependencies actually used by the smoke.
    - `tests/commands/istanbul-smoke.test.ts` runs a real `completion bash` subprocess path.
    - The smoke writes a coverage JSON artifact into a temp dir and proves it is mergeable.
    - The proof is isolated from the main support suites and does not add general coverage tooling.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/commands/istanbul-smoke.test.ts -x</automated>
  </verify>
  <done>A dedicated committed smoke proves Istanbul-style source instrumentation can capture and merge coverage data from at least one isolated subprocess invocation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| invalid fixtures/editor modes → CLI subprocess | Failure-path tests intentionally push malformed input and corrupted YAML into the real CLI. |
| instrumented subprocess → coverage artifact JSON | Child-process coverage data crosses back into the test process for merge validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82.1-05 | T | `tests/commands/support-failures.test.ts` | mitigate | Keep fixture corruption and dirty-repo setup inside temp dirs only, and assert the exact failing command/output for each representative class. |
| T-82.1-06 | I | Istanbul smoke artifact | mitigate | Write coverage JSON only into a test-owned temp dir, parse it with `istanbul-lib-coverage`, and assert mergeability before treating the proof as complete. |
</threat_model>

<verification>
Run:
- `bun test tests/commands/support-failures.test.ts -x`
- `bun test tests/commands/istanbul-smoke.test.ts -x`
- `bun run test && bun run typecheck`
</verification>

<success_criteria>
- Representative failures cover malformed input, missing entity, missing path, validation failure, dirty repo, environment-sensitive behavior, permission/editor-error cases, and one bounded install-family failure without a matrix explosion.
- The Phase 80 inventory is fully updated for 82.1 surfaces.
- Phase 83 receives a committed, passing Istanbul subprocess smoke instead of an unproven idea.
</success_criteria>

<output>
After completion, create `.planning/phases/82.1-support-commands-and-error-path-e2e-coverage/82.1-03-SUMMARY.md`
</output>
