---
phase: "82.1-support-commands-and-error-path-e2e-coverage"
plan: "02"
type: execute
wave: 2
depends_on:
  - "82.1-01"
files_modified:
  - tests/commands/support-doctor-install.test.ts
  - tests/commands/support-integration-config.test.ts
autonomous: true
requirements:
  - E2E-12
must_haves:
  truths:
    - "Doctor coverage stays local-only unless a fixture explicitly enables an integration binary."
    - "Flag-driven `install --hooks` paths run non-interactively from a detected workspace repo and prove create/remove side effects."
    - "`integration list` and `integration <id> config show/example` have stable table, JSON, example, and fallback subprocess contracts."
  artifacts:
    - path: "tests/commands/support-doctor-install.test.ts"
      provides: "doctor and install support-command coverage"
    - path: "tests/commands/support-integration-config.test.ts"
      provides: "integration list/config show/example coverage"
  key_links:
    - from: "tests/commands/support-doctor-install.test.ts"
      to: "src/commands/doctor.ts"
      via: "fixture-controlled doctor subprocess assertions"
      pattern: "doctor --json|healthy|issues"
    - from: "tests/commands/support-doctor-install.test.ts"
      to: "src/commands/install.ts"
      via: "workspace-detected cwd subprocess runs"
      pattern: "install --hooks"
    - from: "tests/commands/support-integration-config.test.ts"
      to: "src/commands/integration.ts"
      via: "list/show/example subprocess assertions"
      pattern: "integration"
---

<objective>
Add the remaining support-command success-path suites after prerequisite verification is green.

Purpose: finish the non-interactive support-command contract coverage from D-02 without mixing in the representative failure matrix.
Output: one doctor/install suite and one integration-config suite.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/82.1-support-commands-and-error-path-e2e-coverage/82.1-CONTEXT.md
@.planning/phases/82.1-support-commands-and-error-path-e2e-coverage/82.1-RESEARCH.md
@.planning/phases/82.1-support-commands-and-error-path-e2e-coverage/82.1-VALIDATION.md
@src/commands/doctor.ts
@src/commands/install.ts
@src/commands/integration.ts
@src/lib/agent-hooks/claude-code.ts
@src/lib/agent-hooks/copilot.ts
@tests/commands/doctor-json.test.ts
@tests/helpers.ts

<interfaces>
From `src/commands/install.ts`:
```ts
export const installCommand = new Command("install")
  .option("--hooks", ...)
  .option("--remove", ...)
  .option("--copilot", ...)
  .option("--claude", ...)
```

From `src/commands/integration.ts`:
```ts
export const integrationCommand = new Command("integration")
  .command("list [workspace]")
  .command("<id> config example")
  .command("<id> config show [workspace]")
```

From `src/lib/agent-hooks/claude-code.ts` and `copilot.ts`:
```ts
claudeCodePlugin.install(repoWorktreePath: string, workspaceName: string): void
claudeCodePlugin.remove(repoWorktreePath: string): void
copilotPlugin.install(repoWorktreePath: string, workspaceName: string): void
copilotPlugin.remove(repoWorktreePath: string): void
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add doctor and install support-command contracts</name>
  <files>tests/commands/support-doctor-install.test.ts</files>
  <read_first>
    @tests/commands/support-doctor-install.test.ts
    @tests/commands/doctor-json.test.ts
    @src/commands/doctor.ts
    @src/commands/install.ts
    @src/lib/agent-hooks/claude-code.ts
    @src/lib/agent-hooks/copilot.ts
    @tests/helpers.ts
  </read_first>
  <action>Create `tests/commands/support-doctor-install.test.ts` after Plan 01 passes. Keep the doctor half local-only per D-02 and Research Finding 4: run `doctor` with integrations disabled in fixture config and assert the command does not require `gh`, `glab`, `tea`, or `jira` to exist in order to exit 0 and print normal health output. For install, create a real fixture workspace repo, run from that repo as cwd, and cover the non-interactive flag-driven contracts only (D-01/D-02): `install --hooks --claude`, `install --hooks --copilot`, and `install --hooks --remove --claude`. Assert exact side effects on `.claude/settings.json` and `.github/hooks/git-stacks.json`, and assert the command does not prompt when flags are present. Do not cover interactive plugin selection here.</action>
  <acceptance_criteria>
    - `tests/commands/support-doctor-install.test.ts` exists.
    - The file contains at least one `doctor` human-output contract test with integrations disabled.
    - The file contains install tests for Claude install, Copilot install, and Claude remove.
    - Install tests assert exact hook file creation/removal under the target repo worktree.
    - No interactive prompt path is exercised.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/commands/support-doctor-install.test.ts -x</automated>
  </verify>
  <done>`doctor` and flag-driven `install --hooks` have deterministic subprocess coverage that stays inside the automation-safe boundary from D-01/D-02.</done>
</task>

<task type="auto">
  <name>Task 2: Add integration list/show/example support coverage</name>
  <files>tests/commands/support-integration-config.test.ts</files>
  <read_first>
    @tests/commands/support-integration-config.test.ts
    @src/commands/integration.ts
    @src/lib/integrations/vscode.ts
    @tests/helpers.ts
  </read_first>
  <action>Create `tests/commands/support-integration-config.test.ts` covering the remaining D-02 support surfaces. Use isolated global config plus one workspace fixture to assert `integration list` table output, `integration list --json` shape, `integration vscode config example` returning the committed YAML example, `integration github config example` returning the fallback “No configuration example available...” message, `integration vscode config show` returning global config, and `integration vscode config show <workspace> --json` returning `{ id, enabled, global, workspace }`. Include one missing-workspace error assertion for `integration <id> config show missing-ws` only if it remains a success-contract helper for the main show path; reserve broader error coverage for Plan 03.</action>
  <acceptance_criteria>
    - `tests/commands/support-integration-config.test.ts` exists.
    - It covers `integration list` table output and `--json`.
    - It covers one `config example` path with real example text and one fallback path without an example.
    - It covers `config show` for global and workspace-aware JSON output.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/commands/support-integration-config.test.ts -x</automated>
  </verify>
  <done>`integration list` and `integration &lt;id&gt; config show/example` are covered with stable subprocess assertions across table, JSON, example, and fallback output modes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture cwd → `install --hooks` | The CLI writes hook files into the repo rooted at the current working directory. |
| fixture config → `doctor` and `integration` | Untrusted YAML controls enabled integrations and rendered config output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82.1-03 | T | `install --hooks` repo target | mitigate | Run install tests only inside fixture repos, assert exact hook file paths, and avoid any write outside the temp worktree. |
| T-82.1-04 | I | `doctor` / `integration` config output | mitigate | Use synthetic configs with no secrets and assert only deterministic fields from isolated fixture homes. |
</threat_model>

<verification>
Run:
- `bun test tests/commands/support-doctor-install.test.ts -x`
- `bun test tests/commands/support-integration-config.test.ts -x`
</verification>

<success_criteria>
- Doctor support coverage remains fixture-controlled and machine-independent.
- Install hook coverage proves non-interactive flag paths and exact file side effects.
- Integration config coverage proves list/show/example output contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/82.1-support-commands-and-error-path-e2e-coverage/82.1-02-SUMMARY.md`
</output>
