---
phase: 102-workspace-root-auto-detection
plan: 02
type: execute
wave: 2
depends_on: ["102-01"]
files_modified:
  - tests/commands/workspace-wrapper-edges.test.ts
  - tests/commands/notes.test.ts
  - README.md
  - src/commands/workspace.ts
  - src/commands/notes.ts
  - src/lib/integrations/issue-utils.ts
autonomous: true
requirements:
  - WDET-01
  - WDET-02
  - WDET-03
requirements_addressed:
  - WDET-01
  - WDET-02
  - WDET-03
must_haves:
  truths:
    - "D-07/D-08/D-09/D-15: Focused real-fixture CLI coverage proves workspace-root detection through at least one workspace-only surface, one env-fallback surface, and one repo-aware surface where cwd still matters after workspace detection."
    - "D-01/D-05/D-14: Representative subprocess tests prove both the workspace root itself and a nested non-repo workspace subdirectory resolve correctly without regressing repo-aware behavior."
    - "D-03/D-04/D-11: User-facing guidance and docs describe the shipped order accurately: explicit arg, cwd detection, then `GS_WORKSPACE_NAME` only where already supported."
    - "Final phase gate remains the existing `bun run verify`, not a new release workflow or CI path."
  artifacts:
    - path: "tests/commands/workspace-wrapper-edges.test.ts"
      provides: "Representative subprocess coverage for workspace-only and repo-aware root detection"
      contains: "workspace root"
    - path: "tests/commands/notes.test.ts"
      provides: "Representative subprocess coverage for cwd-before-env fallback from workspace root/subdir"
      contains: "GS_WORKSPACE_NAME"
    - path: "README.md"
      provides: "User-facing docs aligned to workspace-root autodetection and current fallback order"
      contains: "Autodetects workspace"
  key_links:
    - from: "tests/commands/workspace-wrapper-edges.test.ts"
      to: "src/commands/workspace.ts"
      via: "paths and env subprocess tests prove workspace-root resolution and repo-aware follow-up behavior"
      pattern: "git-stacks paths"
    - from: "tests/commands/notes.test.ts"
      to: "src/commands/notes.ts"
      via: "notes subprocess tests prove cwd beats `GS_WORKSPACE_NAME` even from the workspace root"
      pattern: "GS_WORKSPACE_NAME"
    - from: "README.md"
      to: "src/commands/workspace.ts"
      via: "user docs match the shipped autodetection guidance and optional-workspace order"
      pattern: "Autodetects workspace"
---

<objective>
Add representative real-fixture CLI proof for workspace-root detection and align user-facing guidance with the shipped resolver order.

Purpose: After the shared resolver lands, Phase 102 still needs user-visible evidence. This plan proves the behavior through focused subprocess contracts and updates docs/error guidance so operators are told the truth about workspace-root invocation.

Output: representative subprocess tests, updated README/examples, and bounded command guidance aligned to the implemented resolver order.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/102-workspace-root-auto-detection/102-CONTEXT.md
@.planning/phases/102-workspace-root-auto-detection/102-RESEARCH.md
@.planning/phases/102-workspace-root-auto-detection/102-PATTERNS.md
@.planning/phases/102-workspace-root-auto-detection/102-VALIDATION.md
@.planning/phases/102-workspace-root-auto-detection/102-01-PLAN.md
@.planning/phases/101-completion-completeness-repair/101-02-SUMMARY.md

@README.md
@src/commands/workspace.ts
@src/commands/notes.ts
@src/lib/integrations/issue-utils.ts
@tests/commands/workspace-wrapper-edges.test.ts
@tests/commands/notes.test.ts

<interfaces>
From src/commands/workspace.ts:
`program.command("paths [workspace]")`
`program.command("env [workspace]")`
`program.command("pull [workspace]")`

From src/commands/notes.ts:
`function requireExistingWorkspace(explicitWorkspace?: string): string`

From tests/helpers.ts usage in existing command tests:
`runCli(args, { baseDir, configDir, cwd, env })`
`makeRepoWithRemote(tmpDir, name, branch)`
`makeWorkspaceFixture(configDir, wsName, repos, opts?)`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add representative subprocess coverage for workspace-root detection</name>
  <files>tests/commands/workspace-wrapper-edges.test.ts, tests/commands/notes.test.ts</files>
  <read_first>
    - tests/commands/workspace-wrapper-edges.test.ts
    - tests/commands/notes.test.ts
    - src/commands/workspace.ts
    - src/commands/notes.ts
    - src/lib/workspace-resolution.ts
    - .planning/phases/102-workspace-root-auto-detection/102-CONTEXT.md
    - .planning/phases/102-workspace-root-auto-detection/102-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: A workspace-only surface such as `git-stacks paths` resolves correctly from the workspace root and a nested non-repo workspace subdirectory.
    - Test 2: A repo-aware surface such as `git-stacks env --format json` resolves the workspace from the root but only adds `GS_REPO_NAME` / repo overlay when cwd is inside a repo worktree or `--repo` is passed.
    - Test 3: An env-fallback surface such as `git-stacks notes add` still uses explicit arg -> cwd -> `GS_WORKSPACE_NAME`, including cwd at the workspace root or a workspace subdirectory that is not inside a repo.
  </behavior>
  <action>
    Extend the existing real-fixture subprocess suites instead of creating new harnesses. In `tests/commands/workspace-wrapper-edges.test.ts`, add failing cases for `paths` from the workspace root and a non-repo workspace subdirectory, plus `env --format json` from the workspace root/subdirectory to prove workspace detection succeeds while repo overlay remains absent until repo cwd or explicit `--repo` per D-04. In `tests/commands/notes.test.ts`, add failing cases showing cwd at the workspace root or nested non-repo workspace subdirectory beats `GS_WORKSPACE_NAME`, and that env fallback only activates when cwd detection is unavailable per D-08/D-11. Keep the representative surface set focused to `paths`, `env`, and `notes`; do not explode into every optional-workspace command.
  </action>
  <acceptance_criteria>
    - `tests/commands/workspace-wrapper-edges.test.ts` contains subprocess cases for both the workspace root directory itself and a non-repo subdirectory under that root.
    - The repo-aware `env` assertions show `GS_WORKSPACE_NAME` is present from root cwd while `GS_REPO_NAME` stays absent until repo cwd or explicit `--repo`.
    - `tests/commands/notes.test.ts` proves cwd from workspace root/subdir outranks `GS_WORKSPACE_NAME`, and env fallback still works only when cwd detection fails.
    - `bun test tests/commands/workspace-wrapper-edges.test.ts tests/commands/notes.test.ts` is the representative CLI gate for D-07/D-08/D-09/D-15.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/commands/workspace-wrapper-edges.test.ts tests/commands/notes.test.ts</automated>
  </verify>
  <done>
    Real CLI fixtures prove the three required resolver shapes from workspace-root and workspace-subdirectory contexts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align README text and command guidance to the shipped resolver order</name>
  <files>README.md, src/commands/workspace.ts, src/commands/notes.ts, src/lib/integrations/issue-utils.ts</files>
  <read_first>
    - README.md
    - src/commands/workspace.ts
    - src/commands/notes.ts
    - src/lib/integrations/issue-utils.ts
    - tests/commands/workspace-wrapper-edges.test.ts
    - tests/commands/notes.test.ts
    - .planning/phases/102-workspace-root-auto-detection/102-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `paths`, `env`, and `pull` guidance no longer claim autodetection only works from a repo worktree.
    - Test 2: `notes` guidance still mentions `GS_WORKSPACE_NAME`, but only after explicit arg and cwd detection.
    - Test 3: Final repo verification runs through the existing `bun run verify` gate after the focused subprocess suites are green.
  </behavior>
  <action>
    Update README command examples and prose so the documented behavior matches D-01/D-03/D-11: `paths` and `pull` autodetection should mention the workspace root directory as valid alongside repo worktrees, and any concise notes about optional workspace resolution should preserve explicit arg -> cwd -> `GS_WORKSPACE_NAME` only where that env fallback already existed. Update user-facing error/help guidance in `src/commands/workspace.ts`, `src/commands/notes.ts`, and `src/lib/integrations/issue-utils.ts` so the wording no longer says only "run from inside a worktree" when workspace-root invocation is valid. Keep the guidance bounded and command-specific; do not add new flags, change help schemas, or introduce stale-advisory, TUI, template, or forge/source behavior.
  </action>
  <acceptance_criteria>
    - `README.md` visibly states that `paths` and `pull` autodetection work from the workspace root directory, not just from repo worktrees.
    - `src/commands/workspace.ts` and `src/lib/integrations/issue-utils.ts` no longer emit bounded guidance that falsely implies repo-worktree-only detection.
    - `src/commands/notes.ts` continues to mention `GS_WORKSPACE_NAME` only for the notes surface and only after explicit arg plus cwd detection are unavailable.
    - `bun run verify` is the final automated gate listed for this plan and remains the canonical phase-close command.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/commands/workspace-wrapper-edges.test.ts tests/commands/notes.test.ts</automated>
    <automated>bun run typecheck</automated>
    <automated>bun run verify</automated>
  </verify>
  <done>
    The CLI's user-facing docs and bounded error guidance match the shipped workspace-root behavior, and the full local verification gate still passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subprocess cwd/env fixtures -> workspace resolution | The CLI resolves optional workspaces from real cwd and env inputs, so representative tests must prove the right precedence under user-like conditions. |
| README and stderr guidance -> operator behavior | Users rely on docs and command errors to decide whether to rerun from cwd, pass a workspace, or set `GS_WORKSPACE_NAME`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-04 | Tampering | `tests/commands/workspace-wrapper-edges.test.ts` | mitigate | Task 1 adds root and non-repo-subdirectory subprocess fixtures so representative coverage cannot pass while only repo-cwd behavior still works. |
| T-102-05 | Spoofing | `tests/commands/notes.test.ts` | mitigate | Task 1 proves cwd at the workspace root or workspace subdirectory outranks `GS_WORKSPACE_NAME`, preventing env spoofing from silently overriding a valid cwd match. |
| T-102-06 | Repudiation | `README.md`, `src/commands/workspace.ts`, `src/lib/integrations/issue-utils.ts` | mitigate | Task 2 aligns docs and bounded guidance with the actual resolver order so operators see the same contract the code enforces. |
| T-102-SC | Tampering | Bun/verify toolchain | mitigate | No new packages are introduced; the final gate is the existing `bun run verify` workflow rather than ad-hoc scripts. |
</threat_model>

<verification>
- `bun test tests/commands/workspace-wrapper-edges.test.ts tests/commands/notes.test.ts`
- `bun run typecheck`
- `bun run verify`
</verification>

<success_criteria>
- Representative subprocess coverage proves workspace-root detection through a workspace-only command, an env-fallback command, and a repo-aware command.
- Root/subdirectory cwd detection is shown end to end without regressing repo overlay behavior or env-fallback precedence.
- README examples and bounded command guidance describe workspace-root autodetection truthfully.
- `bun run verify` remains the final phase gate for Phase 102.
</success_criteria>

<output>
Create `.planning/phases/102-workspace-root-auto-detection/102-02-SUMMARY.md` when done
</output>
