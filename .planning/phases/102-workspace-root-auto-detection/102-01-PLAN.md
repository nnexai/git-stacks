---
phase: 102-workspace-root-auto-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/workspace-status.ts
  - src/lib/workspace-resolution.ts
  - src/lib/integrations/issue-utils.ts
  - src/commands/workspace.ts
  - src/commands/files.ts
  - src/commands/command.ts
  - src/commands/notes.ts
  - tests/lib/detect-workspace-cwd.test.ts
autonomous: true
requirements:
  - WDET-01
  - WDET-02
  - WDET-03
requirements_addressed:
  - WDET-01
  - WDET-02
  - WDET-03
must_haves:
  truths:
    - "D-01/D-05/D-06: `detectWorkspaceFromCwd()` resolves a workspace from the workspace root and nested workspace subdirectories while preserving deepest-worktree precedence, trunk skip, and prefix-collision protection."
    - "D-02/D-03/D-04: Workspace-only commands stop after workspace detection, while repo-aware flows can derive repo context afterward and only fail when repo identity is actually required."
    - "D-10/D-11/D-12: Optional-workspace command surfaces share one helper-backed order: explicit arg, cwd detection, then `GS_WORKSPACE_NAME` only on surfaces that already supported env fallback."
    - "D-13: Edge-case coverage stays pragmatic and implementor-led: protect known fragile resolver cases without expanding into a large exhaustive matrix."
    - "Deferred boundary remains intact: no new workspace commands, stale advisories, TUI redesign, template-composition work, or forge/source behavior changes."
  artifacts:
    - path: "src/lib/workspace-status.ts"
      provides: "Root-aware workspace CWD detection with deepest-match protections"
      contains: "detectWorkspaceFromCwd"
    - path: "src/lib/workspace-resolution.ts"
      provides: "Shared optional-workspace resolution helper used by command adapters"
      contains: "resolveOptionalWorkspace"
    - path: "tests/lib/detect-workspace-cwd.test.ts"
      provides: "Direct edge-case coverage for workspace-root matching and shared precedence"
      contains: "workspace root"
  key_links:
    - from: "src/lib/workspace-resolution.ts"
      to: "src/lib/workspace-status.ts"
      via: "Shared helper delegates cwd matching to the root-aware detector"
      pattern: "detectWorkspaceFromCwd"
    - from: "src/commands/workspace.ts"
      to: "src/lib/workspace-resolution.ts"
      via: "paths/env/pull use the shared optional-workspace resolver instead of command-local fallback drift"
      pattern: "resolveOptionalWorkspace"
    - from: "src/commands/notes.ts"
      to: "src/lib/workspace-resolution.ts"
      via: "notes preserves cwd-before-env precedence through the shared helper"
      pattern: "GS_WORKSPACE_NAME"
    - from: "src/lib/integrations/issue-utils.ts"
      to: "src/lib/workspace-resolution.ts"
      via: "integration issue commands share explicit-arg and cwd detection without gaining new env fallback behavior"
      pattern: "resolveOptionalWorkspace"
---

<objective>
Implement the shared root-aware workspace detector and unify optional-workspace resolution behavior across the in-scope command adapters.

Purpose: Phase 102 needs one canonical resolver before CLI fixture coverage is meaningful. This plan establishes the shared matcher, locks edge-case behavior, and removes command-local fallback drift from already-shipped surfaces.

Output: direct detector/regression tests, a shared optional-workspace resolver helper, and command/integration adapters wired to the shared order.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/102-workspace-root-auto-detection/102-CONTEXT.md
@.planning/phases/102-workspace-root-auto-detection/102-RESEARCH.md
@.planning/phases/102-workspace-root-auto-detection/102-PATTERNS.md
@.planning/phases/102-workspace-root-auto-detection/102-VALIDATION.md
@.planning/phases/101-completion-completeness-repair/101-CONTEXT.md
@.planning/phases/101-completion-completeness-repair/101-01-SUMMARY.md
@.planning/phases/101-completion-completeness-repair/101-02-SUMMARY.md

@src/lib/workspace-status.ts
@src/lib/env.ts
@src/lib/integrations/issue-utils.ts
@src/commands/workspace.ts
@src/commands/files.ts
@src/commands/command.ts
@src/commands/notes.ts
@tests/lib/detect-workspace-cwd.test.ts

<interfaces>
From src/lib/workspace-status.ts:
`export type CwdDetectionResult = { ok: true; workspace: Workspace } | { ok: false; error: "no_match" }`
`export function detectWorkspaceFromCwd(cwd?: string): CwdDetectionResult`

From src/lib/env.ts:
`export function detectRepoFromCwd(workspace: Workspace, cwd?: string): string | null`

From src/lib/integrations/issue-utils.ts:
`export function resolveWorkspaceArg(workspaceName: string | undefined, tracker: string, action: string): string`

From src/commands/notes.ts:
`function resolveWorkspace(explicitWorkspace?: string): string | null`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add direct red tests for workspace-root and precedence edge cases</name>
  <files>tests/lib/detect-workspace-cwd.test.ts</files>
  <read_first>
    - tests/lib/detect-workspace-cwd.test.ts
    - src/lib/workspace-status.ts
    - src/lib/integrations/issue-utils.ts
    - .planning/phases/102-workspace-root-auto-detection/102-CONTEXT.md
    - .planning/phases/102-workspace-root-auto-detection/102-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `detectWorkspaceFromCwd()` returns the workspace when `cwd` equals `{workspace_root}/tasks/{workspace}` or a nested non-repo subdirectory under that root, per D-01.
    - Test 2: Nested worktree paths still beat the broader workspace-root candidate, and prefix-collision, trunk, dir, and no-match cases remain protected per D-05/D-06/D-14.
    - Test 3: Shared optional-workspace resolution preserves explicit arg before cwd, and keeps env fallback opt-in only where already supported per D-11.
  </behavior>
  <action>
    Extend `tests/lib/detect-workspace-cwd.test.ts` with failing cases that pin the Phase 102 contract before source edits: exact workspace-root match, workspace-root subdirectory match, overlapping workspace-root prefixes, missing-worktree tolerance through the workspace root, nested worktree precedence over the broader root, and any direct helper or `resolveWorkspaceArg()` assertions needed to capture D-01, D-05, D-06, D-11, and D-14. Keep the file's existing isolated-config and mock strategy; do not replace it with a new harness or broad test-runner call.
  </action>
  <acceptance_criteria>
    - `tests/lib/detect-workspace-cwd.test.ts` contains source-visible cases for exact workspace-root cwd and non-repo workspace-subdirectory cwd.
    - The same file still proves nested worktree precedence and overlapping-prefix protection instead of replacing those guards with looser root matching.
    - `bun test tests/lib/detect-workspace-cwd.test.ts` is the red/green gate for the added Phase 102 cases.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/lib/detect-workspace-cwd.test.ts</automated>
  </verify>
  <done>
    The direct test contract fails before implementation and fully describes the required root-aware detection and fallback-order behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement root-aware detection and the shared optional-workspace helper</name>
  <files>src/lib/workspace-status.ts, src/lib/workspace-resolution.ts</files>
  <read_first>
    - src/lib/workspace-status.ts
    - src/lib/env.ts
    - src/commands/notes.ts
    - src/lib/integrations/issue-utils.ts
    - .planning/phases/102-workspace-root-auto-detection/102-RESEARCH.md
    - .planning/phases/102-workspace-root-auto-detection/102-PATTERNS.md
    - tests/lib/detect-workspace-cwd.test.ts
  </read_first>
  <behavior>
    - Test 1: Workspace root candidates are derived from `getTasksDir(config.workspace_root)` and the persisted workspace name, not from repo task-path existence.
    - Test 2: A deeper worktree `task_path` still wins over a shallower workspace-root match when both contain the current directory.
    - Test 3: Env fallback remains disabled unless the caller opts into it explicitly.
  </behavior>
  <action>
    Extend `detectWorkspaceFromCwd(cwd?)` in `src/lib/workspace-status.ts` so it considers the workspace root `join(getTasksDir(config.workspace_root), workspace.name)` and any nested path under that root per D-01, while keeping D-05/D-06 intact through separator-aware prefix checks and longest-path selection between worktree and root candidates. Create `src/lib/workspace-resolution.ts` exporting the shared helper API for optional-workspace flows, using the repo's existing TypeScript/ESM style: one helper for explicit-arg validation plus cwd detection, and an opt-in branch for `GS_WORKSPACE_NAME` that callers must request explicitly per D-10/D-11. Do not make the new helper read arbitrary env vars, auto-select repos, or broaden command scope.
  </action>
  <acceptance_criteria>
    - `src/lib/workspace-status.ts` contains root-aware detection logic that can match `{workspace_root}/tasks/{workspace}` and deeper subdirectories without regressing separator-boundary protection.
    - `src/lib/workspace-resolution.ts` exports a shared optional-workspace helper that supports explicit arg and cwd detection, with env fallback gated by an explicit option or dedicated function.
    - The implementation keeps missing-worktree tolerance by deriving workspace-root candidates from config and workspace names rather than filesystem existence of repo task directories.
    - `bun test tests/lib/detect-workspace-cwd.test.ts` passes after this task.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/lib/detect-workspace-cwd.test.ts</automated>
  </verify>
  <done>
    The repo has one root-aware detector and one shared optional-workspace helper that encode the Phase 102 precedence rules.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route in-scope command adapters through the shared resolution order</name>
  <files>src/lib/integrations/issue-utils.ts, src/commands/workspace.ts, src/commands/files.ts, src/commands/command.ts, src/commands/notes.ts</files>
  <read_first>
    - src/lib/workspace-resolution.ts
    - src/lib/integrations/issue-utils.ts
    - src/commands/workspace.ts
    - src/commands/files.ts
    - src/commands/command.ts
    - src/commands/notes.ts
    - src/lib/env.ts
    - .planning/phases/102-workspace-root-auto-detection/102-CONTEXT.md
    - .planning/phases/102-workspace-root-auto-detection/102-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Workspace-only surfaces can resolve from workspace root/subdir and stop there.
    - Test 2: Repo-aware follow-up in `workspace env` still uses `detectRepoFromCwd(ws)` only after workspace resolution succeeds.
    - Test 3: `notes` keeps explicit arg -> cwd -> `GS_WORKSPACE_NAME`, while `files`, `command`, `workspace`, and integration issue helpers do not gain new env fallback behavior.
  </behavior>
  <action>
    Replace the in-scope command-local branching with thin adapters around `src/lib/workspace-resolution.ts` for the already-shipped optional-workspace surfaces only: `workspace paths`, `workspace env`, `workspace pull`, `files status|pull|push`, `command list|run`, `notes add|list|clear`, and `resolveWorkspaceArg()` in `src/lib/integrations/issue-utils.ts`. Keep D-02/D-03 by returning workspace identity as soon as it is known for workspace-only commands. Keep D-04 by preserving repo-specific follow-up in `workspace env` through `detectRepoFromCwd(ws)` after workspace resolution, so root cwd yields workspace env without inventing repo identity. Preserve D-11 exactly: explicit arg first, cwd second, and `GS_WORKSPACE_NAME` only for `notes`, which already had that fallback.
  </action>
  <acceptance_criteria>
    - Source reads show `workspace.ts`, `files.ts`, `command.ts`, `notes.ts`, and `issue-utils.ts` delegating optional-workspace selection to `src/lib/workspace-resolution.ts` instead of re-implementing precedence separately.
    - `notes` is still the only in-scope surface in this plan that uses `GS_WORKSPACE_NAME` fallback.
    - `workspace env` still calls `detectRepoFromCwd(ws)` only after workspace resolution, so repo overlay remains conditional on repo cwd or explicit `--repo`.
    - `bun run typecheck` passes with the new helper imports and signatures.
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>
    The command adapters share one resolution policy, and repo-aware behavior is preserved without widening scope.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config-root workspace YAML -> cwd detector | Persisted workspace definitions are translated into path candidates that control which workspace a command will target. |
| shared resolver helper -> command adapters | Multiple user-facing commands trust one helper to select the right workspace and decide whether env fallback is allowed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-01 | Tampering | `src/lib/workspace-status.ts` | mitigate | Task 1 and Task 2 preserve separator-aware prefix guards and longest-path wins so `/tmp/tasks/ws-a` cannot spoof `/tmp/tasks/ws-ab`, and nested worktree paths still outrank the broader workspace root. |
| T-102-02 | Spoofing | `src/lib/workspace-resolution.ts` | mitigate | Task 2 and Task 3 centralize explicit arg -> cwd -> env ordering and keep env fallback opt-in only, preventing commands from silently selecting a workspace from `GS_WORKSPACE_NAME` when that surface never supported it. |
| T-102-03 | Information Disclosure | `src/commands/workspace.ts` | mitigate | Task 3 preserves repo overlay only through `detectRepoFromCwd(ws)` after workspace resolution, so running `git-stacks env` from a workspace root does not leak repo-scoped env for an arbitrary repo. |
| T-102-SC | Tampering | Bun/TypeScript toolchain | mitigate | No new packages are introduced; verification stays on existing `bun test` and `bun run typecheck` commands. |
</threat_model>

<verification>
- `bun test tests/lib/detect-workspace-cwd.test.ts`
- `bun run typecheck`
</verification>

<success_criteria>
- The shared detector resolves workspace roots and nested workspace subdirectories without regressing deepest repo matching, trunk skip, or prefix-collision protection.
- The repo has one shared optional-workspace helper that enforces explicit arg -> cwd -> env ordering, with env fallback only where it already existed.
- `workspace`, `files`, `command`, `notes`, and integration issue helpers stop drifting in their fallback order.
- No new command families, TUI behavior, template composition work, stale-advisory behavior, or forge/source semantics are introduced.
</success_criteria>

<output>
Create `.planning/phases/102-workspace-root-auto-detection/102-01-SUMMARY.md` when done
</output>
