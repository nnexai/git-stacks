---
phase: 81-workspace-and-git-operation-e2e-coverage
plan: 03
type: execute
wave: 2
depends_on: ["81-01"]
files_modified:
  - tests/commands/workspace-lifecycle.test.ts
  - tests/commands/workspace-guards.test.ts
autonomous: true
requirements:
  - E2E-08
  - E2E-14

must_haves:
  truths:
    - "close command removes worktrees and marks workspace as closed without deleting YAML"
    - "clean command removes worktrees but preserves workspace YAML"
    - "remove command cascades through clean → close → delete YAML"
    - "rename command updates workspace name in YAML and filesystem references"
    - "open --no-ide creates worktrees, runs hooks, and writes env files without launching integrations"
    - "Dirty repo guard prevents destructive operations (clean, remove, merge) unless --force is used"
    - "Missing task_path guard produces actionable stderr instead of crash"
    - "Dir repo is skipped correctly by worktree-only operations"
  artifacts:
    - path: "tests/commands/workspace-lifecycle.test.ts"
      provides: "open/close/clean/remove/rename side-effect and cascading E2E coverage"
      min_lines: 100
    - path: "tests/commands/workspace-guards.test.ts"
      provides: "dirty repo, missing path, dir repo, missing remote/upstream guard E2E coverage"
      min_lines: 80
  key_links:
    - from: "tests/commands/workspace-lifecycle.test.ts"
      to: "src/lib/workspace-ops.ts"
      via: "CLI subprocess exercises openWorkspace, closeWorkspace, cleanWorkspace, removeWorkspace, renameWorkspace"
      pattern: "runCli.*open|close|clean|remove|rename"
    - from: "tests/commands/workspace-guards.test.ts"
      to: "src/lib/workspace-ops.ts"
      via: "CLI subprocess triggers dirty/missing guards"
      pattern: "dirty|missing|force"
---

<objective>
Prove workspace lifecycle cascading behavior and guard protections.

Purpose: Proves that close/clean/remove/rename/open lifecycle operations produce correct filesystem and YAML side effects, and that dirty repo, missing path, and dir repo guards work correctly through the real CLI. These are the highest-risk areas for data loss — wrong cascading behavior could delete user work.

Output: `tests/commands/workspace-lifecycle.test.ts` with lifecycle cascading E2E + `tests/commands/workspace-guards.test.ts` with guard behavior E2E.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-workspace-and-git-operation-e2e-coverage/81-CONTEXT.md
@.planning/phases/81-workspace-and-git-operation-e2e-coverage/81-RESEARCH.md
@.planning/phases/81-workspace-and-git-operation-e2e-coverage/81-01-SUMMARY.md
@.planning/codebase/TESTING.md
@CLAUDE.md
@tests/helpers.ts

<interfaces>
<!-- Shared helpers from Plan 01 (tests/helpers.ts) -->

From tests/helpers.ts:
```typescript
export function runCli(cfgDir: string, args: string[], opts?: { env?: Record<string, string>; cwd?: string }): { stdout: string; stderr: string; exitCode: number }
export function makeRepoWithRemote(baseDir: string, repoName: string, wsBranch: string): { originDir: string; mainPath: string; taskPath: string }
export function makeWorkspaceFixture(cfgDir: string, wsName: string, repos: Array<{...}>, opts?: {...}): string
export function makeTmpDir(prefix?: string): string
export function cleanup(dir: string): void
export function applyTestGitEnv(baseDir: string): () => void
export function makeGitRepo(base: string, name?: string): string
export function gitExecOptions(cwd: string, baseDir?: string): ExecSyncOptions
```

<!-- Lifecycle cascading from CLAUDE.md -->
```
remove → clean → close (cascading)
merge → clean → close (cascading)
```

<!-- Workspace command surface -->
From src/commands/workspace.ts:
```
close <workspace>                       — removes worktrees, marks closed
clean [workspace] [--gone] [--force] [--dry-run]  — removes worktrees, preserves YAML
remove <workspace> [--force] [--dry-run]           — full deletion cascade
rename <workspace> <new-name> [--force] [--dry-run] — name update
open <workspace> [--no-ide] [--recreate] [--force]   — create worktrees, hooks, env
```

<!-- Guard behavior from workspace-ops.ts -->
```
- Dirty repo check: isRepoDirty(taskPath) → blocks clean/remove/merge unless --force
- Missing task_path: existsSync check → skip or error
- Dir repos: mode === "trunk" → skip worktree operations
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create workspace lifecycle cascading E2E tests</name>
  <files>tests/commands/workspace-lifecycle.test.ts</files>
  <read_first>
    - tests/helpers.ts (all shared helpers)
    - src/commands/workspace.ts (close action ~line 265, clean action ~line 416, remove action ~line 552, rename action ~line 787, open action ~line 156)
    - src/lib/workspace-ops.ts (openWorkspace, closeWorkspace, cleanWorkspace, removeWorkspace, renameWorkspace — understand side effects)
    - src/lib/config.ts (readWorkspace, workspaceExists, deleteWorkspace — understand YAML state)
  </read_first>
  <action>
Create `tests/commands/workspace-lifecycle.test.ts` to prove lifecycle command side effects through the real CLI.

**Import setup:**
```typescript
import { describe, test, expect, beforeEach, afterEach } from "bun:test"
import { existsSync, readFileSync, writeFileSync } from "fs"
import { join } from "path"
import { execSync } from "child_process"
import {
  makeTmpDir, cleanup, applyTestGitEnv, makeGitRepo,
  makeRepoWithRemote, makeWorkspaceFixture, runCli, gitExecOptions,
} from "../helpers"
```

Standard beforeEach/afterEach with tmpDir, cfgDir, git env isolation. Build a workspace fixture with `makeRepoWithRemote` for realistic git state.

**describe("open --no-ide")** — Proves open creates worktrees and runs hooks without launching integrations.

1. **"open --no-ide succeeds and worktree exists after open"** — Build fixture with workspace referencing a repo. Verify worktree exists at task_path. Run `runCli(cfgDir, ["open", wsName, "--no-ide"])`. Verify exit code 0. Verify the repo's task_path directory still exists and is a git worktree.

2. **"open --no-ide runs post_open hooks"** — Build fixture with `hooks.post_open` set to a probe script. Run `open --no-ide`. Verify probe artifact file was created (proves hook ran).

3. **"open --no-ide does not launch external integrations"** — Verify stderr does not contain integration-related output (e.g., "vscode", "tmux", "aerospace", "niri"). This is a negative assertion.

**describe("close")** — Proves close behavior.

4. **"close removes worktree directory but YAML file still exists"** — Run `runCli(cfgDir, ["close", wsName])`. Verify the worktree at task_path was removed (or pruned). Verify the workspace YAML file at `cfgDir/workspaces/{wsName}.yml` still exists. Note: `close` in this CLI removes worktrees per closeWorkspace in workspace-ops.ts — read the actual implementation to understand what "close" does (it may just prune worktrees without deleting them; adjust assertions accordingly).

5. **"close exits 0 when workspace has no worktrees"** — Close an already-closed workspace (one where task_path doesn't exist). Verify exit code 0 (idempotent).

**describe("clean")** — Proves clean behavior.

6. **"clean removes worktree directories but preserves workspace YAML"** — Run `runCli(cfgDir, ["clean", wsName])`. Verify worktree directory removed. Verify workspace YAML still exists.

7. **"clean --dry-run shows what would be removed without removing"** — Run `runCli(cfgDir, ["clean", wsName, "--dry-run"])`. Verify worktree directory still exists. Verify stdout mentions the workspace.

**describe("remove")** — Proves remove cascading.

8. **"remove deletes worktrees and workspace YAML"** — Run `runCli(cfgDir, ["remove", wsName, "--force"])`. Verify worktree removed. Verify workspace YAML file deleted. Note: use `--force` to bypass dirty checks since we want to test the removal path, not the guard.

9. **"remove --dry-run shows what would be removed without removing"** — Run `runCli(cfgDir, ["remove", wsName, "--dry-run"])`. Verify workspace YAML and worktree still exist. Verify stdout describes what would be removed.

10. **"workspace no longer appears in list after remove"** — After successful remove, run `runCli(cfgDir, ["list"])`. Verify stdout does not contain the workspace name.

**describe("rename")** — Proves rename behavior.

11. **"rename updates workspace name in YAML"** — Run `runCli(cfgDir, ["rename", wsName, "new-name"])`. Verify old YAML file gone (or renamed). Verify new YAML file exists with `name: new-name`. Read new YAML and verify it contains `name: new-name`.

12. **"renamed workspace appears under new name in list"** — After rename, run `runCli(cfgDir, ["list"])`. Verify new name appears, old name does not.

13. **"rename --dry-run shows preview without renaming"** — Run `runCli(cfgDir, ["rename", wsName, "new-name", "--dry-run"])`. Verify original YAML unchanged.

**Implementation notes:**
- Read the actual close/clean/remove/rename implementations in `workspace-ops.ts` carefully. The exact side effects (e.g., does `close` delete worktree dirs or just `git worktree remove`?) determine what to assert. Adjust filesystem assertions based on actual behavior.
- Use `--force` for remove/clean when testing the "happy path" removal, to avoid dirty-repo guards blocking the test (guards are tested in Task 2).
- Check if `rename` changes the YAML filename. Some implementations rename to `{new-name}.yml`, others keep the old filename and just change the `name` field inside. Verify actual behavior.
  </action>
  <verify>
    <automated>cd /home/nnex/dev/prj/git-stacks && bun test tests/commands/workspace-lifecycle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All tests pass in workspace-lifecycle.test.ts
    - open --no-ide creates worktrees and runs hooks without integration launch
    - close/clean preserve YAML, remove worktrees
    - remove cascades and deletes YAML
    - rename updates workspace name correctly
    - dry-run flags produce preview without side effects
    - `bun run test:integ` passes
  </acceptance_criteria>
  <done>Lifecycle E2E tests green; open/close/clean/remove/rename side effects and cascading proven through real CLI.</done>
</task>

<task type="auto">
  <name>Task 2: Create guard behavior E2E tests</name>
  <files>tests/commands/workspace-guards.test.ts</files>
  <read_first>
    - tests/helpers.ts (shared helpers)
    - src/lib/workspace-ops.ts (search for "dirty", "force", "isRepoDirty", "existsSync" — understand guard logic)
    - src/lib/workspace-git.ts (search for "dirty", "remote", "upstream" — understand git operation guards)
    - src/commands/workspace.ts (clean, remove, merge, sync, push — understand flag wiring for --force)
    - .planning/phases/81-workspace-and-git-operation-e2e-coverage/81-CONTEXT.md (D-06 — real git state, not mocks)
  </read_first>
  <action>
Create `tests/commands/workspace-guards.test.ts` to prove guard behavior for dirty repos, missing paths, dir repos, and missing remotes/upstreams (per D-06).

**Import setup:**
```typescript
import { describe, test, expect, beforeEach, afterEach } from "bun:test"
import { writeFileSync, existsSync, mkdirSync } from "fs"
import { join } from "path"
import { execSync } from "child_process"
import {
  makeTmpDir, cleanup, applyTestGitEnv, makeGitRepo,
  makeRepoWithRemote, makeWorkspaceFixture, runCli, gitExecOptions,
} from "../helpers"
```

Standard beforeEach/afterEach.

**describe("dirty repo guards")** — Proves dirty check prevents destructive ops.

1. **"clean refuses when repo is dirty"** — Build workspace fixture with `makeRepoWithRemote`. Add an uncommitted file to the task_path: `writeFileSync(join(taskPath, "dirty.txt"), "uncommitted")`. Run `runCli(cfgDir, ["clean", wsName])`. Verify non-zero exit code. Verify stderr contains "dirty" or similar warning. Verify worktree still exists (not deleted).

2. **"clean --force overrides dirty check"** — Same dirty setup. Run `runCli(cfgDir, ["clean", wsName, "--force"])`. Verify exit code 0. Verify worktree removed.

3. **"remove refuses when repo is dirty"** — Same dirty setup. Run `runCli(cfgDir, ["remove", wsName])`. Verify non-zero exit code. Verify workspace YAML still exists.

4. **"remove --force overrides dirty check"** — Dirty setup. Run `runCli(cfgDir, ["remove", wsName, "--force"])`. Verify exit code 0, YAML deleted.

**describe("missing task_path guards")** — Proves commands handle missing directories gracefully.

5. **"status handles missing task_path without crashing"** — Build workspace fixture where task_path points to a non-existent directory (don't create the git repo). Run `runCli(cfgDir, ["status", wsName, "--json"])`. Verify exit code 0 and JSON has `exists: false` for the repo.

6. **"clean handles missing task_path gracefully"** — Build fixture with non-existent task_path. Run `runCli(cfgDir, ["clean", wsName, "--force"])`. Verify exit code 0 (nothing to clean).

7. **"remove handles missing task_path gracefully"** — Build fixture with non-existent task_path. Run `runCli(cfgDir, ["remove", wsName, "--force"])`. Verify exit code 0 and YAML deleted.

**describe("dir repo behavior")** — Proves dir repos (trunk mode) are handled differently.

8. **"status shows dir repo with mode trunk"** — Build workspace fixture with a repo in `mode: "trunk"` where task_path === main_path. Run `runCli(cfgDir, ["status", wsName, "--json"])`. Verify JSON shows `mode: "trunk"` for that repo.

9. **"clean skips trunk-mode repos (no worktree to remove)"** — Build fixture with trunk-mode repo. Run `runCli(cfgDir, ["clean", wsName, "--force"])`. Verify the trunk-mode repo directory still exists (it should NOT be deleted since it's the main clone, not a worktree).

**describe("non-existent workspace guards")** — Proves commands handle missing workspaces.

10. **"status for non-existent workspace returns error"** — Run `runCli(cfgDir, ["status", "ghost-ws", "--json"])`. Verify non-zero exit code and stderr mentions "not found" or similar.

11. **"close for non-existent workspace returns error"** — Run `runCli(cfgDir, ["close", "ghost-ws"])`. Verify non-zero exit code.

12. **"remove for non-existent workspace returns error"** — Run `runCli(cfgDir, ["remove", "ghost-ws"])`. Verify non-zero exit code.

**Implementation notes:**
- Per D-06, use real git state for dirty-repo tests. Actually write a file to the worktree to make it dirty. Do NOT mock `isRepoDirty`.
- For "missing task_path" tests, create the workspace YAML pointing to a directory that doesn't exist on disk.
- For trunk-mode tests, create a real git repo at mainPath and set taskPath === mainPath in the workspace YAML.
- Read the actual guard messages in workspace-ops.ts stderr output to know what string to check for in stderr assertions.
  </action>
  <verify>
    <automated>cd /home/nnex/dev/prj/git-stacks && bun test tests/commands/workspace-guards.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All tests pass in workspace-guards.test.ts
    - Dirty repo guard proven with real uncommitted files (D-06)
    - --force override proven for clean and remove
    - Missing task_path handled gracefully without crash
    - Dir repo (trunk mode) not erroneously deleted by clean
    - Non-existent workspace produces error exit code and message
    - `bun run test:integ` passes
  </acceptance_criteria>
  <done>Guard E2E tests green; dirty repo, missing path, dir repo, and non-existent workspace guards proven through real CLI with real git state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → CLI subprocess | Tests spawn CLI with controlled env and dirty/missing fixture state |
| CLI → Filesystem | Clean/remove operations delete files in isolated temp dirs only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-07 | Tampering | workspace YAML in temp dir | accept | Test fixtures are ephemeral; no production YAML touched |
| T-81-08 | Denial of Service | force-remove operations | accept | Operations target isolated temp dirs; no real workspace data at risk |
</threat_model>

<verification>
```bash
# Lifecycle tests pass
bun test tests/commands/workspace-lifecycle.test.ts

# Guard tests pass
bun test tests/commands/workspace-guards.test.ts

# No regression
bun run test:integ
```
</verification>

<success_criteria>
- workspace-lifecycle.test.ts passes with ≥10 test cases covering open/close/clean/remove/rename and dry-run
- workspace-guards.test.ts passes with ≥10 test cases covering dirty, missing path, dir repo, and non-existent workspace
- No mocks for git state — all guards proven against real filesystem state (D-06)
- Lifecycle cascading (remove → clean → close) proven through side-effect assertions
- bun run test:integ green
</success_criteria>

<output>
After completion, create `.planning/phases/81-workspace-and-git-operation-e2e-coverage/81-03-SUMMARY.md`
</output>
