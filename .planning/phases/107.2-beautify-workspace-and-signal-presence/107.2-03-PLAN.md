---
phase: 107.2-beautify-workspace-and-signal-presence
plan: "03"
type: execute
wave: 3
depends_on: ["107.2-01", "107.2-02"]
files_modified:
  - native/linux/attention_view.zig
  - native/linux/workspace_view.zig
  - native/linux/app.zig
  - native/linux/application.zig
  - native/tests/attention_test.zig
  - native/tests/application_actions_test.zig
  - native/tests/accessibility_test.zig
  - scripts/verify-native.ts
autonomous: true
requirements: [VIS-01, VIS-03, VIS-04, VIS-05]
must_haves:
  truths:
    - "Final 107.1 provider sessions drive per-session badges, awaiting precedence, activity markers, and unread notifications without a parallel transport."
    - "The inbox separates Needs attention from Recent activity, uses canonical lifecycle/copy/location/time hierarchy, and keeps completed/idle activity non-unread."
    - "Focus and dismissal use service-authoritative final signal actions, revalidate stale identity, preserve failed rows, and expose specified recovery actions."
  artifacts:
    - path: "native/linux/attention_view.zig"
      provides: "Final signal-to-inbox grouping, copy, action, and accessibility projections"
    - path: "native/tests/attention_test.zig"
      provides: "Final activity/notification grouping, ordering, failure, focus, and dismissal goldens"
    - path: "native/linux/app.zig"
      provides: "Provider badges, activity/unread markers, scoped inbox, and retry/fallback GTK bindings"
  key_links:
    - "Final 107.1 signal identities feed workspace_view and attention_view; no local attention store or presentation-owned read mutation exists."
    - "Inbox focus/dismiss actions pass exact service signal/workspace/repository/surface/session identity through application.zig and preserve explicit failures."
---

<objective>
Bind the finished Phase 107.1 signal model to provider presence, activity/unread workspace markers, and the canonical signal inbox.

Purpose: Complete the narrow cross-phase dependency without freezing transitional names or creating another signal path.
Output: Final signal projection adapter, provider/session row state, grouped inbox widgets/actions, and production-shaped fixtures.

Execution boundary: execute-phase may run Plans/Waves 01-02 early. Plan 03 must fail closed before source changes unless the 107.1-03 and 107.1-04 summaries exist and the unified-signal forbidden-symbol release fence passes.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/107.2-beautify-workspace-and-signal-presence/107.2-UI-SPEC.md
@.planning/phases/107.2-beautify-workspace-and-signal-presence/107.2-PATTERNS.md
@.planning/phases/107.1-unify-workspace-signals-and-retire-legacy-messages/107.1-03-SUMMARY.md
@.planning/phases/107.1-unify-workspace-signals-and-retire-legacy-messages/107.1-04-SUMMARY.md
@.planning/phases/107.2-beautify-workspace-and-signal-presence/107.2-02-SUMMARY.md
</context>

## Artifacts this phase produces

- Durable final-signal adapters that populate provider-session badges, awaiting/activity/unread row inputs, and scoped inbox groups.
- `NeedsAttention`/`RecentActivity` inbox projections with canonical copy, relative time, exact location, focus/dismiss/retry/fallback actions, and accessible descriptions.
- Production-shaped final signal fixtures replacing legacy attention fixtures in relevant 107.2 test coverage.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project final provider sessions and signal markers into workspace rows</name>
  <files>native/linux/workspace_view.zig, native/linux/app.zig, native/tests/attention_test.zig, native/tests/workspace_ui_test.zig</files>
  <read_first>.planning/phases/107.1-unify-workspace-signals-and-retire-legacy-messages/107.1-03-SUMMARY.md; .planning/phases/107.1-unify-workspace-signals-and-retire-legacy-messages/107.1-04-SUMMARY.md; final signal model/reducer identifiers; 107.2 UI-SPEC Provider Presence and Activity/Notification sections; Plan 01 row interfaces; PATTERNS.md fence; 107.1-06 Plan Task 2 release-fence contract.</read_first>
  <behavior>
    - One badge appears per active provider session, awaiting sessions sort first, three display before +N, and duplicate providers remain separate sessions.
    - Activity and notification remain independent; completion/idle becomes history and never unread.
    - Marker activation opens the workspace-scoped signal inbox with exact stable identity.
  </behavior>
  <action>Before any implementation edit, require both 107.1-03-SUMMARY.md and 107.1-04-SUMMARY.md, then run the focused verify-gates test and release gate so final signal symbols and the path-aware transitional-symbol fence are proven current; stop Plan 03 without modifying source if any artifact or command fails. Adapt only the final service/native signal state established by those summaries into Plan 01's durable semantic row inputs. Populate badge counts by final provider-session identity and exact scope, not provider label alone; preserve awaiting precedence and letter/symbolic fallback unless locally owned redistributable artwork already exists. Populate activity and notification independently and bind notification activation to the scoped inbox. Replace relevant legacy fixtures with final activity/notification fixtures. Do not preserve transitional transport/action/callback identifiers listed in PATTERNS.md, create a second event decoder/store, infer signals from terminal text, or mark anything read merely by rendering.</action>
  <acceptance_criteria>
    - Tests cover four-plus sessions, duplicate providers, awaiting precedence, simultaneous activity/unread, completion history, and exact workspace scoping.
    - No new provider integration, signal transport, local dismissed-ID store, or presentation lifecycle authority exists.
    - Badge and marker accessible output enumerates provider counts, awaiting, activity, and unread separately.
  </acceptance_criteria>
  <verify><automated>test -f .planning/phases/107.1-unify-workspace-signals-and-retire-legacy-messages/107.1-03-SUMMARY.md && test -f .planning/phases/107.1-unify-workspace-signals-and-retire-legacy-messages/107.1-04-SUMMARY.md && bun test tests/lib/verify-gates.test.ts && bun run verify:gates && bun run native:test:attention && bun run native:test:workspace-ui</automated></verify>
  <done>Final signal state enriches workspace rows through one bounded semantic adapter with no transitional contract frozen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the canonical grouped signal inbox and authoritative actions</name>
  <files>native/linux/attention_view.zig, native/linux/app.zig, native/linux/application.zig, native/tests/attention_test.zig, native/tests/application_actions_test.zig, native/tests/accessibility_test.zig, scripts/verify-native.ts</files>
  <read_first>107.2 UI-SPEC Signal Inbox exact copy/actions and accessibility sections; final 107.1 signal focus/dismiss effects; current pure inbox projection and application action registry; PATTERNS.md error/action handling.</read_first>
  <behavior>
    - Needs attention contains awaiting/failed sessions and unread notifications; Recent activity contains working/completed/idle history without unread mutation.
    - Rows expose provider/lifecycle, title, detail, exact location, relative time, Focus terminal, and notification-only dismissal.
    - Stale focus falls back to valid workspace/repository with specified copy; dismiss failure keeps the row visible and offers retry.
    - Loading, reconnecting, degraded, failure, empty, and filtered-empty states use the exact UI-SPEC copy and recovery actions.
  </behavior>
  <action>Refactor the pure inbox projection over final signals into separate Needs attention and Recent activity groups, deterministic lifecycle language, relative timestamps, provider identity, and exact workspace/repository/surface location. Bind GTK rows/actions through the final 107.1 service-authoritative focus and notification-dismiss effects. Revalidate stale identities before focus, expose the documented nearest valid fallback, keep dismiss failures visible, and implement Retry signals, integration health, connection details, clear filters, and retry dismissal actions exactly where specified. Activity is never manually dismissed unless the final signal is a notification. Extend the existing attention/accessibility verifier modes rather than creating another harness.</action>
  <acceptance_criteria>
    - Golden tests cover all exact empty/error/recovery copy, grouping, lifecycle, timestamps, provider/location hierarchy, stale focus fallback, and dismissal retry.
    - Focus/dismiss tests prove exact final signal and surface identity reaches the authoritative action; rendering alone performs no mutation.
    - Keyboard, mouse, screen-reader role/state/description, and scoped marker activation contracts pass.
  </acceptance_criteria>
  <verify><automated>bun run native:test:attention && bun run native:test:application-actions && bun run native:test:accessibility</automated></verify>
  <done>The production inbox is a calm accessible projection over final signals with exact authoritative actions and recoverable failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signal reducer to presentation/actions | Service-derived identity controls focus and notification dismissal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-107.2-05 | Spoofing | focus action | high | mitigate | Revalidate final signal, workspace, repository, session, and surface identity before exact focus/fallback |
| T-107.2-06 | Tampering | notification dismissal | medium | mitigate | Service-authoritative acknowledgement, failed-row retention, and replay convergence tests |
</threat_model>

<verification>Run focused attention, workspace UI, app-action, and accessibility modes; run `bun run typecheck && bun run native:verify` at wave completion.</verification>
<success_criteria>The final 107.1 signal model is the sole source of provider, activity, notification, and inbox semantics.</success_criteria>
<output>Create `.planning/phases/107.2-beautify-workspace-and-signal-presence/107.2-03-SUMMARY.md` when done.</output>
