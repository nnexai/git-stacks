---
phase: 107.2-beautify-workspace-and-signal-presence
status: human-verification-pending
automated_verified: 2026-07-13
---

# Phase 107.2 Verification

## Automated readiness

The Phase 107.1 verification prerequisite exists and records approved production Linux UAT. The current Phase 107.2 implementation passes the repository's isolation-aware release suite:

- `bun run test`: PASS — unit suite PASS; 83/83 isolated integration files PASS.
- `bun run typecheck`: PASS.
- `bun run test:deps`: PASS — 133 files processed; no circular dependency.
- `bun run native:verify`: PASS — production graph/ABI/model, workspace UI, attention/actions, accessibility templates, 25-cycle stress, GTK, terminal, multisurface, and real workspace lifecycle lanes.
- `bun run verify:gates`: PASS — inventory, mapped tests, coverage artifacts, and the path-aware forbidden transitional-symbol fence are aligned.
- `git diff --check`: PASS.

Plan 107.2-04 spelled the TypeScript step as raw `bun test`. That bypasses `scripts/test-runner.ts` and loads every `mock.module` test into one process-global registry; it is not the repository's full-suite command and produced broad order-dependent mock pollution. The durable full-suite command is `bun run test`, matching `package.json` and the completed 107.1 verification. The concrete resolver-cache leak exposed by the first run was repaired in commit `6c20a706`: its fixture now resets the real config index together with its YAML directory. Focused evidence is 24/24 passing tests.

## Requirement evidence

| Requirement | Current production source | Automated evidence | Status |
| --- | --- | --- | --- |
| VIS-01 | `src/lib/workspace-status.ts` and `src/lib/service/snapshot.ts` own bounded Git/default/remote/optional PR state; `native/linux/service_client.zig` decodes it atomically; `native/linux/workspace_view.zig` projects stable PairKey rows and final per-session provider badges. | workspace-status/snapshot/contract suites; native service-client and workspace-ui suites; full native verification | Automated PASS; visual judgment pending |
| VIS-02 | Additions/removals are service-observed text-line totals; each repository carries its own default branch; missing/degraded/remote and absent optional PR/check remain explicit rather than fabricated. | `tests/lib/workspace-status.test.ts`, `tests/lib/service/snapshot.test.ts`, native service-client and workspace-ui goldens | Automated PASS; live missing/degraded observation pending |
| VIS-03 | `native/linux/workspace_view.zig` implements Pinned/Active/ordinary deduplication, all ten relevance priorities, alphabetical ties, origin counts, exact selection, wide/medium/narrow/200% compression, awaiting-first duplicate-preserving badges, and redundant descriptions. `native/linux/app.zig` binds semantic GTK classes, logical layout, selection/focus, theme tokens, and the native animation setting. | native workspace-ui and accessibility suites; production GTK, multisurface, and stress lanes | Contract PASS; themes, allocation, RTL, motion comfort, and hierarchy pending human observation |
| VIS-04 | `native/linux/attention_view.zig` projects `Needs attention` and `Recent activity`, exact recovery copy, lifecycle/location/time hierarchy, and notification-only dismissal. `native/linux/app.zig`, `application.zig`, and `service_client.zig` bind exact focus plus authenticated service-authoritative dismissal. | native attention, application-actions, accessibility, ABI, and lifecycle suites; forbidden-symbol gate | Automated PASS; real provider/reconnect/focus/dismiss routing pending human observation |
| VIS-05 | GTK rows/actions expose button roles, selected/checked state, accessible labels/descriptions, tooltips, keyboard-equivalent actions, static reduced-motion meaning, and deterministic fallback/retry actions. | native accessibility templates, workspace-ui, attention, application-actions, GTK smoke | Contract PASS; keyboard/mouse/screen-reader behavior pending human observation |

## Detailed contract audit

- Service metadata: Git probing remains outside GTK; additions/removals, per-repository default branch, missing/degraded/remote, and optional PR/check are bounded service fields.
- Sidebar ordering: all ten Active priorities are golden-tested; PairKey deduplication, alphabetical ties, exact selection, and Pinned/Active origin summaries are deterministic.
- Compression: medium removes the secondary line; narrow/200% collapses PR and provider detail before Git while retaining title, awaiting, unread, tooltip, and full accessible description.
- Signals: duplicate provider sessions remain distinct, awaiting sorts first, activity and unread remain independent, and completed activity routes to Recent activity.
- Actions: focus carries the exact final signal key; notification dismissal uses the authenticated service and does not mutate presentation until authoritative convergence; failed dismissal retains the row.
- Presentation: libadwaita/GTK semantic tokens drive selected active/inactive, focus, Git, warning, and activity classes; logical GTK layout is RTL-capable; `gtk-enable-animations` selects animated versus equivalent static activity state.
- Transitional boundary: release gates reject retired message/structured-attention transport symbols; no transitional 107.1 symbol is treated as a durable contract.

## Canonical references and evidence limits

- `https://supacode.sh/screenshot.png` was reachable on 2026-07-13 and remains a hierarchy/density/readability reference only.
- The signed GitHub private-image URL embedded in Plan 04 could not be reopened by the browser safety layer. No fresh inspection is claimed.
- Automated GTK smoke proves production composition, rendering, input, accessibility contracts, and clean lifecycle. It does not prove perceived calmness, real allocation at every theme/scale, assistive announcements, animation comfort, or production provider routing.
- No screenshot or synthetic harness is recorded as human interaction proof. App-window-only screenshots and all observation outcomes remain pending at the blocking checkpoint.

## Production GTK checkpoint

Environment: Linux Wayland (`WAYLAND_DISPLAY=wayland-1` during automated GTK verification), production `git-stacks-native`, managed authenticated service, repository build from the current checkout.

Automated launch command: `bun run native:run`. This builds the production artifact, starts the managed authenticated service, and keeps both alive until the application exits.

Launch status on 2026-07-13: READY. The production GTK process is live against a fresh authenticated managed-service descriptor. A stale `startup.lock` left by an interrupted earlier test process was removed only after confirming that no service/native process or usable descriptor existed; the subsequent managed launch succeeded. The live surface uses the current configured workspaces and real service data. Dense/theme/scale/signal states must be established and observed during the checkpoint; no fixture-only state is represented as production interaction proof.

- [ ] Dense hierarchy across light, dark, high contrast, selected active/inactive, and system accent.
- [ ] Wide/medium/narrow, 200% text, long-name ellipsis, and RTL allocation.
- [ ] Keyboard, mouse, and screen-reader roles, states, descriptions, focus order, scope, actions, retry, and fallback.
- [ ] Motion enabled and native animations disabled with equivalent static meaning.
- [ ] Real duplicate-provider, awaiting, notification, completion, reconnect, exact focus, and durable dismissal convergence.
- [ ] Missing/degraded repository and absent PR/check behavior.

No item above is approved by automation. Type `approved` only after every live observation passes; otherwise record each remaining issue.
