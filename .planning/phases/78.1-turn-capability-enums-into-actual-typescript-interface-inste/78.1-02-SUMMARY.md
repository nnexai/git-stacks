---
phase: 78.1
plan: 02
subsystem: integrations
tags: [runner, plugins, predicates, intersections]
requirements_completed: [ENGN-07, ENGN-08]
---

# Phase 78.1 Plan 02 Summary

Updated every first-party integration plugin and the integration runner to use structural capability typing instead of a parallel `capabilities` Set.

## Accomplishments

- Removed `capabilities: new Set(...)` and `type Capability` imports from all 10 integration plugins
- Tightened each plugin export to the expected intersection type (for example `Integration & Generates & HasCommands & HasConfigExample`)
- Rewrote `src/lib/integrations/runner.ts` to gate `generate`, `cleanup`, `applies`, and `windowDetector` through the new type predicates
- Brought direct source consumers along with the refactor so `bun run typecheck` returned to green

## Verification

- No production file under `src/lib/integrations/` still declares `capabilities: new Set(`
- `src/lib/integrations/runner.ts` contains no `.capabilities` references and no non-null assertions on integration dispatch
- `bun run typecheck` exits 0

## Deviations from Plan

- **[Rule 3 - Blocking] Updated downstream source consumers during the runner migration** — `src/commands/integration.ts` and `src/tui/dashboard/WorkspaceDetail.tsx` still assumed capability members were visible on the registry union, so they were updated in the same execution pass to keep the type boundary coherent and restore green typechecking.

## Next Phase Readiness

- Plan 03 can now remove the CLI capability surface, update the test fixtures, and finish the planning-artifact cleanup on top of a green build
