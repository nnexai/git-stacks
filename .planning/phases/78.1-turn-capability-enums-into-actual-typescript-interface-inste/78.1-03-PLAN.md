---
phase: 78.1
plan: 03
type: execute
wave: 3
depends_on:
  - 78.1-02
files_modified:
  - src/commands/integration.ts
  - tests/lib/integrations/runner.test.ts
  - tests/lib/integrations/window-detector.test.ts
  - tests/lib/integration-commands.test.ts
  - tests/tui/workspace-wizard.test.ts
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - ENGN-09
must_haves:
  truths:
    - "git-stacks integration list (table mode) does not contain the string 'Capabilities'"
    - "git-stacks integration list --json does not contain the key 'capabilities' on any row"
    - "TAG_MAP constant is deleted from src/commands/integration.ts"
    - "All test fakes have removed `capabilities: new Set(...)` literals"
    - "tests/lib/integration-commands.test.ts no longer has the 'Phase 76-02: capabilities column' test block"
    - "REQUIREMENTS.md ENGN-07/08/09 are rewritten in place to reflect narrow-interface contracts (still marked Complete)"
    - "STATE.md has a new Phase 78.1 decisions section explaining the supersession of Phase 76 D-01..D-07"
    - "bun run typecheck and bun run test both exit 0"
  artifacts:
    - path: "src/commands/integration.ts"
      provides: "integration list command without Capabilities column or capabilities JSON field"
      contains: "integration list"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated ENGN-07/08/09 acceptance criteria"
      contains: "ENGN-07"
    - path: ".planning/STATE.md"
      provides: "Phase 78.1 decisions section noting supersession of Phase 76 capabilities design"
      contains: "Phase 78.1"
  key_links:
    - from: "src/commands/integration.ts"
      to: "git-stacks integration list table output"
      via: "console.log"
      pattern: "integration list"
---

<objective>
Complete Phase 78.1 by removing the Capabilities column from `git-stacks integration list` (per D-09, D-10, D-11), updating four test files to drop `capabilities: new Set(...)` literals from fake integrations and deleting the obsolete "Phase 76-02: capabilities column" test block (per D-17), rewriting REQUIREMENTS.md ENGN-07/08/09 in place to reflect the narrow-interface design (per D-13), and appending a Phase 78.1 decisions section to STATE.md explaining the supersession of Phase 76 D-01..D-07 (per D-15).

Purpose: Erase the user-facing surface introduced by Phase 76 (the Capabilities column was unreleased — see D-16) and update the audit trail (REQUIREMENTS, STATE) to reflect the new design without leaving stale text. Phase 76 historical artifacts are NOT touched (per D-14).

Output: Updated CLI command, four updated test files, updated REQUIREMENTS.md, and a new STATE.md decision section. After this plan, both `bun run typecheck` AND `bun run test` exit 0, and `git-stacks integration list` shows no capabilities column.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/78.1-turn-capability-enums-into-actual-typescript-interface-inste/78.1-CONTEXT.md
@.planning/phases/78.1-turn-capability-enums-into-actual-typescript-interface-inste/78.1-01-PLAN.md
@.planning/phases/78.1-turn-capability-enums-into-actual-typescript-interface-inste/78.1-02-PLAN.md
@CLAUDE.md
@src/commands/integration.ts
@.planning/REQUIREMENTS.md
@.planning/STATE.md

<interfaces>
After Plans 01 and 02, the following are true:
- `Capability` type and `capabilities` field are GONE from `src/lib/integrations/types.ts`
- All 10 plugins have intersection types and no `capabilities: new Set(...)` literal
- `runner.ts` uses type predicates with zero non-null assertions
- `bun run typecheck` exits 0

Current state of `src/commands/integration.ts` lines 7-14 (TAG_MAP — delete entirely):

```typescript
const TAG_MAP: Record<string, string> = {
  generate: 'gen',
  cleanup: 'clean',
  commands: 'cmd',
  configExample: 'cfg',
  windowDetection: 'win',
  applies: 'apl',
}
```

Current state of `src/commands/integration.ts` lines 32-39 (JSON branch — drop `capabilities` field):

```typescript
const jsonRows = integrations.map((i) => {
  const enabled = ws
    ? resolveEnabled(i.id, i.enabledByDefault, { workspace: ws, config: globalConfig, tasksDir: "" })
    : resolveEnabledGlobally(i.id, i.enabledByDefault, globalConfig)
  const configured = Object.keys(globalConfig.integrations[i.id] ?? {}).length > 0
  return { id: i.id, label: i.label, enabled, configured, capabilities: [...i.capabilities] }
})
```

Current state of `src/commands/integration.ts` lines 42-56 (table branch — drop column):

```typescript
const rows = integrations.map((i) => {
  const enabled = ws
    ? resolveEnabled(i.id, i.enabledByDefault, { workspace: ws, config: globalConfig, tasksDir: "" })
    : resolveEnabledGlobally(i.id, i.enabledByDefault, globalConfig)
  const configured = Object.keys(globalConfig.integrations[i.id] ?? {}).length > 0
  const caps = [...i.capabilities].map((c) => TAG_MAP[c] ?? c).sort().join(' ')
  return { id: i.id, label: i.label, enabled, configured, caps }
})
console.log(`${"ID".padEnd(14)} ${"Label".padEnd(18)} ${"Enabled".padEnd(9)} ${"Configured".padEnd(12)} Capabilities`)
console.log(`${"─".repeat(14)} ${"─".repeat(18)} ${"─".repeat(9)} ${"─".repeat(12)} ${"─".repeat(20)}`)
for (const row of rows) {
  const enabledStr = row.enabled ? "yes" : "no"
  const configuredStr = row.configured ? "yes" : "no"
  console.log(`${row.id.padEnd(14)} ${row.label.padEnd(18)} ${enabledStr.padEnd(9)} ${configuredStr.padEnd(12)} ${row.caps}`)
}
```

Tests with `capabilities: new Set(...)` to fix:
- `tests/lib/integrations/runner.test.ts` — fake integrations (multiple) on lines 32, 49, 65, 83, 94, 106, 117
- `tests/lib/integrations/window-detector.test.ts` — fakes on lines 32, 47, 60
- `tests/lib/integration-commands.test.ts` — `Phase 76-02: capabilities column` describe block lines 285-315 (entire block deleted)
- `tests/tui/workspace-wizard.test.ts` — fakes on lines 53, 66

REQUIREMENTS.md ENGN-07/08/09 currently say (lines 28-30):
```
- [x] **ENGN-07**: Each integration plugin declares its capabilities via a typed `capabilities` field
- [x] **ENGN-08**: Integration runner uses capability declarations instead of optional chaining to gate calls
- [x] **ENGN-09**: `integration list` displays plugin capabilities
```

STATE.md current Phase 76 entries (lines 56-58 in Accumulated Context "Decisions" section) — NOT to be touched per D-15:
```
- [Phase 76]: capabilities is required (not optional) on Integration interface ...
- [Phase 76]: Runner uses capabilities.has() gates with non-null assertion ...
- [Phase 76]: [Phase 76-02]: TAG_MAP placed at module top in integration.ts ...
- [Phase 76]: [Phase 76-02]: JSON branch runs before table rendering in integration list action ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Strip Capabilities column and TAG_MAP from src/commands/integration.ts</name>
  <files>src/commands/integration.ts</files>
  <read_first>
    - src/commands/integration.ts (the entire current file — see TAG_MAP, JSON branch, and table branch)
    - .planning/phases/78.1-turn-capability-enums-into-actual-typescript-interface-inste/78.1-CONTEXT.md (D-09, D-10, D-11, D-12)
  </read_first>
  <action>
Make these exact edits to `src/commands/integration.ts`:

**1. Delete the `TAG_MAP` constant entirely** (currently lines 7-14):

```typescript
const TAG_MAP: Record<string, string> = {
  generate: 'gen',
  cleanup: 'clean',
  commands: 'cmd',
  configExample: 'cfg',
  windowDetection: 'win',
  applies: 'apl',
}
```

Remove the entire block including the blank line above it if needed for formatting, but keep the rest of the file structure intact.

**2. Update the JSON branch** (currently around line 37). Change the returned object literal:

Before:
```typescript
return { id: i.id, label: i.label, enabled, configured, capabilities: [...i.capabilities] }
```

After:
```typescript
return { id: i.id, label: i.label, enabled, configured }
```

(D-10: drop the `capabilities` field. The JSON shape becomes `{ id, label, enabled, configured }`.)

**3. Update the table branch** (currently lines 42-56). Make these changes:

- Delete the `caps` computation line entirely:
  ```typescript
  const caps = [...i.capabilities].map((c) => TAG_MAP[c] ?? c).sort().join(' ')
  ```
- In the row mapper return value, drop `caps`:
  - Before: `return { id: i.id, label: i.label, enabled, configured, caps }`
  - After: `return { id: i.id, label: i.label, enabled, configured }`
- Update the header row to remove the `Capabilities` column:
  - Before: `console.log(\`${"ID".padEnd(14)} ${"Label".padEnd(18)} ${"Enabled".padEnd(9)} ${"Configured".padEnd(12)} Capabilities\`)`
  - After: `console.log(\`${"ID".padEnd(14)} ${"Label".padEnd(18)} ${"Enabled".padEnd(9)} Configured\`)`
- Update the divider row to remove the capabilities segment:
  - Before: `console.log(\`${"─".repeat(14)} ${"─".repeat(18)} ${"─".repeat(9)} ${"─".repeat(12)} ${"─".repeat(20)}\`)`
  - After: `console.log(\`${"─".repeat(14)} ${"─".repeat(18)} ${"─".repeat(9)} ${"─".repeat(10)}\`)`
- Update the data row print to drop the `${row.caps}` interpolation:
  - Before: `console.log(\`${row.id.padEnd(14)} ${row.label.padEnd(18)} ${enabledStr.padEnd(9)} ${configuredStr.padEnd(12)} ${row.caps}\`)`
  - After: `console.log(\`${row.id.padEnd(14)} ${row.label.padEnd(18)} ${enabledStr.padEnd(9)} ${configuredStr}\`)`

The rest of the file (per-integration loop at line 59 onwards, configCmd, the sub.command additions) is unchanged.

**Why:** The Capabilities column was added in Phase 76 but the v0.17.0 milestone has not shipped. Per D-12 and D-16, the entire mental model of "capabilities are an introspectable runtime thing" is removed — they remain a compile-time concept only. The CLI must show no trace of them. Per D-11, TAG_MAP is no longer referenced anywhere and must be deleted, not orphaned.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
    Expected: exits 0.
  </verify>
  <acceptance_criteria>
    - `grep -c "TAG_MAP" src/commands/integration.ts` returns 0
    - `grep -c "Capabilities" src/commands/integration.ts` returns 0
    - `grep -c "capabilities" src/commands/integration.ts` returns 0
    - `grep -c "i.capabilities" src/commands/integration.ts` returns 0
    - `grep -c "row.caps" src/commands/integration.ts` returns 0
    - `grep -c "i\\.capabilities" src/commands/integration.ts` returns 0
    - The integration list command still exists: `grep -q '"list \\[workspace\\]"' src/commands/integration.ts`
    - `bun run typecheck` exits 0
    - Manual smoke (verifiable but not automated in this acceptance check; Task 4 of this plan handles full verification): `bun run src/index.ts integration list` produces output without the string "Capabilities"
    - Manual smoke: `bun run src/index.ts integration list --json` produces JSON without the key "capabilities"
  </acceptance_criteria>
  <done>
    integration.ts no longer references TAG_MAP, capabilities field, or Capabilities column. JSON output is `{ id, label, enabled, configured }` per row. Table output has only ID/Label/Enabled/Configured columns. typecheck exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update four test files — drop capabilities Set fakes and delete obsolete "Phase 76-02" test block</name>
  <files>tests/lib/integrations/runner.test.ts, tests/lib/integrations/window-detector.test.ts, tests/lib/integration-commands.test.ts, tests/tui/workspace-wizard.test.ts</files>
  <read_first>
    - tests/lib/integrations/runner.test.ts (entire file — note all fake integration declarations and the test for capability gating)
    - tests/lib/integrations/window-detector.test.ts (entire file — note all fake integration declarations)
    - tests/lib/integration-commands.test.ts lines 270-316 (the "Phase 76-02: capabilities column" describe block to delete)
    - tests/tui/workspace-wizard.test.ts lines 1-130 (fake integrations)
    - src/lib/integrations/runner.ts (post-Plan-02 — the runner now relies on method/property presence, not capabilities Set)
    - src/lib/integrations/types.ts (post-Plan-01 — confirms no Capability type)
    - .planning/phases/78.1-turn-capability-enums-into-actual-typescript-interface-inste/78.1-CONTEXT.md (D-17)
  </read_first>
  <action>
Edit the four test files as follows. The runtime guidance is: the runner now uses METHOD/PROPERTY PRESENCE on the fake object as the capability declaration. So instead of `capabilities: new Set(['generate'])`, the fake just needs the `generate` method present. Instead of `capabilities: new Set([])`, the fake just omits the methods.

***

**File 1: `tests/lib/integrations/runner.test.ts`**

For each of the 7 fake integrations in the `fakeIntegrations` array (high, low, mid, disabled, no-applies, no-generate, skip-me):

- DELETE the line `capabilities: new Set([...]),` from each fake.

The methods (`generate`, `applies`, `open`) should already match what the test wants the fake to do — they're either present (capability gated true) or absent (capability gated false). Specifically verify that:

- **high, low, mid, disabled, skip-me**: capabilities was `new Set(['generate'])` — they have a `generate` method already. Just delete the capabilities line.
- **no-applies**: capabilities was `new Set(['generate', 'applies'])` — has `generate` AND `applies` methods. Delete the capabilities line. (The fake's `applies: () => false` is what makes it skip — that already works under predicate gating.)
- **no-generate**: capabilities was `new Set([])` — and the comment says "generate is intentionally undefined". Confirm there's NO `generate` field on this fake. If by mistake one is present, remove it. The lack of `generate` is what makes `isGenerator(i)` return false. Delete the capabilities line.

After deletion, the test assertions should still pass because:
- `runIntegrationGenerate`'s test "executes integrations in ascending order" — relies on `isGenerator` predicate seeing `typeof generate === 'function'` for high/mid/low. Still works.
- "skips integrations where applies() returns false" — relies on `isConditional(noApplies)` returning true (because `typeof applies === 'function'`) and `applies()` returning false. Still works.
- "integration without generate capability does not throw and has path=null" — relies on `isGenerator(noGenerate)` returning false because `typeof generate === 'undefined'`. Still works.
- The test "skips generate when capability not declared even if method exists" (around line 207) becomes structurally identical to "integration without generate capability does not throw". The test name and comments may now be slightly stale ("capability not declared") — leave the test as-is, the assertion still passes. Do not delete this test even though the wording is now slightly redundant; D-17 only mandates deleting capabilities Sets, not deleting tests in this file.

NOTE: the runner cleanup tests (`runIntegrationCleanup`) assert that no fake has cleanup. Under predicate gating, `isCleaner(i)` checks `typeof cleanup === 'function'` — none of the fakes have a cleanup method, so the test still passes. No changes needed.

***

**File 2: `tests/lib/integrations/window-detector.test.ts`**

For each of the 3 fake integrations (`windowIntegration`, `nonWindowIntegration`, `consumerIntegration`):

- DELETE the line `capabilities: new Set([...]),` from each fake.

After deletion:
- `windowIntegration` still has `windowDetector: windowDetectorMock` — `isWindowDetecting` predicate sees the property, returns true. Still detected. ✓
- `nonWindowIntegration` has NO `windowDetector` field — predicate returns false. Still not detected. ✓
- `consumerIntegration` has NO `windowDetector` field — predicate returns false. Still not detected. ✓

All existing assertions about `mockBegin` / `mockResolve` / `windowOpenMock` call counts continue to pass without changes.

***

**File 3: `tests/lib/integration-commands.test.ts`**

DELETE the entire describe-block-internal section currently spanning lines 285-315, including these three tests:

```typescript
// --- Phase 76-02: capabilities column ---

test("all integrations have capabilities field (Set instance)", async () => {
  const { integrations } = await import("@/lib/integrations/index")
  for (const i of integrations) {
    expect(i.capabilities).toBeDefined()
    expect(i.capabilities instanceof Set).toBe(true)
  }
})

test("list subcommand output includes Capabilities column header", () => {
  const src = require("fs").readFileSync(
    require("path").join(process.cwd(), "src/commands/integration.ts"),
    "utf8"
  )
  expect(src).toContain("TAG_MAP")
  expect(src).toContain("Capabilities")
  expect(src).toContain("generate: 'gen'")
  expect(src).toContain("cleanup: 'clean'")
  expect(src).toContain("windowDetection: 'win'")
})

test("list subcommand --json output includes capabilities array", () => {
  const src = require("fs").readFileSync(
    require("path").join(process.cwd(), "src/commands/integration.ts"),
    "utf8"
  )
  expect(src).toContain("[...i.capabilities]")
  expect(src).toContain("capabilities:")
})
```

Delete the section header comment `// --- Phase 76-02: capabilities column ---` AND all three tests within it. The closing `})` of the surrounding describe block must be preserved. The "Phase 50: integrations without commands still have config" test immediately above (and any other adjacent tests) MUST be preserved unchanged.

After deletion, no test in this file references `i.capabilities`, `TAG_MAP`, or "Capabilities".

***

**File 4: `tests/tui/workspace-wizard.test.ts`**

For each of the 2 fake integrations in `fakeIntegrations` (vscode and tmux), DELETE the line `capabilities: new Set([...]),`.

After deletion, the fakes still have `generate`, `open`, `configurePrompt`, `isEnabled`, etc., so any test that introspects these methods continues to work. The wizard logic does not consult `capabilities` — it iterates the integrations array and calls `configurePrompt`/`generate` directly via plan-time checks, all of which work via method presence.

***

**Cross-cutting check after all four files:** `grep -r "capabilities: new Set" tests/` MUST return zero matches.
  </action>
  <verify>
    <automated>bun run test</automated>
    Expected: exits 0 (entire test suite passes).
  </verify>
  <acceptance_criteria>
    - `grep -rc "capabilities: new Set" tests/` returns 0 (across all test files)
    - `grep -c "capabilities" tests/lib/integrations/runner.test.ts` returns 0 OR matches only refer to comments/wording (no `capabilities:` or `i.capabilities` left). Run: `grep -c "capabilities:" tests/lib/integrations/runner.test.ts` returns 0
    - `grep -c "capabilities:" tests/lib/integrations/window-detector.test.ts` returns 0
    - `grep -c "Phase 76-02: capabilities column" tests/lib/integration-commands.test.ts` returns 0
    - `grep -c "TAG_MAP" tests/lib/integration-commands.test.ts` returns 0
    - `grep -c "Capabilities column header" tests/lib/integration-commands.test.ts` returns 0
    - `grep -c "capabilities array" tests/lib/integration-commands.test.ts` returns 0
    - `grep -c "capabilities:" tests/tui/workspace-wizard.test.ts` returns 0
    - The `fakeIntegrations` arrays in all four test files still exist (test infrastructure preserved): `grep -q "fakeIntegrations" tests/lib/integrations/runner.test.ts && grep -q "fakeIntegrations" tests/lib/integrations/window-detector.test.ts && grep -q "fakeIntegrations" tests/tui/workspace-wizard.test.ts`
    - `bun run test` exits 0
    - `bun run typecheck` exits 0
  </acceptance_criteria>
  <done>
    All four test files no longer reference `capabilities: new Set(...)` on fakes; the obsolete "Phase 76-02: capabilities column" describe block in integration-commands.test.ts is deleted; full test suite passes via `bun run test`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Rewrite REQUIREMENTS.md ENGN-07/08/09 in place; append Phase 78.1 decisions section to STATE.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the entire current file — locate ENGN-07/08/09 in the Engine Hardening section AND in the Traceability table)
    - .planning/STATE.md (the Accumulated Context > Decisions section showing Phase 76 entries on lines 56-58, the format used by other phase decision entries)
    - .planning/phases/78.1-turn-capability-enums-into-actual-typescript-interface-inste/78.1-CONTEXT.md (D-13, D-14, D-15, D-16)
  </read_first>
  <action>
**A) Rewrite REQUIREMENTS.md ENGN-07/08/09 in place (per D-13).**

Locate the Engine Hardening section of `.planning/REQUIREMENTS.md` (around lines 28-30 currently). Replace the three lines:

```
- [x] **ENGN-07**: Each integration plugin declares its capabilities via a typed `capabilities` field
- [x] **ENGN-08**: Integration runner uses capability declarations instead of optional chaining to gate calls
- [x] **ENGN-09**: `integration list` displays plugin capabilities
```

With these new lines (still marked Complete — D-13):

```
- [x] **ENGN-07**: Each integration plugin declares its optional behaviors via narrow TypeScript interfaces (Generates, Cleans, HasCommands, HasConfigExample, WindowDetecting, Conditional) composed at the export site via intersection types
- [x] **ENGN-08**: Integration runner uses type predicates (`isGenerator`, `isCleaner`, `isConditional`, `isWindowDetecting`) to gate dispatch, with zero non-null assertions on integration method calls
- [x] **ENGN-09**: Plugin capabilities are a compile-time concept only — no runtime introspection surface in `git-stacks integration list`
```

Then locate the Traceability table at the bottom of REQUIREMENTS.md and update the three rows for ENGN-07/08/09. The current rows look like:

```
| ENGN-07 | Phase 76 | Complete |
| ENGN-08 | Phase 76 | Complete |
| ENGN-09 | Phase 76 | Complete |
```

Replace with:

```
| ENGN-07 | Phase 76 → Phase 78.1 | Complete |
| ENGN-08 | Phase 76 → Phase 78.1 | Complete |
| ENGN-09 | Phase 76 → Phase 78.1 | Complete |
```

This preserves the audit trail (per D-13) — the requirement was originally completed in Phase 76 but its acceptance criteria were rewritten in Phase 78.1. No other rows in the table change. The "Last updated" footer can be updated to today's date (2026-04-06) but is optional.

Phase 76 historical artifacts are NOT touched (per D-14): do NOT modify `.planning/phases/76-integration-plugin-capability-contracts/*`.

***

**B) Append a Phase 78.1 decisions section to STATE.md (per D-15).**

Locate the `## Accumulated Context` > `### Decisions` section in `.planning/STATE.md`. Find the existing `[Phase 78-operation-runner-with-rollback]:` entries (currently the last ones in the Decisions list). Append the following entries at the end of the Decisions list, AFTER the existing Phase 78 entries (do NOT modify any existing entries — D-15):

```
- [Phase 78.1]: Replaces Phase 76 D-01 through D-07 — the `Capability` string union and `capabilities: ReadonlySet<Capability>` field on `Integration` are removed. Six narrow interfaces (Generates, Cleans, HasCommands, HasConfigExample, WindowDetecting, Conditional) declared in `src/lib/integrations/types.ts` are composed via intersection types at each plugin export site. Method/property presence on the plugin object IS the capability declaration — single source of truth.
- [Phase 78.1]: Runner gating mechanism replaced — `runner.ts` now uses four type predicates (`isGenerator`, `isCleaner`, `isConditional`, `isWindowDetecting`) colocated in `types.ts`. Zero non-null assertions on integration method calls. Phase 76 D-04 (mandatory non-null assertions) is fully superseded.
- [Phase 78.1]: `git-stacks integration list` no longer displays a Capabilities column or `capabilities` JSON field. `TAG_MAP` constant deleted from `src/commands/integration.ts`. Phase 76 D-08, D-09, D-10 user-facing surface is fully removed (Phase 76-02 plan superseded). Capabilities are now compile-time-only.
- [Phase 78.1]: Phase 76 artifacts and STATE.md Phase 76 decision entries are intentionally preserved as historical record (D-14, D-15). REQUIREMENTS.md ENGN-07/08/09 acceptance text rewritten in place; phase column updated to "Phase 76 → Phase 78.1"; status remains Complete.
- [Phase 78.1]: CHANGELOG constraint for Phase 79 release-prep (D-16) — neither the Phase 76 capabilities column addition nor the Phase 78.1 removal land in the v0.17.0 CHANGELOG. Net user-visible change is zero (the feature was added and removed within the same unreleased milestone). Phase 79 must NOT mention "capabilities column", "integration capabilities", or "Phase 78.1" in CHANGELOG.md.
```

Also update the `last_updated` timestamp at the top of STATE.md frontmatter to the current execution date, and update `last_activity` similarly. Do NOT change `progress.completed_phases` — that's a Phase 78 concern, not Phase 78.1. Do NOT modify the Current Position section unless explicitly told.

Verify after the edit that NO existing Phase 76 decision lines were touched (per D-15: "STATE.md Phase 76 decision lines (the Phase 76 D-01 through D-10 entries in Accumulated Context) are NOT edited"). The Phase 76 entries must remain bit-identical.
  </action>
  <verify>
    <automated>grep -c "Phase 78.1" .planning/STATE.md</automated>
    Expected: returns 5 or more (the five new decision lines, plus possibly other mentions).
  </verify>
  <acceptance_criteria>
    - `grep -q "narrow TypeScript interfaces" .planning/REQUIREMENTS.md`
    - `grep -q "type predicates" .planning/REQUIREMENTS.md`
    - `grep -q "compile-time concept only" .planning/REQUIREMENTS.md`
    - `grep -c "ENGN-07.*Phase 76 → Phase 78.1" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "ENGN-08.*Phase 76 → Phase 78.1" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "ENGN-09.*Phase 76 → Phase 78.1" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "Each integration plugin declares its capabilities via a typed" .planning/REQUIREMENTS.md` returns 0 (the old text is gone)
    - `grep -c "displays plugin capabilities" .planning/REQUIREMENTS.md` returns 0 (the old ENGN-09 text is gone)
    - All three ENGN entries still marked `[x]` (Complete)
    - `grep -c "\\[Phase 78.1\\]:" .planning/STATE.md` returns 5 (five new decision lines)
    - `grep -c "Phase 76.*capabilities is required" .planning/STATE.md` returns 1 (the existing Phase 76 line is preserved unchanged)
    - `grep -c "Phase 76.*non-null assertion" .planning/STATE.md` returns 1 (the existing Phase 76 line is preserved unchanged)
    - `grep -c "TAG_MAP placed at module top" .planning/STATE.md` returns 1 (existing Phase 76-02 line preserved)
    - The Phase 76 historical phase directory is untouched: `find .planning/phases/76-integration-plugin-capability-contracts/ -newer .planning/phases/78.1-turn-capability-enums-into-actual-typescript-interface-inste/78.1-CONTEXT.md -type f` returns nothing
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md ENGN-07/08/09 rewritten in place with new acceptance text; Traceability rows updated to "Phase 76 → Phase 78.1". STATE.md has five new Phase 78.1 decision entries appended; existing Phase 76 entries untouched. Phase 76 historical artifacts not modified.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Final verification — typecheck, full test suite, CLI smoke check</name>
  <files>(no file edits; verification only)</files>
  <read_first>
    - .planning/phases/78.1-turn-capability-enums-into-actual-typescript-interface-inste/78.1-CONTEXT.md (the must-haves to verify)
  </read_first>
  <action>
Run the full verification suite to prove Phase 78.1 is complete:

1. Run `bun run typecheck` and confirm exit code 0.
2. Run `bun run test` and confirm exit code 0. (Use the project test runner script — NOT `bun test tests/` directly, per CLAUDE.md guidance about mock pollution.)
3. Run `bun run src/index.ts integration list` (or `bun run dev integration list`) and confirm:
   - Output contains the headers `ID`, `Label`, `Enabled`, `Configured`
   - Output does NOT contain the string `Capabilities`
4. Run `bun run src/index.ts integration list --json` and confirm:
   - Output is valid JSON
   - Each row has keys `id`, `label`, `enabled`, `configured`
   - No row has the key `capabilities`
5. Spot-check that no production source file imports `Capability`:
   - `grep -rl "type Capability" src/lib/integrations/` should return nothing
   - `grep -rl "type Capability" src/commands/` should return nothing
6. Spot-check zero non-null assertions on integration method calls in runner.ts:
   - `grep -E "integration\\.(generate|applies|cleanup)!" src/lib/integrations/runner.ts` returns nothing

If any check fails, return to the responsible task and fix; do not proceed with marking the plan complete.

This task's purpose is to be the gate for the entire phase. After this task succeeds, Phase 78.1 is done.
  </action>
  <verify>
    <automated>bun run typecheck && bun run test</automated>
    Expected: both exit 0.
  </verify>
  <acceptance_criteria>
    - `bun run typecheck` exits 0
    - `bun run test` exits 0
    - `bun run src/index.ts integration list 2>&1 | grep -c "Capabilities"` returns 0
    - `bun run src/index.ts integration list --json 2>&1 | grep -c '"capabilities"'` returns 0
    - `bun run src/index.ts integration list 2>&1 | grep -q "Configured"` (column still present)
    - `bun run src/index.ts integration list --json 2>&1 | grep -q '"configured"'` (key still present)
    - `grep -rc "type Capability" src/lib/integrations/` returns 0 (zero hits across all files)
    - `grep -rc "capabilities: new Set" src/` returns 0
    - `grep -rc "capabilities: new Set" tests/` returns 0
    - `grep -E "integration\\.(generate|applies|cleanup)!" src/lib/integrations/runner.ts` returns nothing
    - `grep -q "isGenerator" src/lib/integrations/runner.ts`
  </acceptance_criteria>
  <done>
    typecheck and full test suite green; CLI smoke proves no Capabilities column or capabilities JSON field; no production source imports `Capability`; runner.ts uses predicates with no `!` assertions on integration methods. Phase 78.1 is complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries are crossed by this plan. Changes are: (a) removal of one column from a CLI list output, (b) test file updates, (c) markdown documentation edits to REQUIREMENTS.md and STATE.md. None of these introduce new input parsing, network calls, file I/O, or auth surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78.1-03 | (none) | src/commands/integration.ts | accept | No new threats. The CLI command is removing functionality (a column and a JSON field), not adding. Removing fields cannot introduce new attack surface — the fields previously had no auth implications and the rest of the command is unchanged. Per Phase 78.1 security_context: "no new threats — refactor preserves existing behavior". |
| T-78.1-04 | (none) | tests/* | accept | Test-only edits. Tests run with elevated trust by definition; removing assertions does not affect production security posture. |
| T-78.1-05 | (none) | .planning/REQUIREMENTS.md, .planning/STATE.md | accept | Documentation edits with no code execution implications. |
</threat_model>

<verification>
- `src/commands/integration.ts`: no Capabilities column, no TAG_MAP, no `i.capabilities` references
- 4 test files: no `capabilities: new Set(` literals
- `tests/lib/integration-commands.test.ts`: no "Phase 76-02: capabilities column" describe section
- REQUIREMENTS.md: ENGN-07/08/09 rewritten in place, phase column shows "Phase 76 → Phase 78.1", still Complete
- STATE.md: 5 new Phase 78.1 decision entries appended; Phase 76 entries unchanged
- `bun run typecheck` exits 0
- `bun run test` exits 0
- `git-stacks integration list` table has no "Capabilities" column header
- `git-stacks integration list --json` has no `capabilities` key on any row
</verification>

<success_criteria>
1. CLI: `git-stacks integration list` shows ID/Label/Enabled/Configured columns only (no Capabilities)
2. CLI: `git-stacks integration list --json` returns `{ id, label, enabled, configured }` per row
3. `TAG_MAP` is gone from `src/commands/integration.ts`
4. All test files have `capabilities: new Set(` removed; the obsolete Phase 76-02 describe block is deleted
5. REQUIREMENTS.md ENGN-07/08/09 rewritten in place with narrow-interface acceptance text; phase column updated to "Phase 76 → Phase 78.1"; entries still Complete
6. STATE.md has new Phase 78.1 decision entries; Phase 76 entries preserved untouched
7. `bun run typecheck` exits 0
8. `bun run test` exits 0
9. Phase 76 historical phase directory is not modified
</success_criteria>

<output>
After completion, create `.planning/phases/78.1-turn-capability-enums-into-actual-typescript-interface-inste/78.1-03-SUMMARY.md`
</output>
