---
phase: 78.1-turn-capability-enums-into-actual-typescript-interface-inste
verified: 2026-04-06T18:48:32Z
status: passed
score: 12/12 must-haves verified
re_verification:
  previous_status: gaps_found
  previous_score: 11/12
  gaps_closed:
    - "bun run test exits 0"
  gaps_remaining: []
  regressions: []
---

# Phase 78.1: turn capability enums into actual typescript interface instead of capability return function. also remove it from the integration list and documentation Verification Report

**Phase Goal:** Replace the Capability string union and capabilities Set on Integration with narrow TypeScript interfaces composed via intersection types; remove the Capabilities column from `git-stacks integration list` and the audit-trail updates in REQUIREMENTS.md/STATE.md.
**Verified:** 2026-04-06T18:48:32Z
**Status:** passed
**Re-verification:** Yes — after gap closure

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | `src/lib/integrations/types.ts` no longer exports `Capability` | ✓ VERIFIED | No `export type Capability` or `ReadonlySet<Capability>` remains; `types.ts` exposes only `Integration` plus narrow interfaces/predicates. |
| 2 | Base `Integration` no longer has a `capabilities` field | ✓ VERIFIED | `src/lib/integrations/types.ts:42-70` contains only always-present base fields. |
| 3 | `types.ts` exports six narrow interfaces | ✓ VERIFIED | `Generates`, `Cleans`, `HasCommands`, `HasConfigExample`, `WindowDetecting`, `Conditional` are defined at `src/lib/integrations/types.ts:72-109`. |
| 4 | `types.ts` exports four narrowing predicates | ✓ VERIFIED | `isGenerator`, `isCleaner`, `isConditional`, `isWindowDetecting` are defined at `src/lib/integrations/types.ts:118-132` with `i is Integration & X` return types. |
| 5 | All 10 plugins removed `capabilities: new Set(...)` and use intersection typing where needed | ✓ VERIFIED | No `capabilities: new Set` or `type Capability` match remains under `src/lib/integrations/`; plugin exports show `Integration & ...` annotations and `cmux` remains plain `Integration`. |
| 6 | `runner.ts` gates optional behavior through predicates, not `.capabilities.has(...)` or non-null assertions | ✓ VERIFIED | `src/lib/integrations/runner.ts:1-79` imports predicates and uses them for generate/applies/window-detection/cleanup dispatch; no `.capabilities` matches remain. |
| 7 | `git-stacks integration list` table output does not contain `Capabilities` | ✓ VERIFIED | `bun run src/index.ts integration list` prints only `ID`, `Label`, `Enabled`, and `Configured`. |
| 8 | `git-stacks integration list --json` output does not include `capabilities` | ✓ VERIFIED | `bun run src/index.ts integration list --json` returns rows with only `id`, `label`, `enabled`, `configured`. |
| 9 | `REQUIREMENTS.md` ENGN-07/08/09 reflect the narrow-interface design | ✓ VERIFIED | `.planning/REQUIREMENTS.md:28-30` rewrites ENGN-07/08/09 around narrow interfaces, predicates, and compile-time-only capability visibility. |
| 10 | `STATE.md` contains Phase 78.1 supersession notes | ✓ VERIFIED | `.planning/STATE.md:77-79` records Phase 76 supersession, predicate gating, and CLI capability removal. |
| 11 | `bun run typecheck` passes | ✓ VERIFIED | `bun run typecheck` exited 0. |
| 12 | `bun run test` passes | ✓ VERIFIED | `bun run test` exited 0 after `tests/tui/dashboard/WorkspaceDetail.test.tsx` exported `isConditional` from its mocked types module. |

**Score:** 12/12 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `src/lib/integrations/types.ts` | Narrow interfaces + predicates replace capability union/set | ✓ VERIFIED | Exists, substantive, and exports the new contracts. |
| `src/lib/integrations/{vscode,intellij,cmux,tmux,niri,aerospace,github,gitlab,gitea,jira}.ts` | Plugin capability declarations via intersections/method presence | ✓ VERIFIED | No capability-set literals remain; exports match the planned intersection shapes. |
| `src/lib/integrations/runner.ts` | Predicate-gated dispatch | ✓ VERIFIED | Exists, wired to `./types`, and no `.capabilities` access remains. |
| `src/commands/integration.ts` | CLI without capabilities column/JSON field | ✓ VERIFIED | Source and CLI smoke checks both confirm removal. |
| `.planning/REQUIREMENTS.md` | Updated ENGN-07/08/09 text | ✓ VERIFIED | Requirement text updated and mapped to Phase 78.1. |
| `.planning/STATE.md` | Phase 78.1 decision notes | ✓ VERIFIED | Supersession notes are present. |
| `tests/tui/dashboard/WorkspaceDetail.test.tsx` | Test suite remains green after downstream consumer change | ✓ VERIFIED | Mocked `../../../src/lib/integrations/types` now exports `isConditional`, matching `WorkspaceDetail.tsx` imports. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `src/lib/integrations/runner.ts` | `src/lib/integrations/types.ts` predicates | `import { isGenerator, isCleaner, isConditional, isWindowDetecting }` | ✓ WIRED | Import present and predicates are used in all dispatch paths. |
| Plugin exports | Narrow interfaces in `types.ts` | `export const xIntegration: Integration & ...` | ✓ WIRED | All 10 plugins type against the new contracts. |
| `src/commands/integration.ts` | CLI output | `console.log` / JSON serialization | ✓ WIRED | Smoke output matches the source changes. |
| `tests/tui/dashboard/WorkspaceDetail.test.tsx` | `src/tui/dashboard/WorkspaceDetail.tsx` | `mock.module(".../types")` | ✓ WIRED | The mock now exports `isConditional`, so the component imports cleanly under test. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| `src/commands/integration.ts` | `rows` / `jsonRows` | `integrations` registry + `readGlobalConfig()` / `readWorkspace()` | Yes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Type safety restored | `bun run typecheck` | Exit 0 | ✓ PASS |
| Table output omits capabilities | `bun run src/index.ts integration list` | No `Capabilities` column; shows `ID/Label/Enabled/Configured` | ✓ PASS |
| JSON output omits capabilities | `bun run src/index.ts integration list --json` | Valid JSON rows with `id,label,enabled,configured` only | ✓ PASS |
| Phase test suite stays green | `bun run test` | Exit 0 | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| ENGN-07 | 78.1-01, 78.1-02 | Each integration plugin declares optional behaviors via narrow TypeScript interfaces composed at the export site | ✓ SATISFIED | `types.ts` exports interfaces; plugins export as `Integration & ...` intersections. |
| ENGN-08 | 78.1-01, 78.1-02 | Integration runner uses type predicates over method/property presence to gate optional integration behavior | ✓ SATISFIED | `runner.ts` imports and uses `isGenerator`, `isCleaner`, `isConditional`, `isWindowDetecting`. |
| ENGN-09 | 78.1-03 | `integration list` omits compile-time-only capability details from both table and JSON output | ✓ SATISFIED | `integration.ts` and CLI smoke checks show no `Capabilities` column or `capabilities` field. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| — | — | None detected in scanned production or test phase files | ℹ️ Info | No TODO/placeholder/stub patterns found that block the phase goal. |

### Human Verification Required

None.

### Gaps Summary

No remaining blockers. The previous test gap is closed, the full suite passes, and the phase goal is achieved end-to-end.

---

_Verified: 2026-04-06T18:48:32Z_
_Verifier: Claude (gsd-verifier)_
