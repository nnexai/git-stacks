---
phase: 78.1
plan: 01
subsystem: integrations
tags: [types, predicates, capability-contracts]
requirements_completed: [ENGN-07, ENGN-08]
---

# Phase 78.1 Plan 01 Summary

Replaced the runtime `Capability` union and `capabilities` field with narrow structural interfaces plus colocated type predicates in `src/lib/integrations/types.ts`.

## Accomplishments

- Removed `Capability` and the optional capability-bearing members from the base `Integration` interface
- Added `Generates`, `Cleans`, `HasCommands`, `HasConfigExample`, `WindowDetecting`, and `Conditional`
- Added `isGenerator`, `isCleaner`, `isConditional`, and `isWindowDetecting` so downstream code can narrow without non-null assertions

## Verification

- `Capability`, `ReadonlySet<Capability>`, and all former optional capability members are gone from `src/lib/integrations/types.ts`
- The six narrow interfaces and four predicate exports are present
- `bun run typecheck` fails only at still-unmigrated consumers, which is the expected handoff into Plan 02

## Deviations from Plan

None - plan executed exactly as written.

## Next Phase Readiness

- Plan 02 can now update plugin export annotations and the runner to use the new structural contracts
- The current typecheck failures identify the remaining consumers that still rely on the removed `capabilities` surface
