---
phase: 84.1-coverage-report-accuracy-and-tui-instrumentation-follow-up
plan: 02
type: verify
wave: 2
depends_on:
  - 84.1-01
files_modified: []
autonomous: true
requirements:
  - COVR-01
  - COVR-02
  - COVR-03
  - COVR-04
  - GATE-03
must_haves:
  truths:
    - "The canonical `bun run coverage` report validates freshly exercised regular TS and TUI TSX sentinel files."
    - "`bun run verify:gates` rejects zero-hit sentinel reports and passes against the freshly generated canonical report."
    - "`bun run verify` remains the local release-prep path and exits 0 after the Phase 84.1 instrumentation repair."
  artifacts:
    - path: ".coverage/coverage-final.json"
      provides: "Fresh merged Istanbul coverage map with nonzero sentinel hits"
    - path: ".coverage/coverage-summary.json"
      provides: "Fresh machine-readable summary output"
    - path: ".coverage/lcov.info"
      provides: "Fresh LCOV output"
    - path: ".coverage/index.html"
      provides: "Fresh HTML coverage report"
  key_links:
    - from: "bun run coverage"
      to: ".coverage/coverage-final.json"
      via: "canonical report generation"
      pattern: "coverage-final.json"
    - from: "bun run verify:gates"
      to: "scripts/verify-gates.ts"
      via: "sentinel gate validation"
      pattern: "Coverage sentinel problems"
---

<objective>
Verify the Phase 84.1 coverage instrumentation repair through the canonical full-suite coverage and local release-prep gate path.

Purpose: keep final validation separate from implementation so execution scheduling stays narrow and the phase has one explicit release-readiness proof plan.
Output: a verification summary proving the freshly generated coverage report has real hits for the Phase 84.1 sentinels and passes the existing local verification workflow.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/84.1-coverage-report-accuracy-and-tui-instrumentation-follow-up/84.1-CONTEXT.md
@.planning/phases/84.1-coverage-report-accuracy-and-tui-instrumentation-follow-up/84.1-RESEARCH.md
@.planning/phases/84.1-coverage-report-accuracy-and-tui-instrumentation-follow-up/84.1-VALIDATION.md
@.planning/phases/84.1-coverage-report-accuracy-and-tui-instrumentation-follow-up/84.1-PATTERNS.md
@.planning/phases/84.1-coverage-report-accuracy-and-tui-instrumentation-follow-up/84.1-01-PLAN.md
@scripts/coverage-runner.ts
@scripts/coverage-preload.ts
@scripts/verify.ts
@scripts/verify-gates.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the canonical coverage and release-prep gate path end to end</name>
  <read_first>
    <file>.planning/phases/84.1-coverage-report-accuracy-and-tui-instrumentation-follow-up/84.1-01-SUMMARY.md</file>
    <file>scripts/coverage-runner.ts</file>
    <file>scripts/coverage-preload.ts</file>
    <file>scripts/verify.ts</file>
    <file>scripts/verify-gates.ts</file>
    <file>package.json</file>
  </read_first>
  <files>NONE — verification only unless Plan 84.1-01 summary identifies a direct Phase 84.1 regression that must be fixed before verification can pass</files>
  <action>Run the serial verification path that proves Phase 84.1 is release-ready after Plan 84.1-01. First run `bun run coverage` and inspect `.coverage/coverage-final.json` for nonzero statement or function hits on `src/lib/messages.ts` and `src/tui/dashboard/ActionMenu.tsx`. Then run `bun run verify:gates` so the sentinel checks validate the freshly generated canonical report. Finally run `bun run verify` to prove the full local release-prep path still composes prerequisites, coverage, gates, tests, dependency checks, and typecheck. If a command fails because the selected instrumentation path broke subprocess coverage, TSX loading, or normal test behavior, fix only the direct regression in `scripts/coverage-preload.ts`, `scripts/coverage-runner.ts`, `scripts/verify-gates.ts`, or their tests; do not weaken sentinel gates, remove existing quality commands, add CI, or introduce numeric coverage thresholds.</action>
  <acceptance_criteria>
    <criterion>`bun run coverage` exits 0 and writes `.coverage/coverage-final.json`, `.coverage/coverage-summary.json`, `.coverage/lcov.info`, and `.coverage/index.html`.</criterion>
    <criterion>Fresh `.coverage/coverage-final.json` reports nonzero statement or function hits for `src/lib/messages.ts`.</criterion>
    <criterion>Fresh `.coverage/coverage-final.json` reports nonzero statement or function hits for `src/tui/dashboard/ActionMenu.tsx`.</criterion>
    <criterion>`bun run verify:gates` exits 0 against the freshly generated coverage artifacts.</criterion>
    <criterion>`bun run verify` exits 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>bun run coverage</automated>
    <automated>bun --print 'const cov=await Bun.file(".coverage/coverage-final.json").json(); for (const f of ["src/lib/messages.ts","src/tui/dashboard/ActionMenu.tsx"]) { const e=cov[f]; if (!e || !(Object.values(e.s).some(Boolean) || Object.values(e.f).some(Boolean))) { console.error(`${f} zero-hit`); process.exit(1); } console.log(`${f} hit`); }'</automated>
    <automated>bun run verify:gates</automated>
    <automated>bun run verify</automated>
  </verify>
  <done>The canonical local release-prep path proves the coverage report is trustworthy for the Phase 84.1 sentinel source files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| full coverage run → release readiness | A passing focused repair must also work in the canonical full-suite report. |
| generated `.coverage/` artifacts → gates | Fresh report artifacts must be parsed and sentinel-checked before release closeout. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84.1-05 | T | final coverage artifacts | mitigate | Inspect nonzero sentinel hits immediately after `bun run coverage` before accepting `verify:gates` and `verify`. |
| T-84.1-06 | D | local release-prep path | mitigate | Run `bun run verify` after focused gate checks so prerequisite, coverage, gate, test, dependency, and typecheck commands remain composed. |
</threat_model>

<verification>
- `bun run coverage`
- `bun --print 'const cov=await Bun.file(".coverage/coverage-final.json").json(); for (const f of ["src/lib/messages.ts","src/tui/dashboard/ActionMenu.tsx"]) { const e=cov[f]; if (!e || !(Object.values(e.s).some(Boolean) || Object.values(e.f).some(Boolean))) process.exit(1); }'`
- `bun run verify:gates`
- `bun run verify`
</verification>

<success_criteria>
- `bun run coverage` produces all stable `.coverage/` artifacts.
- `src/lib/messages.ts` has nonzero statement or function hits in the canonical coverage report.
- `src/tui/dashboard/ActionMenu.tsx` has nonzero statement or function hits in the canonical coverage report.
- `bun run verify:gates` passes against the fresh report.
- `bun run verify` passes end to end.
</success_criteria>

<output>
After completion, create `.planning/phases/84.1-coverage-report-accuracy-and-tui-instrumentation-follow-up/84.1-02-SUMMARY.md`
</output>
