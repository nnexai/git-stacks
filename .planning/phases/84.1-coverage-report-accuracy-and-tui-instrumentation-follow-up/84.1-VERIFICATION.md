---
phase: 84.1-coverage-report-accuracy-and-tui-instrumentation-follow-up
verified: 2026-05-14T21:26:23Z
status: passed
score: 9/9 must-haves verified
overrides_applied: 0
---

# Phase 84.1: Coverage Report Accuracy and TUI Instrumentation Follow-up Verification Report

**Phase Goal:** Coverage reports are trustworthy enough to close v0.17.1: existing tests that import source through relative paths, especially TUI/TSX tests, are captured by the Istanbul instrumentation path instead of appearing as near-zero coverage.
**Verified:** 2026-05-14T21:26:23Z
**Status:** passed
**Re-verification:** No - initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Implementation begins with a small spike comparing redirect repair against load-time instrumentation per D-01. | VERIFIED | Commit `67c663f` exists as `docs(84.1-01): document blocked coverage spike`; research/context documents compare redirect repair and load-time instrumentation, and final code selects the third proven runtime-root path after those paths failed. |
| 2 | `bun run coverage` remains the canonical release-readiness coverage report per D-04. | VERIFIED | `package.json` keeps `coverage: bun run scripts/coverage-runner.ts`; `bun run coverage` exited 0 and generated current artifacts. |
| 3 | `coverage:unit` and `coverage:integ` remain scoped debugging aids per D-05. | VERIFIED | `package.json` keeps `coverage:unit` and `coverage:integ` mapped to `scripts/coverage-runner.ts --unit/--integ`; `parseArgs()` preserves unit/integ mode selection. |
| 4 | Stable `.coverage/coverage-final.json`, `.coverage/coverage-summary.json`, `.coverage/lcov.info`, and `.coverage/index.html` outputs remain intact per D-06. | VERIFIED | `scripts/coverage-runner.ts` emits json, json-summary, lcovonly, and html reports; `ls -l` confirmed all four files exist after the fresh run. |
| 5 | Local verification fails when sentinel source files remain at zero hits despite valid artifacts per D-07, D-08, and D-09. | VERIFIED | `collectCoverageSentinelProblems()` checks `src/lib/messages.ts` and `src/tui/dashboard/ActionMenu.tsx`; regression tests cover zero-hit, missing, and aggregate failures. |
| 6 | The phase does not add CI, broad TUI E2E behavior coverage, or mandatory numeric coverage thresholds per D-12. | VERIFIED | Grep found no CI or threshold policy changes in modified coverage/gate files; `COVR-05` remains deferred in requirements. |
| 7 | The canonical `bun run coverage` report validates freshly exercised regular TS and TUI TSX sentinel files. | VERIFIED | Fresh sentinel inspection reported `src/lib/messages.ts: statements=924 functions=174` and `src/tui/dashboard/ActionMenu.tsx: statements=407 functions=253`. |
| 8 | `bun run verify:gates` rejects zero-hit sentinel reports and passes against the freshly generated canonical report. | VERIFIED | `bun run verify:gates` exited 0 after the fresh report; `tests/lib/verify-gates.test.ts` proves zero-hit sentinel reports fail and format `Coverage sentinel problems:`. |
| 9 | `bun run verify` remains the local release-prep path and exits 0 after the instrumentation repair. | VERIFIED | `scripts/verify.ts` still runs `verify:prereqs`, `coverage`, `verify:gates`, `test`, `test:deps`, and `typecheck` in order. Those child commands were run in this verification and exited 0; exact wrapper rerun was skipped to avoid immediately duplicating the just-run full coverage/test cycle. |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `scripts/coverage-preload.ts` | Runtime coverage preload path for direct imports and subprocesses | VERIFIED | Exists and substantive; writes shards via `afterAll(writeCoverageShard)` and process exit, redirects source imports to instrumented files, and rewrites CLI subprocess `bun run src/index.ts` to the instrumented entry. |
| `scripts/coverage-runner.ts` | Canonical coverage orchestration and stable artifact generation | VERIFIED | Exists and substantive; instruments `src/**/*.{ts,tsx}` into `.coverage/runtime-root/src`, runs tests with `cwd: RUNTIME_ROOT`, filters shards to source files, and emits stable reports. |
| `scripts/verify-gates.ts` | Aggregated local gate with sentinel hit checks | VERIFIED | Exists and substantive; `coverageSentinels` is part of `report.ok`, with missing/zero-hit/outside-tree problem reporting. |
| `tests/lib/coverage-surface.test.ts` | Regression coverage for public coverage command/artifact surface | VERIFIED | Exists, wired into `bun run test`, and passed in focused and full test runs. |
| `tests/lib/verify-gates.test.ts` | Regression coverage for zero-hit sentinel gate failures | VERIFIED | Exists, wired into `bun run test`, and passed in focused and full test runs. |
| `.coverage/coverage-final.json` | Fresh merged Istanbul coverage map with nonzero sentinel hits | VERIFIED | Generated by `bun run coverage`; sentinel counts confirmed nonzero. |
| `.coverage/coverage-summary.json` | Fresh machine-readable summary output | VERIFIED | Generated by `bun run coverage`; file exists and is non-empty. |
| `.coverage/lcov.info` | Fresh LCOV output | VERIFIED | Generated by `bun run coverage`; file exists and is non-empty. |
| `.coverage/index.html` | Fresh HTML coverage report | VERIFIED | Generated by `bun run coverage`; file exists and is non-empty. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `tests/lib/messages.test.ts` | `src/lib/messages.ts` | regular TypeScript source sentinel | VERIFIED | Fresh canonical coverage records `src/lib/messages.ts` with 924 statement hits and 174 function hits. |
| `tests/tui/dashboard/ActionMenu.test.tsx` | `src/tui/dashboard/ActionMenu.tsx` | TUI TSX source sentinel | VERIFIED | Fresh canonical coverage records `ActionMenu.tsx` with 407 statement hits and 253 function hits. |
| `scripts/verify-gates.ts` | `.coverage/coverage-final.json` | sentinel hit validation | VERIFIED | `readCoverageFinal()` reads `.coverage/coverage-final.json`; sentinel checks feed `report.ok`; `bun run verify:gates` passed against fresh artifacts. |
| `bun run coverage` | `.coverage/coverage-final.json` | canonical report generation | VERIFIED | `package.json` points `coverage` to `scripts/coverage-runner.ts`; runner emits `coverage-final.json`; command exited 0. |
| `bun run verify:gates` | `scripts/verify-gates.ts` | sentinel gate validation | VERIFIED | `package.json` points `verify:gates` to `scripts/verify-gates.ts`; command exited 0 and printed the passing gate message. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `scripts/coverage-runner.ts` | merged Istanbul coverage map | instrumented runtime-root test shards under `.coverage/shards` plus blank templates from `src/**/*.{ts,tsx}` | Yes - fresh coverage produced nonzero sentinel counts and stable artifacts | VERIFIED |
| `scripts/coverage-preload.ts` | `globalThis.__coverage__` shard data | Bun test preload `afterAll` and process-exit shard writers | Yes - full coverage run merged shards from 43 unit and 68 integration coverage files | VERIFIED |
| `scripts/verify-gates.ts` | `coverageSentinels` | parsed `.coverage/coverage-final.json` counters | Yes - fresh report passes; fixture tests prove missing/zero-hit reports fail | VERIFIED |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Focused coverage/gate tests pass | `bun test tests/lib/coverage-surface.test.ts tests/lib/verify-gates.test.ts` | 14 pass, 0 fail | PASS |
| Canonical coverage generates full report | `bun run coverage` | 43/43 unit coverage files and 68/68 integration coverage files passed; artifacts written | PASS |
| Sentinels have nonzero hits | `bun --print ...coverage-final.json...` | `messages.ts` 924/174, `ActionMenu.tsx` 407/253 | PASS |
| Local gate accepts fresh report | `bun run verify:gates` | `verify:gates passed: inventory, mapped tests, and coverage artifacts are aligned.` | PASS |
| Release-prep child commands pass | `bun run verify:prereqs && bun run test:deps`; `bun run test`; `bun run typecheck` | All exited 0; `bun run test` reported unit PASS and 68/68 integration files passed | PASS |

### Probe Execution

No `scripts/**/tests/probe-*.sh` probes exist in this repo. Step 7c: SKIPPED (no runnable probe files).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| COVR-01 | 84.1-01, 84.1-02 | Developer can run one command that generates a coverage report for the full test suite. | SATISFIED | `bun run coverage` exited 0 and generated full report artifacts. |
| COVR-02 | 84.1-01, 84.1-02 | Coverage reports include source files exercised by subprocess-based E2E tests through Istanbul-compatible instrumentation and merged artifacts. | SATISFIED | Runtime-root instrumentation and shard merge are implemented; canonical report contains real source counters. |
| COVR-03 | 84.1-01, 84.1-02 | Coverage output is stable and usable by local verification gates. | SATISFIED | `.coverage/coverage-final.json`, summary, LCOV, and HTML exist; `verify:gates` consumed them successfully. |
| COVR-04 | 84.1-01, 84.1-02 | Normal test commands are not slowed or changed unless coverage is requested. | SATISFIED | `package.json` normal `test`, `test:unit`, `test:integ`, `test:deps`, and `typecheck` command bodies are unchanged; `bun run test` passed. |
| GATE-03 | 84.1-01, 84.1-02 | Existing unit, integration, dependency, and typecheck commands continue to pass. | SATISFIED | `bun run test`, `bun run test:deps`, and `bun run typecheck` exited 0. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---:|---|---|---|
| `tests/lib/verify-gates.test.ts` | 53 | `test placeholder` fixture text | INFO | Test fixture content only; does not flow to user-visible output or implementation behavior. |

### Human Verification Required

None.

### Gaps Summary

No blocking gaps found. The canonical coverage path now produces a trustworthy Istanbul report for the known regular TS and TUI TSX sentinels, and local gates reject structurally valid reports when those sentinels are missing or zero-hit. The only notable verifier constraint is that the exact `bun run verify` wrapper was not rerun after its child commands were verified, to avoid immediately repeating the full coverage and test cycles.

---

_Verified: 2026-05-14T21:26:23Z_
_Verifier: the agent (gsd-verifier)_
