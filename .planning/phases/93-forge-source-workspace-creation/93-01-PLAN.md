---
phase: 93
plan: 01
title: Source CLI Contract And Resolver Handoff
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/workspace.ts
  - src/tui/workspace-wizard.ts
  - src/lib/workspace-source.ts
  - src/lib/integrations/forge-source.ts
  - tests/commands/workspace-source.test.ts
  - tests/lib/workspace-source.test.ts
requirements:
  - FSRC-01
  - FSRC-04
autonomous: true
must_haves:
  truths:
    - "D-01: `--source` requires at least one `--template` and rejects `--from`."
    - "D-02: ambiguous repo matches fail with `--repo <name>` guidance."
    - "D-03: only full forge web URLs are accepted."
    - "D-04: provider shorthand is not implemented."
    - "D-05: `--dry-run` previews resolved source, matched repo, branch/ref, and planned creation without side effects."
    - "D-09: existing workspace name collisions behave exactly like normal `git-stacks new`."
    - "D-17: resolver failures happen before workspace YAML or worktree side effects."
    - "D-20: GitLab, Gitea, and GitHub URL/provider paths are represented by the shared resolver shape."
  artifacts:
    - "Commander `new` options for --source, --repo, and --dry-run"
    - "Workspace-source orchestration entrypoint"
    - "Focused CLI and resolver-handoff tests"
  key_links:
    - "src/commands/workspace.ts passes source options to runWorkspaceNew()"
    - "src/tui/workspace-wizard.ts calls src/lib/workspace-source.ts before createWorkspace()"
    - "src/lib/workspace-source.ts consumes Phase 92 forge-source resolver contract"
---

# Phase 93 Plan 01 - Source CLI Contract And Resolver Handoff

<objective>
Add the user-facing `git-stacks new <name> --template <template> --source <forge-url>` contract and the no-side-effect resolver/matching handoff needed before source fetch and workspace creation.
</objective>

<tasks>
  <task id="93-01-01" type="auto">
    <name>Add source options to the new command and non-interactive validation</name>
    <read_first>
      - src/commands/workspace.ts
      - src/tui/workspace-wizard.ts
      - tests/commands/template-consumption.test.ts
      - .planning/phases/93-forge-source-workspace-creation/93-CONTEXT.md
    </read_first>
    <files>src/commands/workspace.ts, src/tui/workspace-wizard.ts, tests/commands/workspace-source.test.ts</files>
    <action>Add Commander options `--source <forge-url>`, `--repo <name>`, and `--dry-run` to `new`. Update the action option type and `runWorkspaceNew()` signature to receive `{ source?: string; repo?: string; dryRun?: boolean }` alongside existing `nonInteractive`, `branch`, and `open`. Enforce D-01 and D-03: if `--source` is present, require at least one `--template`, reject `--from`, reject missing workspace name in `--non-interactive`, and pass only full URL strings accepted by `new URL()`. Keep normal template and `--from` behavior unchanged when `--source` is absent. Add subprocess tests that `new src-ws --source https://gitlab.example.com/org/api/-/merge_requests/1 --non-interactive` without `--template` exits 1 and mentions `--source requires --template`, and that combining `--source` with `--from` exits 1 and mentions `--from and --source`.</action>
    <verify>
      <automated>bun test tests/commands/workspace-source.test.ts</automated>
    </verify>
    <acceptance_criteria>
      - `src/commands/workspace.ts` contains `.option("--source <forge-url>"`.
      - `src/commands/workspace.ts` contains `.option("--repo <name>"`.
      - `src/commands/workspace.ts` contains `.option("--dry-run"`.
      - `tests/commands/workspace-source.test.ts` asserts `--source` without `--template` exits 1.
      - `tests/commands/workspace-source.test.ts` asserts `--source` with `--from` exits 1.
    </acceptance_criteria>
    <done>CLI option parsing rejects invalid source creation combinations before resolver, fetch, YAML, or worktree side effects.</done>
  </task>

  <task id="93-01-02" type="auto">
    <name>Create source orchestration resolver handoff and dry-run preview</name>
    <read_first>
      - src/tui/workspace-wizard.ts
      - src/lib/integrations/forge-utils.ts
      - src/lib/integrations/forge-source.ts
      - .planning/phases/92-forge-source-research-and-resolver-design/92-03-PLAN.md
      - .planning/phases/93-forge-source-workspace-creation/93-PATTERNS.md
    </read_first>
    <files>src/lib/workspace-source.ts, src/tui/workspace-wizard.ts, tests/lib/workspace-source.test.ts, tests/commands/workspace-source.test.ts</files>
    <action>Create `src/lib/workspace-source.ts` with exported `prepareWorkspaceSource()` and `formatWorkspaceSourceError()` functions. `prepareWorkspaceSource()` accepts source URL, optional repo override, composed template repos, registry, workspace name, and dry-run flag; calls the Phase 92 forge source resolver/parser; validates matched repo is in the template and `mode: "worktree"`; returns `{ ok: true, branch, matchedRepoName, sourceMetadata, preview }` or typed failures `unsupported_forge`, `url_parse_failed`, `repo_not_matched`, `ambiguous_repo`, `template_repo_missing`, `not_worktree_mode`, `cli_unavailable`, `auth_required`, `metadata_unavailable`, and `branch_conflict`. `formatWorkspaceSourceError()` must mention `--repo <name>` for ambiguous matches and template override guidance for missing template repos. Wire `runWorkspaceNew()` so `--dry-run --source` prints source URL, forge id, change number, matched repo, source branch/ref, target branch, and planned workspace name, then returns without calling `createWorkspace()`.</action>
    <verify>
      <automated>bun test tests/lib/workspace-source.test.ts tests/commands/workspace-source.test.ts</automated>
    </verify>
    <acceptance_criteria>
      - `src/lib/workspace-source.ts` exports `prepareWorkspaceSource`.
      - `src/lib/workspace-source.ts` exports `formatWorkspaceSourceError`.
      - Ambiguous repo tests assert stderr/stdout includes `--repo <name>`.
      - Dry-run tests assert no workspace YAML file exists after the command.
      - Dry-run output contains `source`, `matched repo`, `source branch`, and `source ref`.
    </acceptance_criteria>
    <done>Source URL resolution and preview are available without creating workspaces or worktrees, and all pre-fetch failures are typed and actionable.</done>
  </task>
</tasks>

<threat_model>
| Threat | Surface | Mitigation |
|--------|---------|------------|
| T-93-01 spoofed or malformed URL creates unintended workspace | `--source` CLI input | Validate with `new URL()`, typed unsupported/parse failures, and no side effects before success. |
| T-93-02 ambiguous repo match chooses the wrong repository | template repo matching | Fail with `ambiguous_repo` and require `--repo <name>`. |
</threat_model>

<verification>
- `bun test tests/lib/workspace-source.test.ts tests/commands/workspace-source.test.ts`
- `bun run typecheck`
</verification>

<success_criteria>
- `git-stacks new --source` is only accepted with `--template`.
- Resolver and repo matching failures occur before YAML or worktree side effects.
- Dry-run previews source creation without writing workspace state.
</success_criteria>
