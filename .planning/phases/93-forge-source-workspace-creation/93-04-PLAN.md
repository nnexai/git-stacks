---
phase: 93
plan: 04
title: Provider Path Coverage And Final Gates
type: execute
wave: 3
depends_on:
  - "02"
  - "03"
files_modified:
  - tests/lib/integrations/forge-source.test.ts
  - tests/lib/workspace-source.test.ts
  - tests/commands/workspace-source.test.ts
  - tests/commands/workspace-source-git.test.ts
  - docs/forge-source-resolver.md
  - .planning/phases/93-forge-source-workspace-creation/93-VALIDATION.md
requirements:
  - FSRC-01
  - FSRC-04
  - FSRC-05
  - FSRC-06
  - FSRC-07
autonomous: true
must_haves:
  truths:
    - "D-19: fork/external contributor fetch failures provide actionable provider guidance."
    - "D-20: GitLab, Gitea, and GitHub source creation paths are implemented through the shared resolver shape."
    - "D-21: GitLab and GitHub rely on official docs, injected executor tests, URL/resolver contract tests, and local Git fixtures; Gitea may use local Tea command-shape validation."
    - "D-22: release/docs do not overstate live forge coverage."
    - "FSRC-07 is covered as an explicit non-implementation boundary because D-15 forbids auto-labeling in Phase 93."
  artifacts:
    - "Provider URL/resolver contract coverage for GitLab, Gitea, and GitHub"
    - "Source creation final focused gate"
    - "Documentation note for live validation limits and no provider checkout"
  key_links:
    - "tests prove all provider paths use resolver shape"
    - "docs explain live validation limits"
    - "final gate runs source tests and typecheck"
---

# Phase 93 Plan 04 - Provider Path Coverage And Final Gates

<objective>
Close Phase 93 with provider-path coverage for GitLab, Gitea, and GitHub, local source-workspace smoke tests, and documentation/source assertions that keep live validation and label behavior honest.
</objective>

<tasks>
  <task id="93-04-01" type="auto">
    <name>Cover GitLab, Gitea, and GitHub source paths through resolver contracts</name>
    <read_first>
      - src/lib/integrations/forge-source.ts
      - src/lib/workspace-source.ts
      - tests/lib/integrations/forge-source.test.ts
      - tests/lib/workspace-source.test.ts
      - .planning/phases/93-forge-source-workspace-creation/93-RESEARCH.md
    </read_first>
    <files>tests/lib/integrations/forge-source.test.ts, tests/lib/workspace-source.test.ts, tests/commands/workspace-source.test.ts</files>
    <action>Add or extend tests so GitLab `https://gitlab.example.com/group/api/-/merge_requests/42`, Gitea `https://git.example.test/org/api/pulls/7`, and GitHub `https://github.com/org/api/pull/9` all produce normalized source metadata consumed by workspace creation. The tests must assert provider terminology: GitLab uses `change_type: "mr"` and GitHub/Gitea use `change_type: "pr"`. Tests must exercise provider-specific fetch guidance for auth/permission/fork access failures without invoking live `glab`, `gh`, or authenticated `tea`.</action>
    <verify>
      <automated>bun test tests/lib/integrations/forge-source.test.ts tests/lib/workspace-source.test.ts tests/commands/workspace-source.test.ts</automated>
    </verify>
    <acceptance_criteria>
      - Tests include at least one GitLab MR URL with `/-/merge_requests/42`.
      - Tests include at least one Gitea PR URL with `/pulls/7`.
      - Tests include at least one GitHub PR URL with `/pull/9`.
      - Tests assert GitLab metadata uses `mr` and GitHub/Gitea metadata uses `pr`.
      - Tests do not require installed or authenticated `glab` or `gh`.
    </acceptance_criteria>
    <done>All three provider paths are represented through the shared resolver/source creation contract under local automated tests.</done>
  </task>

  <task id="93-04-02" type="auto">
    <name>Document validation limits and assert provider checkout commands are not internal implementation</name>
    <read_first>
      - docs/forge-source-resolver.md
      - .planning/phases/92-forge-source-research-and-resolver-design/92-RESEARCH.md
      - .planning/phases/93-forge-source-workspace-creation/93-RESEARCH.md
      - .planning/REQUIREMENTS.md
    </read_first>
    <files>docs/forge-source-resolver.md, .planning/phases/93-forge-source-workspace-creation/93-VALIDATION.md</files>
    <action>Update `docs/forge-source-resolver.md` with a Phase 93 implementation note that source workspace creation uses plain Git fetch plus existing git-stacks worktree creation, not provider checkout commands. State GitLab/GitHub live authenticated runs are not local automated gates, Gitea Tea validation is limited to local command shape unless credentials are available, and Phase 93 does not auto-label source-created workspaces because D-15 defers auto-labels. Update `93-VALIDATION.md` sign-off notes if final focused commands differ from this plan.</action>
    <verify>
      <automated>rg -n "plain Git fetch|Provider checkout commands are not internal checkout implementation|live authenticated|does not auto-label|D-15" docs/forge-source-resolver.md .planning/phases/93-forge-source-workspace-creation/93-VALIDATION.md</automated>
    </verify>
    <acceptance_criteria>
      - `docs/forge-source-resolver.md` contains `Provider checkout commands are not internal checkout implementation`.
      - `docs/forge-source-resolver.md` mentions GitLab/GitHub live authenticated validation limits.
      - `docs/forge-source-resolver.md` says Phase 93 does not auto-label source-created workspaces per D-15.
    </acceptance_criteria>
    <done>Project docs and validation notes accurately describe what was implemented and what was not live-validated.</done>
  </task>

  <task id="93-04-03" type="auto">
    <name>Run final focused source-workspace gate and typecheck</name>
    <read_first>
      - package.json
      - tests/lib/integrations/forge-source.test.ts
      - tests/lib/workspace-source.test.ts
      - tests/commands/workspace-source.test.ts
      - tests/commands/workspace-source-git.test.ts
      - tests/lib/config.test.ts
    </read_first>
    <files>tests/lib/integrations/forge-source.test.ts, tests/lib/workspace-source.test.ts, tests/commands/workspace-source.test.ts, tests/commands/workspace-source-git.test.ts, tests/lib/config.test.ts</files>
    <action>Run the final focused gate: `bun test tests/lib/integrations/forge-source.test.ts tests/lib/workspace-source.test.ts tests/commands/workspace-source.test.ts tests/commands/workspace-source-git.test.ts tests/lib/config.test.ts && bun run typecheck`. Fix only Phase 93 regressions in source creation, schema, tests, or docs. Do not add provider shorthand, dashboard UI, live forge matrix requirements, or source auto-label implementation.</action>
    <verify>
      <automated>bun test tests/lib/integrations/forge-source.test.ts tests/lib/workspace-source.test.ts tests/commands/workspace-source.test.ts tests/commands/workspace-source-git.test.ts tests/lib/config.test.ts && bun run typecheck</automated>
    </verify>
    <acceptance_criteria>
      - Focused Bun test command exits 0.
      - `bun run typecheck` exits 0.
      - `rg -n "glab mr checkout|gh pr checkout|tea pulls checkout" src` returns no internal checkout implementation hits.
      - Source-created workspace tests cover YAML, branch/ref, dry-run, and failure guidance.
    </acceptance_criteria>
    <done>The Phase 93 source workspace implementation is ready for verification with local automated evidence and honest validation boundaries.</done>
  </task>
</tasks>

<threat_model>
| Threat | Surface | Mitigation |
|--------|---------|------------|
| T-93-09 provider-specific path silently untested | GitLab/Gitea/GitHub sources | Contract tests cover each URL/provider shape and metadata terminology. |
| T-93-10 docs overclaim live forge support | docs/release boundary | Source assertions require validation limit wording. |
| T-93-11 deferred labels sneak into implementation | source labels | Final tests assert no automatic review/source labels are added. |
</threat_model>

<verification>
- `bun test tests/lib/integrations/forge-source.test.ts tests/lib/workspace-source.test.ts tests/commands/workspace-source.test.ts tests/commands/workspace-source-git.test.ts tests/lib/config.test.ts`
- `bun run typecheck`
- `rg -n "glab mr checkout|gh pr checkout|tea pulls checkout" src` should return no implementation hits.
</verification>

<success_criteria>
- GitLab, Gitea, and GitHub source creation paths are covered through local resolver/source contracts.
- Final source workspace focused tests and typecheck pass.
- Documentation does not overstate live forge validation or auto-label support.
</success_criteria>
