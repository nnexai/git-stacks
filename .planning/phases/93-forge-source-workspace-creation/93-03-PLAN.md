---
phase: 93
plan: 03
title: Source Metadata Schema And Label Boundary
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/lib/config.ts
  - src/lib/workspace-lifecycle.ts
  - src/lib/workspace-source.ts
  - tests/lib/config.test.ts
  - tests/lib/workspace-lifecycle-create.test.ts
  - tests/commands/workspace-source.test.ts
requirements:
  - FSRC-06
  - FSRC-07
autonomous: true
must_haves:
  truths:
    - "D-12: source provenance is a dedicated `WorkspaceSchema.source` block."
    - "D-13: source provenance is not stored under `settings.integrations`."
    - "D-14: normalized source metadata includes URL, forge id, base URL, change type/number, source/target branch/ref, matched repo, web URL, and fetched ref."
    - "D-15: Phase 93 does not auto-label source-created workspaces; template and manual labels remain the only label inputs."
    - "D-16: existing YAML edit flows expose source metadata, and schema validation fails clearly for malformed source metadata."
    - "D-22: docs/release wording must not overstate live forge coverage."
  artifacts:
    - "WorkspaceSourceSchema"
    - "CreateWorkspaceInputs.source"
    - "YAML persistence and validation tests"
    - "Explicit label non-goal assertions"
  key_links:
    - "workspace-source produces source metadata"
    - "createWorkspace writes source metadata to workspace YAML"
    - "config schema validates edited source metadata"
---

# Phase 93 Plan 03 - Source Metadata Schema And Label Boundary

<objective>
Persist forge-source provenance as a dedicated workspace YAML block while preserving the locked Phase 93 decision that source creation does not add automatic review/source labels.
</objective>

<tasks>
  <task id="93-03-01" type="auto">
    <name>Add WorkspaceSchema source block and config validation</name>
    <read_first>
      - src/lib/config.ts
      - tests/lib/config.test.ts
      - .planning/phases/93-forge-source-workspace-creation/93-CONTEXT.md
    </read_first>
    <files>src/lib/config.ts, tests/lib/config.test.ts</files>
    <action>Add `WorkspaceSourceSchema` in `src/lib/config.ts` with `kind: z.literal("forge")`, `forge: z.enum(["gitlab", "gitea", "github"])`, `base_url`, `url`, `change_type: z.enum(["mr", "pr"])`, `change_number`, `repo`, `repo_path`, `source_branch`, `source_ref`, `target_branch`, `web_url`, and `fetched_ref`. Add optional `title` only when metadata is available. Add `source: WorkspaceSourceSchema.optional()` to `WorkspaceSchema`, export `WorkspaceSource`, and do not modify `WorkspaceSettingsSchema` for provenance. Add config tests that valid source YAML parses, malformed `source.kind` fails with an invalid config message, and `settings.integrations.gitlab.source` is not required or used for source provenance.</action>
    <verify>
      <automated>bun test tests/lib/config.test.ts</automated>
    </verify>
    <acceptance_criteria>
      - `src/lib/config.ts` exports `WorkspaceSourceSchema`.
      - `WorkspaceSchema` contains `source: WorkspaceSourceSchema.optional()`.
      - `tests/lib/config.test.ts` includes a valid `source:` YAML parse assertion.
      - `tests/lib/config.test.ts` includes an invalid source metadata failure assertion.
    </acceptance_criteria>
    <done>Workspace YAML accepts and validates a dedicated forge source provenance block outside integration settings.</done>
  </task>

  <task id="93-03-02" type="auto">
    <name>Write source metadata during workspace creation</name>
    <read_first>
      - src/lib/workspace-lifecycle.ts
      - src/lib/workspace-source.ts
      - tests/lib/workspace-lifecycle-create.test.ts
      - tests/commands/workspace-source.test.ts
    </read_first>
    <files>src/lib/workspace-lifecycle.ts, src/lib/workspace-source.ts, tests/lib/workspace-lifecycle-create.test.ts, tests/commands/workspace-source.test.ts</files>
    <action>Extend `CreateWorkspaceInputs` with `source?: WorkspaceSource`. In `createWorkspace()`, add `...(inputs.source ? { source: inputs.source } : {})` to `workspaceObj` before the commit point. Ensure `workspace-source` maps resolver metadata into the exact schema keys: `url`, `forge`, `base_url`, `change_type`, `change_number`, `repo`, `repo_path`, `source_branch`, `source_ref`, `target_branch`, `web_url`, and `fetched_ref`. Add tests that a successful source-created workspace YAML contains top-level `source:` and does not contain source provenance under `settings.integrations`.</action>
    <verify>
      <automated>bun test tests/lib/workspace-lifecycle-create.test.ts tests/commands/workspace-source.test.ts</automated>
    </verify>
    <acceptance_criteria>
      - `CreateWorkspaceInputs` includes `source?: WorkspaceSource`.
      - Workspace YAML from a source-created subprocess test contains `source:`.
      - The same YAML contains `forge: gitlab` and `change_number: "42"` or `change_number: 42`.
      - The same YAML does not contain `settings.integrations.gitlab.source`.
    </acceptance_criteria>
    <done>Source-created workspaces persist normalized forge provenance in the dedicated source block.</done>
  </task>

  <task id="93-03-03" type="auto">
    <name>Preserve manual/template labels and assert no source auto-labeling</name>
    <read_first>
      - src/lib/workspace-lifecycle.ts
      - src/tui/workspace-wizard.ts
      - tests/commands/template-consumption.test.ts
      - tests/commands/workspace-source.test.ts
      - .planning/REQUIREMENTS.md
      - .planning/phases/93-forge-source-workspace-creation/93-CONTEXT.md
    </read_first>
    <files>tests/commands/workspace-source.test.ts, .planning/phases/93-forge-source-workspace-creation/93-RESEARCH.md</files>
    <action>Add explicit tests for the D-15/FSRC-07 boundary: a source-created workspace using a template with labels and `--label manual` keeps only template/manual labels and does not add `review`, forge id, or change-number labels. Do not add source label implementation. Keep `93-RESEARCH.md` Common Pitfalls documenting that FSRC-07 conflicts with locked D-15 and is intentionally not implemented in Phase 93.</action>
    <verify>
      <automated>bun test tests/commands/workspace-source.test.ts && rg -n "FSRC-07 conflicts with D-15|Do not auto-label" .planning/phases/93-forge-source-workspace-creation/93-RESEARCH.md</automated>
    </verify>
    <acceptance_criteria>
      - `tests/commands/workspace-source.test.ts` asserts source creation does not add `review` label.
      - `tests/commands/workspace-source.test.ts` asserts source creation does not add `gitlab` label unless explicitly supplied as `--label`.
      - `tests/commands/workspace-source.test.ts` asserts template labels still persist.
      - `93-RESEARCH.md` documents the FSRC-07/D-15 conflict.
    </acceptance_criteria>
    <done>Label behavior remains exactly template/manual only, with the roadmap requirement conflict made visible instead of silently implemented against CONTEXT.md.</done>
  </task>
</tasks>

<threat_model>
| Threat | Surface | Mitigation |
|--------|---------|------------|
| T-93-06 malformed source YAML breaks later commands | workspace YAML edit flow | Zod schema validates the dedicated source block and fails clearly on malformed values. |
| T-93-07 source provenance hidden in integration settings | workspace metadata | Store provenance only in top-level `source`, never under `settings.integrations`. |
| T-93-08 labels imply unverified review/source semantics | workspace labels | Do not auto-label; preserve existing template/manual label behavior. |
</threat_model>

<verification>
- `bun test tests/lib/config.test.ts tests/lib/workspace-lifecycle-create.test.ts tests/commands/workspace-source.test.ts`
- `bun run typecheck`
</verification>

<success_criteria>
- Source-created workspace YAML includes a valid dedicated `source` block.
- Existing YAML edit validation catches malformed source metadata.
- Source creation does not add review/source labels in Phase 93.
</success_criteria>
