---
phase: 41-release-prep
plan: 01
type: standard
wave: 1
depends_on: []
files_modified:
  - package.json
  - CHANGELOG.md
  - README.md
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03

must_haves:
  truths:
    - "`package.json` `version` field reads `\"0.10.0\"`"
    - "CHANGELOG.md has a `## [0.10.0]` section at the top (above `## [0.9.1]`) dated with the release date"
    - "CHANGELOG.md v0.10.0 entry covers four features: `paths` command, `pull` command, TUI upstream staleness badges, template composition"
    - "CHANGELOG.md follows the existing Keep a Changelog format: `### Added` / `### Changed` subsections with bold title + 2-3 sentence descriptions"
    - "README.md documents `git-stacks paths` with usage examples including the `--prefix` agent CLI injection pattern"
    - "README.md documents `git-stacks pull` with usage examples"
    - "README.md mentions template composition (`includes:` and multi-`--template`) in the Templates section"
    - "README.md mentions TUI upstream staleness badges in the Dashboard section"
  artifacts:
    - path: "package.json"
      provides: "Version bump to 0.10.0"
      contains: "\"version\": \"0.10.0\""
    - path: "CHANGELOG.md"
      provides: "v0.10.0 release notes"
      contains: "## [0.10.0]"
    - path: "README.md"
      provides: "Documentation for paths and pull commands"
      contains: "git-stacks paths"
  key_links:
    - from: "CHANGELOG.md"
      to: "package.json"
      via: "Version number must match"
      pattern: "0.10.0"
---

<objective>
Release preparation for v0.10.0: bump version in package.json, add CHANGELOG entry covering all four new features (paths, pull, TUI staleness, template composition), and update README with new command documentation and usage examples.

Purpose: Ship v0.10.0 as a documented release with all new features discoverable by users reading the CHANGELOG and README.

Output: Updated package.json (version), CHANGELOG.md (new release section), and README.md (new command docs).
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-release-prep/41-CONTEXT.md

@package.json
@CHANGELOG.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version in package.json to 0.10.0</name>
  <files>package.json</files>
  <read_first>
    - package.json (current version field — should read "0.9.1")
  </read_first>
  <action>
Edit `package.json` and change the `version` field from `"0.9.1"` to `"0.10.0"`.

The exact edit:
- Old: `"version": "0.9.1"`
- New: `"version": "0.10.0"`

No other fields in package.json should be changed.

Commit: `chore(41-01): bump version to 0.10.0`
  </action>
  <acceptance_criteria>
    - `package.json` contains `"version": "0.10.0"`
    - `package.json` does NOT contain `"0.9.1"` in the version field
    - `grep '"version": "0.10.0"' package.json` returns a match
    - No other fields in package.json were modified
  </acceptance_criteria>
  <verify>
    <automated>grep '"version": "0.10.0"' package.json</automated>
  </verify>
  <done>package.json version field reads "0.10.0".</done>
</task>

<task type="auto">
  <name>Task 2: Add v0.10.0 section to CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (lines 1-40 — existing format: `## [version] — date` headers, `### Added` / `### Changed` / `### Fixed` subsections, bold title + 2-3 sentence description per feature)
    - .planning/phases/41-release-prep/41-CONTEXT.md (D-04 and D-05 — format and feature list decisions)
    - src/commands/workspace.ts (lines containing `.command("paths` and `.command("pull` — verify exact command names and options for accurate descriptions)
    - src/lib/config.ts (grep for `includes` in StackSchema — verify template composition schema field name)
  </read_first>
  <action>
Insert a new `## [0.10.0]` section at the top of CHANGELOG.md, between the `---` separator (line 5) and the existing `## [0.9.1]` section (line 7).

The new section to insert (use today's date in YYYY-MM-DD format):

```markdown
## [0.10.0] — 2026-03-26

### Added

**Agent path discovery** — `git-stacks paths [workspace]` outputs one repo path per line to stdout, designed for piping into agent CLI tools. Pass `--prefix "--add-dir"` to prepend each path with a flag string (e.g., `--add-dir /home/user/workspaces/tasks/ws/api`). Supports `--filter worktree|trunk` to restrict output by repo mode. Autodetects workspace from current directory when no argument is given.

**Multi-repo pull** — `git-stacks pull [workspace]` pulls the latest commits for all repos in a workspace with a single command. Worktree repos pull their workspace branch; trunk repos pull their default branch. Uses `--ff-only` for safety — diverged branches fail fast with a clear message. Dirty repos are skipped with a warning and the command exits non-zero. Deduplicates fetches per unique clone path.

**TUI upstream staleness** — the dashboard workspace detail pane now shows an "N behind" badge per repo when upstream has newer commits. Staleness is fetched on workspace focus with a 5-minute TTL cache. Press `r` on a focused workspace to force-refresh. Network failures show a `?` badge without crashing the TUI.

**Template composition** — templates can declare `includes: [base, api]` to merge repos from other templates as building blocks. `git-stacks new --template api --template frontend` composes templates ad-hoc without requiring a saved meta-template. When the same repo appears in multiple templates, worktree mode wins over trunk. Hooks concatenate in include order with the top-level template's hooks running last. Circular includes are detected with a clear error.

---
```

The inserted text goes between line 5 (`---`) and line 7 (`## [0.9.1]`). The result should be:
- Line 5: `---` (existing)
- Line 6: empty line
- Lines 7-20: new v0.10.0 section
- Line 21: `---` (separator before 0.9.1)
- Line 22: empty line
- Line 23: `## [0.9.1] — 2026-03-25` (existing, shifted down)

Commit: `docs(41-01): add v0.10.0 CHANGELOG entry`
  </action>
  <acceptance_criteria>
    - CHANGELOG.md contains `## [0.10.0]`
    - The `## [0.10.0]` section appears BEFORE `## [0.9.1]`
    - CHANGELOG.md contains `**Agent path discovery**`
    - CHANGELOG.md contains `**Multi-repo pull**`
    - CHANGELOG.md contains `**TUI upstream staleness**`
    - CHANGELOG.md contains `**Template composition**`
    - CHANGELOG.md contains `### Added` under the `## [0.10.0]` header
    - `grep -c "## \[0.10.0\]" CHANGELOG.md` returns `1`
    - The section follows Keep a Changelog format matching v0.9.0 style (bold title + description)
  </acceptance_criteria>
  <verify>
    <automated>grep "## \[0.10.0\]" CHANGELOG.md && grep "Agent path discovery" CHANGELOG.md && grep "Multi-repo pull" CHANGELOG.md && grep "TUI upstream staleness" CHANGELOG.md && grep "Template composition" CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG.md has a complete v0.10.0 section with all four features documented.</done>
</task>

<task type="auto">
  <name>Task 3: Update README.md with new command documentation</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — understand existing structure: Concepts, Quick Start, Repo Registry, Templates, Workspaces, Dashboard, Configuration sections)
    - .planning/phases/41-release-prep/41-CONTEXT.md (D-01 through D-03 and specifics — README documentation decisions)
    - src/commands/workspace.ts (grep for `.command("paths` and `.command("pull` — verify exact option names and descriptions for accurate docs)
    - src/lib/config.ts (grep for `includes` — verify template composition field name)
  </read_first>
  <action>
Make three additions to README.md:

**Addition 1: Add `paths` and `pull` commands to the Workspaces section**

In the Workspaces command list block (lines 73-87), add these two lines after `git-stacks cd <name> [repo]` (line 86) and before the closing triple-backtick (line 87):

```
git-stacks paths [workspace]       # Output repo paths (one per line) for agent CLI injection
git-stacks pull [workspace]        # Pull latest for all repos (--ff-only, skips dirty)
```

**Addition 2: Add a new "Agent Path Discovery" section after the "Shell `cd` integration" section (after line 101) and before "Workspace Notifications" (line 103)**

Insert this new section:

```markdown
## Agent Path Discovery

Query workspace repo paths for injection into agent CLI tools:

```bash
# List all repo paths in a workspace (one per line)
git-stacks paths my-feature

# Prepend each path with a CLI flag for piping into an agent command
git-stacks paths my-feature --prefix "--add-dir"
# Output:
#   --add-dir /home/user/workspaces/tasks/my-feature/api
#   --add-dir /home/user/workspaces/tasks/my-feature/frontend
#   --add-dir /home/user/workspaces/main/shared-lib

# Inject directly into an agent CLI invocation
claude $(git-stacks paths my-feature --prefix "--add-dir")

# Filter by repo mode
git-stacks paths my-feature --filter worktree   # Only worktree repos
git-stacks paths my-feature --filter trunk       # Only trunk repos

# Autodetects workspace when run inside a worktree directory
cd ~/workspaces/tasks/my-feature/api
git-stacks paths    # same as: git-stacks paths my-feature
```

Worktree repos emit their task path; trunk repos emit their main clone path. Repos with missing task directories are skipped with a stderr warning.
```

**Addition 3: Add `pull` command section right after the Agent Path Discovery section**

```markdown
## Multi-Repo Pull

Pull latest commits for all repos in a workspace:

```bash
# Pull all repos in a workspace
git-stacks pull my-feature

# Autodetects workspace from current directory
git-stacks pull
```

Worktree repos pull their workspace branch; trunk repos pull their default branch. Uses `--ff-only` for safety — diverged branches produce a clear error identifying the repo and branch. Dirty repos (uncommitted changes) are skipped with a warning. Exit code is non-zero if any repo was skipped or failed.
```

**Addition 4: Update the Templates section (lines 59-68)**

After the existing template command block's closing triple-backtick (line 68), add a brief paragraph about template composition:

```markdown

Templates support composition via `includes:` — declare building-block templates that merge their repos:

```yaml
# ~/.config/git-stacks/templates/full-stack.yml
name: full-stack
includes: [api, frontend]    # Merges repos from both templates
repos: []                     # Can add additional repos on top
```

Ad-hoc composition on the command line: `git-stacks new my-feature --template api --template frontend`. When the same repo appears in multiple templates, worktree mode wins over trunk. Hooks concatenate in include order with the top-level template's hooks running last.
```

**Addition 5: Update the Dashboard section description (line 140)**

On line 140, update the workspace tab description to mention upstream staleness. Change:

```
- **Workspaces tab**: open, rename, sync, merge, run, clean, remove, edit YAML; notification previews in list rows; full message history in detail pane
```

To:

```
- **Workspaces tab**: open, rename, sync, merge, run, clean, remove, edit YAML; notification previews in list rows; full message history in detail pane; per-repo "N behind" staleness badges in detail view
```

And add a new bullet after the `r` keybinding line (line 144):

After:
```
  - `r` — reload data from disk
```

This line already exists. Keep it as-is. The staleness refresh keybinding is also `r` when a workspace is focused, so no separate keybinding entry is needed — the existing `r` description covers both reload and refresh. If the TUI implementation uses a different key, adjust accordingly.

Commit: `docs(41-01): update README with paths, pull, composition, and staleness docs`
  </action>
  <acceptance_criteria>
    - README.md contains `git-stacks paths [workspace]` in the workspace command list
    - README.md contains `git-stacks pull [workspace]` in the workspace command list
    - README.md contains `## Agent Path Discovery` as a section header
    - README.md contains `## Multi-Repo Pull` as a section header
    - README.md contains the agent CLI injection example: `claude $(git-stacks paths my-feature --prefix "--add-dir")`
    - README.md contains `--filter worktree` usage example
    - README.md contains `git-stacks pull my-feature` usage example
    - README.md contains `includes: [api, frontend]` template composition example
    - README.md contains `--template api --template frontend` ad-hoc composition example
    - README.md mentions "N behind" staleness in the Dashboard section
    - `grep -c "Agent Path Discovery" README.md` returns `1`
    - `grep -c "Multi-Repo Pull" README.md` returns `1`
    - `grep "includes:" README.md` returns a match
    - The existing README sections (Concepts, Quick Start, Repo Registry, etc.) are unchanged
  </acceptance_criteria>
  <verify>
    <automated>grep "Agent Path Discovery" README.md && grep "Multi-Repo Pull" README.md && grep "git-stacks paths" README.md && grep "git-stacks pull" README.md && grep "includes:" README.md && grep "staleness" README.md</automated>
  </verify>
  <done>README.md updated with paths command docs, pull command docs, template composition docs, and TUI staleness mention.</done>
</task>

</tasks>

<verification>
```bash
# Version bump
grep '"version": "0.10.0"' package.json

# CHANGELOG has v0.10.0 entry
grep "## \[0.10.0\]" CHANGELOG.md

# CHANGELOG covers all four features
grep "Agent path discovery" CHANGELOG.md
grep "Multi-repo pull" CHANGELOG.md
grep "TUI upstream staleness" CHANGELOG.md
grep "Template composition" CHANGELOG.md

# README has new sections
grep "## Agent Path Discovery" README.md
grep "## Multi-Repo Pull" README.md

# README has usage examples
grep "git-stacks paths" README.md
grep "git-stacks pull" README.md
grep "\-\-prefix" README.md
grep "includes:" README.md

# README mentions staleness in dashboard
grep "staleness" README.md

# Typecheck still passes (no code changes, but verify)
bun run typecheck
```
</verification>

<success_criteria>
1. `package.json` version field reads `0.10.0`
2. CHANGELOG.md has a `## [0.10.0]` section covering all four features: paths, pull, TUI staleness, template composition
3. README.md documents `git-stacks paths` with usage examples including the `--prefix` agent CLI injection pattern
4. README.md documents `git-stacks pull` with usage examples
5. README.md mentions template composition (`includes:` and multi-`--template`) in the Templates section
6. README.md mentions TUI upstream staleness badges in the Dashboard section
7. All existing content in CHANGELOG and README is preserved (no regressions)
8. Typecheck still passes
</success_criteria>

<output>
After completion, create `.planning/phases/41-release-prep/41-01-SUMMARY.md`
</output>
