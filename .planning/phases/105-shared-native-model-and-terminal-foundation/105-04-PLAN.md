---
phase: 105-shared-native-model-and-terminal-foundation
plan: 04
type: execute
wave: 4
depends_on: [105-01, 105-03]
files_modified:
  - native/terminal/ownership.zig
  - native/terminal/guard.zig
  - native/terminal/diagnostics.zig
  - native/tests/ownership_test.zig
autonomous: true
requirements: [CORE-02, CORE-05, TERM-02, TERM-04]
must_haves:
  truths:
    - "One live surface exclusively owns one PTY/process group and all normal/crash teardown paths prove absence or preserve failed_cleanup."
    - "The separate guard cleans registered process groups after client death through a private inherited channel."
  artifacts:
    - path: "native/terminal/ownership.zig"
      provides: "Exclusive lifecycle, confirmation, escalation, reaping, and cleanup-proof state machine"
    - path: "native/terminal/guard.zig"
      provides: "Private inherited-channel crash cleanup guard"
  key_links:
    - from: "native/terminal/ownership.zig"
      to: "native/terminal/guard.zig"
      via: "registration before live exposure and unregister after absence proof"
    - from: "native/terminal/ownership.zig"
      to: "native/core/reducer.zig"
      via: "ended/failed_cleanup lifecycle actions and close-confirmation effects"
---

<objective>
Implement and prove terminal process ownership before a GTK/libghostty host consumes it.

Purpose: Establish teardown, crash cleanup, and failed-cleanup truth as a narrow executable boundary.
Output: Ownership state machine, sibling guard, redacted diagnostics, and real process-group tests.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-CONTEXT.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-RESEARCH.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-VALIDATION.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-01-SUMMARY.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement exclusive PTY/process-group ownership and the crash guard</name>
  <files>native/terminal/ownership.zig, native/terminal/guard.zig, native/terminal/diagnostics.zig, native/tests/ownership_test.zig</files>
  <behavior>
    - "Creation reaches live only after surface, PTY, child PID/PGID, birth token, and guard registration all succeed."
    - "Meaningful foreground activity requests confirmation; idle shells and ended surfaces do not per D-10."
    - "Close requests graceful exit for 2 seconds, then escalates the entire owned group through SIGHUP, SIGTERM after 500 ms, and SIGKILL after 2 seconds per D-09."
    - "Client control-channel EOF makes the sibling guard clean every registered group; forged/public commands and self/guard group IDs are rejected per D-12."
    - "Absence that cannot be proved within injected polling bounds yields visible failed_cleanup with bounded redacted diagnostics per D-11."
  </behavior>
  <action>Build the explicit ownership state machine with injectable clock, sleep/poll, signal, reap, `/proc` birth-token lookup, spawn, and private guard-channel seams. Acquire forward and unwind reverse; do not commit `live` until every ownership fact and guard registration is confirmed. Validate PGID differs from client and guard groups and pair it with a Linux birth token to resist reuse. Implement idempotent child-exit, close, quit, rapid-close-before-setsid, and duplicate-callback handling. Apply D-09/D-10/D-11 using the researched injectable timeout/escalation defaults, preserving the primary error while collecting cleanup diagnostics. Implement D-12 as a sibling guard using only an inherited private pipe/socket: registration precedes live exposure, unregister follows proven absence, EOF triggers bounded teardown of all groups, and diagnostics exclude argv, environment, tokens, clipboard, and terminal bytes. Add real process-group tests for descendants, escalation, client SIGKILL, reuse/self-group rejection, guard protocol forgery, and injected failed cleanup.</action>
  <verify><automated>bun run native:test:lifecycle</automated></verify>
  <done>All exit/close/quit/crash paths either prove every owned process absent and end cleanly or retain failed_cleanup; the client SIGKILL test leaves zero registered descendants.</done>
</task>

</tasks>

<verification>
Run `bun run native:test:lifecycle` and confirm normal close, forced escalation, app quit, client SIGKILL, PGID reuse rejection, and injected failure preserve the required ownership truth.
</verification>

<success_criteria>
Ownership and guard behavior are independently executable and verified before terminal host integration begins.
</success_criteria>

<output>
Create `.planning/phases/105-shared-native-model-and-terminal-foundation/105-04-SUMMARY.md` when done.
</output>
