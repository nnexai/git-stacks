---
phase: 105-shared-native-model-and-terminal-foundation
plan: 05
type: execute
wave: 5
depends_on: [105-04]
files_modified:
  - native/build.zig
  - native/terminal/adapter.zig
  - native/terminal/vt_adapter.zig
  - native/linux/app.zig
  - native/linux/terminal_host.zig
  - native/linux/terminal_widget.zig
  - native/linux/renderer.zig
  - native/tests/terminal_host_test.zig
  - native/tests/vt_adapter_test.zig
  - native/tests/renderer_test.zig
  - native/tests/terminal_widget_test.zig
  - scripts/verify-native.ts
  - package.json
autonomous: true
requirements: [TERM-01, TERM-03, TERM-05]
must_haves:
  truths:
    - "The exact pinned Zig ghostty-vt module constructs terminal, stream, and render state and is the sole production Ghostty API boundary per D-15."
    - "A production GTK executable opens a real focusable terminal widget, displays deterministic VT content, resizes, and quits cleanly."
    - "Graphical prerequisites fail closed with bounded waits and captured display, GTK, renderer, and stderr diagnostics."
  artifacts:
    - path: native/terminal/vt_adapter.zig
      provides: Sole pinned ghostty-vt import and product terminal-state interface
    - path: native/linux/app.zig
      provides: Production GtkApplication main used by both native:run and smoke tests
    - path: native/linux/terminal_widget.zig
      provides: Focusable custom GTK widget with snapshot and generation-safe lifecycle
    - path: native/linux/renderer.zig
      provides: Replaceable GtkSnapshot/GSK/Pango renderer seam and deterministic frame renderer
  key_links:
    - from: native/build.zig
      to: pinned Ghostty src/lib_vt.zig
      via: exact-source Zig module declaration
    - from: native/linux/terminal_widget.zig
      to: native/terminal/vt_adapter.zig
      via: immutable render-frame snapshot
    - from: scripts/verify-native.ts
      to: native/linux/app.zig
      via: native:smoke-app launches the same production main as native:run
---

<objective>
Replace the invalid full-surface assumption with the pinned Zig `ghostty-vt` boundary and ship a genuinely runnable Linux GTK terminal frame.

Purpose: Make the first corrective slice observable in a real window and prove the new API/build/widget boundary before PTY integration.
Output: Pinned VT adapter, custom GTK widget, snapshot renderer seam, production executable, and bounded graphical smoke.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-CONTEXT.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-RESEARCH.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-VALIDATION.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-PATTERNS.md
@.planning/spikes/004-a-upstream-linux-embedding-api/README.md
@.planning/spikes/004-b-pinned-internal-gtk-integration/README.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-04-SUMMARY.md
</context>

<tasks>

<scope_cohesion>
This plan intentionally spans the exact-pin migration, widget, and executable because they form one atomic vertical proof: the new VT boundary is not validated as the Linux foundation until the production GTK main renders its state. Splitting before the runnable frame would recreate the compile-only failure mode that triggered this corrective replan. Tasks retain focused commits and gates inside the plan.
</scope_cohesion>

<task type="auto" tdd="true">
  <name>Task 1: Replace the synthetic full-surface seam with the pinned VT adapter</name>
  <files>native/build.zig, native/terminal/adapter.zig, native/terminal/vt_adapter.zig, native/tests/vt_adapter_test.zig, scripts/verify-native.ts, package.json</files>
  <read_first>native/build.zig, native/terminal/adapter.zig, native/tests/terminal_host_test.zig, scripts/verify-native.ts, native/deps/ghostty.lock, .planning/phases/105-shared-native-model-and-terminal-foundation/105-RESEARCH.md, .planning/phases/105-shared-native-model-and-terminal-foundation/105-PATTERNS.md</read_first>
  <behavior>
    - "A real pinned Terminal/Stream/RenderState parses printable and Unicode bytes, resizes/reflows, enters and exits alternate screen, and yields inspectable cells/modes."
    - "Key and safe-paste encoding are product-facing adapter operations; Ghostty types never reach GTK, native/core, or the public C ABI."
    - "Old full-surface symbols, nullable fake surface state, and synthetic event-recording interaction tests are absent."
  </behavior>
  <action>Write the failing compatibility tests first, then declare pinned `src/lib_vt.zig` as a Zig module and implement `vt_adapter.zig` as the only production import per D-15. Own Terminal, Stream, RenderState, resize, parsing, immutable frame extraction, mode/event extraction, key encoding, and safe paste behind product types. Replace—not preserve—the old `adapter.zig` event recorder; rename/remove it and update the audit so permitted Ghostty references are limited to `vt_adapter.zig`, the build declaration, and named compatibility tests. Delete all production/test assertions based on the Apple-only full-surface C API, fake nullable surfaces, or recorded events. Prohibit private Ghostty GTK imports, maintained forks, standalone Ghostty, and public-ABI leakage.</action>
  <verify><automated>bun run native:test:vt &amp;&amp; bun run native:test:quick</automated></verify>
  <acceptance_criteria>`native:test:vt` exercises real pinned VT objects and fails on pin/API drift; source audit reports no obsolete full-surface hosting or Ghostty symbols outside the explicit boundary.</acceptance_criteria>
  <done>The exact pinned VT module is executable behind a narrow product adapter and every synthetic parallel terminal seam from partial 105-05 is removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the real focusable GTK widget and snapshot renderer seam</name>
  <files>native/linux/terminal_host.zig, native/linux/terminal_widget.zig, native/linux/renderer.zig, native/tests/renderer_test.zig, native/tests/terminal_widget_test.zig, native/tests/terminal_host_test.zig, native/build.zig</files>
  <read_first>native/linux/terminal_host.zig, native/linux/app.zig, native/tests/terminal_host_test.zig, native/terminal/vt_adapter.zig, .planning/phases/105-shared-native-model-and-terminal-foundation/105-RESEARCH.md</read_first>
  <behavior>
    - "A custom GTK4 widget realizes, accepts focus, measures/allocates, snapshots a detached deterministic VT frame, resizes its grid, and unrealizes without late callback use."
    - "The renderer interface is replaceable and its initial implementation uses GtkSnapshot/GSK/Pango, not bespoke GL/Vulkan."
    - "The widget declares honest role/name/description/focus semantics without claiming a cell text provider."
  </behavior>
  <action>Replace `terminal_host.zig` synthetic booleans with a real custom GTK type and split widget/renderer responsibilities. Establish a renderer interface over detached product RenderFrame values, implement deterministic backgrounds/glyphs/cursor/focus using GtkSnapshot/GSK/Pango per the locked corrective choice, and keep mutable VT traversal outside GTK snapshot. Derive rows/columns from allocation and measured monospace metrics; queue redraw only on the main context. Use generation/liveness state so queued redraws become inert after unrealize. Provide truthful accessible role/name/description/focus properties per D-16 but no fabricated cell-level text semantics. Tests must realize the production widget under a selected display harness, not a headless event struct, and must fail closed with captured GTK/display diagnostics if prerequisites are absent.</action>
  <verify><automated>bun run native:test:renderer &amp;&amp; bun run native:test:widget</automated></verify>
  <acceptance_criteria>Renderer semantic fixtures and a real GTK realize/focus/snapshot/resize/unrealize harness pass; missing display/GTK prerequisites produce an immediate classified failure with diagnostics.</acceptance_criteria>
  <done>The production widget renders deterministic VT content through the replaceable GTK snapshot seam and exposes only implemented accessibility semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ship and smoke the production GTK executable</name>
  <files>native/linux/app.zig, native/build.zig, scripts/verify-native.ts, package.json</files>
  <read_first>native/linux/app.zig, native/build.zig, scripts/verify-native.ts, package.json, native/linux/terminal_widget.zig</read_first>
  <behavior>
    - "native:build-app emits a named executable whose main creates GtkApplication, one window, and the production terminal widget."
    - "native:run launches that artifact for a human; native:smoke-app launches the same main with deterministic in-memory VT content."
    - "The smoke waits boundedly for window/widget/render readiness, requests normal quit, and rejects crashes, timeouts, or leftover application processes."
  </behavior>
  <action>Replace the factory-only `app.zig` with `pub fn main` and a production GtkApplication/window/widget composition. Add build/install/run targets and package scripts `native:build-app`, `native:run`, and `native:smoke-app`. Give the production main a narrow test configuration for deterministic in-memory VT content and a readiness/result channel without creating a second app implementation. Smoke the executable under the available real or virtual display, capture backend/display/GTK/Pango/renderer/stderr diagnostics, cap startup and shutdown waits at 30 seconds, and fail closed when prerequisites cannot launch the real window.</action>
  <verify><automated>bun run native:build-app &amp;&amp; bun run native:smoke-app</automated></verify>
  <acceptance_criteria>The installed production artifact exists and the bounded smoke proves its real GTK window/widget rendered known VT text, resized/focused, quit normally, and left no app process.</acceptance_criteria>
  <done>A human can run `bun run native:run` and interact with a real focusable GTK frame; this plan contains no human checkpoint.</done>
</task>

</tasks>

<artifacts_and_symbols_produced>
- `native/terminal/vt_adapter.zig`: `VtAdapter`, `RenderFrame`, pinned VT encode/parse/resize operations.
- `native/linux/renderer.zig`: replaceable `Renderer` seam and GtkSnapshot/Pango implementation.
- `native/linux/terminal_widget.zig`: production custom widget lifecycle and readiness hooks.
- `native/linux/app.zig`: production `main` shared by run and smoke commands.
- Package scripts: `native:test:vt`, `native:test:renderer`, `native:test:widget`, `native:build-app`, `native:run`, `native:smoke-app`.
</artifacts_and_symbols_produced>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Pinned upstream source → adapter | Unstable exact-pin Zig API enters product code. |
| VT frame → GTK snapshot | Untrusted terminal content crosses parser/render and async lifecycle boundaries. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-105-VT-01 | Tampering | VT dependency boundary | high | mitigate | Exact pin and source audit; adapter-only import; compatibility tests. |
| T-105-OUTPUT-01 | Denial of Service | render frame | high | mitigate | Bound frame/event data and detached immutable snapshot input. |
| T-105-ASYNC-01 | Tampering | GTK callbacks | high | mitigate | Main-context ownership, generation tokens, bounded smoke teardown. |
</threat_model>

<verification>`bun run native:test:vt &amp;&amp; bun run native:test:renderer &amp;&amp; bun run native:test:widget &amp;&amp; bun run native:build-app &amp;&amp; bun run native:smoke-app &amp;&amp; bun run native:test:quick`</verification>
<success_criteria>The obsolete Linux full-surface fiction is gone and the exact production application visibly renders pinned VT state in a real GTK widget with bounded automated evidence.</success_criteria>
<output>Create `.planning/phases/105-shared-native-model-and-terminal-foundation/105-05-SUMMARY.md` when done.</output>
