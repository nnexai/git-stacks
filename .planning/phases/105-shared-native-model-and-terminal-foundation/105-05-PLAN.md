---
phase: 105-shared-native-model-and-terminal-foundation
plan: 05
type: execute
wave: 5
depends_on: [105-01, 105-04]
files_modified:
  - native/deps/ghostty.lock
  - native/build.zig
  - scripts/verify-native.ts
  - package.json
  - native/tests/ghostty_surface_abi.zig
autonomous: true
requirements: [TERM-03]
must_haves:
  truths:
    - "The native build consumes only Limux Ghostty commit 81ab8ffa90185221782baf785e85387321e16f8d with Zig 0.15.2 and rejects source, tree, toolchain, or exported-ABI drift."
    - "Production builds a full app-runtime=none libghostty surface library; no production build imports ghostty-vt or links the product renderer/PTY runtime."
    - "An explicit audit reports divergence from upstream and the bounded Linux embedding patch before a pin can advance."
  artifacts:
    - path: "native/deps/ghostty.lock"
      provides: "Immutable Limux fork commit, tree digest, Zig artifacts, expected ABI, and upstream merge-base metadata"
    - path: "native/tests/ghostty_surface_abi.zig"
      provides: "Compile/link/runtime ABI contract for the exact rendered surface API"
    - path: "scripts/verify-native.ts"
      provides: "Source integrity, build, symbol, and upstream-drift gates"
  key_links:
    - from: "native/deps/ghostty.lock"
      to: "scripts/verify-native.ts"
      via: "verified checkout and build of the exact commit and source tree"
    - from: "native/build.zig"
      to: "libghostty.so"
      via: "app-runtime=none full embedded runtime linked by the Linux application"
---

<objective>
Replace the superseded upstream ghostty-vt pin with the exact full-surface Limux Ghostty build contract required by D-15, D-17, and D-20.

Purpose: Make the validated rendered surface ABI reproducible and force every upgrade through compatibility and drift evidence.
Output: Immutable fork/toolchain lock, full libghostty build/link path, ABI probe, and upstream-drift audit.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-CONTEXT.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-RESEARCH.md
@.planning/spikes/006-full-ghostty-gtk-runtime/README.md
@.planning/spikes/007-reusable-linux-surface-extraction/README.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-01-SUMMARY.md
@native/deps/ghostty.lock
@native/build.zig
@scripts/verify-native.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze and audit the validated Linux surface source</name>
  <files>native/deps/ghostty.lock, scripts/verify-native.ts, native/tests/ghostty_surface_abi.zig</files>
  <behavior>
    - "Setup checks out exact commit 81ab8ffa90185221782baf785e85387321e16f8d and accepts only its recorded source-tree digest."
    - "Verification rejects a dirty checkout, remote mismatch, commit mismatch, tree mismatch, Zig mismatch, missing Linux platform payload, or changed expected symbol/type."
    - "A drift report records fork head, upstream merge base, ahead/behind counts, and central include/ghostty.h plus src/apprt/embedded.zig patch state without mutating the checkout."
  </behavior>
  <action>Replace the prior release-tag provenance with D-20's exact `am-will/ghostty` commit, its verified tree digest, compatible Zig 0.15.2 artifact hashes, upstream remote/base metadata, and a checked expected-symbol manifest. Extend setup/audit modes to verify the immutable checkout and inspect only the bounded Linux surface seams. Compile a C-import contract that names app/config creation and destruction; Linux surface creation/draw/size/content-scale/focus; key/text/preedit/mouse/scroll/IME; clipboard completion; size/process/confirm/close observations; display realized/unrealized; and callback configuration. Emit actionable drift diagnostics and never auto-rebase or silently accept changed ABI/source.</action>
  <verify><automated>bun run native:setup &amp;&amp; bun run native:audit-ghostty &amp;&amp; bun run native:test:surface-abi</automated></verify>
  <done>The exact fork/toolchain/source/API contract is reproducible and any unreviewed source or upstream drift fails with bounded diagnostics.</done>
</task>

<task type="auto">
  <name>Task 2: Build and link full libghostty while excising the competing production stack</name>
  <files>native/build.zig, scripts/verify-native.ts, package.json</files>
  <action>Build the pinned Ghostty checkout with `-Dapp-runtime=none`, `ReleaseFast`, and disabled distributable docs/help/theme payloads as validated by Spike 006; install or link the resulting `libghostty.so` and header into the native GTK executable with an explicit runtime library path. Rework native command modes around surface ABI/build/run/smoke. Remove production build imports and commands for the custom `ghostty-vt` adapter, product PTY/runtime, Pango renderer/widget, input encoder, and partial appearance parser per D-17/D-18/D-21; retained Plan 105-04 ownership tests remain independently runnable but cannot be linked into the terminal application. Add a build-graph audit that fails if the production target regains any of those competing modules.</action>
  <verify><automated>bun run native:test:surface-abi &amp;&amp; bun run native:build-app &amp;&amp; bun run native:audit-production-graph</automated></verify>
  <done>The installed native executable links full pinned libghostty and its production dependency graph contains exactly one terminal renderer/configuration/PTY owner: Ghostty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Remote dependency to native build | Mutable or substituted Ghostty source becomes executable native code. |
| Unversioned downstream C ABI to host | Silent signature/symbol drift can corrupt memory. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-105-SUPPLY-01 | Tampering | Ghostty checkout/toolchain | high | mitigate | Exact commit/tree/artifact digests, clean checkout, remote verification. |
| T-105-ABI-01 | Tampering | embedded C ABI | high | mitigate | Compile/link symbol contract and explicit pin-advance audit. |
| T-105-DRIFT-01 | Repudiation | downstream patch | medium | mitigate | Persist merge base and measured ahead/behind/central diff report. |
</threat_model>

<verification>`bun run native:setup &amp;&amp; bun run native:audit-ghostty &amp;&amp; bun run native:test:surface-abi &amp;&amp; bun run native:build-app &amp;&amp; bun run native:audit-production-graph`.</verification>
<success_criteria>The validated full Ghostty surface library is exactly pinned, reproducible, linked, drift-audited, and the custom terminal implementation is absent from the production graph.</success_criteria>
<output>Create `.planning/phases/105-shared-native-model-and-terminal-foundation/105-05-SUMMARY.md` when done.</output>
