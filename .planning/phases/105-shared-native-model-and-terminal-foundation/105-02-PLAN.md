---
phase: 105-shared-native-model-and-terminal-foundation
plan: 02
type: execute
wave: 2
depends_on: [105-01]
files_modified:
  - native/include/git_stacks_native_v1.h
  - native/core/identity.zig
  - native/core/contract.zig
  - native/core/abi.zig
  - native/tests/fixtures/README.md
  - native/tests/abi_harness.c
  - scripts/verify-native.ts
autonomous: true
requirements: [CORE-01, CORE-03, CORE-04]
must_haves:
  truths:
    - "Linux executes the canonical fixture harness; the ABI/header remains platform-neutral and compile-portable for Phase 107 macOS integration."
    - "Malformed, oversized, mismatched-version, and invalid-lifetime ABI calls fail deterministically without exposing Zig, GTK, or libghostty layouts."
    - "Actual macOS execution/parity is explicitly deferred to Phase 107 and Phase 105 makes no runtime-proof claim."
  artifacts:
    - path: "native/include/git_stacks_native_v1.h"
      provides: "Portable opaque v1 product ABI"
    - path: "native/core/abi.zig"
      provides: "Bounded ABI validation, allocation ownership, and structured errors"
  key_links:
    - from: "native/core/abi.zig"
      to: "native/include/git_stacks_native_v1.h"
      via: "version-suffixed exported symbols and matching allocation/free contract"
    - from: "native/tests/fixtures"
      to: "tests/fixtures/service-v1"
      via: "one checked canonical fixture export workflow"
---

<objective>
Establish the portable product-owned contract boundary and prove Linux execution without blocking Phase 107 macOS integration.

Purpose: Give both native shells one reproducible ABI and fixture source of truth while keeping terminal hosting platform-specific.
Output: Stable identity/contract decoding, opaque C ABI, Linux golden fixture execution, and macOS-portability checks.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-CONTEXT.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-RESEARCH.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-VALIDATION.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-01-SUMMARY.md
@src/lib/service/contract.ts
@tests/fixtures/service-v1
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define strict native identities and the versioned opaque ABI</name>
  <files>native/include/git_stacks_native_v1.h, native/core/identity.zig, native/core/contract.zig, native/core/abi.zig, native/tests/abi_harness.c</files>
  <behavior>
    - "C consumers exchange workspace, repository, command, operation, surface, and attention identities without platform types."
    - "Null handles, invalid pointer/length pairs, oversized input, invalid UTF-8/JSON, ABI mismatch, double destroy, post-destroy use, and allocation/free misuse return stable structured failures."
    - "Returned bytes remain valid until the matching product free and no Zig allocator, enum, slice, struct, GTK type, or libghostty handle crosses the header."
  </behavior>
  <action>Create strict Zig identity and Phase 104 contract decoders and a version-suffixed C header using opaque handles, fixed-width status/scalar types, explicit UTF-8 byte pointer/length values, and product-owned free functions per CORE-01/CORE-03. Preserve protocol `v1`, existing JSON field names, opaque identity domains, decimal revisions, discriminated outcomes, and structured errors from `src/lib/service/contract.ts`. Use JSON bytes for ABI interchange and caller-owned copies. Bound every input/allocation, validate before state mutation, make destruction idempotency policy explicit, and return structured error bytes rather than thread-local strings. The exported ABI must contain no platform or upstream terminal symbols.</action>
  <verify><automated>bun run native:test:model</automated></verify>
  <done>The Linux ABI harness passes all ownership, version, length, UTF-8, JSON, lifecycle, and symbol-isolation assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run one canonical golden corpus on Linux and preserve macOS portability</name>
  <files>native/tests/fixtures/README.md, native/tests/abi_harness.c, scripts/verify-native.ts</files>
  <behavior>
    - "One checked export consumes the exact Phase 104 fixture bytes; independent hand-maintained native variants are rejected."
    - "Bun, Zig, and Linux C executions decode the same corpus and emit equivalent canonical identity/contract bytes."
    - "The header/harness avoids Linux-only public ABI constructs and passes Clang portability diagnostics suitable for later macOS execution."
    - "A changed fixture, ABI header, source pin, or toolchain pin reports the specific drift."
  </behavior>
  <action>Wire CORE-04 golden parity to `tests/fixtures/service-v1/` through one deterministic checked copy/generation workflow and reject stale/divergent copies. Execute Bun, Zig, and Linux C against the same corpus and compare canonical bytes/hashes. Compile the public header and harness with strict C/Clang portability diagnostics, keep OS-specific types and conditionals out of the public ABI, and record that these checks establish compatibility only—not macOS runtime parity. Per the explicit user scope decision, do not add or require a macOS runner in Phase 105: actual macOS execution, byte-parity proof, and native-shell integration belong to Phase 107. Extend `scripts/verify-native.ts` with the Linux execution and portability commands.</action>
  <verify><automated>bun run native:test:model &amp;&amp; bun run native:test:quick</automated></verify>
  <done>Linux consumers agree on canonical bytes, portability diagnostics reject platform leakage, and the artifact explicitly defers actual macOS execution/parity to Phase 107 without claiming it here.</done>
</task>

</tasks>

<verification>
Run `bun run native:test:model` and `bun run native:test:quick`; confirm Linux canonical-byte parity and strict public-header portability diagnostics. Actual macOS execution/parity and terminal hosting are deferred to Phase 107 by explicit user decision.
</verification>

<success_criteria>
Strict ABI negative tests and Linux fixture parity pass, the ABI presents no known macOS portability blocker, and no Phase 105 artifact falsely claims macOS runtime proof.
</success_criteria>

<output>
Create `.planning/phases/105-shared-native-model-and-terminal-foundation/105-02-SUMMARY.md` when done.
</output>
