---
phase: 105-shared-native-model-and-terminal-foundation
plan: 08
type: execute
wave: 8
depends_on: [105-07]
files_modified:
  - native/tests/lifecycle_stress.zig
  - native/tests/accessibility_test.zig
  - scripts/verify-native.ts
  - package.json
  - docs/native-terminal-acceptance.md
  - docs/native-terminal-accessibility.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [CORE-01, CORE-02, CORE-03, CORE-04, CORE-05, TERM-01, TERM-02, TERM-03, TERM-04, TERM-05]
must_haves:
  truths:
    - "Stress cycles production widget, VT, PTY, process group, FDs, GLib sources, async operations, and caches—not synthetic counters—and proves bounded return."
    - "Automated accessibility checks match actual widget role/name/description/focus/actions and make no fabricated cell-text claim."
    - "Human evidence begins only after native:verify and the production real-PTY smoke pass."
    - "Wayland/X11 where supported, 25 compositor cycles, interaction, cleanup, and AT-SPI/assistive-technology observations are recorded against the exact production artifact before closeout."
  artifacts:
    - path: native/tests/lifecycle_stress.zig
      provides: Production real-resource lifecycle stress
    - path: docs/native-terminal-acceptance.md
      provides: Completed exact-environment interaction and compositor evidence
    - path: docs/native-terminal-accessibility.md
      provides: Completed truthful GTK/AT-SPI accessibility evidence
  key_links:
    - from: native/tests/lifecycle_stress.zig
      to: native/terminal/runtime.zig
      via: production widget/VT/PTY lifecycle construction
    - from: scripts/verify-native.ts
      to: native:smoke-terminal
      via: mandatory executable/PTY gate before checkpoint
    - from: docs/native-terminal-accessibility.md
      to: native/linux/terminal_widget.zig
      via: observed declarations and explicit unsupported semantics
---

<objective>Replace synthetic lifecycle claims with production-resource evidence, verify honest accessibility, complete real-session acceptance, and reconcile Phase 105 only after every gate passes.

Purpose: Close the phase with evidence a user can trust rather than headless stand-ins.
Output: Real stress/accessibility gates, completed compositor/AT evidence, final regression run, and accurate planning state.</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-CONTEXT.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-RESEARCH.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-VALIDATION.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-PATTERNS.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-07-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Replace synthetic stress with production widget, VT, PTY, and process evidence</name>
  <files>native/tests/lifecycle_stress.zig, scripts/verify-native.ts, package.json</files>
  <read_first>native/tests/lifecycle_stress.zig, native/terminal/runtime.zig, native/terminal/ownership.zig, native/linux/terminal_widget.zig, scripts/verify-native.ts, .planning/phases/105-shared-native-model-and-terminal-foundation/105-RESEARCH.md, .planning/phases/105-shared-native-model-and-terminal-foundation/105-PATTERNS.md</read_first>
  <behavior>
    - "After warmup, 100 cycles create a production widget/runtime/VT/real PTY, type/resize/destroy, and assert exact zero widgets/surfaces, children, PGIDs, PTY FDs, GLib sources, and async operations every cycle."
    - "FD/thread counts return to baseline; final-50 RSS slope is at most 64 KiB/cycle and final median is at most warm median plus 16 MiB."
    - "The 500-cycle environment lane reports progress and uses the same production body; failures capture residual identities/resources."
  </behavior>
  <action>Retain only threshold math/evidence vocabulary from the partial stress harness and replace its synthetic lifecycle body with the actual app widget/runtime/VT/PTY ownership composition per D-14. Include 10-cycle warmup, 100 ordinary cycles, exact production counter checks each cycle, FD/thread/RSS trends, RenderState/cache retention accounting, late-callback injection, and zero group/child proof. Make `GIT_STACKS_NATIVE_EXTENDED_STRESS=1` run 500 cycles with periodic progress. Add `native:test:stress`; fail closed when the display harness or resource probes are unavailable, recording diagnostics rather than treating a skipped real resource as pass.</action>
  <verify><automated>bun run native:test:stress</automated></verify>
  <acceptance_criteria>The ordinary real-runtime stress passes every exact-zero and trend threshold; the harness contains no synthetic terminal implementation or event-recorder acceptance path.</acceptance_criteria>
  <done>TERM-04 has automated evidence over the production resource graph and an extended lane.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the honest native accessibility contract and pre-checkpoint gate</name>
  <files>native/tests/accessibility_test.zig, docs/native-terminal-accessibility.md, docs/native-terminal-acceptance.md, scripts/verify-native.ts, package.json</files>
  <read_first>native/linux/terminal_widget.zig, native/linux/input.zig, docs/native-terminal-acceptance.md, docs/native-terminal-accessibility.md, scripts/verify-native.ts</read_first>
  <behavior>
    - "Automated inspection sees the declared role/name/description/help/focus and only actions actually wired."
    - "Tests reject a claimed cell text/caret/selection provider when the widget does not implement one."
    - "native:verify composes retained core/ABI/model/restore/lifecycle gates, VT/PTY/render/input/widget/runtime/stress/accessibility, production app build, and real terminal smoke."
  </behavior>
  <action>Implement accessibility tests over the production GTK widget and revise the evidence templates to use `native:run` and the real architecture. Preserve `Status: NOT YET OBSERVED` until human results are attached. Assert focus/name/description/help/actions and reject misleading cell-text claims per D-16. Update `native:verify` to compose exact-pin/source audit, all retained 105-01..04 tests, every new focused suite, production app build, `native:smoke-app`, and `native:smoke-terminal`; it must fail before the checkpoint if any production target, graphical prerequisite, PTY roundtrip, cleanup counter, or accessibility declaration is missing.</action>
  <verify><automated>bun run native:test:accessibility &amp;&amp; bun run native:verify &amp;&amp; bun run native:smoke-terminal</automated></verify>
  <acceptance_criteria>Accessibility declarations match implementation and the complete automated native gate plus real production PTY roundtrip pass before any approval request is emitted.</acceptance_criteria>
  <done>The phase is mechanically ready for meaningful real-session observation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Complete compositor, lifecycle, interaction, and accessibility evidence</name>
  <files>docs/native-terminal-acceptance.md, docs/native-terminal-accessibility.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>docs/native-terminal-acceptance.md, docs/native-terminal-accessibility.md, .planning/phases/105-shared-native-model-and-terminal-foundation/105-RESEARCH.md, .planning/phases/105-shared-native-model-and-terminal-foundation/105-VALIDATION.md</read_first>
  <action>Pause only after Task 2's automated commands pass. Present the exact `bun run native:run` command and production artifact. Require the complete D-13 environment and interaction evidence plus D-14 lifecycle and D-16 accessibility matrices; unavailable Wayland/X11 lanes need captured environment evidence, not invented passes. After approval, rerun final gates and only then mark TERM-01/02/04/05 and Phase 105 complete in requirements/roadmap/state; preserve Phase 106 concurrent edits and do not rewrite completed 105-01..04 artifacts.</action>
  <what-built>A production executable with a proven real PTY roundtrip, complete interaction paths, real-resource stress, and automated truthful accessibility declarations.</what-built>
  <how-to-verify>1. Launch `bun run native:run` and record distro, session/compositor, GTK/Pango/font, locale/IME, renderer backend, GPU/driver, Zig, and peeled Ghostty commit. 2. Complete keyboard/rich keys, mouse selection/reporting, Unicode/graphemes/complex scripts, resize/reflow, alternate screen, system/primary clipboard, IME preedit/commit/cursor, focus, child exit, graceful/forced close, app quit, and simulated crash cleanup. 3. Perform 25 real-compositor create/type/resize/destroy cycles with process/resource evidence. 4. Repeat Wayland and X11 where supported, or attach unavailability diagnostics. 5. Inspect GTK accessibility with GTK Inspector/AT-SPI tooling and an assistive technology such as Orca; record focus/input/actions and explicitly mark absent cell text/caret/selection semantics unsupported or unverified.</how-to-verify>
  <verify><automated>bun run native:verify &amp;&amp; bun run native:smoke-terminal &amp;&amp; bun run test &amp;&amp; bun run typecheck &amp;&amp; bun run test:deps &amp;&amp; bun run verify:gates</automated></verify>
  <acceptance_criteria>Completed evidence is tied to the exact production artifact and environment; every supported lane passes, unavailable lanes have diagnostics, no misleading accessibility claim remains, and final repository gates pass.</acceptance_criteria>
  <done>Human approves the usable production terminal evidence and post-approval closeout accurately reconciles Phase 105 without altering Phase 106 work.</done>
  <resume-signal>Type "approved" after attaching the completed evidence matrices, or describe the failing observation.</resume-signal>
</task>
</tasks>

<artifacts_and_symbols_produced>
- `native/tests/lifecycle_stress.zig`: production 100/500-cycle resource stress.
- `native/tests/accessibility_test.zig`: declared-semantics verification.
- Completed `docs/native-terminal-acceptance.md` and `docs/native-terminal-accessibility.md`.
- Final Phase 105 requirement and state reconciliation after approval only.
</artifacts_and_symbols_produced>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Production lifecycle → evidence | Synthetic or skipped resources could create false acceptance. |
| Widget semantics → assistive technology/user | Misleading accessibility claims create trust and usability harm. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-105-EVIDENCE-01 | Repudiation | lifecycle evidence | high | mitigate | Exact environment/artifact metadata, production counters, bounded diagnostics. |
| T-105-A11Y-01 | Spoofing | accessibility contract | medium | mitigate | Automated declaration checks plus GTK Inspector/AT observation. |
| T-105-PGID-01 | Elevation of Privilege | repeated teardown | critical | mitigate | Retained birth-token validation and exact zero process/group checks. |
| T-105-ASYNC-01 | Tampering | repeated async teardown | high | mitigate | Generation checks and exact zero GLib/async counters. |
</threat_model>

<verification>Before checkpoint: `bun run native:test:stress &amp;&amp; bun run native:test:accessibility &amp;&amp; bun run native:verify &amp;&amp; bun run native:smoke-terminal`. After approval: `bun run native:verify &amp;&amp; bun run test &amp;&amp; bun run typecheck &amp;&amp; bun run test:deps &amp;&amp; bun run verify:gates`.</verification>
<success_criteria>Phase 105 closes only when production terminal interaction, ownership cleanup, real-resource trends, and honest accessibility are proven automatically and observed on real supported Linux sessions.</success_criteria>
<output>Create `.planning/phases/105-shared-native-model-and-terminal-foundation/105-08-SUMMARY.md` when done.</output>
