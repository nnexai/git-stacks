---
phase: 105-shared-native-model-and-terminal-foundation
plan: 08
type: execute
wave: 8
depends_on: [105-07]
files_modified:
  - native/tests/lifecycle_stress.zig
  - native/tests/accessibility_test.zig
  - docs/native-terminal-acceptance.md
  - docs/native-terminal-accessibility.md
  - scripts/verify-native.ts
  - package.json
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [CORE-02, CORE-05, TERM-01, TERM-02, TERM-03, TERM-04, TERM-05]
must_haves:
  truths:
    - "Repeated production Ghostty surface create/realize/type/resize/unrealize/destroy cycles leave zero surfaces, callbacks, children, and unbounded resource trends."
    - "The accessibility contract claims only observed GTK/Ghostty focus, input, action, and text semantics and explicitly records upstream gaps."
    - "Human evidence proves actual Ghostty font/config/cursor fidelity and complete terminal interaction in the exact production artifact."
  artifacts:
    - path: "native/tests/lifecycle_stress.zig"
      provides: "Real full-surface single/multi-pane lifecycle and bounded-resource stress"
    - path: "docs/native-terminal-acceptance.md"
      provides: "Exact production interaction/config/lifecycle evidence matrix"
    - path: "docs/native-terminal-accessibility.md"
      provides: "Honest inspected accessibility contract"
  key_links:
    - from: "scripts/verify-native.ts"
      to: "production native executable and libghostty"
      via: "composed source/build/ABI/model/graphical/interaction/stress/accessibility gate"
    - from: "human evidence matrices"
      to: "native/deps/ghostty.lock"
      via: "recorded exact commit/toolchain/artifact/environment"
---

<objective>
Close Phase 105 with production full-surface stress, honest accessibility, and real-session acceptance under D-13, D-14, D-16, and D-23.

Purpose: Prove the corrected architecture delivers fully functional terminal panes rather than another synthetic or partial renderer.
Output: Automated production stress/accessibility gates, completed human matrices, and Phase 105 closeout.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-CONTEXT.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-RESEARCH.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-VALIDATION.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-07-SUMMARY.md
@docs/native-terminal-acceptance.md
@docs/native-terminal-accessibility.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stress the production single- and multi-surface resource graph</name>
  <files>native/tests/lifecycle_stress.zig, scripts/verify-native.ts, package.json</files>
  <behavior>
    - "The ordinary lane performs at least 25 production graphical create/realize/type/resize/unrealize/destroy cycles with alternating single/two-surface and destroy orders."
    - "Every cycle proves zero live Ghostty surfaces, queued callbacks, clipboard completions, GL areas/contexts, and surviving terminal children after bounded drain."
    - "RSS, file descriptors, threads, and GPU-visible allocations return to a calibrated bounded baseline with no monotonic cycle-over-cycle growth."
    - "An opt-in extended lane performs at least 250 cycles against the same production artifact and emits machine-readable diagnostics."
  </behavior>
  <action>Replace custom runtime stress with a graphical harness that exercises the exact `ghostty_runtime`/`ghostty_surface` production objects under Xvfb or the available compositor. Include shell I/O and resize between lifecycle transitions, both two-surface destroy orders, child normal exit, requested close, app quit, and abrupt host termination where safely observable. Count the host registry and Ghostty process outcomes; sample `/proc` RSS/FD/thread data after main-loop quiescence and use a documented warm-up plus bounded slope/range policy per D-14. Preserve Plan 105-04's independent guard evidence but do not treat its product-owned PTY as production terminal evidence.</action>
  <verify><automated>bun run native:test:stress &amp;&amp; GIT_STACKS_NATIVE_EXTENDED_STRESS=1 bun run native:test:stress</automated></verify>
  <done>Ordinary and extended production surface stress leave exact-zero live resources and pass calibrated non-growth thresholds with useful failure diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce honest accessibility and compose the corrected acceptance gate</name>
  <files>native/tests/accessibility_test.zig, docs/native-terminal-acceptance.md, docs/native-terminal-accessibility.md, scripts/verify-native.ts, package.json</files>
  <behavior>
    - "Automated inspection verifies the actual leaf role, name, focus, keyboard/IME, visible focus/cursor, and only actions/text semantics that the embedded surface really exposes."
    - "Evidence templates identify full Ghostty rendered surfaces, exact pin/config delegation, two-surface readiness, and every D-23 interaction lane."
    - "native:verify composes retained model/restore/ownership gates with exact pin, ABI, production graph, surface, interaction, terminal, multi-surface, stress, accessibility, and executable smoke gates."
  </behavior>
  <action>Rewrite the earlier custom-renderer evidence as full Ghostty production evidence. Inspect the actual GTK accessibility tree and declare only verified semantics per D-16; reject claims of cell text/caret/selection when the GtkGLArea surface does not expose them, while treating broken focus/input as failure. Record upstream limitations distinctly from product regressions. Make `native:verify` fail on any custom production module, skipped graphical prerequisite, synthetic terminal, absent cursor/font/config evidence, zero geometry, failed query/TUI/clipboard/IME interaction, multi-surface crosstalk, nonzero teardown counter, or unfilled automated metadata. Keep human observation rows `NOT YET OBSERVED` until the checkpoint.</action>
  <verify><automated>bun run native:test:accessibility &amp;&amp; bun run native:verify</automated></verify>
  <done>All automated gates exercise the corrected production surface and the remaining claims are explicitly reserved for human observation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve real full-surface terminal and accessibility evidence</name>
  <files>docs/native-terminal-acceptance.md, docs/native-terminal-accessibility.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <action>Pause only after the complete automated gate passes. Present the exact production `bun run native:run` artifact. Require evidence against D-13/D-16/D-23, including a side-by-side standalone Ghostty comparison using the same configuration. After approval, rerun all gates and reconcile Phase 105 only; preserve every Phase 106 edit.</action>
  <what-built>A production GtkGLArea-hosted full Ghostty terminal leaf with exact config/font/rendering ownership, complete interaction forwarding, truthful lifecycle observations, independent multi-surface operation, stress proof, and honest accessibility declarations.</what-built>
  <how-to-verify>1. Run `bun run native:run`; record distro, Wayland/X11 compositor, GTK, locale/IME, renderer/GPU, Zig, exact Ghostty fork commit, config paths, and artifact hash. 2. Compare against standalone Ghostty using the same config: font family/size/weight, cursor style/blink, palette, key behavior, shell integration, and config includes must match except documented host transparency/window differences. 3. Verify fish starts without terminal-query timeout and its autosuggestions are discernible. 4. Verify typing, rich Ctrl/Alt/function/navigation keys, Unicode/graphemes/complex scripts, selection, system and primary copy/paste, IME preedit/commit/cursor, mouse reporting, smooth scroll, focus, resize/reflow, alternate screen, and full-screen TUIs using the complete pane. 5. Open the two-surface smoke host: independently focus, type, select/copy/paste, resize, run a TUI, close either order, and confirm no crosstalk or surviving child. 6. Verify child exit, close confirmation for active work, ordinary close, app quit, and crash-cleanup evidence. 7. Inspect GTK accessibility with GTK Inspector/AT-SPI and Orca where available; record focus/input/actions and mark absent cell text/caret/selection semantics unsupported or unverified rather than passing them.</how-to-verify>
  <verify><automated>bun run native:verify &amp;&amp; bun run test &amp;&amp; bun run typecheck &amp;&amp; bun run test:deps &amp;&amp; bun run verify:gates</automated></verify>
  <done>The user approves complete production evidence; all supported lanes pass, unavailable accessibility lanes have honest diagnostics, and Phase 105 records are closed without touching Phase 106.</done>
  <resume-signal>Type "approved" after attaching the completed evidence matrices, or describe the failing observation.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Production runtime to acceptance evidence | Synthetic/skipped resources could falsely certify the terminal. |
| Embedded GL surface to assistive technology | Missing semantics can be misrepresented as accessible behavior. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-105-EVIDENCE-01 | Repudiation | acceptance harness | high | mitigate | Exact artifact/environment metadata and production-only gates. |
| T-105-LEAK-01 | Denial of Service | repeated surface lifecycle | high | mitigate | Exact-zero counters plus calibrated RSS/FD/thread trends. |
| T-105-A11Y-01 | Spoofing | accessibility contract | medium | mitigate | Automated tree inspection and explicit unsupported/unverified semantics. |
</threat_model>

<verification>Before checkpoint: `bun run native:test:stress &amp;&amp; bun run native:test:accessibility &amp;&amp; bun run native:verify`. After approval: `bun run native:verify &amp;&amp; bun run test &amp;&amp; bun run typecheck &amp;&amp; bun run test:deps &amp;&amp; bun run verify:gates`.</verification>
<success_criteria>Phase 105 closes only after the exact production full Ghostty surface passes interaction, multi-surface, lifecycle, stress, configuration/font/cursor fidelity, and honest accessibility acceptance.</success_criteria>
<output>Create `.planning/phases/105-shared-native-model-and-terminal-foundation/105-08-SUMMARY.md` when done.</output>
