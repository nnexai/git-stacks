---
phase: 105-shared-native-model-and-terminal-foundation
plan: 06
type: execute
wave: 6
depends_on: [105-05]
files_modified:
  - native/terminal/pty.zig
  - native/terminal/runtime.zig
  - native/terminal/ownership.zig
  - native/terminal/guard.zig
  - native/linux/app.zig
  - native/linux/terminal_widget.zig
  - native/tests/pty_test.zig
  - native/tests/terminal_runtime_test.zig
  - native/tests/ownership_test.zig
  - scripts/verify-native.ts
  - package.json
autonomous: true
requirements: [CORE-02, CORE-05, TERM-01, TERM-02, TERM-04]
must_haves:
  truths:
    - "One product-owned PTY child/session/process group registers with the retained guard before runtime live exposure."
    - "Real PTY output drives pinned VT/render state and production widget input returns through bounded encoding/writes to the child."
    - "Child exit, close, quit, and crash cleanup drive retained reducer ownership truth and leave no unaccounted child, PGID, FD, or GLib source."
    - "native:smoke-terminal completes a real command roundtrip through the production executable."
  artifacts:
    - path: native/terminal/pty.zig
      provides: Product-owned PTY/session/process-group spawn and nonblocking I/O
    - path: native/terminal/runtime.zig
      provides: PTY-VT-widget lifecycle composition and reducer events
    - path: native/tests/terminal_runtime_test.zig
      provides: Real PTY/VT/frame/input/exit integration evidence
  key_links:
    - from: native/terminal/runtime.zig
      to: native/terminal/ownership.zig
      via: guard registration before live and cleanup outcome after absence proof
    - from: native/terminal/runtime.zig
      to: native/terminal/vt_adapter.zig
      via: PTY reads and encoded input
    - from: native/linux/terminal_widget.zig
      to: native/terminal/runtime.zig
      via: production input, resize, redraw, close, and exit events
---

<objective>Connect the runnable GTK frame to one real product-owned PTY shell and the completed process-group/guard/reducer lifecycle.

Purpose: Make the terminal actually usable before interaction polish or human evidence.
Output: PTY module, terminal runtime, production shell roundtrip smoke, and real lifecycle integration.</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-CONTEXT.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-RESEARCH.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-VALIDATION.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-PATTERNS.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-04-SUMMARY.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-05-SUMMARY.md
</context>

<tasks>
<scope_cohesion>
This plan intentionally keeps PTY acquisition, runtime composition, and the production roundtrip together because TERM-02 ownership is not proven until the same guarded child is driven through the real VT/widget path and cleaned after executable shutdown. Splitting would leave a horizontal PTY seam or postpone the required usable-shell gate. Each task still has an independent focused test boundary.
</scope_cohesion>
<task type="auto" tdd="true">
  <name>Task 1: Implement the product-owned PTY boundary</name>
  <files>native/terminal/pty.zig, native/tests/pty_test.zig, native/terminal/ownership.zig, native/terminal/guard.zig</files>
  <read_first>native/terminal/ownership.zig, native/terminal/guard.zig, native/terminal/diagnostics.zig, native/tests/ownership_test.zig, .planning/phases/105-shared-native-model-and-terminal-foundation/105-RESEARCH.md, .planning/phases/105-shared-native-model-and-terminal-foundation/105-PATTERNS.md</read_first>
  <behavior>
    - "Spawn opens a PTY, creates a child session/controlling terminal/process group, closes unintended descriptors, validates PID/PGID/birth token, registers guard, then becomes live."
    - "Input/output/winsize handle partial operations, EINTR, EAGAIN, EOF, HUP, and exit in any order with bounded queues."
    - "Failure at every acquisition stage unwinds in reverse and proves the process group absent or reports failed_cleanup."
  </behavior>
  <action>Write real fixture-child tests first, then implement `pty.zig` using POSIX PTY/session/process APIs with close-on-exec descriptor allowlisting and explicit launch configuration. Integrate the retained D-09 through D-12 ownership/guard code rather than duplicating it: validate PGID/birth token, register before live, and unregister only after reap/absence proof. Cover winsize, partial reads/writes, bounded queues, EOF/HUP/SIGCHLD races, spawn/register/watch failures, and redacted diagnostics. Never persist argv/environment/FD/PID liveness and never let a child inherit the guard control channel.</action>
  <verify><automated>bun run native:test:pty &amp;&amp; bun run native:test:lifecycle</automated></verify>
  <acceptance_criteria>Real PTY fixture tests prove command I/O, resize, group identity, safe descriptor inheritance, every injected unwind, and absence-before-unregister while retained ownership tests remain green.</acceptance_criteria>
  <done>A product-owned PTY/process group can be acquired and released without violating completed lifecycle invariants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose PTY, VT, widget, reducer, and guard into one runtime</name>
  <files>native/terminal/runtime.zig, native/linux/terminal_widget.zig, native/linux/app.zig, native/tests/terminal_runtime_test.zig, native/tests/ownership_test.zig</files>
  <read_first>native/terminal/pty.zig, native/terminal/vt_adapter.zig, native/terminal/ownership.zig, native/core/reducer.zig, native/linux/terminal_widget.zig, native/linux/app.zig</read_first>
  <behavior>
    - "PTY bytes update real VT state and a detached frame; normalized widget input is VT-encoded and reaches the child."
    - "Pixel allocation updates both VT rows/columns and TIOCSWINSZ; output schedules redraw on the GTK main context."
    - "EOF/child exit/close/quit/crash emit ended or failed_cleanup reducer actions and invalidate FD, timeout, redraw, and clipboard callbacks by generation."
  </behavior>
  <action>Create one runtime owning PTY master, child/group identity, VT objects, bounded write/event queues, GLib sources, render frame, and widget generation. A GLib nonblocking source may own parsing for this slice; otherwise enforce a single VT owner and immutable frame transfer. Wire widget input/resize/close and app quit to it. Preserve D-10 meaningful-activity confirmation and D-11 reducer truth after widget destruction. Remove GLib sources and stop input before freeing runtime data; ignore late completions by generation. Tests must construct production runtime types and drive real child bytes, not a parallel fake terminal.</action>
  <verify><automated>bun run native:test:runtime &amp;&amp; bun run native:test:lifecycle</automated></verify>
  <acceptance_criteria>Runtime tests prove output-to-frame, input-to-child, resize, exit, close, quit, simulated crash, reverse unwind, zero owner counters, and explicit failed_cleanup propagation.</acceptance_criteria>
  <done>The production widget owns one live terminal runtime whose lifecycle is represented truthfully by the retained shared model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gate the production terminal with a real shell roundtrip</name>
  <files>native/linux/app.zig, scripts/verify-native.ts, package.json</files>
  <read_first>native/linux/app.zig, native/terminal/runtime.zig, scripts/verify-native.ts, package.json</read_first>
  <behavior>
    - "native:smoke-terminal launches production main with a deterministic fixture shell, waits for a unique prompt, injects through the production widget input path, and observes expected rendered output."
    - "Smoke quits through application lifecycle and reports zero children, PGIDs, PTY FDs, and GLib sources."
    - "Every wait is bounded and failures capture launch, display, PTY, child-exit, ownership, and render diagnostics without terminal/clipboard secrets."
  </behavior>
  <action>Add `native:test:pty`, `native:test:runtime`, and `native:smoke-terminal` orchestration. The smoke must reuse production main/widget/runtime with a fixture launch spec, send a deterministic command through the same input entrypoint used by GTK, and observe a unique marker from the real RenderFrame before normal quit. Reject test-only PTY or renderer paths. Cap roundtrip at 45 seconds, collect classified graphical and ownership diagnostics on failure, and assert all production resource counters are zero.</action>
  <verify><automated>bun run native:smoke-terminal &amp;&amp; bun run native:test:lifecycle</automated></verify>
  <acceptance_criteria>The production executable completes the deterministic PTY command roundtrip and clean shutdown; a missing display, stalled shell, missing output, cleanup residue, or timeout fails closed with evidence.</acceptance_criteria>
  <done>Automated proof now exists for a real interactive shell path, so later human verification has a usable interface.</done>
</task>
</tasks>

<artifacts_and_symbols_produced>
- `native/terminal/pty.zig`: `Pty`, validated child/group identity, winsize and bounded I/O.
- `native/terminal/runtime.zig`: `TerminalRuntime`, production resource counters, reducer/lifecycle bridge.
- Package scripts: `native:test:pty`, `native:test:runtime`, `native:smoke-terminal`.
</artifacts_and_symbols_produced>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Child process → PTY/runtime | Untrusted output, timing, descendants, and terminal protocols enter native state. |
| Runtime → guard | Validated group identities cross a private cleanup channel. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-105-PGID-01 | Elevation of Privilege | PGID signaling | critical | mitigate | Session/birth-token validation, group exclusions, registration-before-live, race tests. |
| T-105-FD-01 | Information Disclosure | child spawn | high | mitigate | CLOEXEC and descriptor allowlist; explicit environment; fixture inspection. |
| T-105-QUEUE-01 | Denial of Service | PTY queues | high | mitigate | Bounded partial-I/O queues and deterministic backpressure tests. |
| T-105-ASYNC-01 | Tampering | GLib/runtime callbacks | high | mitigate | Generation tokens and source removal before free. |
</threat_model>

<verification>`bun run native:test:pty &amp;&amp; bun run native:test:runtime &amp;&amp; bun run native:smoke-terminal &amp;&amp; bun run native:test:lifecycle &amp;&amp; bun run native:test:quick`</verification>
<success_criteria>A production GTK terminal performs a real shell command roundtrip while exclusive PTY/process-group ownership and reducer cleanup truth hold across every exit path.</success_criteria>
<output>Create `.planning/phases/105-shared-native-model-and-terminal-foundation/105-06-SUMMARY.md` when done.</output>
