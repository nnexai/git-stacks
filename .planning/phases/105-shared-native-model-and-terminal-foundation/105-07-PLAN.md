---
phase: 105-shared-native-model-and-terminal-foundation
plan: 07
type: execute
wave: 7
depends_on: [105-03, 105-04, 105-06]
files_modified:
  - native/linux/ghostty_input.zig
  - native/linux/ghostty_clipboard.zig
  - native/linux/ghostty_surface.zig
  - native/linux/ghostty_runtime.zig
  - native/linux/app.zig
  - native/terminal/ghostty_process_control.zig
  - native/terminal/guard.zig
  - native/core/reducer.zig
  - native/tests/ghostty_interaction_test.zig
  - native/tests/reducer_test.zig
  - scripts/verify-native.ts
autonomous: true
requirements: [CORE-02, CORE-05, TERM-01, TERM-02, TERM-05]
must_haves:
  truths:
    - "Keyboard, committed text, IME preedit, mouse, scroll, focus, selection, and both clipboard channels reach the correct Ghostty surface using complete event data."
    - "Fish initialization queries, rich keyboard protocols, Unicode, alternate screen, resize/reflow, and full-screen TUIs operate through Ghostty's production surface."
    - "Ghostty remains sole process owner while child-exit, close-confirmation, close, quit, and failed cleanup observations update the product model truthfully."
    - "Two independently focused/resized surfaces can coexist without callback, clipboard, input, or model-identity crosstalk."
  artifacts:
    - path: "native/linux/ghostty_input.zig"
      provides: "GTK key/text/IME/pointer/scroll translation into Ghostty events"
    - path: "native/linux/ghostty_clipboard.zig"
      provides: "Asynchronous system/primary clipboard request completion and selection actions"
    - path: "native/tests/ghostty_interaction_test.zig"
      provides: "Production interaction, TUI, protocol, and two-surface isolation coverage"
  key_links:
    - from: "GTK event controllers and IM context"
      to: "native/linux/ghostty_surface.zig"
      via: "per-surface input generation and focus routing"
    - from: "Ghostty runtime action/child callbacks"
      to: "native/core/reducer.zig"
      via: "product-owned surface identity observations without product PTY ownership"
    - from: "native/linux/ghostty_clipboard.zig"
      to: "libghostty.so"
      via: "read/write callbacks and matching complete_clipboard_request"
---

<objective>
Complete Ghostty-native interaction fidelity, production lifecycle observation, and two-surface isolation per D-18, D-22, and D-23.

Purpose: Turn the rendered leaf into a fully functional terminal pane and establish the reusable multi-pane boundary Phase 106 needs.
Output: Complete GTK event/clipboard adapters, truthful model observations, and real production-path interaction tests.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-CONTEXT.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-RESEARCH.md
@.planning/spikes/008-multi-pane-ghostty-integration/README.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-03-SUMMARY.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-04-SUMMARY.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-06-SUMMARY.md
@native/core/reducer.zig
@native/terminal/ownership.zig
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Forward complete GTK input, IME, mouse, selection, and clipboard behavior</name>
  <files>native/linux/ghostty_input.zig, native/linux/ghostty_clipboard.zig, native/linux/ghostty_surface.zig, native/tests/ghostty_interaction_test.zig</files>
  <behavior>
    - "Press/repeat/release includes physical keycode, logical key, modifiers, consumed modifiers, composing state, text, and unshifted codepoint for the focused surface."
    - "IME preedit text plus cursor and committed text are forwarded independently; the IME cursor rectangle follows ghostty_surface_ime_point."
    - "Pointer motion/enter/leave, buttons, click count, smooth/discrete scroll, modifiers, focus, and scale-adjusted coordinates reach Ghostty."
    - "System and primary clipboard reads/writes complete asynchronously on the requesting live generation; selection copy and paste actions use Ghostty behavior."
  </behavior>
  <action>Port the event-controller and clipboard topology from Limux rather than reusing the superseded product encoder. Translate GTK key events to Ghostty's complete key struct and send committed text separately; connect `GtkIMContext` preedit/commit and update its cursor location from Ghostty. Forward focus, pointer, buttons, motion, leave, and scrolling using content-scale-correct surface coordinates and Ghostty enums. Implement clipboard callbacks for standard/selection targets, request reason, confirmation policy, async GDK reads, writes, and exact request completion. Bind copy/paste/select-all/clear/font-size actions to Ghostty surface actions/config APIs. Every async closure must retain surface identity plus generation and cancel safely after destroy.</action>
  <verify><automated>bun run native:test:interaction &amp;&amp; bun run native:smoke-terminal</automated></verify>
  <done>All interaction channels use the focused live Ghostty generation and automated real-shell/TUI probes prove keyboard, queries, mouse, Unicode, IME, selection, clipboard, resize, and alternate-screen behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adapt Ghostty lifecycle observations into the shared model and prove surface isolation</name>
  <files>native/linux/ghostty_runtime.zig, native/linux/ghostty_surface.zig, native/linux/app.zig, native/terminal/ghostty_process_control.zig, native/terminal/guard.zig, native/core/reducer.zig, native/tests/ghostty_interaction_test.zig, native/tests/reducer_test.zig, scripts/verify-native.ts</files>
  <behavior>
    - "Title, cwd, bell, attention, close request, child exit status, process-exited, and needs-confirm-quit observations apply only to the matching product surface identity."
    - "A close requests Ghostty graceful shutdown, waits two seconds, then uses the registered PID/PGID/birth token to escalate the whole group through SIGHUP, SIGTERM after 500 ms, and SIGKILL after two seconds until absence is proven."
    - "The sibling guard owns the same validated registration over its private inherited channel and performs the identical bounded group cleanup after host crash; unproven absence maps to failed_cleanup."
    - "Two surfaces have distinct handles, identities, GtkGLAreas, focus/input queues, sizes, clipboard requests, process lifetimes, and teardown generations while sharing only app/config."
    - "Destroying one surface leaves the other responsive and app quit drains every surface before shared runtime destruction."
  </behavior>
  <action>Per D-22, preserve the shared reducer/restoration model and reuse Plan 105-04's proven registration, escalation, guard protocol, birth-token validation, and absence-proof algorithms through a new `ghostty_process_control` adapter. Ghostty still creates/owns the PTY and child; immediately after surface creation, query the bounded embedded ABI for PID, PGID, and Linux birth token, reject unsafe identities, register with the sibling guard before exposing `live`, and unwind the surface if registration fails. Implement D-09 close as Ghostty graceful-close followed by the exact bounded whole-group escalation when still present; unregister only after child-exit/reap plus birth-token-aware absence proof. D-10 confirmation uses Ghostty's `needs_confirm_quit`; D-11 retains failed_cleanup and redacted diagnostics whenever identity or absence cannot be proven; D-12 guard EOF performs the same escalation for every registration after host crash. Add real descendant, stale-token/PID-reuse, host-SIGKILL, close/quit, and injected-unproven-absence tests against the production surface. Associate title/cwd/bell/attention/exit outcomes with stable product surface identity. Extend the host smoke mode to create two leaves in a GTK split solely to prove Phase 106 readiness: focus/type/resize independently, destroy either order, and verify surviving responsiveness plus zero surfaces/callbacks/registered groups/children.</action>
  <verify><automated>bun run native:test:model &amp;&amp; bun run native:test:lifecycle &amp;&amp; bun run native:test:interaction &amp;&amp; bun run native:smoke-multisurface &amp;&amp; bun run native:audit-production-graph</automated></verify>
  <done>The product model truthfully observes Ghostty-owned lifetimes and two independently operable leaves establish the exact reusable boundary needed by Phase 106.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| GTK/GDK input and clipboard to terminal child | Untrusted user/clipboard data crosses into a shell process. |
| Async callbacks to surface registry | Late completion can target a destroyed or different pane. |
| Ghostty lifecycle to product reducer | Opaque terminal lifetime must not be falsely represented. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-105-CLIP-01 | Spoofing | clipboard completion | high | mitigate | Match request, identity, generation, and target before completion. |
| T-105-INPUT-01 | Tampering | key/IME translation | medium | mitigate | Complete event fields and focused-surface tests. |
| T-105-LIFE-01 | Repudiation | lifecycle observation | high | mitigate | Stable surface IDs, explicit outcomes, bounded diagnostics, no inferred liveness. |
| T-105-XTALK-01 | Elevation of Privilege | multi-surface registry | high | mitigate | Per-surface userdata/generation isolation and destroy-order tests. |
</threat_model>

<verification>`bun run native:test:model &amp;&amp; bun run native:test:interaction &amp;&amp; bun run native:smoke-terminal &amp;&amp; bun run native:smoke-multisurface &amp;&amp; bun run native:audit-production-graph`.</verification>
<success_criteria>Production panes have complete Ghostty-native interaction, truthful product observations, no competing PTY ownership, and proven independent multi-surface operation.</success_criteria>
<output>Create `.planning/phases/105-shared-native-model-and-terminal-foundation/105-07-SUMMARY.md` when done.</output>
