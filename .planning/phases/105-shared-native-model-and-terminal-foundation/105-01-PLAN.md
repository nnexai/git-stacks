---
phase: 105-shared-native-model-and-terminal-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - native/build.zig
  - native/build.zig.zon
  - native/deps/ghostty.lock
  - scripts/verify-native.ts
  - package.json
autonomous: true
requirements: [TERM-03]
must_haves:
  truths:
    - "The native build fails closed unless it uses Zig 0.15.2 and peeled Ghostty v1.3.1 commit 332b2aefc6e72d363aa93ab6ecfc86eeeeb5ed28."
  artifacts:
    - path: "native/deps/ghostty.lock"
      provides: "Immutable Ghostty tag object, peeled source commit, source checksum, Zig version, and Zig artifact checksum provenance"
    - path: "scripts/verify-native.ts"
      provides: "Repo-controlled native prerequisite and test orchestration"
  key_links:
    - from: "scripts/verify-native.ts"
      to: "native/deps/ghostty.lock"
      via: "checksum and exact-version prerequisite validation"
---

<objective>
Prove the exact pinned native stack is feasible before any model, ABI, or terminal implementation depends on it.

Purpose: Retire the highest-risk libghostty/Zig/API assumptions as the first dependency.
Output: Exact-pin native build, feasibility evidence, immutable provenance, and native gate entrypoints.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-CONTEXT.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-RESEARCH.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-VALIDATION.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-PATTERNS.md
@src/lib/service/contract.ts
@tests/fixtures/service-v1
@scripts/verify-prereqs.ts
@scripts/verify-gates.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the exact-pin feasibility spike and freeze native provenance</name>
  <files>native/build.zig, native/build.zig.zon, native/deps/ghostty.lock, scripts/verify-native.ts, package.json</files>
  <action>Before building the model, implement the early feasibility spike required by the roadmap concern and D-15. Provision the official Zig 0.15.2 artifact through a repo-controlled version plus platform checksum path; reject ambient Zig 0.16.0. Fetch Ghostty only from its canonical repository, record the complete annotated tag object `22efb0be2bbea73e5339f5426fa3b20edabcaa11` separately from the exact peeled source commit `332b2aefc6e72d363aa93ab6ecfc86eeeeb5ed28`, and make lock verification assert both complete identities, checked-out HEAD, source checksum, and upstream declared minimum Zig. Prove a clean compile can import the pinned full libghostty surface API and reference create/free, draw, focus, size/content-scale, key/text/preedit, mouse, IME point, clipboard, child-exit, and close-confirmation seams. Keep the spike behind `native/build.zig`; do not use `libghostty-vt` as the host or add registry packages. Add `native:test:quick`, focused native scripts, and `native:verify` package scripts through `scripts/verify-native.ts`, using collected fail-closed diagnostics and no network during ordinary verification. If the exact pin cannot compile or a named API is absent, stop this plan with captured compiler/API evidence instead of designing against another revision.</action>
  <verify>
    <automated>bun run native:test:terminal-build</automated>
  </verify>
  <done>A clean repo-controlled Zig 0.15.2 build verifies the exact peeled Ghostty commit and named API surface, while wrong compiler/source/checksum inputs fail before compilation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TypeScript fixture bytes → Zig decoder | Contract-controlled but untrusted bytes cross into native memory and state. |
| C caller → product ABI | Foreign callers control pointers, lengths, call order, and retained allocations. |
| Network/source artifacts → native build | Compiler and Ghostty source provenance determine executable native code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-105-01 | Tampering | native pins and fixture corpus | high | mitigate | Verify exact compiler/source checksums, peeled commit, canonical fixture hashes, and fail closed before build. |
| T-105-02 | Denial of Service | ABI decode/allocation | high | mitigate | Bound byte lengths and allocations; reject malformed input before mutation. |
| T-105-03 | Elevation of Privilege | foreign-pointer ownership | high | mitigate | Opaque handles, generation/liveness validation, product-owned copies, and matching frees with negative tests. |
| T-105-SC | Tampering | source/toolchain acquisition | high | mitigate | No registry installs; verify canonical repository and official compiler artifact hashes. |
</threat_model>

<verification>
Run `bun run native:test:terminal-build`. Confirm the feasibility report records Zig 0.15.2, Ghostty peeled commit `332b2aefc6e72d363aa93ab6ecfc86eeeeb5ed28`, annotated tag object `22efb0be2bbea73e5339f5426fa3b20edabcaa11`, and all required full-surface API seams.
</verification>

<success_criteria>
The pinned native stack compiles reproducibly; strict cross-language fixture decoding and ABI negative tests pass; wrong pins, divergent fixtures, invalid calls, and platform/upstream symbol leakage are mechanically rejected.
</success_criteria>

<output>
Create `.planning/phases/105-shared-native-model-and-terminal-foundation/105-01-SUMMARY.md` when done.
</output>
