---
phase: 99-dashboard-actions-and-correctness-polish
plan: 04
title: Action Menu Regression And Rollback Exclusion Gate
type: execute
wave: 4
depends_on:
  - 99-01
  - 99-02
  - 99-03
files_modified:
  - tests/tui/dashboard/ActionMenu.test.tsx
  - tests/tui/dashboard/RepoActionMenu.test.tsx
  - tests/tui/dashboard/integ-action-menu.test.tsx
  - tests/tui/dashboard/CreateProgressView.test.tsx
  - tests/tui/dashboard/snapshots/ProgressView.snap.test.tsx
autonomous: true
requirements:
  - TUI-05
  - TUI-06
user_setup: []
must_haves:
  truths:
    - "D-04: action-menu shortcuts, labels, disabled states, and footer hints stay coherent across Workspaces, Templates, and Repos."
    - "D-08: manual command failures stay visible until keypress."
    - "D-12: issue-open failures stay visible until keypress."
    - "D-13: rollback progress visibility is excluded from Phase 99 and remains unchanged."
    - "The roadmap `DASH-01` text is superseded by `99-CONTEXT.md` for this phase."
  artifacts:
    - path: "tests/tui/dashboard/ActionMenu.test.tsx"
      provides: "Workspace action menu regression coverage"
    - path: "tests/tui/dashboard/RepoActionMenu.test.tsx"
      provides: "Repo action menu regression coverage"
    - path: "tests/tui/dashboard/integ-action-menu.test.tsx"
      provides: "End-to-end dashboard action routing coverage"
    - path: "tests/tui/dashboard/CreateProgressView.test.tsx"
      provides: "Negative guard that rollback progress rendering is not Phase 99 work"
  key_links:
    - from: "tests/tui/dashboard/integ-action-menu.test.tsx"
      to: "src/tui/dashboard/App.tsx"
      via: "keyboard routing and progress view assertions"
      pattern: "ProgressView"
---

# Phase 99 Plan 04 - Action Menu Regression And Rollback Exclusion Gate

<objective>
Finish Phase 99 by locking regression coverage for action-menu coherence and the explicit rollback-progress exclusion.

Purpose: make the new dashboard actions safe to ship without accidentally absorbing the older `DASH-01` rollback-progress scope.
Output: focused regression tests and negative guards around shortcuts, disabled states, progress persistence, and non-target create-flow files.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/99-dashboard-actions-and-correctness-polish/99-CONTEXT.md
@.planning/phases/99-dashboard-actions-and-correctness-polish/99-UI-SPEC.md
@.planning/phases/99-dashboard-actions-and-correctness-polish/99-VALIDATION.md
@src/tui/dashboard/ActionMenu.tsx
@src/tui/dashboard/RepoActionMenu.tsx
@src/tui/dashboard/TemplateActionMenu.tsx
@src/tui/dashboard/App.tsx
@src/tui/dashboard/CreateProgressView.tsx
@tests/tui/dashboard/CreateProgressView.test.tsx
</context>

<tasks>
  <task type="auto" tdd="true">
    <name>Task 1: Complete action-menu regression matrix</name>
    <files>tests/tui/dashboard/ActionMenu.test.tsx, tests/tui/dashboard/RepoActionMenu.test.tsx, tests/tui/dashboard/integ-action-menu.test.tsx, tests/tui/dashboard/snapshots/ProgressView.snap.test.tsx</files>
    <read_first>tests/tui/dashboard/ActionMenu.test.tsx, tests/tui/dashboard/RepoActionMenu.test.tsx, tests/tui/dashboard/integ-action-menu.test.tsx, tests/tui/dashboard/snapshots/ProgressView.snap.test.tsx, .planning/phases/99-dashboard-actions-and-correctness-polish/99-VALIDATION.md</read_first>
    <behavior>
      - Test 1: workspace, template, and repo menus keep stable labels and shortcuts after the new rows are added.
      - Test 2: disabled issue and command rows remain visible but cannot be activated.
      - Test 3: issue-open and manual-command failure states render progress/error text and require a keypress to return to list.
      - Test 4: footer/help text does not advertise unavailable rollback progress or stale cleanup controls.
    </behavior>
    <action>Fill any coverage gaps left by Plans 01 through 03. Prefer focused component tests for row labels/shortcuts/disabled states and App integration tests for progress persistence. Add or update a `ProgressView` snapshot only if a new failure-output state needs a stable terminal assertion; otherwise keep coverage in component tests.</action>
    <verify>
      <automated>bun run test tests/tui/dashboard/ActionMenu.test.tsx tests/tui/dashboard/RepoActionMenu.test.tsx tests/tui/dashboard/integ-action-menu.test.tsx tests/tui/dashboard/snapshots/ProgressView.snap.test.tsx</automated>
    </verify>
    <done>The action menu regression matrix covers labels, shortcuts, disabled states, picker routing, and progress persistence.</done>
  </task>

  <task type="auto" tdd="true">
    <name>Task 2: Add rollback exclusion guard</name>
    <files>tests/tui/dashboard/CreateProgressView.test.tsx, src/tui/dashboard/CreateProgressView.tsx, src/tui/dashboard/App.tsx</files>
    <read_first>tests/tui/dashboard/CreateProgressView.test.tsx, src/tui/dashboard/CreateProgressView.tsx, src/tui/dashboard/App.tsx, .planning/phases/99-dashboard-actions-and-correctness-polish/99-CONTEXT.md</read_first>
    <behavior>
      - Test 1: no Phase 99 test requires new create-flow rollback progress rows.
      - Test 2: `CreateProgressView` keeps its existing row contract and does not add file-op, workspace-file-op, or env-file rollback rows in this phase.
      - Test 3: if `App.tsx` is touched by Phase 99 plans, its create-flow parser remains behaviorally unchanged except for unrelated formatting.
    </behavior>
    <action>Add a narrow negative guard only if needed to prevent scope creep: assert that the current create-progress row model remains repo-focused and does not include Phase 99 action-menu additions or new rollback event rendering. Do not modify `CreateProgressView.tsx` or the create-flow parser unless a test import path needs a harmless fixture adjustment. If a concurrent Phase 98 change already touched these files, re-read and preserve those changes.</action>
    <verify>
      <automated>bun run test tests/tui/dashboard/CreateProgressView.test.tsx</automated>
    </verify>
    <done>Phase 99 has an explicit test/documentation guard that rollback progress visibility remains outside the phase.</done>
  </task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests -> shipped action contract | Regression tests define which keyboard actions and disabled states are safe to rely on. |
| roadmap text -> execution scope | Older `DASH-01` roadmap text must not override the newer Phase 99 context scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-09 | Tampering | action-menu shortcuts | mitigate | Component tests assert old and new shortcut dispatch after all menu changes. |
| T-99-10 | Repudiation | scope override | mitigate | Add a rollback exclusion guard tied to `99-CONTEXT.md` so execution does not silently absorb `DASH-01`. |
| T-99-11 | Denial of Service | progress failure views | mitigate | Tests assert failure views remain visible until keypress for issue and command actions. |
| T-99-SC | Tampering | dependencies | mitigate | No dependency changes; use existing Bun/OpenTUI tests. |
</threat_model>

<verification>
- `bun run test tests/tui/dashboard/ActionMenu.test.tsx tests/tui/dashboard/RepoActionMenu.test.tsx tests/tui/dashboard/integ-action-menu.test.tsx tests/tui/dashboard/CreateProgressView.test.tsx tests/tui/dashboard/snapshots/ProgressView.snap.test.tsx`
- `bun run typecheck`
- `git diff --check`
</verification>

<success_criteria>
- Action menus across Workspaces, Templates, and Repos have coherent shortcuts, labels, disabled states, and footer/help behavior.
- Issue and command failures stay visible until keypress.
- Rollback progress visibility remains excluded from Phase 99 implementation and test expectations.
- No create-flow rollback rendering is implemented in this phase.
</success_criteria>

<output>
Create `.planning/phases/99-dashboard-actions-and-correctness-polish/99-04-SUMMARY.md` when done.
</output>
