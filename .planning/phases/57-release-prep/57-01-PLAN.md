---
phase: 57-release-prep
plan: 01
type: standard
wave: 1
depends_on: []
files_modified:
  - package.json
  - CHANGELOG.md
  - README.md
autonomous: true
requirements: [REL-01, REL-02, REL-03]

must_haves:
  truths:
    - "`package.json` `version` field reads `\"0.13.0\"`"
    - "CHANGELOG.md has a `## [0.13.0]` section at the top (above `## [0.12.0]`) with today's date"
    - "CHANGELOG.md v0.13.0 entry covers four feature areas: shell completion fixes (Phase 53), env command (Phase 54), Copilot hook support (Phase 55), doctor & config polish (Phase 56)"
    - "CHANGELOG.md follows the existing Keep a Changelog format: `### Added` / `### Fixed` / `### Changed` subsections with bold title + description"
    - "No 'unreleased' marker appears in CHANGELOG.md"
    - "README.md documents `git-stacks env [workspace]` command with format options and usage examples"
    - "README.md documents `git-stacks install --hooks --copilot` / `--claude` flags"
    - "`git-stacks --version` outputs `0.13.0` (verified via `bun run src/index.ts --version`)"
  artifacts:
    - path: "package.json"
      provides: "Version 0.13.0"
      contains: "\"version\": \"0.13.0\""
    - path: "CHANGELOG.md"
      provides: "v0.13.0 release notes"
      contains: "## [0.13.0]"
    - path: "README.md"
      provides: "Env command and Copilot hooks documentation"
      contains: "git-stacks env"
  key_links:
    - from: "CHANGELOG.md"
      to: "package.json"
      via: "Version number must match"
      pattern: "0.13.0"
---

<objective>
Release preparation for v0.13.0: bump version in package.json, add CHANGELOG section covering all features from Phases 53-56 (shell completion fixes, env command, Copilot hook support, doctor & config polish), and update README with new user-facing commands (`env`, `install --hooks --copilot/--claude`).

Purpose: Ship v0.13.0 as a documented release with all new features discoverable by users reading the CHANGELOG and README.

Output: Updated package.json (version), CHANGELOG.md (new release section), and README.md (env command + Copilot hooks docs).
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/57-release-prep/57-CONTEXT.md
@.planning/phases/53-shell-completion-fixes/53-CONTEXT.md
@.planning/phases/54-env-command/54-CONTEXT.md
@.planning/phases/55-copilot-hook-support/55-CONTEXT.md
@.planning/phases/56-doctor-config-polish/56-CONTEXT.md
@.planning/quick/260402-6c0-update-changelog-and-readme-to-prepare-f/260402-6c0-PLAN.md

@package.json
@CHANGELOG.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version in package.json to 0.13.0</name>
  <files>package.json</files>
  <read_first>
    - package.json (current version field — reads "0.12.0")
  </read_first>
  <action>
Read `package.json` and change the `version` field from `"0.12.0"` to `"0.13.0"`.

Edit:
- Old: `"version": "0.12.0"`
- New: `"version": "0.13.0"`

No other fields in package.json should be changed.

Verify with: `bun run src/index.ts --version` — should output `0.13.0`.

Commit: `chore(57-01): bump version to 0.13.0`
  </action>
  <acceptance_criteria>
    - `package.json` contains `"version": "0.13.0"`
    - `grep '"version": "0.13.0"' package.json` returns a match
    - `bun run src/index.ts --version` outputs `0.13.0`
    - No other fields in package.json were modified
  </acceptance_criteria>
  <verify>
    <automated>grep '"version": "0.13.0"' package.json && bun run src/index.ts --version 2>&1 | grep "0.13.0"</automated>
  </verify>
  <done>package.json version field reads "0.13.0" and CLI outputs correct version.</done>
</task>

<task type="auto">
  <name>Task 2: Add v0.13.0 section to CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (lines 1-80 — existing format: `## [version] — date` headers, `### Added` / `### Fixed` / `### Changed` subsections, bold title + description per feature)
    - .planning/phases/53-shell-completion-fixes/53-CONTEXT.md (Phase 53: arity enforcement, enum auto-detection, parent flag leakage fix)
    - .planning/phases/54-env-command/54-CONTEXT.md (Phase 54: `git-stacks env [workspace]` with --format flag, CWD auto-detection, --repo flag)
    - .planning/phases/55-copilot-hook-support/55-CONTEXT.md (Phase 55: Copilot AgentHookPlugin, --copilot/--claude flags, simultaneous install)
    - .planning/phases/56-doctor-config-polish/56-CONTEXT.md (Phase 56: conditional forge CLI checks in doctor, tmux configExample with panes)
    - .planning/phases/57-release-prep/57-CONTEXT.md (D-03 through D-05 — CHANGELOG format decisions)
  </read_first>
  <action>
Insert a new `## [0.13.0]` section at the top of CHANGELOG.md, between the first `---` separator (line 5) and the existing `## [0.12.0]` section (line 7).

The new section to insert (use today's date 2026-04-02):

```markdown
## [0.13.0] — 2026-04-02

### Added

**Env command** — `git-stacks env [workspace]` shows all merged environment variables that would be injected when opening a workspace. Includes GS_* injected vars, user-defined `env:` from workspace YAML, and resolved port vars. Supports `--format table` (default, human-readable columns), `--format shell` (`export KEY=value` lines, sourceable), `--format dotenv` (`KEY=value` lines), and `--format json` (JSON object). Auto-detects workspace from CWD when no argument is given. `--repo <name>` adds per-repo vars (GS_REPO_NAME, GS_REPO_PATH, GS_REPO_CLONE_PATH) to the output.

**Copilot hook support** — `git-stacks install --hooks` now supports GitHub Copilot alongside Claude Code. Use `--copilot` to install Copilot hooks (`.github/hooks/git-stacks.json`), `--claude` to install Claude Code hooks (existing behavior, now under an explicit flag), or both flags together to install both simultaneously. When neither flag is given, an interactive prompt lets you choose which hook set(s) to install. Copilot hooks provide notification parity with Claude Code hooks — session end, user prompt, and tool-use events are all mapped to `git-stacks message send` calls.

**Tmux config example** — `git-stacks integration tmux config example` now prints a practical YAML snippet showing the native `panes` array with direction, surfaces, and commands, replacing the previous minimal `enabled: true` example.

### Fixed

**Shell completion arity enforcement** — after all positional arguments are filled, pressing Tab no longer repeats workspace/template/repo completions. Only remaining unused flags are offered. Variadic args (`[command...]`) remain exempt. Fix applies to bash, zsh, and fish.

**Shell completion enum values** — flags with constrained values (e.g., `--format`, `--sort`, `--strategy`) now offer their valid values as Tab completions. Auto-detected from Commander.js `.choices()` calls; `OPTION_ENUMS` map serves as a fallback. Command definitions updated to use `.choices()` where option values are constrained.

**Shell completion flag leakage** — flags from `git-stacks list` (e.g., `--sort`, `--status`) no longer appear in completions for `git-stacks integration list`. Option enum completions are now scoped to the command path they belong to, not emitted globally.

### Changed

**Conditional forge CLI checks in doctor** — `git-stacks doctor` now only checks for forge CLI binaries (`gh`, `glab`, `tea`) when the respective integration is configured with `enabled: true` in global config. Previously, all forge CLIs were checked unconditionally.

---
```

The inserted text goes between line 5 (`---`) and line 7 (`## [0.12.0]`). The result should be:
- Line 5: `---` (existing)
- Line 6: empty line
- Lines 7-29: new v0.13.0 section (including trailing `---` separator)
- Line 30: empty line
- Line 31: `## [0.12.0] — 2026-03-29` (existing, shifted down)

Commit: `docs(57-01): add v0.13.0 CHANGELOG entry`
  </action>
  <acceptance_criteria>
    - CHANGELOG.md contains `## [0.13.0]`
    - The `## [0.13.0]` section appears BEFORE `## [0.12.0]`
    - CHANGELOG.md contains `**Env command**`
    - CHANGELOG.md contains `**Copilot hook support**`
    - CHANGELOG.md contains `**Shell completion arity enforcement**`
    - CHANGELOG.md contains `**Shell completion enum values**`
    - CHANGELOG.md contains `**Shell completion flag leakage**`
    - CHANGELOG.md contains `**Conditional forge CLI checks in doctor**`
    - CHANGELOG.md contains `**Tmux config example**`
    - CHANGELOG.md contains `### Added` under the `## [0.13.0]` header
    - CHANGELOG.md contains `### Fixed` under the `## [0.13.0]` header
    - CHANGELOG.md contains `### Changed` under the `## [0.13.0]` header
    - `grep -c "## \[0.13.0\]" CHANGELOG.md` returns `1`
    - No "unreleased" text appears in CHANGELOG.md
    - The section follows Keep a Changelog format matching v0.12.0 style (bold title + description)
  </acceptance_criteria>
  <verify>
    <automated>grep "## \[0.13.0\]" CHANGELOG.md && grep "Env command" CHANGELOG.md && grep "Copilot hook support" CHANGELOG.md && grep "Shell completion arity enforcement" CHANGELOG.md && grep "Shell completion enum values" CHANGELOG.md && grep "Shell completion flag leakage" CHANGELOG.md && grep "Conditional forge CLI checks" CHANGELOG.md && grep "Tmux config example" CHANGELOG.md && grep -ci "unreleased" CHANGELOG.md | grep "^0$"</automated>
  </verify>
  <done>CHANGELOG.md has a complete v0.13.0 section with all features from Phases 53-56 documented.</done>
</task>

<task type="auto">
  <name>Task 3: Update README.md with env command and Copilot hooks documentation</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — identify insertion points for env command and agent hook updates)
    - .planning/phases/54-env-command/54-CONTEXT.md (D-01 through D-11 — env command format and behavior)
    - .planning/phases/55-copilot-hook-support/55-CONTEXT.md (D-01 through D-04 — flag design)
    - .planning/phases/57-release-prep/57-CONTEXT.md (D-06 through D-08 — README update decisions)
  </read_first>
  <action>
Make two changes to README.md:

**Change 1: Add `env` command to the Workspaces command list (line 99)**

After the existing line:
```
git-stacks pull [workspace]        # Pull latest for all repos (--ff-only, skips dirty)
```

Insert:
```
git-stacks env [workspace]         # Show merged env vars that would be injected at open time
```

**Change 2: Add Env Inspection section**

Insert a new `## Env Inspection` section AFTER the `## Multi-Repo Pull` section (after line 157) and BEFORE the `## Workspace Notifications` section (line 159).

Content to insert:

```markdown
## Env Inspection

Inspect all environment variables that a workspace would inject when opened:

```bash
# Show all env vars for a workspace (table format)
git-stacks env my-feature

# Auto-detect workspace from current directory
git-stacks env

# Output as sourceable shell exports
git-stacks env my-feature --format shell
# Output: export GS_WORKSPACE_NAME=my-feature
#         export GS_WORKSPACE_BRANCH=my-feature
#         export API_PORT=10000
#         ...

# Output as dotenv file (redirectable)
git-stacks env my-feature --format dotenv > .env

# Output as JSON object
git-stacks env my-feature --format json

# Include per-repo vars for a specific repo
git-stacks env my-feature --repo backend-api
```

Shows GS_* injected vars, user-defined `env:` from workspace YAML, and resolved port vars — the full picture of what hooks and processes see at open time. When run inside a repo worktree without `--repo`, the repo is auto-detected and its vars are included.
```

**Change 3: Update Agent Hook Installer section (lines 218-232)**

Replace the current Agent Hook Installer section content with expanded documentation that covers the new `--copilot` and `--claude` flags:

Find the section starting at `## Agent Hook Installer` and replace everything from the section header through the paragraph ending with `src/lib/agent-hooks/`.` with:

```markdown
## Agent Hook Installer

Install AI agent notification hooks into the current project so the TUI dashboard shows when an agent needs your attention:

```bash
# Interactive — choose which agent frameworks to install
git-stacks install --hooks

# Install Claude Code hooks explicitly
git-stacks install --hooks --claude

# Install GitHub Copilot hooks
git-stacks install --hooks --copilot

# Install both Claude Code and Copilot hooks simultaneously
git-stacks install --hooks --copilot --claude

# Remove installed hooks
git-stacks install --hooks --remove
```

Claude Code hooks are written to `.claude/settings.json`. Copilot hooks are written to `.github/hooks/git-stacks.json`. Both hook sets bridge agent lifecycle events (task completion, user questions, prompt submission) into workspace notifications via `git-stacks message send`.

When neither `--claude` nor `--copilot` is passed, an interactive prompt lets you choose which hook set(s) to install. The plugin system is extensible — new agent frameworks can be added as plugins in `src/lib/agent-hooks/`.
```

These three changes preserve all existing README content. The env command section follows the same pattern as Agent Path Discovery and Multi-Repo Pull (heading, description, code examples, explanation).

Commit: `docs(57-01): add env command and Copilot hooks to README`
  </action>
  <acceptance_criteria>
    - README.md Workspaces command list contains `git-stacks env [workspace]`
    - README.md contains `## Env Inspection` as a section heading
    - README.md contains `--format shell` in the env section
    - README.md contains `--format dotenv` in the env section
    - README.md contains `--format json` in the env section
    - README.md contains `--repo backend-api` in the env section
    - README.md Agent Hook Installer section contains `--copilot` flag
    - README.md Agent Hook Installer section contains `--claude` flag
    - README.md contains `git-stacks install --hooks --copilot --claude`
    - README.md mentions `.github/hooks/git-stacks.json` for Copilot hooks
    - README.md mentions `.claude/settings.json` for Claude Code hooks
    - The `## Env Inspection` section appears BEFORE `## Workspace Notifications`
    - The `## Env Inspection` section appears AFTER `## Multi-Repo Pull`
    - All existing README content outside the modified sections is preserved
    - `grep "git-stacks env" README.md | wc -l` returns at least 6 matches
    - `grep "\-\-copilot" README.md | wc -l` returns at least 2 matches
  </acceptance_criteria>
  <verify>
    <automated>grep "## Env Inspection" README.md && grep "git-stacks env" README.md && grep "\-\-format shell" README.md && grep "\-\-format dotenv" README.md && grep "\-\-format json" README.md && grep "\-\-repo backend-api" README.md && grep "\-\-copilot" README.md && grep "\-\-claude" README.md && grep "git-stacks install --hooks --copilot --claude" README.md && grep ".github/hooks/git-stacks.json" README.md</automated>
  </verify>
  <done>README.md updated with env command documentation (section + command list entry) and expanded Agent Hook Installer section with --copilot/--claude flags.</done>
</task>

</tasks>

<verification>
```bash
# Version
grep '"version": "0.13.0"' package.json

# CLI version output
bun run src/index.ts --version 2>&1 | grep "0.13.0"

# CHANGELOG has v0.13.0 entry
grep "## \[0.13.0\]" CHANGELOG.md

# CHANGELOG covers all feature areas
grep "Env command" CHANGELOG.md
grep "Copilot hook support" CHANGELOG.md
grep "Shell completion arity enforcement" CHANGELOG.md
grep "Shell completion enum values" CHANGELOG.md
grep "Shell completion flag leakage" CHANGELOG.md
grep "Conditional forge CLI checks" CHANGELOG.md
grep "Tmux config example" CHANGELOG.md

# No unreleased marker
grep -ci "unreleased" CHANGELOG.md | grep "^0$"

# README has env command section
grep "## Env Inspection" README.md
grep "git-stacks env" README.md

# README has Copilot flags in hook installer
grep "\-\-copilot" README.md
grep "\-\-claude" README.md

# README has both hook file paths
grep ".github/hooks/git-stacks.json" README.md
grep ".claude/settings.json" README.md

# Typecheck still passes (no code changes, but verify)
bun run typecheck
```
</verification>

<success_criteria>
1. `package.json` version field reads `0.13.0` and `git-stacks --version` outputs `0.13.0`
2. CHANGELOG.md has a `## [0.13.0]` section with no "unreleased" marker, covering: env command, Copilot hook support, tmux config example (Added); shell completion arity/enum/leakage fixes (Fixed); conditional forge CLI checks (Changed)
3. README.md documents `git-stacks env [workspace]` with format options (`--format shell|dotenv|json|table`) and `--repo` flag
4. README.md Agent Hook Installer section documents `--copilot`, `--claude`, and combined usage
5. All existing content in CHANGELOG and README is preserved (no regressions)
6. Typecheck still passes
</success_criteria>

<output>
After completion, create `.planning/phases/57-release-prep/57-01-SUMMARY.md`
</output>
