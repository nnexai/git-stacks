---
phase: 106-linux-workspace-commands-and-attention
plan: 01
type: execute
wave: 1
depends_on: [105-09]
files_modified:
  - src/lib/service/contract.ts
  - src/lib/service/snapshot.ts
  - src/lib/service/event-journal.ts
  - src/service/server.ts
  - src/lib/agent-hooks/types.ts
  - src/lib/agent-hooks/claude-code.ts
  - src/lib/agent-hooks/copilot.ts
  - tests/lib/service/contract.test.ts
  - tests/lib/service/snapshot.test.ts
  - tests/lib/service/event-journal.test.ts
  - tests/lib/service/native-launch.test.ts
  - tests/lib/agent-hooks/structured-attention.test.ts
  - tests/fixtures/service-v1/discovery.json
  - tests/fixtures/service-v1/workspace-snapshot.json
autonomous: true
requirements: [ACT-01, ACT-02, ACT-03, ACT-06]
must_haves:
  truths:
    - "A native client resolves a shell or configured command from stable identities and the current authoritative revision immediately before startup."
    - "Working, waiting, completed, failed, and idle hook states reach the journal as strictly validated identity-addressed events without terminal-output scraping."
    - "Resolution or validation failure returns a structured error and supplies no launch specification that could create a phantom tab."
  artifacts:
    - path: "src/lib/service/contract.ts"
      provides: "Stable command identities, fresh launch-resolution request/response schemas, and structured attention event schema"
    - path: "src/service/server.ts"
      provides: "Authenticated native launch-resolution route"
    - path: "src/lib/agent-hooks/types.ts"
      provides: "Validated structured hook lifecycle input"
    - path: "tests/lib/service/native-launch.test.ts"
      provides: "Fresh-resolution and no-phantom-launch contract coverage"
  key_links:
    - from: "src/service/server.ts"
      to: "src/lib/service/snapshot.ts"
      via: "authenticated stable-ID and revision-bound launch resolution"
    - from: "src/lib/agent-hooks/claude-code.ts"
      to: "src/lib/service/event-journal.ts"
      via: "structured attention publication rather than free-form message inference"
---

<objective>
Extend the authoritative service boundary with fresh native launch resolution and structured attention publication.

Purpose: Phase 106 cannot safely create tabs or route attention until commands and attention have stable identities and strict service-owned semantics.
Output: Versioned schemas, resolution route, hook adapters, fixtures, and compatibility/journal tests implementing D-11 through D-15 and ACT-01 through ACT-03.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/106-linux-workspace-commands-and-attention/106-CONTEXT.md
@.planning/phases/106-linux-workspace-commands-and-attention/106-RESEARCH.md
@.planning/phases/106-linux-workspace-commands-and-attention/106-PATTERNS.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-09-SUMMARY.md
@src/lib/service/contract.ts
@src/lib/service/snapshot.ts
@src/lib/service/event-journal.ts
@src/service/server.ts
</context>

## Artifacts this phase produces

- `CommandId`, `NativeLaunchResolutionRequest`, `NativeLaunchResolution`, `StructuredAttentionEvent`, and negotiated capability fields in `src/lib/service/contract.ts`.
- Fresh shell/configured-command resolution in `src/lib/service/snapshot.ts` and the authenticated server route in `src/service/server.ts`.
- Structured lifecycle hook input/publication in `src/lib/agent-hooks/types.ts`, `claude-code.ts`, and `copilot.ts`.
- New `native-launch.test.ts`, `structured-attention.test.ts`, and canonical service fixture coverage.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze stable command, launch-resolution, and attention contracts</name>
  <files>src/lib/service/contract.ts, tests/lib/service/contract.test.ts, tests/fixtures/service-v1/discovery.json, tests/fixtures/service-v1/workspace-snapshot.json</files>
  <read_first>src/lib/service/contract.ts, tests/lib/service/contract.test.ts, tests/fixtures/service-v1/discovery.json, tests/fixtures/service-v1/workspace-snapshot.json, .planning/phases/106-linux-workspace-commands-and-attention/106-CONTEXT.md</read_first>
  <behavior>
    - "Configured commands with duplicate display names have distinct stable opaque identities and explicit workspace/repository scope."
    - "Launch requests carry workspace, repository, optional command identity, and expected authoritative revision; responses contain structured argv/cwd/environment/ports/configuration without resolved secrets."
    - "Attention events carry a stable attention identity, lifecycle state, workspace identity, optional repository/surface identities, source, safe presentation fields, timestamp, and journal sequence."
    - "Old/new fixture behavior is explicit: negotiate the extended capability rather than allowing strict legacy clients to misparse added fields."
  </behavior>
  <action>Write failing schema and compatibility tests first, then extend the strict v1 contract. Resolve the research open questions by introducing stable opaque command identities and a negotiated structured-attention/native-launch capability; keep existing legacy message behavior additive. Validate identity domains and nesting inputs at the trust boundary. Preserve the existing secret-redaction rule and do not expose YAML or native/GTK types. This implements D-11, D-12, D-14, and D-15 while providing the identity substrate for D-13 and D-17.</action>
  <verify><automated>bun test tests/lib/service/contract.test.ts</automated></verify>
  <acceptance_criteria>Strict contract tests prove duplicate command disambiguation, capability negotiation, lifecycle enum completeness, identity validation, and rejection of malformed, cross-domain, or secret-bearing payloads.</acceptance_criteria>
  <done>The service contract has stable command and attention identities plus negotiated fresh-resolution schemas without breaking legacy fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve shell and configured-command launches authoritatively</name>
  <files>src/lib/service/snapshot.ts, src/service/server.ts, tests/lib/service/snapshot.test.ts, tests/lib/service/native-launch.test.ts</files>
  <read_first>src/lib/service/snapshot.ts, src/service/server.ts, tests/lib/service/snapshot.test.ts, src/lib/service/contract.ts</read_first>
  <behavior>
    - "A valid current workspace/repository selection resolves a shell from current cwd, environment, ports, and configuration."
    - "A configured-command identity resolves independently of its display name and only inside its declared scope."
    - "Stale revision, missing identity, unavailable capability, and resolution failure return structured errors and no launch specification."
  </behavior>
  <action>Add focused tests before implementation. Add an authenticated native launch-resolution route using the existing server admission ordering and authoritative snapshot/env/ports seams. Re-resolve on every request; never treat snapshot display strings or GTK labels as execution authority and never shell-interpolate native inputs. Bind the request to the current revision and stable workspace/repository/command identities. Return only a validated structured launch specification after redaction. This is D-13 and ACT-01/ACT-02's service half.</action>
  <verify><automated>bun test tests/lib/service/snapshot.test.ts tests/lib/service/native-launch.test.ts</automated></verify>
  <acceptance_criteria>Tests prove current shell and duplicate-name command resolution, authenticated admission, stale/missing rejection, redaction, and an empty success channel on every failure.</acceptance_criteria>
  <done>Native launch intents can obtain fresh authoritative specifications, while all invalid or stale requests fail structurally before terminal creation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Publish structured agent lifecycle attention through the durable journal</name>
  <files>src/lib/service/event-journal.ts, src/lib/agent-hooks/types.ts, src/lib/agent-hooks/claude-code.ts, src/lib/agent-hooks/copilot.ts, tests/lib/service/event-journal.test.ts, tests/lib/agent-hooks/structured-attention.test.ts</files>
  <read_first>src/lib/service/event-journal.ts, src/lib/agent-hooks/types.ts, src/lib/agent-hooks/claude-code.ts, src/lib/agent-hooks/copilot.ts, tests/lib/service/event-journal.test.ts</read_first>
  <behavior>
    - "Claude and Copilot adapters publish working, waiting, completed, failed, and idle as typed state, not inferred prose."
    - "Injected workspace/repository/surface identities are validated; missing surface identity remains repository/workspace-scoped and is never guessed."
    - "Stable attention IDs and journal sequences make replay and duplicate delivery idempotent."
    - "Legacy message persistence remains failure-isolated and compatible."
  </behavior>
  <action>Write lifecycle and journal tests first. Replace free-form completion-only hook commands with validated structured publication while retaining the legacy compatibility path required by Phase 104. Inject stable context identities into resolved terminal environments, accept absent surface identity as a broader scoped status, and reject invalid cross-scope identities. Append through `EventJournal.appendAttention` so ordering, replay, compaction, and failure isolation remain unchanged. Do not inspect terminal output. This implements ACT-03, ACT-06, D-14, and D-15.</action>
  <verify><automated>bun test tests/lib/agent-hooks/structured-attention.test.ts tests/lib/service/event-journal.test.ts</automated></verify>
  <acceptance_criteria>Tests cover all five states, exact and broader-scope identities, duplicates/replay, invalid identity rejection, legacy compatibility, and journal-unavailable failure isolation.</acceptance_criteria>
  <done>Agent hooks produce replayable identity-bound lifecycle attention without scraping output or weakening legacy behavior.</done>
</task>

</tasks>

<verification>
Run all focused commands above, then `bun run typecheck`, `bun run test:deps`, and `bun run verify:gates`.
</verification>

<success_criteria>
The service supplies stable command identities, fresh authoritative launch specifications, and ordered structured attention that downstream native code can consume without guessing identity, parsing prose, or creating phantom tabs.
</success_criteria>

<output>Create `.planning/phases/106-linux-workspace-commands-and-attention/106-01-SUMMARY.md` when done.</output>
