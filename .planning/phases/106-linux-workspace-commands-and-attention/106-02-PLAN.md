---
phase: 106-linux-workspace-commands-and-attention
plan: 02
type: execute
wave: 2
depends_on: [106-01]
files_modified:
  - native/core/identity.zig
  - native/core/model.zig
  - native/core/reducer.zig
  - native/core/persistence.zig
  - native/core/abi.zig
  - native/include/git_stacks_native_v1.h
  - native/linux/tab_registry.zig
  - native/tests/reducer_test.zig
  - native/tests/persistence_test.zig
  - native/tests/tab_registry_test.zig
  - native/tests/attention_test.zig
  - native/tests/abi_harness.c
  - native/build.zig
  - scripts/verify-native.ts
  - package.json
autonomous: true
requirements: [LNX-01, LNX-02, LNX-03, LNX-04, LNX-05, LNX-06, ACT-01, ACT-02, ACT-03, ACT-04, ACT-05, ACT-06]
must_haves:
  truths:
    - "Every workspace-repository pair owns an ordered independent collection whose live hosts survive navigation unchanged."
    - "Restored tabs preserve presentation order and lineage but are always explicitly ended."
    - "Attention aggregation and fallback routing are pure identity-based behavior; receipt never emits focus."
  artifacts:
    - path: "native/core/model.zig"
      provides: "Normalized workspaces, repositories, pair collections, surfaces, commands, and attention state"
    - path: "native/linux/tab_registry.zig"
      provides: "Navigation-independent ownership of live TerminalHost instances"
    - path: "native/core/reducer.zig"
      provides: "Launch, read, navigation, and explicit attention-selection effects"
    - path: "native/tests/attention_test.zig"
      provides: "Derived unread/severity and no-focus-theft/fallback coverage"
  key_links:
    - from: "native/core/reducer.zig"
      to: "native/linux/tab_registry.zig"
      via: "request-resolve then create/register then surface-started effect ordering"
    - from: "native/core/persistence.zig"
      to: "native/core/model.zig"
      via: "presentation-only pair/tab/pin/order restoration as ended state"
---

<objective>
Extend the shared native model from one surface to normalized pair-bound tab collections and identity-addressed attention, then prove navigation-independent host ownership.

Purpose: GTK must project deterministic product state while terminal processes remain owned independently of visible navigation.
Output: Multi-surface model/ABI/persistence, attention routing, host registry, focused Zig/C tests, and native verification targets implementing D-01 through D-08 and D-14 through D-18.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-linux-workspace-commands-and-attention/106-CONTEXT.md
@.planning/phases/106-linux-workspace-commands-and-attention/106-RESEARCH.md
@.planning/phases/106-linux-workspace-commands-and-attention/106-PATTERNS.md
@.planning/phases/106-linux-workspace-commands-and-attention/106-01-SUMMARY.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-03-SUMMARY.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-04-SUMMARY.md
@.planning/phases/105-shared-native-model-and-terminal-foundation/105-05-SUMMARY.md
@native/core/model.zig
@native/core/reducer.zig
@native/core/persistence.zig
@native/linux/terminal_host.zig
</context>

## Artifacts this phase produces

- `PairKey`, normalized workspace/repository/command/attention collections, `FocusRoute`, `FallbackReason`, and multi-surface actions/effects in native core.
- Versioned multi-entry presentation persistence and updated opaque ABI/C header symbols.
- `native/linux/tab_registry.zig` with registry-owned `TerminalHost` lifetimes.
- `native:test:tabs`, `native:test:attention`, and expanded `native:test:restore` targets plus their Zig tests.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize navigation, pair collections, surfaces, and presentation persistence</name>
  <files>native/core/identity.zig, native/core/model.zig, native/core/persistence.zig, native/tests/persistence_test.zig, native/tests/reducer_test.zig</files>
  <read_first>native/core/identity.zig, native/core/model.zig, native/core/persistence.zig, native/tests/persistence_test.zig, native/tests/reducer_test.zig, .planning/phases/106-linux-workspace-commands-and-attention/106-CONTEXT.md</read_first>
  <behavior>
    - "Workspace-first normalized state supports expandable repository rows and collapses a single-repository workspace while retaining active context."
    - "Selection restores the last valid pair, then deterministically falls back to first valid pinned pair and first valid workspace/repository."
    - "Pinned IDs and manual order reconcile against authoritative snapshots; vanished pins are removed with one notice."
    - "Each exact pair stores independently ordered, renamed surfaces; restored entries retain order/title/cwd/exit/lineage but become ended."
  </behavior>
  <action>Write normalization and restore tests before extending the one-surface Phase 105 model. Introduce stable `PairKey` and ordered ID collections rather than widget-owned objects. Implement D-01, D-02, D-03, D-04, D-05, D-06, D-07, and D-08 exactly: organization mode persistence, pinned section/manual order, deterministic reconciliation, pair-local tab order/title, ended restoration, and relaunch lineage with a distinct identity. Persist only the Phase 105 presentation allowlist; exclude argv, environment, credentials, PID/PGID, ownership, and live lifecycle. Preserve valid records independently and quarantine corrupt entries.</action>
  <verify><automated>bun run native:test:restore &amp;&amp; bun run native:test:model</automated></verify>
  <acceptance_criteria>Tests prove pair isolation, deterministic selection fallback, vanished-pin notice-once behavior, manual ordering/rename, multi-entry ended restoration, quarantine, and secret/liveness exclusion.</acceptance_criteria>
  <done>Core state and persistence truthfully represent all navigation and tab collections without platform UI ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reduce structured attention into derived aggregates and explicit focus routes</name>
  <files>native/core/model.zig, native/core/reducer.zig, native/core/abi.zig, native/include/git_stacks_native_v1.h, native/tests/attention_test.zig, native/tests/reducer_test.zig, native/tests/abi_harness.c</files>
  <read_first>native/core/model.zig, native/core/reducer.zig, native/core/abi.zig, native/include/git_stacks_native_v1.h, native/tests/reducer_test.zig, src/lib/service/contract.ts</read_first>
  <behavior>
    - "Failed and waiting are primary unread severity, completed is secondary unread, and working/idle remain visible status without unread increments."
    - "Workspace/repository counts and severity are derived from normalized items; replay/duplicate/read/removal cannot drift counters."
    - "Receipt updates state only and never emits navigation, present, or focus effects."
    - "Explicit selection routes exact live surface, ended predecessor, repository, then workspace with a machine-readable reason."
    - "Selecting one item marks it read; visibly focusing an exact tab clears current items for that tab; navigation alone is read-neutral."
  </behavior>
  <action>Add RED tests for D-14, D-15, D-16, D-17, D-18 and ACT-03 through ACT-06. Extend the pure reducer and opaque ABI with normalized attention items, derived aggregates, explicit `select_attention`, exact-tab-visible read handling, and `FocusRoute`/`FallbackReason`. Validate identity nesting against current normalized entities and retain unresolved items diagnostically. Ensure only explicit selection may emit a platform focus/navigation effect; event receipt and ordinary workspace/repository selection cannot. Keep GTK types out of core/header.</action>
  <verify><automated>bun run native:test:attention &amp;&amp; bun run native:test:model</automated></verify>
  <acceptance_criteria>Focused tests cover all lifecycle priorities, duplicate/replay/read behavior, direct and every fallback route, unresolved diagnostics, read-neutral navigation, and a zero-focus-effect invariant for asynchronous receipt.</acceptance_criteria>
  <done>Attention is deterministic, accessible to projections, and incapable of stealing focus without an explicit selection action.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Own live hosts in a pair-keyed registry and commit tabs only after successful startup</name>
  <files>native/linux/tab_registry.zig, native/tests/tab_registry_test.zig, native/build.zig, scripts/verify-native.ts, package.json</files>
  <read_first>native/linux/terminal_host.zig, native/terminal/ownership.zig, native/linux/app.zig, native/build.zig, scripts/verify-native.ts, package.json</read_first>
  <behavior>
    - "Changing selected pair preserves hidden hosts' identity, generation, PGID, ownership registration, and process lifetime."
    - "Resolution succeeds first, host registration-before-live succeeds second, and only then is a live tab appended."
    - "Resolution/startup failure changes neither registry nor tab collection."
    - "Explicit close, child exit, application quit, and crash invoke Phase 105 teardown; view detachment does not."
  </behavior>
  <action>Create a registry around the existing `TerminalHost`; do not replace Phase 105 PTY/process ownership. Key collections by exact stable pair and hosts by surface identity. Separate view attach/detach from `TerminalHost.unrealize()` and use a fake backend to assert navigation churn never changes generation, PGID, guard registration, or live count. Implement the no-phantom ordering for D-06, D-10, D-13, ACT-01, and ACT-02. Wire focused Zig targets through `native/build.zig`, the repo-controlled verifier, and package scripts rather than ambient Zig.</action>
  <verify><automated>bun run native:test:tabs &amp;&amp; bun run native:test:quick</automated></verify>
  <acceptance_criteria>Registry tests cover multiple pairs/tabs, repeated switching, reorder/rename/relaunch lineage, resolution/start failure, close/exit/quit, and invariant host generations/PGIDs during ordinary navigation.</acceptance_criteria>
  <done>Multiple live terminals remain independently owned and unchanged across navigation, with no visible tab before authoritative launch and ownership success.</done>
</task>

</tasks>

<verification>Run `bun run native:test:restore`, `bun run native:test:attention`, `bun run native:test:tabs`, `bun run native:test:quick`, and `bun run typecheck`.</verification>
<success_criteria>Core and registry behavior fully represent pair-bound tabs, truthful restoration, derived attention, explicit focus routing, and navigation-independent live terminal ownership.</success_criteria>
<output>Create `.planning/phases/106-linux-workspace-commands-and-attention/106-02-SUMMARY.md` when done.</output>
