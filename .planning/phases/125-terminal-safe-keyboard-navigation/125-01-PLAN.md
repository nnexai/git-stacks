---
phase: 125-terminal-safe-keyboard-navigation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/protocol/src/web.ts
  - tests/service/web-shortcut-contract.test.ts
autonomous: true
requirements: [KEY-01, KEY-02, KEY-05, KEY-06]
must_haves:
  truths:
    - "One browser-safe protocol contract defines every action, platform, physical binding, primary override, alias, reset, and unbind intent used downstream."
    - "Safe platform defaults and optional familiar aliases are representable without duplicating stable action metadata across packages."
    - "All shortcut request and response shapes are strict, bounded, and disclose no global config or host detail."
  artifacts:
    - path: packages/protocol/src/web.ts
      provides: "Canonical shortcut action/platform/binding/settings/mutation schemas and exported types"
    - path: tests/service/web-shortcut-contract.test.ts
      provides: "Executable schema and action/default inventory contract"
  key_links:
    - from: packages/client/src/shortcuts.ts
      to: packages/protocol/src/web.ts
      via: "Plan 03 imports the same canonical types and inventory"
    - from: packages/service/src/secure/router.ts
      to: packages/protocol/src/web.ts
      via: "Plan 02 maps strict transport requests to transport-independent core intents"
---

<objective>
Land the canonical shortcut data contract before the two parallel foundations.

Purpose: Authority and client matching must not invent competing binding models or stable action registries.
Output: Strict protocol schemas/types plus a contract suite that freezes the eight-action inventory and alias/reset/unbind representation.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/125-terminal-safe-keyboard-navigation/125-CONTEXT.md
@.planning/phases/125-terminal-safe-keyboard-navigation/125-RESEARCH.md
@packages/protocol/src/web.ts

<interfaces>
`packages/protocol/src/web.ts` already owns strict browser-safe schemas. Keep this plan data-only: it defines no persistence, matcher, DOM, or service routing behavior.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the canonical action, platform, binding, and effective settings schemas</name>
  <files>packages/protocol/src/web.ts, tests/service/web-shortcut-contract.test.ts</files>
  <behavior>
    - Exactly eight stable action IDs and exactly macOS/Linux platforms parse; unknown values fail.
    - A normalized binding is a physical `KeyA`-`KeyZ` code plus explicit ctrl/alt/shift/meta booleans; unknown fields and malformed codes fail.
    - Effective settings carry one primary binding or null plus a bounded alias list for every action and a shortcut-specific revision.
    - The full response cannot carry config, paths, command text, environment, identity, or workspace definitions.
  </behavior>
  <action>Define and export strict Zod schemas/types for platform, the eight `workspace.switch`, `commands.open`, `workspace.new`, `terminal.new`, `terminal.close`, `terminal.previous`, `terminal.next`, and `attention.next` IDs, normalized physical bindings, per-action effective primary-plus-aliases, and complete settings. Bound aliases to a small maximum and require unique action rows. Keep display labels and defaults out of transport so client presentation can evolve without persisting derived data. This is the canonical browser transport contract, not a dependency for the core leaf package; service maps between the two tiers.</action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/service/web-shortcut-contract.test.ts</automated>
  </verify>
  <done>The protocol exports one strict data contract capable of representing safe defaults, additional aliases, and complete effective state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define distinct set-primary, set-aliases, unbind, and reset mutation intents</name>
  <files>packages/protocol/src/web.ts, tests/service/web-shortcut-contract.test.ts</files>
  <behavior>
    - `set-primary` carries one binding; `set-aliases` carries a bounded alias list; `unbind` and `reset` carry neither.
    - Every mutation carries platform, action ID, and expected shortcut revision.
    - Reset is distinguishable from unbind: reset restores default primary and removes aliases; unbind makes primary null and removes aliases.
    - Strict parsing rejects hybrid, unknown, duplicate, and oversized bodies.
  </behavior>
  <action>Add a strict discriminated mutation schema and get request schema. Encode primary replacement, alias-list replacement, explicit unbind, and reset-to-platform-default as separate intents. Add schema tests proving they cannot be confused and that malicious extra global-config fields fail before any downstream authority could run.</action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/service/web-shortcut-contract.test.ts && npm run typecheck --workspace @git-stacks/protocol</automated>
  </verify>
  <done>Downstream plans can implement aliases and reset/unbind without type invention or semantic ambiguity.</done>
</task>

</tasks>

<verification>
Run the focused schema suite and protocol typecheck. Inspect exports to ensure no default registry or persistence implementation is duplicated here.
</verification>

<success_criteria>
- One strict protocol vocabulary is available before parallel Plans 02 and 03.
- Primary bindings, additional aliases, reset, and unbind are all distinct.
- The transport remains bounded and host-detail-free.
</success_criteria>

<output>
Create `.planning/phases/125-terminal-safe-keyboard-navigation/125-01-SUMMARY.md` when done.
</output>
