---
phase: 125-terminal-safe-keyboard-navigation
plan: "05"
type: execute
wave: 4
depends_on: ["125-02", "125-03", "125-04"]
files_modified:
  - packages/web/src/app.ts
  - packages/web/src/app.css
  - tests/service/web-keyboard-overlays.test.ts
autonomous: true
requirements: [KEY-01, KEY-06, KEY-07, KEY-08, KEY-09, KEY-10, ATTN-03]
must_haves:
  truths:
    - "Workspace and configured-command overlays share fuzzy top-partial selection inside one focus-contained singleton controller."
    - "Keyboard help and shortcut settings always show authoritative current bindings and reject conflicts without replacing prior state."
    - "Closing/replacing overlays never emits palette keys to the PTY and restores the original terminal or a safe fallback."
    - "Next Attention and Keyboard Shortcuts remain visible and accessible at narrow widths."
  artifacts:
    - path: packages/web/src/app.ts
      provides: "Singleton overlays, fuzzy switcher/commands, help, settings, capture, focus containment, and toolbar controls"
    - path: packages/web/src/app.css
      provides: "UI-SPEC conformant result/settings/key-cap/responsive states"
    - path: tests/service/web-keyboard-overlays.test.ts
      provides: "DOM harness evidence for overlay, focus, settings, and discoverability behavior"
  key_links:
    - from: packages/web/src/app.ts
      to: packages/web/src/navigation.ts
      via: "overlay/help/settings callbacks and pointer controls resolve the same canonical action entries"
    - from: packages/web/src/app.ts
      to: shortcuts.set
      via: "expected shortcut revision with complete response replacement after authoritative success"
---

<objective>
Complete the user-facing singleton fuzzy overlays, keyboard help, authoritative shortcut customization, focus safety, and discoverability UI.

Purpose: Make all keyboard behavior usable and explainable without allowing modal stacking, stale bindings, or terminal keystroke leakage.
Output: UI-SPEC-complete app/CSS implementation and executable DOM/browser-harness coverage.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-terminal-safe-keyboard-navigation/125-CONTEXT.md
@.planning/phases/125-terminal-safe-keyboard-navigation/125-UI-SPEC.md
@.planning/phases/125-terminal-safe-keyboard-navigation/125-04-SUMMARY.md
@packages/web/src/app.ts
@packages/web/src/app.css

<interfaces>
Extend the existing `modal()` seam rather than adding a second framework. Reuse `selectPair()`, `createTerminal(command.id)`, terminal `focusInput()`, `toast()`, and the canonical action registry. Search input retains DOM focus with combobox/listbox/option and aria-activedescendant semantics.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade one modal controller and deliver both shared fuzzy overlays</name>
  <files>packages/web/src/app.ts, tests/service/web-keyboard-overlays.test.ts</files>
  <behavior>
    - Reinvoking the same surface refocuses it; invoking another replaces content with one backdrop/handler and preserves the original terminal return target.
    - Workspace rows include only active workspace/repository pairs; partial Enter selects the top fuzzy row.
    - Commands remain selected-pair configured commands only and partial Enter runs the top result through createTerminal(command.id).
    - ArrowUp/Down wrap, Home/End jump, Enter/Escape are contained, Tab cycles enabled controls, and close restores terminal/fallback focus.
  </behavior>
  <action>Evolve `modal()` into one named singleton controller used by new workspace switcher, configured commands, help/settings, capture, and existing confirmations/editors. Preserve the original terminal focus target across replacements; make confirmation/editor surfaces exclusive to unrelated actions. Implement both searchable overlays using Plan 03's shared scorer/navigation, deterministic empty ordering, active-descendant semantics, pointer/keyboard active-row synchronization, exact UI-SPEC empty/no-match copy, and top partial Enter. Workspace input must use only active projected pairs; command input stays selected-pair scoped. Consume all overlay navigation/close keys so none reaches PTY.</action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/service/web-keyboard-overlays.test.ts</automated>
  </verify>
  <done>A DOM harness proves singleton/refocus/replacement, one backdrop/listener, active-result semantics, partial Enter, focus trap/restore, archived exclusion, and command scoping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add authoritative keyboard help and conflict-safe shortcut capture</name>
  <files>packages/web/src/app.ts, tests/service/web-keyboard-overlays.test.ts</files>
  <behavior>
    - Help groups all eight actions under Navigation/Workspace/Terminal and shows current effective primary plus aliases or Unbound.
    - Settings loads authoritative data without flashing defaults, supports primary capture, add/remove alias, reset, and unbind with distinct intents and expected revision.
    - Unsafe/composition/AltGraph/pure-modifier input does not save; conflict names the other action and preserves both bindings.
    - Save updates caps only from the successful complete service response; failure restores prior state and offers retry.
  </behavior>
  <action>Implement `Keyboard shortcuts` help and `Customize shortcuts` settings inside the singleton controller following the exact UI-SPEC groups, labels, current-platform modifier names, primary/alias key caps, loading/error/busy/capture copy, and focus behavior. Refresh through `shortcuts.get` on settings open. Capture either the primary or a new alias; expose removal per alias. Ignore pure modifiers/composition/AltGraph, validate safe modifiers and conflicts across all primaries/aliases locally, and keep capture active on conflict. Mutate only via the distinct Plan 01 intents with expected revision; replace registry/help/settings state only with the complete successful response. Reset restores the safe primary and removes aliases; Unbind clears both. Never silently swap another action or persist browser state.</action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/service/web-keyboard-overlays.test.ts tests/service/web-shortcut-authority.test.ts</automated>
  </verify>
  <done>Help/settings/capture reflect authoritative bindings across success, conflict, stale/failure, reset, unbind, alias, loading, and retry states.</done>
</task>

<task type="auto">
  <name>Task 3: Finish toolbar discoverability, responsive/accessibility styling, and phase gates</name>
  <files>packages/web/src/app.ts, packages/web/src/app.css, tests/service/web-keyboard-overlays.test.ts</files>
  <action>Following the approved UI-SPEC, add visible `Next attention` and `Keyboard shortcuts` toolbar controls that invoke the same registry callbacks; keep both present as 32px labelled/title controls at 640px and below. Implement existing-token styles for active listbox rows, result metadata, grouped help/settings rows, `<kbd>` caps, inline conflict/error/busy/capture states, contained modal scrolling, truncation/wrapping, and the existing 2px focus-visible/reduced-motion/theme rules. Add accessible dialog/combobox/listbox/option/live-region labels and result-count announcements. Cover zero/one/many, overflow, long text, unavailable/loading/error/partial states in the executable harness; then run the full Phase 125 focused and repository gates.</action>
  <verify>
    <automated>GIT_STACKS_KEY_STORE=file ./node_modules/.bin/vitest run tests/lib/web-shortcut-config.test.ts tests/lib/client-shortcuts.test.ts tests/lib/client-fuzzy.test.ts tests/lib/client-attention.test.ts tests/service/web-shortcut-authority.test.ts tests/service/web-keyboard-navigation.test.ts tests/service/web-keyboard-overlays.test.ts tests/service/web-presentation.test.ts && npm run web:build && npm run typecheck && npm run test:deps && npm run verify:gates</automated>
  </verify>
  <done>The approved UI contract is implemented with visible discoverability, all automated Phase 125 behavior is green, and no automated result is represented as live human browser approval.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Overlay/capture input to app action | User key input must remain contained and cannot silently alter another binding. |
| Settings UI to authoritative service | Stale or failed writes must not become effective client state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-125-10 | Tampering | binding capture/settings | high | mitigate | Local complete conflict validation plus expected service revision and response-only state replacement. |
| T-125-11 | Elevation of privilege | exclusive modal/action availability | medium | mitigate | One controller disables unrelated actions during confirmation/editor/capture and contains all navigation keys. |
| T-125-12 | Denial of service | overlay key repeat/listeners | medium | mitigate | Same-surface refocus, replacement in one controller, one containment listener, and repeat suppression. |
| T-125-SC | Tampering | UI dependency supply chain | low | accept | UI uses existing native DOM/CSS/xterm stack; no component registry or package install enters the phase. |
</threat_model>

<verification>
Run all focused authority/client/web tests, production web build, seven workspace typechecks, dependency/cycle gate, and repository verify gates. Inspect the built UI through automated harness evidence only.
</verification>

<success_criteria>
- Workspace/command search, help, settings, capture, and existing modal surfaces share one controller and focus contract.
- Help/settings always reflect current authoritative bindings and conflict-safe mutation results.
- Visible toolbar actions and responsive/accessibility states match 125-UI-SPEC.md.
- Phase 127 retains live xterm focus, physical-keyboard, shell/TUI pass-through, and screenshot comparison before tag; this plan stops at automated readiness.
</success_criteria>

<output>
Create `.planning/phases/125-terminal-safe-keyboard-navigation/125-05-SUMMARY.md` when done.
</output>
