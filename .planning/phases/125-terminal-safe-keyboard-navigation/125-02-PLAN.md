---
phase: 125-terminal-safe-keyboard-navigation
plan: "02"
type: execute
wave: 2
depends_on: ["125-01"]
files_modified:
  - packages/core/src/config.ts
  - packages/core/src/web-shortcuts.ts
  - packages/service/src/secure/router.ts
  - packages/service/src/main.ts
  - tests/lib/web-shortcut-config.test.ts
  - tests/service/web-shortcut-authority.test.ts
  - tests/service/web-shortcut-contract.test.ts
autonomous: true
requirements: [KEY-01, KEY-02, KEY-05, KEY-06]
must_haves:
  truths:
    - "Shortcut overrides and aliases are authoritative global configuration, never durable browser state or workspace YAML."
    - "Every action supports safe default, primary rebind, bounded aliases, explicit unbind, and reset; conflicts across every effective chord fail atomically."
    - "Browser clients read and mutate only bounded effective settings through scoped methods and shortcut-specific optimistic concurrency."
  artifacts:
    - path: packages/core/src/web-shortcuts.ts
      provides: "Core-owned defaults, effective settings, validation, revision, read, and atomic mutation authority"
    - path: packages/service/src/secure/router.ts
      provides: "Scoped shortcuts.get and shortcuts.set routing"
    - path: tests/service/web-shortcut-contract.test.ts
      provides: "Protocol/core default and inventory agreement"
  key_links:
    - from: packages/service/src/secure/router.ts
      to: packages/core/src/web-shortcuts.ts
      via: "explicitly maps protocol requests to narrow core intents composed in service main"
    - from: packages/service/src/secure/router.ts
      to: packages/protocol/src/web.ts
      via: "strict browser parsing and response projection; core never imports protocol"
---

<objective>
Implement authoritative shortcut persistence and the path-minimizing secure service methods on the canonical Plan 01 contract.

Purpose: Rebind/unbind/reset/alias state must survive browser restart without browser-local durability or duplicate contract ownership.
Output: Core global-config authority, service routing/composition, and focused authority/contract tests.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-terminal-safe-keyboard-navigation/125-CONTEXT.md
@.planning/phases/125-terminal-safe-keyboard-navigation/125-RESEARCH.md
@.planning/phases/125-terminal-safe-keyboard-navigation/125-01-SUMMARY.md
@packages/core/src/config.ts
@packages/protocol/src/web.ts
@packages/service/src/secure/router.ts
@packages/service/src/main.ts

<interfaces>
Core is a leaf and must not import Plan 01 or `@git-stacks/protocol`. Define transport-independent domain constants/types in core. `updateGlobalConfig()` already supplies the leased atomic mutation boundary. Service may import both tiers, maps protocol bodies/results explicitly, receives narrow capabilities, and never reads or writes YAML itself.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add strict global overrides and atomic effective-setting authority</name>
  <files>packages/core/src/config.ts, packages/core/src/web-shortcuts.ts, tests/lib/web-shortcut-config.test.ts, tests/service/web-shortcut-contract.test.ts</files>
  <behavior>
    - Missing entries derive all eight safe macOS/Linux primaries with no aliases.
    - Stored overrides can set primary, replace aliases, unbind all, or reset by removing the action entry.
    - Optional Ctrl+K/Ctrl+Shift+P aliases coexist with safe primaries; conflicts across all primary and alias chords reject the complete write.
    - Stale revision, invalid/unsafe chord, unknown action, or duplicate effective chord leaves config byte-equivalent and preserves unrelated fields.
  </behavior>
  <action>Extend `GlobalConfigSchema` with strict optional platform/action shortcut overrides containing optional primary (binding or null) and bounded aliases. Implement transport-independent core action/platform/binding/intent constants and types, defaults, effective derivation, complete-registry conflict/safety validation, deterministic shortcut-subdocument fingerprint, read, and the four mutation intents in `web-shortcuts.ts`. Do not import protocol. Run read/recheck/validate/write inside `updateGlobalConfig()` and preserve unrelated config. Reset deletes the action override; unbind stores primary null and clears aliases. Add an agreement test proving protocol action inventory and core defaults share the same literal IDs; the downstream client agreement is added after both parallel foundations merge.</action>
  <verify>
    <automated>GIT_STACKS_KEY_STORE=file ./node_modules/.bin/vitest run tests/lib/web-shortcut-config.test.ts tests/service/web-shortcut-contract.test.ts</automated>
  </verify>
  <done>All persistence modes round-trip distinctly and every effective-chord conflict is an atomic no-write failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose and compose strict scoped settings methods</name>
  <files>packages/service/src/secure/router.ts, packages/service/src/main.ts, tests/service/web-shortcut-authority.test.ts</files>
  <behavior>
    - `shortcuts.get` returns exactly the complete effective Plan 01 settings response.
    - `shortcuts.set` accepts only the strict mutation union and returns the complete replacement response.
    - Read/write scopes, unavailable capability, stale revision, invalid request, and non-disclosure are executable.
  </behavior>
  <action>Add `shortcuts.get` under `snapshot.read` and `shortcuts.set` under `operation.write`. Parse Plan 01 schemas, explicitly map transport values to core intents, delegate to injected read/update capabilities, and project core results back through the strict response schema. Map stale/conflict/validation failures to stable service error codes and never expose arbitrary GlobalConfig. Compose the core capabilities in `service/main.ts` next to pin/priority setters. Keep the daemonless CLI and web snapshot unchanged.</action>
  <verify>
    <automated>GIT_STACKS_KEY_STORE=file ./node_modules/.bin/vitest run tests/lib/web-shortcut-config.test.ts tests/service/web-shortcut-authority.test.ts tests/service/web-shortcut-contract.test.ts && npm run typecheck && npm run test:deps</automated>
  </verify>
  <done>The managed service provides durable conflict-safe shortcut settings without expanding browser authority or package direction.</done>
</task>

</tasks>

<verification>
Run focused authority/contract suites, all package typechecks, dependency gates, and non-disclosure assertions.
</verification>

<success_criteria>
- Rebind, alias, unbind, and reset persist distinctly through core authority.
- Conflict and stale writes preserve the prior complete registry.
- Browser responses contain only normalized shortcut settings.
- Live editing remains Phase 127 pre-tag UAT.
</success_criteria>

<output>
Create `.planning/phases/125-terminal-safe-keyboard-navigation/125-02-SUMMARY.md` when done.
</output>
