---
phase: 88-functional-coverage-readiness-gate
plan: 02
type: execute
wave: 2
depends_on:
  - 88-01
files_modified:
  - scripts/verify-gates.ts
  - tests/lib/verify-gates.test.ts
  - package.json
  - README.md
  - CHANGELOG.md
  - .planning/v0.17.1-FUNCTIONAL-COVERAGE-READINESS.md
autonomous: true
requirements:
  - GATE-04
  - COVR-05
must_haves:
  truths:
    - "`bun run verify:gates` includes functional readiness findings in its aggregated local gate report."
    - "`bun run verify` refreshes canonical coverage before evaluating readiness gates."
    - "Maintainer docs distinguish green suite, covered source, and functional confidence."
    - "Phase 88 does not introduce CI, numeric threshold policy, milestone archive, tag, publish, or `$gsd-complete-milestone` execution."
  artifacts:
    - path: "scripts/verify-gates.ts"
      provides: "Functional readiness gate integration"
      exports: ["collectVerifyGateReport", "formatVerifyGateReport"]
    - path: "tests/lib/verify-gates.test.ts"
      provides: "Regression tests for aggregated readiness findings"
    - path: "package.json"
      provides: "Optional `verify:functional` debug script while preserving `verify`"
    - path: "README.md"
      provides: "Maintainer-facing local functional readiness documentation"
    - path: "CHANGELOG.md"
      provides: "Scoped v0.17.1 release-readiness note"
  key_links:
    - from: "scripts/verify-gates.ts"
      to: "scripts/functional-coverage-readiness.ts"
      via: "collector import and report aggregation"
      pattern: "collectFunctionalCoverageReadiness"
    - from: "scripts/verify.ts"
      to: "bun run coverage -> bun run verify:gates"
      via: "existing verify command order"
      pattern: "bun run coverage"
    - from: "README.md"
      to: "scripts/verify-gates.ts"
      via: "documented local gate behavior"
      pattern: "functional confidence"
---

<objective>
Wire functional readiness into the local verification gate and update final pre-release evidence docs.

Purpose: make the Phase 88 readiness result enforceable by existing local scripts while preserving the no-CI, no-threshold, no-finalization milestone boundary.
Output: gate integration, tests, script/docs updates, and final local verification proof.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/88-functional-coverage-readiness-gate/88-CONTEXT.md
@.planning/phases/88-functional-coverage-readiness-gate/88-RESEARCH.md
@.planning/phases/88-functional-coverage-readiness-gate/88-PATTERNS.md
@.planning/phases/88-functional-coverage-readiness-gate/88-VALIDATION.md
@.planning/phases/88-functional-coverage-readiness-gate/88-01-PLAN.md
@scripts/functional-coverage-readiness.ts
@tests/functional-readiness-inventory.ts
@scripts/verify-gates.ts
@scripts/verify.ts
@tests/lib/verify-gates.test.ts
@package.json
@README.md
@CHANGELOG.md

<interfaces>
From `scripts/verify.ts`:
```typescript
export type RunCommand = (command: string) => Promise<number>
export type VerifyWorkflowOptions = {
  runCommand?: RunCommand
  log?: (message: string) => void
}
export function getVerifyCommands(): string[]
export async function runVerifyWorkflow(options?: VerifyWorkflowOptions): Promise<number>
```

From `scripts/verify-gates.ts` before Phase 88:
```typescript
export function collectVerifyGateReport(options?: CollectOptions): VerifyGateReport
export function formatVerifyGateReport(report: VerifyGateReport): string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add functional readiness to the aggregated local gate</name>
  <read_first>
    <file>scripts/verify-gates.ts</file>
    <file>scripts/verify.ts</file>
    <file>scripts/functional-coverage-readiness.ts</file>
    <file>tests/functional-readiness-inventory.ts</file>
    <file>tests/lib/verify-gates.test.ts</file>
    <file>package.json</file>
  </read_first>
  <files>scripts/verify-gates.ts, tests/lib/verify-gates.test.ts, package.json</files>
  <behavior>
    - Test 1: `collectVerifyGateReport` includes functional readiness must-fix findings while still returning inventory, mapped-test, artifact, and sentinel findings from the same run.
    - Test 2: `formatVerifyGateReport` prints a `Functional readiness problems:` section for must-fix readiness gaps.
    - Test 3: accepted-gap and deferred-external-environment readiness items are visible through the functional readiness report but do not make `verify:gates` fail.
    - Test 4: `package.json` keeps `verify` as the umbrella command and, if `verify:functional` is added, it calls `bun run scripts/functional-coverage-readiness.ts`.
  </behavior>
  <action>Update `scripts/verify-gates.ts` so `VerifyGateReport` includes a `functionalReadiness` section returned by `collectFunctionalCoverageReadiness({ root })`. The overall `ok` result must be false when the readiness collector reports malformed coverage input or any `must-fix-before-release` area, and true when only covered, accepted-gap, or deferred-external-environment areas remain. Extend `formatVerifyGateReport` to print grouped functional readiness problems without suppressing existing inventory, mapped-test, artifact, or coverage-sentinel output. Add fixture tests in `tests/lib/verify-gates.test.ts` using the same temp-root coverage JSON style already in that file. Optionally add `verify:functional` to `package.json` as a debugging command, but keep `verify` unchanged as the umbrella command that runs `bun run coverage` before `bun run verify:gates`. Do not add CI workflows, numeric threshold policy, fail-fast behavior, milestone archive/tag/publish commands, or `$gsd-complete-milestone` execution.</action>
  <acceptance_criteria>
    <criterion>`scripts/verify-gates.ts` imports or calls `collectFunctionalCoverageReadiness`.</criterion>
    <criterion>`VerifyGateReport` includes `functionalReadiness` and `report.ok` accounts for readiness must-fix or malformed-input findings.</criterion>
    <criterion>`formatVerifyGateReport` contains the heading `Functional readiness problems:`.</criterion>
    <criterion>`tests/lib/verify-gates.test.ts` asserts readiness findings aggregate with at least one existing gate finding.</criterion>
    <criterion>`package.json` still has `"verify": "bun run scripts/verify.ts"`.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/lib/functional-coverage-readiness.test.ts tests/lib/verify-gates.test.ts</automated>
    <automated>bun run coverage</automated>
    <automated>bun run verify:gates</automated>
  </verify>
  <done>`verify:gates` enforces functional readiness through the existing aggregated local gate path.</done>
</task>

<task type="auto">
  <name>Task 2: Update maintainer docs and run final local readiness verification</name>
  <read_first>
    <file>README.md</file>
    <file>CHANGELOG.md</file>
    <file>.planning/v0.17.1-FUNCTIONAL-COVERAGE-READINESS.md</file>
    <file>scripts/verify.ts</file>
    <file>scripts/verify-gates.ts</file>
    <file>package.json</file>
  </read_first>
  <files>README.md, CHANGELOG.md, .planning/v0.17.1-FUNCTIONAL-COVERAGE-READINESS.md</files>
  <action>Update the existing README local verification section so it explains that `bun run verify` refreshes canonical coverage, runs `verify:gates`, and now checks functional readiness. Use the exact terms `green suite`, `covered source`, and `functional confidence`, and explain that Phase 88 uses targeted readiness sentinels rather than numeric coverage thresholds per COVR-05. Update the top v0.17.1 changelog notes with a scoped functional-readiness bullet that mentions accepted gaps, deferred external-environment coverage, and must-fix-before-release gaps. Refresh `.planning/v0.17.1-FUNCTIONAL-COVERAGE-READINESS.md` after the gate wiring so its command list and categories match the implemented script. Do not modify `.planning/milestones/`, do not delete `.planning/REQUIREMENTS.md`, do not bump package version, do not create tags, do not publish, and do not run `$gsd-complete-milestone`.</action>
  <acceptance_criteria>
    <criterion>`README.md` contains `green suite`, `covered source`, `functional confidence`, and `bun run verify:gates` in the local verification section.</criterion>
    <criterion>`README.md` states that Phase 88 does not add numeric coverage thresholds.</criterion>
    <criterion>`CHANGELOG.md` contains `Functional readiness` and `must-fix-before-release` in the v0.17.1 entry.</criterion>
    <criterion>`.planning/v0.17.1-FUNCTIONAL-COVERAGE-READINESS.md` matches the implemented gate command names and retains the no archive/tag/publish/finalization boundary.</criterion>
    <criterion>`bun run verify` exits 0 after the docs and gate wiring.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>grep -n "functional confidence" README.md</automated>
    <automated>grep -n "must-fix-before-release" CHANGELOG.md .planning/v0.17.1-FUNCTIONAL-COVERAGE-READINESS.md</automated>
    <automated>bun run verify</automated>
  </verify>
  <done>The final pre-finalization docs and local gate output provide release-readiness evidence without executing milestone lifecycle work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| readiness collector -> `verify:gates` | New readiness findings must compose with existing local gate findings without hiding failures. |
| local gate output -> maintainer decision | Maintainers rely on gate wording to separate passing tests, source coverage, and functional confidence. |
| docs -> milestone lifecycle | Release-readiness docs must not become archive/tag/publish instructions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-04 | T | `scripts/verify-gates.ts` | mitigate | Include functional readiness in `VerifyGateReport.ok` and add regression tests for aggregate failure output. |
| T-88-05 | R | `README.md`, `CHANGELOG.md` | mitigate | Use exact readiness terms and command names so future agents cannot equate green tests with functional confidence. |
| T-88-06 | E | milestone finalization boundary | mitigate | Keep archive/tag/publish and `$gsd-complete-milestone` explicitly prohibited in actions and acceptance criteria. |
</threat_model>

<verification>
- `bun test tests/lib/functional-coverage-readiness.test.ts tests/lib/verify-gates.test.ts`
- `bun run coverage`
- `bun run verify:gates`
- `bun run verify`
</verification>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | - | Maintainers can decide whether v0.17.1 is ready using functional coverage evidence, explicit accepted gaps, and local gates | 01, 02 | COVERED | Plan 01 creates evidence/model; Plan 02 wires gates/docs.
REQ | GATE-04 | Functional coverage readiness gate distinguishes green tests, covered source, accepted gaps, deferred external-environment coverage, and must-fix-before-release gaps | 01, 02 | COVERED | Inventory/model in Plan 01, verify:gates integration in Plan 02.
REQ | COVR-05 | Future numeric thresholds may be enforced after coverage source stabilizes | 01, 02 | COVERED | Plans document targeted sentinels now and no numeric threshold policy in Phase 88.
RESEARCH | canonical coverage artifact | Use `bun run coverage` / `.coverage/coverage-final.json` as the source artifact | 01, 02 | COVERED | Collector reads canonical JSON; verify path runs coverage before gates.
RESEARCH | aggregated local gates | Extend `scripts/verify-gates.ts` without fail-fast behavior | 02 | COVERED | Plan 02 Task 1 integrates readiness into aggregate report.
RESEARCH | no finalization | Do not archive/tag/publish or run milestone lifecycle commands | 01, 02 | COVERED | Both plans prohibit finalization in task actions.
CONTEXT | Readiness Evidence | Produce/document functional-only coverage view and classify uncovered areas | 01 | COVERED | Plan 01 creates inventory, collector, and evidence document.
CONTEXT | Gate Strategy | Prefer targeted sentinel gates, script-driven local verification, aggregate findings | 01, 02 | COVERED | Collector uses sentinels; Plan 02 integrates aggregate gate output.
CONTEXT | Scope Boundary | Exclude broad TUI, real external integrations, CI, archive/tag/publish/finalization | 01, 02 | COVERED | Exclusions are explicit in inventory/evidence and task actions.
</source_audit>

<success_criteria>
- Functional readiness findings are part of `verify:gates`.
- `bun run verify` still refreshes canonical coverage before gates.
- README, CHANGELOG, and readiness evidence distinguish green suite, covered source, and functional confidence.
- No milestone lifecycle/finalization artifact or command is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/88-functional-coverage-readiness-gate/88-02-SUMMARY.md`
</output>
