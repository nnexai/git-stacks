---
phase: 86-workspace-command-workflow-edge-coverage
plan: 03
type: execute
wave: 2
depends_on:
  - 86-01
  - 86-02
files_modified:
  - tests/commands/workspace-wrapper-edges.test.ts
  - tests/e2e-inventory.ts
autonomous: true
requirements:
  - CMD-01
  - CMD-02
  - CMD-03
  - CMD-04
  - GATE-03
must_haves:
  truths:
    - "`run`, `paths`, `env`, `status`, `sync`, `push`, and `pull` wrappers have focused real CLI coverage for meaningful option interactions, JSON contracts, cwd detection, and no-op/error branches."
    - "The canonical E2E inventory maps all Phase 86 command-workflow coverage to concrete test files."
    - "`bun run verify:gates` and `bun run verify` remain the local proof path for GATE-03."
  artifacts:
    - path: "tests/commands/workspace-wrapper-edges.test.ts"
      provides: "focused wrapper command edge coverage"
    - path: "tests/e2e-inventory.ts"
      provides: "canonical E2E inventory mappings for Phase 86 surfaces"
  key_links:
    - from: "src/commands/workspace.ts"
      to: "tests/helpers.ts"
      via: "executor will exercise wrapper command wiring through existing `runCli` helpers"
      pattern: "run|paths|env|status|sync|push|pull"
    - from: "tests/e2e-inventory.ts"
      to: "src/commands/workspace.ts"
      via: "executor will extend existing workspace git-operation inventory mapping with Phase 86 tests"
      pattern: "workspace.git-operations"
    - from: "tests/e2e-inventory.ts"
      to: "src/commands/workspace.ts"
      via: "executor will extend existing workspace run inventory mapping with wrapper edge tests"
      pattern: "workspace.run"
    - from: "tests/e2e-inventory.ts"
      to: "src/commands/workspace.ts"
      via: "executor will extend existing workspace paths/env/status inventory mappings with wrapper edge tests"
      pattern: "workspace.paths|workspace.env|workspace.status"
---

<objective>
Add wrapper-command edge coverage and align the canonical E2E inventory with the Phase 86 test suite.

Purpose: CMD-04 requires stable command-wrapper coverage, while GATE-03 requires local gates and inventory mappings to remain trustworthy after Phase 86 expands command coverage.
Output: `workspace-wrapper-edges.test.ts` and updated `tests/e2e-inventory.ts`.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/86-workspace-command-workflow-edge-coverage/86-CONTEXT.md
@.planning/phases/86-workspace-command-workflow-edge-coverage/86-RESEARCH.md
@.planning/phases/86-workspace-command-workflow-edge-coverage/86-PATTERNS.md
@.planning/phases/86-workspace-command-workflow-edge-coverage/86-VALIDATION.md
@.planning/phases/81-workspace-and-git-operation-e2e-coverage/81-02-SUMMARY.md
@.planning/phases/81-workspace-and-git-operation-e2e-coverage/81-04-SUMMARY.md
@.planning/phases/82.1-support-commands-and-error-path-e2e-coverage/82.1-03-SUMMARY.md
@tests/helpers.ts
@tests/e2e-inventory.ts
@tests/commands/workspace-json-contracts.test.ts
@tests/commands/workspace-execution-context.test.ts
@tests/commands/workspace-git-ops.test.ts
@src/commands/workspace.ts

<interfaces>
From `tests/helpers.ts`:
- `runCli(argv: string[], opts: RunCliOptions): RunCliResult`
- `formatCliFailure(result: RunCliResult): string`
- `makeRepoWithRemote(baseDir: string, repoName?: string, wsBranch?: string): RepoFixture`
- `makeWorkspaceFixture(configDir: string, wsName: string, repos: RepoFixture[], opts?: object): object`
- `gitExecOptions(cwd: string, baseDir?: string): ExecSyncOptions`
- `cleanup(dir: string): void`

From `tests/e2e-inventory.ts`:
- `E2E_INVENTORY: readonly E2EInventoryItem[]`
- Workspace entries already include `workspace.status`, `workspace.run`, `workspace.paths`, `workspace.env`, and `workspace.git-operations`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cover wrapper command edges</name>
  <files>tests/commands/workspace-wrapper-edges.test.ts</files>
  <behavior>
    - `run` reports option conflicts and emits parseable JSON for mixed success/failure without spinner assertions.
    - `paths` supports cwd detection and reports no-path/filter-empty branches with nonzero exit.
    - `env` supports cwd detection, `--repo`, and JSON output.
    - `status` preserves JSON/cwd/fetch contracts not already covered by earlier Phase 81 files.
    - `sync`, `push`, and `pull` cover no-op and representative error branches through local remotes.
  </behavior>
  <action>Create `tests/commands/workspace-wrapper-edges.test.ts`. Reuse `runCli`, explicit `cwd`, local remote helpers, and workspace fixtures. Select focused cases that materially exercise command-wrapper parsing and dispatch beyond existing Phase 81 coverage. Parse JSON from stdout where JSON is the contract; for commands that can print progress before JSON, extract the JSON payload rather than snapshotting full output. Assert no-op/error branches through exit code, structured result shape, and short safety-critical text only.</action>
  <verify>
    <automated>bun test tests/commands/workspace-wrapper-edges.test.ts</automated>
  </verify>
  <done>CMD-04 wrapper edges are covered through real CLI subprocesses with stable JSON/cwd/no-op/error assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Map Phase 86 tests in the E2E inventory and run gates</name>
  <files>tests/e2e-inventory.ts</files>
  <behavior>
    - `workspace.git-operations` maps the new recreate, clean-gone, destructive-safety, and wrapper-edge tests where relevant.
    - `workspace.status`, `workspace.run`, `workspace.paths`, and `workspace.env` map the new wrapper-edge coverage where relevant.
    - Existing exclusions for prompts, TUI behavior, editor-launching flows, and external environments remain explicit.
    - Local gates accept the expanded mappings.
  </behavior>
  <action>Update `tests/e2e-inventory.ts` after Plans 01 and 02 have created their files. Add the Phase 86 test files to the relevant existing workspace inventory entries instead of creating duplicate inventory items for the same command family. Keep rationale text accurate: the new files cover edge workflows and safety contracts, while earlier Phase 81 files continue to cover baseline happy paths. Do not mark wizard-driven, TUI, editor-launching, or external environment flows in scope.</action>
  <verify>
    <automated>bun test tests/commands/workspace-wrapper-edges.test.ts && bun run verify:gates && bun run verify</automated>
  </verify>
  <done>Inventory mappings include every Phase 86 test file, excluded boundaries remain explicit, and local verification gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> wrapper dispatch | Untrusted command options control subprocess execution, JSON mode, cwd detection, and git operations. |
| Workspace cwd -> workspace detection | Current working directory can implicitly select workspace/repo context. |
| Inventory -> local gates | Inventory mappings determine whether local verification flags missing command coverage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-07 | Tampering | wrapper command option parsing | mitigate | Tests assert option conflicts, no-op branches, and error branches do not silently succeed. |
| T-86-08 | Spoofing | cwd-based workspace detection | mitigate | Tests run wrappers from explicit workspace/repo cwd and from outside cwd to prove correct detection/failure. |
| T-86-09 | Repudiation | E2E inventory and local gates | mitigate | Inventory mappings are updated and `bun run verify:gates` proves local gate acceptance. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Status | Plan coverage |
|--------|------|--------|---------------|
| GOAL | Stable workspace command workflows have behavior coverage where CLI wiring matters | COVERED | Task 1 wrapper tests plus inventory/gate alignment. |
| REQ | CMD-01 | COVERED | Task 2 maps Plan 01 coverage. |
| REQ | CMD-02 | COVERED | Task 2 maps Plan 02 clean-gone coverage. |
| REQ | CMD-03 | COVERED | Task 2 maps Plan 02 destructive safety coverage. |
| REQ | CMD-04 | COVERED | Task 1 and Task 2. |
| REQ | GATE-03 | COVERED | Task 2 runs `verify:gates` and `verify`. |
| RESEARCH | Wrapper coverage should target option interactions, JSON contracts, cwd detection, and no-op/error branches | COVERED | Task 1. |
| CONTEXT | Wrapper coverage for `run`, `paths`, `env`, `status`, `sync`, `push`, and `pull` | COVERED | Task 1. |
| CONTEXT | Update only stable machine-readable/safety-critical contracts and avoid brittle prompt/spinner assertions | COVERED | Task actions. |
| CONTEXT | Keep Phase 87 integration contracts and Phase 88 readiness gates out of Phase 86 | COVERED | Task 2 keeps inventory exclusions explicit and does not add external environment scope. |
</source_audit>

<verification>
Run `bun test tests/commands/workspace-wrapper-edges.test.ts` after Task 1. After inventory updates, run `bun run verify:gates` and `bun run verify`.
</verification>

<success_criteria>
CMD-04 wrapper behavior is covered by real subprocess tests, every Phase 86 command test file is mapped in `tests/e2e-inventory.ts`, and canonical local gates pass.
</success_criteria>

<output>
After completion, create `.planning/phases/86-workspace-command-workflow-edge-coverage/86-03-SUMMARY.md`.
</output>
