---
phase: 86-workspace-command-workflow-edge-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/commands/workspace-recreate.test.ts
autonomous: true
requirements:
  - CMD-01
  - GATE-03
must_haves:
  truths:
    - "`open --recreate` has real CLI subprocess coverage for template-backed no-change, repo drift, configuration drift, missing-template, workspace-without-template, and automation-safe force/cancel behavior."
    - "Tests assert durable workspace YAML and filesystem state instead of prompt wording, spinner wording, or broad stdout snapshots."
    - "Coverage executes real `src/commands/workspace.ts` command wiring through `runCli`, not copied implementation logic."
  artifacts:
    - path: "tests/commands/workspace-recreate.test.ts"
      provides: "`open --recreate` command edge coverage"
  key_links:
    - from: "src/commands/workspace.ts"
      to: "tests/helpers.ts"
      via: "executor will exercise existing `open --recreate` command wiring through `runCli`"
      pattern: "--recreate"
    - from: "tests/helpers.ts"
      to: "src/commands/workspace.ts"
      via: "executor will use existing CLI/YAML fixture helpers for recreate command tests"
      pattern: "runCli|writeTemplateFixture|writeWorkspaceFixture"
---

<objective>
Add focused `open --recreate` coverage for template-backed workspace update workflows.

Purpose: CMD-01 requires stable user-facing coverage for recreate behavior where command wiring, template lookup, force/cancel safety, and workspace YAML mutation matter.
Output: `tests/commands/workspace-recreate.test.ts` with real subprocess tests and durable state assertions.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/86-workspace-command-workflow-edge-coverage/86-CONTEXT.md
@.planning/phases/86-workspace-command-workflow-edge-coverage/86-RESEARCH.md
@.planning/phases/86-workspace-command-workflow-edge-coverage/86-PATTERNS.md
@.planning/phases/86-workspace-command-workflow-edge-coverage/86-VALIDATION.md
@.planning/phases/81-workspace-and-git-operation-e2e-coverage/81-01-SUMMARY.md
@.planning/phases/81-workspace-and-git-operation-e2e-coverage/81-03-SUMMARY.md
@tests/helpers.ts
@tests/commands/workspace-create-clone.test.ts
@tests/commands/workspace-lifecycle.test.ts
@src/commands/workspace.ts

<interfaces>
From `tests/helpers.ts`:
- `runCli(argv: string[], opts: RunCliOptions): RunCliResult`
- `formatCliFailure(result: RunCliResult): string`
- `createConfigFixture(baseDir: string, workspaceRoot?: string): string`
- `writeRegistryFixture(configDir: string, yaml?: string): string`
- `writeTemplateFixture(configDir: string, fileName: string, yaml: string): string`
- `writeWorkspaceFixture(configDir: string, fileName: string, yaml: string): string`
- `makeRepoWithRemote(baseDir: string, repoName?: string, wsBranch?: string): RepoFixture`
- `cleanup(dir: string): void`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cover recreate refusal and no-change paths</name>
  <files>tests/commands/workspace-recreate.test.ts</files>
  <behavior>
    - Missing template exits nonzero, reports a safety-critical missing-template message, and leaves workspace YAML unchanged.
    - Workspace without a `template` field exits nonzero and leaves workspace YAML unchanged.
    - Template-backed no-change recreate succeeds without mutating workspace YAML.
    - Non-force recreate with detected drift does not mutate workspace YAML in a non-interactive subprocess.
  </behavior>
  <action>Create `tests/commands/workspace-recreate.test.ts` using `bun:test`, `makeTmpDir`, `cleanup`, `createConfigFixture`, `writeRegistryFixture`, `writeTemplateFixture`, `writeWorkspaceFixture`, `runCli`, and `formatCliFailure`. Build fixtures with real local repo paths where repo lookup is required. Assert exit codes, short safety-critical stderr/stdout fragments, and before/after workspace YAML equality for refusal/no-change branches. Keep assertions away from prompt rendering and spinner/progress wording per Phase 86 context.</action>
  <verify>
    <automated>bun test tests/commands/workspace-recreate.test.ts</automated>
  </verify>
  <done>`workspace-recreate.test.ts` proves missing-template, workspace-without-template, no-change, and non-force cancel-safe recreate behavior through real CLI subprocesses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover forced recreate drift application</name>
  <files>tests/commands/workspace-recreate.test.ts</files>
  <behavior>
    - `open --recreate --force --no-ide --no-cmux` adds template repos that are missing from workspace YAML.
    - Forced recreate removes workspace repos that are no longer present in the template.
    - Forced recreate copies hook, env, env_file, files, and integrations drift from the template into workspace YAML.
    - Forced recreate preserves workspace identity fields such as name, branch, and template link.
  </behavior>
  <action>Extend the same test file with forced drift scenarios. Use direct YAML fixture setup so expected workspace state is explicit. For repo additions, include registry entries with local paths and assert resulting repo entries have stable names, modes, main paths, and task paths. For removed repos and config drift, parse the resulting YAML and assert exact durable fields instead of broad stdout. Keep desktop/plugin execution disabled with `--no-ide --no-cmux`.</action>
  <verify>
    <automated>bun test tests/commands/workspace-recreate.test.ts && bun run test:integ</automated>
  </verify>
  <done>Forced recreate applies repo and configuration drift exactly through CLI wiring, and the command integration suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> workspace command | Untrusted command arguments select workspace/template names and force behavior. |
| Template YAML -> workspace YAML | Template data is copied into persisted workspace config. |
| Registry YAML -> filesystem paths | Registry paths determine repo entries and task paths written during recreate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-01 | Tampering | `open --recreate` refusal paths | mitigate | Tests assert missing template, workspace-without-template, and non-force drift paths leave workspace YAML unchanged. |
| T-86-02 | Repudiation | `open --recreate` CLI output | mitigate | Tests assert bounded safety-critical messages for refused operations without snapshotting broad prose. |
| T-86-03 | Tampering | forced template drift copy | mitigate | Tests parse persisted YAML and assert only intended repo/config fields change while identity fields remain stable. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Status | Plan coverage |
|--------|------|--------|---------------|
| GOAL | Stable workspace command workflows have coverage where CLI wiring matters | COVERED | This plan covers `open --recreate`; Plans 02-03 cover remaining command workflows. |
| REQ | CMD-01 | COVERED | Tasks 1-2. |
| REQ | GATE-03 | COVERED | Task 2 integration verification. |
| RESEARCH | Use real CLI subprocess tests and stable state assertions | COVERED | Tasks require `runCli`, YAML parsing, and durable state assertions. |
| CONTEXT | `open --recreate` template-backed no-change, drift, missing-template, no-template, force/cancel behavior | COVERED | Tasks 1-2. |
| CONTEXT | Avoid wizard prompts, TUI rendering, external desktop/plugin environments | COVERED | Actions disable IDE/cmux and avoid prompt/spinner assertions. |
</source_audit>

<verification>
Run `bun test tests/commands/workspace-recreate.test.ts` after each task. Run `bun run test:integ` after Task 2.
</verification>

<success_criteria>
CMD-01 is traceably covered by a real command test file, and `open --recreate` tests prove both non-mutating refusal paths and forced mutation paths without brittle prompt or spinner assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/86-workspace-command-workflow-edge-coverage/86-01-SUMMARY.md`.
</output>
