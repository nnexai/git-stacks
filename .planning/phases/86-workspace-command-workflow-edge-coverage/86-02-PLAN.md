---
phase: 86-workspace-command-workflow-edge-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/commands/workspace-clean-gone.test.ts
  - tests/commands/workspace-destructive-safety.test.ts
autonomous: true
requirements:
  - CMD-02
  - CMD-03
  - GATE-03
must_haves:
  truths:
    - "`clean --gone` has real CLI subprocess coverage for deleted upstream detection, dirty refusal, dry-run, force, multi-workspace handling, and removal failure reporting."
    - "`clean`, `remove`, `merge`, and `rename` have representative CLI safety smoke for dry-run, force, missing entity, and non-interactive non-force refusal where command wiring matters."
    - "Tests use local bare remotes and real workspace fixtures instead of live remotes or copied implementation logic."
  artifacts:
    - path: "tests/commands/workspace-clean-gone.test.ts"
      provides: "`clean --gone` command edge coverage"
    - path: "tests/commands/workspace-destructive-safety.test.ts"
      provides: "destructive workspace command safety smoke"
  key_links:
    - from: "src/commands/workspace.ts"
      to: "tests/helpers.ts"
      via: "executor will exercise existing `clean --gone` command wiring through local bare remote fixtures"
      pattern: "--gone"
    - from: "tests/helpers.ts"
      to: "src/commands/workspace.ts"
      via: "executor will use existing local git helpers for gone and destructive command tests"
      pattern: "makeBareRemote|makeRepoWithRemote|gitExecOptions"
    - from: "src/commands/workspace.ts"
      to: "tests/helpers.ts"
      via: "executor will cover destructive command wiring through real CLI subprocesses"
      pattern: "clean|remove|merge|rename"
---

<objective>
Add focused command safety coverage for `clean --gone` and destructive workspace commands.

Purpose: CMD-02 and CMD-03 require confidence that command wiring honors dry-run, force, dirty-worktree, gone-branch, missing entity, and error-reporting contracts through real CLI execution.
Output: `workspace-clean-gone.test.ts` and `workspace-destructive-safety.test.ts`.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/86-workspace-command-workflow-edge-coverage/86-CONTEXT.md
@.planning/phases/86-workspace-command-workflow-edge-coverage/86-RESEARCH.md
@.planning/phases/86-workspace-command-workflow-edge-coverage/86-PATTERNS.md
@.planning/phases/86-workspace-command-workflow-edge-coverage/86-VALIDATION.md
@.planning/phases/81-workspace-and-git-operation-e2e-coverage/81-03-SUMMARY.md
@.planning/phases/81-workspace-and-git-operation-e2e-coverage/81-04-SUMMARY.md
@tests/helpers.ts
@tests/commands/workspace-guards.test.ts
@tests/commands/workspace-git-ops.test.ts
@src/commands/workspace.ts

<interfaces>
From `tests/helpers.ts`:
- `runCli(argv: string[], opts: RunCliOptions): RunCliResult`
- `formatCliFailure(result: RunCliResult): string`
- `makeRepoWithRemote(baseDir: string, repoName?: string, wsBranch?: string): RepoFixture`
- `makeWorkspaceFixture(configDir: string, wsName: string, repos: RepoFixture[], opts?: object): object`
- `gitExecOptions(cwd: string, baseDir?: string): ExecSyncOptions`
- `applyTestGitEnv(baseDir: string): () => void`
- `cleanup(dir: string): void`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cover clean gone remote-state behavior</name>
  <files>tests/commands/workspace-clean-gone.test.ts</files>
  <behavior>
    - Deleted upstream branches are detected using local bare remotes.
    - `clean --gone --dry-run` reports intended gone workspace cleanup without deleting workspace YAML or worktrees.
    - Dirty worktrees make `clean --gone` fail before deleting YAML or worktrees.
    - `clean --gone --force` removes multiple gone workspaces and leaves non-gone workspaces intact.
    - A stable removal failure fixture reports the failed workspace and continues/report exits according to current command contract.
  </behavior>
  <action>Create `tests/commands/workspace-clean-gone.test.ts` with real local git fixtures. Use `makeRepoWithRemote` and local bare remotes to create workspace branches, delete upstream refs, and fetch/prune as needed so `clean --gone` observes real gone state. Use `makeWorkspaceFixture` for workspace YAML. Assert YAML/worktree existence before and after dry-run, dirty refusal, force removal, multi-workspace removal, and removal failure reporting. Keep assertions on short safety-critical messages and durable state.</action>
  <verify>
    <automated>bun test tests/commands/workspace-clean-gone.test.ts</automated>
  </verify>
  <done>`clean --gone` coverage proves gone detection, dry-run, dirty refusal, force, multi-workspace handling, and removal failure reporting through real CLI subprocesses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover destructive command safety smoke</name>
  <files>tests/commands/workspace-destructive-safety.test.ts</files>
  <behavior>
    - `clean`, `remove`, `merge`, and `rename` missing-entity paths fail without mutating existing workspace files.
    - Dry-run variants do not remove or rename workspace YAML/worktrees.
    - Force variants mutate only the intended workspace state.
    - Non-force destructive commands in non-interactive subprocesses refuse or cancel without mutation when current command wiring prompts before lower-level mutation.
  </behavior>
  <action>Create `tests/commands/workspace-destructive-safety.test.ts` with representative smoke for `clean`, `remove`, `merge`, and `rename`. Reuse Phase 81 guard/lifecycle patterns. Do not build an exhaustive cross-product matrix. For each command, select the missing-entity, dry-run, force, or non-force branch that is not already fully proven by lower-level core tests and assert exit code plus durable YAML/filesystem/git state. If current command behavior prompts before lower-level dirty checks, assert non-mutation rather than trying to drive the prompt.</action>
  <verify>
    <automated>bun test tests/commands/workspace-destructive-safety.test.ts && bun run test:integ</automated>
  </verify>
  <done>Destructive command safety smoke covers CLI-level dry-run/force/error contracts without prompt-driving or broad output snapshots.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> destructive command | Untrusted command names and flags request workspace deletion, cleanup, merge, or rename. |
| Local git remote -> gone detection | Remote branch state determines whether a workspace is eligible for gone cleanup. |
| Workspace YAML -> filesystem mutation | Persisted workspace paths determine which worktrees/config files are removed or renamed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-04 | Tampering | `clean --gone --dry-run` and dirty refusal | mitigate | Tests assert dry-run and dirty refusal leave YAML and worktrees intact. |
| T-86-05 | Tampering | `clean --gone --force` | mitigate | Tests assert only gone workspaces are removed and non-gone workspaces remain. |
| T-86-06 | Repudiation | destructive command errors | mitigate | Tests assert bounded missing-entity and failure reporting for safety-critical branches. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Status | Plan coverage |
|--------|------|--------|---------------|
| GOAL | Stable workspace command workflows have behavior coverage where CLI wiring matters | COVERED | This plan covers cleanup/destructive workflows; Plans 01 and 03 cover recreate/wrappers. |
| REQ | CMD-02 | COVERED | Task 1. |
| REQ | CMD-03 | COVERED | Task 2. |
| REQ | GATE-03 | COVERED | Task 2 integration verification. |
| RESEARCH | Use local bare remotes for gone-branch behavior | COVERED | Task 1. |
| CONTEXT | `clean --gone` gone-branch detection, dirty refusal, dry-run/force, multi-workspace, removal failure | COVERED | Task 1. |
| CONTEXT | Destructive command smoke for `clean`, `remove`, `merge`, `rename` | COVERED | Task 2. |
| CONTEXT | Avoid prompt-driving and brittle prompt/spinner assertions | COVERED | Task actions explicitly assert non-mutation and stable messages only. |
</source_audit>

<verification>
Run the targeted new command tests after each task. Run `bun run test:integ` after Task 2.
</verification>

<success_criteria>
CMD-02 and CMD-03 are traceably covered by real CLI subprocess tests using local git fixtures, and destructive operations prove non-mutating safety paths plus explicit force behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/86-workspace-command-workflow-edge-coverage/86-02-SUMMARY.md`.
</output>
