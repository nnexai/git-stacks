---
phase: 124-user-shell-and-environment-authority
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/lib/service/contract.test.ts
  - tests/service-node/secure-contract-runtime.test.mjs
  - tests/service/web-projection.test.ts
  - tests/tui/dashboard/managed-service-bootstrap.test.ts
  - tests/commands/user-shell-host-fixture.test.ts
  - tests/fixtures/user-shell/bash-init.sh
  - tests/fixtures/user-shell/zsh-init.zsh
  - tests/fixtures/user-shell/fish-init.fish
  - tests/lib/service/launch-context.test.ts
  - tests/service/managed-service-process.test.ts
  - tests/service/web-terminal.test.ts
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02
  - SHELL-03
  - SHELL-04
  - SHELL-05
  - SHELL-06
  - SHELL-07
must_haves:
  truths:
    - "Secure refresh/TUI authorization and PTY/real-shell/SSH behavior are captured by executable RED fixtures before service implementation begins."
    - "Authenticated same-user local TUI is allowed, while browser, helper, pairing, and remote origins are rejected before parsing or store invocation."
    - "Typed terminal steps, real shell profiles, two-agent SSH rotation, future-launch-only environment changes, projection non-disclosure, and cleanup each have exact-sentinel RED evidence."
    - "Each RED command exits nonzero because its exact sentinel fails, never because of harness, import, syntax, fixture, or optional local capability failure."
  artifacts:
    - path: "tests/service-node/secure-contract-runtime.test.mjs"
      provides: "Runtime refresh trust-boundary RED matrix with parse/store spies"
    - path: "tests/tui/dashboard/managed-service-bootstrap.test.ts"
      provides: "Awaited refresh-before-first-TUI-launch RED contract"
    - path: "tests/commands/user-shell-host-fixture.test.ts"
      provides: "Real-shell profile and two-ssh-agent RED fixture"
    - path: "tests/service/web-terminal.test.ts"
      provides: "Typed sequential terminal-step, PTY, exit, and cleanup RED contract"
  key_links:
    - from: "tests/service-node/secure-contract-runtime.test.mjs"
      to: "packages/service/src/secure/router.ts"
      via: "origin matrix plus parse/store invocation spies"
      pattern: "local|browser|helper|pairing|remote"
    - from: "tests/tui/dashboard/managed-service-bootstrap.test.ts"
      to: "packages/service/src/policy/client.ts"
      via: "refresh acceptance ordered before first terminal/process request"
      pattern: "refresh|terminal.create"
    - from: "tests/commands/user-shell-host-fixture.test.ts"
      to: "tests/fixtures/user-shell"
      via: "real Bash, zsh, and fish startup plus ssh-agent/ssh-add rotation"
      pattern: "bash|zsh|fish|ssh-agent|ssh-add"
  prohibitions:
    - "Do not edit production source in this plan."
    - "Do not accept browser/remote refresh, combined generated shell steps, value disclosure, or hosted-capability inference from a local run."
    - "Do not tag, push, publish, create releases, or edit release metadata."
---

<objective>
Establish the pre-implementation secure refresh, TUI, terminal, real-shell, SSH, and projection RED contracts.

Purpose: Lock service trust boundaries and real launch behavior independently from the core adapter RED work in Plan 00.
Output: Eleven service/host test and fixture files with two exact-sentinel RED gates consumed by Plans 03 through 05.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/124-user-shell-and-environment-authority/124-CONTEXT.md
@.planning/phases/124-user-shell-and-environment-authority/124-RESEARCH.md
@.planning/phases/124-user-shell-and-environment-authority/124-PATTERNS.md
@.planning/phases/124-user-shell-and-environment-authority/124-VALIDATION.md

<interfaces>
This plan is test-only. Runtime authorization tests must observe parsing and store calls separately. Real-shell fixtures may capability-skip a genuinely missing local shell with a precise reason unless required mode is set; such a skip can neither cause nor satisfy the RED sentinel. Hosted execution remains a Phase 127 pre-tag receipt boundary.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock secure refresh, projection non-disclosure, and TUI ordering</name>
  <read_first>
    - packages/protocol/src/service.ts
    - packages/service/src/secure/router.ts
    - packages/service/src/policy/client.ts
    - packages/service/src/web/projection.ts
    - tests/service-node/secure-contract-runtime.test.mjs
  </read_first>
  <files>tests/lib/service/contract.test.ts, tests/service-node/secure-contract-runtime.test.mjs, tests/service/web-projection.test.ts, tests/tui/dashboard/managed-service-bootstrap.test.ts</files>
  <behavior>
    - Authenticated same-user local CLI/TUI refresh reaches parsing/store exactly once; browser, helper, pairing, and remote origins reach neither parser nor store, including with malformed bodies.
    - Valid refresh atomically replaces PATH/SSH_AUTH_SOCK, omission clears, invalid input preserves old state, and existing processes retain their launch snapshot.
    - Refresh values are absent from snapshots, revisions, logs, URLs, YAML, browser projections, reconnect payloads, and returned diagnostics.
    - TUI refresh acceptance completes before its first core-state, terminal, or process request; denial/failure prevents that request.
  </behavior>
  <action>Add strict contract schemas expectations, runtime parse/store spies, unique disclosure canaries, replacement/clear temporal assertions, and ordered TUI client events. Require one failing assertion named exactly `PHASE124_RED refresh authorization TUI ordering contract`; fixture setup and all trust-boundary observations preceding it must pass.</action>
  <verify>
    <automated>bash -lc 'set -o pipefail; out=$({ GIT_STACKS_KEY_STORE=file ./node_modules/.bin/vitest run tests/lib/service/contract.test.ts tests/service/web-projection.test.ts; node --test tests/service-node/secure-contract-runtime.test.mjs; bun test --preload @opentui/solid/preload tests/tui/dashboard/managed-service-bootstrap.test.ts; } 2>&amp;1); status=$?; printf "%s\n" "$out"; test "$status" -ne 0; printf "%s\n" "$out" | rg -q "PHASE124_RED refresh authorization TUI ordering contract"; ! printf "%s\n" "$out" | rg -q "Cannot find module|SyntaxError|Failed to load|No test files found"'</automated>
  </verify>
  <done>The secure refresh/projection/TUI suite loads and fails specifically on its exact missing-behavior sentinel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock typed terminal steps, real profiles, SSH rotation, and cleanup</name>
  <read_first>
    - packages/service/src/web/terminal-manager.ts
    - packages/service/src/policy/snapshot.ts
    - tests/lib/service/launch-context.test.ts
    - tests/service/managed-service-process.test.ts
    - tests/service/web-terminal.test.ts
  </read_first>
  <files>tests/commands/user-shell-host-fixture.test.ts, tests/fixtures/user-shell/bash-init.sh, tests/fixtures/user-shell/zsh-init.zsh, tests/fixtures/user-shell/fish-init.fish, tests/lib/service/launch-context.test.ts, tests/service/managed-service-process.test.ts, tests/service/web-terminal.test.ts</files>
  <behavior>
    - One logical command terminal runs bounded typed pre/main/post/repository steps sequentially as separate adapter invocations, preserving each step's unchanged command, cwd, environment, exit, diagnostics, and cleanup.
    - Real installed Bash/zsh/fish fixtures prove interactive-login aliases/functions/nvm-style PATH, quoting, exits, initialization failure, cancellation, and actual shell identity.
    - Two real ssh-agent instances with ephemeral keys prove existing process A versus future PTY/non-PTY B behavior and successful ssh-add invocation without service restart.
    - Fixture teardown kills agents and descendant trees and removes keys/files even on assertion failure.
  </behavior>
  <action>Create real shell startup fixtures, typed-step protocol/manager expectations, PTY/process spies, and robust two-agent teardown. Treat local missing-shell capability precisely, while required mode must fail on every skip. Require one failing assertion named exactly `PHASE124_RED terminal steps SSH rotation contract`; missing optional local zsh may be reported but cannot satisfy or cause it.</action>
  <verify>
    <automated>bash -lc 'set -o pipefail; out=$(GIT_STACKS_KEY_STORE=file ./node_modules/.bin/vitest run tests/commands/user-shell-host-fixture.test.ts tests/lib/service/launch-context.test.ts tests/service/managed-service-process.test.ts tests/service/web-terminal.test.ts 2>&amp;1); status=$?; printf "%s\n" "$out"; test "$status" -ne 0; printf "%s\n" "$out" | rg -q "PHASE124_RED terminal steps SSH rotation contract"; ! printf "%s\n" "$out" | rg -q "Cannot find module|SyntaxError|Failed to load|No test files found"'</automated>
  </verify>
  <done>The terminal/host/SSH suite loads, cleans up resources, and fails specifically on its exact missing-behavior sentinel.</done>
</task>

</tasks>

<threat_model>
## STRIDE threat register

| Category | Threat | Disposition and mitigation |
|---|---|---|
| Spoofing | Browser/remote is represented as trusted local TUI. | Runtime origin matrix with separate parse/store spies. |
| Tampering | Multiple terminal steps become generated shell source. | Typed ordered-step expectations and separate adapter-call spies. |
| Repudiation | Harness/capability failure is mistaken for RED evidence. | Exact sentinels plus negative harness-error scan and explicit capability records. |
| Information Disclosure | Refresh/socket/key/secret values reach browser or durable state. | Canary scans across all projection/persistence/diagnostic surfaces. |
| Denial of Service | Agents or child trees survive failure/cancellation. | Finally teardown and descendant-liveness assertions. |
| Elevation of Privilege | Remote refresh redirects future launches. | Same-user local-only authorization and allowlisted replacement keys. |
</threat_model>

<verification>
Run both RED commands independently. Each must exit nonzero, contain its exact sentinel, and contain no enumerated harness/import/syntax failure marker; optional local capability reporting must be distinct from the sentinel.
</verification>

<success_criteria>
- Secure/service/host portions of SHELL-01 through SHELL-07 have pre-implementation RED coverage.
- Plans 03, 04, and 05 depend on this contract and no production file is modified.
- No release, tag, push, or publish side effect occurs.
</success_criteria>

<output>
Create `.planning/phases/124-user-shell-and-environment-authority/124-06-SUMMARY.md` when done.
</output>
