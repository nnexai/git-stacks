---
phase: 124-user-shell-and-environment-authority
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/lib/user-shell-adapter.test.ts
  - tests/lib/workspace-env.test.ts
  - tests/lib/lifecycle.test.ts
  - tests/commands/run-parallel.test.ts
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02
  - SHELL-03
  - SHELL-04
  - SHELL-07
must_haves:
  truths:
    - "Core shell, profile, environment, timeout, quoting, and process-tree behavior is captured by executable RED fixtures before production implementation begins."
    - "The RED verification exits nonzero because its exact Phase 124 sentinel fails, not because of an import, syntax, fixture, missing-tool, or harness error."
    - "The contract rejects every invalid SHELL without fallback and proves initialization-only timeout plus complete descendant cleanup."
  artifacts:
    - path: "tests/lib/user-shell-adapter.test.ts"
      provides: "Shell discovery, profile initialization, quoting, timeout, diagnostics, and process-tree RED contract"
    - path: "tests/lib/workspace-env.test.ts"
      provides: "Replacement environment precedence and reserved GS_* protection RED contract"
    - path: "tests/lib/lifecycle.test.ts"
      provides: "Separate lifecycle-step invocation and cancellation RED contract"
  key_links:
    - from: "tests/lib/user-shell-adapter.test.ts"
      to: "packages/core/src/user-shell.ts"
      via: "future adapter contract exercised through harness-safe boundaries"
      pattern: "SHELL|bash|zsh|fish|SIGTERM|SIGKILL"
    - from: "tests/lib/workspace-env.test.ts"
      to: "packages/core/src/workspace-env.ts"
      via: "ordered overlay and replacement assertions"
      pattern: "PATH|SSH_AUTH_SOCK|GS_"
  prohibitions:
    - "Do not edit production source in this plan."
    - "Do not accept RED caused by missing imports, syntax errors, broken fixtures, or unavailable optional local shells."
    - "Do not tag, push, publish, create releases, or edit release metadata."
---

<objective>
Establish the pre-implementation core shell and process RED contract.

Purpose: Lock fail-closed discovery, interactive-login profile behavior, exact command transport, environment precedence, initialization timeout, and process-tree cleanup before core implementation.
Output: Four focused test files and an exact-sentinel RED gate consumed by Plans 01 and 02.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/124-user-shell-and-environment-authority/124-CONTEXT.md
@.planning/phases/124-user-shell-and-environment-authority/124-RESEARCH.md
@.planning/phases/124-user-shell-and-environment-authority/124-PATTERNS.md
@.planning/phases/124-user-shell-and-environment-authority/124-VALIDATION.md

<interfaces>
This plan is test-only. Tests may express intended future symbols through harness-safe dynamic boundaries or assert legacy behavior directly, but the suite must load fully and reach the named sentinel. Shell-family unit fixtures must not require zsh to be installed locally.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock discovery, profile, command, environment, and process-tree behavior</name>
  <read_first>
    - packages/core/src/lifecycle.ts
    - packages/core/src/workspace-env.ts
    - packages/core/src/node-runtime.ts
    - tests/lib/lifecycle.test.ts
    - tests/commands/run-parallel.test.ts
  </read_first>
  <files>tests/lib/user-shell-adapter.test.ts, tests/lib/workspace-env.test.ts, tests/lib/lifecycle.test.ts, tests/commands/run-parallel.test.ts</files>
  <behavior>
    - Absolute executable Bash/zsh/fish paths initialize interactive-login profiles; unset, relative, missing, non-executable, and unsupported SHELL values fail without `/bin/sh` fallback.
    - Aliases, functions, nvm-like PATH, exact nested quoting, nonzero exits, and discovery/validation/initialization/execution diagnostics are asserted with unit fixtures for all three families.
    - The 10 second deadline ends after initialization; successfully initialized commands have no runtime timeout; cancellation performs TERM, grace, KILL across descendants.
    - Environment precedence is refresh, shell-init, configured overlays, secrets, then reserved GS_*; replacement omission clears old PATH/SSH_AUTH_SOCK.
    - Lifecycle and parallel execution keep pre/main/post/repository commands separate rather than generating combined shell source.
  </behavior>
  <action>Create or extend the four focused fixtures without production edits. Use fake executable shells and injected clocks/spawners so all family semantics are deterministic on a host without zsh. Add exact command-byte assertions, descendant-liveness probes, separated diagnostic/output assertions, overlay conflicts, and no-fallback/no-concatenation negatives. Ensure the harness reaches one failing assertion named exactly `PHASE124_RED shell profile process-tree contract`; all fixture setup preceding it must pass.</action>
  <verify>
    <automated>bash -lc 'set -o pipefail; out=$(GIT_STACKS_KEY_STORE=file ./node_modules/.bin/vitest run tests/lib/user-shell-adapter.test.ts tests/lib/workspace-env.test.ts tests/lib/lifecycle.test.ts tests/commands/run-parallel.test.ts 2>&amp;1); status=$?; printf "%s\n" "$out"; test "$status" -ne 0; printf "%s\n" "$out" | rg -q "PHASE124_RED shell profile process-tree contract"; ! printf "%s\n" "$out" | rg -q "Cannot find module|SyntaxError|Failed to load|No test files found"'</automated>
  </verify>
  <done>The focused core suite loads and fails specifically on the exact missing shell/profile/process-tree behavior sentinel.</done>
</task>

</tasks>

<threat_model>
## STRIDE threat register

| Category | Threat | Disposition and mitigation |
|---|---|---|
| Spoofing | Workspace overlay forges reserved service identity. | Conflict tests require reserved GS_* keys last. |
| Tampering | Generated shell source changes the user command. | Exact one-argument and no-concatenation assertions. |
| Repudiation | Harness failure is mistaken for RED evidence. | Exact sentinel plus negative harness-error scan. |
| Information Disclosure | Environment or secret values enter diagnostics. | Canary-based metadata-only diagnostic assertions. |
| Denial of Service | Initialization/descendants survive timeout or cancellation. | Clocked initialization deadline and descendant-liveness tests. |
| Elevation of Privilege | Fallback shell bypasses validation. | Complete invalid-SHELL matrix and explicit no-fallback assertion. |
</threat_model>

<verification>
Run the single focused RED command. It must exit nonzero, contain `PHASE124_RED shell profile process-tree contract`, and contain none of the enumerated harness/import/syntax failure markers.
</verification>

<success_criteria>
- Core portions of SHELL-01 through SHELL-04 and SHELL-07 have pre-implementation RED coverage.
- Plan 01 depends on this contract and no production file is modified.
- No release, tag, push, or publish side effect occurs.
</success_criteria>

<output>
Create `.planning/phases/124-user-shell-and-environment-authority/124-00-SUMMARY.md` when done.
</output>
