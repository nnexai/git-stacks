---
context: phase
phase: 126-web-workflow-and-forge-source-parity
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-07-16T20:36:21.747Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Canonical inventory must fail closed — absence, loading, or failure of service action descriptors must render explicit non-actionable state; it must never enable locally assembled legacy actions.
- [ ] CONSTRAINT: Green focused tests and a SUMMARY are not review gates — both repair branches produced passing evidence before independent review found real high-severity behavior gaps; require full relevant gates plus clean independent review before merge.
- [ ] CONSTRAINT: Focus restoration must use production-wired lifecycle coverage — helper-only tests and fake visible elements do not prove the actual scope-menu → action callback → modal/controller → restore path.
- [ ] CONSTRAINT: Hidden scope-menu items require structural restoration — reopening the originating menu, restoring `aria-expanded`, validating the concrete item, and focusing it must be one coordinated path with fallback when the item is gone or unusable.
- [ ] CONSTRAINT: Preserve the release boundary — do not tag, push, publish, create a release, or claim RC approval; Phase 127 must stop for the user's manual verification first.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Enabled legacy fallback | Plan 06 originally exposed enabled client-local actions while canonical inventory loaded or failed, bypassing service policy. | blocking | Keep explicit loading/error states non-actionable; preserve RED tests proving zero dispatch without authoritative descriptors. |
| Summary-before-review | Plan 06 and Plan 07 each had green focused evidence and summaries before independent review found contract failures. | blocking | Full relevant suite + clean independent review are mandatory before cherry-pick. |
| Stale fixture encodes forbidden behavior | Plan 06 full TUI suite still contains App integration fixtures that throw from `fetchWorkspaceActionInventory` and expect the removed legacy fallback. | blocking | Update fixtures to provide canonical inventory; retain separate fail-closed loading/error coverage; rerun `npm run test:tui`. |
| Helper-only focus proof | Plan 07 tests exercised exported helpers with simulated elements but not the production scope-menu listener, callbacks, modal/controller, or restoration wiring. | blocking | Add integration DOM/controller tests through the exact coordinator API used by production. |
| Connected means focusable | Plan 07 treated a connected element as a valid return target even when hidden by its menu ancestor; later checks still accept disabled/inert/CSS-hidden or focus-failing targets. | blocking | Coordinate menu reopening and item focus; reject unusable targets, revalidate at focus time, verify focus moved, and run fallback otherwise. |
| Environment diagnostics treated as gates | Editor diagnostics from isolated worktrees can reflect missing Bun/Node/workspace declarations and stale shared dependencies. | advisory | Trust exact executed package gates; rerun standard workspace gates after cherry-pick to main. |

<current_state>
Main is cleanable at `9e095470` on `main`; only the pause-state update and these handoff artifacts are being committed. Phase 126 remains incomplete with Plans 06 and 08 lacking summaries on main. Plan 07's original implementation is on main, but its reviewed repair branch is intentionally unmerged.

Two isolated repair worktrees are clean:

- `/tmp/git-stacks-126-06`, branch `codex/phase126-06`, tip `61b8e786`
- `/tmp/git-stacks-126-07-review-fix`, branch `codex/phase126-07-review-fix`, tip `17ec414b`

Plan 08 has not started. No tag, push, publish, release, or RC claim occurred.
</current_state>

<completed_work>

- Phase 126 Plans 01-05 remain merged through `9ffa4b07`.
- Plan 06 original implementation commits remain `7864361b 25954a59 d8ea6969 f065271a 4975050a 519ca023 5573d52e d182db26`.
- Plan 06 review repair commits are:
  - `6ad1dcf1` — RED regression coverage for reviewed TUI gaps.
  - `5a648e3d` — authority, confirmation, parity, operation recovery, reviewed-create recovery, shortcut, note rejection, and shared-latch repairs.
  - `61b8e786` — honest repair evidence in `126-06-SUMMARY.md`.
- Plan 06 passing evidence: focused `WorkspaceParity` 13/13, `ForgeSourceReview` 5/5, `ActionMenu` 17/17; TUI/client/service typechecks; TUI build; dependency and architecture gates; authority/whitespace scans.
- Plan 07 original implementation remains on main through `5365b46d`.
- Plan 07 repair branch includes prior commits `75bea09a bacb6adc bf3009c7`, initial focus fix `c368a1ec`, and stale-invoker repair `17ec414b`.
- Plan 07 passing focused evidence at the last repair: 45 web workspace/navigation/overlay tests, web build, branch-local source typecheck, client/protocol typechecks, dependency gate, and diff check.
- GSD 1.7.0 workflow update remains committed at `0837a1ed`; the earlier pause artifact is `9e095470`.
</completed_work>

<remaining_work>

1. **Plan 06 full-suite closure in `/tmp/git-stacks-126-06`:**
   - Update stale App integration fixtures that intentionally throw from `fetchWorkspaceActionInventory` and expect the forbidden legacy menu.
   - Fixtures for normal interaction must supply authoritative canonical inventories.
   - Preserve explicit loading/error fail-closed tests proving no action dispatch.
   - Run the repository-required isolated TUI command: `npm run test:tui` (never a broad direct Bun invocation that permits mock pollution), plus focused/type/build/dependency/architecture gates.
   - Update `126-06-SUMMARY.md` with exact final evidence and commit atomically.
2. **Independent Plan 06 re-review:** adversarially verify every original finding and the fixture correction; do not merge on self-check alone.
3. **Plan 07 production-wired focus repair in `/tmp/git-stacks-126-07-review-fix`:**
   - Current scope handler hides the menu before invoking the concrete item (`packages/web/src/app.ts` around 1099-1107).
   - Current usability helper rejects the resulting hidden-ancestor item (`packages/web/src/navigation.ts` around 40-46), so Notes/confirmation cannot restore it.
   - Implement a coordinated restoration target/callback that can reopen the originating scope menu, restore `aria-expanded`, verify the original item still exists and is usable, and focus it; otherwise fall back to the actual visible invoker or xterm.
   - Reject disabled, inert, CSS-hidden, disconnected, and focus-failing targets. Revalidate inside the animation-frame callback and verify focus moved before returning.
   - Replace helper-only proof with integration-level DOM/controller coverage that executes actual production wiring: scope-menu Notes/confirmation close restores the concrete item; removed menu/item falls back; Rename cancel followed by Create does not use stale scope state; confirmation path is covered.
   - Ensure tests fail if capture, menu reopening, usability validation, or production wiring is removed.
4. **Independent Plan 07 re-review:** require a CLEAN verdict; repeated review has already found two blocker rounds.
5. **Integration:** only after both branches are cleanly reviewed, cherry-pick exact commit sets onto main, rebuild shared packages, and run standard service/web/TUI typechecks, focused suites, full TUI suite, web/TUI builds, dependency/architecture gates, and diff check.
6. Execute Phase 126 Plan 08 conformance, architecture/full gates, verification, and `126-PHASE127-HANDOFF.md`.
</remaining_work>

<decisions_made>

- Reused the existing clean repair worktrees instead of redispatching Plan 06 from scratch because the safe-resume gate found committed implementation and duplicate execution was unsafe.
- Kept Plans 06 and 07 unmerged despite green focused tests because independent review is the merge gate.
- Stopped before another repair cycle when orchestration context crossed the workflow's POOR threshold.
- Service/core remain the sole authority; clients consume canonical action/operation/notes/files/forge contracts.
- No tag, push, publish, release, or RC approval claim before Phase 127 manual verification.
</decisions_made>

<blockers>

- Plan 06 full isolated TUI suite is red due to stale integration fixtures expecting the now-forbidden authority fallback.
- Plan 06 still needs independent re-review after full-suite closure.
- Plan 07 still has two independent-review blockers: hidden scope-menu restoration is structurally incomplete, and tests do not exercise production wiring.
- Plan 07 has one related warning: unusable/focus-failing targets can suppress fallback.
- There is no external or human-action blocker; all remaining blockers are technical.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — exact branch tips, blockers, and next action.
2. This file — failure-derived constraints and detailed review findings.
3. `.planning/phases/126-web-workflow-and-forge-source-parity/126-06-PLAN.md` — TUI contract.
4. `/tmp/git-stacks-126-06/.planning/phases/126-web-workflow-and-forge-source-parity/126-06-SUMMARY.md` — current repair claims and gate evidence.
5. `.planning/phases/126-web-workflow-and-forge-source-parity/126-07-PLAN.md` — web contract.
6. `/tmp/git-stacks-126-07-review-fix/.planning/phases/126-web-workflow-and-forge-source-parity/126-07-SUMMARY.md` — current repair claims and caveats.
7. `.planning/phases/126-web-workflow-and-forge-source-parity/126-08-PLAN.md` — do not execute until Plans 06/07 are cleanly reviewed and merged.
8. `.planning/phases/126-web-workflow-and-forge-source-parity/126-CONTEXT.md` and `126-UI-SPEC.md` — locked authority, focus, and cross-client UX decisions.

## Critical Anti-Patterns (do NOT repeat these)

- Do not make missing/failed inventory mean “all legacy actions enabled.”
- Do not update the full-suite fixtures by weakening fail-closed production behavior; normal fixtures must provide canonical inventory.
- Do not accept helper-level focus tests as proof of production wiring.
- Do not focus a hidden item without reopening its menu and restoring the menu's accessibility state.
- Do not return from focus restoration merely because `focus()` was called; verify a usable target received focus or run fallback.
- Do not merge based on passing focused tests or a SUMMARY; independent review must be clean.
- Do not start Plan 08 before both repair branches are merged with post-merge gates green.

## Infrastructure State

- Main worktree: `main` at `9e095470` before this pause commit.
- Plan 06 worktree: clean at `61b8e786`; retain it.
- Plan 07 repair worktree: clean at `17ec414b`; retain it.
- User worktrees `/home/nnex/workspaces/tasks/test-2/git-stacks` and `/home/nnex/workspaces/tasks/test/git-stacks` remain untouched.
- No background agents, servers, watchers, test processes, async jobs, or external jobs remain.
- Editor diagnostics shown during the session are not accepted as gate evidence; execute standard package commands in the correct worktree/environment.

<context>
The milestone remains in Phase 126 Wave 5/6. The safe strategy is a fresh-session repair/re-review loop on the two retained worktrees: close Plan 06's stale full-suite fixtures and independently review it; structurally repair Plan 07's actual scope-menu restoration lifecycle with production-wired tests and independently review it. Only then cherry-pick both branches to main, run post-merge gates, and begin Plan 08. Preserve deterministic-vs-live evidence honesty and the stop-before-release boundary.
</context>

<next_action>
Start with `/gsd-resume-work`, acknowledge every blocking constraint, then repair the stale Plan 06 App integration fixtures in `/tmp/git-stacks-126-06` and rerun `npm run test:tui`. In parallel, repair the production-wired Plan 07 scope-menu restoration blockers in `/tmp/git-stacks-126-07-review-fix`. Do not merge either branch or start Plan 08 first.
</next_action>
