---
context: phase
phase: 126-web-workflow-and-forge-source-parity
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-07-16T20:01:36.694Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Canonical inventory must fail closed — the current Plan 06 branch exposes a fully enabled legacy menu while the service inventory loads or fails; no action may be invokable until authoritative descriptors arrive.
- [ ] CONSTRAINT: A green implementation summary is not a review gate — Plan 06's full TUI suite passed, but independent review still found five high-severity contract gaps; repair with RED tests and re-review before merge.
- [ ] CONSTRAINT: Preserve the release boundary — do not tag, push, publish, or create a release; Phase 127 must stop for the user's manual verification first.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Enabled legacy fallback | Missing canonical descriptors currently switch the TUI to locally enabled actions, bypassing service policy. | blocking | Render explicit loading/error states and prove with tests that no action dispatch is possible without authoritative descriptors. |
| Summary-before-review | `126-06-SUMMARY.md` records completion even though independent review found untested authority and recovery gaps. | blocking | Treat the branch as incomplete, add RED coverage for every finding, and require a clean independent re-review before cherry-pick. |
| Placement-blind focus coverage | Web focus tests cover the context menu but not the separate scope-menu placement. | advisory | Capture each concrete invoker and add a scope-menu-specific focus restoration test. |

<current_state>
Main is clean at `0837a1ed` (`chore: update gsd workflows`) with GSD 1.7.0 installed. Phase 126 Plans 01-05 and the original Plan 07 implementation are already on main. Two isolated repair branches are clean but intentionally unmerged:

- `/tmp/git-stacks-126-06`, branch `codex/phase126-06`, tip `d182db26`
- `/tmp/git-stacks-126-07-review-fix`, branch `codex/phase126-07-review-fix`, tip `bf3009c7`

Plan 126-08 has not started. Phase 127 has not been written or executed. No tag, push, publish, or release action occurred.
</current_state>

<completed_work>

Completed checkpoints:

- Phase 126 Plans 01-05 are merged through `9ffa4b07`.
- Plan 06 implementation is committed as `7864361b 25954a59 d8ea6969 f065271a 4975050a 519ca023 5573d52e d182db26`.
- Plan 06 branch evidence before review: full `npm run test:tui`, focused forge/parity/file suites, TUI and service typechecks, TUI build, dependency gate, and authority scan passed.
- Plan 07 original implementation is already on main through `5365b46d`.
- Plan 07 repair commits are `75bea09a` (RED), `bacb6adc` (GREEN), and `bf3009c7` (summary).
- Plan 07 repair evidence: 16 files / 188 focused tests, web build, dependency gate, diff check, and package typechecks passed.
- Independent Plan 07 re-review found no Critical/High authority or security regression; the six original gaps are materially closed.
- User's GSD update was committed separately as `0837a1ed`; the CLI reports version 1.7.0 and its help command succeeds.
</completed_work>

<remaining_work>

1. Repair Plan 06 on `codex/phase126-06` with RED tests for all independent-review findings:
   - `App.tsx:655` / `ActionMenu.tsx:116`: inventory loading/failure falls back to a fully enabled legacy menu.
   - `web.ts:115`, `App.tsx:847`, `App.tsx:931`: canonical Merge bypasses required confirmation.
   - `ActionMenu.tsx:76,101`: canonical rendering drops Edit, Clean, Run, Issue, and Commands.
   - `App.tsx:713` / `WorkspaceOperationView.tsx:67`: `submit-unknown` can trap the user despite Back copy.
   - `App.tsx:744` / client `forge-review.ts:296`: failed/cancelled reviewed creation does not flow through `forgeReview.observeOperation`, so editable review is not restored.
   - Warnings: duplicate `v` Pin/Unpin shortcut, unhandled note add/clear rejections, and local action latches instead of shared `createWorkspaceActionRegistry` policy.
2. Independently re-review Plan 06 and only then cherry-pick its exact commits onto main.
3. Repair the residual Plan 07 warning: `packages/web/src/app.ts:1108` scope-menu `row.run()` does not set `pendingOverlayInvoker`; add a placement-specific focus test and re-review.
4. Cherry-pick the reviewed Plan 07 repair commits onto main, rebuild shared packages, and run standard service/web/TUI typechecks plus focused suites/build/dependency gates.
5. Execute Phase 126 Plan 08 conformance, architecture gates, full gates, and `126-PHASE127-HANDOFF.md`.
6. Continue Phase 127 under the updated autonomous workflows, retaining live/manual/hosted evidence honesty and the stop-before-release boundary.
</remaining_work>

<decisions_made>

- Stop now so the next session uses updated GSD 1.7.0 workflows end-to-end.
- Do not merge a branch merely because its tests and summary are green; independent review findings are blocking.
- Archive continues to stop all service-owned terminals before persistence.
- Dirty worktree Force Remove remains triggerable in TUI/web only after exact workspace-name confirmation.
- Service/core remain the sole authority; clients consume canonical action/operation/notes/files/forge contracts.
- No tag, push, publish, or release before the user's Phase 127 manual verification.
</decisions_made>

<blockers>

- Plan 06 has five high-severity untested contract gaps plus three warnings; it must not be merged.
- Plan 07 has one residual focus-restoration warning; repair it before merge.
- There is no external or human blocker to resuming implementation.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable checkpoint and exact next action.
2. This file — full review findings and branch topology.
3. `.planning/phases/126-web-workflow-and-forge-source-parity/126-06-PLAN.md` — TUI contract that remains incomplete.
4. `.planning/phases/126-web-workflow-and-forge-source-parity/126-07-PLAN.md` — web contract and repaired scope.
5. `.planning/phases/126-web-workflow-and-forge-source-parity/126-08-PLAN.md` — do not execute until Plans 06/07 are cleanly reviewed and merged.
6. `.planning/phases/126-web-workflow-and-forge-source-parity/126-CONTEXT.md` and `126-UI-SPEC.md` — locked authority and cross-client UX decisions.

## Critical Anti-Patterns (do NOT repeat these)

- Do not make absence/failure of service inventory mean “all legacy actions enabled.” Use a closed loading/error state.
- Do not implement confirmations independently in a canonical dispatch switch. Consume the shared registry/policy so Merge and destructive actions cannot bypass confirmation.
- Do not let a terminal error view advertise Back while its key handler rejects Back for that state.
- Do not classify reviewed-create terminal failure outside `forgeReview.observeOperation`; that loses the recoverable editable-review transition.
- Do not use a published-runtime dynamic import trick or `any`/empty service shim. The current Plan 06 typed static bridge is valid and should remain typed.

## Infrastructure State

- Main worktree: clean at `0837a1ed`.
- Plan 06 worktree: clean at `d182db26`; retain it for repair.
- Plan 07 repair worktree: clean at `bf3009c7`; retain it for the focus fix.
- User worktrees `/home/nnex/workspaces/tasks/test-2/git-stacks` and `/home/nnex/workspaces/tasks/test/git-stacks` were untouched.
- No servers, watchers, test processes, async jobs, or external jobs are running.
- No uncommitted files remain in any worktree named above.

<context>
The milestone remains in Phase 126 Wave 5/6. The correct resumption strategy is to repair the reviewed branches in parallel, independently re-review them, cherry-pick only clean commit sets onto main, then start Plan 126-08. Phase 127 planning research exists only as prior conversational/scout context; no Phase 127 artifacts were written, so resume from the live planning files and updated workflows rather than treating those notes as committed decisions.
</context>

<next_action>
Start with: `$gsd-resume-work`, acknowledge every blocking constraint, then repair Plan 06 in `/tmp/git-stacks-126-06` before starting Plan 126-08.
</next_action>
