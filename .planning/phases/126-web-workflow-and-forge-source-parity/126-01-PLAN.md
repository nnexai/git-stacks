---
phase: 126-web-workflow-and-forge-source-parity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/protocol/src/service.ts
  - packages/protocol/src/web.ts
  - tests/service/web-workflow-contract.test.ts
autonomous: true
requirements: [PARITY-01, PARITY-02, PARITY-03, PARITY-04, PARITY-05, SOURCE-01, SOURCE-02, SOURCE-03, SOURCE-04]
must_haves:
  truths:
    - "One strict protocol vocabulary identifies every canonical workspace, notes, file-status, cancellation, resolve, review, and create intent without granting either client authority."
    - "Browser-safe projections are bounded allowlists that cannot carry host paths, raw provider output, credentials, clone/fetch coordinates, commands, or arbitrary core records."
    - "Reviewed creation has distinct Resolve and Review/Submit contracts; Enter on unresolved URL cannot be represented as creation."
  artifacts:
    - path: packages/protocol/src/web.ts
      provides: "Strict browser action, note, safe file-status, operation cancellation, forge resolve/review schemas and types"
    - path: packages/protocol/src/service.ts
      provides: "Carrier-neutral operation cancellation and reviewed-source primitives"
    - path: tests/service/web-workflow-contract.test.ts
      provides: "Executable inventory, bounds, strictness, and disclosure contract"
  key_links:
    - from: packages/service/src/secure/router.ts
      to: packages/protocol/src/web.ts
      via: "Plan 04 parses every browser request and validates every projected response"
    - from: packages/client/src/workspace-actions.ts
      to: packages/protocol/src/web.ts
      via: "Plan 05 consumes canonical action IDs and authoritative availability"
---

<objective>
Land the strict browser/service contract before any parallel authority or resolver work.

Purpose: Prevent core, service, client, web, and TUI from inventing incompatible action, cancellation, note/file, or reviewed-source shapes.
Output: Bounded Zod schemas/types and an adversarial protocol suite.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-web-workflow-and-forge-source-parity/126-CONTEXT.md
@.planning/phases/126-web-workflow-and-forge-source-parity/126-RESEARCH.md
@.planning/phases/126-web-workflow-and-forge-source-parity/126-UI-SPEC.md
@packages/protocol/src/service.ts
@packages/protocol/src/web.ts

Decision traceability: D-01: canonical action vocabulary; D-02: optional shortcut invoker; D-03: core/service authority; D-04: typed availability; D-06: durable identity; D-07: honest cancellation; D-14: append-only notes; D-17: shared file states; D-18: path-free DTO; D-20: GitHub/GitLab only; D-21: explicit resolve result; D-22: opaque safe token; D-24: complete reviewed draft; D-25: Resolve cannot Create; D-28: typed non-destructive failures.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze canonical action and honest operation contracts</name>
  <files>packages/protocol/src/service.ts, packages/protocol/src/web.ts, tests/service/web-workflow-contract.test.ts</files>
  <read_first>packages/protocol/src/service.ts, packages/protocol/src/web.ts, packages/service/src/policy/core-contract.ts, .planning/phases/126-web-workflow-and-forge-source-parity/126-CONTEXT.md</read_first>
  <behavior>Every required action ID parses exactly once; availability carries an enumerated disabled reason and safe message; confirmation is none/confirm/exact-name; running operations project action/workspace identity and typed cancellability; cancel results distinguish requested, already-finished, too-late, and not-cancellable.</behavior>
  <action>Add strict stable action-ID, subject, availability, confirmation, operation summary/cancellation state, cancel request/result, and browser operation schemas. Extend operation mutation unions for pull and note add/clear while retaining Phase 123 lifecycle shapes unchanged. Keep labels/presentation outside service.ts where not transport truth; reject duplicate IDs, hybrid unions, unknown fields, unsafe arbitrary progress/result records, and invalid cancellation states.</action>
  <acceptance_criteria>Protocol tests enumerate archive/unarchive/remove/force-remove/rename/open/close/pin/unpin/sync/pull/push/merge/notes list-add-clear/files inspect/cancel; unknown or duplicate rows fail; terminal operations expose Cancel only through a typed available state; post-commit/finished/non-cancellable results remain distinguishable.</acceptance_criteria>
  <verify><automated>./node_modules/.bin/vitest run tests/service/web-workflow-contract.test.ts</automated></verify>
  <done>Downstream packages share one strict action and cancellation vocabulary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define bounded notes and path-free file-status projections</name>
  <files>packages/protocol/src/web.ts, tests/service/web-workflow-contract.test.ts</files>
  <read_first>packages/protocol/src/web.ts, packages/core/src/notes.ts, packages/core/src/workspace-file-status.ts, .planning/phases/126-web-workflow-and-forge-source-parity/126-UI-SPEC.md</read_first>
  <behavior>Notes are newest-first bounded plain-text records with workspace and revision; add/clear carry expected revisions; file detail exposes logical IDs/targets, type/state/severity/count/reason only; path-bearing and raw-error fields are schema-invalid.</behavior>
  <action>Add strict request/response schemas for note list/add/clear and sanitized lazy file-status detail. Bound record counts and UTF-8 bytes, enumerate shared healthy/missing/pullable/pushable/diverged/error states and safe reason codes, and exclude rich-core `root`, `mainPath`, `taskPath`, warnings/errors, verbose diff arrays, and arbitrary hints. Encode retryable typed read/mutation errors without raw nested causes.</action>
  <acceptance_criteria>Canary objects containing absolute POSIX/Windows paths, raw errors, config roots, or extra diff arrays fail strict parsing; valid zero/one/many note and file groups round-trip; malformed notes/file entries cannot mutate or masquerade as empty.</acceptance_criteria>
  <verify><automated>./node_modules/.bin/vitest run tests/service/web-workflow-contract.test.ts && npm run typecheck --workspace @git-stacks/protocol</automated></verify>
  <done>Both clients can consume one bounded notes/file vocabulary without importing rich core models.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Define resolve-review-submit and typed forge failures</name>
  <files>packages/protocol/src/service.ts, packages/protocol/src/web.ts, tests/service/web-workflow-contract.test.ts</files>
  <read_first>packages/protocol/src/service.ts, packages/protocol/src/web.ts, packages/core/src/integrations/forge-source.ts, packages/core/src/workspace-source.ts, .planning/phases/126-web-workflow-and-forge-source-parity/126-RESEARCH.md</read_first>
  <behavior>Resolve accepts one full URL only; safe result anchors provider/change/repo/head/base/fork identity plus opaque token/expiry/revision/confidence; submit carries token, expected revision, and complete editable draft; typed failures cover every SOURCE-04 class without raw provider authority.</behavior>
  <action>Add strict GitHub-PR/GitLab-MR provider terminology, safe source identity, candidate/template/repository/branch draft, review token, resolve request/response, reviewed-create request, and failure-detail schemas. Explicitly exclude Gitea in this new contract and reject URL credentials, raw CLI JSON/stderr, clone URLs, paths, refspec commands, fetch coordinates, client-selected provider commands, and mutable immutable-source fields. Make resolve and reviewed create distinct route/mutation kinds.</action>
  <acceptance_criteria>Resolve bodies cannot contain a creation request; reviewed-create cannot parse without token/revision/complete draft; opaque token bounds and provider terminology are enforced; every malformed/unsupported/tool/auth/closed/match/fork/branch/stale/expired/source-changed failure has a bounded typed code and recovery enum.</acceptance_criteria>
  <verify><automated>./node_modules/.bin/vitest run tests/service/web-workflow-contract.test.ts && npm run typecheck --workspace @git-stacks/protocol</automated></verify>
  <done>Resolver, review UI, and service authority can proceed without inventing transport fields.</done>
</task>

</tasks>

<threat_model>
ASVS L1; block on high. T-126-01 (information disclosure, high): strict allowlists reject paths, credentials, raw provider output, clone/fetch data, and arbitrary errors. T-126-02 (tampering, high): discriminated resolve/review/create unions prevent unresolved or client-authored source authority. T-126-03 (replay, high): token/revision/idempotency fields are mandatory protocol inputs, with enforcement owned by Plan 04.
</threat_model>

<verification>Run the strict schema suite and protocol typecheck; inspect exports for no core/service/client dependency.</verification>

<success_criteria>
- All nine phase requirements map to strict, bounded protocol primitives.
- Resolve, review, create, cancellation, notes, and file status are unambiguous and path-free.
- Phase 123 lifecycle and Phase 125 shortcut contracts remain compatible.
</success_criteria>

## Artifacts this phase produces

- Canonical `WebWorkspaceActionId`, action availability/confirmation/subject schemas and operation cancellation view/result types.
- Bounded web notes and sanitized file-status request/response schemas.
- GitHub/GitLab forge resolve, safe review draft/token, reviewed-create, and typed failure schemas.
- `tests/service/web-workflow-contract.test.ts`.

<output>Create `126-01-SUMMARY.md` when complete.</output>
