---
phase: 126-web-workflow-and-forge-source-parity
plan: "02"
type: execute
wave: 2
depends_on: ["126-01"]
files_modified:
  - packages/core/src/integrations/forge-source.ts
  - packages/core/src/integrations/forge-source-resolver.ts
  - packages/core/src/workspace-source.ts
  - packages/core/src/index.ts
  - tests/lib/forge-source-resolver.test.ts
  - tests/lib/workspace-source.test.ts
autonomous: true
requirements: [SOURCE-01, SOURCE-02, SOURCE-04]
must_haves:
  truths:
    - "GitHub PR and GitLab MR URLs resolve through injected argv execution into real provider source repository, branch, ref, and immutable head SHA metadata."
    - "Fork/source-project resolution never assumes origin, invents a synthetic source branch/ref, invokes a shell, or exposes trusted fetch coordinates beyond core/service."
    - "Resolve failures are typed and non-destructive; no Git ref, workspace YAML, worktree, credential, or provider output survives or crosses the core boundary."
  artifacts:
    - path: packages/core/src/integrations/forge-source-resolver.ts
      provides: "Provider-backed resolver, injected runner contract, strict provider normalization, matching, and failure taxonomy"
    - path: packages/core/src/workspace-source.ts
      provides: "Real source metadata and SHA-aware plain-Git source preparation"
    - path: tests/lib/forge-source-resolver.test.ts
      provides: "URL/argv/provider/fork/self-hosted/failure and no-synthetic-ref evidence"
  key_links:
    - from: packages/service/src/policy/forge-source-review.ts
      to: packages/core/src/integrations/forge-source-resolver.ts
      via: "Plan 04 injects resolver and stores trusted result server-side"
    - from: packages/core/src/workspace-source.ts
      to: packages/core/src/workspace-creation.ts
      via: "reviewed create reuses the existing normal YAML-last/rollback path"
---

<objective>
Replace synthetic forge-source assumptions with provider-backed, test-injected core authority.

Purpose: Reviewed creation is unsafe until the real fork/source repository, branch/ref, and head SHA are known.
Output: A core-leaf GitHub/GitLab resolver and SHA-aware plain-Git source preparation.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-web-workflow-and-forge-source-parity/126-CONTEXT.md
@.planning/phases/126-web-workflow-and-forge-source-parity/126-RESEARCH.md
@docs/forge-source-resolver.md
@packages/core/src/integrations/forge-source.ts
@packages/core/src/workspace-source.ts
@packages/core/src/integrations/runner.ts

Decision traceability: D-20: supported provider URLs; D-21: canonical real source metadata; D-26: normal plain-Git creation path; D-27: normalized editable suggestion; D-28: typed non-destructive resolver/fetch failures.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve strict GitHub/GitLab URLs with an injected bounded runner</name>
  <files>packages/core/src/integrations/forge-source.ts, packages/core/src/integrations/forge-source-resolver.ts, packages/core/src/index.ts, tests/lib/forge-source-resolver.test.ts</files>
  <read_first>packages/core/src/integrations/forge-source.ts, packages/core/src/integrations/runner.ts, packages/core/src/node-runtime.ts, packages/core/src/config.ts, docs/forge-source-resolver.md, .planning/phases/126-web-workflow-and-forge-source-parity/126-RESEARCH.md</read_first>
  <behavior>GitHub uses `gh api graphql --hostname`; GitLab uses `glab api --hostname` with encoded target project/IID then source-project lookup; configured self-hosted hosts are explicit; missing CLI/auth/not-found/closed/rate-limit/timeout/invalid-output are typed.</behavior>
  <action>Create a core-owned resolver with an injected argv runner accepting timeout/output cap/AbortSignal and never a shell string. Tighten Phase 126 parsing to GitHub pull and GitLab merge-request URLs on built-in or configured hosts; keep legacy Gitea CLI behavior outside this new resolver. Strictly parse minimal provider JSON into a trusted union with target/source identities, branches, real refs/OIDs, immutable full head SHA, target SHA, cross-repository flag, and server-only fetch coordinates. Classify failures without returning raw stdout/stderr/argv or environment.</action>
  <acceptance_criteria>Argv snapshots show explicit hostname, no shell/token argv, no `glab mr view <url>`, and no provider checkout; nested GitLab groups and configured hosts resolve; malformed/credentialed/wrong-kind/Gitea URLs fail; output/timeout/abort/auth/closed/null-head cases are typed and bounded.</acceptance_criteria>
  <verify><automated>./node_modules/.bin/vitest run tests/lib/forge-source-resolver.test.ts</automated></verify>
  <done>Core can query both supported providers for real immutable source metadata under deterministic tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement authoritative repository/template matching and safe suggestions</name>
  <files>packages/core/src/integrations/forge-source-resolver.ts, tests/lib/forge-source-resolver.test.ts</files>
  <read_first>packages/core/src/integrations/forge-source-resolver.ts, packages/core/src/integrations/forge-utils.ts, packages/core/src/config.ts, .planning/milestones/v0.18.0-phases/92-forge-source-research-and-resolver-design/92-CONTEXT.md</read_first>
  <behavior>Matching precedence is repo metadata, integration base URL, then unique bounded remote inference; ambiguous/missing/template-missing/non-worktree results fail typed; suggested names use existing normalization and never become final authority.</behavior>
  <action>Match canonical host plus repository path against enabled registry/template state with explicit metadata first. Return candidate/match/confidence and a normalized safe suggested workspace name, but no protocol import. Preserve provider terminology and immutable change identity. Do not silently choose among ambiguity or add source repos missing from the selected template.</action>
  <acceptance_criteria>Fixtures prove precedence, self-hosted identity, unique inference, ambiguity, missing template repo, trunk/dir rejection, fork source/target separation, conflict-safe suggested names, and core remains a leaf under dependency tests.</acceptance_criteria>
  <verify><automated>./node_modules/.bin/vitest run tests/lib/forge-source-resolver.test.ts && npm run test:deps</automated></verify>
  <done>Provider results bind to authoritative repository/template candidates without client policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove synthetic ref/origin assumptions from source preparation</name>
  <files>packages/core/src/workspace-source.ts, tests/lib/workspace-source.test.ts, tests/lib/forge-source-resolver.test.ts</files>
  <read_first>packages/core/src/workspace-source.ts, packages/core/src/git.ts, packages/core/src/workspace-creation.ts, tests/lib/workspace-source.test.ts, .planning/milestones/v0.18.0-phases/93-forge-source-workspace-creation/93-CONTEXT.md</read_first>
  <behavior>Preparation fetches the trusted source repository's real branch into an operation-private ref, resolves it, compares full SHA, rejects moved/mismatched heads, preserves exact-SHA branch reuse, and cleans private refs on every failure/rollback.</behavior>
  <action>Refactor trusted source preparation to accept provider-resolved source fetch coordinates/ref/head SHA rather than deriving `source/<forge>-...`, `refs/heads/...`, or unconditional `origin`. Fetch through existing plain Git helpers into a private git-stacks ref, verify byte-for-byte SHA before branch/worktree creation, retain existing branch-conflict and YAML-last rollback semantics, and expose cleanup hooks for Plan 04's reviewed-create transaction. Keep provider checkout commands absent.</action>
  <acceptance_criteria>Tests fail any synthetic ref/origin path; fork fetch uses source repository; moved head and fetched-SHA mismatch create no YAML/worktree and remove private refs; same-SHA existing branch succeeds and different-SHA branch returns typed conflict.</acceptance_criteria>
  <verify><automated>./node_modules/.bin/vitest run tests/lib/workspace-source.test.ts tests/lib/forge-source-resolver.test.ts && npm run typecheck --workspace @git-stacks/core && npm run test:deps</automated></verify>
  <done>Normal workspace creation can start only from the provider-reviewed immutable source.</done>
</task>

</tasks>

<threat_model>
ASVS L1; block on high. T-126-04 (command injection, high): argv-only runner, fixed executable/verbs, allowed host, no shell or token argv. T-126-05 (untrusted provider data, high): strict bounded JSON schemas discard raw buffers. T-126-06 (TOCTOU/fork confusion, high): real source repository/ref/full SHA replace synthetic origin assumptions and are rechecked by Plan 04.
</threat_model>

<verification>Run resolver/source suites, core typecheck, and dependency gate; scan for synthetic source patterns and provider checkout commands.</verification>

<success_criteria>
- Both supported provider URLs resolve real source metadata with injected deterministic runners.
- Forks/self-hosted hosts and repository matching are authoritative and typed.
- Source preparation is plain-Git, SHA-safe, rollback-clean, and free of synthetic ref/origin assumptions.
</success_criteria>

## Artifacts this phase produces

- `ForgeCommandRunner`, provider request/result/failure unions, `resolveForgeChangeSource()`, matching helpers, and safe-name proposal helper.
- Real-SHA/private-ref source preparation inputs and cleanup behavior.
- `tests/lib/forge-source-resolver.test.ts`.

<output>Create `126-02-SUMMARY.md` when complete.</output>
