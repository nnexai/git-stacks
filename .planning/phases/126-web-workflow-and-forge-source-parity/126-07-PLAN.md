---
phase: 126-web-workflow-and-forge-source-parity
plan: "07"
type: execute
wave: 5
depends_on: ["126-04", "126-05"]
files_modified:
  - packages/web/src/navigation.ts
  - packages/web/src/app.ts
  - packages/web/src/app.css
  - tests/service/web-workspace-actions.test.ts
  - tests/service/web-forge-review.test.ts
autonomous: true
requirements: [PARITY-01, PARITY-02, PARITY-03, PARITY-04, PARITY-05, SOURCE-01, SOURCE-03, SOURCE-04]
must_haves:
  truths:
    - "Web visible controls, context menus, and optional keyboard invokers render and invoke one canonical action descriptor with identical disabled reason, confirmation, one-shot operation, cancellation, and refresh semantics."
    - "Notes, file detail, and durable operation cards use bounded service projections, preserve terminal focus, and never expose or reconstruct host paths/provider authority."
    - "The singleton forge surface visibly separates Resolve URL from editable Review workspace and explicit Create; 320px layouts preserve every primary/recovery action without horizontal scroll."
    - "Empty/loading/error/populated/partial states retain stable context, named copy, and retry/correction; unavailable actions stay visible and focusable with reasons."
    - "Overflow/zero-one-many/long-text states use bounded cards, scrolling, grouping, wrapping, active/roving focus, and accessible full safe values."
  artifacts:
    - path: packages/web/src/app.ts
      provides: "Sole web UI owner for parity actions, operations, notes/files, and reviewed creation"
    - path: packages/web/src/navigation.ts
      provides: "Canonical descriptor invoker adapter for pointer/menu/shortcut"
    - path: packages/web/src/app.css
      provides: "UI-SPEC responsive, theme, typography, focus, operation/detail/review styling"
  key_links:
    - from: packages/web/src/app.ts
      to: packages/client/src/workspace-actions.ts
      via: "every control/menu/key resolves one callback"
    - from: packages/web/src/app.ts
      to: packages/client/src/forge-review.ts
      via: "singleton overlay renders Resolve/Review states and explicit submit"
---

<objective>
Deliver complete web parity and reviewed forge creation in the existing imperative UI.

Purpose: Make web a high-frequency control surface without moving policy, persistence, Git, forge, or operation authority into the browser.
Output: Canonical controls/menus, operation cards, notes/file dialogs, resolve/review UI, CSS, and DOM harnesses.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-web-workflow-and-forge-source-parity/126-UI-SPEC.md
@packages/web/src/navigation.ts
@packages/web/src/app.ts
@packages/web/src/app.css
@packages/web/src/overlay-controller.ts

Decision traceability: D-08: complete contextual web surface without generic palette; D-10: pointer/menu/key parity; D-11: one-shot/reconnect; D-12: safe feedback/selection; D-14: authoritative notes; D-17: lazy shared file states; D-23: editable anchored review; D-25: Resolve Enter only; D-29: distinct resolve/create status.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render one canonical action surface and durable operation region</name>
  <files>packages/web/src/navigation.ts, packages/web/src/app.ts, tests/service/web-workspace-actions.test.ts</files>
  <read_first>packages/web/src/navigation.ts, packages/web/src/app.ts, packages/web/src/overlay-controller.ts, packages/client/src/workspace-actions.ts, packages/client/src/operation-tracker.ts, .planning/phases/126-web-workflow-and-forge-source-parity/126-UI-SPEC.md</read_first>
  <behavior>Workspace actions button, row/scope context menus, direct pin, and optional shortcuts share descriptor/callback; unavailable rows are focusable with `aria-disabled`/reason and zero transport; confirmations are identical; rapid input submits once; operation cards reconnect/cancel/refresh honestly.</behavior>
  <action>Generalize the Phase 125 navigation registry adapter to canonical workspace descriptors without adding default shortcuts or turning configured commands into an app palette. Render UI-SPEC Workspace/Git/Details/Lifecycle groups and one visible Workspace actions control; keep archive/remove/force behavior unchanged. Add up to three persistent accessible operation cards plus singleton overflow dialog driven by the tracker, with pre-ID busy state, safe progress, reconnect, Cancel only when service says available, too-late copy, and authoritative refresh before unlocking/selecting successor.</action>
  <acceptance_criteria>DOM tests prove callback/confirmation identity across pointer/context/key, roving menu behavior, unavailable reason announcement/no request, duplicate click/Enter/repeat one request, reconnect zero resubmit, cancel once, terminal outcome refresh, and archived Unarchive-only behavior.</acceptance_criteria>
  <verify><automated>GIT_STACKS_KEY_STORE=file ./node_modules/.bin/vitest run tests/service/web-workspace-actions.test.ts tests/service/web-keyboard-navigation.test.ts tests/service/web-keyboard-overlays.test.ts</automated></verify>
  <done>Web actions and durable operations conform to shared authority without terminal-focus regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add singleton notes and path-free file-status details</name>
  <files>packages/web/src/app.ts, packages/web/src/app.css, tests/service/web-workspace-actions.test.ts</files>
  <read_first>packages/web/src/app.ts, packages/web/src/app.css, packages/web/src/overlay-controller.ts, packages/protocol/src/web.ts, .planning/phases/126-web-workflow-and-forge-source-parity/126-UI-SPEC.md</read_first>
  <behavior>Notes dialog lists newest-first, adds once, clears only after safe-default confirmation, refreshes authoritatively, and never persists locally; file dialog loads lazily, groups shared states/reasons/counts, retries locally, and renders no canary path.</behavior>
  <action>Implement `Workspace notes — {workspace}` and `Workspace file status — {workspace}` inside the singleton overlay controller. Use native labelled controls, composer validation/busy state, exact clear confirmation, stable list on failure, fixed safe empty/loading/error copy, collapsible file groups default-open on attention, and accessible state/severity text. Use only safe DTO fields and preserve original terminal return target through compatible overlay replacement.</action>
  <acceptance_criteria>DOM fixtures cover empty/loading/error/populated/partial/overflow/zero-one-many/long text; invalid note input sends nothing; clear default focus is Keep notes; add/clear failure does not fabricate list; POSIX/Windows/raw-error canaries never enter text/attributes/title/live regions.</acceptance_criteria>
  <verify><automated>GIT_STACKS_KEY_STORE=file ./node_modules/.bin/vitest run tests/service/web-workspace-actions.test.ts tests/service/web-presentation.test.ts</automated></verify>
  <done>Web details are authoritative, accessible, path-free, and terminal-focus safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deliver responsive Resolve URL and Review workspace overlay</name>
  <files>packages/web/src/app.ts, packages/web/src/app.css, tests/service/web-forge-review.test.ts</files>
  <read_first>packages/web/src/app.ts, packages/web/src/app.css, packages/web/src/overlay-controller.ts, packages/client/src/forge-review.ts, .planning/phases/126-web-workflow-and-forge-source-parity/126-UI-SPEC.md</read_first>
  <behavior>Create offers template/repos or PR/MR entry; Resolve Enter resolves only; Review anchors provider source and edits name/template/repos/source repo/branches; Create is sole accent and explicit/one-shot; typed failures preserve correct state; 320/375/desktop source assertions prevent overflow/clipping.</behavior>
  <action>Replace the one-step source portion of creation with a wide singleton two-step overlay driven by the shared reducer. Render the exact step indicator/copy, immutable source anchor, editable groups, field errors, `Change URL`/`Close`/`Create workspace`, and operation handoff. Trap focus at first unresolved/invalid field, consume opening/Enter events, preserve terminal return target, and keep review on correctable failure. Add sticky header/footer, bounded scroll, 14px/650 controls, theme-safe contrast, 640/375/320 stacking/wrapping, viewport-clamped menus/cards, and no horizontal overflow.</action>
  <acceptance_criteria>DOM tests prove Resolve Enter/opening event never creates, immutable source remains anchored, draft edits round-trip, rapid Create one request, stale/expired/source-changed requires resolve, correctable failure remains Review, accepted operation closes only after reconciliation, focus restores, and executable CSS/DOM assertions cover desktop/375/320 light/dark states.</acceptance_criteria>
  <verify><automated>GIT_STACKS_KEY_STORE=file ./node_modules/.bin/vitest run tests/service/web-forge-review.test.ts tests/service/web-workspace-actions.test.ts tests/service/web-keyboard-overlays.test.ts && npm run web:build && npm run typecheck --workspace @git-stacks/web</automated></verify>
  <done>Web reviewed creation is explicit, accessible, responsive, and never confused with provider resolution.</done>
</task>

</tasks>

<threat_model>
ASVS L1; block on high. T-126-20 (browser authority, high): UI imports protocol/client only and delegates all mutation/provider/file/note policy. T-126-21 (duplicate/replay, high): synchronous shared latches and operation-ID observation cover pointer/key/reconnect. T-126-22 (disclosure/XSS, high): safe DTO text enters native textContent/attributes only; no raw provider/path data or HTML injection. T-126-SC (supply chain, low): existing native DOM/CSS stack only; no UI registry/package install.
</threat_model>

<verification>Run focused web DOM/navigation suites, production web build/typecheck, and inspect 320px/source/accessibility/redaction assertions. Do not report these as live screenshot/xterm approval.</verification>

<success_criteria>
- Web exposes the full required action/detail set through one registry and operation tracker.
- Notes/file detail is bounded, lazy, accessible, and path-free.
- PR/MR creation is Resolve → Review → explicit Create and remains usable at 320px.
</success_criteria>

## Artifacts this phase produces

- Canonical web action/context-menu renderers and operation-card/overflow UI.
- Notes and sanitized file-status singleton dialog renderers.
- Forge resolve/review form renderer and responsive CSS classes/media rules.
- Web action and forge-review DOM test files.

<output>Create `126-07-SUMMARY.md` when complete.</output>
