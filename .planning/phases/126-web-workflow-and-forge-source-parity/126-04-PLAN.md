---
phase: 126-web-workflow-and-forge-source-parity
plan: "04"
type: execute
wave: 3
depends_on: ["126-01", "126-02", "126-03"]
files_modified:
  - packages/service/src/web/projection.ts
  - packages/service/src/policy/forge-source-review.ts
  - packages/service/src/secure/router.ts
  - packages/service/src/main.ts
  - tests/service/web-safe-detail.test.ts
  - tests/service/forge-source-review.test.ts
  - tests/service/web-workflow-authority.test.ts
autonomous: true
requirements: [PARITY-01, PARITY-02, PARITY-03, PARITY-04, SOURCE-01, SOURCE-02, SOURCE-03, SOURCE-04]
must_haves:
  truths:
    - "Secure routes expose canonical actions, bounded notes, path-free file status, honest operations/cancellation, and reviewed-source methods under explicit scopes."
    - "Opaque short-lived review tokens bind principal, canonical source, complete trusted resolution, catalog revision, request body, and one idempotency key; restart/expiry/reuse fail closed."
    - "Reviewed create revalidates authoritative draft membership and provider state, verifies fetched ref equals immutable head SHA, then calls only normal workspace creation."
  artifacts:
    - path: packages/service/src/policy/forge-source-review.ts
      provides: "Token store, resolve/review/revalidate/provider-recheck/SHA-safe create authority"
    - path: packages/service/src/web/projection.ts
      provides: "Allowlist action/note/file/operation/source projections and redaction guards"
    - path: packages/service/src/secure/router.ts
      provides: "Sole secure route owner for Phase 126 methods"
  key_links:
    - from: packages/service/src/secure/router.ts
      to: packages/service/src/policy/forge-source-review.ts
      via: "resolve and reviewed-create routes bind the authenticated principal"
    - from: packages/service/src/policy/forge-source-review.ts
      to: packages/core/src/workspace-source.ts
      via: "provider recheck and fetched-SHA verification precede normal creation"
---

<objective>
Expose the Phase 126 authority safely and close review-token/provider/SHA TOCTOU races.

Purpose: This is the single owner of secure/router hotspots and the browser-safe projection boundary.
Output: Scoped routes, allowlist projections, review-token authority, and adversarial security tests.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-web-workflow-and-forge-source-parity/126-RESEARCH.md
@packages/service/src/web/projection.ts
@packages/service/src/secure/router.ts
@packages/service/src/main.ts
@packages/service/src/security/tokens.ts

Decision traceability: D-03: service routing authority; D-15: scoped note capabilities; D-18: path-free allowlist; D-21: resolve without mutation; D-22: token/source/revision binding; D-24: submit-time authoritative revalidation; D-26: normal creation; D-28: safe failures/rollback.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build strict path/provider-safe projections and scoped routes</name>
  <files>packages/service/src/web/projection.ts, packages/service/src/secure/router.ts, packages/service/src/main.ts, tests/service/web-safe-detail.test.ts, tests/service/web-workflow-authority.test.ts</files>
  <read_first>packages/service/src/web/projection.ts, packages/service/src/secure/router.ts, packages/service/src/main.ts, packages/core/src/workspace-file-status.ts, packages/core/src/notes.ts, packages/protocol/src/web.ts</read_first>
  <behavior>Browser/TUI safe routes return validated action/note/file/operation DTOs; list/add/clear and lazy file detail use IDs/revisions; scopes reject unauthorized callers; raw rich-core fields and provider/system errors never serialize.</behavior>
  <action>Extend the existing projection module with allowlist constructors for canonical action inventory, bounded notes, sanitized file groups/reasons, operation/cancellation outcomes, and safe source drafts. Replace `core.workspace.files` rich browser exposure and name-based notes reads with strict routes; retain a trusted internal/core shape only where needed. Add action inventory and note mutations through router parsing, scoped methods, and main composition. Generate fixed safe messages from reason codes rather than regex-scrubbing arbitrary strings.</action>
  <acceptance_criteria>Route tests seed POSIX/Windows roots, main/task paths, warnings/errors, diff arrays, clone URLs, argv/stdout/stderr, environment, and credentials; none occur in serialized response/error/event/progress/log capture; wrong scope/principal and malformed/extra bodies fail before authority runs.</acceptance_criteria>
  <verify><automated>GIT_STACKS_KEY_STORE=file ./node_modules/.bin/vitest run tests/service/web-safe-detail.test.ts tests/service/web-workflow-authority.test.ts</automated></verify>
  <done>Both clients receive one strict safe projection and browser mode can no longer obtain rich file status.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Issue principal/revision/source-bound review tokens</name>
  <files>packages/service/src/policy/forge-source-review.ts, packages/service/src/secure/router.ts, packages/service/src/main.ts, tests/service/forge-source-review.test.ts</files>
  <read_first>packages/service/src/security/tokens.ts, packages/service/src/policy/snapshot.ts, packages/service/src/secure/router.ts, packages/core/src/integrations/forge-source-resolver.ts, packages/protocol/src/web.ts, .planning/phases/126-web-workflow-and-forge-source-parity/126-CONTEXT.md</read_first>
  <behavior>Resolve queries provider without workspace/Git mutation, returns one opaque high-entropy expiring token and safe draft; token record holds trusted metadata server-side and binds principal/catalog/source; URL change/restart/expiry/wrong principal/reuse fail closed.</behavior>
  <action>Create an in-memory review authority with injectable clock/random/resolver and bounded TTL. On resolve, rebuild authoritative catalog, resolve provider/match, and store complete trusted source/fetch data server-side keyed by 256-bit opaque token. Project only safe identity/draft/candidates/confidence/expiry/revision. Bind the token to authenticated principal and canonical source; service restart naturally invalidates it. Keep validation-correctable review state separate from token authority.</action>
  <acceptance_criteria>Entropy/opacity fixtures, expiry boundaries, restart, wrong principal, wrong source/body, stale catalog, already-bound idempotency key, and duplicate resolve all behave deterministically; resolve creates no refs/YAML/worktrees and response contains no fetch coordinate.</acceptance_criteria>
  <verify><automated>GIT_STACKS_KEY_STORE=file ./node_modules/.bin/vitest run tests/service/forge-source-review.test.ts</automated></verify>
  <done>Safe editable review state exists without transferring provider or filesystem authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Revalidate reviewed creation and close provider-to-fetch SHA races</name>
  <files>packages/service/src/policy/forge-source-review.ts, packages/service/src/secure/router.ts, packages/service/src/main.ts, tests/service/forge-source-review.test.ts</files>
  <read_first>packages/service/src/policy/forge-source-review.ts, packages/core/src/workspace-source.ts, packages/core/src/workspace-creation.ts, packages/service/src/policy/operations.ts, packages/service/src/secure/router.ts, .planning/phases/126-web-workflow-and-forge-source-parity/126-RESEARCH.md</read_first>
  <behavior>Submit reserves token for one idempotency/body, revalidates name/template/repositories/modes/branches/current revision, re-queries provider identity/open/head SHA, fetches real source, compares SHA, then starts one normal creation operation; every rejection is non-destructive.</behavior>
  <action>Implement reviewed-create admission in the review authority and route it through the existing operation registry/workspace-create adapter. Atomically reserve token and bind canonical request hash/idempotency key. Rebuild current config, validate the complete edited draft, re-resolve provider, require unchanged provider/host/repo/change/source project/ref/full head SHA, then invoke Plan 02 private-ref fetch and SHA verification. Commit source prep only immediately before normal workspace creation; on provider movement, fetch mismatch, branch conflict, cancellation, or create rollback, delete private refs and preserve only safe correctable draft fields when allowed.</action>
  <acceptance_criteria>TOCTOU fixtures move head before recheck and between recheck/fetch; both return `source_changed`, create no YAML/worktree, and remove private refs. Token/body/principal/revision/reuse races fail. Same-SHA branch reuse succeeds; mismatch/fork access failure is typed; provider checkout commands are never invoked.</acceptance_criteria>
  <verify><automated>GIT_STACKS_KEY_STORE=file ./node_modules/.bin/vitest run tests/service/forge-source-review.test.ts tests/service/web-safe-detail.test.ts tests/service/web-workflow-authority.test.ts && npm run typecheck --workspace @git-stacks/service && npm run test:deps</automated></verify>
  <done>Only a current reviewed provider head can enter normal workspace creation exactly once.</done>
</task>

</tasks>

<threat_model>
ASVS L1; block on high. T-126-10 (information disclosure, high): allowlist projection plus protocol revalidation excludes paths, credentials, clone/fetch data, raw provider and host errors. T-126-11 (token replay/elevation, high): high-entropy TTL tokens bind principal/source/revision/body/idempotency and are atomically reserved. T-126-12 (TOCTOU, high): provider recheck and fetched-ref full-SHA equality precede commit/normal creation. T-126-13 (authorization, high): explicit secure scopes and principal-aware operation ownership gate every route.
</threat_model>

<verification>Run all secure route/redaction/token/TOCTOU suites, service typecheck, dependency gate, and inspect browser serialization for canaries.</verification>

<success_criteria>
- Secure routes are strict, scoped, and return only safe DTOs.
- Review tokens are opaque, short-lived, principal/revision/source/body bound, and restart-safe.
- Reviewed create closes provider/catalog/fetch races and uses only normal creation with cleanup.
</success_criteria>

## Artifacts this phase produces

- `ForgeSourceReviewAuthority`, review token record/store, resolve and reviewed-create admission methods.
- Safe action/note/file/operation/source projection helpers and route handlers.
- Token/principal/revision/provider/SHA TOCTOU and redaction suites.

<output>Create `126-04-SUMMARY.md` when complete.</output>
