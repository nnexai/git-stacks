---
phase: 126-web-workflow-and-forge-source-parity
plan: "03"
type: execute
wave: 2
depends_on: ["126-01"]
files_modified:
  - packages/service/src/policy/core-contract.ts
  - packages/service/src/policy/operations.ts
  - packages/service/src/policy/workspace-actions.ts
  - packages/core/src/notes.ts
  - tests/lib/service/workspace-action-authority.test.ts
  - tests/lib/service/operations.test.ts
autonomous: true
requirements: [PARITY-01, PARITY-02, PARITY-03, PARITY-04]
must_haves:
  truths:
    - "Service-calculated action availability and disabled reasons wrap existing core semantics; no browser/TUI lifecycle, Git, notes, or file policy variant exists."
    - "Pull, notes Add/Clear, and all applicable mutations have one durable operation identity, one-shot idempotency, safe progress/results, and authoritative refresh after every terminal outcome."
    - "Cancellation availability is declared per execution and becomes too-late at an explicit commit boundary; cancel never claims rollback or silently replaces intent."
  artifacts:
    - path: packages/service/src/policy/workspace-actions.ts
      provides: "Canonical authoritative action availability/confirmation projection"
    - path: packages/service/src/policy/operations.ts
      provides: "Pull/notes adapters and serialized honest cancellation outcomes"
    - path: tests/lib/service/workspace-action-authority.test.ts
      provides: "Parity, disabled-reason, one-shot, notes, cancellation, and refresh evidence"
  key_links:
    - from: packages/service/src/secure/router.ts
      to: packages/service/src/policy/workspace-actions.ts
      via: "Plan 04 projects the sole action authority to both clients"
    - from: packages/service/src/policy/operations.ts
      to: packages/core/src/workspace-git.ts
      via: "Pull reuses existing core pullWorkspace behavior"
---

<objective>
Build canonical service action authority and honest durable operation semantics in parallel with the provider resolver.

Purpose: Both clients need the same availability, notes/Git adapters, progress, cancellation, and refresh truth.
Output: Service policy modules, operation extensions, and race-heavy authority tests.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-web-workflow-and-forge-source-parity/126-CONTEXT.md
@.planning/phases/123-archived-workspaces-and-safe-removal/123-CONTEXT.md
@packages/service/src/policy/core-contract.ts
@packages/service/src/policy/operations.ts
@packages/core/src/notes.ts
@packages/core/src/workspace-git.ts

Decision traceability: D-03: core/service mutation authority; D-04: authoritative reasons; D-05: reuse existing lifecycle/Git semantics; D-06: durable operation and refresh; D-07: honest cancellation; D-09: TUI parity inputs; D-14: append-only notes; D-15: typed list/add/clear and refresh; D-16: notes remain separate operator memory; D-17: service-computed lazy file status; D-19: one sanitized projection.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project canonical action availability and confirmation policy</name>
  <files>packages/service/src/policy/workspace-actions.ts, packages/service/src/policy/core-contract.ts, tests/lib/service/workspace-action-authority.test.ts</files>
  <read_first>packages/service/src/policy/core-contract.ts, packages/service/src/policy/snapshot.ts, packages/service/src/policy/workspace-lifecycle.ts, packages/core/src/workspace-git.ts, packages/core/src/workspace-pins.ts, .planning/phases/126-web-workflow-and-forge-source-parity/126-CONTEXT.md</read_first>
  <behavior>Every relevant active/archived workspace gets stable action descriptors; unavailable relevant actions remain present with typed reason; lifecycle confirmations reuse Phase 123; in-flight operations disable conflicting intent; Force Remove exists only after fresh typed dirty eligibility.</behavior>
  <action>Create a service policy that derives protocol-independent canonical action availability/disabled reason/confirmation from authoritative catalog, repo capabilities, lifecycle inspection, pins, and current operations. Reuse core/service guards for archive/unarchive/remove/force/rename/open/close/sync/pull/push/merge and expose notes/files read actions. Do not inspect client UI state or hide unavailable relevant actions; archived rows remain Unarchive-only at projection time.</action>
  <acceptance_criteria>Table tests cover active/archived/dirty/stale/missing/non-worktree/no-remote/in-flight/no-capability states; exact-name Force Remove cannot arise from non-dirty failure; one descriptor/confirmation result is identical for web and TUI consumers.</acceptance_criteria>
  <verify><automated>./node_modules/.bin/vitest run tests/lib/service/workspace-action-authority.test.ts</automated></verify>
  <done>One service policy owns action availability and confirmation semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Pull and authoritative notes mutation adapters</name>
  <files>packages/service/src/policy/core-contract.ts, packages/service/src/policy/operations.ts, packages/core/src/notes.ts, tests/lib/service/workspace-action-authority.test.ts</files>
  <read_first>packages/service/src/policy/operations.ts, packages/service/src/policy/core-contract.ts, packages/core/src/workspace-git.ts, packages/core/src/notes.ts, .planning/milestones/v0.19.0-phases/96-workspace-notes/96-CONTEXT.md</read_first>
  <behavior>Pull reports per-repo safe progress through existing core behavior; note list is newest-first with revision; Add/Clear are durable/idempotent/revision-bound; malformed JSONL fails without append/delete; Clear requires canonical confirmation upstream.</behavior>
  <action>Add `workspace.pull`, `workspace.notes.add`, and `workspace.notes.clear` core/service mutation schemas and operation adapters. Give notes a deterministic fingerprint/revision without changing JSONL record semantics, limit list projection at service boundary, and refresh notes/snapshot summaries after success or failure. Preserve existing sync/push/merge/open/close semantics and avoid emitting notes as signals or YAML fields.</action>
  <acceptance_criteria>Tests prove Pull uses `pullWorkspace`, note Add/Clear never mutate malformed storage or stale revision, idempotent replay cannot duplicate a note, newest-first list refreshes, and neither notes nor Pull leak raw paths/errors in safe results.</acceptance_criteria>
  <verify><automated>./node_modules/.bin/vitest run tests/lib/service/workspace-action-authority.test.ts tests/lib/service/operations.test.ts</automated></verify>
  <done>Missing high-frequency operations use core authority and durable service semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make cancellation outcomes serialized, explicit, and honest</name>
  <files>packages/service/src/policy/operations.ts, tests/lib/service/operations.test.ts, tests/lib/service/workspace-action-authority.test.ts</files>
  <read_first>packages/service/src/policy/operations.ts, packages/protocol/src/service.ts, tests/lib/service/operations.test.ts, .planning/phases/126-web-workflow-and-forge-source-parity/126-RESEARCH.md</read_first>
  <behavior>Executions declare safe-boundaries or none; cancel under registry serialization returns requested/already-finished/too-late/not-cancellable; commit is visible before irreversible work; duplicate cancel is one outcome; terminal refresh occurs regardless of result.</behavior>
  <action>Extend `OperationExecution` and registry cancellation state with explicit capability and commit visibility. Decide cancellation under the same serialization queue as operation transitions. Only AbortSignal-aware safe-boundary steps advertise availability; operations without real cancellation remain not-cancellable. Preserve operation terminal state as truth and make consumers re-read/refresh rather than infer rollback from request acceptance.</action>
  <acceptance_criteria>Deferred race tests cover before start, during safe work, after commit, after success/failure/cancel, duplicate request, non-cancellable work, and restart; no test observes Cancel available after commit or a cancellation result claiming undone effects.</acceptance_criteria>
  <verify><automated>./node_modules/.bin/vitest run tests/lib/service/operations.test.ts tests/lib/service/workspace-action-authority.test.ts && npm run typecheck --workspace @git-stacks/service</automated></verify>
  <done>Clients can expose Cancel only when the service can still honor it.</done>
</task>

</tasks>

<threat_model>
ASVS L1; block on high. T-126-07 (client policy elevation, high): action availability derives only from authoritative state/operations. T-126-08 (replay/race, high): idempotency plus synchronous registry serialization and explicit commit boundary prevent duplicate/late cancellation claims. T-126-09 (storage tampering, high): malformed note JSONL and stale fingerprints fail before mutation.
</threat_model>

<verification>Run focused action/operation suites and service typecheck; verify no client imports or second lifecycle/Git policy implementation.</verification>

<success_criteria>
- All parity mutations and details have canonical availability, disabled reason, confirmation, and service authority.
- Pull and notes mutations are durable, idempotent, and refresh authoritative state.
- Cancellation is accurately available and returns race-specific outcomes without rollback claims.
</success_criteria>

## Artifacts this phase produces

- `workspace-actions.ts` action availability/confirmation policy and reason enums/mappers.
- Pull and note mutation adapters plus note revision helper.
- Operation cancellation declaration/result and serialized commit-boundary behavior.
- `tests/lib/service/workspace-action-authority.test.ts`.

<output>Create `126-03-SUMMARY.md` when complete.</output>
