---
phase: 126-web-workflow-and-forge-source-parity
plan: "05"
type: execute
wave: 4
depends_on: ["126-01", "126-03", "126-04"]
files_modified:
  - packages/client/src/workspace-actions.ts
  - packages/client/src/operation-tracker.ts
  - packages/client/src/forge-review.ts
  - packages/client/src/index.ts
  - tests/lib/client-workspace-actions.test.ts
  - tests/lib/client-operation-tracker.test.ts
  - tests/lib/client-forge-review.test.ts
autonomous: true
requirements: [PARITY-04, PARITY-05, SOURCE-01, SOURCE-03, SOURCE-04]
must_haves:
  truths:
    - "One pure shared action registry combines authoritative descriptors with surface callbacks; pointer, menu, and optional keyboard invokers cannot create a second operation path."
    - "A synchronous one-shot latch precedes every await; reconnect observes a known operation ID only, cancel sends once only while authoritative state allows it, and every terminal outcome requires refresh."
    - "Resolve, editable Review, and explicit Create are separate client states; token/revision/immutable identity remain anchored and client validation never becomes authority."
  artifacts:
    - path: packages/client/src/workspace-actions.ts
      provides: "Pure action descriptor registry, invoker resolution, confirmation and one-shot coordination"
    - path: packages/client/src/operation-tracker.ts
      provides: "Durable operation observation/reconnect/cancel/refresh state machine"
    - path: packages/client/src/forge-review.ts
      provides: "Resolve-review-create state reducer and editable/immutable draft separation"
  key_links:
    - from: packages/web/src/app.ts
      to: packages/client/src/workspace-actions.ts
      via: "Plan 07 renders and invokes the same descriptor callbacks"
    - from: packages/tui/src/App.tsx
      to: packages/client/src/operation-tracker.ts
      via: "Plan 06 consumes identical durable operation semantics"
---

<objective>
Create the pure shared coordination layer used by both clients.

Purpose: Parity must be executable outside DOM/OpenTUI and invokers must share one callback, one-shot, operation, and review state model.
Output: Three client modules, public exports, and exhaustive deterministic tests.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-web-workflow-and-forge-source-parity/126-CONTEXT.md
@packages/client/src/shortcuts.ts
@packages/client/src/reducer.ts
@packages/client/src/index.ts
@packages/web/src/navigation.ts

Decision traceability: D-02: shortcut is optional invoker; D-10: every invoker shares policy; D-11: one-shot/reconnect observation; D-12: named feedback and authoritative selection; D-23: editable review with immutable anchor; D-24: draft is intent only; D-25: explicit reviewed Create; D-29: resolution and creation states remain distinct.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalize one shared action descriptor registry</name>
  <files>packages/client/src/workspace-actions.ts, packages/client/src/index.ts, tests/lib/client-workspace-actions.test.ts</files>
  <read_first>packages/client/src/shortcuts.ts, packages/web/src/navigation.ts, packages/protocol/src/web.ts, .planning/phases/125-terminal-safe-keyboard-navigation/125-CONTEXT.md, .planning/phases/126-web-workflow-and-forge-source-parity/126-CONTEXT.md</read_first>
  <behavior>Registry preserves canonical ID/label/subject/availability/reason/confirmation/pending/cancellation and one callback; every pointer/menu/key invoker resolves by ID; unavailable invocation reports reason without transport; repeated pending activation submits once.</behavior>
  <action>Add a pure `createWorkspaceActionRegistry` that accepts authoritative descriptors and a typed callback map, overlays only surface-local availability (exclusive dialog/busy) without changing policy, and returns stable render/invoke entries. Keep Phase 125 shortcut registry as an optional invoker adapter rather than expanding default global bindings. Acquire a synchronous per-subject/action latch before confirmation/transport await and release only after operation identity or terminal failure handling.</action>
  <acceptance_criteria>Tests prove full canonical inventory, one callback identity across invoker kinds, same confirmation/disabled reason, unavailable zero transport, rapid pointer/Enter/key repeat one invocation, and no DOM/OpenTUI/core imports.</acceptance_criteria>
  <verify><automated>./node_modules/.bin/vitest run tests/lib/client-workspace-actions.test.ts</automated></verify>
  <done>Both renderers can expose different placements without owning different operations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Track durable operations, reconnect, cancellation, and refresh</name>
  <files>packages/client/src/operation-tracker.ts, packages/client/src/index.ts, tests/lib/client-operation-tracker.test.ts</files>
  <read_first>packages/client/src/reducer.ts, packages/client/src/event-subscription.ts, packages/protocol/src/web.ts, packages/service/src/policy/operations.ts, .planning/phases/126-web-workflow-and-forge-source-parity/126-UI-SPEC.md</read_first>
  <behavior>Pre-ID submit is one-shot; post-ID reconnect calls get/subscribe only; cancel appears/sends once only in available state; too-late/non-cancellable preserve observation; success/failure/cancel all transition through refresh before unlock.</behavior>
  <action>Implement a pure operation tracker/coordinator with explicit submitting/observing/reconnecting/terminal/refreshing states, known operation ID retention, bounded operation cards, cancel-once latch, and callback hooks for transport and authoritative refresh. Never resubmit original intent on transport ambiguity. Preserve safe action/workspace/repository detail and coalesce progress without inferring rollback or completion from cancel response.</action>
  <acceptance_criteria>Deferred tests cover dropped submit response versus known-ID reconnect, repeated event delivery, cancel race outcomes, refresh failure lock/retry, three-card overflow, and selection reconciliation request on all terminal states; original submit count remains one.</acceptance_criteria>
  <verify><automated>./node_modules/.bin/vitest run tests/lib/client-operation-tracker.test.ts</automated></verify>
  <done>Web and TUI share one non-replay and honest cancellation state machine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Model resolve-review-create with immutable source anchoring</name>
  <files>packages/client/src/forge-review.ts, packages/client/src/index.ts, tests/lib/client-forge-review.test.ts</files>
  <read_first>packages/protocol/src/web.ts, .planning/phases/126-web-workflow-and-forge-source-parity/126-UI-SPEC.md, .planning/phases/126-web-workflow-and-forge-source-parity/126-CONTEXT.md</read_first>
  <behavior>Enter in Resolve emits resolve only; successful resolution enters Review; editable name/template/repos/source selection/branches validate locally but immutable provider identity/token/revision remain unchanged; Create is explicit/one-shot; stale/expired/source-changed returns safely to Resolve while correctable errors retain Review.</behavior>
  <action>Create a pure reducer/coordinator separating URL/resolving/error, reviewed editable draft, immutable source anchor, submitting, and accepted-operation states. Model field validation only as user feedback; always send complete draft/token/expected revision to service. Invalidate token on URL change. Distinguish resolve success from creation success and define recovery mapping from typed server failures without parsing messages.</action>
  <acceptance_criteria>Tests prove resolve Enter cannot create, the opening event cannot leak into Create, immutable fields cannot be changed through reducer actions, rapid Create submits once, stale/expired/source-changed preserve URL and require re-resolve, validation-correctable failures preserve safe edits, and success waits for operation/snapshot reconciliation.</acceptance_criteria>
  <verify><automated>./node_modules/.bin/vitest run tests/lib/client-forge-review.test.ts tests/lib/client-workspace-actions.test.ts tests/lib/client-operation-tracker.test.ts && npm run typecheck --workspace @git-stacks/client && npm run test:deps</automated></verify>
  <done>Both clients can render the same safe reviewed-creation state machine.</done>
</task>

</tasks>

<threat_model>
ASVS L1; block on high. T-126-14 (duplicate/replay, high): synchronous latch and known-operation observation prevent repeat/reconnect resubmission. T-126-15 (client authority, high): reducers consume typed projections and never import core/provider/filesystem modules. T-126-16 (draft tampering, high): immutable identity/token/revision remain separate and every complete draft is revalidated server-side.
</threat_model>

<verification>Run all client suites, client typecheck, and dependency gate; scan modules for DOM/OpenTUI/core/process/filesystem imports.</verification>

<success_criteria>
- Every invoker shares one descriptor and callback.
- Durable operation and cancel/reconnect behavior is one-shot and refresh-gated.
- Resolve and reviewed Create are explicit, typed, editable where allowed, and never authoritative in the client.
</success_criteria>

## Artifacts this phase produces

- `WorkspaceActionDescriptor`, registry/invocation coordinator, and optional shortcut adapter.
- `OperationTracker` state/actions and operation-card projection helpers.
- Forge review state/reducer/validation/recovery helpers.
- Three focused client test files.

<output>Create `126-05-SUMMARY.md` when complete.</output>
