---
phase: 126-web-workflow-and-forge-source-parity
plan: "06"
type: execute
wave: 5
depends_on: ["126-04", "126-05"]
files_modified:
  - packages/tui/src/App.tsx
  - packages/tui/src/ActionMenu.tsx
  - packages/tui/src/WorkspaceDetail.tsx
  - packages/tui/src/WorkspaceOperationView.tsx
  - packages/tui/src/WorkspaceNotesDialog.tsx
  - packages/tui/src/WorkspaceFileStatusDialog.tsx
  - packages/tui/src/ForgeSourceReviewDialog.tsx
  - tests/tui/dashboard/WorkspaceParity.test.tsx
  - tests/tui/dashboard/ForgeSourceReview.test.tsx
autonomous: true
requirements: [PARITY-01, PARITY-02, PARITY-03, PARITY-04, PARITY-05, SOURCE-01, SOURCE-03, SOURCE-04]
must_haves:
  truths:
    - "OpenTUI adds Pull, Pin/Unpin, Notes, File status, and Cancel while adapting existing actions to the same canonical descriptors, confirmations, one-shot tracker, and authoritative refresh."
    - "Notes and file detail consume the same bounded path-free service DTOs as web; no TUI-only rich policy or core notes/file status shape remains in these surfaces."
    - "Create from PR/MR URL visibly separates Resolve from editable Review and explicit Create; narrow/short terminals remain navigable and never accept blind creation."
    - "Empty/loading/error/populated/partial states retain stable frames and written next actions; unavailable rows remain visible with reasons."
    - "Overflow/zero-one-many/long-text states scroll, count, wrap or safely ellipsize while headers, errors, and key hints remain visible."
  artifacts:
    - path: packages/tui/src/ActionMenu.tsx
      provides: "Canonical grouped action rows with disabled reasons and stable keys"
    - path: packages/tui/src/WorkspaceOperationView.tsx
      provides: "Unified durable progress/reconnect/cancel/result frame"
    - path: packages/tui/src/ForgeSourceReviewDialog.tsx
      provides: "Resolve and editable reviewed-create OpenTUI flow"
  key_links:
    - from: packages/tui/src/App.tsx
      to: packages/client/src/workspace-actions.ts
      via: "all action-menu/direct/key invokers resolve one descriptor"
    - from: packages/tui/src/ForgeSourceReviewDialog.tsx
      to: packages/client/src/forge-review.ts
      via: "contained keys render the shared state machine"
---

<objective>
Deliver deterministic OpenTUI parity on the shared authority and coordination layers.

Purpose: Close Pull/Pin/Cancel/notes/source gaps without adding local domain authority or divergent progress behavior.
Output: Grouped menus, unified operation/detail/review surfaces, narrow layouts, and render/key tests.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-web-workflow-and-forge-source-parity/126-UI-SPEC.md
@packages/tui/src/App.tsx
@packages/tui/src/ActionMenu.tsx
@packages/tui/src/WorkspaceDetail.tsx
@packages/tui/src/CenteredDialog.tsx

Decision traceability: D-09: Pull/Pin/Cancel TUI gaps; D-10: shared invokers; D-12: named feedback/selection; D-14: notes surface; D-15: confirmed clear/refreshed list; D-17: lazy shared file states; D-19: sanitized TUI projection; D-23: editable anchored review; D-25: Resolve Enter only; D-29: separate resolve/create status.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route all TUI workspace actions through canonical descriptors and operation tracker</name>
  <files>packages/tui/src/App.tsx, packages/tui/src/ActionMenu.tsx, packages/tui/src/WorkspaceOperationView.tsx, tests/tui/dashboard/WorkspaceParity.test.tsx</files>
  <read_first>packages/tui/src/App.tsx, packages/tui/src/ActionMenu.tsx, packages/tui/src/ProgressView.tsx, packages/tui/src/SyncProgressView.tsx, packages/client/src/workspace-actions.ts, packages/client/src/operation-tracker.ts, .planning/phases/126-web-workflow-and-forge-source-parity/126-UI-SPEC.md</read_first>
  <behavior>Grouped rows include all required actions and disabled reasons; Pull/Pin/Unpin exist; existing archive/remove/force/rename/open/close/sync/push/merge use same descriptor; pointer/letter/Enter share callback; repeated keys submit once; `[c]` appears/sends once only while cancellable.</behavior>
  <action>Adapt `ActionMenu` and App routing to the shared descriptor registry, preserving established keys where possible and keeping unavailable rows selectable for reason announcement. Add Pull and direct Pin/Unpin. Replace per-action progress variation for these actions with one operation view driven by the tracker: named workspace/action/stage/repository rows, reconnect, honest cancellation/finishing copy, terminal result, and refresh-before-unlock. Preserve Phase 123 exact-name Force Remove and terminal-stop semantics.</action>
  <acceptance_criteria>OpenTUI key/render tests prove one inventory/callback/confirmation, unavailable zero transport, rapid key one submit, Cancel only when available and once, reconnect no resubmit, all outcomes refresh selection, and archived rows expose Unarchive only.</acceptance_criteria>
  <verify><automated>GIT_STACKS_KEY_STORE=file ./node_modules/.bin/vitest run tests/tui/dashboard/WorkspaceParity.test.tsx tests/tui/dashboard/ActionMenu.test.tsx tests/tui/dashboard/integ-workspace-archive-remove.test.tsx</automated></verify>
  <done>TUI action and progress semantics conform to the shared model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add authoritative notes and sanitized file-status dialogs</name>
  <files>packages/tui/src/WorkspaceDetail.tsx, packages/tui/src/WorkspaceNotesDialog.tsx, packages/tui/src/WorkspaceFileStatusDialog.tsx, packages/tui/src/App.tsx, tests/tui/dashboard/WorkspaceParity.test.tsx</files>
  <read_first>packages/tui/src/WorkspaceDetail.tsx, packages/tui/src/CenteredDialog.tsx, packages/tui/src/hooks/useWorkspaceFileStatus.ts, packages/protocol/src/web.ts, .planning/milestones/v0.19.0-phases/98-grounded-dashboard-control-center/98-CONTEXT.md, .planning/phases/126-web-workflow-and-forge-source-parity/126-UI-SPEC.md</read_first>
  <behavior>Notes newest-first list/add/confirmed clear uses safe DTO and refresh; file status loads lazily in workspace/repo groups with shared state/reason/count; errors retain authoritative data/retry; no host path can render; long/zero/many content remains scrollable.</behavior>
  <action>Replace rich core notes/file imports in touched detail paths with service-projected types. Keep compact summaries in `WorkspaceDetail`, and add large `CenteredDialog` list/add/clear and file-status detail surfaces with exact UI-SPEC copy/key hints. Contain input keys, reject invalid note input before transport, preserve list on add failure, never optimistically clear, and render state text/marker alongside color. Fix header/footer while scrolling bodies and report omitted rows at short heights.</action>
  <acceptance_criteria>Render/key fixtures cover empty/loading/error/populated/partial/overflow/zero-one-many/long text, malformed note failure, clear safe-default confirmation, all file states, path canaries absent, and originating row/focus restored on Back.</acceptance_criteria>
  <verify><automated>GIT_STACKS_KEY_STORE=file ./node_modules/.bin/vitest run tests/tui/dashboard/WorkspaceParity.test.tsx tests/tui/dashboard/WorkspaceDetail.test.tsx tests/tui/dashboard/snapshots/WorkspaceDetail.snap.test.tsx</automated></verify>
  <done>TUI details use the same authoritative safe records as web.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deliver contained PR/MR Resolve and Review creation flow</name>
  <files>packages/tui/src/App.tsx, packages/tui/src/ForgeSourceReviewDialog.tsx, tests/tui/dashboard/ForgeSourceReview.test.tsx</files>
  <read_first>packages/tui/src/App.tsx, packages/tui/src/WizardView.tsx, packages/tui/src/CenteredDialog.tsx, packages/client/src/forge-review.ts, .planning/phases/126-web-workflow-and-forge-source-parity/126-UI-SPEC.md</read_first>
  <behavior>Entry says Create from PR/MR URL; Enter in focused URL resolves only; Review anchors provider identity and edits name/template/repos/source repo/branches; `c` creates once only when valid; pickers restore row; <80/<56/<40x12 layouts obey contract.</behavior>
  <action>Add a create entry and contained resolver/review dialog driven entirely by the shared reducer. Render provider terminology and immutable source card first, then editable groups and inline typed errors. Consume typing/Enter/Escape so no key leaks to app handlers; require explicit `c` for Create and hand accepted operation to the unified progress view. Stack below 80 columns, safely ellipsize immutable values below 56, keep full editable values, and show the exact too-small message/back-only behavior below 40x12.</action>
  <acceptance_criteria>Tests prove Resolve Enter and opening key cannot Create, rapid `c` submits once, invalid/unresolved/expired/stale drafts cannot Create, correctable failure preserves edits, immutable identity remains anchored, and wide/narrow/too-small layouts retain title/error/footer without overlap.</acceptance_criteria>
  <verify><automated>GIT_STACKS_KEY_STORE=file ./node_modules/.bin/vitest run tests/tui/dashboard/ForgeSourceReview.test.tsx tests/tui/dashboard/WorkspaceParity.test.tsx && npm run tui:build && npm run typecheck --workspace @git-stacks/tui</automated></verify>
  <done>OpenTUI exposes the reviewed source flow with deterministic parity and narrow safety.</done>
</task>

</tasks>

<threat_model>
ASVS L1; block on high. T-126-17 (client authority, high): TUI consumes protocol/client projections and removes rich core policy from touched surfaces. T-126-18 (key leakage/replay, high): contained views consume keys and shared latches prevent duplicate submit/cancel. T-126-19 (information disclosure, high): only sanitized logical file/provider identities render; canary snapshots enforce absence.
</threat_model>

<verification>Run TUI focused render/key suites, snapshots, package build/typecheck, and scan touched files for rich core Git/notes/file/provider authority imports.</verification>

<success_criteria>
- TUI exposes the complete required parity action set and honest operation/cancel behavior.
- Notes/file detail is authoritative, lazy, path-free, and navigable at constrained sizes.
- PR/MR source creation requires Resolve, Review, explicit one-shot Create.
</success_criteria>

## Artifacts this phase produces

- `WorkspaceOperationView`, `WorkspaceNotesDialog`, `WorkspaceFileStatusDialog`, and `ForgeSourceReviewDialog` components.
- Canonical ActionMenu descriptor rows and App adapters.
- OpenTUI parity/review render and key suites.

<output>Create `126-06-SUMMARY.md` when complete.</output>
