---
phase: 126-web-workflow-and-forge-source-parity
plan: "08"
type: execute
wave: 6
depends_on: ["126-06", "126-07"]
files_modified:
  - tests/architecture/phase126-client-authority.test.mjs
  - tests/service/phase126-cross-client-conformance.test.ts
  - .planning/phases/126-web-workflow-and-forge-source-parity/126-PHASE127-HANDOFF.md
autonomous: true
requirements: [PARITY-01, PARITY-02, PARITY-03, PARITY-04, PARITY-05, SOURCE-01, SOURCE-02, SOURCE-03, SOURCE-04]
must_haves:
  truths:
    - "D-01: conformance proves one canonical action model and callback inventory."
    - "D-02: conformance proves shortcuts are optional invokers, not default action implementations."
    - "D-03: architecture gates prove core/service-only mutation authority."
    - "D-04: conformance proves authoritative availability and visible disabled reasons."
    - "D-05: regression suites preserve existing lifecycle and Git semantics."
    - "D-06: operation conformance proves one durable identity and terminal refresh."
    - "D-07: cancellation races prove service-reported availability and honest outcomes."
    - "D-08: web conformance proves the full contextual surface without a generic palette."
    - "D-09: TUI conformance proves Pull, Pin/Unpin, Cancel, and adapted existing actions."
    - "D-10: cross-invoker tests prove identical guards, confirmation, progress, errors, and refresh."
    - "D-11: one-shot and reconnect tests prove no duplicate or replayed intent."
    - "D-12: surface tests prove named safe feedback and authoritative selection reconciliation."
    - "D-13: the handoff separates deterministic coverage from Phase 127 live approval."
    - "D-14: notes tests preserve newest-first append-only list/add/confirmed-clear behavior."
    - "D-15: notes authority tests prove typed core backing, malformed safety, and refresh."
    - "D-16: architecture tests reject note signals, workspace YAML fields, and client persistence."
    - "D-17: file tests prove lazy service-computed shared states, counts, severity, and reasons."
    - "D-18: redaction canaries prove the browser-safe file projection is path-free."
    - "D-19: cross-client conformance proves TUI and web use the same sanitized projection."
    - "D-20: provider tests cover full GitHub PR and GitLab MR URLs plus configured hosts."
    - "D-21: resolver tests prove explicit no-mutation resolution and complete canonical metadata."
    - "D-22: token/redaction tests prove opaque source/revision binding and no trusted fetch disclosure."
    - "D-23: both review surfaces prove editable plans with immutable source anchors."
    - "D-24: admission tests prove complete draft/token/revision revalidation before one operation."
    - "D-25: both input harnesses prove Resolve Enter cannot create."
    - "D-26: architecture/source tests prove plain Git normal creation and no provider checkout."
    - "D-27: resolver/review tests prove normalized suggestions stay editable and non-authoritative."
    - "D-28: failure and rollback matrices prove typed non-destructive recovery."
    - "D-29: state-machine tests prove resolver and creation status remain distinct and safely recoverable."
    - "Web and TUI expose the same required action IDs, labels/reasons, confirmation policies, operation/cancellation outcomes, notes/file states, and reviewed-source transitions from shared contracts."
    - "Architecture gates reject client Git/config/notes/files/provider/process authority, rich file DTOs, path/provider leaks, synthetic refs/origin assumptions, and alternate provider checkout implementations."
    - "All UI consideration states are executable: stable empty/loading/error/populated/partial frames, bounded overflow, zero-one-many collections, and safe long-text behavior at web/TUI constrained sizes."
    - "Automated local closure records no human/live claim; Phase 127 retains authenticated hosts, live reconnect/cancel, physical keyboard/xterm, screenshots, responsive and interactive cross-client approval before tag."
  artifacts:
    - path: tests/architecture/phase126-client-authority.test.mjs
      provides: "No-client-authority, redaction, dependency, synthetic-source, and provider-checkout sentinels"
    - path: tests/service/phase126-cross-client-conformance.test.ts
      provides: "Cross-client inventory/copy/confirmation/state-machine conformance"
    - path: .planning/phases/126-web-workflow-and-forge-source-parity/126-PHASE127-HANDOFF.md
      provides: "Exact automated evidence boundary and live/hosted pre-tag checklist inputs"
  key_links:
    - from: tests/service/phase126-cross-client-conformance.test.ts
      to: packages/protocol/src/web.ts
      via: "canonical action/note/file/operation/source inventories"
    - from: .planning/phases/126-web-workflow-and-forge-source-parity/126-PHASE127-HANDOFF.md
      to: .planning/ROADMAP.md
      via: "Phase 127 manual/hosted verification consumes the handoff before any tag"
---

<objective>
Prove cross-client conformance, enforce security architecture, run full gates, and preserve an honest Phase 127 pre-tag handoff.

Purpose: Prevent two individually green clients from diverging or automated fixtures from being mistaken for live approval.
Output: Architecture/conformance sentinels, complete local gate evidence, and a durable Phase 127 checklist handoff.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-web-workflow-and-forge-source-parity/126-VALIDATION.md
@.planning/phases/126-web-workflow-and-forge-source-parity/126-UI-SPEC.md
@.planning/ROADMAP.md
@tests/architecture/package-boundaries.test.mjs
@tests/architecture/secure-browser-bundle.test.mjs

Decision traceability: D-13: deterministic coverage and Phase 127 live boundary; every other locked decision is traced in Plans 01-07 and cross-checked here.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce no-client-authority and zero disclosure architecture</name>
  <files>tests/architecture/phase126-client-authority.test.mjs</files>
  <read_first>tests/architecture/package-boundaries.test.mjs, tests/architecture/secure-browser-bundle.test.mjs, packages/web/src/app.ts, packages/tui/src/App.tsx, packages/service/src/web/projection.ts, packages/core/src/workspace-source.ts, .planning/phases/126-web-workflow-and-forge-source-parity/126-RESEARCH.md</read_first>
  <behavior>Scanner rejects core Git/config/notes/file/forge authority from client/web/TUI, direct process/fs/provider calls, rich file-status browser routing, path/provider credential fields, synthetic source refs/origin fetch, `glab mr view <url>`, and provider checkout commands; hostile fixtures prove each rule fires.</behavior>
  <action>Add executable architecture scans over source and built browser assets, using explicit allowlists for legitimate protocol/client imports. Include hostile synthetic fixtures for every sentinel so absence-only checks cannot silently break. Verify core remains a leaf and protocol/router/app hotspot ownership did not duplicate. Scan serialized schema keys/messages and client-visible operation/events for forbidden host-detail vocabulary while avoiding false positives in tests/docs.</action>
  <acceptance_criteria>Each hostile fixture fails its named sentinel; production source passes; no broad regex-only redaction is accepted as proof; `npm run test:deps` remains green and browser bundle exposes no process/fs/core/provider authority.</acceptance_criteria>
  <verify><automated>node --test tests/architecture/phase126-client-authority.test.mjs tests/architecture/package-boundaries.test.mjs tests/architecture/secure-browser-bundle.test.mjs && npm run test:deps</automated></verify>
  <done>Future drift cannot silently restore client authority, disclosure, or synthetic source assumptions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove cross-client action, state, copy, and UI-state conformance</name>
  <files>tests/service/phase126-cross-client-conformance.test.ts</files>
  <read_first>packages/protocol/src/web.ts, packages/client/src/workspace-actions.ts, packages/client/src/operation-tracker.ts, packages/client/src/forge-review.ts, packages/web/src/navigation.ts, packages/tui/src/ActionMenu.tsx, .planning/phases/126-web-workflow-and-forge-source-parity/126-UI-SPEC.md</read_first>
  <behavior>Web and TUI inventories cover every required action and identical availability/confirmation/cancel outcome; notes/file states and provider terms map consistently; Resolve/Review/Create transitions match; every UI consideration category has executable surface evidence.</behavior>
  <action>Create a conformance suite that imports shared registries/reducers plus test adapters from both surfaces rather than comparing hand-written duplicate fixtures. Assert stable action grouping/labels/reasons/confirmation, exact destructive safe defaults, one-shot and reconnect semantics, all note/file states, GitHub pull/head/base and GitLab merge/source/target terminology, and resolution-versus-creation status. Cross-reference existing DOM/OpenTUI harness cases for empty/loading/error/populated/partial/overflow/zero-one-many/long text and fail if either surface drops a category.</action>
  <acceptance_criteria>One table enumerates all PARITY/SOURCE actions/states with web and TUI evidence; deleting/renaming an action or changing one surface confirmation/copy causes failure; archived rows and Force Remove eligibility remain exact; no test asserts live screenshot/host approval.</acceptance_criteria>
  <verify><automated>GIT_STACKS_KEY_STORE=file ./node_modules/.bin/vitest run tests/service/phase126-cross-client-conformance.test.ts tests/service/web-workspace-actions.test.ts tests/service/web-forge-review.test.ts tests/tui/dashboard/WorkspaceParity.test.tsx tests/tui/dashboard/ForgeSourceReview.test.tsx</automated></verify>
  <done>Deterministic evidence proves both clients consume one contract rather than merely resembling each other.</done>
</task>

<task type="auto">
  <name>Task 3: Run complete gates and write the Phase 127 live/hosted handoff</name>
  <files>.planning/phases/126-web-workflow-and-forge-source-parity/126-PHASE127-HANDOFF.md</files>
  <read_first>.planning/phases/126-web-workflow-and-forge-source-parity/126-VALIDATION.md, .planning/phases/126-web-workflow-and-forge-source-parity/126-UI-SPEC.md, .planning/ROADMAP.md, package.json, .github/workflows/build.yml</read_first>
  <action>Run all focused Phase 126 suites, full tests, coverage, seven workspace typechecks, dependency/cycle/architecture gates, web and TUI production builds, and verify gates sequentially where artifacts are shared. Repair only phase-scoped regressions. Write `126-PHASE127-HANDOFF.md` with exact commands/results/commit, unverified live assumptions, and Phase 127 checklist: authenticated GitHub/GitLab and supported self-hosted receipts; provider auth/closed/fork/branch failures; live reconnect/progress/cancel; physical keyboard/pointer/xterm/focus; browser screenshots desktop/375/320 light/dark; interactive OpenTUI wide/narrow; cross-client copy/reason/confirmation/result; explicit stop before tag/push/publish/release.</action>
  <acceptance_criteria>All local commands exit zero with counts recorded; no skipped/fixture-only evidence is called hosted/live/human; handoff retains every UI-SPEC Phase 127 item and names tag/push/publish/release as prohibited until manual approval.</acceptance_criteria>
  <verify><automated>npm test && npm run coverage && npm run typecheck && npm run test:deps && npm run web:build && npm run tui:build && npm run verify:gates</automated></verify>
  <done>Phase 126 is locally verifiable and Phase 127 has an exact, honest pre-tag continuation point.</done>
</task>

</tasks>

<threat_model>
ASVS L1; block on high. T-126-23 (architecture regression, high): executable hostile sentinels reject client authority/disclosure/synthetic source. T-126-24 (cross-client policy drift, high): conformance imports shared contracts and both adapters. T-126-25 (false verification, medium): handoff separates deterministic local evidence from live authenticated/visual/human approval and blocks release claims.
</threat_model>

<verification>Run architecture, conformance, every focused suite, full tests/coverage/types/deps/builds/verify gates; inspect the handoff for complete Phase 127 and no release side effect.</verification>

<success_criteria>
- No client authority, unsafe projection, synthetic source, provider checkout, or duplicate action implementation survives gates.
- Both clients conform across inventory, copy/reasons, confirmations, operations, details, and reviewed creation.
- Phase 127 receives complete live/hosted/manual pre-tag work; Phase 126 stops at automated readiness.
</success_criteria>

## Artifacts this phase produces

- `tests/architecture/phase126-client-authority.test.mjs` with hostile sentinel fixtures.
- `tests/service/phase126-cross-client-conformance.test.ts` and evidence table.
- `126-PHASE127-HANDOFF.md` with local evidence and live/hosted/manual checklist.

<output>Create `126-08-SUMMARY.md` when complete.</output>
