---
phase: 89
plan: 02
type: execute
wave: 2
depends_on:
  - 89-01
files_modified:
  - src/lib/files.ts
  - src/lib/git.ts
  - tests/lib/files.test.ts
  - tests/lib/lifecycle-files-env-config-real-fixture.test.ts
autonomous: true
requirements:
  - FSYNC-02
requirements_addressed:
  - FSYNC-02
must_haves:
  truths:
    - "D-06: Relative source resolution matches current file ops: workspace-level sync resolves from workspace root and per-repo sync resolves from repo main_path."
    - "D-07: source may be relative, home-relative, or absolute."
    - "D-08: target is always relative to the destination root."
    - "D-09: Absolute targets are rejected."
    - "D-10: Missing literal sources fail loudly; zero-match globs may warn if sync retains glob support."
    - "D-11: Phase 89 refuses existing targets by default."
    - "D-12: Target exists means any filesystem entry at the target path: file, directory, symlink, or dangling symlink."
    - "D-13: Git-tracked target collisions are detected explicitly and refused."
    - "D-14: Target safety rejects absolute paths, .. traversal, empty targets, and resolved paths outside the destination root."
    - "D-15: Phase 89 does not write sync marker or drift metadata."
  artifacts:
    - path: "src/lib/files.ts"
      provides: "sync source-to-target materialization and target safety checks"
      contains: "sync"
    - path: "src/lib/git.ts"
      provides: "tracked path detection helper for repo-level sync collisions"
      contains: "ls-files"
    - path: "tests/lib/files.test.ts"
      provides: "filesystem sync materialization and safety coverage"
      contains: "files.sync"
    - path: "tests/lib/lifecycle-files-env-config-real-fixture.test.ts"
      provides: "real git tracked-collision coverage"
      contains: "git"
---

<objective>
Materialize `files.sync` sources into explicit workspace/repo targets as real files and directories while refusing unsafe, existing, or git-tracked targets.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add filesystem tests for sync materialization and target safety</name>
  <read_first>
    - tests/lib/files.test.ts
    - src/lib/files.ts
    - src/lib/config.ts
    - tests/helpers.ts
    - .planning/phases/89-files-sync-schema-and-materialization/89-CONTEXT.md
  </read_first>
  <files>tests/lib/files.test.ts</files>
  <action>Add tests for new sync behavior in `tests/lib/files.test.ts`. Cover file sync from `source: "source/file.txt"` to `target: "target/file.txt"`, directory sync from `source: "source/dir"` to `target: "target/dir"`, absolute source paths, and home-relative source paths if existing test helpers make HOME stable enough. Assert synced targets are real files/directories by checking `lstatSync(target).isSymbolicLink()` is `false`. Add failure tests for missing literal source, empty target, absolute target, `../escape` target, resolved outside-root target, existing file target, existing directory target, existing symlink target, and dangling symlink target.</action>
  <acceptance_criteria>
    - `tests/lib/files.test.ts` asserts a synced file contains the source contents at the explicit target path.
    - `tests/lib/files.test.ts` asserts a synced directory preserves a nested file.
    - `tests/lib/files.test.ts` asserts synced file and directory targets are not symbolic links.
    - `tests/lib/files.test.ts` contains failure assertions for missing source, empty target, absolute target, traversal target, outside-root target, existing file, existing directory, existing symlink, and dangling symlink.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/lib/files.test.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add real git collision coverage for tracked sync targets</name>
  <read_first>
    - tests/lib/lifecycle-files-env-config-real-fixture.test.ts
    - tests/helpers.ts
    - src/lib/files.ts
    - src/lib/git.ts
  </read_first>
  <files>tests/lib/lifecycle-files-env-config-real-fixture.test.ts</files>
  <action>Add a real temp git repo/worktree fixture that creates and commits `tracked.txt`, then attempts repo-level sync to `target: "tracked.txt"` from a separate source. Assert the result is `{ ok: false }` and the error contains `tracked target` and `tracked.txt`. Use `gitExecOptions()` or `getTestGitEnv()` from `tests/helpers.ts` for deterministic git identity. Do not mock git for this test.</action>
  <acceptance_criteria>
    - `tests/lib/lifecycle-files-env-config-real-fixture.test.ts` initializes a real git repo and commits `tracked.txt`.
    - The tracked collision test calls the public repo-level file op surface, not a private helper only.
    - The tracked collision test asserts the target file content remains unchanged after the failed sync.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/lib/lifecycle-files-env-config-real-fixture.test.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Implement sync target validation and materialization</name>
  <read_first>
    - src/lib/files.ts
    - src/lib/git.ts
    - src/lib/config.ts
    - tests/lib/files.test.ts
    - tests/lib/lifecycle-files-env-config-real-fixture.test.ts
  </read_first>
  <files>src/lib/files.ts, src/lib/git.ts</files>
  <action>In `src/lib/files.ts`, extend `mergeFiles()` to return `sync` along with `copy` and `symlink`. Add a sync-specific processor for entries with `source` and `target`. Resolve `source` using `resolveSourcePath(entry.source, sourceBaseDir)`. Resolve `target` under `destDir` with a helper that rejects empty strings, absolute paths, `..` traversal, and any normalized path outside `destDir`. Before copying, call `dstExists(targetPath)` and return an error if true. Use `cpSync(sourcePath, targetPath, { recursive: true })` after creating the parent directory. Do not call `applyEntry()` for sync. In `src/lib/git.ts`, add an exported helper such as `isGitTrackedPath(repoPath: string, relPath: string): Promise<boolean>` using `git -C <repoPath> ls-files --error-unmatch -- <relPath>` with `.quiet().nothrow()`. Use it from repo-level sync before writing the target, returning a clear error for tracked targets. Do not create sync metadata files.</action>
  <acceptance_criteria>
    - `src/lib/files.ts` has a sync-specific copy path that refuses existing targets instead of silently skipping them.
    - `src/lib/files.ts` rejects absolute, empty, traversal, and outside-root targets before filesystem writes.
    - `src/lib/files.ts` does not write marker, manifest, drift, status, or push-back metadata.
    - `src/lib/git.ts` exposes a typed tracked-path helper that uses `git ls-files --error-unmatch --`.
    - `applyFileOpsForRepo()` and `applyFileOpsForWorkspace()` process `merged.sync` after existing copy/symlink handling without changing copy/symlink behavior.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/lib/files.test.ts tests/lib/lifecycle-files-env-config-real-fixture.test.ts && bun run typecheck</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML sync paths -> filesystem writes | User-controlled `source` and `target` values cross into local filesystem operations. |
| Workspace repo config -> git tracked-state query | Repo paths and targets cross into git subprocess calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-02 | Tampering | Sync target resolution | mitigate | Reject absolute, empty, traversal, and outside-root targets before writing. |
| T-89-03 | Tampering | Git-tracked targets | mitigate | Refuse repo-level sync when `git ls-files --error-unmatch -- <target>` reports the target is tracked. |
| T-89-04 | Information Disclosure | Source path support | accept | Absolute and home-relative sources are intentional for private planning/config roots; no command output should print file contents. |
</threat_model>

<verification>
Required checks:
- `bun test tests/lib/files.test.ts`
- `bun test tests/lib/lifecycle-files-env-config-real-fixture.test.ts`
- `bun run typecheck`
</verification>

<success_criteria>
- FSYNC-02 is implemented for real file/directory materialization.
- Unsafe targets and existing targets are refused.
- Repo-level tracked target collisions are refused.
- Phase 90 sync policy and drift behavior remain unimplemented.
</success_criteria>

