---
phase: 82-template-repo-label-and-message-e2e-coverage
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/commands/template-commands.test.ts
  - tests/commands/template-consumption.test.ts
  - tests/e2e-inventory.ts
autonomous: true
requirements:
  - E2E-09
must_haves:
  truths:
    - "Template command coverage proves current CLI contracts for list/show/clone/rename/remove without drifting into wizard-only flows."
    - "Template-backed workspace creation/composition coverage proves `new --non-interactive` and downstream clone behavior preserve labels and composed config."
    - "The Phase 80 inventory maps template flows to the new suites and keeps excluded wizard flows explicit."
  artifacts:
    - path: tests/commands/template-commands.test.ts
      provides: "Focused subprocess coverage for template commands"
      min_lines: 120
    - path: tests/commands/template-consumption.test.ts
      provides: "Focused subprocess coverage for non-interactive template consumption"
      min_lines: 140
    - path: tests/e2e-inventory.ts
      provides: "Flow-level template mappings and exclusions"
      contains: "template-commands.test.ts"
  key_links:
    - from: tests/commands/template-commands.test.ts
      to: src/commands/template.ts
      via: "bun run src/index.ts template ..."
      pattern: "template (show|clone|rename|remove|list)"
    - from: tests/commands/template-consumption.test.ts
      to: src/commands/workspace.ts
      via: "bun run src/index.ts new/clone --non-interactive"
      pattern: "--non-interactive"
---

<objective>
Close the template portion of Phase 82 without violating the locked scope.

Purpose: Deliver the two required template suites — pure template commands and template-backed consumption — while blocking execution if Phase 80 / 81.1 / 81.1.1 prerequisites are not actually present on the branch.
Output: One blocking prerequisite check, two focused template E2E suites, and inline inventory mappings/exclusions.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/82-template-repo-label-and-message-e2e-coverage/82-CONTEXT.md
@.planning/phases/82-template-repo-label-and-message-e2e-coverage/82-RESEARCH.md
@.planning/phases/82-template-repo-label-and-message-e2e-coverage/82-VALIDATION.md
@.planning/phases/80-e2e-cli-harness-and-living-inventory/80-CONTEXT.md
@.planning/phases/81.1-repo-add-honors-enabled-forge-integrations/81.1-CONTEXT.md
@.planning/phases/81.1.1-minimal-non-interactive-workspace-create-and-clone-variants/81.1.1-CONTEXT.md
@tests/helpers.ts
@tests/commands/template-list.test.ts
@tests/commands/template-label.test.ts
@src/commands/template.ts
@src/commands/workspace.ts
@src/lib/workspace-lifecycle.ts

<interfaces>
From `src/commands/template.ts`:
```typescript
templateCommand.command("list").option("--label <tag>", ...).action(...)
templateCommand.command("show <template>").action(...)
templateCommand.command("clone <template> <new-name>").action(...)
templateCommand.command("rename <template> <new-name>").action(...)
templateCommand.command("remove <template>").option("--force", ...).action(...)
```

From `src/commands/workspace.ts` (Phase 81.1.1 prerequisite surface expected here):
```typescript
program.command("new [name]").option("--from <source>", ...).option("--template <name>", ...)
program.command("clone [workspace]")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify prerequisite surfaces before any Phase 82 file changes</name>
  <read_first>
    @.planning/STATE.md
    @.planning/REQUIREMENTS.md
    @.planning/phases/80-e2e-cli-harness-and-living-inventory/80-CONTEXT.md
    @.planning/phases/81.1-repo-add-honors-enabled-forge-integrations/81.1-CONTEXT.md
    @.planning/phases/81.1.1-minimal-non-interactive-workspace-create-and-clone-variants/81.1.1-CONTEXT.md
    @tests/helpers.ts
    @src/commands/repo.ts
    @src/lib/integrations/forge-utils.ts
    @src/lib/integrations/types.ts
    @src/commands/workspace.ts
  </read_first>
  <files>tests/helpers.ts, tests/e2e-inventory.ts, src/commands/repo.ts, src/lib/integrations/forge-utils.ts, src/lib/integrations/types.ts, src/commands/workspace.ts</files>
  <action>Run a blocking preflight exactly as required by the phase guidance. Confirm the Phase 80 harness/inventory surface exists (`tests/e2e-inventory.ts` plus a shared CLI subprocess helper in `tests/helpers.ts`), confirm Phase 81.1’s repo-add prerequisite is present via enabled-aware forge detection on the repo-add path, and confirm Phase 81.1.1’s `--non-interactive` create/clone surface exists in `src/commands/workspace.ts`. If any prerequisite is missing, STOP immediately and report the unmet prerequisite(s); do not create `tests/commands/template-commands.test.ts`, `tests/commands/template-consumption.test.ts`, or modify the inventory in this task or later tasks.</action>
  <acceptance_criteria>
    - `test -f tests/e2e-inventory.ts`
    - `grep -Eq 'runCli|spawnCli|runCommand' tests/helpers.ts`
    - `grep -q -- '--non-interactive' src/commands/workspace.ts`
    - `grep -Eq 'resolveEnabledGlobally|enabled.*forge|detect.*Enabled.*Forge' src/commands/repo.ts src/lib/integrations/forge-utils.ts src/lib/integrations/types.ts`
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f tests/e2e-inventory.ts &amp;&amp; grep -Eq "runCli|spawnCli|runCommand" tests/helpers.ts &amp;&amp; grep -q -- "--non-interactive" src/commands/workspace.ts &amp;&amp; grep -Eq "resolveEnabledGlobally|enabled.*forge|detect.*Enabled.*Forge" src/commands/repo.ts src/lib/integrations/forge-utils.ts src/lib/integrations/types.ts'</automated>
  </verify>
  <done>Execution halts with a clear unmet-prerequisite report when any required Phase 80 / 81.1 / 81.1.1 surface is absent; otherwise later Phase 82 template work may proceed.</done>
</task>

<task type="auto">
  <name>Task 2: Add pure template command subprocess coverage and map it in the inventory</name>
  <read_first>
    @.planning/REQUIREMENTS.md
    @.planning/phases/82-template-repo-label-and-message-e2e-coverage/82-CONTEXT.md
    @.planning/phases/82-template-repo-label-and-message-e2e-coverage/82-RESEARCH.md
    @tests/commands/template-list.test.ts
    @tests/commands/template-label.test.ts
    @tests/e2e-inventory.ts
    @src/commands/template.ts
  </read_first>
  <files>tests/commands/template-commands.test.ts, tests/e2e-inventory.ts</files>
  <action>Per D-10, D-11, D-12, D-19, D-20, and D-21, create `tests/commands/template-commands.test.ts` as the focused pure-template suite for `template list`, `template show`, `template clone`, `template rename`, `template remove --force`, and the current template-label contract already exposed by `src/commands/template.ts`. Reuse the Phase 80 harness surface instead of bespoke spawn wrappers when available. Keep assertions on current user-facing contracts and persisted YAML where needed; avoid brittle list-order snapshots. Do NOT include `template new`, `template edit`, prompt-driven remove, or failure-matrix cases. Update `tests/e2e-inventory.ts` inline so the template command flow IDs map to `template-commands.test.ts` and excluded wizard flows stay explicit.</action>
  <acceptance_criteria>
    - `test -f tests/commands/template-commands.test.ts`
    - `grep -q 'template show' tests/commands/template-commands.test.ts`
    - `grep -q 'template clone' tests/commands/template-commands.test.ts`
    - `grep -q 'template rename' tests/commands/template-commands.test.ts`
    - `grep -q 'template remove' tests/commands/template-commands.test.ts`
    - `grep -q 'template-commands.test.ts' tests/e2e-inventory.ts`
    - `grep -q 'template new' tests/e2e-inventory.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/commands/template-commands.test.ts -x</automated>
  </verify>
  <done>The repo has one focused template-command subprocess suite covering the allowed Phase 82 template command contracts, and the inventory maps those flow-level items to the new suite while keeping wizard-only flows excluded.</done>
</task>

<task type="auto">
  <name>Task 3: Add template-consumption subprocess coverage for non-interactive create/composition/clone</name>
  <read_first>
    @.planning/REQUIREMENTS.md
    @.planning/phases/82-template-repo-label-and-message-e2e-coverage/82-CONTEXT.md
    @.planning/phases/82-template-repo-label-and-message-e2e-coverage/82-RESEARCH.md
    @.planning/phases/81.1.1-minimal-non-interactive-workspace-create-and-clone-variants/81.1.1-CONTEXT.md
    @tests/e2e-inventory.ts
    @src/commands/workspace.ts
    @src/lib/workspace-lifecycle.ts
  </read_first>
  <files>tests/commands/template-consumption.test.ts, tests/e2e-inventory.ts</files>
  <action>Per D-10, D-11, D-12, D-13, D-19, D-20, and D-21, create `tests/commands/template-consumption.test.ts` for the template-backed consumption scenarios only: `new --non-interactive --from <template>`, repeatable `--template` composition, propagated labels/config in the resulting workspace YAML, and a follow-on `clone --non-interactive` assertion that the created workspace’s template-derived labels/config persist through cloning. Use fixture templates and registry/workspace state; assert resulting workspace snapshots and propagated labels/config, not prompt text. Do NOT test `template new`, local-path `--from`, or any interactive fallback path. Update `tests/e2e-inventory.ts` inline so the flow-level template consumption items point to `template-consumption.test.ts`.</action>
  <acceptance_criteria>
    - `test -f tests/commands/template-consumption.test.ts`
    - `grep -q -- 'new --non-interactive' tests/commands/template-consumption.test.ts`
    - `grep -q -- 'clone --non-interactive' tests/commands/template-consumption.test.ts`
    - `grep -q -- '--template' tests/commands/template-consumption.test.ts`
    - `grep -q 'template-consumption.test.ts' tests/e2e-inventory.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/commands/template-consumption.test.ts -x</automated>
  </verify>
  <done>The repo has a separate template-consumption suite proving non-interactive create/composition/clone behavior and the inventory maps those flow-level items to that suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → child CLI | Untrusted argv/env crosses into real CLI subprocesses |
| child CLI → config/git home | Test fixtures must not touch the developer's real config/home |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01 | T | prerequisite preflight / test harness | mitigate | Task 1 blocks execution unless the Phase 80 isolated CLI harness and inventory surface exist, preventing accidental mutation of developer config/home. |
| T-82-02 | D | template consumption subprocesses | mitigate | Use only `--non-interactive` create/clone paths and explicitly forbid wizard-driven `template new` / prompt flows in Tasks 2-3 to prevent hangs. |
| T-82-03 | T | template inventory mapping | mitigate | Update `tests/e2e-inventory.ts` inline in each task so scope exclusions (`template new`, `template edit`) remain machine-parseable and cannot silently drift. |
</threat_model>

<verification>
- Run the blocking prerequisite command first; if it fails, stop and report unmet prerequisites.
- `bun test tests/commands/template-commands.test.ts -x`
- `bun test tests/commands/template-consumption.test.ts -x`
</verification>

<success_criteria>
- Phase 82 stops cleanly instead of absorbing missing Phase 80 / 81.1 / 81.1.1 work.
- Template command coverage and template consumption coverage are split into separate focused suites.
- Inventory mappings/exclusions for the template flows are updated inline and remain machine-parseable.
</success_criteria>

<output>
After completion, create `.planning/phases/82-template-repo-label-and-message-e2e-coverage/82-01-SUMMARY.md`
</output>
