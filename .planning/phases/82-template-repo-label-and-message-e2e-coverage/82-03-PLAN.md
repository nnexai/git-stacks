---
phase: 82-template-repo-label-and-message-e2e-coverage
plan: "03"
type: execute
wave: 3
depends_on:
  - 82-02
files_modified:
  - src/lib/messages.ts
  - tests/commands/message.test.ts
  - tests/e2e-inventory.ts
autonomous: true
requirements:
  - E2E-11
must_haves:
  truths:
    - "Message CLI coverage proves send/list/clear against durable JSONL state, not live dashboard IPC."
    - "Automation-safe socket opt-out prevents unintended local dashboard traffic during tests."
    - "The inventory maps message flows to the new suite and keeps the durable-contract scope explicit."
  artifacts:
    - path: src/lib/messages.ts
      provides: "Explicit message socket opt-out seam"
      contains: "GIT_STACKS_DISABLE_MESSAGE_SOCKET"
    - path: tests/commands/message.test.ts
      provides: "Focused subprocess coverage for message send/list/clear"
      min_lines: 140
    - path: tests/e2e-inventory.ts
      provides: "Message flow mappings"
      contains: "message.test.ts"
  key_links:
    - from: src/commands/message.ts
      to: src/lib/messages.ts
      via: "appendMessage() then pushToSocket()"
      pattern: "pushToSocket"
    - from: tests/commands/message.test.ts
      to: src/lib/messages.ts
      via: "child env sets GIT_STACKS_DISABLE_MESSAGE_SOCKET=1"
      pattern: "GIT_STACKS_DISABLE_MESSAGE_SOCKET"
---

<objective>
Finish Phase 82 with the durable message CLI/file contract and an explicit automation-safe socket bypass.

Purpose: Cover `message send/list/clear` without touching real local dashboard IPC, while keeping the Phase 80 inventory aligned with the shipped message suite.
Output: One explicit socket opt-out seam, one focused message subprocess suite, and inline inventory mappings.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/82-template-repo-label-and-message-e2e-coverage/82-CONTEXT.md
@.planning/phases/82-template-repo-label-and-message-e2e-coverage/82-RESEARCH.md
@.planning/phases/82-template-repo-label-and-message-e2e-coverage/82-VALIDATION.md
@tests/lib/messages.test.ts
@tests/e2e-inventory.ts
@src/commands/message.ts
@src/lib/messages.ts

<interfaces>
From `src/lib/messages.ts`:
```typescript
export interface MessageRecord {
  workspace: string
  text: string
  from?: string
  timestamp: string
}
export async function appendMessage(...)
export async function listMessages(...)
export async function clearMessages(...)
export async function pushToSocket(record: MessageRecord): Promise<void>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an explicit automation-safe message socket opt-out</name>
  <read_first>
    @.planning/phases/82-template-repo-label-and-message-e2e-coverage/82-CONTEXT.md
    @.planning/phases/82-template-repo-label-and-message-e2e-coverage/82-RESEARCH.md
    @tests/lib/messages.test.ts
    @src/commands/message.ts
    @src/lib/messages.ts
  </read_first>
  <files>src/lib/messages.ts</files>
  <action>Per D-17 and D-18, add the explicit `GIT_STACKS_DISABLE_MESSAGE_SOCKET=1` bypass inside `src/lib/messages.ts:pushToSocket()` before any `Bun.connect()` call. Keep the seam local to the socket boundary, keep normal best-effort behavior unchanged when the env var is absent, and do not add a hidden command-layer test hook or change the socket path. This task exists to block unintended IPC to a real local dashboard during E2E execution.</action>
  <acceptance_criteria>
    - `grep -q 'GIT_STACKS_DISABLE_MESSAGE_SOCKET' src/lib/messages.ts`
    - `grep -q 'Bun.connect' src/lib/messages.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/lib/messages.test.ts -x</automated>
  </verify>
  <done>`pushToSocket()` exits early when `GIT_STACKS_DISABLE_MESSAGE_SOCKET=1` is set and otherwise preserves the existing best-effort socket behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add the focused message subprocess suite and map it in the inventory</name>
  <read_first>
    @.planning/REQUIREMENTS.md
    @.planning/phases/82-template-repo-label-and-message-e2e-coverage/82-CONTEXT.md
    @.planning/phases/82-template-repo-label-and-message-e2e-coverage/82-RESEARCH.md
    @tests/lib/messages.test.ts
    @tests/e2e-inventory.ts
    @src/commands/message.ts
    @src/lib/messages.ts
  </read_first>
  <files>tests/commands/message.test.ts, tests/e2e-inventory.ts</files>
  <action>Per D-14, D-16, D-17, D-18, D-19, D-20, and D-21, create `tests/commands/message.test.ts` as the single focused message suite. Cover `message send`, `message list`, and `message clear`, including `--workspace` / `GS_WORKSPACE_NAME` resolution, sender metadata, newest-first ordering, JSONL persistence, and missing-workspace behavior. Set `GIT_STACKS_DISABLE_MESSAGE_SOCKET=1` in every child-process env for this suite so no test can touch a real local dashboard socket. Assert only the durable CLI/file contract; do NOT add live socket delivery assertions. Update `tests/e2e-inventory.ts` inline so message flow items map to `message.test.ts` and the durable-contract-only scope is preserved.</action>
  <acceptance_criteria>
    - `test -f tests/commands/message.test.ts`
    - `grep -q 'GIT_STACKS_DISABLE_MESSAGE_SOCKET' tests/commands/message.test.ts`
    - `grep -q 'GS_WORKSPACE_NAME' tests/commands/message.test.ts`
    - `grep -q 'message clear' tests/commands/message.test.ts`
    - `grep -q 'message.test.ts' tests/e2e-inventory.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/commands/message.test.ts -x</automated>
  </verify>
  <done>The repo has one focused message subprocess suite proving the durable CLI/file contract, and the inventory maps message flows to that suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child CLI → local IPC socket | Message send can otherwise attempt to contact a real dashboard socket |
| child CLI → message JSONL files | Tests write durable notification state under isolated config dirs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-07 | I | `src/lib/messages.ts` socket boundary | mitigate | Add and use the explicit `GIT_STACKS_DISABLE_MESSAGE_SOCKET=1` guard before `Bun.connect()` so tests cannot talk to a real local dashboard. |
| T-82-08 | D | message subprocess suite | mitigate | Keep coverage on the durable CLI/file contract only; no live IPC assertions, reducing timing-sensitive hangs and flakiness. |
| T-82-09 | T | isolated message storage | mitigate | Continue using isolated config dirs/messages paths from the Phase 80 harness so JSONL writes never hit the developer's real message store. |
</threat_model>

<verification>
- `bun test tests/lib/messages.test.ts -x`
- `bun test tests/commands/message.test.ts -x`
</verification>

<success_criteria>
- Message tests cannot hit a real local dashboard socket.
- `message send/list/clear` is covered through one focused subprocess suite.
- Inventory mappings for message flows are updated inline and remain machine-parseable.
</success_criteria>

<output>
After completion, create `.planning/phases/82-template-repo-label-and-message-e2e-coverage/82-03-SUMMARY.md`
</output>
