---
phase: 82-template-repo-label-and-message-e2e-coverage
plan: "02"
type: execute
wave: 2
depends_on:
  - 82-01
files_modified:
  - tests/helpers.ts
  - tests/commands/repo.test.ts
  - tests/commands/label.test.ts
  - tests/e2e-inventory.ts
autonomous: true
requirements:
  - E2E-10
  - E2E-11
must_haves:
  truths:
    - "Repo registry coverage proves the allowed git-repo and dir-repo success paths without falling into prompt-driven or excluded flows."
    - "Workspace label coverage closes the remaining add/remove/list/clear contract gap by extending the existing subprocess suite, not replacing it."
    - "The inventory maps repo and label flows to their suites and keeps `repo scan` excluded inline."
  artifacts:
    - path: tests/commands/repo.test.ts
      provides: "Focused subprocess coverage for repo add/list/show/rename/remove"
      min_lines: 180
    - path: tests/commands/label.test.ts
      provides: "Extended subprocess coverage for label add/remove/list/clear contracts"
      min_lines: 100
    - path: tests/e2e-inventory.ts
      provides: "Repo and label flow mappings plus exclusions"
      contains: "repo.test.ts"
  key_links:
    - from: tests/commands/repo.test.ts
      to: src/commands/repo.ts
      via: "bun run src/index.ts repo ..."
      pattern: "Registered '"
    - from: tests/commands/repo.test.ts
      to: tests/helpers.ts
      via: "temp-bin PATH helper for stub gh/glab"
      pattern: "PATH"
    - from: tests/commands/label.test.ts
      to: src/commands/label.ts
      via: "bun run src/index.ts label ..."
      pattern: "Labels"
---

<objective>
Cover the repo registry and workspace label suites exactly within the locked Phase 82 boundaries.

Purpose: Deliver the repo registry split (git vs dir, no-enabled-forge vs exact-one-enabled-match) and finish the workspace label subprocess coverage without dragging in excluded prompt or failure work.
Output: One focused repo suite, one focused label extension, and inline inventory mappings/exclusions.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/82-template-repo-label-and-message-e2e-coverage/82-CONTEXT.md
@.planning/phases/82-template-repo-label-and-message-e2e-coverage/82-RESEARCH.md
@.planning/phases/82-template-repo-label-and-message-e2e-coverage/82-VALIDATION.md
@.planning/phases/81.1-repo-add-honors-enabled-forge-integrations/81.1-CONTEXT.md
@tests/helpers.ts
@tests/commands/label.test.ts
@tests/e2e-inventory.ts
@src/commands/repo.ts
@src/commands/label.ts
@src/lib/integrations/forge-utils.ts
@src/lib/integrations/types.ts

<interfaces>
From `src/commands/repo.ts`:
```typescript
console.log(`Registered '${name}' (${type}) at ${localPath} [${defaultBranch}]${forgeLabel}${dirLabel}`)
repoCommand.command("remove <repo>").option("--force", ...)
```

From `src/commands/label.ts`:
```typescript
labelCommand.command("add <workspace> <labels...>")
labelCommand.command("remove <workspace> <labels...>")
labelCommand.command("list <workspace>")
labelCommand.command("clear <workspace>")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add repo registry subprocess coverage for git-vs-dir and enabled-forge boundaries</name>
  <read_first>
    @.planning/REQUIREMENTS.md
    @.planning/phases/82-template-repo-label-and-message-e2e-coverage/82-CONTEXT.md
    @.planning/phases/82-template-repo-label-and-message-e2e-coverage/82-RESEARCH.md
    @.planning/phases/81.1-repo-add-honors-enabled-forge-integrations/81.1-CONTEXT.md
    @tests/helpers.ts
    @tests/e2e-inventory.ts
    @src/commands/repo.ts
    @src/lib/integrations/forge-utils.ts
    @src/lib/integrations/types.ts
  </read_first>
  <files>tests/helpers.ts, tests/commands/repo.test.ts, tests/e2e-inventory.ts</files>
  <behavior>
    - Git repo add proves current-branch auto-detect by registering a real git repo checked out on a non-`main` branch without passing `--branch`, then asserting the registered/default branch and stdout reflect that current branch.
    - Git repo add proves explicit `--branch <name>` override by registering a real git repo with `--branch release/test` and asserting stdout plus persisted `registry.yml` store `release/test` instead of the checked-out branch.
    - Git repo add proves `--name <name>` override on at least one success-path registration so the add surface matches the requirement contract.
    - Git repo add proves both the no-enabled-forge path and the exact-one-enabled-match path using stub `gh`/`glab` binaries, isolated config enablement, and matching remote URLs.
    - Dir repo scenarios prove forge detection is skipped, default branch is `main`, and shared list/show/rename/remove behavior still works.
    - Repo removal uses `--force` only and never enters the prompt path.
  </behavior>
  <action>Per D-01 through D-09 and D-19 through D-21, create `tests/commands/repo.test.ts` with separate git-repo and dir-repo scenario groups. For git repos, include three explicit `repo add` contract cases: (1) current-branch auto-detect with no `--branch`, using a real repo checked out on a non-`main` branch and asserting stdout plus persisted `registry.yml` store that checked-out branch; (2) explicit `repo add --branch <name>` override, asserting stdout plus persisted `registry.yml` store the override rather than the checked-out branch; and (3) at least one success-path `repo add --name <name>` registration proving the requirement-level add surface. Also prove both the no-enabled-forge path and the exact-one-enabled-match path, using stub `gh`/`glab` binaries in a dedicated temp-bin directory prepended only to child `PATH`, matching remote URLs, and isolated config enablement; use no real auth and never involve `tea`. Assert forge persistence through `repo add` stdout plus `registry.yml`, while keeping `repo list` / `repo show` assertions on their existing user-facing contracts. For dir repos, prove forge detection is skipped and default branch is `main`. Use `repo remove --force` only. If Phase 80 did not already provide a temp-bin helper, add a narrow one to `tests/helpers.ts` for child-process-only PATH shaping. Update `tests/e2e-inventory.ts` inline so repo registry flow items map to `repo.test.ts` and `repo scan` stays explicitly excluded with rationale.</action>
  <acceptance_criteria>
    - `test -f tests/commands/repo.test.ts`
    - `grep -q 'no enabled forge' tests/commands/repo.test.ts`
    - `grep -q 'exact one enabled forge match' tests/commands/repo.test.ts`
    - `grep -q 'current branch auto-detect' tests/commands/repo.test.ts`
    - `grep -q -- '--branch' tests/commands/repo.test.ts`
    - `grep -q -- '--name' tests/commands/repo.test.ts`
    - `grep -q 'dir repo' tests/commands/repo.test.ts`
    - `grep -q -- '--force' tests/commands/repo.test.ts`
    - `grep -q 'repo.test.ts' tests/e2e-inventory.ts`
    - `grep -q 'repo scan' tests/e2e-inventory.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/commands/repo.test.ts -x</automated>
  </verify>
  <done>The repo suite covers the allowed success-path registry behavior for both git and dir repos, proves forge persistence via stdout plus `registry.yml`, and keeps `repo scan` excluded from Phase 82.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the existing workspace label subprocess suite and map it in the inventory</name>
  <read_first>
    @.planning/REQUIREMENTS.md
    @.planning/phases/82-template-repo-label-and-message-e2e-coverage/82-CONTEXT.md
    @.planning/phases/82-template-repo-label-and-message-e2e-coverage/82-RESEARCH.md
    @tests/commands/label.test.ts
    @tests/e2e-inventory.ts
    @src/commands/label.ts
  </read_first>
  <files>tests/commands/label.test.ts, tests/e2e-inventory.ts</files>
  <action>Per D-14, D-15, D-19, D-20, and D-21, extend `tests/commands/label.test.ts` rather than replacing it. Close the remaining success-path/output-contract gap for `label add`, `label remove`, `label list`, and `label clear`, including explicit assertions on the current stdout and persisted workspace YAML behavior. Keep this suite narrow: do not add representative failure matrices or unrelated workspace command coverage. Update `tests/e2e-inventory.ts` inline so the workspace-label flow items point to the extended `label.test.ts` suite.</action>
  <acceptance_criteria>
    - `grep -q 'clear' tests/commands/label.test.ts`
    - `grep -q 'Labels cleared.' tests/commands/label.test.ts`
    - `grep -q 'No labels remaining.' tests/commands/label.test.ts`
    - `grep -q 'label.test.ts' tests/e2e-inventory.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/commands/label.test.ts -x</automated>
  </verify>
  <done>The existing label subprocess suite now covers the remaining add/remove/list/clear contract surface and the inventory maps workspace-label flows to that file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → child CLI PATH/env | Stub forge binaries and isolated config/home are injected into subprocess env |
| child CLI → registry/config files | Repo and label commands write user-visible YAML state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-04 | T | repo suite PATH handling | mitigate | Prepend a dedicated temp-bin directory only in child env for `repo.test.ts`; never alter the parent shell PATH, preventing stub `gh`/`glab` confusion. |
| T-82-05 | D | repo add / remove subprocesses | mitigate | Stay on the locked no-prompt paths only: no-enabled-forge or exact-one-enabled-match for add, and `remove --force` for delete. |
| T-82-06 | T | registry/config isolation | mitigate | Reuse isolated config/git-home helpers from Phase 80 so repo/label tests cannot mutate the developer's real config or git home. |
</threat_model>

<verification>
- `bun test tests/commands/repo.test.ts -x`
- `bun test tests/commands/label.test.ts -x`
</verification>

<success_criteria>
- Repo coverage is split by git-vs-dir behavior and proves only the locked success-path forge cases.
- Repo add explicitly proves current-branch auto-detect, `--branch` override, and `--name` override on the success path.
- Workspace label coverage is extended in-place instead of rewritten.
- Inventory mappings/exclusions for repo and label flows are updated inline.
</success_criteria>

<output>
After completion, create `.planning/phases/82-template-repo-label-and-message-e2e-coverage/82-02-SUMMARY.md`
</output>
