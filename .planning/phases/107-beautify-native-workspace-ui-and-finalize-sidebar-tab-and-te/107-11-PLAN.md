---
phase: 107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te
plan: 11
type: execute
wave: 8
depends_on:
  - 107-10
files_modified:
  - native/linux/app.zig
  - native/linux/attention_view.zig
  - native/linux/command_launcher.zig
  - native/linux/app_contract_test.zig
  - native/tests/attention_test.zig
  - native/tests/application_actions_test.zig
  - native/tests/accessibility_test.zig
autonomous: true
requirements:
  - LNX-09
  - ACT-07
must_haves:
  truths:
    - "A visible attention control opens rows showing provider, title, optional detail, location, unread state, and fallback explanation; a numeric badge is not the only provider UI."
    - "Activating an attention row dispatches `win.focus-attention` with the stable attention ID and focuses the exact surviving surface or documented nearest context only after explicit user action."
    - "Selecting a terminal tab dispatches `exact_tab_visible` after that exact page is visibly selected and clears only direct surface attention."
    - "With search focused, launcher Down/Up moves result selection, Enter activates the selected/first result, Escape restores prior focus, and zero results show distinct no-commands versus no-matches guidance."
    - "Closing an ended tab is immediate; closing a live tab from X, menu, or Ctrl+Shift+W presents one confirmation and performs no terminal teardown until affirmative response."
    - "Cancel/stale/double close callbacks cannot destroy a host, remove the wrong page, or leave a tab-view close transaction unresolved."
  artifacts:
    - path: "native/linux/attention_view.zig"
      provides: "Provider-aware row projection and explicit activation bindings"
      contains: "AttentionRow"
    - path: "native/linux/command_launcher.zig"
      provides: "Selection and distinct empty-state projection"
      contains: "EmptyState"
    - path: "native/linux/app.zig"
      provides: "Attention popover, keyboard launcher bridge, and guarded live-close confirmation"
      contains: "PendingTabClose"
    - path: "native/tests/accessibility_test.zig"
      provides: "Rendered attention text/activation and live-close semantic evidence"
  key_links:
    - from: "native/linux/app.zig"
      to: "native/linux/attention_view.zig"
      via: "stable row IDs become `win.focus-attention` action targets"
      pattern: "focus-attention"
    - from: "native/linux/app.zig"
      to: "native/core/reducer.zig"
      via: "selected page dispatches exact_tab_visible before attention projection refresh"
      pattern: "exact_tab_visible"
    - from: "native/linux/app.zig"
      to: "native/linux/tab_registry.zig"
      via: "affirmative live-close callback alone performs teardown and model removal"
      pattern: "close_page_finish"
---

<objective>
Close the priority interaction gaps around explicit attention, keyboard launcher use, and accidental live-terminal termination.

Purpose: ACT-07 is not user-visible/routable today, keyboard-first command use is incomplete, and one accidental close destroys live work immediately.
Output: Provider-aware attention rows, exact read clearing, complete launcher navigation/empty states, guarded live close, and production widget/action tests.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te/107-CONTEXT.md
@.planning/phases/107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te/107-PATTERNS.md
@.planning/phases/107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te/107-UI-REVIEW.md
@.planning/phases/107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te/107-10-SUMMARY.md
@.planning/phases/106-linux-workspace-commands-and-attention/106-CONTEXT.md

<interfaces>
`win.focus-attention` already reduces `select_attention`; this plan must create visible action targets rather than a second routing path.

`command_launcher.Launcher.collect(query, out)` produces stable command indexes ordered by recency then name. Extend selection/empty projection around this output without becoming a general command palette.

`AdwTabView::close-page` returns true to defer the transaction and `adw_tab_view_close_page_finish(view, page, confirm)` resolves it. Stable surface ID/generation must be captured before any dialog callback.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render provider-aware attention rows and wire exact routing/read clearing</name>
  <read_first>
    - native/linux/attention_view.zig
    - native/linux/app.zig
    - native/core/model.zig
    - native/core/reducer.zig
    - native/tests/attention_test.zig
    - native/tests/accessibility_test.zig
    - native/tests/application_actions_test.zig
    - .planning/phases/107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te/107-CONTEXT.md
  </read_first>
  <files>native/linux/attention_view.zig, native/linux/app.zig, native/tests/attention_test.zig, native/tests/application_actions_test.zig, native/tests/accessibility_test.zig</files>
  <behavior>
    - Attention rows render provider label, title, optional detail, workspace/repository/tab location, occurred time, unread state, and nearest-context explanation when exact surface is gone.
    - Row activation passes the stable attention ID to `win.focus-attention`; asynchronous receipt and refresh never activate it.
    - Explicit attention activation marks that item read and routes exact surface, ended predecessor, repository, workspace, or unresolved as defined by the reducer.
    - `selectedPageChanged` dispatches exact-tab-visible for the selected live/ended surface only after the page is active; generic workspace/repository navigation clears nothing.
  </behavior>
  <action>Extend `attention_view` with bounded `AttentionRow` projection helpers that resolve provider display names, title/detail, human location, unread/priority, occurrence text, and fallback explanation from shared model state. Replace the generic header text-only treatment with a labeled attention button/popover/list; keep aggregate count as a badge but render real rows. Bind each row's stable ID as the target of the existing `win.focus-attention` GAction and close the popover only after dispatch. In selected-page handling, derive the exact stable surface ID, confirm the page is selected, dispatch `reducer.Action.exact_tab_visible`, then refresh badges/rows; do not dispatch from workspace/repository selection. Attach the row's redundant provider/title/unread/location text to the rendered accessible object for test inspection.</action>
  <acceptance_criteria>
    - Native attention tests prove all four providers, optional detail, unread/read styles, exact and fallback copy, and zero focus on receipt/refresh.
    - Application action tests activate a visible Codex row and observe the exact stable focus route, then verify the item becomes read.
    - Selecting one exact tab clears only its direct items; sibling surface, repository, and workspace items remain unread.
    - Rendered accessibility test inspects provider/title/location text on the actual row/button, not only a model helper.
  </acceptance_criteria>
  <verify>
    <automated>bun run native:test:attention &amp;&amp; bun run native:test:application-actions &amp;&amp; bun run native:test:accessibility</automated>
  </verify>
  <done>Codex and other providers are visibly distinguishable and route/read behavior is reachable only through explicit user activation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the command-only launcher keyboard complete and explanatory</name>
  <read_first>
    - native/linux/command_launcher.zig
    - native/linux/app.zig
    - native/linux/application.zig
    - native/tests/application_actions_test.zig
    - native/linux/app_contract_test.zig
    - .planning/phases/107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te/107-UI-REVIEW.md
  </read_first>
  <files>native/linux/command_launcher.zig, native/linux/app.zig, native/tests/application_actions_test.zig, native/linux/app_contract_test.zig</files>
  <behavior>
    - Refresh selects the first valid result when any exist and preserves selection by stable command ID when filtering permits.
    - With search entry focused, Down/Up wraps or clamps deterministically across visible rows and Enter activates selected/first result.
    - Empty source command set displays `No configured commands for this workspace`; nonempty source with no query matches displays `No commands match this search` plus clear-search guidance.
    - Enter does nothing with a status row/no result; readiness/failure keeps launcher open with actionable error; Escape restores the exact prior focus target.
  </behavior>
  <action>Add `EmptyState` and selection helpers to `command_launcher.Launcher`, keyed by stable command ID rather than transient row number. Update `refreshLauncher` to insert a non-activatable status row for no-configured versus no-match states and select the first real result otherwise. Extend the search entry key controller for GDK Down, Up, and Enter while preserving Escape: move `GtkListBox` selection, activate only a command row through existing `launcherActivated`, and restore prior terminal/widget focus on close. Keep the overlay restricted to configured commands for the selected workspace/repository and retain recent-first/alphabetic ordering and duplicate scope labels.</action>
  <acceptance_criteria>
    - Focused keyboard tests cover initial selection, Down/Up boundaries, Enter, filter selection preservation, zero results, disabled readiness, failure, and Escape restoration.
    - Status rows cannot launch a command and expose distinct exact copy for empty catalog versus no match.
    - Pointer row activation and keyboard Enter converge on the same action/launch path.
    - Launcher remains command-only and emits no general application actions or arbitrary shell input.
  </acceptance_criteria>
  <verify>
    <automated>bun run native:test:application-actions &amp;&amp; bun run native:test:workspace-ui &amp;&amp; bun run native:audit-production-graph</automated>
  </verify>
  <done>The configured-command launcher is fully usable and understandable from the keyboard with search focused.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Confirm before closing a live terminal and preserve ended-tab immediacy</name>
  <read_first>
    - native/linux/app.zig
    - native/linux/tab_registry.zig
    - native/linux/ghostty_surface.zig
    - native/linux/app_contract_test.zig
    - native/tests/application_actions_test.zig
    - native/tests/tab_registry_test.zig
    - .planning/phases/107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te/107-UI-REVIEW.md
  </read_first>
  <files>native/linux/app.zig, native/linux/app_contract_test.zig, native/tests/application_actions_test.zig</files>
  <behavior>
    - Ended tab close removes presentation immediately without confirmation or process teardown.
    - Live tab close defers the tab-view transaction and opens one alert naming the terminal and consequence; no model/registry/Ghostty mutation occurs yet.
    - Cancel calls close-page-finish false and retains the exact page/host/focus; confirm validates page/surface/generation, tears down once, removes model state, and finishes true.
    - X button, menu close, and Ctrl+Shift+W converge; repeated close while pending cannot open another dialog; stale callbacks cannot close a reused page/surface.
  </behavior>
  <action>Add state-owned `PendingTabClose` containing referenced tab view/page, stable surface ID, generation, and one `AdwAlertDialog`/supported libadwaita confirmation object. In the existing `close-page` interception, keep programmatic and ended removal paths immediate. For live lifecycle, return true to defer, create one confirmation with `Cancel` and destructive `Close terminal`, and leave registry/model/Ghostty untouched. On cancel resolve the tab transaction with false and restore focus. On affirmative response re-lookup the same page/surface/generation, ignore stale/replaced targets safely, then call the existing one-shot registry teardown/model removal and finish true. Clear pending state on every completion/window shutdown and have all close entry points use this handler.</action>
  <acceptance_criteria>
    - Production action tests prove zero teardown before confirmation, no close on cancel, one teardown on confirm, and immediate ended-tab removal.
    - Pointer/menu/shortcut close produce the same pending state and copy.
    - Double activation shows one dialog; page/surface removal or generation change before response produces no wrong-host teardown or unresolved transaction.
    - Application quit retains its existing explicit whole-app cleanup semantics and does not create per-tab prompts during shutdown.
  </acceptance_criteria>
  <verify>
    <automated>bun run native:test:application-actions &amp;&amp; bun run native:test:tabs &amp;&amp; bun run native:audit-production-graph &amp;&amp; bun run native:smoke-workspace</automated>
  </verify>
  <done>Accidental close cannot terminate live work, while ended tabs remain quick to remove and ownership stays one-shot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Attention event to platform focus | Agent-originated data can request a route only after explicit row activation. |
| Tab close input to process teardown | Pointer/keyboard/menu input can terminate an owned PTY/process group. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-107-11-01 | Elevation of Privilege | attention focus | high | mitigate | Stable-ID GAction plus explicit user activation; receipt/refresh remain zero-focus. |
| T-107-11-02 | Tampering | attention display/action binding | medium | mitigate | Project from shared owned model and bind stable service/internal IDs, not row indexes. |
| T-107-11-03 | Denial of Service | live terminal close | high | mitigate | Deferred tab transaction, destructive confirmation, stable generation revalidation, and one-shot teardown. |
| T-107-11-04 | Repudiation | launcher activation | low | accept | Keyboard and pointer converge on one stable command action; focused tests record selection behavior. |
</threat_model>

## Artifacts this phase produces

- `attention_view.AttentionRow` provider-aware projection
- Visible attention button/popover/list bound to `win.focus-attention`
- Production `exact_tab_visible` dispatch on selected tab
- `command_launcher.EmptyState` and stable selection helpers
- Up/Down/Enter search-entry launcher handling and distinct empty/no-match rows
- `PendingTabClose` and live-terminal confirmation transaction

<verification>
Run attention, application-action, accessibility, workspace UI, tabs, production-graph, and workspace graphical smoke gates. Close tests must inspect teardown counters and stable generations, not only dialog visibility.
</verification>

<success_criteria>
Provider attention is visible and explicitly actionable, the launcher is keyboard complete, and live terminal work cannot be destroyed by one unconfirmed close input.
</success_criteria>

<output>
Create `.planning/phases/107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te/107-11-SUMMARY.md` when done.
</output>
