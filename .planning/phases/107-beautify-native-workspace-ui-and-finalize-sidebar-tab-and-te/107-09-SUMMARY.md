---
phase: 107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te
plan: 09
subsystem: native-sync
tags: [zig, reconciliation, attention, terminal-ownership, refresh]
requires: [107-08]
provides: [orphan-terminal-reconciliation, bounded-live-pair-admission, coalesced-refresh-coordinator]
affects: [107-10, native-linux-client]
tech-stack:
  added: []
  patterns: [authoritative-presentation-merge, generation-checked-single-flight]
key-files:
  created: [native/linux/service_sync.zig, native/tests/service_sync_test.zig]
  modified: [native/core/model.zig, native/linux/app_graph.zig, native/linux/tab_registry.zig, native/linux/workspace_view.zig, native/build.zig, scripts/verify-native.ts, package.json]
decisions:
  - Live terminal pair identities are rejected before ownership registration at the shared 32-pair bound.
  - Replay cursors remain staged until their refresh generation succeeds.
metrics:
  duration: 18m
  completed: 2026-07-12
status: complete
---

# Phase 107 Plan 09: Native Reconciliation and Sync Summary

Authoritative workspace refresh now preserves native terminal presentation through separately bounded orphan tombstones, while a UI-neutral coordinator coalesces invalidation, periodic, conflict, replay-gap, and retry work.

## Tasks Completed

1. Added live-pair ownership queries, pre-registration capacity enforcement, stable presentation reconciliation, orphan retention/release, attention read-state preservation, and disabled-action orphan row projection.
2. Added a generation-checked single-flight refresh coordinator with trailing revision/reason coalescing, success-atomic replay cursor adoption, stale failure state, and cancellation.
3. Registered focused and quick native sync verification commands.

## Verification

- `bun run native:test:service-sync` — passed
- `bun run native:test:tabs` — passed
- `bun run native:test:app-graph` — passed
- `bun run native:test:workspace-ui` — passed
- `bun run native:test:attention` — passed
- `bun run native:test:quick` — passed

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected the attention overflow fixture identity**
- **Found during:** Overall verification
- **Issue:** The nominal new attention ID collided with one of the 64 generated capacity entries, exercising update rather than overflow.
- **Fix:** Chose an identity outside the generated byte range and documented the invariant.
- **Files modified:** `native/tests/attention_test.zig`
- **Commit:** `ee604fdc`

## Known Stubs

None.

## Self-Check: PASSED

All created files and task commits exist, and focused plus combined native gates pass.
