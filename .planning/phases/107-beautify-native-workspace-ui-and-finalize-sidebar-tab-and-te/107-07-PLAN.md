---
phase: 107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te
plan: 07
type: execute
wave: 4
depends_on:
  - 107-01
  - 107-03
  - 107-06
files_modified:
  - native/core/model.zig
  - native/core/reducer.zig
  - native/core/abi.zig
  - native/tests/reducer_test.zig
  - native/tests/attention_test.zig
  - native/tests/abi_harness.c
  - native/tests/fixtures/abi-v1-attention-legacy.json
autonomous: true
requirements:
  - LNX-08
  - ACT-07
must_haves:
  truths:
    - "Native attention state retains provider, title, optional detail, occurrence time, service ID, unread state, and exact routing identities rather than discarding presentation data after decode."
    - "Receiving attention remains focus-neutral; only explicit attention activation emits a focus route, and exact-tab visibility clears only exact-surface unread items."
    - "All native fixed collection and UTF-8 byte capacities match the service-owned compatibility contract, and every insertion path validates complete UTF-8 before writing without truncation."
    - "A workspace/repository/command snapshot above a compatible limit becomes an explicit capacity-incompatible state while retaining the last safe state; it is never partially decoded or silently truncated."
    - "Attention overflow deterministically reuses duplicate IDs or evicts a read/status-only item; if all slots hold unread actionable attention, the model records a visible overflow condition rather than silently dropping it."
    - "Canonical ABI JSON includes the new attention and capacity state so Linux and later macOS shells consume the same product truth."
    - "ABI v1 provider/title/detail/time fields are additive: legacy v1 attention payloads without them remain accepted with explicit defaults."
  artifacts:
    - path: "native/core/model.zig"
      provides: "Provider-aware attention and explicit capacity compatibility state"
      contains: "NativeModelLimits"
    - path: "native/core/reducer.zig"
      provides: "Bounded attention insertion, capacity-incompatible action, and exact read semantics"
      contains: "capacity_exceeded"
    - path: "native/core/abi.zig"
      provides: "Canonical cross-language attention/capacity projection"
  key_links:
    - from: "native/core/reducer.zig"
      to: "native/core/model.zig"
      via: "attention_received stores all presentation fields and capacity_exceeded retains last safe state"
      pattern: "attention_received|capacity_exceeded"
    - from: "native/core/abi.zig"
      to: "native/core/model.zig"
      via: "canonical JSON serializes provider/title/detail/occurred_at and overflow/capacity diagnostics"
      pattern: "provider|title|detail|capacity"
    - from: "native/core/reducer.zig"
      to: "native/linux/attention_view.zig"
      via: "later GTK rows consume stable attention ID and explicit select_attention/exact_tab_visible transitions"
      pattern: "select_attention|exact_tab_visible"
---

<objective>
Strengthen the shared native model before transport/UI work by retaining all ACT-07 presentation data and making fixed-capacity behavior explicit, safe, and testable.

Purpose: The current reducer drops provider/title/detail and bounded arrays can reject or overwrite authoritative data without a product-visible compatibility contract.
Output: Named limits, safe capacity transitions, provider-aware attention state, exact read/focus semantics, and ABI/boundary tests.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te/107-CONTEXT.md
@.planning/phases/107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te/107-PATTERNS.md
@.planning/phases/106-linux-workspace-commands-and-attention/106-02-SUMMARY.md
@.planning/phases/107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te/107-03-SUMMARY.md
@.planning/phases/107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te/107-06-SUMMARY.md

<interfaces>
Plan 107-03 freezes `NATIVE_MODEL_LIMITS` in the authenticated discovery contract. Mirror all of its collection counts and UTF-8 byte ceilings in Zig, including separate 32-slot authoritative-pair and orphan-tombstone capacities; compile-time assertions must fail on any backing-storage drift.

`reducer.Action` already has `attention_received`, `select_attention`, and `exact_tab_visible`; `reducer.Effect.platform_focus` remains the only focus-producing effect.

`model.Attention` already separates a stable internal UUID from the service attention ID and routing identities. Extend this struct rather than creating widget-only provider state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Name every compatible capacity and fail closed before bounded writes</name>
  <read_first>
    - native/core/model.zig
    - native/core/reducer.zig
    - native/core/abi.zig
    - native/tests/reducer_test.zig
    - native/tests/abi_harness.c
    - src/lib/service/contract.ts
    - native/tests/service_client_test.zig
  </read_first>
  <files>native/core/model.zig, native/core/reducer.zig, native/core/abi.zig, native/tests/reducer_test.zig, native/tests/abi_harness.c</files>
  <behavior>
    - `NativeModelLimits` exposes every Plan 107-03 count and UTF-8 byte ceiling, including 32 authoritative pairs, 32 live-pair identities, and 32 reserved orphan tombstones; compile-time assertions bind each value to its backing storage.
    - Exact-maximum workspace, repository, pair, command, and attention states are accepted without out-of-bounds writes.
    - State reserves a dedicated 32-entry orphan-tombstone array/count separate from the 32 authoritative pair entries, so later reconciliation never spends authoritative capacity on a live orphan.
    - Maximum-plus-one authoritative workspace/repository/pair/command input yields a `capacity_exceeded` action/result with kind, maximum, and attempted values.
    - Applying capacity failure retains the last safe workspaces, pairs, surfaces, pins, selection, and attention, marks connection incompatible, and requests no automatic focus.
  </behavior>
  <action>Add `NativeModelLimits`, string-byte limit constants, `CapacityKind`, and bounded `CapacityFailure`/state diagnostic fields to `model.zig`; copy the exact Plan 107-03 contract and derive or compile-time assert every constant against backing arrays. Reserve a distinct `OrphanPairTombstone` array/count sized to the advertised orphan capacity, with stable pair identity, last-known bounded workspace/repository display names, and bounded surface presentation; do not merge it into authoritative `pairs` or implement reconciliation yet. Add a reducer `capacity_exceeded` action that copies the prior safe product state, marks connection `.incompatible`, records limit/maximum/attempted, and emits no focus or persistence effect. Audit all reducer insert/copy loops so they validate well-formed UTF-8 and complete encoded byte length before mutation, then return an explicit error/action rather than slicing, truncating at a byte/codepoint boundary, or relying on traps. Extend canonical state validation/JSON and the C ABI harness to expose the diagnostic. Do not convert the reducer to allocator-owned platform state in this plan; the accepted compatibility policy is fixed, advertised, fail-closed capacity with boundary coverage.</action>
  <acceptance_criteria>
    - Compile-time assertions fail if any advertised native limit drifts from its actual array length.
    - Model tests prove 32 authoritative pairs and an independently addressable orphan slot coexist without changing authoritative `pair_count`.
    - Reducer tests cover each exact maximum and maximum-plus-one case and verify the prior state's canonical bytes remain unchanged except connection/diagnostic fields on failure.
    - No insertion loop increments a count before proving capacity.
    - Table-driven ASCII and multibyte fixtures prove each named string accepts an exact-byte-boundary value, rejects byte-limit-plus-one, never splits a code point, and leaves the previous canonical state unchanged on rejection.
    - ABI output contains capacity kind/maximum/attempted and remains valid JSON for a capacity failure state.
  </acceptance_criteria>
  <verify>
    <automated>bun run native:test:model &amp;&amp; bun run native:test:quick</automated>
  </verify>
  <done>Every bounded native collection has a truthful compatibility policy and automated edge proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retain provider-aware attention and deterministic overflow/read semantics</name>
  <read_first>
    - native/core/model.zig
    - native/core/reducer.zig
    - native/core/abi.zig
    - native/tests/attention_test.zig
    - native/tests/reducer_test.zig
    - src/lib/service/contract.ts
    - .planning/phases/107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te/107-CONTEXT.md
  </read_first>
  <files>native/core/model.zig, native/core/reducer.zig, native/core/abi.zig, native/tests/attention_test.zig, native/tests/reducer_test.zig, native/tests/abi_harness.c, native/tests/fixtures/abi-v1-attention-legacy.json</files>
  <behavior>
    - Attention stores provider enum, title up to 160 UTF-8 bytes, optional detail up to 500 UTF-8 bytes, bounded ISO occurrence time, bounded service ID, routing identities, status, resolution, and read flag.
    - ABI v1 accepts both the legacy attention shape and the enriched shape; absent provider/title/detail/time fields receive documented defaults while newly encoded v1 payloads include the enriched fields.
    - Duplicate service attention IDs update the existing record without consuming a slot or increasing unread aggregate twice.
    - At capacity, the oldest read item is evicted first, then the oldest working/idle item; when no safe eviction exists, `attention_overflow_count` increases and remains canonical/UI-visible.
    - `attention_received` emits no focus; `select_attention` follows exact surface, ended predecessor, repository, workspace, unresolved; `exact_tab_visible` marks only matching surface items read.
  </behavior>
  <action>Add `AttentionProvider` with `claude`, `copilot`, `codex`, and `other`, then extend `Attention` with the shared bounded title/detail/occurred-at storage and lengths. Validate full UTF-8 byte lengths before every owned copy and reject oversize values without prefix truncation. Make the new ABI v1 input members optional/additive: parse the checked-in legacy fixture with documented `other`/empty-detail/time defaults, while canonical output and enriched inputs round-trip all fields with JSON escaping and a distinct service ID. Refactor `attention_received` to update by service ID, otherwise apply the deterministic safe-eviction order, and record `attention_overflow_count` when all 64 slots contain unread actionable items. Preserve the invariant that asynchronous receipt emits `.none`. Keep explicit focus routing in `select_attention`; ensure `exact_tab_visible` requires the exact surface identity and never clears repository/workspace-only items. Add provider-specific, legacy-v1, duplicate, overflow, fallback-route, exact-read, and Unicode byte-boundary tests.</action>
  <acceptance_criteria>
    - Native tests round-trip Codex provider, title, Unicode detail, occurrence time, service ID, and all routing identities through model/canonical ABI JSON.
    - The checked-in pre-enrichment ABI v1 fixture still decodes, receives deterministic defaults, and re-encodes as valid v1; enriched fields do not require a protocol bump.
    - Duplicate update and safe-eviction tests leave correct counts and aggregates; all-actionable overflow increments a visible diagnostic and does not corrupt memory.
    - Receipt/navigation actions produce zero platform-focus effects; only explicit select-attention does.
    - Exact-tab visibility clears matching direct items and leaves sibling/repository/workspace items unread.
  </acceptance_criteria>
  <verify>
    <automated>bun run native:test:attention &amp;&amp; bun run native:test:model &amp;&amp; bun run native:test:quick</automated>
  </verify>
  <done>The shared product model, not GTK widgets, owns complete provider-aware attention and honest overflow state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Decoded wire data to fixed native arrays | Remote-sized counts and strings enter bounded Zig storage. |
| Asynchronous attention to focus effects | Agent-originated state must not cause unsolicited platform focus/navigation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-107-07-01 | Denial of Service | bounded state insertion | high | mitigate | Check all counts/lengths before writes, expose capacity diagnostics, and retain last safe state. |
| T-107-07-02 | Tampering | provider/title/detail decode | medium | mitigate | Strict provider enum, bounded owned copies, JSON escaping, and canonical ABI tests. |
| T-107-07-03 | Elevation of Privilege | attention focus routing | high | mitigate | Receipt/navigation emit no focus; only explicit selection derives `platform_focus`. |
| T-107-07-04 | Repudiation | attention overflow | medium | mitigate | Deterministic eviction rules and canonical overflow count prevent silent loss. |
</threat_model>

## Artifacts this phase produces

- `NativeModelLimits`
- `CapacityKind`, `CapacityFailure`, and capacity diagnostic state fields
- Dedicated bounded `OrphanPairTombstone` storage separate from authoritative pairs
- `reducer.Action.capacity_exceeded`
- `AttentionProvider`
- Provider/title/detail/occurred-at fields on `model.Attention`
- `attention_overflow_count`
- Canonical ABI fields for capacity and provider-aware attention
- `native/tests/fixtures/abi-v1-attention-legacy.json`

<verification>
Run model, attention, and combined quick native gates. Boundary tests must exercise each exact maximum and maximum-plus-one value with runtime safety enabled.
</verification>

<success_criteria>
The portable native model preserves the complete attention contract and cannot silently corrupt, truncate, or discard authoritative state at a fixed-capacity boundary.
</success_criteria>

<output>
Create `.planning/phases/107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te/107-07-SUMMARY.md` when done.
</output>
