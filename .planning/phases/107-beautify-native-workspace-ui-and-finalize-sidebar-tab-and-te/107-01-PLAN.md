---
phase: 107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/service/contract.ts
  - src/lib/service/snapshot.ts
  - native/core/model.zig
  - native/core/persistence.zig
  - native/linux/service_client.zig
  - native/linux/ghostty_runtime.zig
  - native/linux/ghostty_surface.zig
  - native/linux/app.zig
autonomous: true
requirements: []
---

<objective>Deliver the post-stability Supacode-quality sidebar, repository targeting, automatic terminal titles, manual title overrides, and cohesive native visual language.</objective>

<tasks>

<task type="auto">
  <name>Project labels and compose the grouped sidebar</name>
  <action>Add labels to authoritative snapshots and native decoding; render pinned workspaces separately and unpinned workspaces in collapsible label/repository groups. Make single-repository workspaces direct rows and multi-repository workspaces explicit repository choices.</action>
  <verify>Focused service contract/snapshot tests, native build, workspace lifecycle smoke.</verify>
  <acceptance_criteria>Grouping is selectable and persisted; pinned entries do not collapse; launch target is unambiguous.</acceptance_criteria>
</task>

<task type="auto">
  <name>Use Ghostty titles with pinned manual overrides</name>
  <action>Propagate Ghostty SET_TITLE through the adapter, update automatic model titles, compact long titles for display, eliminate ID fallbacks, and make rename pin until cleared.</action>
  <verify>Surface tests, persistence tests, native build and lifecycle smoke.</verify>
  <acceptance_criteria>Automatic titles update, internal IDs never render, manual titles survive automatic updates.</acceptance_criteria>
</task>

<task type="auto">
  <name>Apply native visual polish and protect lifecycle behavior</name>
  <action>Apply product CSS, coherent spacing and labels, accessible native controls, then run complete regression gates.</action>
  <verify>bun run native:verify; bun run test; bun run typecheck; bun run test:deps; bun run verify:gates.</verify>
  <acceptance_criteria>All automated gates pass and the real GTK surface remains ready for final human visual UAT.</acceptance_criteria>
</task>

</tasks>

<verification>Run all focused and complete gates; do not accept source-only UI assertions in place of the production graphical smoke.</verification>
<success_criteria>Every locked Phase 107 decision is implemented without regressing Phase 106 lifecycle stability.</success_criteria>
