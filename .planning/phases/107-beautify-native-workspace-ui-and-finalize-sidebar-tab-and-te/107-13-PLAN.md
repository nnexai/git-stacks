---
phase: 107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te
plan: 13
type: execute
wave: 10
depends_on:
  - 107-12
files_modified:
  - native/linux/app.zig
  - scripts/verify-native.ts
  - package.json
  - docs/native-workspace-acceptance.md
  - docs/native-terminal-accessibility.md
autonomous: false
requirements:
  - LNX-07
  - LNX-08
  - LNX-09
  - ACT-07
must_haves:
  truths:
    - "A production GTK app started against an empty isolated service creates its first workspace through the visible Name/Branch/Source dialog and opens a registered terminal without restart."
    - "While that app stays open, a separate CLI process can add, edit, rename, and remove workspaces; each change appears through durable invalidation and authoritative refresh."
    - "A removed workspace with a live terminal remains visibly orphaned until confirmed close, while pins, tab order/manual title, selected context, and unread Codex provider/title/detail remain coherent."
    - "A forced HTTP 409 replay gap refreshes once and resumes from the server cursor; count and multibyte UTF-8 byte maximum-plus-one inputs are rejected before mutation, while 32 authoritative pairs plus a separate live orphan render safely."
    - "Keyboard-only creation, launcher, workspace navigation, pin/reorder, attention focus, tab navigation, and live-close cancel/confirm work in the production surface without focus/IME regression."
    - "Light, dark, and high-contrast presentation at default and narrow widths plus 200% text scaling has recorded app-window evidence; selected/unread/destructive/status states remain distinguishable."
    - "A human assistive-technology pass confirms actual roles, names, selected/pressed/expanded/status state, focus order, and redundant provider attention text."
    - "All focused, native, TypeScript, dependency, and release gates pass after the real-surface UAT; synthetic harness output alone is not accepted as visual/AT proof."
  artifacts:
    - path: "scripts/verify-native.ts"
      provides: "Production hardening smoke and theme/keyboard evidence orchestration"
      contains: "smokeNativeHardening"
    - path: "package.json"
      provides: "Focused hardening acceptance command"
      contains: "native:smoke-hardening"
    - path: "docs/native-workspace-acceptance.md"
      provides: "External-sync, creation, keyboard, adaptive, theme, and live-close evidence matrix"
    - path: "docs/native-terminal-accessibility.md"
      provides: "Workspace-shell assistive-technology observation record"
  key_links:
    - from: "scripts/verify-native.ts"
      to: "native/linux/app.zig"
      via: "isolated production GTK hardening scenario drives real GActions/dialogs/workers and asserts emitted checkpoints"
      pattern: "GIT_STACKS_NATIVE_HARDENING"
    - from: "scripts/verify-native.ts"
      to: "src/service/main.ts"
      via: "managed service plus separate CLI processes share one isolated config root"
      pattern: "GIT_STACKS_CONFIG_DIR"
    - from: "docs/native-workspace-acceptance.md"
      to: "107-UI-REVIEW.md"
      via: "each visual, keyboard, adaptive, state, and close criterion has an observation/result"
      pattern: "light|dark|high contrast|keyboard|narrow|close"
---

<objective>
Prove the reopened Phase 107 behavior in the real production GTK/service/Ghostty surface, record visual and assistive-technology evidence, and run the complete regression gate before Phase 108.

Purpose: Unit/controller/source tests cannot prove an interactive empty-start app, outside-process reconciliation, theme contrast, keyboard focus, or real accessible widget behavior.
Output: A bounded production hardening smoke, evidence matrices, complete automated gates, and blocking human native UAT.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te/107-CONTEXT.md
@.planning/phases/107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te/107-PATTERNS.md
@.planning/phases/107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te/107-UI-REVIEW.md
@.planning/phases/107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te/107-12-SUMMARY.md
@docs/native-workspace-acceptance.md
@docs/native-terminal-accessibility.md

<interfaces>
`scripts/verify-native.ts` already owns graphical-session setup, managed-service fixtures, production app spawn/timeouts, diagnostics rejection, and `smokeWorkspaceLifecycle()` checkpoint matching.

The final scenario must run the production executable and named GActions/dialog callbacks. Model-only helpers, hand-fed reducer actions, or a fake snapshot server alone do not satisfy this plan.

Phase 108 packaging, installed-layout proof, Wayland/X11 matrix, and macOS execution remain out of scope; use the source checkout and current graphical test session only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a production empty-start, external-sync, Codex, replay, capacity, and orphan smoke</name>
  <read_first>
    - scripts/verify-native.ts
    - native/linux/app.zig
    - package.json
    - tests/service/workspace-sync.test.ts
    - native/tests/app_graph_test.zig
    - native/tests/service_client_test.zig
    - native/tests/application_actions_test.zig
    - .planning/phases/107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te/107-CONTEXT.md
  </read_first>
  <files>native/linux/app.zig, scripts/verify-native.ts, package.json</files>
  <behavior>
    - Isolated fixture has registered local Git repositories/templates but zero workspaces; the production app reaches visible empty/create state before any terminal exists.
    - Scripted production actions fill Name/Branch/Source, submit, observe operation progress, refresh, select first repository, and register a real Ghostty host.
    - Separate CLI processes add/edit/rename/remove workspace state; the running app observes monotonic invalidations without restart or focus theft.
    - Codex waiting attention with provider/title/detail/exact surface survives at least one periodic refresh and external mutation, activates exact focus, and clears on exact visible tab only.
    - Removing the live workspace shows orphan state; cancel close retains host; confirm performs one teardown and removes the orphan.
    - Forced replay gap adopts server cursor after refresh; count and multibyte UTF-8 byte max-plus-one service inputs have no filesystem effect; external max-plus-one snapshot shows capacity details while safe state remains.
    - A fixture with all 32 authoritative pair slots plus one live deleted-pair tombstone renders the orphan's last-known workspace/repository labels without dropping an authoritative pair or host.
  </behavior>
  <action>Extend the existing production test-mode dispatcher in `app.zig` with a narrowly gated `GIT_STACKS_NATIVE_HARDENING=1` scenario that activates the same GActions/dialog/controller/worker callbacks used by users and emits stable checkpoint markers only after observable widget/model/registry conditions hold. In `verify-native.ts`, implement `smokeNativeHardening()` with a unique isolated config root, disposable real Git repositories, registry/template fixtures, an initially empty managed service, and the production GTK binary under the graphical-session helper. Drive the visible create flow, then use separate Bun CLI subprocesses for outside add/edit/rename/remove and service attention publication. Add deterministic hooks/fixture controls to force replay retention gap, every advertised collection/string-byte boundary, and a 32-authoritative-plus-one-live-orphan projection without changing production defaults. Require monotonic revision, stable cursor, exact provider fields, preserved presentation, last-known orphan labels/host identity, live-close cancel/confirm, Unicode byte-boundary rejection without mutation, one-shot cleanup, and no GTK/GLib/panic diagnostics. Bound the complete scenario to 90 seconds with process-group cleanup in `finally`. Add `native:smoke-hardening` and include it in `native:verify` only when graphical prerequisites are available under the existing policy.</action>
  <acceptance_criteria>
    - Smoke starts with zero workspaces and proves the real empty-state action/dialog before creation.
    - Every external mutation checkpoint is backed by a higher authoritative revision and visible production projection, not a hand-fed action.
    - Codex provider/title/detail/unread and exact focus/read behavior survive refresh; attention receipt never changes focus automatically.
    - Orphan checkpoint retains the same live host/process identity; cancel changes nothing; confirm produces one teardown and no orphan process.
    - Gap checkpoint shows one snapshot refresh and new cursor; capacity checkpoints prove no successful over-count or multibyte-over-byte-limit service mutation, exact UTF-8 boundaries remain accepted, and external overflow is explicit.
    - Exact 32 authoritative pairs plus one separate orphan remain visible together; the orphan row shows last-known labels and retains the same live host identity.
    - Timeout/failure always kills app/service/child process groups and removes isolated config/worktrees.
  </acceptance_criteria>
  <verify>
    <automated>bun run native:smoke-hardening</automated>
  </verify>
  <done>The highest-risk reopened behavior passes in one real, bounded, empty-start production GTK scenario with outside processes.</done>
</task>

<task type="auto">
  <name>Task 2: Prepare and prefill release evidence matrices from deterministic checks</name>
  <read_first>
    - docs/native-workspace-acceptance.md
    - docs/native-terminal-accessibility.md
    - scripts/verify-native.ts
    - .planning/phases/107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te/107-UI-REVIEW.md
    - .planning/phases/107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te/107-12-SUMMARY.md
  </read_first>
  <files>scripts/verify-native.ts, docs/native-workspace-acceptance.md, docs/native-terminal-accessibility.md</files>
  <action>Extend the workspace acceptance document with a dated Phase 107 hardening matrix for creation, external add/edit/rename/remove, replay gap, attention preservation/routing, orphan/live close, capacity, launcher keyboard, pin/reorder keyboard, actionable states, default/narrow/200%-text layouts, and light/dark/high-contrast modes. Extend the accessibility document with workspace-shell rows for names/roles, selected/pressed/expanded/status properties, icon tooltips, focus order, attention redundant text, launcher, creation dialog, sidebar overlay, and live-close dialog; preserve truthful `unsupported/unverified` language until observed. Add a verify-native evidence preflight that checks every required row/column and automatically records command/checkpoint references, environment, dimensions, theme, and pending-result cells without claiming human observations. Store any app-window-only captures under the ignored `.planning/ui-reviews/` path; do not capture the whole desktop or terminal secrets.</action>
  <acceptance_criteria>
    - Acceptance matrix has explicit rows for all Phase 107 success criteria and all 20 UI-review warning themes, with automated reference and human observation columns.
    - Theme matrix covers light, dark, high contrast at 800x480 and 700px narrow plus one 200% text run.
    - Accessibility matrix covers actual widget roles/states/names/focus and clearly distinguishes automated evidence from human AT observation.
    - Preflight fails if a required row is absent and never turns a placeholder into `pass` without a real recorded observation.
  </acceptance_criteria>
  <verify>
    <automated>bun run native:test:accessibility &amp;&amp; bun run native:smoke-hardening</automated>
  </verify>
  <done>Final UAT has a complete, non-self-certifying evidence contract tied to deterministic production checks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Perform final native visual, keyboard, and assistive-technology UAT</name>
  <read_first>
    - docs/native-workspace-acceptance.md
    - docs/native-terminal-accessibility.md
    - .planning/phases/107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te/107-UI-REVIEW.md
    - .planning/phases/107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te/107-CONTEXT.md
  </read_first>
  <files>docs/native-workspace-acceptance.md, docs/native-terminal-accessibility.md</files>
  <action>After every automated command below passes, launch the production native app against the prepared isolated hardening fixture and present the exact observation sequence in `how-to-verify`. Record the user's pass/fail notes in the two evidence matrices after the checkpoint resumes. Do not accept smoke markers, model tests, source inspection, or screenshots alone as proof of keyboard feel, visual hierarchy, focus, contrast, or assistive-technology announcements. If any observation fails, record it as a Phase 107 gap and stop completion rather than deferring it to Phase 108.</action>
  <acceptance_criteria>
    - User completes each numbered visual, keyboard, live-close, adaptive, theme, and AT observation on the production GTK surface.
    - Evidence documents contain date/environment/result/notes for every required human row and no unresolved placeholder remains.
    - Any failure blocks Phase 107 completion and is reported for a new gap plan; packaging/macOS work is not used as an excuse or substitute.
    - After approved observations, every full regression command below passes on the final tree.
  </acceptance_criteria>
  <verify>
    <automated>bun run native:smoke-hardening &amp;&amp; bun run native:verify &amp;&amp; bun run test &amp;&amp; bun run typecheck &amp;&amp; bun run test:deps &amp;&amp; bun run verify:gates</automated>
    <human-check>Complete the production app observations below and approve only when the evidence matrices match what was actually observed.</human-check>
  </verify>
  <what-built>Feature-complete and polished Phase 107 native client: first-workspace creation, external synchronization, Codex attention, safe terminal ownership, adaptive/actionable UI, keyboard completeness, and accessibility semantics.</what-built>
  <how-to-verify>
    1. Start with the isolated zero-workspace fixture. Confirm the window is useful, Create workspace is primary, and Name/Branch/Source creates from a template and from multiple registered repositories with progress/failure recovery.
    2. Keep a terminal live. From a second terminal run the prepared CLI add/edit/rename/remove steps. Confirm each change appears without app restart, existing tab/title/pin/order/attention stays coherent, and deletion shows an orphan rather than killing the host.
    3. Publish the prepared Codex waiting item. Confirm provider, title, detail, unread, and location; confirm it does not steal focus; activate the row and confirm exact focus; select the exact tab and confirm only direct attention clears.
    4. Use keyboard only: open/complete/cancel creation, navigate sidebar/groups/tabs, toggle and reorder pin, open launcher, filter, Up/Down/Enter, observe no-commands/no-match, restore focus with Escape, and exercise live-close cancel then confirm.
    5. Resize across 720px and to 700px, then test 200% text. Confirm overlay sidebar, terminal visibility, dialog clamping, actionable state pages, and focus order.
    6. Inspect light, dark, and high-contrast modes at 800x480 and narrow width. Confirm selected/group/unread/destructive/status hierarchy, typography, spacing, icons, truncation/tooltips, and no low-contrast alpha-only dependency.
    7. With the available screen reader/accessibility inspector, verify control names/roles, selected pair, grouping/pin pressed state, expander/status state, attention redundant text, icon-only labels, dialog names, and logical focus announcements.
  </how-to-verify>
  <resume-signal>Type "approved" only after all seven observations pass, or describe every failed observation.</resume-signal>
  <done>Human native UAT and the complete automated gate both pass on the final Phase 107 tree, with evidence recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Acceptance harness to real user config/processes | Test orchestration must remain isolated and must not mutate or capture unrelated workspaces/terminals. |
| Automated evidence to release decision | Synthetic markers can falsely imply visual, keyboard, or AT quality without human observation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-107-13-01 | Tampering | isolated hardening fixture | high | mitigate | Unique config/worktree roots, disposable local repos, bounded subprocess environment, and cleanup in `finally`. |
| T-107-13-02 | Information Disclosure | screenshots/logs | high | mitigate | Capture app window only under ignored path, use synthetic fixture data, reject whole-desktop/secret capture. |
| T-107-13-03 | Repudiation | visual/AT acceptance | high | mitigate | Separate automated references from dated human observations and block on unresolved rows. |
| T-107-13-04 | Denial of Service | graphical smoke children | medium | mitigate | 90-second timeout, process-group cancellation, service/app join, and artifact cleanup. |
</threat_model>

## Multi-Source Coverage Audit

| Source | ID/item | Coverage | Status |
|--------|---------|----------|--------|
| GOAL | Coherent Supacode-quality native workspace interface | 107-01, 107-10, 107-11, 107-12, 107-13 | COVERED |
| REQ | LNX-07 native Name/Branch/Source creation | 107-02, 107-03, 107-08, 107-10, 107-13 | COVERED |
| REQ | LNX-08 automatic outside-client reconciliation | 107-04, 107-05, 107-07, 107-08, 107-09, 107-10, 107-13 | COVERED |
| REQ | LNX-09 keyboard/accessibility/close/adaptive/actionable | 107-11, 107-12, 107-13 | COVERED |
| REQ | ACT-07 truthful Codex/provider presentation and routing | 107-06, 107-07, 107-08, 107-09, 107-11, 107-13 | COVERED |
| RESEARCH | No 107-RESEARCH.md exists; PATTERNS.md is the technical analog contract | 107-02 through 107-13 | N/A |
| CONTEXT | Original pin/group/repository/title/visual decisions | 107-01 plus 107-12/107-13 regression proof | COVERED |
| CONTEXT | Dialog has exactly name, branch, source | 107-02, 107-08, 107-10, 107-13 | COVERED |
| CONTEXT | One template or selected registered repos; engine owns paths/YAML | 107-02, 107-03, 107-08, 107-10 | COVERED |
| CONTEXT | Idempotent progress operation and useful empty installation | 107-03, 107-08, 107-10, 107-13 | COVERED |
| CONTEXT | Service-owned external monitoring/invalidation/reconciliation | 107-04, 107-05, 107-08, 107-09, 107-10, 107-13 | COVERED |
| CONTEXT | Preserve presentation/live hosts/attention and show live orphan | 107-07, 107-09, 107-10, 107-13 | COVERED |
| CONTEXT | Codex install/provider/title/detail/exact routing | 107-06, 107-07, 107-08, 107-11, 107-13 | COVERED |
| CONTEXT | Attention is additive and best-effort | 107-06, 107-13 | COVERED |
| CONTEXT | Exact tab clears direct unread; explicit row routes nearest | 107-07, 107-11, 107-13 | COVERED |
| CONTEXT | UI-review priorities are release criteria | 107-11, 107-12, 107-13 | COVERED |
| CONTEXT | Phase 108 packaging/macOS scope fence | Every new plan remains checkout-local Linux hardening | COVERED |
| UI REVIEW | Recovery copy and actionable non-ready states | 107-12, 107-13 | COVERED |
| UI REVIEW | Launcher no-command/no-match and Up/Down/Enter | 107-11, 107-13 | COVERED |
| UI REVIEW | Friendly errors; consistent terminal language; title-reset help | 107-12, 107-13 | COVERED |
| UI REVIEW | Workspace/repository glyphs and native organization toggle | 107-12, 107-13 | COVERED |
| UI REVIEW | Icon labels/tooltips and user-facing attention status | 107-11, 107-12, 107-13 | COVERED |
| UI REVIEW | Theme contrast and non-color state cues | 107-12, 107-13 | COVERED |
| UI REVIEW | Title-only expander weight and compact header attention | 107-12, 107-13 | COVERED |
| UI REVIEW | Coherent spacing scale and active adaptive breakpoints | 107-12, 107-13 | COVERED |
| UI REVIEW | Default/narrow/200%-text layout | 107-12, 107-13 | COVERED |
| UI REVIEW | Real accessible roles/states and rendered attention text | 107-11, 107-12, 107-13 | COVERED |
| UI REVIEW | Confirmation before live-terminal close | 107-11, 107-13 | COVERED |
| UI REVIEW | Final light/dark/high-contrast, keyboard, and AT evidence | 107-13 | COVERED |

## Artifacts this phase produces

- `smokeNativeHardening()` and `GIT_STACKS_NATIVE_HARDENING` production scenario
- `native:smoke-hardening`
- Phase 107 hardening evidence matrix in `docs/native-workspace-acceptance.md`
- Workspace-shell AT evidence matrix in `docs/native-terminal-accessibility.md`
- Dated final native visual/keyboard/assistive-technology UAT record

<verification>
Run the production hardening smoke, complete native verify, full isolated test runner, typecheck, dependency graph, and release gates. Then complete the blocking seven-step human UAT and rerun the full commands after evidence edits.
</verification>

<success_criteria>
All four Phase 107 requirements and every reopened context/UI-review criterion are demonstrated in the real native surface, with no unresolved human evidence and no regression gate failure before Phase 108 begins.
</success_criteria>

<output>
Create `.planning/phases/107-beautify-native-workspace-ui-and-finalize-sidebar-tab-and-te/107-13-SUMMARY.md` when done.
</output>
