---
phase: 74-template-label-cli-propagation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/template.ts
  - src/lib/labels.ts
  - tests/commands/template-label.test.ts
  - tests/commands/template-list.test.ts
autonomous: true
requirements:
  - TLBL-01
  - TLBL-02
  - TLBL-03
  - TLBL-04
  - TLBL-05
must_haves:
  truths:
    - "User can run `git-stacks template label add|remove|list|clear` and the template YAML changes immediately."
    - "`git-stacks template list --label <label>` keeps only templates whose `labels` arrays include every requested exact term."
    - "When no template matches the requested labels, the CLI prints `No templates match labels: ...` instead of the generic empty-template message."
  artifacts:
    - path: "src/commands/template.ts"
      provides: "Nested template label CRUD command group plus repeatable --label filtering on template list"
      contains: "new Command(\"label\")"
    - path: "src/lib/labels.ts"
      provides: "Shared exact-match AND helper that accepts any label-bearing entity"
      contains: "export function matchesLabels"
    - path: "tests/commands/template-label.test.ts"
      provides: "CLI coverage for template label add/remove/list/clear and invalid-label rejection"
      contains: "describe(\"template label command\""
    - path: "tests/commands/template-list.test.ts"
      provides: "CLI coverage for template list --label exact-match AND filtering"
      contains: "describe(\"template list --label\""
  key_links:
    - from: "src/commands/template.ts"
      to: "src/lib/config.ts"
      via: "readTemplate/writeTemplate/templateExists/listTemplates"
      pattern: "readTemplate|writeTemplate|templateExists|listTemplates"
    - from: "src/commands/template.ts"
      to: "src/lib/labels.ts"
      via: "matchesLabels() inside template list filtering"
      pattern: "matchesLabels\\("
---

<objective>
Deliver template-label CLI parity and template-list label filtering per D-01 through D-04.

Purpose: Expose template label management through the existing `template` command tree and keep label filter semantics identical to workspace listing.
Output: Nested `template label` commands, repeatable `template list --label`, and CLI regression coverage for CRUD/filter behavior.
</objective>

<execution_context>
@/home/nnex/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/74-template-label-cli-propagation/74-CONTEXT.md
@.planning/phases/74-template-label-cli-propagation/74-RESEARCH.md
@.planning/phases/74-template-label-cli-propagation/74-VALIDATION.md
@.planning/milestones/v0.14.0-phases/60-labels/60-01-SUMMARY.md
@.planning/milestones/v0.14.0-phases/60-labels/60-02-SUMMARY.md
@src/commands/template.ts
@src/commands/label.ts
@src/commands/workspace.ts
@src/lib/config.ts
@src/lib/labels.ts
@tests/commands/label.test.ts

<interfaces>
From src/lib/labels.ts:
```typescript
export function matchesLabels(workspace: Workspace, terms: string[]): boolean
```

From src/commands/label.ts:
```typescript
labelCommand
  .command("add <workspace> <labels...>")
  .command("remove <workspace> <labels...>")
  .command("list <workspace>")
  .command("clear <workspace>")
```

From src/commands/workspace.ts:
```typescript
.option("--label <tag>", "Filter by label (repeatable, AND logic)", (val: string, arr: string[]) => {
  arr.push(val)
  return arr
}, [] as string[])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CLI regression coverage for template label CRUD and template list filtering</name>
  <files>tests/commands/template-label.test.ts, tests/commands/template-list.test.ts</files>
  <read_first>
    - tests/commands/template-label.test.ts (confirm the file does not exist yet before creating it)
    - tests/commands/template-list.test.ts (confirm the file does not exist yet before creating it)
    - tests/commands/label.test.ts
    - tests/commands/list-columns.test.ts
    - src/commands/template.ts
    - src/commands/label.ts
    - src/lib/config.ts
  </read_first>
  <behavior>
    - Test 1: `bun run src/index.ts template label add api backend release:1` writes both labels once and prints `Labels: backend, release:1`.
    - Test 2: `template label list api` prints one label per line and `template label clear api` removes the `labels:` field and prints `Labels cleared.`.
    - Test 3: `template label remove api backend` prints `No labels remaining.` when the last label is removed, and `template label add api "bad label"` exits 1 with `Invalid label "bad label"`.
    - Test 4: `template list --label backend --label sprint:14` returns only templates containing both exact labels, while `template list --label frontend` prints `No templates match labels: frontend`.
  </behavior>
  <action>Create `tests/commands/template-label.test.ts` and `tests/commands/template-list.test.ts` using the same `Bun.spawnSync(["bun", "run", "src/index.ts", ...])` fixture pattern as `tests/commands/label.test.ts`. The template-label fixture must create `config.yml`, `registry.yml`, and `templates/api.yml` under a temp `GIT_STACKS_CONFIG_DIR`; the template YAML must start with `labels: [backend]` so add/list/remove/clear can assert direct persistence. The test cases must assert these exact user-visible strings: `Labels: backend, release:1`, `No labels.`, `No labels remaining.`, `Labels cleared.`, and `Invalid label \"bad label\"`. Create `tests/commands/template-list.test.ts` with three template YAML fixtures named `api`, `web`, and `ops`; set `api.labels` to `["backend", "sprint:14"]`, `web.labels` to `["frontend"]`, and `ops.labels` to `["backend"]`. Assert that `bun run src/index.ts template list --label backend --label sprint:14` contains `api` but not `web` or `ops`, and that `bun run src/index.ts template list --label frontend --label missing` prints `No templates match labels: frontend, missing`.</action>
  <acceptance_criteria>
    - `tests/commands/template-label.test.ts` exists and contains `describe("template label command"`.
    - `tests/commands/template-label.test.ts` contains the exact output strings `Labels cleared.` and `No labels remaining.`.
    - `tests/commands/template-list.test.ts` exists and contains `describe("template list --label"`.
    - `tests/commands/template-list.test.ts` asserts the exact string `No templates match labels: frontend, missing`.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/commands/template-label.test.ts && bun test tests/commands/template-list.test.ts</automated>
  </verify>
  <done>Both CLI test files exist, fail before implementation, and cover TLBL-01 through TLBL-05 with exact output assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement nested template label commands and repeatable template list filtering</name>
  <files>src/commands/template.ts, src/lib/labels.ts</files>
  <read_first>
    - src/commands/template.ts
    - src/commands/label.ts
    - src/commands/workspace.ts
    - src/lib/labels.ts
    - src/lib/config.ts
    - tests/commands/template-label.test.ts
    - tests/commands/template-list.test.ts
  </read_first>
  <behavior>
    - Test 1: `template label add|remove|list|clear` mirrors workspace-label validation, deduplication, empty-state text, and direct YAML writes per D-01 and D-02.
    - Test 2: `template list --label` accepts repeatable flags and uses exact-match AND semantics per D-03 and D-04.
  </behavior>
  <action>In `src/lib/labels.ts`, replace the `Workspace`-specific signature with `type LabeledEntity = { labels?: string[] }` and change `matchesLabels` to `export function matchesLabels(entity: LabeledEntity, terms: string[]): boolean { if (terms.length === 0) return true; const labels = entity.labels ?? []; return terms.every(term => labels.includes(term)); }`. In `src/commands/template.ts`, import `matchesLabels` from `../lib/labels` and `formatError` from `../lib/errors`. Add `const LABEL_REGEX = /^[A-Za-z0-9._:-]+$/`, `validateLabel(label: string): void`, and `requireTemplate(name: string): void` helpers. `validateLabel` must emit `Invalid label "${label}": may only contain letters, digits, dots, colons, hyphens, underscores` and `process.exit(1)`. `requireTemplate` must gate every template-label mutation and exit with `formatError(\`Template '${name}' not found\`, "run: git-stacks template list")`. Replace the existing `template list` action with `.option("--label <tag>", "Filter by label (repeatable, AND logic)", (val: string, arr: string[]) => { arr.push(val); return arr }, [] as string[])`; filter `listTemplates()` through `matchesLabels(tpl, opts.label)`; print `No templates match labels: ${opts.label.join(", ")}` when labels were supplied and the filtered set is empty; otherwise keep the existing empty-template message and row formatting. Add `const templateLabelCommand = new Command("label").description("Manage template labels")` inside `template.ts`, register it with `templateCommand.addCommand(templateLabelCommand)`, and add these exact subcommands: `add <template> <labels...>` merges `tpl.labels ?? []` with `labels` via `new Set` and prints `Labels: ${merged.join(", ")}`; `remove <template> <labels...>` filters labels and writes `labels: undefined` when empty, then prints either `Labels: ${filtered.join(", ")}` or `No labels remaining.`; `list <template>` prints one label per line or `No labels.`; `clear <template>` writes `labels: undefined` and prints `Labels cleared.`. Do not add any new top-level command in `src/index.ts`; D-01 requires these commands to live under `git-stacks template label`.</action>
  <acceptance_criteria>
    - `src/commands/template.ts` contains `new Command("label")`.
    - `src/commands/template.ts` contains `.command("add <template> <labels...>")`, `.command("remove <template> <labels...>")`, `.command("list <template>")`, and `.command("clear <template>")`.
    - `src/commands/template.ts` contains `.option("--label <tag>", "Filter by label (repeatable, AND logic)"`.
    - `src/commands/template.ts` contains the exact strings `No templates match labels:` and `Labels cleared.`.
    - `src/lib/labels.ts` contains `type LabeledEntity = { labels?: string[] }`.
    - `src/lib/labels.ts` contains `return terms.every(term => labels.includes(term))`.
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck && bun test tests/commands/template-label.test.ts && bun test tests/commands/template-list.test.ts</automated>
  </verify>
  <done>`git-stacks template label ...` exists under the `template` command tree, `template list --label` behaves exactly like workspace label filtering, and all targeted tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> template YAML writes | Untrusted label strings cross into persisted config data. |
| Template YAML -> list output | Stored label values are rendered back to the local user and used for filter decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01 | T | `src/commands/template.ts` label add/remove commands | mitigate | Validate every CLI label with `^[A-Za-z0-9._:-]+$` before calling `writeTemplate()`, and keep Zod-backed template parsing as the second validation boundary. |
| T-74-02 | D | `src/commands/template.ts` list filter | mitigate | Use repeatable `--label` accumulation with exact `matchesLabels()` checks only; no regex or glob evaluation is introduced, which keeps filter cost linear in stored label count. |
| T-74-03 | R | `tests/commands/template-label.test.ts` | mitigate | Lock in exact stdout/stderr strings for invalid labels and no-match cases so future changes cannot silently weaken validation or error reporting. |
</threat_model>

<verification>
Run:

```bash
bun run typecheck
bun test tests/commands/template-label.test.ts
bun test tests/commands/template-list.test.ts
```

Both commands must pass. `template label` help output should be inspectable if a targeted test fails, but the automated gate is the typecheck plus the two command tests above.
</verification>

<success_criteria>
- `git-stacks template label add <template> <label...>` persists deduplicated labels to template YAML.
- `git-stacks template label remove/list/clear` mirrors workspace-label messaging and empty-state behavior.
- `git-stacks template list --label <label>` supports repeated flags with exact-match AND logic.
- Missing-match output is `No templates match labels: ...`, not the generic empty-template message.
</success_criteria>

<output>
After completion, create `.planning/phases/74-template-label-cli-propagation/74-01-SUMMARY.md`
</output>
