# Phase 81.1: Repo add honors enabled forge integrations - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-10T20:22:00Z
**Phase:** 81.1-repo-add-honors-enabled-forge-integrations
**Areas discussed:** Enablement source of truth

---

## Enablement source of truth

### Forge detector allowlist

| Option | Description | Selected |
|--------|-------------|----------|
| Global integration config only | Use global integration enablement as the sole source of truth for which forge detectors `repo add` may consider. | ✓ |
| Global config when present, otherwise installed CLI fallback | Treat config as advisory and still infer from installed CLIs when unset. | |
| Separate repo-add-specific setting or flag | Introduce new config/flag just for repo registration behavior. | |

**User's choice:** Global integration config only.
**Notes:** The user wants `repo add` to respect the integrations actually enabled in git-stacks config rather than treating all installed forge CLIs as eligible.

---

### Disabled forge handling

| Option | Description | Selected |
|--------|-------------|----------|
| Hide from both detection and prompt options | Disabled forges are invisible to `repo add`. | ✓ |
| No auto-detection, but still allow manual prompt selection | Disabled forges stay manually selectable. | |
| Show in prompt as disabled/unavailable | Keep them visible for explanation only. | |

**User's choice:** Hide from both detection and prompt options.
**Notes:** Disabled forges should not appear anywhere in the `repo add` decision path.

---

### No enabled match

| Option | Description | Selected |
|--------|-------------|----------|
| Register without a forge and do not prompt | Keep `repo add` deterministic when no enabled forge matches. | ✓ |
| Prompt with enabled subset plus None | Still ask the user to choose among enabled options. | |
| Fail with a clear error | Require explicit forge enablement before adding the repo. | |

**User's choice:** Register without a forge and do not prompt.
**Notes:** The point of the fix is to avoid surprise prompts when no enabled forge is in play.

---

### Multiple enabled matches

| Option | Description | Selected |
|--------|-------------|----------|
| Prompt among enabled matches plus None | Preserve user choice only for real enabled ambiguity. | ✓ |
| Auto-pick by fixed priority | Resolve ambiguity silently using a hard-coded ranking. | |
| Register without a forge and warn | Avoid prompting even when multiple enabled matches exist. | |

**User's choice:** Prompt only among the enabled matching forges plus None.
**Notes:** Prompting remains acceptable when the ambiguity is real and restricted to enabled choices.

---

## the agent's Discretion

- Exact helper extraction and prompt wording.
- Exact placement of the enablement filtering between `repo add` and forge detection helpers.
- Exact test split across command-level and lower-level verification.

## Deferred Ideas

None.
