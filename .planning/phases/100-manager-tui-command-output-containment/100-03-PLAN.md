---
phase: 100-manager-tui-command-output-containment
plan: 03
type: execute
wave: 3
depends_on: ["100-02"]
files_modified:
  - tests/tui/dashboard/integ-action-menu.test.tsx
  - tests/tui/dashboard/snapshots/ProgressView.snap.test.tsx
  - tests/lib/lifecycle.test.ts
  - tests/lib/workspace-command.test.ts
  - tests/tui/dashboard/issue-actions.test.ts
autonomous: true
requirements:
  - TOUT-01
  - TOUT-02
  - TOUT-03
  - TOUT-04

must_haves:
  truths:
    - "Long, stderr-only, no-output success, nonzero exit, and interrupted/cancelled command states have automated coverage or explicit documented non-support"
    - "A dashboard character-frame regression proves noisy command output remains inside the OpenTUI modal"
    - "`bun run typecheck` passes after command-output API changes"
    - "`bun run test` passes or any unrelated pre-existing failure is documented with evidence"
  artifacts:
    - path: "tests/tui/dashboard/integ-action-menu.test.tsx"
      provides: "Noisy command and restore regression coverage"
      contains: "captureCharFrame"
    - path: "tests/tui/dashboard/snapshots/ProgressView.snap.test.tsx"
      provides: "Viewer state regression coverage"
      contains: "earlier lines omitted"
    - path: "tests/lib/lifecycle.test.ts"
      provides: "Captured shell edge-case coverage"
      contains: "stderr"
  key_links:
    - from: "tests/tui/dashboard/integ-action-menu.test.tsx"
      to: "src/tui/dashboard/App.tsx"
      via: "manual command output callback is mocked and rendered through dashboard frame"
      pattern: "runManualCommandMock"
    - from: "tests/tui/dashboard/snapshots/ProgressView.snap.test.tsx"
      to: "src/tui/dashboard/ProgressView.tsx"
      via: "all output status variants render inside CenteredDialog"
      pattern: "Command failed"
---

<objective>
Close the regression matrix for command-output containment and run the phase-level verification gates.

Purpose: The first two plans add the model, runner, and UI wiring. This plan proves the reported RC defect cannot recur across noisy, failing, stderr-only, and no-output cases, then runs the canonical verification commands before handoff to `$gsd-execute-phase` and later `$gsd-verify-work`.

Output: strengthened regression tests and verification evidence for Phase 100.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/100-manager-tui-command-output-containment/100-CONTEXT.md
@.planning/phases/100-manager-tui-command-output-containment/100-RESEARCH.md
@.planning/phases/100-manager-tui-command-output-containment/100-PATTERNS.md
@.planning/phases/100-manager-tui-command-output-containment/100-VALIDATION.md
@.planning/phases/100-manager-tui-command-output-containment/100-01-SUMMARY.md
@.planning/phases/100-manager-tui-command-output-containment/100-02-SUMMARY.md

@tests/tui/dashboard/integ-action-menu.test.tsx
@tests/tui/dashboard/snapshots/ProgressView.snap.test.tsx
@tests/lib/lifecycle.test.ts
@tests/lib/workspace-command.test.ts
@tests/tui/dashboard/issue-actions.test.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| A future command path reintroduces inherited stderr without detection | high | Add source/behavior assertions around known paths and character-frame dashboard regression coverage. |
| Long output breaks modal layout or hides failure details | medium | Test bounded tail, omitted marker, and final failure/status lines. |
| No-output commands look stuck because the viewer has no diagnostic state | medium | Test explicit empty/running and no-output success states. |
| Verification passes without proving real alternate-screen containment | medium | Include a TUI integration frame capture and a manual smoke instruction in summary evidence. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add edge-case coverage for output states</name>
  <files>tests/tui/dashboard/snapshots/ProgressView.snap.test.tsx, tests/lib/lifecycle.test.ts, tests/lib/workspace-command.test.ts, tests/tui/dashboard/issue-actions.test.ts</files>
  <read_first>
    - tests/tui/dashboard/snapshots/ProgressView.snap.test.tsx (current ProgressView tests)
    - tests/lib/lifecycle.test.ts (captured stream mock helpers)
    - tests/lib/workspace-command.test.ts (manual command callback tests from Plan 01)
    - tests/tui/dashboard/issue-actions.test.ts (issue open captured stream tests from Plan 02)
  </read_first>
  <action>
    Add focused tests for: long output truncation with omitted count, stderr-only output, no-output success, nonzero exit state, and captured sequence first-failure stop. If interrupted/cancelled commands are not represented by the current runner API, document that the Phase 100 implementation exposes `cancelled` as a display status type but no cancellation control is implemented because D-09 blocks dismissing running commands and D-11 excludes hidden background state.
  </action>
  <acceptance_criteria>
    - `ProgressView` tests contain assertions for `earlier lines omitted`, `No command output yet`, `Command failed`, and a stderr line.
    - Lifecycle tests contain a captured stderr-only case.
    - Workspace command tests contain a no-output success callback case or prove no callback calls still produce explicit completion in `App.tsx`.
    - Issue action tests contain a nonzero exit case and assert `exitCode` is preserved.
  </acceptance_criteria>
  <verify>
    bun run test:unit
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add dashboard noisy-command containment regression</name>
  <files>tests/tui/dashboard/integ-action-menu.test.tsx</files>
  <read_first>
    - tests/tui/dashboard/integ-action-menu.test.tsx (existing manual command integration tests and `captureCharFrame` usage)
    - src/tui/dashboard/App.tsx (manual command output callback and close behavior)
  </read_first>
  <action>
    Extend the manual command integration coverage so `runManualCommandMock` invokes the supplied `onOutput` callback with at least 120 mixed stdout/stderr lines before resolving. Assert the captured frame contains the progress title, recent tail output, omitted-lines marker, and stderr/failure text where applicable. Assert old early lines that should have been truncated are absent from the frame. After command completion, press a key and assert the frame returns to the workspace list on the workspaces tab.
  </action>
  <acceptance_criteria>
    - Integration test uses `captureCharFrame()` to inspect the OpenTUI frame after noisy command output.
    - The frame contains a recent line such as `line-119` and does not contain an early truncated line such as `line-001`.
    - The frame contains the omitted-lines marker.
    - After close, the frame contains `Workspaces` and the test workspace name, and does not contain `Running command verify`.
  </acceptance_criteria>
  <verify>
    bun run test:integ
  </verify>
</task>

<task type="auto">
  <name>Task 3: Run full verification and record evidence</name>
  <files>.planning/phases/100-manager-tui-command-output-containment/100-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/100-manager-tui-command-output-containment/100-VALIDATION.md (phase validation map and full-suite commands)
    - package.json (current script names)
    - scripts/test-runner.ts (custom test runner behavior)
  </read_first>
  <action>
    Run `bun run typecheck` and `bun run test`. If either fails, determine whether the failure is caused by Phase 100 changes. Fix Phase 100-caused failures before completing this plan. If a failure is unrelated and pre-existing, capture the exact command, failing test/file, and reason in the summary deviations section. Add a manual smoke note for a real `git-stacks manage` noisy command only if it was run; otherwise state that the automated character-frame regression is the completed gate and manual smoke remains for `$gsd-verify-work`.
  </action>
  <acceptance_criteria>
    - `bun run typecheck` exits 0 or the summary documents an unrelated pre-existing failure with exact evidence.
    - `bun run test` exits 0 or the summary documents an unrelated pre-existing failure with exact evidence.
    - `100-03-SUMMARY.md` records the focused and full verification commands with pass/fail status.
    - No Phase 100-caused test failure remains open at plan completion.
  </acceptance_criteria>
  <verify>
    bun run typecheck
    bun run test
  </verify>
</task>

</tasks>

<verification>
- `bun run test:unit`
- `bun run test:integ`
- `bun run typecheck`
- `bun run test`
</verification>

<success_criteria>
- The command-output viewer has automated coverage for noisy, stderr-only, no-output, success, and failure states.
- A TUI integration frame proves command output stays inside the OpenTUI modal and closes back to the dashboard context.
- Full typecheck and test gates are run with evidence.
- All Phase 100 requirements TOUT-01 through TOUT-04 are covered by plans and verification.
</success_criteria>
