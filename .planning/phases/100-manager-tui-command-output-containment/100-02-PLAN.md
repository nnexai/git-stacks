---
phase: 100-manager-tui-command-output-containment
plan: 02
type: execute
wave: 2
depends_on: ["100-01"]
files_modified:
  - src/tui/dashboard/App.tsx
  - src/tui/dashboard/ProgressView.tsx
  - src/tui/dashboard/issue-actions.ts
  - tests/tui/dashboard/snapshots/ProgressView.snap.test.tsx
  - tests/tui/dashboard/issue-actions.test.ts
  - tests/tui/dashboard/integ-action-menu.test.tsx
autonomous: true
requirements:
  - TOUT-01
  - TOUT-02
  - TOUT-03
  - TOUT-04

must_haves:
  truths:
    - "D-01: All non-editor TUI-launched run, manual command, and issue-open output is captured into viewer state"
    - "D-03: The existing progress-modal pattern is upgraded into the bounded command-output viewer"
    - "D-04: The viewer shows command label, running/exit status, recent output, and close/back behavior"
    - "D-05: The viewer renders only the bounded recent tail of command output"
    - "D-06: The viewer receives stdout/stderr events in callback delivery order"
    - "D-07: Stderr lines are visually distinguished from stdout/system lines"
    - "D-08: The viewer shows an omitted-lines marker when older output is dropped"
    - "D-09: Running commands cannot be dismissed into hidden background state"
    - "D-10: Finished command viewers close back to the existing dashboard list context"
    - "D-11: No hidden background command queue or background command state is introduced"
    - "`handleRun()` no longer uses `stderr: \"inherit\"` while OpenTUI is active"
    - "`executeManualCommand()` passes an output callback to `runManualCommand()` and displays emitted stdout/stderr lines"
    - "`ProgressView` renders running, success, failure, stderr, omitted-lines, and no-output states inside `CenteredDialog`"
    - "Finished command viewers close through the existing post-progress key path and preserve active tab/list context"
  artifacts:
    - path: "src/tui/dashboard/App.tsx"
      provides: "D-01/D-09/D-10/D-11 TUI command lifecycle wiring through bounded structured output"
      contains: "appendCommandOutput"
    - path: "src/tui/dashboard/ProgressView.tsx"
      provides: "D-03/D-04/D-05/D-07/D-08 bounded command-output modal viewer"
      contains: "omitted"
    - path: "src/tui/dashboard/issue-actions.ts"
      provides: "Issue open returns tagged captured output"
      contains: "stream"
  key_links:
    - from: "src/tui/dashboard/App.tsx"
      to: "src/lib/workspace-command.ts"
      via: "executeManualCommand passes onOutput callback"
      pattern: "onOutput"
    - from: "src/tui/dashboard/App.tsx"
      to: "src/tui/dashboard/ProgressView.tsx"
      via: "ProgressView receives structured command output state"
      pattern: "output="
---

<objective>
Convert the manager progress modal into a bounded command-output viewer and route TUI command paths through captured stdout/stderr.

Purpose: This plan removes the known raw-output corruption paths from `git-stacks manage`, shows useful bounded diagnostics to the operator, and keeps completion/close behavior inside the existing dashboard modal flow.

Output: updated dashboard command wiring, upgraded `ProgressView`, tagged issue-open output, and focused component/integration tests.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/100-manager-tui-command-output-containment/100-CONTEXT.md
@.planning/phases/100-manager-tui-command-output-containment/100-RESEARCH.md
@.planning/phases/100-manager-tui-command-output-containment/100-PATTERNS.md
@.planning/phases/100-manager-tui-command-output-containment/100-UI-SPEC.md
@.planning/phases/100-manager-tui-command-output-containment/100-VALIDATION.md
@.planning/phases/100-manager-tui-command-output-containment/100-01-SUMMARY.md

@src/tui/dashboard/App.tsx
@src/tui/dashboard/ProgressView.tsx
@src/tui/dashboard/issue-actions.ts
@src/tui/dashboard/CenteredDialog.tsx
@tests/tui/dashboard/snapshots/ProgressView.snap.test.tsx
@tests/tui/dashboard/issue-actions.test.ts
@tests/tui/dashboard/integ-action-menu.test.tsx
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| `git-stacks run` stderr writes raw bytes behind OpenTUI | high | Change `handleRun()` to pipe stderr and append tagged output through the bounded helper. |
| Manual command output is hidden after switching to captured mode | medium | Display all callback output in the viewer and add explicit success/failure system lines. |
| Running commands are accidentally dismissible into hidden background state | medium | Keep the existing global-key guard while `progressDone` is false and render close/back copy only after completion. |
| Output viewer loses operator context after close | medium | Reuse existing post-progress key path, reload, and `clampCursor()` without resetting `tab`, selected row, or action context beyond returning to list. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade ProgressView to structured bounded output</name>
  <files>src/tui/dashboard/ProgressView.tsx, tests/tui/dashboard/snapshots/ProgressView.snap.test.tsx</files>
  <read_first>
    - src/tui/dashboard/ProgressView.tsx (current centered modal)
    - src/tui/dashboard/CenteredDialog.tsx (dialog sizing and title conventions)
    - src/tui/dashboard/command-output.ts (types and helper from Plan 01)
    - .planning/phases/100-manager-tui-command-output-containment/100-UI-SPEC.md (copy/color contract)
  </read_first>
  <action>
    Change `ProgressView` props to accept structured command output state, plus a title and optional legacy compatibility only if needed to keep unrelated progress callers compiling during this plan. Render a top status line with `Working...` for `status: "running"`, `Done. Press any key to continue.` for success, and `Command failed. Review stderr lines, then close and retry from the action menu.` plus close hint for failure. Render `No command output yet` when there are no lines. If `omittedCount > 0`, render `... N earlier lines omitted ...` before recent output. Render stderr lines with `fg="red"` or the project-supported destructive color and stdout/system lines with the existing muted/white style.
  </action>
  <acceptance_criteria>
    - `ProgressView` renders `No command output yet` for a running command with zero lines.
    - `ProgressView` renders `... 12 earlier lines omitted ...` when `omittedCount === 12`.
    - `ProgressView` renders stderr text in a distinct destructive color path in source, using `stream === "stderr"` as the condition.
    - ProgressView tests cover running, success, failure, omitted marker, and stderr display.
  </acceptance_criteria>
  <verify>
    bun run test:unit
  </verify>
</task>

<task type="auto">
  <name>Task 2: Route run, manual command, and issue-open output through captured viewer state</name>
  <files>src/tui/dashboard/App.tsx, src/tui/dashboard/issue-actions.ts, tests/tui/dashboard/issue-actions.test.ts, tests/tui/dashboard/integ-action-menu.test.tsx</files>
  <read_first>
    - src/tui/dashboard/App.tsx (`handleRun()`, `executeManualCommand()`, `executeIssueOpen()`, progress key handling, `ProgressView` render site)
    - src/tui/dashboard/issue-actions.ts (`openWorkspaceIssue()`)
    - src/lib/workspace-command.ts (`runManualCommand()` callback shape from Plan 01)
    - tests/tui/dashboard/integ-action-menu.test.tsx (manual command flow)
    - tests/tui/dashboard/issue-actions.test.ts (issue open spawn tests)
  </read_first>
  <action>
    Replace `progressLines` string state with command output state from `command-output.ts`, or add a parallel state and adapt all progress callers in this file. Add local helpers `resetCommandOutput(status = "running")`, `appendCommandLine(line)`, and `finishCommandOutput(status)`. Update `handleRun()` to spawn `["git-stacks", "run", name]` with `stdout: "pipe"` and `stderr: "pipe"`, drain both streams concurrently, append tagged lines, then set success/failure based on `proc.exited`. Update `executeManualCommand()` to pass `onOutput` to `runManualCommand()` and append callback lines. Update `executeIssueOpen()` to append tagged issue output lines and status lines. Update `openWorkspaceIssue()` result lines from `string[]` to tagged output lines while preserving `exitCode`.
  </action>
  <acceptance_criteria>
    - `src/tui/dashboard/App.tsx` no longer contains `stderr: "inherit"` inside `handleRun()`.
    - `executeManualCommand()` calls `runManualCommand(workspace, commandName, expect.objectContaining({ onOutput: ... }))` in the integration test.
    - Issue action tests prove stdout and stderr are both captured and returned with `stream` fields.
    - Existing open/close/clean/remove/merge progress paths still render their existing text through system output lines.
    - A manual command failure frame contains `ERROR: Command verify failed with exit code 42.` and `Failed command: bun test.` inside the progress viewer.
  </acceptance_criteria>
  <verify>
    bun run test:unit
    bun run test:integ
  </verify>
</task>

<task type="auto">
  <name>Task 3: Preserve close/back context after command completion</name>
  <files>src/tui/dashboard/App.tsx, tests/tui/dashboard/integ-action-menu.test.tsx</files>
  <read_first>
    - src/tui/dashboard/App.tsx (global `useKeyboard` handling for `view().view === "progress"`, tab/cursor state, reload/clamp behavior)
    - tests/tui/dashboard/integ-action-menu.test.tsx (manual command interaction helpers)
  </read_first>
  <action>
    Keep global keyboard handling so keys are ignored while command status is running and so a key after success/failure returns to `view: "list"`, calls `reload()`, and clamps cursor without changing `tab()`. Add or update integration tests that select a workspace row, open the manual command picker, run a command, wait for completion, press a close key, and assert the workspace tab is still active and the same workspace/command context is visible or selected. Add a failure variant to prove nonzero exit closes through the same path after completion.
  </action>
  <acceptance_criteria>
    - A test proves pressing a key while command output status is running does not close the viewer.
    - A test proves pressing a key after command success returns to the workspace list without switching to templates or repos.
    - A test proves pressing a key after command failure returns to the workspace list and does not leave stale `command-picker` or `progress` view text visible.
    - Source still contains no hidden background command queue/state for dismissed running commands.
  </acceptance_criteria>
  <verify>
    bun run test:integ
  </verify>
</task>

</tasks>

<verification>
- `bun run test:unit`
- `bun run test:integ`
- `bun run typecheck`
</verification>

<success_criteria>
- TUI run/manual/issue-open command output is captured and displayed inside the OpenTUI viewer.
- Stderr is visibly distinct and never inherited by the dashboard process for these paths.
- Long output is bounded with an omitted-lines marker.
- Closing after completion restores the dashboard list context without raw output corruption.
</success_criteria>
