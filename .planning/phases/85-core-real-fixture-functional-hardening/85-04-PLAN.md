---
phase: 85-core-real-fixture-functional-hardening
plan: 04
type: execute
wave: 2
depends_on:
  - 85-01
  - 85-02
  - 85-03
files_modified:
  - .planning/phases/85-core-real-fixture-functional-hardening/85-COVERAGE-REVIEW.md
  - tests/lib/core-source-coverage-gaps.test.ts
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-04
  - CORE-05
  - GATE-03
must_haves:
  truths:
    - "Phase 85 coverage improvements are verified against the canonical `.coverage/coverage-final.json` report."
    - "Focused gap closure executes real core source modules and remains inside the Phase 85 boundary."
    - "`bun run verify` remains the local umbrella gate after Phase 85 hardening tests land."
  artifacts:
    - path: ".planning/phases/85-core-real-fixture-functional-hardening/85-COVERAGE-REVIEW.md"
      provides: "Coverage report review for Phase 85 core source modules"
    - path: "tests/lib/core-source-coverage-gaps.test.ts"
      provides: "Focused source tests for high-value Phase 85 coverage gaps found after Plans 85-01 through 85-03"
  key_links:
    - from: "scripts/coverage-runner.ts"
      to: "scripts/verify-gates.ts"
      via: "manual report inspection documented by executor"
      pattern: "coverage-final.json"
    - from: "scripts/verify-gates.ts"
      to: "tests/lib/coverage-surface.test.ts"
      via: "direct imports of uncovered high-value source exports"
      pattern: "collectVerifyGateReport|coverageSentinels"
---

<objective>
Review canonical coverage after the Phase 85 hardening tests land and close focused core source gaps.

Purpose: CORE-05 requires that added coverage represents real source execution. GATE-03 requires the local verification path to stay green after the expanded tests.
Output: Coverage review notes and one focused core-source gap test file.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/85-core-real-fixture-functional-hardening/85-CONTEXT.md
@.planning/phases/85-core-real-fixture-functional-hardening/85-RESEARCH.md
@.planning/phases/85-core-real-fixture-functional-hardening/85-PATTERNS.md
@.planning/phases/85-core-real-fixture-functional-hardening/85-VALIDATION.md
@.planning/phases/84-local-coverage-gates-docs-and-release-prep/84-02-SUMMARY.md
@.planning/phases/84.1-coverage-report-accuracy-and-tui-instrumentation-follow-up/84.1-02-SUMMARY.md
@scripts/coverage-runner.ts
@scripts/verify-gates.ts
@tests/helpers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review canonical coverage for Phase 85 core modules</name>
  <files>.planning/phases/85-core-real-fixture-functional-hardening/85-COVERAGE-REVIEW.md</files>
  <action>Run `bun run coverage` after Plans 85-01 through 85-03 are complete. Inspect `.coverage/coverage-final.json` for `src/lib/workspace-ops.ts`, `src/lib/git.ts`, `src/lib/lifecycle.ts`, `src/lib/files.ts`, `src/lib/env.ts`, `src/lib/secrets.ts`, `src/lib/ports.ts`, and `src/lib/config.ts`. Create `85-COVERAGE-REVIEW.md` with the command run, artifact paths, targeted files, observed nonzero hit evidence, and high-value Phase 85 gaps selected for closure. Do not introduce numeric coverage thresholds, CI requirements, or Phase 86-88 scope.</action>
  <verify>
    <automated>test -f .planning/phases/85-core-real-fixture-functional-hardening/85-COVERAGE-REVIEW.md</automated>
    <automated>bun run coverage</automated>
  </verify>
  <done>Coverage review exists and identifies focused in-scope core source gaps based on canonical coverage artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close focused core source coverage gaps</name>
  <files>tests/lib/core-source-coverage-gaps.test.ts</files>
  <behavior>
    - Tests directly import real `src/lib/**` modules selected by the coverage review.
    - Tests cover high-value stable branches inside Phase 85 scope without copying implementation logic.
    - Tests avoid prompt-driving, broad TUI behavior, external desktop/plugin environments, and Phase 86-88 work.
  </behavior>
  <action>Create `tests/lib/core-source-coverage-gaps.test.ts` with focused tests for the gaps documented in `85-COVERAGE-REVIEW.md`. Keep each case tied to a Phase 85 core module and an observable safety or functional contract. Do not add broad command-wrapper assertions or integration contract tests.</action>
  <verify>
    <automated>bun test tests/lib/core-source-coverage-gaps.test.ts</automated>
    <automated>bun run coverage</automated>
    <automated>bun run verify:gates</automated>
    <automated>bun run verify</automated>
  </verify>
  <done>Focused gap tests pass, canonical coverage regenerates, verify gates pass, and `bun run verify` passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Coverage artifact -> planning evidence | Generated Istanbul JSON is interpreted to choose gap-closure tests. |
| Test files -> source coverage report | Added tests affect release-confidence evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-04-01 | Repudiation | coverage review | mitigate | Document exact command, artifact path, targeted source files, and selected gaps in `85-COVERAGE-REVIEW.md`. |
| T-85-04-02 | Tampering | coverage report interpretation | mitigate | Use canonical `bun run coverage` and inspect `.coverage/coverage-final.json`, not ad hoc report fragments. |
| T-85-04-03 | Information Disclosure | coverage artifacts | accept | Artifacts contain local source paths only and remain in ignored `.coverage/` output. |
</threat_model>

<verification>
Run `bun run coverage`, the focused gap test, `bun run verify:gates`, and `bun run verify`. Do not run milestone lifecycle or finalization commands.
</verification>

<success_criteria>
Coverage review and focused gap tests prove Phase 85 coverage came from real source execution; all Phase 85 requirements are represented; GATE-03 is verified through the local umbrella command.
</success_criteria>

<output>
After completion, create `.planning/phases/85-core-real-fixture-functional-hardening/85-04-SUMMARY.md`
</output>
