---
phase: 85-core-real-fixture-functional-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/lib/lifecycle-files-env-config-real-fixture.test.ts
autonomous: true
requirements:
  - CORE-03
  - CORE-04
  - CORE-05
  - GATE-03
must_haves:
  truths:
    - "Hook execution is covered for ordering, cwd, env injection, captured output, and failure propagation."
    - "File operation, env/secrets/ports, and config persistence behavior is covered where it affects workspace setup and cleanup."
    - "Config write behavior proves backward-compatible YAML reads and atomic writes through real config source exports."
  artifacts:
    - path: "tests/lib/lifecycle-files-env-config-real-fixture.test.ts"
      provides: "Real-fixture tests across lifecycle, files, env, secrets, ports, and config"
  key_links:
    - from: "src/lib/lifecycle.ts"
      to: "tests/lib/lifecycle.test.ts"
      via: "runHooks and runHooksCaptured with real subprocesses or the public _exec seam"
      pattern: "runHooksCaptured|runHooks"
    - from: "src/lib/files.ts"
      to: "tests/lib/files.test.ts"
      via: "real file copy/symlink/glob/external warning operations"
      pattern: "processFileList|applyFileOpsForWorkspace|warnExternalFiles"
    - from: "src/lib/config.ts"
      to: "tests/lib/config.test.ts"
      via: "real write/read round trips in isolated config homes"
      pattern: "writeWorkspace|readWorkspace|writeGlobalConfig|readGlobalConfig"
---

<objective>
Add real source coverage for lifecycle hooks, file operations, env/secrets/ports, and config persistence.

Purpose: CORE-03 and CORE-04 require confidence in hook ordering/cwd/env/output/failure behavior plus file, env, secret, port, and YAML persistence contracts. This plan keeps those tests source-oriented and automation-safe.
Output: `tests/lib/lifecycle-files-env-config-real-fixture.test.ts`.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/85-core-real-fixture-functional-hardening/85-CONTEXT.md
@.planning/phases/85-core-real-fixture-functional-hardening/85-RESEARCH.md
@.planning/phases/85-core-real-fixture-functional-hardening/85-PATTERNS.md
@.planning/phases/85-core-real-fixture-functional-hardening/85-VALIDATION.md
@.planning/phases/81-workspace-and-git-operation-e2e-coverage/81-02-SUMMARY.md
@tests/helpers.ts
@tests/lib/lifecycle.test.ts
@tests/lib/files.test.ts
@tests/lib/env.test.ts
@tests/lib/secrets.test.ts
@tests/lib/ports.test.ts
@tests/lib/config.test.ts
@src/lib/lifecycle.ts
@src/lib/files.ts
@src/lib/env.ts
@src/lib/secrets.ts
@src/lib/ports.ts
@src/lib/config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add hook execution real-fixture tests</name>
  <files>tests/lib/lifecycle-files-env-config-real-fixture.test.ts</files>
  <behavior>
    - Hook commands run in the requested cwd and receive explicit env values.
    - Captured output records stdout/stderr line data in order.
    - Failure propagation stops subsequent hooks when abort is enabled and preserves prior captured output.
  </behavior>
  <action>Create the combined test file and add lifecycle-focused cases using `runHooks` and `runHooksCaptured`. Use real temp directories and durable artifact files where practical. Use `_exec` only as the existing public seam for source behavior that cannot safely spawn. Restore `_exec` in cleanup if changed.</action>
  <verify>
    <automated>bun test tests/lib/lifecycle-files-env-config-real-fixture.test.ts</automated>
  </verify>
  <done>Hook ordering, cwd, env, captured output, and failure propagation behavior is covered through real source exports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add files, env, secrets, ports, and config persistence tests</name>
  <files>tests/lib/lifecycle-files-env-config-real-fixture.test.ts</files>
  <behavior>
    - File copy/symlink/glob/idempotency/external warning behavior is asserted through `src/lib/files.ts` exports.
    - Env resolver order, secret skip/failure behavior, port collision handling, and repo overlay behavior are asserted through real source functions.
    - Backward-compatible YAML reads and atomic workspace/global config writes are asserted in an isolated config home, including absence of leftover `.tmp` siblings.
  </behavior>
  <action>Extend the combined test file with focused cases for `files.ts`, `env.ts`, `secrets.ts`, `ports.ts`, and `config.ts`. Keep setup local and deterministic. Do not require macOS Keychain, Linux secret-tool, real external commands except tiny local fake executables under the temp fixture root. Do not add numeric coverage thresholds.</action>
  <verify>
    <automated>bun test tests/lib/lifecycle-files-env-config-real-fixture.test.ts</automated>
    <automated>bun run test:unit</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <done>CORE-03 and CORE-04 source tests pass, unit suite passes, and typecheck passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hook shell -> lifecycle source | Shell command exit codes and output enter lifecycle logic. |
| Fixture files -> file/config source | Temporary files and YAML are parsed and written by source modules. |
| Secret command resolver -> env source | Local fake command output becomes environment data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-03-01 | Elevation of Privilege | hook command execution | mitigate | Execute only tiny fixture-local shell commands and assert cwd/env explicitly. |
| T-85-03-02 | Tampering | config atomic writes | mitigate | Use isolated config homes and assert no leftover tmp files after writes. |
| T-85-03-03 | Information Disclosure | secret resolution | mitigate | Use env/fake command resolvers only and never call real keychain or secret-tool binaries. |
</threat_model>

<verification>
Run the focused combined test, `bun run test:unit`, and `bun run typecheck`. Keep all external effects inside temp directories.
</verification>

<success_criteria>
CORE-03 hook behavior and CORE-04 file/env/secrets/ports/config behavior are covered through real source exports; CORE-05 is satisfied by avoiding copied implementation logic; GATE-03 remains green for unit and typecheck gates.
</success_criteria>

<output>
After completion, create `.planning/phases/85-core-real-fixture-functional-hardening/85-03-SUMMARY.md`
</output>
