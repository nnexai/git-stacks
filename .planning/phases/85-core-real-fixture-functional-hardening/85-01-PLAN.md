---
phase: 85-core-real-fixture-functional-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/helpers.ts
  - tests/lib/workspace-ops-real-fixture.test.ts
autonomous: true
requirements:
  - CORE-01
  - CORE-03
  - CORE-05
  - GATE-03
must_haves:
  truths:
    - "Workspace lifecycle rollback, cleanup, rename, merge, missing-path, and destructive safety behavior is exercised against real temp workspaces and local git repositories."
    - "Shared Phase 85 fixture setup extends existing helpers instead of replacing the harness."
    - "Tests call real workspace source exports and do not inline workspace implementation logic."
  artifacts:
    - path: "tests/helpers.ts"
      provides: "Reusable helper additions for Phase 85 real workspace fixtures"
      exports: ["phase-specific reusable fixture helper if needed"]
    - path: "tests/lib/workspace-ops-real-fixture.test.ts"
      provides: "Real-fixture source tests for workspace lifecycle hardening"
  key_links:
    - from: "tests/helpers.ts"
      to: "src/lib/workspace-ops.ts"
      via: "real captured workspace operation exports from tests/helpers.ts"
      pattern: "real(Clean|Remove|Merge|Rename|Close)Workspace"
    - from: "tests/helpers.ts"
      to: "src/lib/git.ts"
      via: "shared temp config/git workspace fixture setup"
      pattern: "makeRepoWithRemote|makeWorkspaceFixture"
---

<objective>
Add real-fixture source coverage for core workspace lifecycle behavior.

Purpose: CORE-01 requires confidence in rollback, cleanup, rename, merge, missing path, and destructive safety behavior using real temp directories and local git repositories. CORE-05 requires that coverage come from real source execution.
Output: Reusable helper additions where needed and a focused workspace lifecycle real-fixture test file.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/85-core-real-fixture-functional-hardening/85-CONTEXT.md
@.planning/phases/85-core-real-fixture-functional-hardening/85-RESEARCH.md
@.planning/phases/85-core-real-fixture-functional-hardening/85-PATTERNS.md
@.planning/phases/85-core-real-fixture-functional-hardening/85-VALIDATION.md
@.planning/phases/81-workspace-and-git-operation-e2e-coverage/81-01-SUMMARY.md
@.planning/phases/81-workspace-and-git-operation-e2e-coverage/81-03-SUMMARY.md
@tests/helpers.ts
@tests/lib/workspace-ops.test.ts
@src/lib/workspace-ops.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reusable real workspace fixture helper only if needed</name>
  <files>tests/helpers.ts</files>
  <behavior>
    - Test helper users can create an isolated config home plus remote-backed worktree workspace without duplicating setup across Phase 85 files.
    - Helper output uses real temp directories, local repositories, and existing workspace YAML fixture contracts.
  </behavior>
  <action>Inspect existing `makeRepoWithRemote`, `makeWorkspaceFixture`, and captured real workspace exports. If the new workspace lifecycle tests need repeated setup beyond those helpers, add one narrow helper to `tests/helpers.ts` that composes existing helpers. Keep the existing `runCli(argv, opts)` API and all existing helper names intact per Phase 85 context. Do not replace helper architecture or add command-wrapper-only helpers.</action>
  <verify>
    <automated>rg -n "makeRepoWithRemote|makeWorkspaceFixture|realCleanWorkspace|realRemoveWorkspace|realMergeWorkspace|realRenameWorkspace" tests/helpers.ts</automated>
  </verify>
  <done>Reusable fixture setup exists through existing or extended helpers, and no existing helper API is removed or renamed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover workspace lifecycle behavior with real fixtures</name>
  <files>tests/lib/workspace-ops-real-fixture.test.ts</files>
  <behavior>
    - Clean/remove/merge dry-run paths do not remove worktrees, workspace YAML, or local branches.
    - Forced clean/remove/merge paths mutate the expected real filesystem/git/config state and preserve safety boundaries on failure.
    - Rename updates workspace identity and re-registers worktree paths against real git worktrees.
    - Missing `task_path` cases return safe results without crashing or deleting unrelated files.
    - Hook rollback interaction is exercised through real source exports and durable probe artifacts.
  </behavior>
  <action>Create `tests/lib/workspace-ops-real-fixture.test.ts`. Use real captured exports from `tests/helpers.ts` such as `realCleanWorkspace`, `realRemoveWorkspace`, `realMergeWorkspace`, `realRenameWorkspace`, `realReadWorkspace`, and `realWriteWorkspace` where available. Use local git repos from `makeRepoWithRemote` and direct workspace YAML fixtures from `makeWorkspaceFixture`. Assertions must inspect filesystem state, YAML state, branch state, and probe artifacts. Avoid brittle progress wording except where it is part of a safety contract.</action>
  <verify>
    <automated>bun test tests/lib/workspace-ops-real-fixture.test.ts</automated>
    <automated>bun run test:unit</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <done>Focused real-fixture workspace tests pass, the unit suite passes through the repo runner, and typecheck passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture filesystem -> workspace source modules | Tests create untrusted temporary paths and YAML read by source code. |
| Local git repos -> git wrappers | Test repositories exercise git subprocess behavior with disposable data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-01-01 | Tampering | `tests/helpers.ts` fixture paths | mitigate | Use temp directories and assert destructive operations do not touch paths outside the fixture root. |
| T-85-01-02 | Repudiation | lifecycle assertions | mitigate | Use durable YAML, filesystem, git branch, and probe artifact assertions instead of relying on progress text only. |
| T-85-01-03 | Information Disclosure | local developer config | mitigate | Use isolated config homes and never read the developer's real `GIT_STACKS_CONFIG_DIR`. |
</threat_model>

<verification>
Run the focused test file, `bun run test:unit`, and `bun run typecheck`. Do not run `bun test tests/` directly.
</verification>

<success_criteria>
CORE-01 lifecycle coverage exists in `tests/lib/workspace-ops-real-fixture.test.ts`; CORE-03 rollback interaction is represented where hooks affect lifecycle rollback; CORE-05 is satisfied by real source exports; GATE-03 remains green for unit and typecheck gates.
</success_criteria>

<output>
After completion, create `.planning/phases/85-core-real-fixture-functional-hardening/85-01-SUMMARY.md`
</output>
