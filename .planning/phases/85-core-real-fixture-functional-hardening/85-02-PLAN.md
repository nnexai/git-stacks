---
phase: 85-core-real-fixture-functional-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/lib/git-real-remote.test.ts
autonomous: true
requirements:
  - CORE-02
  - CORE-05
  - GATE-03
must_haves:
  truths:
    - "Core git operations are covered against local bare remotes for sync, pull, push, no-op, failure, branch-state, and dirty-worktree behavior."
    - "Tests execute real `src/lib/git.ts` exports instead of only command wrappers."
    - "No real remote service, network host, or developer repository is required."
  artifacts:
    - path: "tests/lib/git-real-remote.test.ts"
      provides: "Real local bare-remote tests for core git wrappers"
  key_links:
    - from: "src/lib/git.ts"
      to: "tests/lib/git.test.ts"
      via: "direct imports of git wrapper exports"
      pattern: "pushBranch|pullFFOnly|fetchOrigin|ensureUpstreamTracking|getCommitsAhead|getCommitsBehind"
    - from: "tests/helpers.ts"
      to: "src/lib/git.ts"
      via: "local bare remote and repo fixture helpers"
      pattern: "makeBareRemote|makeRepoWithRemote"
---

<objective>
Add real local-remote coverage for the core git wrapper layer.

Purpose: CORE-02 requires branch-state, no-op, failure, dirty-worktree, sync/pull/push behavior against local bare remotes. This plan targets the source wrapper layer directly so Phase 85 does not duplicate the earlier Phase 81 CLI-only pass.
Output: `tests/lib/git-real-remote.test.ts`.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/85-core-real-fixture-functional-hardening/85-CONTEXT.md
@.planning/phases/85-core-real-fixture-functional-hardening/85-RESEARCH.md
@.planning/phases/85-core-real-fixture-functional-hardening/85-PATTERNS.md
@.planning/phases/85-core-real-fixture-functional-hardening/85-VALIDATION.md
@.planning/phases/81-workspace-and-git-operation-e2e-coverage/81-04-SUMMARY.md
@tests/helpers.ts
@tests/lib/git.test.ts
@src/lib/git.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add real-remote branch-state tests for git wrappers</name>
  <files>tests/lib/git-real-remote.test.ts</files>
  <behavior>
    - `getCommitsAhead` and `getCommitsBehind` report correct counts after peer clone pushes.
    - `isFetchStale` uses git common dir behavior for worktrees after fetch state changes.
    - `ensureUpstreamTracking` distinguishes existing local tracking, remote-only branch availability, and missing remote branch cases.
  </behavior>
  <action>Create `tests/lib/git-real-remote.test.ts` with direct imports from `src/lib/git.ts` and fixture setup from `tests/helpers.ts`. Use local bare remotes and peer clones to create real divergence, stale fetch state, and branch tracking conditions. Keep assertions on returned structured values and real git state.</action>
  <verify>
    <automated>bun test tests/lib/git-real-remote.test.ts</automated>
  </verify>
  <done>Branch-state and tracking tests pass against local bare remotes and direct source exports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add push, pull, no-op, failure, and dirty-worktree tests</name>
  <files>tests/lib/git-real-remote.test.ts</files>
  <behavior>
    - `pushBranch` succeeds, reports no-op/up-to-date behavior, and returns structured failures for non-fast-forward or missing upstream conditions.
    - `pullFFOnly` fast-forwards when possible and returns structured failure data for divergence or missing remote branch.
    - Dirty worktree behavior that impacts user operations is represented through real repo state and wrapper outputs.
  </behavior>
  <action>Extend the same test file with wrapper-level push and pull scenarios. Use disposable repos only; do not use network remotes. Do not assert on incidental git stderr wording beyond source-owned structured reason strings.</action>
  <verify>
    <automated>bun test tests/lib/git-real-remote.test.ts</automated>
    <automated>bun run test:unit</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <done>Core git wrapper tests pass, unit suite passes, and typecheck passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local git subprocess -> wrapper return values | Git command output and exit codes cross into TypeScript wrappers. |
| Test-created remotes -> source wrappers | Local bare remotes emulate remote state without external network trust. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-02-01 | Spoofing | local remote fixtures | mitigate | Create all remotes under disposable temp roots and assert remote URLs point to those roots. |
| T-85-02-02 | Tampering | git repositories | mitigate | Isolate git author/env with existing helper patterns and never reuse developer repos. |
| T-85-02-03 | Denial of Service | git subprocess hangs | accept | Existing git wrappers are already synchronous subprocess calls; tests use local repos only and no network remotes. |
</threat_model>

<verification>
Run the focused git real-remote test, then `bun run test:unit` and `bun run typecheck`. Do not add real network dependencies.
</verification>

<success_criteria>
CORE-02 source-level git behavior is covered through local bare remotes; CORE-05 is satisfied by direct `src/lib/git.ts` execution; GATE-03 remains green for unit and typecheck gates.
</success_criteria>

<output>
After completion, create `.planning/phases/85-core-real-fixture-functional-hardening/85-02-SUMMARY.md`
</output>
