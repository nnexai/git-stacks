---
phase: 36-release-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - CHANGELOG.md
  - README.md
  - src/tui/dashboard/WorkspaceDetail.tsx
  - src/tui/dashboard/TemplateDetail.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "package.json version is 0.9.0"
    - "CHANGELOG.md has a [0.9.0] section documenting all user-facing changes from phases 33-35"
    - "README.md mentions name-based identity and dynamic name completion"
    - "TUI dashboard hides integrations that are globally disabled and have no override"
  artifacts:
    - path: "package.json"
      provides: "Version bump to 0.9.0"
      contains: '"version": "0.9.0"'
    - path: "CHANGELOG.md"
      provides: "v0.9.0 release notes"
      contains: "## [0.9.0]"
    - path: "README.md"
      provides: "Updated docs for v0.9.0 features"
      contains: "name field"
    - path: "src/tui/dashboard/WorkspaceDetail.tsx"
      provides: "Integration filtering in workspace detail"
      contains: "source === \"global\""
    - path: "src/tui/dashboard/TemplateDetail.tsx"
      provides: "Integration filtering in template detail"
      contains: "source === \"global\""
  key_links:
    - from: "package.json"
      to: "src/lib/version.ts"
      via: "getVersionString reads pkg.version"
      pattern: 'version.*0\.9\.0'
---

<objective>
Ship v0.9.0: bump version, document all milestone changes in CHANGELOG.md, update README.md for new features, and hide globally disabled integrations in the TUI dashboard.

Purpose: Close out the v0.9.0 milestone with proper versioning, documentation, and a small UI fix.
Output: Updated package.json, CHANGELOG.md, README.md, and two TUI dashboard files.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-release-prep/36-CONTEXT.md

Source files for TUI fix:
@src/tui/dashboard/WorkspaceDetail.tsx
@src/tui/dashboard/TemplateDetail.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version and write CHANGELOG entry</name>
  <files>package.json, CHANGELOG.md</files>
  <read_first>
    - package.json (current version is 0.8.0, line 3)
    - CHANGELOG.md (existing format with Added/Changed/Fixed sections)
    - .planning/ROADMAP.md (phases 33-35 descriptions for what shipped)
    - .planning/phases/33-name-based-identity/33-CONTEXT.md (name-based identity details)
    - .planning/phases/34-completion-audit-forge-issue-coverage/34-CONTEXT.md (completion audit details)
    - .planning/phases/35-dynamic-name-completion/35-CONTEXT.md (dynamic completion details)
  </read_first>
  <action>
1. In `package.json`, change `"version": "0.8.0"` to `"version": "0.9.0"`.

2. In `CHANGELOG.md`, insert a new `## [0.9.0] --- 2026-03-25` section above the existing `## [0.8.0]` entry. Follow the established format (Added/Changed/Fixed subsections with brief descriptions). Per D-01, no "Breaking Changes" section for v0.9.0.

Content for the CHANGELOG entry:

```
## [0.9.0] --- 2026-03-25

### Changed

**Name-based identity** --- workspaces and templates are now looked up by the `name` field in their YAML config, not by filename. Existing configs work without changes (the `name` field defaults to the filename stem). `git-stacks doctor` detects and reports name/filename drift.

**Template rename cascade** --- `git-stacks template rename` now updates all workspace YAML files that reference the renamed template. Supports `--dry-run` to preview changes before applying.

**Dynamic shell completion** --- tab-completing workspace and template names now reads candidate values from YAML `name` fields instead of listing filenames. Works in bash, zsh, and fish.

**Shell completion coverage** --- all integration subcommands (forge `pr create/open/status`, `issue link/unlink/open` for GitHub/GitLab/Gitea/Jira, `tmux attach`, `niri focus-workspace`) now have full tab-completion support in all three shells.

### Fixed

- **TUI dashboard integration display** --- integrations that are globally disabled no longer appear in the workspace and template detail panes. Only integrations that are enabled or have an explicit per-workspace/template override are shown.

### Internal

- **Test isolation framework** --- custom test runner separates unit tests (shared process) from integration tests (per-file isolated process). Mock factory pattern replaces partial mock.module exports. All cache-busting query-string imports and test-isolation-only DI objects removed from production code.
```

Note: Use em-dash (---) consistently with the existing CHANGELOG style. Check the existing entries to confirm whether they use `---` or `--` and match that style exactly.
  </action>
  <verify>
    <automated>grep -q '"version": "0.9.0"' package.json && grep -q '## \[0.9.0\]' CHANGELOG.md && grep -q 'Name-based identity' CHANGELOG.md && grep -q 'Dynamic shell completion' CHANGELOG.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - package.json line 3 contains `"version": "0.9.0"`
    - CHANGELOG.md contains `## [0.9.0]` section header
    - CHANGELOG.md v0.9.0 section contains "Name-based identity" under Changed
    - CHANGELOG.md v0.9.0 section contains "Template rename cascade" under Changed
    - CHANGELOG.md v0.9.0 section contains "Dynamic shell completion" under Changed
    - CHANGELOG.md v0.9.0 section contains "Shell completion coverage" under Changed
    - CHANGELOG.md v0.9.0 section contains "TUI dashboard integration display" under Fixed
    - CHANGELOG.md v0.9.0 section contains "Test isolation framework" under Internal
    - CHANGELOG.md v0.9.0 section does NOT contain "Breaking Changes"
    - The `## [0.9.0]` section appears ABOVE the `## [0.8.0]` section (newest first)
  </acceptance_criteria>
  <done>package.json version is 0.9.0. CHANGELOG.md has a complete [0.9.0] section with all v0.9.0 user-facing changes documented. No breaking changes section.</done>
</task>

<task type="auto">
  <name>Task 2: Update README and fix TUI dashboard integration filtering</name>
  <files>README.md, src/tui/dashboard/WorkspaceDetail.tsx, src/tui/dashboard/TemplateDetail.tsx</files>
  <read_first>
    - README.md (current documentation, especially Concepts, Configuration, Shell Completions sections)
    - src/tui/dashboard/WorkspaceDetail.tsx (lines 95-165, integration rendering loop)
    - src/tui/dashboard/TemplateDetail.tsx (lines 60-109, integration rendering loop)
    - src/lib/integrations/types.ts (resolveEnabledGlobally function)
  </read_first>
  <action>
**README.md updates:**

1. In the **Shell Completions** section (near bottom, after the completion commands), add a note about dynamic name resolution. After the existing sentence about completions covering "all commands, subcommands, dynamic entity names...", add:

   `Workspace and template names are resolved dynamically from YAML `name` fields at completion time, so tab-completion always reflects the current config even when filenames diverge from names.`

2. In the **Concepts** section (near top), update the **Workspace** paragraph to clarify name-based identity. Change:
   `Stored at `~/.config/git-stacks/workspaces/{name}.yml`.`
   to:
   `Stored at `~/.config/git-stacks/workspaces/{name}.yml`. Looked up by the `name` field inside the YAML, not by filename.`

3. Similarly update the **Template** paragraph. Change:
   `Stored at `~/.config/git-stacks/templates/{name}.yml`.`
   to:
   `Stored at `~/.config/git-stacks/templates/{name}.yml`. Looked up by the `name` field inside the YAML, not by filename.`

4. In the **Configuration** section, after `git-stacks doctor --fix   # Auto-repair drift`, add:
   `# Doctor also detects name/filename drift in workspaces and templates`
   as a comment line in the code block.

**TUI dashboard fix (hide globally disabled integrations):**

5. In `src/tui/dashboard/WorkspaceDetail.tsx`, inside the `<For each={integrations}>` callback (around line 102-164), after the cascade resolution determines `enabled` and `source`, add a filter: if `!enabled && source === "global"`, return `null` (skip rendering). Insert this check right after the cascade resolution block (after line 140, before the "Config summary" comment on line 142):

   ```tsx
   // Hide integrations that are globally disabled with no override (folded todo)
   if (!enabled && source === "global") return null
   ```

6. In `src/tui/dashboard/TemplateDetail.tsx`, inside the `<For each={integrations}>` callback (around line 67-103), add the same filter after the cascade resolution (after line 81, before the "Config summary" comment on line 83):

   ```tsx
   // Hide integrations that are globally disabled with no override (folded todo)
   if (!enabled && source === "global") return null
   ```

This means: integrations shown with red X only when explicitly disabled at workspace or template level (a conscious decision). Integrations that are simply "not enabled by default and not configured" are hidden to reduce clutter.
  </action>
  <verify>
    <automated>grep -q 'name field inside the YAML' README.md && grep -q 'resolved dynamically from YAML' README.md && grep -q '!enabled && source === "global"' src/tui/dashboard/WorkspaceDetail.tsx && grep -q '!enabled && source === "global"' src/tui/dashboard/TemplateDetail.tsx && bun run typecheck && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - README.md Workspace concept paragraph contains "Looked up by the `name` field inside the YAML"
    - README.md Template concept paragraph contains "Looked up by the `name` field inside the YAML"
    - README.md Shell Completions section contains "resolved dynamically from YAML"
    - README.md Configuration section mentions name/filename drift detection
    - WorkspaceDetail.tsx contains `if (!enabled && source === "global") return null`
    - TemplateDetail.tsx contains `if (!enabled && source === "global") return null`
    - `bun run typecheck` passes with zero errors
  </acceptance_criteria>
  <done>README.md documents name-based identity and dynamic completion. TUI dashboard hides globally disabled integrations that have no per-workspace/template override. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `grep '"version": "0.9.0"' package.json` returns a match
2. `grep '## \[0.9.0\]' CHANGELOG.md` returns a match
3. `grep 'name field inside the YAML' README.md` returns matches for both Workspace and Template
4. `grep '!enabled && source === "global"' src/tui/dashboard/WorkspaceDetail.tsx src/tui/dashboard/TemplateDetail.tsx` returns matches in both files
5. `bun run typecheck` exits 0
6. `bun test tests/` exits 0 (no regressions)
</verification>

<success_criteria>
- package.json version is 0.9.0
- CHANGELOG.md has complete v0.9.0 release notes covering all 4 phases (33, 34, 34.1, 35)
- README.md reflects name-based identity and dynamic completion
- TUI dashboard no longer shows globally disabled integrations (only shows enabled or explicitly overridden)
- All tests pass, TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/36-release-prep/36-01-SUMMARY.md`
</output>
