---
phase: 08-dashboard-tab-layout
plan: 05
type: execute
wave: 1
depends_on: ["08-01", "08-02", "08-03", "08-04"]
files_modified:
  - src/tui/dashboard/App.tsx
  - src/tui/dashboard/ActionMenu.tsx
  - src/tui/dashboard/TemplateActionMenu.tsx
  - src/tui/dashboard/ConfirmDialog.tsx
  - src/tui/dashboard/ProgressView.tsx
  - src/tui/dashboard/HelpOverlay.tsx
  - src/tui/dashboard/WorkspaceList.tsx
  - src/tui/dashboard/TemplateList.tsx
  - src/tui/dashboard/RepoList.tsx
autonomous: true
gap_closure: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, DASH-06, DASH-07, DASH-08, DASH-09, DASH-10, DASH-11]

must_haves:
  truths:
    - "Dashboard renders two separate bordered boxes (list on top, detail on bottom) without overflow when BatchBar appears"
    - "Pressing 1/2/3 or [/] switches the visible tab content immediately (no freeze)"
    - "Action menu renders inside the detail box without double borders, with padding"
    - "HelpOverlay covers the full terminal area when opened with ?"
    - "Pressing / immediately shows 'filter: _' in the help bar even before any characters are typed"
  artifacts:
    - path: "src/tui/dashboard/App.tsx"
      provides: "Two-box layout with Switch/Match tab rendering, help overlay as sole child, filter in help bar"
      contains: "Switch, Match"
    - path: "src/tui/dashboard/ActionMenu.tsx"
      provides: "Borderless action menu with padding"
    - path: "src/tui/dashboard/TemplateActionMenu.tsx"
      provides: "Borderless template action menu with padding"
    - path: "src/tui/dashboard/ConfirmDialog.tsx"
      provides: "Borderless confirm dialog with padding"
    - path: "src/tui/dashboard/ProgressView.tsx"
      provides: "Borderless progress view"
    - path: "src/tui/dashboard/HelpOverlay.tsx"
      provides: "Full-screen overlay with height=100% width=100%"
    - path: "src/tui/dashboard/WorkspaceList.tsx"
      provides: "List without inline filter display"
    - path: "src/tui/dashboard/TemplateList.tsx"
      provides: "List without inline filter display"
    - path: "src/tui/dashboard/RepoList.tsx"
      provides: "List without inline filter display"
  key_links:
    - from: "src/tui/dashboard/App.tsx"
      to: "solid-js Switch/Match"
      via: "import { Switch, Match } from solid-js; used for tab content rendering"
      pattern: "<Switch>.*<Match when=\\{tab\\(\\)"
    - from: "src/tui/dashboard/App.tsx"
      to: "HelpOverlay"
      via: "Show when={helpOpen()} as first child of outer box; Show when={!helpOpen()} wrapping two-box layout"
      pattern: "<Show when=\\{helpOpen\\(\\)\\}>.*<HelpOverlay"
    - from: "src/tui/dashboard/App.tsx"
      to: "filter display in help bar"
      via: "filtering() boolean controls filter line in help bar sibling"
      pattern: "<Show when=\\{filtering\\(\\)\\}>"
---

<objective>
Fix all 5 UAT gaps from Phase 8 user acceptance testing. Two major issues (layout overflow from single outer box, tab-switching freeze from Show blocks) and three minor issues (action menu double borders, help overlay smushed, filter indicator missing on empty string).

Purpose: Unblock all 6 skipped UAT tests (tests 3, 6, 7, 9, 10, 11) and fix the 5 failing tests (tests 1, 2, 8, 12, 13) to achieve full UAT pass.

Output: Refactored App.tsx with two-box layout + Switch/Match, fixed component borders, full-screen HelpOverlay, filter in help bar.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dashboard-tab-layout/08-CONTEXT.md
@.planning/phases/08-dashboard-tab-layout/08-RESEARCH.md
@.planning/phases/08-dashboard-tab-layout/08-UAT.md
@.planning/phases/08-dashboard-tab-layout/08-01-SUMMARY.md
@.planning/phases/08-dashboard-tab-layout/08-02-SUMMARY.md
@.planning/phases/08-dashboard-tab-layout/08-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/tui/dashboard/types.ts:
```typescript
export type Tab = "workspaces" | "templates" | "repos"
export type Action = "open" | "edit" | "rename" | "clean" | "remove" | "merge"
export type UIView =
  | { view: "list" }
  | { view: "action-menu"; index: number }
  | { view: "confirm"; index: number; action: Action; batch?: boolean }
  | { view: "progress"; message: string }
  | { view: "inline-input"; index: number; purpose: "rename" | "clone-template"; prefill: string }
```

From src/tui/dashboard/ActionMenu.tsx (current — to be modified):
```typescript
type Props = {
  workspaceName: string
  onAction: (action: Action) => void
  onCancel: () => void
  onRun?: () => void
}
```

From src/tui/dashboard/TemplateActionMenu.tsx (current — to be modified):
```typescript
type Props = {
  templateName: string
  onAction: (action: "edit" | "clone" | "remove") => void
  onCancel: () => void
}
```

From src/tui/dashboard/ConfirmDialog.tsx (current — to be modified):
```typescript
type Props = {
  message: string
  onConfirm: () => void
  onCancel: () => void
}
```

From src/tui/dashboard/ProgressView.tsx (current — to be modified):
```typescript
type Props = {
  title: string
  lines: string[]
  done: boolean
}
```

From src/tui/dashboard/HelpOverlay.tsx (current — to be modified):
```typescript
type Props = {
  tab: Tab
  onClose: () => void
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix child component borders, sizing, and filter display</name>
  <files>
    src/tui/dashboard/ActionMenu.tsx
    src/tui/dashboard/TemplateActionMenu.tsx
    src/tui/dashboard/ConfirmDialog.tsx
    src/tui/dashboard/ProgressView.tsx
    src/tui/dashboard/HelpOverlay.tsx
    src/tui/dashboard/WorkspaceList.tsx
    src/tui/dashboard/TemplateList.tsx
    src/tui/dashboard/RepoList.tsx
  </files>
  <read_first>
    src/tui/dashboard/ActionMenu.tsx
    src/tui/dashboard/TemplateActionMenu.tsx
    src/tui/dashboard/ConfirmDialog.tsx
    src/tui/dashboard/ProgressView.tsx
    src/tui/dashboard/HelpOverlay.tsx
    src/tui/dashboard/WorkspaceList.tsx
    src/tui/dashboard/TemplateList.tsx
    src/tui/dashboard/RepoList.tsx
    .planning/phases/08-dashboard-tab-layout/08-RESEARCH.md
    .planning/phases/08-dashboard-tab-layout/08-CONTEXT.md
  </read_first>
  <action>
Fix 8 child components. These components will be rendered inside the detail box (which has its own border), so they must NOT have their own border or width constraints. The filter display moves to App.tsx's help bar, so it must be removed from list components.

**ActionMenu.tsx** (UAT Issue 3 — action menu floating):
- Line 37: Remove `border`, `title={...}`, and `width="50%"` from the outer `<box>`.
- Replace with: `<box flexDirection="column" paddingTop={1} paddingLeft={2}>`
- The `For each={actions}` loop, the `[u] Run` text, and the `[Esc] Back` text remain unchanged inside the box.
- The keyboard handler (useKeyboard) remains completely unchanged.
- The Props type remains completely unchanged.

**TemplateActionMenu.tsx** (UAT Issue 3 — action menu floating):
- Line 19: Remove `border`, `title={...}`, and `width="50%"` from the outer `<box>`.
- Replace with: `<box flexDirection="column" paddingTop={1} paddingLeft={2}>`
- All text children (`[e] Edit`, `[c] Clone`, `[r] Remove`, `[Esc] Back`) remain unchanged.
- The keyboard handler remains completely unchanged.

**ConfirmDialog.tsx** (UAT Issue 3 consistency — double border):
- Line 17: Remove `border` and `width="60%"` from the outer `<box>`.
- Replace with: `<box flexDirection="column" paddingTop={2} paddingLeft={2}>`
- The two text children (message + yes/no) remain unchanged.
- The keyboard handler remains completely unchanged.

**ProgressView.tsx** (UAT Issue 3 consistency — double border):
- Line 13: Remove `border`, `title={props.title}`, and `width="70%"` from the outer `<box>`.
- Replace with: `<box flexDirection="column">`
- All children (spinner box, For lines, done text) remain unchanged.
- The detail box's title prop in App.tsx will show the progress message instead.

**HelpOverlay.tsx** (UAT Issue 4 — smushed into empty box):
- Line 16: Add `height="100%"` and `width="100%"` to the box. Keep `border` and `title="Keybindings"`.
- The line becomes: `<box border title="Keybindings" flexDirection="column" height="100%" width="100%">`
- All text children remain unchanged.
- The keyboard handler remains completely unchanged.

**WorkspaceList.tsx** (UAT Issue 5 — filter display moving to App.tsx):
- Lines 29-31: Remove the entire `<Show when={props.filter}>` block and its `<text fg="cyan">  filter: {props.filter}</text>` child.
- The rest of the component (scrollOffset, visibleEntries, For loop, scroll indicator) remains unchanged.
- The `filter` prop is still received and used for data filtering by the parent — only the display is removed.

**TemplateList.tsx** (UAT Issue 5 — filter display moving to App.tsx):
- Lines 27-29: Remove the entire `<Show when={props.filter}>` block and its `<text fg="cyan">  filter: {props.filter}</text>` child.
- Everything else remains unchanged.

**RepoList.tsx** (UAT Issue 5 — filter display moving to App.tsx):
- Lines 32-34: Remove the entire `<Show when={props.filter}>` block and its `<text fg="cyan">  filter: {props.filter}</text>` child.
- Everything else remains unchanged.
  </action>
  <verify>
    <automated>cd /home/nnex/dev/prj/git-stacks && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - ActionMenu.tsx contains `paddingTop={1}` and `paddingLeft={2}` and does NOT contain `border` or `width="50%"` on the outer box element
    - TemplateActionMenu.tsx contains `paddingTop={1}` and `paddingLeft={2}` and does NOT contain `border` or `width="50%"` on the outer box element
    - ConfirmDialog.tsx contains `paddingTop={2}` and `paddingLeft={2}` and does NOT contain `border` or `width="60%"` on the outer box element
    - ProgressView.tsx outer box does NOT contain `border`, `title=`, or `width="70%"`
    - HelpOverlay.tsx contains `height="100%"` and `width="100%"` and still contains `border` and `title="Keybindings"`
    - WorkspaceList.tsx does NOT contain `<Show when={props.filter}>`
    - TemplateList.tsx does NOT contain `<Show when={props.filter}>`
    - RepoList.tsx does NOT contain `<Show when={props.filter}>`
    - `bun run typecheck` exits 0
  </acceptance_criteria>
  <done>All 8 child components updated: action menus/confirm/progress have no borders, HelpOverlay has full dimensions, filter display removed from all three list components. Type check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor App.tsx — two-box layout, Switch/Match, help overlay placement, filter in help bar</name>
  <files>
    src/tui/dashboard/App.tsx
  </files>
  <read_first>
    src/tui/dashboard/App.tsx
    src/tui/dashboard/ActionMenu.tsx
    src/tui/dashboard/TemplateActionMenu.tsx
    src/tui/dashboard/ConfirmDialog.tsx
    src/tui/dashboard/ProgressView.tsx
    src/tui/dashboard/HelpOverlay.tsx
    .planning/phases/08-dashboard-tab-layout/08-RESEARCH.md
    .planning/phases/08-dashboard-tab-layout/08-CONTEXT.md
  </read_first>
  <action>
Major refactor of App.tsx to fix UAT Issues 1 (layout overflow), 2 (tab-switching freeze), 4 (help overlay placement), and 5 (filter indicator in help bar). This is the central change that addresses the two major bugs.

**Import change (line 2):**
Change `import { createSignal, createMemo, Show } from "solid-js"` to:
```tsx
import { createSignal, createMemo, Show, Switch, Match } from "solid-js"
```

**Remove the `separatorLine` memo** (lines 123-129). It is no longer needed — the detail box's `title` prop replaces it.

**Add a `detailBoxTitle` memo** in its place:
```tsx
const detailBoxTitle = createMemo(() => {
  const v = view()
  if (v.view === "progress") return ` ${(v as any).message} `
  const name = selectedName()
  return name ? ` ${name} ` : ""
})
```

**Simplify height memos** (lines 76-79). The `innerHeight` and `detailHeight` memos can be removed. Keep `listHeight` but simplify it since it is only used as a scroll viewport estimate for list components:
```tsx
const listHeight = createMemo(() => Math.max(6, Math.floor(dims().height * 0.6) - 2))
```
Remove `innerHeight` and `detailHeight` entirely.

**Add a `helpBarText` memo** for the context-sensitive help text:
```tsx
const helpBarText = createMemo(() => {
  const t = tab()
  if (t === "workspaces")
    return "  1/2/3 Tabs  up/dn Navigate  Enter Actions  Space Select  / Filter  R Refresh  ? Help  q Quit"
  if (t === "templates")
    return "  1/2/3 Tabs  up/dn Navigate  Enter Actions  / Filter  R Refresh  ? Help  q Quit"
  return "  1/2/3 Tabs  up/dn Navigate  / Filter  R Refresh  ? Help  q Quit"
})
```

**Replace the entire return statement (lines 510-653)** with the two-box layout. The new structure is:

```tsx
return (
  <box flexDirection="column" height="100%">
    {/* Help overlay replaces EVERYTHING when open */}
    <Show when={helpOpen()}>
      <HelpOverlay tab={tab()} onClose={() => setHelpOpen(false)} />
    </Show>

    <Show when={!helpOpen()}>
      {/* TOP BOX: list pane with tab title in border */}
      <box border title={tabTitle()} flexDirection="column" flexGrow={3} minHeight={10}>
        <Switch>
          <Match when={tab() === "workspaces"}>
            <WorkspaceList
              entries={filteredEntries()}
              cursor={cursor()}
              selected={selected()}
              filter={filtering() ? filter() : ""}
              height={listHeight()}
            />
          </Match>
          <Match when={tab() === "templates"}>
            <TemplateList
              entries={filteredTemplates()}
              cursor={tabCursor.templates[0]()}
              filter={tabFiltering.templates[0]() ? tabFilter.templates[0]() : ""}
              height={listHeight()}
            />
          </Match>
          <Match when={tab() === "repos"}>
            <RepoList
              entries={filteredRepos()}
              cursor={tabCursor.repos[0]()}
              filter={tabFiltering.repos[0]() ? tabFilter.repos[0]() : ""}
              height={listHeight()}
            />
          </Match>
        </Switch>
        {/* Batch bar INSIDE top box as footer row */}
        <Show when={view().view === "list" && selected().size > 0}>
          <BatchBar count={selected().size} />
        </Show>
      </box>

      {/* BOTTOM BOX: detail / action-menu / confirm / progress / inline-input */}
      <box border title={detailBoxTitle()} flexDirection="column" flexGrow={2} minHeight={10}>
        {/* List view — tab-specific detail */}
        <Show when={view().view === "list"}>
          <Switch>
            <Match when={tab() === "workspaces"}>
              <WorkspaceDetail entry={currentEntry()} />
            </Match>
            <Match when={tab() === "templates"}>
              <TemplateDetail template={currentTemplate()} />
            </Match>
            <Match when={tab() === "repos"}>
              <RepoDetail
                entry={currentRepo()}
                allTemplates={templateEntries()}
                allWorkspaces={allWorkspaces()}
              />
            </Match>
          </Switch>
        </Show>

        {/* Action menus */}
        <Show when={view().view === "action-menu"}>
          <Switch>
            <Match when={tab() === "workspaces"}>
              <ActionMenu
                workspaceName={currentEntry()?.workspace.name ?? ""}
                onAction={(action) => runAction(action, (view() as any).index)}
                onCancel={() => setView({ view: "list" })}
                onRun={() => handleRun(selectedName())}
              />
            </Match>
            <Match when={tab() === "templates"}>
              <TemplateActionMenu
                templateName={currentTemplate()?.name ?? ""}
                onAction={handleTemplateAction}
                onCancel={() => setView({ view: "list" })}
              />
            </Match>
          </Switch>
        </Show>

        {/* Confirm dialog */}
        <Show when={view().view === "confirm"}>
          {(() => {
            const v = view() as { view: "confirm"; index: number; action: Action; batch?: boolean }
            const label = confirmContext() === "template"
              ? `${v.action} template '${filteredTemplates()[v.index]?.name}'?`
              : v.batch
              ? `${v.action} ${selected().size} workspace(s)?`
              : `${v.action} '${filteredEntries()[v.index]?.workspace.name}'?`
            return (
              <ConfirmDialog
                message={label}
                onConfirm={() => executeConfirmed(v.action, v.index, v.batch)}
                onCancel={() => setView({ view: "list" })}
              />
            )
          })()}
        </Show>

        {/* Inline input */}
        <Show when={view().view === "inline-input"}>
          <InlineInput
            label={inlineInputLabel()}
            prefill={(view() as any).prefill ?? ""}
            onConfirm={handleInlineInputConfirm}
            onCancel={handleInlineInputCancel}
          />
        </Show>

        {/* Progress view */}
        <Show when={view().view === "progress"}>
          <ProgressView
            title={(view() as any).message}
            lines={progressLines()}
            done={progressDone()}
          />
        </Show>
      </box>

      {/* HELP BAR / FILTER LINE / LOADING — outside both boxes, fixed 1 row */}
      <box height={1}>
        <Show when={filtering()}>
          <text fg="cyan">  filter: {filter() || "_"}</text>
        </Show>
        <Show when={!filtering() && loading()}>
          <text fg="gray">  (loading statuses...)</text>
        </Show>
        <Show when={!filtering() && !loading()}>
          <text fg="gray">{helpBarText()}</text>
        </Show>
      </box>
    </Show>
  </box>
)
```

**Key structural changes from current code:**
1. The outer `<box border title={tabTitle()}>` on line 511 becomes `<box flexDirection="column" height="100%">` — NO border on the outer wrapper.
2. The three `<Show when={tab() === ...}>` blocks for list content (lines 531-555) become `<Switch><Match>` — fixes the tab-switching freeze.
3. The three `<Show when={tab() === ...}>` blocks for detail content (lines 564-577) become `<Switch><Match>`.
4. The two `<Show when={tab() === ... && view().view === "action-menu"}>` blocks (lines 581-596) become `<Show when={view().view === "action-menu"}>` wrapping `<Switch><Match>`.
5. The separator text `<text fg="gray">{separatorLine()}</text>` on line 559 is removed. The detail box `title={detailBoxTitle()}` replaces it.
6. The `<box height={listHeight()}>` wrapper around list content (line 530) is replaced by the top `<box border>` with `flexGrow={3}`.
7. The `<box flexDirection="column" height={detailHeight()}>` for detail content (line 562) is replaced by the bottom `<box border>` with `flexGrow={2}`.
8. Loading indicator (lines 512-517) moves inside the help bar sibling (third child of outer box).
9. Progress view is no longer a sibling competing with the list — it renders inside the bottom detail box, replacing the detail content.
10. BatchBar (lines 630-632) moves INSIDE the top box as a footer row.
11. Help bar (lines 634-645) becomes a `<box height={1}>` sibling OUTSIDE both boxes.
12. Help overlay (lines 647-650) becomes `<Show when={helpOpen()}>` as the FIRST child of the outer box, with `<Show when={!helpOpen()}>` wrapping the two-box layout + help bar.
13. Filter indicator: `<Show when={filtering()}>` in the help bar shows `filter: {filter() || "_"}` immediately when `/` is pressed.

**What must NOT change:**
- The keyboard handler (useKeyboard) remains completely unchanged — all key bindings, the tab switching logic, the filter mode, the progress/confirm/action-menu guards.
- All signal declarations (tab, view, selected, progressLines, progressDone, helpOpen, confirmContext, tabCursor, tabFilter, tabFiltering) remain unchanged.
- All accessor memos (cursor, filter, filtering, setCursor, setFilter, setFiltering) remain unchanged.
- All filtered memos (filteredEntries, filteredTemplates, filteredRepos) remain unchanged.
- All current memos (currentEntry, currentTemplate, currentRepo, allWorkspaces) remain unchanged.
- All action handler functions (runAction, executeConfirmed, launchEditor, handleInlineInputConfirm, handleInlineInputCancel, handleTemplateAction, handleRun, clampCursor) remain unchanged.
- All imports except the solid-js import line remain unchanged.
  </action>
  <verify>
    <automated>cd /home/nnex/dev/prj/git-stacks && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx imports include `Switch, Match` from "solid-js" (line 2 contains `Switch` and `Match`)
    - App.tsx does NOT contain `separatorLine` anywhere
    - App.tsx contains `detailBoxTitle` memo
    - App.tsx does NOT contain `innerHeight` or `detailHeight` memos
    - App.tsx contains `flexGrow={3}` (top box) and `flexGrow={2}` (bottom box)
    - App.tsx contains `minHeight={10}` on both bordered boxes
    - App.tsx outer return starts with `<box flexDirection="column" height="100%">` (NO border on outer wrapper)
    - App.tsx contains two `<box border` elements (top box with `title={tabTitle()}` and bottom box with `title={detailBoxTitle()}`)
    - App.tsx contains `<Switch>` and `<Match when={tab() ===` for list content rendering
    - App.tsx contains `<Switch>` and `<Match when={tab() ===` for detail content rendering
    - App.tsx contains `<Show when={helpOpen()}>` followed by `<HelpOverlay` as first child of outer box
    - App.tsx contains `<Show when={!helpOpen()}>` wrapping the two-box layout
    - App.tsx help bar contains `<Show when={filtering()}>` with `filter: {filter() || "_"}`
    - App.tsx does NOT contain `<Show when={tab() === "workspaces"}>` anywhere (all replaced with Switch/Match)
    - App.tsx does NOT contain `separatorLine()` text element
    - `bun run typecheck` exits 0
  </acceptance_criteria>
  <done>App.tsx fully refactored: two-box layout with flexGrow, Switch/Match for all tab content, HelpOverlay as sole child when open, filter indicator in help bar. All 5 UAT gaps addressed. Type check passes.</done>
</task>

</tasks>

<verification>
1. `bun run typecheck` passes — no type errors in any modified file.
2. `bun test tests/` passes — existing lib tests not broken by TUI changes.
3. Manual UAT via `git-stacks manage`:
   - Test 1 (layout): Two visible bordered boxes; selecting items does NOT cause overflow.
   - Test 2 (tabs): Pressing 2/3/1 switches visible list content immediately.
   - Test 3 (state): Per-tab cursor preserved across tab switches.
   - Test 6 (templates): Templates tab shows real data.
   - Test 7 (repos): Repos tab shows data with disk indicator.
   - Test 8 (actions): Action menu appears with padding inside detail box, not floating in corner.
   - Test 9 (template edit): Template edit opens $EDITOR.
   - Test 10 (template clone): Template clone flow works.
   - Test 11 (template remove): Template remove flow works.
   - Test 12 (help): Keybindings fill the overlay, not smushed.
   - Test 13 (filter): Pressing / immediately shows "filter: _".
</verification>

<success_criteria>
- All 8 files modified with the exact changes specified.
- `bun run typecheck` exits 0.
- `bun test tests/` exits 0.
- All 5 UAT gaps are addressed: two-box layout (issue 1), Switch/Match tabs (issue 2), borderless action menus (issue 3), full-screen help overlay (issue 4), filter indicator in help bar (issue 5).
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard-tab-layout/08-05-SUMMARY.md`
</output>
