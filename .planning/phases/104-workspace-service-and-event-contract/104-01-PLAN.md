---
phase: 104-workspace-service-and-event-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/service/contract.ts
  - src/lib/service/identity.ts
  - src/lib/config.ts
  - tests/lib/service/contract.test.ts
  - tests/lib/service/identity.test.ts
  - tests/fixtures/service-v1/discovery.json
  - tests/fixtures/service-v1/workspace-snapshot.json
autonomous: true
requirements: [SVC-01, SVC-02, SVC-04]
must_haves:
  truths:
    - "A client can validate strict v1 discovery, success, error, capability, entity, launch, operation, cursor, and event envelopes (D-04)."
    - "Workspace and repository IDs survive rename and missing legacy IDs are added atomically without changing name-based callers (D-02)."
    - "Golden JSON fixtures are accepted by exported schemas and contain no undeclared fields."
  artifacts:
    - path: src/lib/service/contract.ts
      provides: "Canonical strict Zod wire contract and inferred types"
    - path: src/lib/service/identity.ts
      provides: "Lazy stable identity migration"
    - path: tests/fixtures/service-v1/workspace-snapshot.json
      provides: "Cross-language fixture for Phase 105"
  key_links:
    - from: src/lib/service/identity.ts
      to: src/lib/config.ts
      via: "validated read/write APIs and optional persisted id fields"
      pattern: "WorkspaceSchema|WorkspaceRepoSchema|writeWorkspace"
    - from: tests/fixtures/service-v1/discovery.json
      to: src/lib/service/contract.ts
      via: "schema parse"
      pattern: "DiscoveryResponseSchema"
---

<objective>
Establish the versioned wire contract and rename-safe persisted identities per D-02 and D-04 while preserving existing config consumers per SVC-04.

Purpose: Downstream service plans and Phase 105 clients need one executable, fixture-backed contract rather than inferred TypeScript internals.
Output: Strict schemas/types, additive ID migration, and golden discovery/snapshot fixtures.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-workspace-service-and-event-contract/104-CONTEXT.md
@.planning/phases/104-workspace-service-and-event-contract/104-RESEARCH.md
@.planning/phases/104-workspace-service-and-event-contract/104-PATTERNS.md
@src/lib/config.ts
@tests/helpers.ts

<interfaces>
Use `WorkspaceSchema`, `WorkspaceRepoSchema`, `Workspace`, `WorkspaceRepo`, `readWorkspace`, `listWorkspaces`, and `writeWorkspace` from `src/lib/config.ts`. Preserve name-based lookup and make `id` additive/optional in YAML-facing schemas. Follow the schema/type export pairing already used by config.ts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze the strict v1 wire contract and golden fixtures</name>
  <files>src/lib/service/contract.ts, tests/lib/service/contract.test.ts, tests/fixtures/service-v1/discovery.json, tests/fixtures/service-v1/workspace-snapshot.json</files>
  <behavior>
    - Valid golden discovery and aggregate snapshot fixtures parse exactly; unknown fields and malformed opaque IDs/cursors are rejected.
    - Discovery declares protocol `v1`, capabilities, limits, and explicit capability availability; unavailable optional behavior maps to `capability_unavailable` per D-04.
    - Error, operation, progress, launch-context, and event unions never serialize raw Error instances, stacks, credentials, raw YAML, or undeclared data.
  </behavior>
  <action>Create `src/lib/service/contract.ts` with `z.strictObject` schemas and `z.infer` exports for request IDs, opaque entity/operation IDs, decimal-string-backed opaque cursors, success/error envelopes, discovery/capabilities/limits, aggregate snapshots, redacted launch specifications, operation lifecycle/progress/result/error, replay-gap metadata, and operation/attention/control events. Encode D-03 revision and timestamp fields and D-10 through D-17 vocabulary even where later plans implement persistence. Use stable machine error and stage codes; exact counts are optional pairs and no percent field exists per D-11. Resolve the secret-policy question by allowing ordinary resolved environment values plus redaction/reference metadata while omitting resolved secret values by default. Add exact JSON fixtures and tests proving every fixture round-trips through its exported schema and unknown/sensitive fields fail.</action>
  <verify><automated>bun test tests/lib/service/contract.test.ts &amp;&amp; bun run typecheck</automated></verify>
  <done>All canonical wire variants are strict, exported, fixture-backed, and discovery makes optional capability absence explicit without disabling compatible endpoints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add stable lazy workspace and repository identity migration</name>
  <files>src/lib/service/identity.ts, src/lib/config.ts, tests/lib/service/identity.test.ts</files>
  <behavior>
    - Legacy workspace/repository YAML without IDs receives random opaque IDs once; repeated reads and service restarts return the same IDs.
    - Workspace and repository rename/path changes preserve IDs.
    - Concurrent migration cannot allocate two committed identities and invalid/symlinked paths fail without partial writes.
    - Existing name-based config read/write/list behavior remains unchanged.
  </behavior>
  <action>Add optional UUID `id` fields to workspace and workspace-repository schemas in `config.ts` without changing lookup keys, display names, filenames, or CLI/OpenTUI return shapes (D-02, SVC-04). Implement identity migration in `identity.ts` using injected random UUID and persistence seams, validated atomic config writes, re-read/compare handling for races, and preservation of all unrelated YAML data. Export service-oriented helpers that return an identity-complete workspace but keep migration out of ordinary CLI reads. Update tests with isolated config and hostile legacy/rename/concurrency cases.</action>
  <verify><automated>bun test tests/lib/service/identity.test.ts tests/lib/config.test.ts &amp;&amp; bun run typecheck</automated></verify>
  <done>Stable opaque IDs persist across restart and rename, legacy config migrates atomically on service access, and existing config tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Persisted YAML to service contract | User-editable legacy data becomes trusted identity and wire data only after validation. |
| TypeScript internals to native JSON | Internal errors and sensitive/undeclared fields must not cross the public contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-104-01 | Tampering | identity.ts/config.ts | high | mitigate | Validate before atomic write, reject unsafe path forms, and test concurrent migration. |
| T-104-02 | Information Disclosure | contract.ts launch/error schemas | high | mitigate | Strict allowlisted schemas omit secrets, credentials, raw errors, and stacks. |
| T-104-03 | Spoofing | opaque IDs | medium | mitigate | Generate UUIDs with the runtime CSPRNG and persist rather than derive from names. |
</threat_model>

<verification>
Run `bun test tests/lib/service/contract.test.ts tests/lib/service/identity.test.ts tests/lib/config.test.ts && bun run typecheck`.
</verification>

<success_criteria>
- SVC-01 contract fixtures validate with explicit capabilities and stable structured errors.
- SVC-02 entities use stable opaque persisted identities across rename/restart.
- SVC-04 existing config/name behavior remains green.
</success_criteria>

<output>
Create `.planning/phases/104-workspace-service-and-event-contract/104-01-SUMMARY.md` when done.
</output>
