---
phase: 104-workspace-service-and-event-contract
plan: "04"
type: execute
wave: 3
depends_on: ["104-01", "104-05"]
files_modified:
  - src/lib/service/operations.ts
  - tests/lib/service/operations.test.ts
  - tests/lib/service/idempotency.test.ts
autonomous: true
requirements: [EVT-01, EVT-02, EVT-03]
must_haves:
  truths:
    - "A mutation is durably accepted before asynchronous execution and follows accepted to running to exactly one terminal state (D-10)."
    - "Progress uses stable stage/message/count fields and cancellation reports completed work and rollback outcome (D-11, D-13)."
    - "Credential-scoped equivalent retries return one operation across restart; different input conflicts (D-12)."
    - "Every persisted lifecycle transition and progress update is appended to the shared durable event journal before live publication (D-14, EVT-03)."
  artifacts:
    - path: src/lib/service/operations.ts
      provides: "Durable operation/idempotency registry and cooperative runner"
    - path: tests/lib/service/idempotency.test.ts
      provides: "Crash/restart and conflict proof"
  key_links:
    - from: src/lib/service/operations.ts
      to: src/lib/service/event-journal.ts
      via: "injected operation event publisher for every lifecycle transition and progress update"
      pattern: "OperationEventPublisher|publishOperationEvent"
    - from: src/lib/service/operations.ts
      to: src/lib/operation-runner.ts
      via: "safe-step execution and rollback result adaptation"
      pattern: "createRunner"
---

<objective>
Create the durable asynchronous mutation and idempotency substrate implementing D-10 through D-13.

Purpose: Native requests must return promptly, survive retry/restart, and expose honest partial cancellation without duplicating destructive work.
Output: Persisted operation registry, representative mutation adapters, structured progress, cancellation, retention, and tests.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/104-workspace-service-and-event-contract/104-CONTEXT.md
@.planning/phases/104-workspace-service-and-event-contract/104-RESEARCH.md
@.planning/phases/104-workspace-service-and-event-contract/104-01-SUMMARY.md
@.planning/phases/104-workspace-service-and-event-contract/104-05-SUMMARY.md
@src/lib/operation-runner.ts
@src/lib/workspace-ops.ts
@src/lib/workspace-lifecycle.ts
@src/lib/workspace-command.ts

<interfaces>
Adapt `createRunner(onProgress)` and its `RunnerResult` rollback vocabulary. The initial advertised mutation set is the smallest existing workspace set sufficient to prove async lifecycle, safe cancellation, and destructive idempotency: workspace open and close. Do not add unrelated CRUD, forge, or command-authoring product scope.
Use the append-first `OperationEventPublisher` exported by `src/lib/service/event-journal.ts`; operations must not call the broker or SSE transport directly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist operation lifecycle and cooperative cancellation</name>
  <files>src/lib/service/operations.ts, tests/lib/service/operations.test.ts</files>
  <behavior>
    - Acceptance persists and returns an operation ID before the injected executor begins; ordinary reads remain runnable while work is active.
    - Transitions are accepted, running, then one of succeeded/failed/cancelled with timestamps and structured terminal data.
    - Cancellation is observed only between named safe steps and records completed work, rollback attempted/succeeded, original failure, and rollback failures separately.
    - Restart converts non-resumable accepted/running records to a structured interrupted failure rather than rerunning them.
    - Accepted, running, progress, and terminal records are passed to the journal publisher in transition order; each state becomes observable only after its event append succeeds.
  </behavior>
  <action>Implement a serialized, atomically persisted operation registry with injected clock/ID/root/executor and the 24-hour/10,000-terminal-record policy; active records are never evicted. Inject the Plan 05 `OperationEventPublisher` and route every accepted, running, structured progress, succeeded, failed, cancelled, and restart-interrupted transition through it in registry order. Persist the operation state first, then await the publisher's durable journal append before notifying operation observers or returning the newly visible transition; never call `event-broker.ts` or SSE code directly. If event append fails, expose a structured internal failure and do not live-publish an unjournaled transition. Define stage-coded progress with message and optional exact completed/total pairs, never fabricated percentages (D-10, D-11). Schedule execution after durable acceptance, adapt existing runner rollback semantics, and check cancellation tokens between explicit safe steps so the terminal record is honest about partial work (D-13). Add recovery rules and tests for nonblocking query callbacks, transition validity, event ordering, append-before-observer publication, append failure, failure/rollback ordering, cancellation timing, retention, and restart.</action>
  <verify><automated>bun test tests/lib/service/operations.test.ts tests/lib/service/event-journal.test.ts &amp;&amp; bun run typecheck</automated></verify>
  <done>Operations return promptly, run independently of queries, persist every valid transition, append its lifecycle/progress event before observer publication, recover safely, and expose honest cancellation/rollback results.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reserve idempotency before representative workspace execution</name>
  <files>src/lib/service/operations.ts, tests/lib/service/idempotency.test.ts</files>
  <behavior>
    - `(client_id, endpoint, idempotency_key)` with equal canonical request hash returns the original operation before, during, and after terminal execution.
    - The same key with different input returns `idempotency_conflict` and schedules no executor.
    - A crash after reservation but before execution cannot create a second operation on retry.
    - Workspace open and close adapters prove long-running lifecycle, safe cancellation boundaries, and destructive deduplication without broadening product scope.
  </behavior>
  <action>Add canonical request hashing and an atomic reservation mapping to operation ID before any executor scheduling per D-12. Scope keys by authenticated client and endpoint, persist terminal mappings for the configured retention window, and serialize competing reservations. Register only existing workspace `open` and `close` mutation adapters as the initial capability set: they are the smallest existing lifecycle pair that proves asynchronous progress, cancellation boundaries, and idempotency without adding product breadth. Preserve direct CLI/OpenTUI calls; adapters invoke existing library functions and translate callbacks/results into contract records. Test parallel duplicate requests, conflicts, restart at each reservation/execution boundary, retention expiry, and exactly-once executor calls.</action>
  <verify><automated>bun test tests/lib/service/idempotency.test.ts tests/lib/service/operations.test.ts &amp;&amp; bun run typecheck</automated></verify>
  <done>Retries cannot duplicate workspace work, conflicts are explicit, restart behavior is deterministic, and only open/close are advertised as mutation capabilities.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Authenticated request to destructive executor | Retried or conflicting input must not duplicate mutations. |
| Operation persistence to restart recovery | Partial records must not be interpreted as permission to rerun work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-104-10 | Tampering | idempotency request hash | high | mitigate | Strict request parse, canonical serialization, credential/endpoint scoped reservation before execution. |
| T-104-11 | Repudiation | operation terminal records | medium | mitigate | Durable timestamps, stages, completed work, original error, and rollback outcome. |
| T-104-12 | Denial of Service | terminal registry | medium | mitigate | Age and count retention with active-operation exemption and injected boundary tests. |
</threat_model>

<verification>
Run `bun test tests/lib/service/operations.test.ts tests/lib/service/idempotency.test.ts && bun run typecheck`.
</verification>

<success_criteria>
- EVT-01 mutations are asynchronous, structured, query-safe, and cancellable at safe boundaries.
- EVT-02 reservations survive terminal state and restart and never duplicate work.
- EVT-03 operation lifecycle and progress records enter the same append-first durable stream as attention events.
</success_criteria>

<output>
Create `.planning/phases/104-workspace-service-and-event-contract/104-04-SUMMARY.md` when done.
</output>
