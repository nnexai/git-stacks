---
phase: 104-workspace-service-and-event-contract
plan: "05"
type: execute
wave: 2
depends_on: ["104-01"]
files_modified:
  - src/lib/service/event-journal.ts
  - src/lib/service/event-broker.ts
  - src/lib/messages.ts
  - tests/lib/service/event-journal.test.ts
  - tests/lib/service/event-broker.test.ts
autonomous: true
requirements: [EVT-03, EVT-04, EVT-05, SVC-04]
must_haves:
  truths:
    - "Operation and attention events share one durable service-wide monotonic cursor and are appended before live publication (D-14)."
    - "Retention is bounded by seven days and 64 MiB; stale cursors return replay_gap with earliest cursor and snapshot revision (D-15, D-16)."
    - "Each subscriber is capped by 256 events and 1 MiB; overflow disconnects with last delivered cursor without blocking producers (D-17)."
  artifacts:
    - path: src/lib/service/event-journal.ts
      provides: "Validated ordered durable append/replay/compaction"
    - path: src/lib/service/event-broker.ts
      provides: "Bounded nonblocking per-client mailboxes"
  key_links:
    - from: src/lib/service/operations.ts
      to: src/lib/service/event-journal.ts
      via: "injected append-first operation event publisher consumed by Plan 04"
      pattern: "publishOperationEvent|appendEvent"
    - from: src/lib/messages.ts
      to: src/lib/service/event-journal.ts
      via: "compatible attention publication adapter"
      pattern: "appendMessage|publish"
---

<objective>
Implement the authoritative ordered event journal and bounded live broker required by D-14 through D-17.

Purpose: Reconnecting clients need crash-safe replay or explicit rebuild, while slow consumers must never block producers or exhaust memory.
Output: Durable journal, replay-gap/compaction semantics, bounded broker, and compatible attention publication.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/104-workspace-service-and-event-contract/104-CONTEXT.md
@.planning/phases/104-workspace-service-and-event-contract/104-RESEARCH.md
@.planning/phases/104-workspace-service-and-event-contract/104-01-SUMMARY.md
@src/lib/messages.ts

<interfaces>
Preserve `appendMessage`, `listMessages`, `listMessagesSync`, `clearMessages`, and best-effort legacy dashboard notification behavior. New structured attention publication is additive; existing JSONL message commands remain compatible.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append, publish, replay, validate, and compact one ordered durable journal</name>
  <files>src/lib/service/event-journal.ts, tests/lib/service/event-journal.test.ts</files>
  <behavior>
    - Concurrent publishers receive unique increasing decimal-string sequences and durable validated records before callbacks run.
    - The exported operation-event publisher accepts lifecycle/progress payloads without a preassigned cursor, validates and appends them, then invokes live publication only after durable append succeeds.
    - Restart preserves sequence; replay returns strictly ordered records after a cursor and tolerates only a partial final record.
    - Cursors before retained history return replay_gap with earliest available cursor and injected current snapshot revision.
    - Compaction enforces both age and byte limits by atomic replacement and never removes an active append generation incorrectly.
  </behavior>
  <action>Create a single serialized event writer using strict contract schemas, decimal-string/bigint sequence persistence, append-first publication, and confined service paths per D-14. Export an injected `OperationEventPublisher`/`publishOperationEvent` seam that accepts typed lifecycle and progress payloads without caller-assigned sequence, validates them, durably appends and fsyncs the allocated record, and only then invokes the live-delivery callback; append failure must publish nothing. Plan 04 consumes this seam, so keep it independent of `operations.ts`. Validate every replayed record, fail on interior corruption, tolerate/repair only a partial final line, and atomically compact under injected seven-day/64-MiB limits per D-15. Decode opaque cursors and return structured `replay_gap` including earliest retained cursor and snapshot revision provider per D-16. Add injected clock/root/limits and crash/restart/concurrent ordering/corrupt-tail/exact-boundary tests, including explicit append-before-callback and append-failure-no-publication assertions.</action>
  <verify><automated>bun test tests/lib/service/event-journal.test.ts &amp;&amp; bun run typecheck</automated></verify>
  <done>Journal order is durable and restart-safe, the operation publisher exposes an append-before-live-delivery contract, compaction is dual-bounded, corruption behavior is explicit, and old cursors produce actionable replay-gap metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add bounded replay-live handoff and compatible attention publication</name>
  <files>src/lib/service/event-broker.ts, src/lib/messages.ts, tests/lib/service/event-broker.test.ts</files>
  <behavior>
    - Subscriber registration captures a journal high-water mark, replays through it, then drains queued newer records without gaps; duplicate cursors are harmless.
    - Event-count and encoded-byte overflow each disconnect a slow subscriber with its last safely delivered cursor.
    - Publishing never awaits subscriber consumption and one subscriber failure cannot affect others.
    - Existing message send/list/clear output and storage remain compatible while appendMessage also emits a structured attention event when a publisher is configured.
  </behavior>
  <action>Implement per-client broker queues capped at 256 events and 1 MiB encoded bytes, with injected limits for tests. Register under the journal serialization boundary, capture a high-water cursor, replay through it, then switch to queued live records without a handoff race. On overflow mark an explicit slow-consumer disconnect with last delivered cursor; never silently drop or await consumers per D-17. Add a failure-isolated optional structured publisher to `messages.ts` after legacy append succeeds, mapping message data to attention events without removing current JSONL/socket compatibility (SVC-04). Test concurrent handoff, both caps, producer latency, disconnect cleanup, subscriber isolation, and unchanged message APIs.</action>
  <verify><automated>bun test tests/lib/service/event-broker.test.ts tests/lib/messages.test.ts &amp;&amp; bun run typecheck</automated></verify>
  <done>Replay-to-live delivery has no race, slow clients are bounded and explicitly disconnected, producers remain nonblocking, and legacy messages still work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Persisted journal to SSE consumer | Corrupt or stale records must not be trusted or silently skipped. |
| Producer to subscriber queues | Slow clients can attempt memory/resource exhaustion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-104-13 | Tampering | event journal/cursor | high | mitigate | Strict record/cursor validation, serialized sequence, atomic compaction, explicit corruption policy. |
| T-104-14 | Denial of Service | event broker | high | mitigate | Dual queue caps, connection cleanup, nonblocking publication, explicit disconnect. |
| T-104-15 | Information Disclosure | attention events | medium | mitigate | Strict event schema excludes raw environment, command output, credentials, and stacks. |
</threat_model>

<verification>
Run `bun test tests/lib/service/event-journal.test.ts tests/lib/service/event-broker.test.ts tests/lib/messages.test.ts && bun run typecheck`.
</verification>

<success_criteria>
- EVT-03 operation/attention records share one ordered durable cursor stream.
- EVT-04 reconnect receives retained replay or explicit rebuild metadata.
- EVT-05 queues/disk are bounded and producers never wait on consumers.
</success_criteria>

<output>
Create `.planning/phases/104-workspace-service-and-event-contract/104-05-SUMMARY.md` when done.
</output>
