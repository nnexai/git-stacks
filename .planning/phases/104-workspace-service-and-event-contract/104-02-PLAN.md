---
phase: 104-workspace-service-and-event-contract
plan: "02"
type: execute
wave: 2
depends_on: ["104-01"]
files_modified:
  - src/lib/service/snapshot.ts
  - tests/lib/service/snapshot.test.ts
  - tests/lib/service/launch-context.test.ts
autonomous: true
requirements: [SVC-02, SVC-03, SVC-04]
must_haves:
  truths:
    - "Each workspace response is one internally consistent aggregate with entities, commands, status, and launch contexts (D-01)."
    - "Equal projections reuse an opaque monotonic revision; changed projections advance it and every response has a diagnostic timestamp (D-03)."
    - "Launch specs contain authoritative cwd, ports, command steps, and safe environment data without requiring YAML reads or exposing resolved secrets."
  artifacts:
    - path: src/lib/service/snapshot.ts
      provides: "Consistent aggregate snapshot and launch projection"
    - path: tests/lib/service/snapshot.test.ts
      provides: "Race/revision/identity tests"
  key_links:
    - from: src/lib/service/snapshot.ts
      to: src/lib/service/identity.ts
      via: "identity-complete authoritative workspace"
      pattern: "ensure.*Identit"
    - from: src/lib/service/snapshot.ts
      to: src/lib/workspace-command.ts
      via: "listManualCommands and planManualCommand"
      pattern: "planManualCommand"
---

<objective>
Build the authoritative aggregate snapshot and safe resolved launch-context adapter required by D-01 and D-03.

Purpose: Native clients must receive one coherent engine-owned view without parsing workspace YAML or observing secret material.
Output: Deterministic snapshot builder with consistency retry, durable revisions, and launch projection.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/104-workspace-service-and-event-contract/104-CONTEXT.md
@.planning/phases/104-workspace-service-and-event-contract/104-RESEARCH.md
@.planning/phases/104-workspace-service-and-event-contract/104-01-SUMMARY.md
@src/lib/workspace-command.ts
@src/lib/workspace-env.ts
@src/lib/workspace-status.ts
@src/lib/workspace-file-status.ts
@src/lib/ports.ts

<interfaces>
Use `listManualCommands(workspace, opts)`, `planManualCommand(workspace, targetName, config)`, `buildWorkspaceEnv(workspace, opts)`, `buildRepoEnv(workspaceEnv, repo)`, `getWorkspaceStatus(workspace)`, and `getWorkspaceFileStatusView(...)`. Snapshot code is an adapter and must not execute manual commands.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove snapshot consistency and monotonic revision semantics</name>
  <files>tests/lib/service/snapshot.test.ts, src/lib/service/snapshot.ts</files>
  <behavior>
    - Pre/post fingerprints that match yield one strict aggregate; a changed fingerprint retries up to three times then returns structured `snapshot_busy`.
    - Canonically identical projections reuse the durable revision across restart; any contract-visible change advances it exactly once.
    - Diagnostic timestamp changes do not advance revision.
    - One-workspace and all-workspace projections contain identity-complete repositories and named commands from one generation.
  </behavior>
  <action>Implement an injectable snapshot builder around identity-complete config reads, current status/file-status modules, and command enumeration. Fingerprint every authoritative input before and after projection, retry three times on change, and never return a mixed generation per D-01. Canonically serialize/hash the strict projection excluding generated timestamp; under a serialized metadata update, atomically persist digest and monotonic decimal-string revision, reusing revision for equal content and returning `snapshot_busy` after exhausted retries per D-03. Tests use injected readers/fingerprints/clock/storage to force races and restart cases.</action>
  <verify><automated>bun test tests/lib/service/snapshot.test.ts &amp;&amp; bun run typecheck</automated></verify>
  <done>Snapshot consistency, bounded retry, durable revision reuse/advance, and all-workspace/one-workspace projection are deterministic and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project redacted authoritative launch specifications</name>
  <files>tests/lib/service/launch-context.test.ts, src/lib/service/snapshot.ts</files>
  <behavior>
    - Shell and named-command launch specs expose resolved cwd, repository binding, command steps, ports, and non-secret environment.
    - Resolved secret values are omitted by default and replaced by redaction/reference metadata.
    - Hidden commands follow existing visibility rules and no command is executed while building a snapshot.
  </behavior>
  <action>Extend snapshot projection to derive shell and command launch specs through existing `workspace-command`, `workspace-env`, config, path, and port functions. Apply a field allowlist: include ordinary resolved environment values, omit resolved secret values by default, and expose only redaction/reference metadata needed for client presentation. Preserve command ordering/scope and existing hidden-command behavior; never parse YAML independently and never execute planned steps. Cover workspace/repository commands, port/env precedence, absent repos, secret references, and strict schema parsing.</action>
  <verify><automated>bun test tests/lib/service/launch-context.test.ts tests/lib/workspace-command.test.ts tests/lib/workspace-env.test.ts &amp;&amp; bun run typecheck</automated></verify>
  <done>Native launch specs are complete for safe execution context, secret-safe by default, schema-valid, and reuse the existing engine semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Config/Git state to aggregate | Independently changing authoritative inputs must not create mixed snapshots. |
| Secret resolver to native client | Resolved environment may contain credentials that are not implied by service authentication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-104-04 | Tampering | snapshot consistency | high | mitigate | Pre/post fingerprints, bounded retry, canonical digest, serialized durable revision. |
| T-104-05 | Information Disclosure | launch contexts | high | mitigate | Explicit field allowlist and default secret omission/redaction tests. |
| T-104-06 | Denial of Service | snapshot retries | medium | mitigate | Three-attempt bound and structured busy response. |
</threat_model>

<verification>
Run `bun test tests/lib/service/snapshot.test.ts tests/lib/service/launch-context.test.ts && bun run typecheck`.
</verification>

<success_criteria>
- Aggregate snapshots satisfy D-01/D-03 under concurrent legacy changes.
- SVC-03 launch data is authoritative and secret-safe by default.
- Existing command/env/status modules remain the single engine boundary.
</success_criteria>

<output>
Create `.planning/phases/104-workspace-service-and-event-contract/104-02-SUMMARY.md` when done.
</output>
