---
phase: 104-workspace-service-and-event-contract
plan: "07"
type: execute
wave: 5
depends_on: ["104-02", "104-05", "104-06"]
files_modified:
  - src/lib/messages.ts
  - src/lib/service/snapshot.ts
  - src/service/main.ts
  - tests/lib/messages.test.ts
  - tests/lib/service/snapshot.test.ts
  - tests/service/events.test.ts
autonomous: true
gap_closure: true
requirements: [SVC-02, SVC-04, EVT-03, EVT-04]
must_haves:
  truths:
    - "A production-managed service publishes legacy-compatible attention messages and operation transitions through the same durable journal and monotonically increasing cursor stream (D-14)."
    - "A production replay_gap names the current nonzero authoritative snapshot revision so a client can rebuild before reconnecting (D-03, D-16)."
    - "Stopping one managed service removes only its own attention adapter and preserves legacy message persistence behavior (SVC-04)."
  artifacts:
    - path: src/service/main.ts
      provides: "Production composition of attention and snapshot-revision providers"
    - path: src/lib/service/snapshot.ts
      provides: "Authoritative current-revision provider owned by the snapshot subsystem"
    - path: tests/service/events.test.ts
      provides: "Real managed-service interleaving and replay-gap transport proof"
  key_links:
    - from: src/lib/messages.ts
      to: src/lib/service/event-journal.ts
      via: "managed-service attention adapter appends before broker publication"
      pattern: "configureAttentionPublication|appendAttention|broker.publish"
    - from: src/lib/service/event-journal.ts
      to: src/lib/service/snapshot.ts
      via: "injected authoritative snapshotRevision callback"
      pattern: "snapshotRevision|currentRevision"
---

<objective>
Close the production composition gaps for the shared attention/operation cursor and authoritative replay-gap rebuild metadata per D-03, D-14, and D-16.

Purpose: The journal and message hooks work in isolation, but native clients cannot rely on them until the managed service owns their wiring and cleanup.
Output: An ownership-safe attention adapter, an authoritative snapshot revision provider, production composition, and behavior-first real-transport tests.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/104-workspace-service-and-event-contract/104-CONTEXT.md
@.planning/phases/104-workspace-service-and-event-contract/104-VERIFICATION.md
@.planning/phases/104-workspace-service-and-event-contract/104-02-SUMMARY.md
@.planning/phases/104-workspace-service-and-event-contract/104-05-SUMMARY.md
@.planning/phases/104-workspace-service-and-event-contract/104-06-SUMMARY.md
@src/lib/messages.ts
@src/lib/service/snapshot.ts
@src/service/main.ts

<interfaces>
Keep legacy message JSONL persistence authoritative and failure-isolated. Resolve a message workspace name through the snapshot/identity subsystem, append the typed attention event to EventJournal, and publish that exact appended record to EventBroker. The revision callback supplied to EventJournal must be produced by the snapshot subsystem and must reflect an authoritative build, not a journal-local counter or constant.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED-test production attention interleaving and authoritative replay gaps</name>
  <files>tests/lib/messages.test.ts, tests/lib/service/snapshot.test.ts, tests/service/events.test.ts</files>
  <behavior>
    - A managed service receives a real appendMessage attention record and an operation publication, then authenticated SSE/replay returns both in their single strictly increasing durable sequence.
    - Restart/replay preserves those event cursors and service shutdown prevents later messages from publishing through the stopped adapter without affecting their legacy JSONL append.
    - After an authoritative snapshot has allocated a revision greater than zero and retention creates a stale cursor, GET /v1/events returns replay_gap whose snapshot_revision equals the snapshot subsystem's current authoritative revision.
    - Two configured attention owners cannot let an older owner's cleanup erase the newer owner's adapter.
  </behavior>
  <action>Write behavior-focused tests before implementation and run them to record RED failures caused by the missing managed-service wiring and default replay revision. Use isolated service/message roots, real bearer-authenticated loopback requests, the real EventJournal/EventBroker, an injected deterministic snapshot builder/revision store, and bounded cleanup; assert decoded SSE event types, cursor order, restart replay, and the exact HTTP replay-gap envelope rather than exported constants. Extend the message unit test to specify ownership-safe disposer semantics while retaining failure isolation.</action>
  <verify><automated>bun test tests/lib/messages.test.ts tests/lib/service/snapshot.test.ts tests/service/events.test.ts</automated></verify>
  <done>The new named behavioral cases fail for the verified production gaps, not because of fixture, authentication, timing, or cleanup errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire attention publication and authoritative revision ownership</name>
  <files>src/lib/messages.ts, src/lib/service/snapshot.ts, src/service/main.ts, tests/lib/messages.test.ts, tests/lib/service/snapshot.test.ts, tests/service/events.test.ts</files>
  <behavior>
    - Attention publication resolves stable workspace UUIDs and uses journal append before broker delivery.
    - EventJournal receives a snapshot-owned provider that returns the revision of a current authoritative snapshot build and never falls back to the default zero in managed composition.
    - Managed-service stop disposes only the adapter instance it installed.
  </behavior>
  <action>Make configureAttentionPublication return an idempotent ownership-aware disposer that clears only the same installed adapter. Extend createSnapshotBuilder with a typed currentRevision operation that performs/uses an authoritative build and returns the greatest allocated decimal revision from that consistent result; cover empty-workspace behavior explicitly without inventing a revision. In startManagedService, construct EventJournal with that snapshot revision provider, install an attention adapter whose workspaceId resolves the named workspace through the authoritative snapshot identity and whose publish awaits journal.appendAttention before broker.publish, and invoke its disposer during stop even when server shutdown or descriptor cleanup throws. Preserve D-14 append-before-live ordering, D-16 exact rebuild metadata, and SVC-04 legacy append success/failure isolation. Make the RED cases green and keep existing snapshot/message tests unchanged.</action>
  <verify><automated>bun test tests/lib/messages.test.ts tests/lib/service/snapshot.test.ts tests/service/events.test.ts &amp;&amp; bun run typecheck</automated></verify>
  <done>Real attention and operation events interleave durably in production, replay gaps expose the current authoritative nonzero revision, and lifecycle cleanup cannot detach another service's publisher.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Legacy message producer to public event contract | Workspace names must resolve to stable identities and sensitive legacy fields must not leak. |
| Snapshot persistence to replay client | Rebuild metadata must identify authoritative state rather than a fabricated or stale constant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-104-20 | Tampering | attention adapter lifecycle | high | mitigate | Ownership-aware disposer and managed shutdown tests prevent cross-instance adapter removal. |
| T-104-21 | Information Disclosure | message to attention projection | high | mitigate | Publish only stable workspace ID, code, and message through strict event schema. |
| T-104-22 | Repudiation | replay-gap revision | high | mitigate | Source revision from an authoritative snapshot build and assert exact HTTP metadata. |
</threat_model>

<verification>
Run the focused RED/GREEN suites, then `bun run typecheck` and `bun run test:deps`.
</verification>

<success_criteria>
- EVT-03 is proven with real production-composed attention and operation events in one cursor stream.
- EVT-04 replay-gap metadata contains the revision returned by the authoritative snapshot subsystem after a nonzero build.
- Existing message persistence and CLI/OpenTUI-compatible behavior remain additive and failure-isolated.
</success_criteria>

<output>
Create `.planning/phases/104-workspace-service-and-event-contract/104-07-SUMMARY.md` when done.
</output>
