---
phase: 104-workspace-service-and-event-contract
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/service/credentials.ts
  - tests/lib/service/credentials.test.ts
  - tests/service/security.test.ts
autonomous: true
requirements: [SVC-05]
must_haves:
  truths:
    - "Each installed official client automatically obtains its own revocable protected credential without a pairing prompt (D-06, D-08)."
    - "Missing, malformed, unknown, and revoked credentials receive the same generic rejection before route details are evaluated (D-09)."
    - "Credential storage rejects unsafe ownership, permissions, and symlink layouts."
  artifacts:
    - path: src/lib/service/credentials.ts
      provides: "Protected per-client provisioning, verification, and revocation"
    - path: tests/service/security.test.ts
      provides: "Admission-oracle and filesystem boundary coverage"
  key_links:
    - from: src/lib/service/credentials.ts
      to: src/service/server.ts
      via: "authenticate-before-route API consumed by Plan 06"
      pattern: "authenticate|verifyCredential"
---

<objective>
Implement the protected, automatic, per-client credential boundary required by D-06, D-08, and D-09.

Purpose: Loopback alone is not an authorization boundary; the service must authenticate official clients without leaking route or credential detail.
Output: Credential registry/provisioning/revocation and deterministic security tests.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/104-workspace-service-and-event-contract/104-CONTEXT.md
@.planning/phases/104-workspace-service-and-event-contract/104-RESEARCH.md
@src/lib/config.ts
@src/lib/paths.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Provision and revoke protected per-client credentials</name>
  <files>src/lib/service/credentials.ts, tests/lib/service/credentials.test.ts</files>
  <behavior>
    - Provisioning an installed client ID is automatic and stable; a distinct client ID receives a distinct random token.
    - Revocation invalidates only the selected client credential and reprovisioning follows an explicit replacement path.
    - Registry directories/files are owner-only; symlinks, wrong owners, and permissive pre-existing files are rejected or repaired only where safe.
    - Tokens never appear in logs, errors, descriptors, or persisted plaintext fields other than the protected credential record.
  </behavior>
  <action>Create a credentials module beneath the configured `service/` root with injected filesystem/random/clock seams. Generate client IDs/tokens with runtime crypto, create secrets exclusively, validate owner-only parent and file modes, reject symlinks/non-owned entries where supported, atomically persist non-secret registry metadata, and expose provision/read-for-official-client/revoke/verify APIs per D-06 and D-08. Verification uses equal-length `timingSafeEqual`; returned diagnostics never include token material. Tests cover exact modes, stale/corrupt registry, per-client isolation, revocation, same-client reuse, and unsafe paths.</action>
  <verify><automated>bun test tests/lib/service/credentials.test.ts &amp;&amp; bun run typecheck</automated></verify>
  <done>Automatic per-installed-client provisioning, stable lookup, revocation, safe storage, and constant-time verification are proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define generic authentication admission semantics</name>
  <files>src/lib/service/credentials.ts, tests/service/security.test.ts</files>
  <behavior>
    - Missing, malformed, unknown, wrong, and revoked bearer values return the same status/body and expose no route/capability/version detail.
    - Authentication occurs before route lookup, request-schema parsing, and per-credential rate state.
    - Valid credentials yield only a stable client identity/capability context, never the secret.
  </behavior>
  <action>Add a transport-neutral admission helper that parses only the Authorization bearer form needed for authentication, returns one constant generic rejection for all unauthenticated cases per D-09, and yields a non-secret authenticated client context on success. Build the security test harness as a callable admission/router stub so it establishes ordering before Plan 06 wires Bun HTTP. Assert identical unauthenticated responses across real/unknown routes and malformed bodies and ensure credential-specific rate/capability information is unreachable before success.</action>
  <verify><automated>bun test tests/service/security.test.ts tests/lib/service/credentials.test.ts &amp;&amp; bun run typecheck</automated></verify>
  <done>The authentication oracle is closed and the server-facing admission interface is ready for transport wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Same-user filesystem to credential registry | Pre-existing paths and permissions may be attacker-controlled. |
| HTTP Authorization header to client identity | Untrusted bearer input must not reveal credential or route state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-104-07 | Spoofing | credential verification | high | mitigate | CSPRNG tokens, per-client records, revocation, equal-length timing-safe comparison. |
| T-104-08 | Information Disclosure | auth rejection | high | mitigate | Authenticate before routing and return one generic response for every failure. |
| T-104-09 | Tampering | credential filesystem | high | mitigate | Owner/mode/symlink validation, exclusive create, and atomic metadata replacement. |
</threat_model>

<verification>
Run `bun test tests/lib/service/credentials.test.ts tests/service/security.test.ts && bun run typecheck`.
</verification>

<success_criteria>
- SVC-05 credential provisioning and revocation are automatic, protected, and per client.
- Unauthenticated callers learn no protocol, route, or credential details.
</success_criteria>

<output>
Create `.planning/phases/104-workspace-service-and-event-contract/104-03-SUMMARY.md` when done.
</output>
