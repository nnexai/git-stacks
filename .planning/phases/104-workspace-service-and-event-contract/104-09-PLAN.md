---
phase: 104-workspace-service-and-event-contract
plan: "09"
type: execute
wave: 2
depends_on: ["104-08"]
files_modified:
  - src/lib/service/contract.ts
  - src/service/server.ts
  - tests/lib/service/contract.test.ts
  - tests/fixtures/service-v1/request-timeout-error.json
  - tests/service/security.test.ts
autonomous: true
gap_closure: true
requirements: [SVC-05, SVC-02, EVT-01]
must_haves:
  truths:
    - "Every ordinary authenticated handler has an enforced 30-second execution deadline distinct from Bun socket idle timeout."
    - "A timed-out handler returns HTTP 504 with a strict stable request_timeout ErrorEnvelope and late completion or rejection cannot write another response."
    - "After a stalled snapshot or operation handler times out, a subsequent valid authenticated request succeeds and SSE remains exempt from the ordinary deadline."
  artifacts:
    - path: src/lib/service/contract.ts
      provides: "Closed request_timeout error code in the canonical v1 contract"
    - path: tests/fixtures/service-v1/request-timeout-error.json
      provides: "Golden strict timeout error envelope"
    - path: src/service/server.ts
      provides: "Injectable ordinary-handler execution deadline and timeout mapping"
    - path: tests/service/security.test.ts
      provides: "Real loopback stalled-adapter timeout and recovery proof"
  key_links:
    - from: src/service/server.ts
      to: src/lib/service/contract.ts
      via: "HTTP 504 timeout response parsed by ErrorEnvelopeSchema"
      pattern: "request_timeout|ErrorEnvelopeSchema"
    - from: src/service/server.ts
      to: snapshot and operation adapters
      via: "ordinary-route promise raced with injected deadline/abort signal"
      pattern: "handlerTimeout|AbortSignal|request_timeout"
---

<objective>
Enforce SVC-05's ordinary request processing deadline independently of socket inactivity.

Purpose: Bun's idle timeout does not bound an awaited snapshot or operation adapter, so authenticated work can occupy service resources indefinitely.
Output: A closed-schema timeout code and golden fixture, an injectable execution-deadline primitive, exact HTTP 504 mapping, and real loopback stall/recovery coverage.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/104-workspace-service-and-event-contract/104-CONTEXT.md
@.planning/phases/104-workspace-service-and-event-contract/104-VERIFICATION.md
@.planning/phases/104-workspace-service-and-event-contract/104-06-SUMMARY.md
@.planning/phases/104-workspace-service-and-event-contract/104-08-SUMMARY.md
@src/lib/service/contract.ts
@src/service/server.ts
@tests/lib/service/contract.test.ts
@tests/service/security.test.ts

<interfaces>
Extend the closed ErrorCodeSchema with exactly `request_timeout`. The public response is HTTP 504 and `{ protocol: "v1", request_id: <opaque request id>, ok: false, error: { code: "request_timeout", message: "Service request timed out" } }`, with no details, cause, adapter text, or stack; it must parse through ErrorEnvelopeSchema and its golden fixture. The deadline applies after authenticate-first admission to every non-SSE route that may await request-body, snapshot, submit, status, or cancel work. Keep Bun request timeout as socket protection, but use a separate injected millisecond execution deadline for handler promises. SSE uses timeout zero and is excluded.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED-test ordinary handler deadline and service recovery</name>
  <files>src/lib/service/contract.ts, tests/lib/service/contract.test.ts, tests/fixtures/service-v1/request-timeout-error.json, tests/service/security.test.ts</files>
  <behavior>
    - ErrorCodeSchema accepts request_timeout, the golden HTTP error body parses exactly through ErrorEnvelopeSchema, and unknown timeout spellings/extra fields are rejected.
    - A bearer-authenticated GET /v1/snapshot backed by a never-settling adapter returns HTTP 504 and the exact golden request_timeout body at an injected short deadline, before any socket idle timeout.
    - A late resolving or rejecting adapter cannot replace the timeout response or surface sensitive error text/unhandled rejection.
    - A subsequent authenticated discovery and fast snapshot request succeeds on the same server.
    - An open SSE connection survives beyond the injected ordinary deadline.
  </behavior>
  <action>First add the canonical request-timeout golden fixture and contract assertions requiring ErrorCodeSchema and ErrorEnvelopeSchema to accept its exact body while rejecting undeclared fields and alternate timeout codes; this is RED until the closed enum is extended. Add real port-zero tests with a valid provisioned credential, injected short handler timeout/timer seam, deferred snapshot adapters, and bounded test-level abort protection. Assert HTTP 504, parse the received JSON with ErrorEnvelopeSchema, compare it to the golden fixture after substituting only the generated request_id, prove the exact stable message and absence of adapter error details, and cover one response only, subsequent request health, and SSE exemption. Run the named cases to RED against the closed schema and current socket-idle-only implementation.</action>
  <verify><automated>bun test tests/lib/service/contract.test.ts tests/service/security.test.ts</automated></verify>
  <done>The new transport cases fail specifically because stalled ordinary handler promises have no independent execution deadline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce and map the ordinary execution deadline</name>
  <files>src/lib/service/contract.ts, src/service/server.ts, tests/lib/service/contract.test.ts, tests/fixtures/service-v1/request-timeout-error.json, tests/service/security.test.ts</files>
  <behavior>
    - All ordinary awaited route work races an injected deadline and receives an AbortSignal where adapters can cooperate.
    - Timeout maps to the canonical ErrorEnvelopeSchema body with code request_timeout, exact message `Service request timed out`, and HTTP 504, and consumes no SSE deadline budget.
    - Deadline timers/listeners are cleared on success, failure, timeout, and server shutdown.
  </behavior>
  <action>Add request_timeout to the closed ErrorCodeSchema and keep ApiErrorSchema/ErrorEnvelopeSchema strict; check in the exact golden fixture. Add handlerTimeoutMs to ServiceServerOptions with the production default derived from REQUEST_TIMEOUT_SECONDS. Introduce one ordinary-route deadline wrapper using an AbortController and injected timer seam: race route execution against the deadline, abort cooperative work at expiry, attach handling to the losing promise so late rejection is contained, and clear all deadline resources in finally. Thread the signal through locally owned adapter interfaces where possible without changing public JSON; wrap buildAll, buildWorkspace, request-body parsing, submit, get, and cancel execution consistently. Map only the dedicated timeout sentinel to HTTP 504 and the exact canonical request_timeout envelope/message defined above; retain server.timeout for socket inactivity and keep /v1/events exempt. Make contract/golden and real-transport RED tests green while preserving authenticate-first ordering, rate/body handling, and SSE changes from Plan 08.</action>
  <verify><automated>bun test tests/lib/service/contract.test.ts tests/service/security.test.ts tests/service/discovery.test.ts tests/service/operations.test.ts tests/service/events-backpressure.test.ts &amp;&amp; bun run typecheck</automated></verify>
  <done>Ordinary processing is bounded independently of socket activity, timeout responses are stable and secret-free, late work is contained, subsequent requests succeed, and SSE remains long-lived.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Authenticated request to engine adapters | A paired client can trigger work that stalls despite continued socket activity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-104-26 | Denial of Service | ordinary handler awaits | high | mitigate | Independent execution race, cooperative abort, and real stalled-adapter recovery test. |
| T-104-27 | Information Disclosure | timeout/late rejection mapping | high | mitigate | Strict golden ErrorEnvelopeSchema body and contained losing promise omit internal errors and stacks. |
| T-104-28 | Denial of Service | deadline resources | medium | mitigate | Clear timers/listeners on every terminal path and assert subsequent server health. |
</threat_model>

<verification>
Run focused security and transport regressions, then `bun run typecheck`, `bun run test:deps`, and `bun run verify:gates`.
</verification>

<success_criteria>
- SVC-05 has a behaviorally enforced 30-second ordinary execution deadline rather than a constant/socket-only assertion.
- Timed-out work returns a stable non-sensitive response and cannot poison later requests.
- The complete authenticated HTTP/SSE transport remains compatible after deadline composition.
</success_criteria>

<source_coverage_audit>
## Multi-Source Coverage Audit

| Source | ID | Gap/requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Secure authoritative state, async operations, and structured events through the local v1 contract | 07-09 | COVERED | Closes all production-boundary verification failures. |
| REQ | SVC-05 | Authenticated bounded request, rate, and timeout policy | 09 | COVERED | Real ordinary-handler deadline and recovery. |
| REQ | EVT-03 | Ordered operation and attention events over SSE | 07 | COVERED | Managed attention composition plus real interleaving. |
| REQ | EVT-04 | Replay or gap with authoritative rebuild metadata | 07 | COVERED | Production snapshot revision provider and HTTP gap test. |
| REQ | EVT-05 | Slow clients cannot create unbounded memory/queues | 08 | COVERED | Stalled real-network dual-cap proof. |
| CONTEXT | D-03 | Opaque monotonic authoritative snapshot revision | 07 | COVERED | Snapshot-owned provider. |
| CONTEXT | D-14 | Shared service-wide cursor for operation and attention | 07 | COVERED | Append-before-broker production wiring. |
| CONTEXT | D-15 | Durable age/size-bounded history | 07 | COVERED | Existing journal retention retained and exercised to create real gap. |
| CONTEXT | D-16 | Gap includes earliest cursor and authoritative revision | 07 | COVERED | Exact HTTP metadata assertion. |
| CONTEXT | D-17 | Bounded queue, explicit disconnect, nonblocking producer | 08 | COVERED | Demand-driven transport retains broker accounting. |
| RESEARCH | - | 30-second ordinary deadline distinct from long-lived SSE | 09 | COVERED | Injectable deadline and SSE exemption. |
| RESEARCH | - | Replay/live ordered handoff and bounded subscriber bytes/events | 07-08 | COVERED | Composition and network-bound proof. |
| VERIFICATION | gap-1 | Production attention publication wiring | 07 | COVERED | Exact missing items planned. |
| VERIFICATION | gap-2 | Authoritative replay-gap snapshot revision | 07 | COVERED | Exact missing items planned. |
| VERIFICATION | gap-3 | True SSE/network backpressure | 08 | COVERED | Exact missing items planned. |
| VERIFICATION | gap-4 | Ordinary-handler execution deadline | 09 | COVERED | Exact missing items planned. |

Existing satisfied SVC-01 through SVC-04 and EVT-01 through EVT-02 behavior is regression-tested where touched. Deferred remote connectivity and Phase 105+ client/terminal work remain excluded.
</source_coverage_audit>

<output>
Create `.planning/phases/104-workspace-service-and-event-contract/104-09-SUMMARY.md` when done.
</output>
