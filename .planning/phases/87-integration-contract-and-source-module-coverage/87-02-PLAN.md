---
phase: 87-integration-contract-and-source-module-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/lib/integrations/github.test.ts
  - tests/lib/integrations/gitlab.test.ts
  - tests/lib/integrations/gitea.test.ts
  - tests/lib/integrations/jira.test.ts
autonomous: true
requirements:
  - INTG-03
  - GATE-03
must_haves:
  truths:
    - "D-05: Forge command modules use injected executors for command argument construction, JSON parse failures, missing PR/issue/repo cases, exit-code propagation, and safe browser-open behavior."
    - "D-06: Real external CLIs, browser windows, desktop sessions, and window managers are not launched by Phase 87 tests."
    - "D-08: GitHub, GitLab, Gitea, and Jira command contracts are in scope where implemented."
    - "INTG-03: Forge command integrations are covered with injected executors for argument construction, JSON parse failures, missing PR/issue/repo cases, exit-code propagation, and safe browser-open behavior."
  artifacts:
    - path: "tests/lib/integrations/github.test.ts"
      provides: "GitHub command contract tests"
    - path: "tests/lib/integrations/gitlab.test.ts"
      provides: "GitLab command contract tests"
    - path: "tests/lib/integrations/gitea.test.ts"
      provides: "Gitea command contract tests"
    - path: "tests/lib/integrations/jira.test.ts"
      provides: "Jira issue command contract tests"
  key_links:
    - from: "tests/lib/integrations/github.test.ts"
      to: "src/lib/integrations/github.ts"
      via: "`githubIntegration.commands()` and `_exec.run`"
      pattern: "_exec.run|githubIntegration"
    - from: "tests/lib/integrations/gitea.test.ts"
      to: "src/lib/integrations/gitea.ts"
      via: "`giteaIntegration.commands()`, `_exec.runCapture`, and `_exec.openUrl`"
      pattern: "giteaIntegration\\.commands|runCapture|openUrl"
    - from: "tests/lib/integrations/jira.test.ts"
      to: "src/lib/integrations/jira.ts"
      via: "`jiraIntegration.commands()` and `_exec.runShell`"
      pattern: "jiraIntegration\\.commands|runShell"
---

<objective>
Expand forge command contract tests so GitHub, GitLab, Gitea, and Jira command modules prove their executor-facing behavior without real external tools.

Purpose: INTG-03 requires command integration confidence for argument construction, failure branches, JSON parsing, safe browser-open behavior, and exit-code propagation.
Output: Focused additions to existing forge command test files.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/87-integration-contract-and-source-module-coverage/87-CONTEXT.md
@.planning/phases/87-integration-contract-and-source-module-coverage/87-RESEARCH.md
@.planning/phases/87-integration-contract-and-source-module-coverage/87-PATTERNS.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/TESTING.md
@src/lib/integrations/github.ts
@src/lib/integrations/gitlab.ts
@src/lib/integrations/gitea.ts
@src/lib/integrations/jira.ts
@tests/lib/integrations/github.test.ts
@tests/lib/integrations/gitlab.test.ts
@tests/lib/integrations/gitea.test.ts
@tests/lib/integrations/jira.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add GitHub and GitLab command failure contracts</name>
  <files>tests/lib/integrations/github.test.ts, tests/lib/integrations/gitlab.test.ts</files>
  <read_first>
    - tests/lib/integrations/github.test.ts
    - tests/lib/integrations/gitlab.test.ts
    - src/lib/integrations/github.ts
    - src/lib/integrations/gitlab.ts
  </read_first>
  <behavior>
    - GitHub `open`, `pr create`, `pr open`, `pr status`, and `issue open` use the expected `gh` args and cwd through `_exec.run`.
    - GitLab `open`, `pr create`, `pr open`, `pr status`, and `issue open` use the expected `glab` args and cwd through `_exec.run`.
    - Nonzero `_exec.run` results propagate through `process.exit(exitCode)` for representative GitHub and GitLab commands.
    - Missing forge repo and missing linked issue branches format errors and exit without running executor commands.
  </behavior>
  <action>Extend the existing Commander-based tests per D-05 and D-06. Keep `makeForgeUtilsMock()` and `makeIssueUtilsMock()` as boundary mocks for command-module tests; do not call real `gh` or `glab`. Add representative nonzero exit-code propagation coverage and missing repo/issue branches for both modules. Preserve existing command-argument assertions and the `process.exit` mock pattern.</action>
  <verify>
    <automated>bun test tests/lib/integrations/github.test.ts tests/lib/integrations/gitlab.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - GitHub and GitLab tests assert at least one nonzero executor exit code is passed to `process.exit`.
    - Missing repo and missing issue branches do not call `_exec.run`.
    - Focused GitHub/GitLab tests pass.
  </acceptance_criteria>
  <done>GitHub and GitLab command contracts cover success and representative safe-failure behavior through injected executors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Gitea and Jira parse/browser/open contracts</name>
  <files>tests/lib/integrations/gitea.test.ts, tests/lib/integrations/jira.test.ts</files>
  <read_first>
    - tests/lib/integrations/gitea.test.ts
    - tests/lib/integrations/jira.test.ts
    - src/lib/integrations/gitea.ts
    - src/lib/integrations/jira.ts
  </read_first>
  <behavior>
    - Gitea `pr open` and `issue open` cover valid JSON, malformed JSON, missing PR/issue, safe browser-open via `_exec.openUrl`, and nonzero capture exits.
    - Gitea web/no-web repository open paths do not launch real browsers and use `_exec.run`, `_exec.runCapture`, `_exec.openUrl`, and `_exec.gitRemoteUrl` only.
    - Jira `issue open` uses global `integrations.jira.open_cmd` when present, falls back to `jira open $ISSUE_ID`, passes `ISSUE_ID` through `_exec.runShell`, and propagates nonzero exit codes.
  </behavior>
  <action>Extend existing Gitea and Jira tests per D-05, D-06, and D-08. For Gitea, exercise JSON parse failures and missing entity cases from `runCapture` output without invoking `tea`, `open`, or `xdg-open`. For Jira, mock `readGlobalConfig()` to cover configured and default `open_cmd` values and assert `_exec.runShell(command, { ISSUE_ID })` receives the exact expected command. Keep all external effects behind `_exec` mocks.</action>
  <verify>
    <automated>bun test tests/lib/integrations/gitea.test.ts tests/lib/integrations/jira.test.ts</automated>
    <automated>bun run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - Gitea tests include malformed JSON and missing PR/issue assertions.
    - Jira tests include configured and default open command assertions plus nonzero exit propagation.
    - Focused Gitea/Jira tests and `bun run test:unit` pass.
  </acceptance_criteria>
  <done>Gitea and Jira command contracts cover parser, missing entity, browser-open, and shell command behavior without real external environments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Command modules -> external forge CLIs | Command modules construct CLI args but tests must intercept executor calls. |
| Command modules -> browser/shell open | Browser and shell-open behavior must remain injected and observable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-02-01 | Elevation of Privilege | Browser/shell open paths | mitigate | Assert `_exec.openUrl` and `_exec.runShell` receive expected arguments and no real opener is launched. |
| T-87-02-02 | Tampering | Forge CLI command construction | mitigate | Assert exact argv arrays and cwd values for GitHub, GitLab, and Gitea commands. |
| T-87-02-03 | Denial of Service | JSON parse and missing entity branches | mitigate | Add malformed JSON and missing PR/issue tests that exit safely. |
</threat_model>

<verification>
Run focused forge command tests, `bun run test:unit`, and `bun run typecheck`. Do not run `bun test tests/` directly.
</verification>

<success_criteria>
INTG-03 is covered for implemented forge command modules, D-05/D-06/D-08 are visible in task directives, and local GATE-03 checks remain green.
</success_criteria>

<output>
After completion, create `.planning/phases/87-integration-contract-and-source-module-coverage/87-02-SUMMARY.md`
</output>
