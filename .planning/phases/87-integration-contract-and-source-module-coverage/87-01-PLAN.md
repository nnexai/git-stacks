---
phase: 87-integration-contract-and-source-module-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/lib/integrations/issue-utils.test.ts
  - tests/lib/integrations/forge-utils.test.ts
autonomous: true
requirements:
  - INTG-02
  - GATE-03
must_haves:
  truths:
    - "D-01: Tests import and exercise real `src/lib/integrations/**` utility modules and public helper APIs."
    - "D-02: Source-bypassing inline copies in issue-utils and forge-utils tests are removed or replaced with real source imports."
    - "D-03: Module mocks are limited to true external boundaries: config storage, workspace detection, subprocess detection, filesystem setup, and environment discovery."
    - "D-04: Mock-module files remain compatible with the custom runner isolation model and do not require `bun test tests/`."
    - "INTG-02: Issue and forge utilities cover workspace/repo resolution, linked issue persistence, error formatting, enabled-forge detection, base branch selection, missing-tool cases, and no-remote cases."
  artifacts:
    - path: "tests/lib/integrations/issue-utils.test.ts"
      provides: "Real-source tests for issue utility contracts"
    - path: "tests/lib/integrations/forge-utils.test.ts"
      provides: "Real-source tests for forge utility contracts"
  key_links:
    - from: "tests/lib/integrations/issue-utils.test.ts"
      to: "src/lib/integrations/issue-utils.ts"
      via: "dynamic real import after dependency mocks"
      pattern: "resolveIssueRef|linkIssue|unlinkIssue|formatIssueError|resolveWorkspaceArg"
    - from: "tests/lib/integrations/forge-utils.test.ts"
      to: "src/lib/integrations/forge-utils.ts"
      via: "dynamic real import after dependency mocks"
      pattern: "resolveForgeRepo|detectForgeForRepoEnabled|formatForgeError"
---

<objective>
Repair utility integration tests so Phase 87 coverage reflects the real `issue-utils` and `forge-utils` source modules.

Purpose: INTG-02 and CORE-05-style milestone constraints require coverage from source execution, not tests that paste implementation logic into mocks.
Output: Updated utility test files that exercise real source exports while mocking only config, workspace detection, and shell detection boundaries.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/87-integration-contract-and-source-module-coverage/87-CONTEXT.md
@.planning/phases/87-integration-contract-and-source-module-coverage/87-RESEARCH.md
@.planning/phases/87-integration-contract-and-source-module-coverage/87-PATTERNS.md
@.planning/phases/87-integration-contract-and-source-module-coverage/87-VALIDATION.md
@.planning/codebase/TESTING.md
@src/lib/integrations/issue-utils.ts
@src/lib/integrations/forge-utils.ts
@tests/lib/integrations/issue-utils.test.ts
@tests/lib/integrations/forge-utils.test.ts
@tests/helpers.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace inline issue-utils implementation copies with real source imports</name>
  <files>tests/lib/integrations/issue-utils.test.ts</files>
  <read_first>
    - tests/lib/integrations/issue-utils.test.ts
    - src/lib/integrations/issue-utils.ts
    - tests/helpers.ts
    - .planning/phases/87-integration-contract-and-source-module-coverage/87-CONTEXT.md
  </read_first>
  <behavior>
    - Existing linked issue resolution, link, unlink, numeric ID coercion, missing link, and error formatting assertions still pass.
    - New or existing CWD workspace resolution tests exercise `resolveWorkspaceArg()` through the real source export with mocked `detectWorkspaceFromCwd()`.
    - The test file no longer contains an inline `mock.module("@/lib/integrations/issue-utils"` factory that reimplements `resolveIssueRef`, `linkIssue`, `unlinkIssue`, `formatIssueError`, or `resolveWorkspaceArg`.
  </behavior>
  <action>Update `tests/lib/integrations/issue-utils.test.ts` per D-01, D-02, D-03, and D-04. Mock `@/lib/config` and `@/lib/workspace-status` as true external boundaries, then dynamically import the real `@/lib/integrations/issue-utils` source module after mocks are registered. Keep process-exit interception around `resolveWorkspaceArg()` failure branches. Preserve all existing behavior assertions for linked issue persistence and error text, and add missing CWD-detection success/failure coverage if the current file does not prove it.</action>
  <verify>
    <automated>bun test tests/lib/integrations/issue-utils.test.ts</automated>
    <automated>! rg -n "Inline the source implementations|function resolveIssueRef|function linkIssue|function unlinkIssue|function formatIssueError|function resolveWorkspaceArg" tests/lib/integrations/issue-utils.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/lib/integrations/issue-utils.test.ts` imports the module under test from `@/lib/integrations/issue-utils`.
    - `tests/lib/integrations/issue-utils.test.ts` does not define local replacement implementations for source exports.
    - Focused issue-utils tests pass.
  </acceptance_criteria>
  <done>Issue utility tests exercise real source exports and keep only dependency-boundary mocks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace inline forge-utils implementation copies with real source imports</name>
  <files>tests/lib/integrations/forge-utils.test.ts</files>
  <read_first>
    - tests/lib/integrations/forge-utils.test.ts
    - src/lib/integrations/forge-utils.ts
    - src/lib/integrations/types.ts
    - tests/helpers.ts
    - .planning/phases/81.1-repo-add-honors-enabled-forge-integrations/81.1-CONTEXT.md
  </read_first>
  <behavior>
    - Existing worktree-only selection, trunk rejection, any-mode selection, forge mismatch, base-branch fallback, detection, enabled-forge allowlist, no-tool, and no-remote assertions still pass.
    - Tests mutate the real exported `_detect` seam instead of defining a parallel `_detect` object inside a mocked `forge-utils` implementation.
    - The test file no longer contains local replacement implementations for `resolveForgeRepo`, `resolveForgeRepoAnyMode`, detection functions, or `formatForgeError`.
  </behavior>
  <action>Update `tests/lib/integrations/forge-utils.test.ts` per D-01, D-02, D-03, D-04, and the Phase 81.1 enabled-forge allowlist rule. Mock `@/lib/config` and shell/Bun boundaries only as needed, then dynamically import the real `@/lib/integrations/forge-utils` source module. Use the real exported `_detect.which`, `_detect.gitRemoteUrl`, and `_detect.teaPullsLs` seams for missing-tool, no-remote, GitHub, GitLab, Gitea, multi-detect, and `detectForgeForRepoEnabled()` cases.</action>
  <verify>
    <automated>bun test tests/lib/integrations/forge-utils.test.ts</automated>
    <automated>! rg -n "Inline the source implementations|function resolveForgeRepo|function resolveForgeRepoAnyMode|async function detectGitHubForge|function formatForgeError" tests/lib/integrations/forge-utils.test.ts</automated>
    <automated>bun run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `tests/lib/integrations/forge-utils.test.ts` imports the module under test from `@/lib/integrations/forge-utils`.
    - Enabled-forge detection tests prove disabled forges are skipped and enabled matching forges are returned.
    - Focused forge-utils tests and `bun run test:unit` pass through the repo runner.
  </acceptance_criteria>
  <done>Forge utility tests exercise real source exports, preserve Phase 81.1 enablement semantics, and avoid real forge tools.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test mocks -> source utility modules | Tests provide fake config, workspace-status, and detection results to real utility source. |
| Source utility modules -> external tools | `_detect` and workspace detection represent shell/environment boundaries that must stay injected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01-01 | Tampering | Utility test mocks | mitigate | Mock only dependency boundaries and assert test files no longer define replacement source implementations. |
| T-87-01-02 | Repudiation | Coverage proof | mitigate | Verify focused tests plus grep gates that reject inline source copies. |
| T-87-01-03 | Denial of Service | Forge detection | mitigate | Use `_detect` mocks for missing-tool/no-remote cases so no real CLIs or network prompts run. |
</threat_model>

<verification>
Run `bun test tests/lib/integrations/issue-utils.test.ts tests/lib/integrations/forge-utils.test.ts`, then `bun run test:unit` and `bun run typecheck`. Do not run `bun test tests/` directly.
</verification>

<success_criteria>
INTG-02 is covered by real-source utility tests. D-01 through D-04 are visible in executable tasks. GATE-03 remains green for the focused and unit/typecheck gates.
</success_criteria>

<output>
After completion, create `.planning/phases/87-integration-contract-and-source-module-coverage/87-01-SUMMARY.md`
</output>
