---
phase: 87-integration-contract-and-source-module-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/lib/integrations/tmux.test.ts
  - tests/lib/integrations/cmux.test.ts
  - tests/lib/integrations/niri.test.ts
  - tests/lib/integrations/aerospace.test.ts
  - tests/lib/integrations/vscode.test.ts
  - tests/lib/integrations/intellij.test.ts
autonomous: true
requirements:
  - INTG-04
  - GATE-03
must_haves:
  truths:
    - "D-06: Session and IDE tests do not launch real external CLIs, browser windows, desktop sessions, or window managers."
    - "D-07: Session/IDE integrations use injected executors or fake detectors for config parsing, command construction, artifact-bag routing, skip behavior, and safe failure handling."
    - "D-08: tmux, cmux, niri, AeroSpace, VSCode, and IntelliJ integration contracts are in scope where applicable."
    - "D-09: Live forge auth, real browser windows, real editor/IDE launches, real window-manager state, broad TUI rendering, CI workflows, and milestone finalization remain out of scope."
    - "INTG-04: Session and IDE integrations are covered through injected executors and fake detectors without real desktop/window-manager environments."
  artifacts:
    - path: "tests/lib/integrations/cmux.test.ts"
      provides: "Cmux integration plugin contract tests"
    - path: "tests/lib/integrations/vscode.test.ts"
      provides: "VSCode integration plugin contract tests"
    - path: "tests/lib/integrations/intellij.test.ts"
      provides: "IntelliJ integration plugin contract tests"
    - path: "tests/lib/integrations/niri.test.ts"
      provides: "Niri integration contract extensions"
    - path: "tests/lib/integrations/aerospace.test.ts"
      provides: "AeroSpace integration contract extensions"
  key_links:
    - from: "tests/lib/integrations/tmux.test.ts"
      to: "src/lib/integrations/tmux.ts"
      via: "mocked `src/lib/tmux.ts` helpers"
      pattern: "tmuxIntegration|openTmuxSession|killTmuxSession"
    - from: "tests/lib/integrations/niri.test.ts"
      to: "src/lib/integrations/niri.ts"
      via: "mocked `src/lib/niri.ts` helpers and artifact bags"
      pattern: "niriIntegration|ArtifactBag|source"
    - from: "tests/lib/integrations/aerospace.test.ts"
      to: "src/lib/integrations/aerospace.ts"
      via: "mocked `src/lib/aerospace.ts` helpers and artifact bags"
      pattern: "aerospaceIntegration|ArtifactBag|source"
---

<objective>
Add and extend session/IDE integration contract tests so Phase 87 covers plugin behavior without real desktop environments.

Purpose: INTG-04 requires confidence that tmux/cmux/niri/AeroSpace/VSCode/IntelliJ integrations parse config, construct commands, use artifact bags, skip safely, and fail safely through injected seams.
Output: Focused test additions under `tests/lib/integrations/`.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/87-integration-contract-and-source-module-coverage/87-CONTEXT.md
@.planning/phases/87-integration-contract-and-source-module-coverage/87-RESEARCH.md
@.planning/phases/87-integration-contract-and-source-module-coverage/87-PATTERNS.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/TESTING.md
@src/lib/integrations/tmux.ts
@src/lib/integrations/cmux.ts
@src/lib/integrations/niri.ts
@src/lib/integrations/aerospace.ts
@src/lib/integrations/vscode.ts
@src/lib/integrations/intellij.ts
@tests/lib/integrations/tmux.test.ts
@tests/lib/integrations/niri.test.ts
@tests/lib/integrations/aerospace.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tmux and cmux pane/config contract coverage</name>
  <files>tests/lib/integrations/tmux.test.ts, tests/lib/integrations/cmux.test.ts</files>
  <read_first>
    - tests/lib/integrations/tmux.test.ts
    - src/lib/integrations/tmux.ts
    - src/lib/integrations/cmux.ts
    - src/lib/tmux.ts
    - src/lib/cmux.ts
  </read_first>
  <behavior>
    - tmux tests cover pane layout config parsing, command dispatch to panes, focus behavior, cleanup skip when session is absent, and safe null/invalid config behavior.
    - New cmux plugin tests cover workspace open, existing workspace handling, pane/surface layout config parsing, command dispatch, focus behavior, and artifact return shape.
    - No real `tmux` or `cmux` process is launched.
  </behavior>
  <action>Extend `tests/lib/integrations/tmux.test.ts` and create `tests/lib/integrations/cmux.test.ts` if it does not exist, per D-06, D-07, and D-08. Mock `@/lib/tmux` and `@/lib/cmux` helper modules before importing integration plugins. Assert exact helper call order for representative pane layout entries: main pane command, split pane direction, cwd/repo surface command where implemented, and focus calls. Keep invalid or missing config as a no-op with no helper calls beyond opening the workspace/session.</action>
  <verify>
    <automated>bun test tests/lib/integrations/tmux.test.ts tests/lib/integrations/cmux.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/lib/integrations/cmux.test.ts` exists and imports `cmuxIntegration`.
    - tmux and cmux tests assert helper calls instead of real CLI calls.
    - Focused tmux/cmux tests pass.
  </acceptance_criteria>
  <done>Terminal multiplexer integration contracts are covered through fake helper modules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add IDE integration plugin contract coverage</name>
  <files>tests/lib/integrations/vscode.test.ts, tests/lib/integrations/intellij.test.ts</files>
  <read_first>
    - src/lib/integrations/vscode.ts
    - src/lib/integrations/intellij.ts
    - src/lib/vscode.ts
    - src/lib/intellij.ts
    - tests/lib/vscode.test.ts
    - tests/lib/intellij.test.ts
  </read_first>
  <behavior>
    - VSCode plugin tests cover `generate()`, `open()` returning null with no artifact path, `_exec.spawn([cmd, artifactPath])`, artifact shape, and command `open <workspace>` error handling for failed generation.
    - IntelliJ plugin tests cover Java-only `applies()`, `generate()`, `open()` returning null with no artifact path, `_exec.spawn(["idea", artifactPath])`, and artifact shape.
    - Tests do not launch real `code`, `code-insiders`, or `idea`.
  </behavior>
  <action>Create plugin-level test files under `tests/lib/integrations/` per D-06, D-07, D-08, and D-09. Mock `@/lib/vscode`, `@/lib/intellij`, and source config/workspace readers as needed before importing the integration modules. Use each module's exported `_exec.spawn` seam for command construction assertions. Reuse existing library-level tests only as pattern references; do not move or delete them unless duplication becomes misleading.</action>
  <verify>
    <automated>bun test tests/lib/integrations/vscode.test.ts tests/lib/integrations/intellij.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Plugin-level VSCode and IntelliJ test files exist under `tests/lib/integrations/`.
    - Tests assert no-artifact skip behavior and spawn argument construction.
    - Focused IDE integration tests pass.
  </acceptance_criteria>
  <done>IDE integration plugin contracts are covered without launching real editors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend niri and AeroSpace artifact/skip/failure contracts</name>
  <files>tests/lib/integrations/niri.test.ts, tests/lib/integrations/aerospace.test.ts</files>
  <read_first>
    - tests/lib/integrations/niri.test.ts
    - tests/lib/integrations/aerospace.test.ts
    - src/lib/integrations/niri.ts
    - src/lib/integrations/aerospace.ts
  </read_first>
  <behavior>
    - Niri tests cover artifact-bag source window routing, not-running skip behavior, invalid/missing config no-op behavior, and safe helper failure handling.
    - AeroSpace tests cover artifact-bag source window routing, not-running skip behavior, config validation failures, flatten/layout/focus command construction, and safe move failure handling.
    - Tests keep all window-manager interactions behind mocked `@/lib/niri` and `@/lib/aerospace` helpers.
  </behavior>
  <action>Extend existing niri and AeroSpace tests per D-06, D-07, D-08, and D-09. Focus on gaps not already covered by current tests: artifact bag reuse from `source`, skip behavior when detectors report unavailable environments, invalid config failures that surface safely, and helper failure branches that do not launch real window-manager commands. Preserve existing cache-busted import patterns and helper mocks.</action>
  <verify>
    <automated>bun test tests/lib/integrations/niri.test.ts tests/lib/integrations/aerospace.test.ts</automated>
    <automated>bun run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - Niri and AeroSpace tests include artifact-bag source routing assertions.
    - Not-running and invalid-config branches are covered without real window-manager calls.
    - Focused niri/AeroSpace tests and `bun run test:unit` pass.
  </acceptance_criteria>
  <done>Window-manager integration contracts cover routing, skip, config, and failure behavior through fake detectors and helper modules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integration plugins -> desktop tools | Plugins construct commands and call helper libraries that would normally reach real tools. |
| Artifact bag -> window-manager routing | Integration artifacts from one plugin influence window movement in another plugin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-03-01 | Tampering | Artifact bag routing | mitigate | Assert source artifact IDs are consumed only through typed window artifact fields. |
| T-87-03-02 | Denial of Service | Missing external environments | mitigate | Cover not-running/no-artifact skip behavior returning null or no-op without process launches. |
| T-87-03-03 | Elevation of Privilege | IDE/window-manager command construction | mitigate | Assert command arrays through injected executors and fake helper modules. |
</threat_model>

<verification>
Run focused session/IDE tests, then `bun run test:unit` and `bun run typecheck`. Do not run `bun test tests/` directly.
</verification>

<success_criteria>
INTG-04 is covered across session, window-manager, and IDE integration contracts. D-06 through D-09 are implemented only through injected seams and fake detectors. GATE-03 remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/87-integration-contract-and-source-module-coverage/87-03-SUMMARY.md`
</output>
