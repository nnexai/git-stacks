# Phase 1.1: File and Folder Copy/Symlink Support - Context

**Gathered:** 2026-03-17
**Status:** Ready for planning

<domain>
## Phase Boundary

Extend the existing file operations system (`src/lib/files.ts`) to support: folder operations alongside files (same arrays, no separation), glob pattern expansion, absolute and `~/`-relative source paths, two config levels (workspace-instance-level applied once, per-repo level applied to each specific repo), loud failure on missing sources, and idempotent re-application on `open`.

Hardlinks and cross-repo source paths are explicitly out of scope for this phase.

</domain>

<decisions>
## Implementation Decisions

### Operations supported
- `copy` and `symlink` only — hardlinks deferred to a future phase
- Files AND folders in the same arrays — no separate `copy_dirs`/`symlink_dirs` keys
- Glob patterns supported in all arrays (workmux-style) — e.g., `.env.*`, `secrets/**`

### Config levels
Two distinct levels with different targets:

**Level 1 — Workspace-instance level (new):**
- `files:` on `StackSchema` → merged with `files:` on `WorkspaceSchema`
- Applied **once** to the workspace instance root: `{tasksDir}/{wsName}/`
- Does NOT apply to each repo — runs once for the whole workspace
- Useful for: shared `.env` at the workspace root, `node_modules` symlink at workspace level, large shared binaries accessible to all repos via relative path

**Level 2 — Per-repo level (extends existing):**
- `files:` on `StackRepoSchema` (already exists) → merged with `files:` on `WorkspaceRepoSchema` (new)
- Applied to that **specific repo's** `task_path` only — not broadcast to other repos
- Per-repo entries in both stack YAML and workspace YAML apply only to their own repo

**Merge semantics:** Within each level, the stack definition and workspace definition are merged additively. The two levels are independent — workspace-instance ops do not affect repos, per-repo ops do not affect the workspace root.

### Source path resolution

**Workspace-instance level:**
- Relative path → resolved relative to the workspace instance root (`{tasksDir}/{wsName}/`)
- Absolute path → used as-is
- `~/`-relative path → expanded via `expandHome()`

**Per-repo level:**
- Relative path → resolved relative to that repo's `main_path`
- Absolute path → used as-is
- `~/`-relative path → expanded via `expandHome()`

### Error behavior
Fail loud — a missing source path (after glob expansion) is an error surfaced to the user, not a silent skip. Current `// silently skip` pattern in `applyFileOperations` is removed.

### When to apply
- **`new`**: Apply all file ops during workspace creation (existing behavior)
- **`open`**: Re-apply idempotently — only apply ops where the destination does not yet exist. Do not overwrite existing destinations.

### Claude's Discretion
- Glob library choice (fast-glob, micromatch, or Bun native — check what's already in dependencies)
- Exact merge function signature (how merged lists are passed into `applyFileOperations`)
- Whether `WorkspaceRepo.files` is optional with `.optional()` default (yes — backward compat required)
- How folder copy is implemented (Node `cpSync` with `recursive: true` vs shell fallback)
- How to handle a glob pattern that matches zero files (warn or error — leaning toward warn since glob zero-match is often intentional)

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Existing implementation
- `src/lib/files.ts` — Current `applyFileOperations(stackRepo, taskPath)` — this is the function to extend/replace
- `src/lib/config.ts` — `StackRepoSchema.files` (existing schema), `StackSchema`, `WorkspaceSchema`, `WorkspaceRepoSchema` — all need `files:` additions
- `src/tui/workspace-wizard.ts` line 224 — only call site of `applyFileOperations`; needs updating for new levels and `open` path

### Inspiration / design reference
- https://github.com/raine/workmux/blob/main/docs/guide/configuration.md — workmux `files:` block design (copy/symlink arrays with glob support, two-level config)

### Requirements
- `.planning/REQUIREMENTS.md` — Phase 1.1 requirements are TBD; this context defines them

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `src/lib/files.ts:applyFileOperations` — existing per-repo file ops; needs to be extended and joined by a new workspace-instance-level function; both become part of an orchestration layer that merges configs and supports globs/folders
- `src/lib/config.ts:StackRepoSchema.files` — existing schema shape (`copy`, `symlink` arrays) — extend, don't replace; the same shape should be used at all levels
- `src/lib/paths.ts:expandHome` — already exists for `~/` expansion; use this for source path resolution
- `tests/helpers.ts` — `makeTmpDir`, `cleanup`, `touch`, `write` — extend with folder fixtures for file-ops tests

### Established Patterns
- Schema fields must use `.optional()` or `.default([])` — new `files:` fields on `StackSchema`/`WorkspaceSchema`/`WorkspaceRepoSchema` must follow CONF-02 (no new required fields)
- Error handling: use discriminated union `{ ok: true } | { ok: false; error: string }` — `applyFileOperations` currently returns `void`; upgrade to return errors
- All YAML schemas live in `src/lib/config.ts` — both new `files:` fields on stack and workspace schemas go here
- TypeScript strict mode — glob result types must be fully typed, no `any`

### Integration Points
- `src/tui/workspace-wizard.ts:224` — `applyFileOperations(stackRepo, wsRepo.task_path)` — update to call new orchestrator for both levels
- `src/lib/workspace-ops.ts` — `openWorkspace` — needs to call file ops on open with idempotent check
- `src/lib/config.ts` — add `files` field to `StackSchema` and `WorkspaceSchema` (workspace-instance level); add `files` to `WorkspaceRepoSchema` (per-repo workspace override)
- Workspace instance path: `join(getTasksDir(config.workspace_root), wsName)` — this is the target for workspace-instance-level file ops

</code_context>

<specifics>
## Specific Ideas

- Design inspiration: workmux's `files:` block — same arrays handle files and folders, glob expansion, two config levels that merge additively
- Use cases driving the design: `.env` files in a central place, `node_modules` or `.pnpm-store` shared via symlink, large model/binary files shared via symlink from a `~/models/` directory
- Source paths with `~/` must expand via the existing `expandHome()` utility in `paths.ts`

</specifics>

<deferred>
## Deferred Ideas

- Hardlinks (`hardlink:` array) — deferred; filesystem limitation (dirs can't be hardlinked) makes design non-trivial
- Global config level `files:` — deferred; applying to every repo everywhere conflicts with loud-fail semantics
- Cross-repo source paths (referencing another repo's path as source) — explicitly out of scope; use absolute/`~/` paths instead
- `"<global>"` merge token (workmux-style include marker) — could allow workspace-level to opt into stack-level list; deferred for now, additive merge is sufficient

</deferred>

---

*Phase: 01.1-file-and-folder-copy-symlink-support-between-repos-for-large-binary-sharing*
*Context gathered: 2026-03-17*
