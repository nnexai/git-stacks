---
phase: 93.1-parallel-integration-test-runner-and-coherent-coverage-mergi
plan: 02
subsystem: testing
tags: [bun, fixtures, temp-dirs, integration-tests]

requires:
  - phase: 93.1-01
    provides: serialOnlyFiles metadata for audited serial exceptions
provides:
  - Parallel-safe temp roots for the known workspace edit, clone, and wizard integration fixtures
  - Fixture-safety regression guard for shared /tmp roots and serial-only metadata
affects:
  - 93.1-03

tech-stack:
  added: []
  patterns:
    - "Integration fixture paths derive from per-file makeTmpDir roots and are cleaned with afterAll."
    - "Shared real-write /tmp roots are protected by a focused source scan test."

key-files:
  created:
    - tests/lib/test-runner-fixture-safety.test.ts
    - .planning/phases/93.1-parallel-integration-test-runner-and-coherent-coverage-mergi/93.1-02-SUMMARY.md
  modified:
    - tests/commands/workspace-edit.test.ts
    - tests/tui/workspace-clone.test.ts
    - tests/tui/workspace-wizard.test.ts

key-decisions:
  - "No serial-only exception was needed; `serialOnlyFiles` remains empty."
  - "Dashboard `/tmp/fake.yml` paths stay allowed because those tests mock editor paths and do not write to them."

patterns-established:
  - "Use `makeTmpDir()` plus `cleanup()` for integration-style test files with workspace/task/editor paths."
  - "Guard collision-prone literals in a test instead of relying on manual review."

requirements-completed: [TEST-02]

duration: 12min
completed: 2026-05-16
---

# Phase 93.1 Plan 02 Summary

**Parallel-safe workspace fixture roots with regression guards for shared `/tmp` collisions**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-05-16T10:25:00Z
- **Completed:** 2026-05-16T10:37:00Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Replaced the known shared `/tmp/test-workspaces` and `/tmp/test.yml` literals in workspace edit, clone, and wizard tests with per-file temp roots.
- Added `tests/lib/test-runner-fixture-safety.test.ts` to catch those shared real-write paths if they return.
- Verified `serialOnlyFiles` remains empty and any future entries must have a named file and human-readable reason.

## Task Commits

1. **Tasks 93.1-02-01 and 93.1-02-02:** `670cb12` (test)

**Plan metadata:** this summary commit follows.

## Files Created/Modified

- `tests/lib/test-runner-fixture-safety.test.ts` - Regression guard for fixture path collisions and serial-only exceptions.
- `tests/commands/workspace-edit.test.ts` - Uses a unique temp workspace root, task root, and editor target.
- `tests/tui/workspace-clone.test.ts` - Uses a unique temp task root in source fixtures and assertions.
- `tests/tui/workspace-wizard.test.ts` - Uses a unique temp workspace root, task root, and editor target.

## Decisions Made

- Mock-only dashboard `/tmp/fake.yml` paths are documented in the guard test as allowed because those files do not write to the path.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Verification

- `bun test tests/lib/test-runner-fixture-safety.test.ts tests/commands/workspace-edit.test.ts tests/tui/workspace-clone.test.ts tests/tui/workspace-wizard.test.ts`
- `bun run typecheck`

## Next Phase Readiness

Coverage integration can now schedule current integration files in bounded parallel workers without known shared real-write fixture collisions, and without needing a serial bucket.

---
*Phase: 93.1-parallel-integration-test-runner-and-coherent-coverage-mergi*
*Completed: 2026-05-16*
