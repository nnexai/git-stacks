---
phase: 81.1.1-minimal-non-interactive-workspace-create-and-clone-variants
plan: 01
subsystem: cli
tags: [commander, workspace, clone, template, non-interactive, bun-test]

requires:
  - phase: 81.1
    provides: enabled-aware repo add forge detection baseline
provides:
  - non-interactive template-backed workspace creation for git-stacks new
  - non-interactive workspace clone path for git-stacks clone
  - focused unit coverage for prompt-free create and clone behavior
affects: [phase-82, e2e-09, workspace-cli, template-e2e]

tech-stack:
  added: []
  patterns:
    - early non-interactive guard before prompt calls
    - TDD RED/GREEN commits for CLI prompt-skipping behavior

key-files:
  created: []
  modified:
    - src/commands/workspace.ts
    - src/tui/workspace-wizard.ts
    - src/tui/workspace-clone.ts
    - tests/tui/workspace-wizard.test.ts
    - tests/tui/workspace-clone.test.ts

key-decisions:
  - "Non-interactive new only resolves template names and repeatable --template composition; local-path --from remains interactive-only."
  - "Non-interactive clone preserves source workspace settings while skipping integration override prompts."
  - "Workspace opening remains opt-in through --open for both non-interactive paths."

patterns-established:
  - "Non-interactive CLI variants validate required inputs up front, emit explicit missing-input errors, and return before prompt calls."
  - "Automation-facing paths assert prompt mocks are not called in tests."

requirements-completed: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11]

duration: 5 min
completed: 2026-05-14
---

# Phase 81.1.1 Plan 01: Minimal Non-Interactive Workspace Create and Clone Variants Summary

**Prompt-free template workspace creation and workspace cloning for downstream subprocess E2E coverage**

## Performance

- **Duration:** 5 min
- **Started:** 2026-05-14T17:52:43Z
- **Completed:** 2026-05-14T17:58:08Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Added `git-stacks new` flags for `--branch`, `--non-interactive`, and `--open`, threaded into `runWorkspaceNew`.
- Added `git-stacks clone` flags for `--name`, `--branch`, `--non-interactive`, and `--open`, with `validateName(opts.name)` applied at the CLI boundary.
- Implemented early non-interactive guards that validate required inputs, skip prompt calls, preserve branch defaults, and only open workspaces when `--open` is set.
- Added focused tests proving prompt mocks are not called for both non-interactive create and clone paths.

## Task Commits

Each task was committed atomically:

1. **Task 1 RED: new --non-interactive tests** - `f03e5da` (test)
2. **Task 1 GREEN: new --non-interactive implementation** - `0d79c38` (feat)
3. **Task 2 RED: clone --non-interactive tests** - `c407050` (test)
4. **Task 2 GREEN: clone --non-interactive implementation** - `257e56b` (feat)

**Plan metadata:** committed separately after summary/state updates.

## Files Created/Modified

- `src/commands/workspace.ts` - Adds non-interactive, branch, open, and clone destination flags and validates clone `--name`.
- `src/tui/workspace-wizard.ts` - Adds prompt-free template-backed creation guard with missing-input validation, template resolution, branch defaulting, and open opt-in.
- `src/tui/workspace-clone.ts` - Adds prompt-free clone guard with source/name validation, worktree creation, integration artifact generation, and open opt-in.
- `tests/tui/workspace-wizard.test.ts` - Covers non-interactive new happy paths, fail-fast paths, prompt skipping, branch defaults, and open behavior.
- `tests/tui/workspace-clone.test.ts` - Covers non-interactive clone happy path, fail-fast paths, prompt skipping, label preservation, branch defaults, and open behavior.

## Verification

- `bun test tests/tui/workspace-wizard.test.ts` - PASS, 16 tests.
- `bun test tests/tui/workspace-clone.test.ts` - PASS, 9 tests.
- `bun run test` - PASS, unit tests PASS and integration tests 58/58 passed.
- `grep -c "non-interactive" src/commands/workspace.ts` - 5.
- `grep -c "opts?.nonInteractive" src/tui/workspace-wizard.ts` - 1.
- `grep -c "opts?.nonInteractive" src/tui/workspace-clone.ts` - 1.

## Decisions Made

- Non-interactive `new --from` intentionally accepts template names only, matching D-10 and avoiding local-path automation scope creep.
- Non-interactive clone preserves the source workspace settings snapshot and skips `promptIntegrationOverrides`, matching the inherited/default cascade requirement.
- Existing interactive flows stay intact; the new behavior is isolated behind early `opts?.nonInteractive` guards.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Known Stubs

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Phase 82 can now use `git-stacks new <name> --from <template> --non-interactive`, repeatable `--template` composition, and `git-stacks clone <source> --name <dest> --non-interactive` in subprocess E2E tests without driving TUI prompts.

## Self-Check: PASSED

- Summary file exists.
- Required modified files exist.
- Task commits exist in git history.
- Focused and full automated verification passed.

---
*Phase: 81.1.1-minimal-non-interactive-workspace-create-and-clone-variants*
*Completed: 2026-05-14*
