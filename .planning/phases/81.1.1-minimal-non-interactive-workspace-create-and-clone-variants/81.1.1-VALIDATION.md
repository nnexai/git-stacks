---
phase: 81.1.1
slug: minimal-non-interactive-workspace-create-and-clone-variants
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-04-10
---

# Phase 81.1.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `bun:test` |
| **Config file** | `package.json` scripts + custom test runner (`scripts/test-runner.ts`) |
| **Quick run command** | `bun test tests/tui/workspace-wizard.test.ts && bun test tests/tui/workspace-clone.test.ts` |
| **Full suite command** | `bun run test` |
| **Estimated runtime** | ~20 seconds |

---

## Sampling Rate

- **After every task commit:** Run `bun test tests/tui/workspace-wizard.test.ts && bun test tests/tui/workspace-clone.test.ts`
- **After every plan wave:** Run `bun run test`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 20 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 81.1.1-01-01 | 01 | 1 | D-01, D-02, D-04, D-09, D-10, D-11 | T-81.1.1-01 / V5-INPUT | `new --non-interactive` only accepts the template-backed paths in scope, fails fast when required inputs are missing, preserves branch defaults when omitted, and skips optional metadata prompts while preserving CLI labels. | unit | `bun test tests/tui/workspace-wizard.test.ts` | ✅ (extend) | ⬜ pending |
| 81.1.1-01-02 | 01 | 1 | D-03, D-06, D-07, D-08 | T-81.1.1-02 / V5-INPUT | `clone --non-interactive` requires `--name`, skips integration and open prompts, preserves the default branch when omitted, and only opens when `--open` is explicitly passed. | unit | `bun test tests/tui/workspace-clone.test.ts` | ✅ (extend) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/tui/workspace-wizard.test.ts` — extend with `new --non-interactive` coverage for `--from`, repeatable `--template`, missing-input errors, prompt skipping, and local-path rejection
- [ ] `tests/tui/workspace-clone.test.ts` — extend with `clone --non-interactive` coverage for required `--name`, default branch behavior, prompt skipping, and `--open`

*Existing infrastructure already covers framework installation and isolated execution.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| None | — | — | — |

*All phase behaviors should have automated verification.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 20s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
