# Phase 81.1.1: Minimal non-interactive workspace create and clone variants - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-10T21:05:00Z
**Phase:** 81.1.1-minimal-non-interactive-workspace-create-and-clone-variants
**Areas discussed:** CLI surface shape, Required input set, Default behavior for skipped prompts, Scope boundary

---

## CLI surface shape

### Non-interactive entrypoint

| Option | Description | Selected |
|--------|-------------|----------|
| Add an explicit non-interactive mode/flag to existing `new` and `clone` | Keep the existing verbs and make automation intentional. | ✓ |
| Add separate dedicated commands | Create parallel non-interactive verbs. | |
| Auto-switch when enough args are present | Infer non-interactive mode implicitly. | |

**User's choice:** Add an explicit non-interactive mode/flag to the existing `new` and `clone` commands.
**Notes:** This should not become a second command family.

---

### Flag naming

| Option | Description | Selected |
|--------|-------------|----------|
| `--non-interactive` | Use an explicit automation-oriented flag name. | ✓ |
| `--yes` / `-y` | Use a generic confirmation-skipping flag. | |
| Support both | Add both styles up front. | |

**User's choice:** `--non-interactive` on `new` and `clone`.
**Notes:** The flag should be descriptive rather than overloaded.

---

### Missing-input behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Fail fast with a clear error | Strict mode that reports missing required inputs. | ✓ |
| Prompt for missing pieces | Fall back into interactive behavior. | |
| Infer/default missing values | Guess values silently. | |

**User's choice:** Fail fast with a clear error listing the missing inputs.
**Notes:** The mode is meant to be deterministic for tests and automation.

---

### Command coverage in this phase

| Option | Description | Selected |
|--------|-------------|----------|
| Land `--non-interactive` for both `new` and `clone` now | Keep the prerequisite complete for both later create and clone E2E work. | ✓ |
| `new` first | Defer clone support. | |
| `clone` first | Defer new support. | |

**User's choice:** Land `--non-interactive` for both `new` and `clone` in this phase.

---

## Required input set

### Branch handling

| Option | Description | Selected |
|--------|-------------|----------|
| Reuse existing default branch logic when omitted | Preserve current branch defaults in non-interactive mode. | ✓ |
| Require explicit branch | Force users to always pass one. | |
| Default only for `new` | Make clone stricter than create. | |

**User's choice:** Reuse the existing default branch logic when `--branch` is omitted.

---

### Wizard field exposure

| Option | Description | Selected |
|--------|-------------|----------|
| Keep it minimal | Reuse existing args/flags plus optional `--branch`; leave description/ports out. | ✓ |
| Add a few more flags | Expose description and ports too. | |
| Expose every wizard field | Full prompt-to-flag parity. | |

**User's choice:** Keep it minimal.

---

### Clone destination name

| Option | Description | Selected |
|--------|-------------|----------|
| Add `--name <new-name>` to `clone` | Supply the missing destination name explicitly without changing positional args. | ✓ |
| Add a second positional argument | Expand the positional command signature. | |
| Read from stdin/environment | Avoid a dedicated CLI flag. | |

**User's choice:** Add `--name <new-name>` to `clone`.

---

## Default behavior for skipped prompts

### Integration overrides

| Option | Description | Selected |
|--------|-------------|----------|
| Skip prompt, keep inherited/default cascade | Preserve normal integration resolution without prompting. | ✓ |
| Disable all integrations | Make non-interactive runs integration-free by default. | |
| Require explicit CLI integration config | Force integration settings into flags. | |

**User's choice:** Skip the override prompt and use the normal inherited defaults/cascade as-is.

---

### Open-now behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Do not open unless explicitly requested | Make opening an opt-in side effect. | ✓ |
| Open automatically | Preserve today’s default open posture. | |
| Always skip opening | Never allow open-at-create in non-interactive mode. | |

**User's choice:** Do not open automatically unless the user passes an explicit open flag.

---

### Open opt-in surface

| Option | Description | Selected |
|--------|-------------|----------|
| Add `--open` | Explicit opt-in for opening in non-interactive mode. | ✓ |
| No create-time open flag | Always require a separate `open` command. | |
| Add `--no-open` and stay open-by-default | Preserve current default with an escape hatch. | |

**User's choice:** Add an explicit `--open` flag.

---

### Optional metadata

| Option | Description | Selected |
|--------|-------------|----------|
| Skip prompts when omitted | Keep CLI labels if present, otherwise no extra labels, empty description, no extra ports. | ✓ |
| Require explicit flags | Make optional metadata mandatory to avoid prompts. | |
| Still prompt | Keep optional prompts even in non-interactive mode. | |

**User's choice:** Skip the prompts and keep the omitted fields empty/defaulted.

---

## Scope boundary

### In-scope create/clone paths

| Option | Description | Selected |
|--------|-------------|----------|
| Only unblock later E2E work | Cover template-backed `new --non-interactive` and `clone --non-interactive`. | ✓ |
| Also include `new --from <local-path>` | Broaden into one-repo auto-register automation. | |
| Broaden to ad-hoc repo-picking | Turn this into generalized non-interactive creation. | |

**User's choice:** Only the paths needed to unblock later E2E work.

---

### Composed-template support

| Option | Description | Selected |
|--------|-------------|----------|
| Include both `--from <template>` and repeatable `--template` composition | Keep later composition E2E work unblocked. | ✓ |
| Only support `--from <template>` | Defer composed templates. | |
| Defer composed templates to Phase 82 | Leave the prerequisite incomplete for composition. | |

**User's choice:** Include both `--from <template>` and repeatable `--template` composition.

---

## the agent's Discretion

- Exact helper extraction for missing-input validation and prompt skipping.
- Exact error wording and `--open` wiring.
- Exact code split between command parsing and prompt flow reuse.

## Deferred Ideas

- Non-interactive ad-hoc repo-picking for `new`
- Non-interactive `new --from <local-path>`
- Full flag parity for all optional wizard fields
