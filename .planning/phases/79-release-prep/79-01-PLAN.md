---
phase: 79
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - CHANGELOG.md
  - README.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - release-prep
must_haves:
  truths:
    - "package.json version is 0.17.0"
    - "`bun run dev --version` reports 0.17.0"
    - "CHANGELOG.md has a v0.17.0 entry with Added, Changed, and Internal sections"
    - "The changelog describes template labels as one cohesive feature and does not mention the reverted Phase 78.1 capabilities surface as a user-facing release item"
    - "README.md documents `git-stacks template label add|remove|list|clear`, `template list --label`, and that template labels propagate to created workspaces"
    - "README.md documents `GS_DEBUG=<module[,module]>` filtering while preserving `GIT_STACKS_DEBUG=1` compatibility guidance"
    - "bun run typecheck, bun run test, and bun run dev --version all succeed"
  artifacts:
    - path: "package.json"
      provides: "Canonical npm package version for the v0.17.0 release"
      contains: "\"version\": \"0.17.0\""
    - path: "CHANGELOG.md"
      provides: "User-facing release notes for v0.17.0"
      contains: "## [0.17.0]"
    - path: "README.md"
      provides: "Release-aligned docs for template labels and debug filters"
      contains: "template label"
  key_links:
    - from: "package.json"
      to: "src/lib/version.ts / src/index.ts"
      via: "versionString package metadata lookup"
      pattern: "0.17.0 visible in CLI version output"
---

<objective>
Execute Phase 79 as a single release-prep plan: bump the published package version to `0.17.0`, add a user-facing `CHANGELOG.md` entry that accurately summarizes the completed v0.17.0 milestone work, extend `README.md` so template-label commands/filtering/propagation and `GS_DEBUG` module filtering are documented, then update the normal GSD state artifacts to mark the phase complete.

Purpose: turn the already-completed milestone implementation work into a ship-ready release surface. This plan is packaging/documentation only and must not introduce new product behavior.

Output: updated release metadata/docs, a completed Phase 79 summary, a verification report, and milestone state files showing v0.17.0 release prep complete.
</objective>

<execution_context>
@CLAUDE.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/79-release-prep/79-CONTEXT.md
@.planning/phases/79-release-prep/79-DISCUSSION-LOG.md
@package.json
@CHANGELOG.md
@README.md
@src/commands/template.ts
@src/index.ts
</context>

<notes>
- Changelog voice is user-facing first.
- Template-label work is one cohesive bullet.
- Reliability/internal work should be grouped into a small number of outcome-focused bullets.
- Do not present the reverted Phase 78.1 capabilities-list surface as a user-facing release item.
</notes>
