---
phase: 73-release-prep
plan: 01
type: execute
wave: 1
depends_on:
  - 72
files_modified:
  - package.json
  - CHANGELOG.md
  - README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "`package.json` version reads `\"0.16.0\"`"
    - "`git-stacks --version` reports `0.16.0`"
    - "CHANGELOG.md has a `## [0.16.0]` section above `## [0.15.0]`"
    - "CHANGELOG.md documents both the stderr-only debug surface and the core engine extraction/testing work"
    - "README.md has a standalone `## Debug Output` section explaining `GIT_STACKS_DEBUG=1`"
    - "README.md states that debug stays on `stderr` and that `manage` suppresses debug before the TUI starts"
  artifacts:
    - path: package.json
      provides: Version 0.16.0
      contains: '"version": "0.16.0"'
    - path: CHANGELOG.md
      provides: v0.16.0 release notes
      contains: "## [0.16.0]"
    - path: README.md
      provides: Debug output docs
      contains: "## Debug Output"
---

# Phase 73 Plan 01: Release Prep v0.16.0

## Objective

Prepare the v0.16.0 release artifacts by bumping the package version, adding a changelog entry for the shipped observability and extraction work, and documenting `GIT_STACKS_DEBUG=1` in the README.

## Tasks

1. Change `package.json` version from `0.15.0` to `0.16.0`.
2. Add a `## [0.16.0] — 2026-04-05` section to `CHANGELOG.md` covering:
   - `GIT_STACKS_DEBUG=1` stderr-only labeled debug output and JSON safety
   - the workspace-engine module split plus `bun run test:deps`
3. Add a standalone `## Debug Output` section to `README.md` with examples, stderr behavior, and the `manage` TUI suppression note.

## Verification

- `grep '"version": "0.16.0"' package.json`
- `bun run src/index.ts --version`
- `grep -n '## \[0.16.0\]' CHANGELOG.md`
- `grep -n '## Debug Output' README.md`
- `bun test tests/lib/observability.test.ts tests/commands/debug-output.test.ts tests/commands/status-json.test.ts`
- `bun run typecheck`

---

*Phase: 73-release-prep*
*Plan: 01*
