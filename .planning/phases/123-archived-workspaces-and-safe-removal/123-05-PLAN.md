---
phase: 123-archived-workspaces-and-safe-removal
plan: "05"
type: execute
wave: 5
depends_on:
  - "123-04"
files_modified:
  - tests/lib/service/operations.test.ts
  - tests/service/operations.test.ts
  - packages/service/src/policy/core-contract.ts
  - packages/service/src/policy/client.ts
  - packages/service/src/secure/router.ts
  - packages/service/src/main.ts
autonomous: true
requirements:
  - ARCH-02
  - ARCH-04
  - REMOVE-02
  - REMOVE-03
  - REMOVE-05
must_haves:
  truths:
    - "Browser and trusted TUI clients reach the same coordinator through strict stable-ID/revision lifecycle requests and operation.write authorization."
    - "Force-only confirmation is rejected on other mutations, server validation remains authoritative, and destructive requests are never retried by router/client adapters."
    - "Exactly one admission, terminal manager, lifecycle coordinator, operation registry, and aggregate snapshot authority are wired at the service composition root."
  artifacts:
    - path: "packages/service/src/secure/router.ts"
      provides: "Scoped strict lifecycle submission into the shared coordinator"
    - path: "packages/service/src/policy/client.ts"
      provides: "Trusted lifecycle operation observation for the TUI"
    - path: "packages/service/src/main.ts"
      provides: "Single shared lifecycle authority composition"
    - path: "tests/service/operations.test.ts"
      provides: "Router/client/composition lifecycle contract coverage"
  key_links:
    - from: "packages/service/src/secure/router.ts"
      to: "packages/service/src/policy/workspace-lifecycle.ts"
      via: "strict operation.submit lifecycle adapter"
      pattern: "workspace\.(archive|unarchive|remove|force-remove)"
    - from: "packages/service/src/main.ts"
      to: "packages/service/src/policy/workspace-lifecycle-admission.ts"
      via: "one shared admission instance passed to terminal manager and coordinator"
      pattern: "createWorkspaceLifecycleAdmission"
  prohibitions:
    - "[flagged-unverified] Do not duplicate lifecycle sequencing, dirty inspection, exact-name validation, filesystem/Git work, or terminal enumeration in router/client adapters."
    - "[flagged-unverified] Do not automatically retry Remove or Force Remove after conflict, reconnect, or any failure."
    - "[flagged-unverified] Do not weaken operation.write or expose internal cross-principal terminal authority."
---

<objective>
Expose the lifecycle coordinator through existing secure operation and trusted-client seams, then wire one shared runtime authority.

Purpose: Complete transport and composition integration without mixing it into terminal/process or lifecycle state-machine implementation contexts.
Output: Router/client/core-contract adapters, single service composition, and focused integration tests.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/123-archived-workspaces-and-safe-removal/123-CONTEXT.md
@.planning/phases/123-archived-workspaces-and-safe-removal/123-RESEARCH.md
@.planning/phases/123-archived-workspaces-and-safe-removal/123-PATTERNS.md
@.planning/phases/123-archived-workspaces-and-safe-removal/123-02-SUMMARY.md
@.planning/phases/123-archived-workspaces-and-safe-removal/123-03-SUMMARY.md
@.planning/phases/123-archived-workspaces-and-safe-removal/123-04-SUMMARY.md

<interfaces>
Consume from Plans 02-04:
- Strict lifecycle mutation/result/failure schemas are owned by Plan 02, not recreated here.
- Plan 03 supplies admission-aware terminal authority; Plan 04 supplies the coordinator and operation execution adapter.
- Server-side fresh dirty inspection and exact authoritative-name validation remain Plan 04 coordinator responsibilities.

Existing seams:
- `secure/router.ts` owns `operation.write` scope and strict request parsing.
- `policy/client.ts` observes durable operations for trusted thin clients.
- `main.ts` is the sole composition root for shared service authorities.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock lifecycle router, client, and composition boundaries</name>
  <read_first>
    - tests/lib/service/operations.test.ts
    - tests/service/operations.test.ts
    - packages/service/src/policy/core-contract.ts
    - packages/service/src/policy/client.ts
    - packages/service/src/secure/router.ts
    - packages/service/src/main.ts
  </read_first>
  <files>tests/lib/service/operations.test.ts, tests/service/operations.test.ts</files>
  <behavior>
    - All four lifecycle kinds require operation.write and strict stable ID/current expected revision request shapes.
    - confirmation_name is accepted only on force-remove; custom clean/non-dirty force still reaches authoritative coordinator rejection.
    - Same idempotency key/body observes the original operation, different body conflicts, and adapters do not resubmit destructive intent after stale/reconnect.
    - Composition passes the same admission instance to terminal manager and coordinator and one aggregate snapshot authority to lifecycle reconciliation.
  </behavior>
  <action>Extend router/operation integration tests with strict request, scope, client observation, same-key, stale, and no-adapter-replay cases before production wiring. Name one failing test exactly `PHASE123_RED lifecycle router client composition contract`. Use injected coordinator and composition seams so tests prove adapters forward typed intent but do not reimplement dirty or terminal policy. Add one identity assertion that terminal manager and coordinator receive the same admission authority. Keep force-on-clean/non-dirty expected as a coordinator error, not a client-side heuristic. The RED gate must reach the exact behavior sentinel and reject syntax, import, fixture, key-store, or environment failures.</action>
  <acceptance_criteria>
    - Tests distinguish schema/scope rejection from coordinator dirty-only validation.
    - No adapter parses lifecycle error text or manufactures force eligibility.
    - Shared composition identity is executable evidence.
    - The initial focused run is nonzero and includes the exact lifecycle router/client/composition behavior sentinel.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -o pipefail; out=$(GIT_STACKS_KEY_STORE=file ./node_modules/.bin/vitest run tests/lib/service/operations.test.ts tests/service/operations.test.ts 2>&1); status=$?; printf "%s\n" "$out"; test "$status" -ne 0; printf "%s\n" "$out" | rg -q "PHASE123_RED lifecycle router client composition contract"'</automated>
  </verify>
  <done>Wave 0 deterministically locks transport and composition contracts before production wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire strict lifecycle transport and one shared runtime authority</name>
  <read_first>
    - tests/lib/service/operations.test.ts
    - tests/service/operations.test.ts
    - packages/protocol/src/service.ts
    - packages/service/src/policy/workspace-lifecycle.ts
    - packages/service/src/policy/core-contract.ts
    - packages/service/src/policy/client.ts
    - packages/service/src/secure/router.ts
    - packages/service/src/main.ts
  </read_first>
  <files>packages/service/src/policy/core-contract.ts, packages/service/src/policy/client.ts, packages/service/src/secure/router.ts, packages/service/src/main.ts</files>
  <action>Extend the trusted core-state/mutation adapters only as required to carry Plan 02's lifecycle schemas. Route archive/unarchive/remove/force-remove through `operation.submit` and the Plan 04 coordinator under operation.write, preserving stable IDs, expected revisions, force-only confirmation, caller idempotency keys, and durable observation. Do not catch-and-retry destructive operations in router or client code. In `main.ts`, instantiate one target admission authority, inject it into the terminal manager and lifecycle coordinator, and compose the coordinator with the existing operation registry and aggregate snapshot adapter. Keep cross-principal close internal, daemonless CLI removal unchanged, and all Git/filesystem authority in core.</action>
  <acceptance_criteria>
    - Web and TUI use one lifecycle operation contract and authorization boundary.
    - No router/client code can enable force after a terminal, stale, not-found, clean, parse, hook, or generic failure.
    - Composition has exactly one authority instance for admission, terminals, operations, and aggregate reconciliation.
    - Package and daemonless CLI boundaries remain green.
  </acceptance_criteria>
  <verify>
    <automated>GIT_STACKS_KEY_STORE=file ./node_modules/.bin/vitest run tests/lib/service/workspace-lifecycle-operations.test.ts tests/lib/service/operations.test.ts tests/service/operations.test.ts tests/service/web-terminal.test.ts && npm run typecheck && npm run test:deps</automated>
  </verify>
  <done>The shared lifecycle pipeline is securely reachable by thin clients without policy duplication or destructive replay.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated client → secure router | Untrusted lifecycle intent crosses scope, strict-schema, revision, and idempotency checks. |
| Router/client adapters → coordinator | Adapters may transport typed intent but may not own dirty, terminal, or deletion policy. |
| Composition root → shared authorities | Duplicate admission or snapshot instances would break safety and reconciliation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-123-03 | Tampering | lifecycle request/retry adapter | high | mitigate | Stable ID/revision, canonical caller idempotency key, and explicit no-replay tests. |
| T-123-06 | Tampering | strict mutation schema/scope | high | mitigate | Strict union plus operation.write on every lifecycle kind. |
| T-123-09 | Elevation of Privilege | force transport | high | mitigate | Force-only confirmation shape and authoritative Plan 04 dirty/name validation. |
| T-123-12 | Information Disclosure | terminal composition | medium | mitigate | Internal bounded close authority only; no client enumeration method. |
</threat_model>

## Cross-plan symbols this plan produces

- Secure lifecycle `operation.submit` routing and trusted client observation consumed by Plans 06-08.
- One shared lifecycle runtime composition in `packages/service/src/main.ts`.

<verification>
Run operation/router/terminal/coordinator suites, typecheck, and dependency gates. Confirm adapters carry typed results without parsing messages or retrying destructive requests.
</verification>

<success_criteria>
- Shared lifecycle transport and composition portions of ARCH-02, ARCH-04, REMOVE-02, REMOVE-03, and REMOVE-05 are green.
- Thin clients gain no Git/filesystem/terminal lifecycle authority and custom force requests remain server constrained.
</success_criteria>

<output>
Create `.planning/phases/123-archived-workspaces-and-safe-removal/123-05-SUMMARY.md` when done.
</output>
