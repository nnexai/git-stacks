---
phase: 123-archived-workspaces-and-safe-removal
plan: "04"
type: execute
wave: 4
depends_on:
  - "123-01"
  - "123-02"
  - "123-03"
files_modified:
  - tests/lib/service/workspace-lifecycle-operations.test.ts
  - packages/service/src/policy/workspace-lifecycle.ts
  - packages/service/src/policy/operations.ts
  - packages/service/src/snapshot-adapter.ts
autonomous: true
requirements:
  - ARCH-02
  - ARCH-04
  - REMOVE-02
  - REMOVE-03
  - REMOVE-04
  - REMOVE-05
must_haves:
  truths:
    - "The target lease spans execution-time revision validation, confirmed terminal shutdown, core mutation, aggregate reconciliation, and release without blocking unrelated workspaces."
    - "Archive/unarchive converge only for the same stable ID already in the requested state, while stale remove/force-remove never replay and same-key retries cannot target recreated state."
    - "Normal dirty removal stops after typed blockers with terminals_stopped and force eligibility; fresh non-forced inspection is the only path to dirty Force Remove eligibility."
    - "Checking/removing/deleting/reconciling progress is published at the actual core boundaries and success carries the authoritative reconciled revision."
  artifacts:
    - path: "packages/service/src/policy/workspace-lifecycle.ts"
      provides: "Per-workspace revision-bound lifecycle coordinator"
      exports: ["createWorkspaceLifecycleCoordinator"]
    - path: "tests/lib/service/workspace-lifecycle-operations.test.ts"
      provides: "Coordinator ordering, concurrency, idempotency, force, and failure tests"
  key_links:
    - from: "packages/service/src/policy/workspace-lifecycle.ts"
      to: "packages/service/src/policy/workspace-lifecycle-admission.ts"
      via: "target lease held across lifecycle execution"
      pattern: "createWorkspaceLifecycleAdmission|acquire"
    - from: "packages/service/src/policy/workspace-lifecycle.ts"
      to: "packages/service/src/web/terminal-manager.ts"
      via: "confirmed bounded closeWorkspace barrier"
      pattern: "closeWorkspace"
    - from: "packages/service/src/policy/workspace-lifecycle.ts"
      to: "packages/core/src/workspace-archive.ts"
      via: "archive state mutation only after terminal/revision prerequisites"
      pattern: "archiveWorkspace|unarchiveWorkspace"
    - from: "packages/service/src/policy/workspace-lifecycle.ts"
      to: "packages/core/src/workspace-lifecycle.ts"
      via: "typed removal inspection and commit only after terminal/revision prerequisites"
      pattern: "inspectWorkspaceRemoval|commitWorkspaceRemoval"
  prohibitions:
    - "[flagged-unverified] Do not mutate archive YAML, inspect dirtiness, remove worktrees, delete directories, or delete YAML after any unconfirmed terminal exit."
    - "[flagged-unverified] Do not validate revision only at submission time, replay destructive work automatically, or treat a missing target under a new idempotency key as success."
    - "[flagged-unverified] Do not permit force for clean targets or terminal, stale, not-found, parse, hook, or other non-dirty failures."
---

<objective>
Implement the service-owned lifecycle coordinator over the proven terminal barrier and typed core archive/removal boundaries.

Purpose: Make execution-time revision, terminal, dirty-force, idempotency, progress, and reconciliation ordering one testable operation pipeline.
Output: Lifecycle coordinator, operation integration, snapshot adapter support, and adversarial coordinator tests.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/123-archived-workspaces-and-safe-removal/123-CONTEXT.md
@.planning/phases/123-archived-workspaces-and-safe-removal/123-RESEARCH.md
@.planning/phases/123-archived-workspaces-and-safe-removal/123-PATTERNS.md
@.planning/phases/123-archived-workspaces-and-safe-removal/123-01-SUMMARY.md
@.planning/phases/123-archived-workspaces-and-safe-removal/123-02-SUMMARY.md
@.planning/phases/123-archived-workspaces-and-safe-removal/123-03-SUMMARY.md

<interfaces>
Consume from Plans 01-03:
- Core exports idempotent archive/unarchive plus typed `inspectWorkspaceRemoval` / `commitWorkspaceRemoval` and real-work callbacks.
- Protocol/catalog schemas carry stable ID, expected revision, force-only confirmation, bounded lifecycle details, and one aggregate revision.
- Admission authority and `WebTerminalManager.closeWorkspace()` provide target-scoped quiesce and confirmed shutdown.

Existing seam:
- `OperationRegistry` owns canonical request hashing, durable reservations, stage publication, and same-key behavior.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the lifecycle coordinator failure-order harness</name>
  <read_first>
    - tests/lib/service/operations.test.ts
    - tests/service/web-terminal.test.ts
    - packages/service/src/policy/operations.ts
    - packages/service/src/snapshot-adapter.ts
    - packages/core/src/workspace-lifecycle.ts
  </read_first>
  <files>tests/lib/service/workspace-lifecycle-operations.test.ts</files>
  <behavior>
    - Execution-time revision is checked under the target lease; stale archive converges only for the same already-archived ID while stale remove/force conflicts.
    - Confirmed terminal close precedes archive/dirty inspection; cleanup failure causes zero core mutation.
    - Same idempotency key/body returns the original operation; same key/different body conflicts; a new key after deletion is not_found and cannot target recreated state.
    - Normal dirty removal returns every blocker with terminals_stopped and force_allowed; clean or any non-dirty failure rejects force.
    - Exact progress order follows stopping terminals, checking worktrees, core removing/deleting callbacks, and reconciled revision before success.
  </behavior>
  <action>Create the coordinator test using deferred promises and injected clock, catalog, operation registry, admission, terminal, core, and progress dependencies. Name one failing test exactly `PHASE123_RED service lifecycle coordinator contract`. Assert exact call order and zero downstream calls after every terminal/revision/not-found/parse/hook/non-dirty failure. Cover stable archive timestamp convergence, same-key hashing, recreated targets, fresh dirty inspection, direct force against clean state, and unrelated target concurrency. The RED gate must reach the sentinel.</action>
  <acceptance_criteria>
    - Every failure test asserts zero calls to forbidden downstream core mutation.
    - Force eligibility is derived only from the current typed dirty inspection.
    - Lifecycle phase events correspond to the injected actual boundary calls.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -o pipefail; out=$(GIT_STACKS_KEY_STORE=file ./node_modules/.bin/vitest run tests/lib/service/workspace-lifecycle-operations.test.ts 2>&1); status=$?; printf "%s\n" "$out"; test "$status" -ne 0; printf "%s\n" "$out" | rg -q "PHASE123_RED service lifecycle coordinator contract"'</automated>
  </verify>
  <done>Wave 0 specifies the complete coordinator state machine and safety order without router or composition-root noise.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement revision-bound lifecycle coordination and honest progress</name>
  <read_first>
    - tests/lib/service/workspace-lifecycle-operations.test.ts
    - packages/service/src/policy/workspace-lifecycle-admission.ts
    - packages/service/src/policy/operations.ts
    - packages/service/src/snapshot-adapter.ts
    - packages/core/src/workspace-archive.ts
    - packages/core/src/workspace-lifecycle.ts
  </read_first>
  <files>packages/service/src/policy/workspace-lifecycle.ts, packages/service/src/policy/operations.ts, packages/service/src/snapshot-adapter.ts, tests/lib/service/workspace-lifecycle-operations.test.ts</files>
  <action>Create `createWorkspaceLifecycleCoordinator(options)` and route all four lifecycle execution kinds through it. Acquire the stable target lease before repeating aggregate revision/ID resolution; hold it through terminal shutdown, core mutation, authoritative catalog rebuild/result publication, and release in finally. Archive/remove/force publish stopping_terminals and abort on cleanup_failed. Normal Remove publishes checking_worktrees immediately before non-forced inspection. Force independently repeats the same inspection, rejects clean as force_not_eligible, propagates every non-dirty failure, verifies exact current authoritative name, and commits with allow_dirty only from the current dirty variant. Map core callbacks to removing_worktrees and deleting_workspace_files at the instant those steps begin; publish reconciling_state before rebuilding the aggregate catalog and store its revision. Preserve canonical request hashing, same-key results, not_found for a new key after deletion, convergence only for archive/unarchive same-ID desired state, and unrelated-target concurrency.</action>
  <acceptance_criteria>
    - Dirty failure is the only result enabling force and includes all blockers plus truthful terminal status.
    - Clean/custom-client force and every non-dirty failure are rejected without commit.
    - Progress and success revision reflect actual core and snapshot boundaries.
    - Core remains daemonless and the coordinator imports core, never the reverse.
  </acceptance_criteria>
  <verify>
    <automated>GIT_STACKS_KEY_STORE=file ./node_modules/.bin/vitest run tests/lib/service/workspace-lifecycle-operations.test.ts tests/service/web-terminal.test.ts && npm run typecheck && npm run test:deps</automated>
  </verify>
  <done>The lifecycle state machine is safe and independently green before transport/router exposure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Coordinator → terminal authority | Mutation cannot proceed without confirmed target shutdown. |
| Coordinator → core mutation | Current revision, exact target, and dirty-only force eligibility must precede filesystem/Git authority. |
| Coordinator → operation registry/catalog | Retry identity and progress must remain truthful across reconciliation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-123-02 | Tampering / Denial of Service | target lifecycle lease | high | mitigate | Acquire before execution-time validation and hold through reconciliation with unrelated-target tests. |
| T-123-03 | Tampering | revision/idempotency execution | high | mitigate | Stable ID, aggregate revision recheck, canonical hashing, no replay, and recreated-target tests. |
| T-123-07 | Tampering / Data Loss | terminal prerequisite | high | mitigate | Confirmed Plan 03 close result gates every core mutation. |
| T-123-09 | Elevation of Privilege | dirty force eligibility | high | mitigate | Fresh non-forced inspection, exact current name, and rejection of clean/non-dirty results. |
</threat_model>

## Cross-plan symbols this plan produces

- `createWorkspaceLifecycleCoordinator(options)` in `packages/service/src/policy/workspace-lifecycle.ts`; Plan 05 wires it and Plans 06-07 consume it through the client contract.
- Typed coordinator failures/results carrying bounded blockers, `terminals_stopped`, `force_allowed`, `force_not_eligible`, and reconciled revision.

<verification>
Run coordinator and terminal focused suites, typecheck, and dependency gates. Inspect exact recorded order from lease acquisition through reconciliation/release.
</verification>

<success_criteria>
- ARCH-02, ARCH-04 terminal sequencing, REMOVE-02, REMOVE-03, REMOVE-04, and service-side REMOVE-05 are green at the coordinator boundary.
- Stale/replay, force-bypass, terminal-TOCTOU, and false-progress threats are executable tests, not prose assumptions.
</success_criteria>

<output>
Create `.planning/phases/123-archived-workspaces-and-safe-removal/123-04-SUMMARY.md` when done.
</output>
