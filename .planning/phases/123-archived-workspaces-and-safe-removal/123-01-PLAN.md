---
phase: 123-archived-workspaces-and-safe-removal
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/lib/workspace-archive.test.ts
  - tests/lib/workspace-pins.test.ts
  - tests/lib/workspace-lifecycle.test.ts
  - tests/commands/workspace-lifecycle.test.ts
  - tests/commands/workspace-destructive-safety.test.ts
  - packages/core/src/config.ts
  - packages/core/src/workspace-archive.ts
  - packages/core/src/workspace-lifecycle.ts
  - packages/core/src/workspace-pins.ts
autonomous: true
requirements:
  - ARCH-01
  - ARCH-04
  - REMOVE-03
  - REMOVE-04
must_haves:
  truths:
    - "Omitted archive fields load as active, while archived and archived_at are a validated pair and repeated archive preserves the first timestamp."
    - "Archive/unarchive preserves repositories, worktrees, files, notes, configuration, pin state, and priority; Phase 123 intentionally stops rather than preserves terminals."
    - "Dirty removal reports typed blocker names, performs zero mutation, and force bypasses only that dirty guard; resolved-definition deletion removes the intended YAML even when its filename drifted."
    - "Core exposes an explicit inspect/commit removal boundary whose typed phases let the service publish checking, worktree-removal, and workspace-file deletion progress in the real call order."
  artifacts:
    - path: "packages/core/src/workspace-archive.ts"
      provides: "Atomic idempotent archive and unarchive domain transitions"
      exports: ["archiveWorkspace", "unarchiveWorkspace"]
    - path: "tests/lib/workspace-archive.test.ts"
      provides: "Archive persistence, stability, preservation, and compatibility coverage"
    - path: "tests/lib/workspace-lifecycle.test.ts"
      provides: "Direct typed dirty outcome, zero-mutation, force-boundary, and staged-removal coverage"
  key_links:
    - from: "packages/core/src/workspace-archive.ts"
      to: "packages/core/src/config.ts"
      via: "updateWorkspace leased atomic read-modify-write"
      pattern: "updateWorkspace"
    - from: "packages/core/src/workspace-lifecycle.ts"
      to: "packages/core/src/config.ts"
      via: "resolved backing workspace definition deletion"
      pattern: "workspaceFilePath|deleteWorkspace"
    - from: "packages/core/src/workspace-lifecycle.ts"
      to: "packages/service/src/policy/workspace-lifecycle.ts"
      via: "typed inspect/commit API plus actual-work callback phases consumed by Plan 04"
      pattern: "inspectWorkspaceRemoval|commitWorkspaceRemoval"
  prohibitions:
    - "[flagged-unverified] Do not preserve or recreate service terminals during archive or unarchive; the locked Phase 123 context supersedes older terminal-preservation text."
    - "[flagged-unverified] Do not build normal repository, command, note, path, pin, action, or detail projections for archived definitions."
    - "[flagged-unverified] Do not erase archived pin metadata when replacing the active pin set."
    - "[flagged-unverified] Do not introduce a second YAML writer, a global lock, client-side Git/filesystem authority, or a service dependency for daemonless CLI removal."
---

<objective>
Establish the core-only archive/removal authority on which the service lifecycle coordinator depends.

Purpose: Make archival backward-compatible and idempotent, and make dirty preflight plus staged deletion explicit without coupling core to service or clients.
Output: Core archive APIs, typed inspect/commit removal contracts, resolved deletion behavior, and focused core tests.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/123-archived-workspaces-and-safe-removal/123-CONTEXT.md
@.planning/phases/123-archived-workspaces-and-safe-removal/123-RESEARCH.md
@.planning/phases/123-archived-workspaces-and-safe-removal/123-PATTERNS.md
@.planning/phases/123-archived-workspaces-and-safe-removal/123-VALIDATION.md

<interfaces>
Existing authorities to preserve:
- `updateWorkspace(name, intent)` in `packages/core/src/config.ts` owns leased, parsed, atomic YAML mutation.
- `WorkspaceSchema` is the persisted boundary; archive fields must be paired and omitted fields must remain valid.
- `workspacePriorityOrder(left, right)` in `packages/client/src/presentation.ts` is the shared ordering seam.
- `createSnapshotBuilder()` in `packages/service/src/policy/snapshot.ts` owns aggregate revision generation.
- `CoreStateSchema` in `packages/service/src/policy/core-contract.ts` is the trusted TUI/service catalog contract.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock core archive, pin, staged removal, and deletion edge contracts</name>
  <read_first>
    - packages/core/src/config.ts
    - packages/core/src/workspace-lifecycle.ts
    - packages/core/src/workspace-pins.ts
    - tests/lib/workspace-pins.test.ts
    - tests/lib/workspace-lifecycle.test.ts
    - tests/commands/workspace-lifecycle.test.ts
  </read_first>
  <files>tests/lib/workspace-archive.test.ts, tests/lib/workspace-pins.test.ts, tests/lib/workspace-lifecycle.test.ts, tests/commands/workspace-lifecycle.test.ts, tests/commands/workspace-destructive-safety.test.ts</files>
  <behavior>
    - Old YAML with neither archive field parses as active; either field alone and persisted archived false are rejected.
    - First archive writes both fields, repeated archive keeps the same timestamp, and unarchive removes both fields while preserving every unrelated field.
    - Archived pinned definitions survive active-pin replacement and regain the stored pin on unarchive.
    - `inspectWorkspaceRemoval` returns every dirty blocker and causes zero filesystem/YAML/worktree mutation; clean inspection returns a bounded commit plan.
    - `commitWorkspaceRemoval` bypasses only the already-inspected dirty guard when explicitly authorized and reports `removing_worktrees` immediately before managed worktree calls and `deleting_workspace_files` immediately before directory/YAML deletion.
    - Removal deletes the resolved backing YAML under filename drift and leaves similarly named/unrelated definitions untouched.
  </behavior>
  <action>Create the missing Wave 0 archive suite and extend the existing pin and lifecycle fixtures before production edits. Name one failing test exactly `PHASE123_RED core archive removal contract` so the RED gate can distinguish the intended missing behavior from import, syntax, fixture, or environment failures. Use injected clocks and isolated config/Git fixtures. Include paired-field validation, timestamp stability, archived-pin preservation, direct typed dirty assertions in `tests/lib/workspace-lifecycle.test.ts`, every blocker name, zero mutation on normal dirty removal, force bypassing only the dirty guard, phase-to-call ordering, filename drift, and unrelated-target assertions. Do not weaken assertions or leave skipped/todo cases.</action>
  <acceptance_criteria>
    - Every edge named in the behavior block has an executable assertion in the listed files, including direct lifecycle-library assertions rather than command-only inference.
    - The initial focused run is nonzero and includes the exact behavior sentinel, proving the harness loaded and reached the intended missing contract.
    - No test expects archived terminals to survive or unarchive to recreate terminal IDs.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -o pipefail; out=$(GIT_STACKS_KEY_STORE=file ./node_modules/.bin/vitest run tests/lib/workspace-archive.test.ts tests/lib/workspace-pins.test.ts tests/lib/workspace-lifecycle.test.ts tests/commands/workspace-lifecycle.test.ts tests/commands/workspace-destructive-safety.test.ts 2>&1); status=$?; printf "%s\n" "$out"; test "$status" -ne 0; printf "%s\n" "$out" | rg -q "PHASE123_RED core archive removal contract"'</automated>
  </verify>
  <done>Wave 0 tests deterministically express archive compatibility, stability, partitioning, revision, pin, and deletion safety before implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement atomic archive transitions and typed safe removal outcomes</name>
  <read_first>
    - tests/lib/workspace-archive.test.ts
    - tests/lib/config.test.ts
    - tests/lib/workspace-pins.test.ts
    - tests/commands/workspace-lifecycle.test.ts
    - packages/core/src/config.ts
    - packages/core/src/workspace-lifecycle.ts
    - packages/core/src/workspace-pins.ts
  </read_first>
  <files>packages/core/src/config.ts, packages/core/src/workspace-archive.ts, packages/core/src/workspace-lifecycle.ts, packages/core/src/workspace-pins.ts</files>
  <action>Add paired optional `archived: true` and offset-aware `archived_at` fields to `WorkspaceSchema`, preserving old YAML compatibility. Implement `archiveWorkspace(name, { clock? })` and `unarchiveWorkspace(name)` as idempotent `updateWorkspace` transforms. Restrict active pin replacement so archived definitions keep stored pin/priority metadata. In `workspace-lifecycle.ts`, export `WorkspaceRemovalInspection`, `WorkspaceRemovalPlan`, `CoreWorkspaceRemovalPhase`, `inspectWorkspaceRemoval(name)`, and `commitWorkspaceRemoval(plan, options)`; inspection performs the current dirty check without mutation, returns an opaque resolved plan in both clean and dirty variants plus every blocker in the dirty variant, and commit accepts an optional `onPhase(phase)` callback invoked immediately before actual worktree removal and before directory/resolved-YAML deletion. Preserve the daemonless `removeWorkspace` facade by composing these exports, and ensure `force` bypasses only dirty protection, not parse, hook, target-resolution, or commit failures. Do not add service imports.</action>
  <acceptance_criteria>
    - ARCH-01 and the non-terminal portion of ARCH-04 pass for old, active, archived, repeated, and unarchived definitions.
    - Dirty normal removal returns every blocking repository in typed data and performs no destructive mutation, directly asserted in `tests/lib/workspace-lifecycle.test.ts`.
    - Force bypasses only the dirty guard through the existing core option; confirmation, dirty-only eligibility, terminal authority, and revision binding remain service concerns.
    - The typed phase callback matches actual call order and cannot claim a destructive phase before its corresponding core step begins.
    - Filename-drift removal deletes only the resolved target definition plus its managed resources.
  </acceptance_criteria>
  <verify>
    <automated>GIT_STACKS_KEY_STORE=file ./node_modules/.bin/vitest run tests/lib/workspace-archive.test.ts tests/lib/workspace-pins.test.ts tests/lib/workspace-lifecycle.test.ts tests/commands/workspace-lifecycle.test.ts tests/commands/workspace-destructive-safety.test.ts && npm run typecheck</automated>
  </verify>
  <done>Core owns canonical archive state and typed removal safety without terminal/client coupling, and all focused core tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML → core schema | User-editable workspace definitions enter authoritative state. |
| Core → service catalog | Sensitive full definitions are reduced to active projections or minimal archived summaries. |
| Lifecycle intent → filesystem/Git | A stable logical target is resolved to authoritative backing paths and managed worktrees. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-123-01 | Tampering | `WorkspaceSchema` / `workspace-archive.ts` | high | mitigate | Enforce paired canonical fields, atomic leased RMW, stable timestamp, and isolated compatibility tests. |
| T-123-04 | Information Disclosure | archived catalog schemas/projection input | high | mitigate | Strict minimal summary schema and partition before normal projection; negative path/secret/detail tests. |
| T-123-08 | Tampering | resolved YAML deletion | high | mitigate | Resolve the backing definition from validated content and assert unrelated definitions/resources remain untouched. |
| T-123-11 | Denial of Service | archive catalog generation | medium | mitigate | Retain existing bounded definition capacity, skip expensive active projection for archived definitions, and use deterministic sorting/digesting. |
</threat_model>

## Cross-plan symbols this plan produces

- `archiveWorkspace(name: string, options?: { clock?: () => Date }): Workspace` and `unarchiveWorkspace(name: string): Workspace` in `packages/core/src/workspace-archive.ts`; Plan 04 consumes them.
- `WorkspaceRemovalInspection`, a union of `{ ok: true; plan: WorkspaceRemovalPlan }`, `{ ok: false; code: "workspace_dirty"; blocking_repositories: string[]; plan: WorkspaceRemovalPlan }`, and bounded non-dirty failures without a plan; Plans 01 and 04 consume it.
- `WorkspaceRemovalPlan`, an opaque target-resolved preparation returned by `inspectWorkspaceRemoval(name: string): Promise<WorkspaceRemovalInspection>` and accepted only by `commitWorkspaceRemoval`.
- `CoreWorkspaceRemovalPhase = "removing_worktrees" | "deleting_workspace_files"` and `commitWorkspaceRemoval(plan, options?: { allow_dirty?: boolean; onPhase?: (phase: CoreWorkspaceRemovalPhase) => void | Promise<void> }): Promise<WorkspaceRemovalResult>`; Plan 04 maps these exact callbacks to operation progress.

<verification>
Run the Plan 01 focused suites and `npm run typecheck`. Confirm direct core tests assert every dirty blocker, zero mutation, force bypass only of the dirty guard, actual phase/call ordering, and resolved target deletion.
</verification>

<success_criteria>
- ARCH-01, ARCH-04 (with the locked terminal override), REMOVE-03, and REMOVE-04 have executable green core coverage.
- Archive mutation, typed removal preparation/commit, and filename-drift safety are authoritative core behavior.
- No package is added and no package boundary, global lock, or daemonless CLI behavior regresses.
</success_criteria>

<output>
Create `.planning/phases/123-archived-workspaces-and-safe-removal/123-01-SUMMARY.md` when done.
</output>
