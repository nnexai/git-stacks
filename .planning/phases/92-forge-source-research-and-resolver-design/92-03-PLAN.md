---
phase: 92
plan: 03
type: execute
wave: 2
depends_on:
  - 92-02
files_modified:
  - src/lib/integrations/forge-source.ts
  - tests/lib/integrations/forge-source.test.ts
autonomous: true
requirements:
  - FSRC-02
  - FSRC-03
  - FSRC-08
must_haves:
  truths:
    - "D-07: Resolver success output includes forge id, instance/base URL, repo identity/path, change type, change number, source/head branch/ref, target/base branch, web URL, matched registry repo, and typed confidence/failure info."
    - "D-08: Provider terminology is preserved: GitLab uses `mr`; GitHub and Gitea use `pr`."
    - "D-09: Include both source/head branch/ref and target/base branch."
    - "D-10: Include normalized source metadata for later workspace YAML persistence."
    - "D-11: Do not include auto-label suggestions in resolver output."
    - "D-12: Use typed result union failures such as `unsupported_forge`, `url_parse_failed`, `repo_not_matched`, `ambiguous_repo`, `cli_unavailable`, and `auth_required`."
    - "D-15: Multiple template repo matches fail as ambiguous."
    - "D-16: Forge source checkout applies only to worktree repos."
    - "D-17: Selected template missing the source repo fails clearly."
    - "D-23: Resolver identifies fork/source project metadata where available."
  key_links:
    - from: "src/lib/integrations/forge-utils.ts"
      pattern: "ForgeRepoResolutionError"
    - from: "src/lib/integrations/forge-utils.ts"
      pattern: "resolveForgeRepo"
    - from: ".planning/phases/92-forge-source-research-and-resolver-design/92-RESEARCH.md"
      pattern: "unsupported_forge"
    - from: ".planning/phases/92-forge-source-research-and-resolver-design/92-RESEARCH.md"
      pattern: "metadataForWorkspace"
  artifacts:
    - path: "src/lib/integrations/forge-source.ts"
      provides: "Pure forge source URL parser and resolver contract types"
      contains: "ForgeSourceResolution"
    - path: "tests/lib/integrations/forge-source.test.ts"
      provides: "Parser and contract regression coverage"
      contains: "merge_requests"
---

<objective>
Create the pure forge source contract and URL parsing seam that Phase 93 can call before metadata lookup and workspace creation, with typed success/failure shapes and self-hosted base URL preservation.
</objective>

<execution_context>
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/git-stacks/.codex/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add parser and contract tests first</name>
  <read_first>
    - tests/lib/integrations/forge-utils.test.ts
    - src/lib/integrations/forge-utils.ts
    - src/lib/config.ts
    - .planning/phases/92-forge-source-research-and-resolver-design/92-CONTEXT.md
    - .planning/phases/92-forge-source-research-and-resolver-design/92-RESEARCH.md
  </read_first>
  <files>tests/lib/integrations/forge-source.test.ts</files>
  <action>Create `tests/lib/integrations/forge-source.test.ts`. Add tests for `parseForgeSourceUrl()` covering `https://gitlab.example.com/group/subgroup/api/-/merge_requests/42`, `https://github.com/org/api/pull/17`, and `https://git.example.test/org/api/pulls/9`. Assert GitLab returns `forge: "gitlab"`, `changeType: "mr"`, `baseUrl: "https://gitlab.example.com"`, `repoPath: "group/subgroup/api"`, and `changeNumber: 42`. Assert GitHub returns `changeType: "pr"` and `repoPath: "org/api"`. Assert Gitea returns `changeType: "pr"` and preserves `baseUrl`. Add tests for unsupported URLs and malformed numbers returning `ok: false` with `error: "url_parse_failed"` or `error: "unsupported_forge"`. Add a source-contract test that an example `ForgeSourceResolution` object has `source.branch`, `target.branch`, `matchedRepo.registryName`, `metadataForWorkspace`, and no `labels` key.</action>
  <acceptance_criteria>
    - `tests/lib/integrations/forge-source.test.ts` contains a GitLab self-hosted MR URL test using `/-/merge_requests/42`.
    - `tests/lib/integrations/forge-source.test.ts` contains a GitHub PR URL test using `/pull/17`.
    - `tests/lib/integrations/forge-source.test.ts` contains a Gitea PR URL test using `/pulls/9`.
    - Tests assert `labels` is not present in the example resolution object.
    - Tests assert malformed or unsupported URLs return typed failures, not thrown exceptions.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/lib/integrations/forge-source.test.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Implement pure source URL parser and typed resolver contract</name>
  <read_first>
    - src/lib/integrations/forge-utils.ts
    - src/lib/config.ts
    - tests/lib/integrations/forge-source.test.ts
    - .planning/phases/92-forge-source-research-and-resolver-design/92-PATTERNS.md
  </read_first>
  <files>src/lib/integrations/forge-source.ts</files>
  <action>Create `src/lib/integrations/forge-source.ts`. Export `ForgeSourceChangeType = "mr" | "pr"`, `ForgeSourceParsed`, `ForgeSourceResolution`, `ForgeSourceResolutionError`, and `parseForgeSourceUrl(raw: string): ForgeSourceParseResult`. Use `new URL(raw)` for parsing. Implement GitLab path parsing for `/-/merge_requests/<number>` with nested repo paths before that marker. Implement GitHub parsing for `/owner/repo/pull/<number>`. Implement Gitea parsing for `/owner/repo/pulls/<number>`. Preserve `baseUrl` as `${url.protocol}//${url.host}`. Return typed failures instead of throwing. Include fields for `source.branch`, `source.ref`, `target.branch`, `matchedRepo.registryName`, `matchedRepo.templateRepoName`, `matchedRepo.workspaceRepoMode`, `metadataForWorkspace`, and `confidence`. Do not include label suggestion fields.</action>
  <acceptance_criteria>
    - `src/lib/integrations/forge-source.ts` exports `parseForgeSourceUrl`.
    - `src/lib/integrations/forge-source.ts` exports `ForgeSourceResolutionError` with `unsupported_forge`, `url_parse_failed`, `repo_not_matched`, `ambiguous_repo`, `template_repo_missing`, `not_worktree_mode`, `cli_unavailable`, `auth_required`, `metadata_unavailable`, and `branch_conflict`.
    - `parseForgeSourceUrl()` uses `URL` parsing.
    - GitLab parser supports nested repo paths before `/-/merge_requests/`.
    - No exported resolver type includes `labels` or `autoLabel`.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/lib/integrations/forge-source.test.ts && bun run typecheck</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User URL -> parsed source metadata | User-controlled source URLs become forge, repo, and change identifiers. |
| Parsed source -> future checkout | Parsed metadata will later decide which ref is fetched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-05 | Spoofing | URL parser | mitigate | Preserve base URL and fail unsupported/malformed URLs with typed failures. |
| T-92-06 | Tampering | Resolver output | mitigate | Exclude label suggestions and require explicit source/target branch/ref fields. |
</threat_model>

<verification>
Required checks:
- `bun test tests/lib/integrations/forge-source.test.ts`
- `bun run typecheck`
</verification>

<success_criteria>
- GitLab, GitHub, and Gitea source URLs parse into normalized contract data.
- Resolver contract contains source/head and target/base placeholders needed by Phase 93.
- Failures are typed and user-safe.
</success_criteria>
