---
phase: 69-extract-workspace-env-ts-and-workspace-lifecycle-ts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/workspace-env.ts
  - src/lib/workspace-lifecycle.ts
  - src/lib/workspace-ops.ts
autonomous: true
requirements:
  - EXTR-02
  - EXTR-03
  - EXTR-09

must_haves:
  truths:
    - "`src/lib/workspace-env.ts` exists and exports `mergeEnv`, `buildBaseEnv`, `buildRepoEnv`, `buildWorkspaceEnv`, `writeEnvFiles`, and the private `resolveWorkspaceEnvVars()` helper"
    - "`src/lib/workspace-lifecycle.ts` exists and exports `closeWorkspace`, `cleanWorkspace`, `removeWorkspace`, and `mergeWorkspace`, with `_executeClose` and `_executeClean` kept private in the same file"
    - "`src/lib/workspace-ops.ts` remains the public facade and re-exports the moved env and lifecycle symbols without changing caller import sites"
    - "`removeWorkspace()` still cascades through `cleanWorkspace()` and `closeWorkspace()` in that order"
    - "`bun run test` passes after the extraction"
  artifacts:
    - path: "src/lib/workspace-env.ts"
      provides: "Env extraction module"
      contains: "export async function buildWorkspaceEnv("
    - path: "src/lib/workspace-lifecycle.ts"
      provides: "Lifecycle extraction module"
      contains: "export async function removeWorkspace("
    - path: "src/lib/workspace-ops.ts"
      provides: "Facade re-export surface"
      contains: "export"
  key_links:
    - from: "src/lib/workspace-ops.ts"
      to: "src/lib/workspace-env.ts"
      via: "Facade import/re-export for env helpers and openWorkspace env assembly"
      pattern: "workspace-env"
    - from: "src/lib/workspace-lifecycle.ts"
      to: "src/lib/workspace-env.ts"
      via: "Direct env helper imports to avoid circular dependency"
      pattern: "buildBaseEnv"
---

<objective>
Extract the env assembly helpers and lifecycle cascade operations out of `src/lib/workspace-ops.ts` into `src/lib/workspace-env.ts` and `src/lib/workspace-lifecycle.ts`, while preserving the existing `workspace-ops.ts` facade, cascade ordering, and passing test suite.

Purpose: isolate the two most depended-on domain modules first so later extraction phases can split the remaining git/status/yaml responsibilities without breaking the public API.
Output: new `workspace-env.ts` and `workspace-lifecycle.ts` modules, a facade-only `workspace-ops.ts`, and a green test suite.
</objective>

<execution_context>
@/home/nnex/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/69-extract-workspace-env-ts-and-workspace-lifecycle-ts/69-CONTEXT.md
@.planning/phases/69-extract-workspace-env-ts-and-workspace-lifecycle-ts/69-RESEARCH.md
@src/lib/workspace-ops.ts
@tests/helpers.ts
@tests/lib/workspace-ops.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract workspace-env.ts and route openWorkspace through it</name>
  <files>src/lib/workspace-env.ts, src/lib/workspace-ops.ts</files>
  <read_first>src/lib/workspace-ops.ts, src/lib/config.ts, src/lib/secrets.ts, src/lib/paths.ts</read_first>
  <action>Create `src/lib/workspace-env.ts` by moving `mergeEnv`, `buildBaseEnv`, `buildRepoEnv`, `buildWorkspaceEnv`, `writeEnvFiles`, and the private `resolveWorkspaceEnvVars()` helper out of `src/lib/workspace-ops.ts` without changing their signatures or runtime behavior. Preserve the current env semantics exactly: ports still stringify into env vars, secret refs still resolve or blank with warnings when `skipSecrets` is set, and `writeEnvFiles()` still rejects path escapes and symlink targets. Then update `src/lib/workspace-ops.ts` so `openWorkspace()` imports the env helpers from the new module and uses `buildWorkspaceEnv()` as the single env-assembly path instead of keeping local secret-resolution logic.</action>
  <verify>bun test tests/lib/workspace-ops.test.ts</verify>
  <acceptance_criteria>
    - `src/lib/workspace-env.ts` contains `export function mergeEnv(` and `export async function buildWorkspaceEnv(`
    - `src/lib/workspace-env.ts` contains `async function resolveWorkspaceEnvVars(`
    - `src/lib/workspace-ops.ts` no longer defines `mergeEnv`, `buildBaseEnv`, `buildRepoEnv`, `buildWorkspaceEnv`, or `writeEnvFiles`
    - `src/lib/workspace-ops.ts` imports the env helpers from `src/lib/workspace-env.ts`
    - `tests/lib/workspace-ops.test.ts` still passes its env and `openWorkspace` coverage
  </acceptance_criteria>
  <done>The env module is isolated and `openWorkspace()` resolves env through the new module without changing observable behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Extract workspace-lifecycle.ts and keep the facade stable</name>
  <files>src/lib/workspace-lifecycle.ts, src/lib/workspace-ops.ts</files>
  <read_first>src/lib/workspace-ops.ts, src/lib/lifecycle.ts, src/lib/integrations/runner.ts, src/lib/workspace-env.ts</read_first>
  <action>Create `src/lib/workspace-lifecycle.ts` by moving `closeWorkspace`, `cleanWorkspace`, `removeWorkspace`, `mergeWorkspace`, `_executeClose`, and `_executeClean` out of `src/lib/workspace-ops.ts`. Keep the cascade order exactly as implemented now: `removeWorkspace()` must still call `_executeClean()`, `_executeClean()` must still call `_executeClose()`, and `GS_TRIGGERED_BY` must still flow as `"close"`, `"clean"`, and `"remove"` through the existing hook paths. Import env helpers directly from `src/lib/workspace-env.ts` inside the lifecycle module, then make `src/lib/workspace-ops.ts` re-export the moved lifecycle functions so command and TUI import sites remain unchanged.</action>
  <verify>bun test tests/lib/workspace-ops.test.ts</verify>
  <acceptance_criteria>
    - `src/lib/workspace-lifecycle.ts` contains `export async function closeWorkspace(`
    - `src/lib/workspace-lifecycle.ts` contains `async function _executeClose(`
    - `src/lib/workspace-lifecycle.ts` contains `async function _executeClean(`
    - `src/lib/workspace-ops.ts` re-exports `closeWorkspace`, `cleanWorkspace`, `removeWorkspace`, and `mergeWorkspace`
    - `tests/lib/workspace-ops.test.ts` still passes the cascade-order and `GS_TRIGGERED_BY` checks
  </acceptance_criteria>
  <done>The lifecycle cascade lives in its own module and the public facade keeps the same named exports and cascade semantics.</done>
</task>

</tasks>

<verification>
Before declaring the phase planned:
- `bun run test`
- `test -f src/lib/workspace-env.ts`
- `test -f src/lib/workspace-lifecycle.ts`
- `grep -q "export async function buildWorkspaceEnv(" src/lib/workspace-env.ts`
- `grep -q "export async function removeWorkspace(" src/lib/workspace-lifecycle.ts`
</verification>

<success_criteria>

- `69-RESEARCH.md` and `69-01-PLAN.md` exist in the phase directory
- The plan covers EXTR-02, EXTR-03, and EXTR-09
- `workspace-ops.ts` stays the public facade for callers
- The new env and lifecycle modules are isolated without breaking the existing test suite

</success_criteria>

<output>
After completion, create `.planning/phases/69-extract-workspace-env-ts-and-workspace-lifecycle-ts/69-01-SUMMARY.md`
</output>
