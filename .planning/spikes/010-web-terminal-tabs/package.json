{
  "name": "git-stacks-web-terminal-tabs-spike",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build client.ts --outdir public --target browser",
    "start": "bun run build && bun server.ts",
    "check": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "latest",
    "typescript": "^6.0.2"
  }
}
